# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
dist/

# env
.env
.env*.local
!.env.example

# payload (root-level upload directory only — do not match src/collections/Media)
/media/

# migration ETL artifacts (Phase 4) — regenerable dumps/remap-table of real
# production content, not source code
scripts/migrate/data/

# content-freeze snapshots (Phase 6) — regenerable dumps of real production
# content (ids/updatedAt), not source code
.planning/phases/06-deploy-cutover/freeze-snapshots/

# content-humanization full-text snapshots (Phase 29, VOICE-04) — regenerable
# dumps of real production content (full field values), not source code
.planning/phases/29-content-humanization-safety-net/content-snapshots/

# Phase 10 fixture-cleanup cross-script state scratch file — regenerable,
# always deleted by scripts/cleanup-phase10-eeat-fixtures.ts, not source code
scripts/.phase10-fixture-state.json

# Phase 10.6-03 mobile viewport verification screenshots — regenerable
# evidence produced by scripts/verify-mobile-viewport.mjs, not source code
scripts/.mobile-verify-screenshots/

# Phase 11-03 Lighthouse — downloaded Chrome-for-Testing binary, not source code
.lighthouse-chrome/

# Phase 31 posts-rewrite resumable checkpoint logs (post id -> done) — one per
# Wave 2 batch plan, regenerable execution-state scratch files, not source code
.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/posts-progress-batch-*.json

# misc
.DS_Store
*.pem
.vercel

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# Python bytecode
__pycache__/
*.pyc
