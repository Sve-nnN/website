# Neon UNPOOLED/direct connection string — pooled/-pooler string breaks payload migrate
DATABASE_URI=postgresql://user:password@ep-example-000000.us-east-2.aws.neon.tech/dbname?sslmode=require

# high-entropy random string, generate via `openssl rand -base64 32`
PAYLOAD_SECRET=REPLACE_WITH_GENERATED_SECRET

RESEND_API_KEY=re_REPLACE_WITH_RESEND_API_KEY
RESEND_FROM_EMAIL=no-reply@example.com
