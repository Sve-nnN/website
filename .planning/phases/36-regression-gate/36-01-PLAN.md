# Phase 36 Plan 01: Regression Gate

**Requirements:** REG-02
**Type:** Measurement only — no code/content changes (unless gap closure is required)

## Task 1: Lighthouse mobile re-measurement (production build)

1. `npx next build`
2. `PORT=3042 npx next start &` (background), wait for ready, `caffeinate -u &` to prevent display sleep during capture
3. `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3042 --out .planning/phases/36-regression-gate/lh-phase36-post.json --routes-only /,/en,/seo-tecnico-madrid,/en/seo-tecnico-madrid,/seo-tecnico-lima,/en/seo-tecnico-lima`
4. If any route shows an anomalous delta (outside expected noise), reproduce with 2-3 clean re-runs before concluding real vs. noise (Phase 28 precedent).
5. Kill the `next start` PID and `caffeinate`, verify via `lsof -i :3042` and `ps aux | grep next` clear.

**Verify:** `lh-phase36-post.json` exists with 6 route entries, no `error` keys.

## Task 2: H1 + JSON-LD re-snapshot

Against the same production server, for each of the 6 routes: fetch HTML, extract `<h1>` text and any `<script type="application/ld+json">` blocks. Write to `.planning/phases/36-regression-gate/36-post-content.json`.

**Verify:** JSON file has one entry per route with `h1` and `jsonLd` fields populated (matching Phase 32's `32-baseline-content.json` shape).

## Task 3: Programmatic diff + verdict doc

1. Diff `lh-phase36-post.json` against `lh-phase32-baseline.json` per route: flag if `performance` drops more than 5 points, or if any CWV metric (LCP/CLS/TBT) crosses into a worse Lighthouse lab band than its Phase 32 baseline value.
2. Diff `36-post-content.json` against `32-baseline-content.json`: confirm exactly 1 H1 per route, byte-identical H1 text on all 6 routes (no H1 copy changed across any phase in this milestone).
3. Write `.planning/phases/36-regression-gate/36-REGRESSION-DIFF.md` with per-route tables, explicit overall verdict, and root-cause writeups for anything flagged. If a real regression is found: reproduce, root-cause, fix if in-scope, re-measure, document (gap-closure sub-pattern, same as Phase 28).

**Verify:** `36-REGRESSION-DIFF.md` has an explicit PASS/FAIL verdict; if FAIL, gap closure is either completed and re-verified PASS, or explicitly reported as a blocked FAIL requiring a decision beyond this phase's scope.

## Task 4: Close out milestone docs

Update `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` to reflect Phase 36 complete (5/5 phases, 100%), REG-02 checked off, and the full 13-requirement traceability table (noting LOCAL-05's existing placeholder-content caveat, not overstating it). Write `36-VERIFICATION.md` against ROADMAP's Phase 36 success criteria. Commit everything.

**Verify:** `git log` shows one commit covering the phase 36 docs + measurement artifacts; `STATE.md` progress frontmatter shows `completed_phases: 5`, `percent: 100`.
