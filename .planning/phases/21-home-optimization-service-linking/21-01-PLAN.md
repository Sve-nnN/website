---
phase: 21-home-optimization-service-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-phase21-home-optimization.ts
autonomous: true
requirements: [SEO-HOME-01, SEO-HOME-02]

must_haves:
  truths:
    - "Home's aboutSection description paragraph (both locales) explicitly names Next.js/Payload/headless CMS development with SEO integrated into the code"
    - "Home's Header nav includes a 'Servicios'/'Services' item pointing at a real, working /services URL"
    - "The new nav link returns 200 in both locales (/, /en/) without breaking any existing nav item's label or url"
  artifacts:
    - path: "scripts/seed-phase21-home-optimization.ts"
      provides: "Idempotent-ish in-place update of Home's aboutSection block + Header.navItems, mirroring seed-phase13/seed-header-footer-content id-reuse discipline"
      contains: "aboutSection"
  key_links:
    - from: "scripts/seed-phase21-home-optimization.ts"
      to: "src/blocks/AboutSection/config.ts"
      via: "in-place edit of the aboutSection block already in Home's content.layout, by blockType lookup"
      pattern: "aboutSection"
    - from: "scripts/seed-phase21-home-optimization.ts"
      to: "src/globals/Header/index.ts"
      via: "payload.updateGlobal({ slug: 'header', ... }) appending one navItems entry, ids preserved for existing items"
      pattern: "navItems"
---

<objective>
Reinforce Home's "desarrollo real (Next.js/Payload/CMS headless) + SEO técnico" competitive angle in
the existing `aboutSection` block copy, and add a working nav link from Home toward the Phase 19
service pages (SEO-HOME-01, SEO-HOME-02). Zero schema changes, zero `payload.config.ts` edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-home-optimization-service-linking/21-CONTEXT.md
@.planning/REQUIREMENTS.md

@scripts/seed-phase13-home-content.ts
@scripts/seed-header-footer-content.ts
@scripts/fix-header-navitems-es-labels.ts
@src/blocks/AboutSection/config.ts
@src/globals/Header/index.ts
@scripts/seed-phase19-data/group-b.ts

<interfaces>
<!-- aboutSection block's current es/en paragraph[0].text and features[2] (index 2 = "Arquitectura
escalable"/"Scalable Architecture") come from scripts/seed-phase13-home-content.ts's
aboutHeaderCopy/featuresCopy consts — this plan does NOT edit that file (it's Phase 13's own seed,
already run); instead this plan's NEW script re-reads Home's CURRENT live content.layout (via
findByID) and edits the aboutSection block's paragraphs[0]/features[2] fields in place, id-reuse
across the locale loop exactly like seed-phase13-home-content.ts's own pattern (see that file's
"In-place update of the existing aboutSection block" section for the literal code shape to mirror).

Header navItems id-reuse: fetch payload.findGlobal({slug:'header', locale}) per locale, the fetched
navItems array has real `id` fields on every item already (they exist in the DB). Build the new array
by mapping existing items unchanged (ids preserved) and appending ONE new item. On the 'es' locale
write, the new item has no `id` (Payload assigns one). Refetch afterward
(payload.findGlobal({slug:'header', locale:'es'})) to read back the assigned id of the new (last)
item. On the 'en' locale write, build the array from the freshly-refetched EN labels for the 4
existing items (fetch header again with locale:'en' to get their real EN labels) plus the new item
WITH the captured id and English label.

fullstackServiceCopy (Phase 19, already-approved language, src/blocks the differentiator argument):
es includes.paragraphs[0]: "Desarrollo en Next.js con renderizado en servidor y streaming..."
en includes.paragraphs[0]: "Next.js development with server rendering and streaming..."
Echo this established angle in Home's aboutSection paragraph, don't reinvent new claims.
-->
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: [BLOCKING] Seed script — aboutSection copy reinforcement + Header nav link, run + verify</name>
  <files>scripts/seed-phase21-home-optimization.ts</files>
  <read_first>
    scripts/seed-phase13-home-content.ts,
    scripts/seed-header-footer-content.ts,
    scripts/fix-header-navitems-es-labels.ts
  </read_first>
  <action>
    Create `scripts/seed-phase21-home-optimization.ts`:

    1. Fetch Home's `pages` doc by slug 'home' (locale-agnostic id lookup first, e.g.
       `payload.find({ collection: 'pages', where: { slug: { equals: 'home' } }, limit: 1 })`).
    2. For each locale in `['es','en']`: `findByID` the doc with that locale, `depth: 0`. Find the
       `aboutSection` block in `content.layout` by `blockType`. In place, update:
       - `paragraphs[0].text` (reuse its existing `id`): rewrite to explicitly mention Next.js,
         Payload/headless CMS, and SEO integrated into the code, contrasted implicitly against the
         generic "SEO + WordPress" pattern (per D-04, echoing Phase 19's `fullstackServiceCopy`
         language) — integrated naturally into the existing "Mi enfoque en Consultoría Técnica" flow,
         not just appended as a disconnected sentence. Keep the paragraph's existing opening framing
         (SEO + development aren't separate disciplines) since that's still accurate and on-brand.
       - `features[2]` (the "Arquitectura escalable"/"Scalable Architecture" item, reuse its existing
         `id`): rewrite `description` to explicitly name Next.js and headless CMS, keep `icon`/`title`
         unchanged.
       Write back via `payload.update({ collection: 'pages', id: homeDoc.id, locale, data: { content:
       { layout: <layout with aboutSection block replaced> } } })`. Only replace the `content.layout`
       array element at the aboutSection index — every other block in the layout array must be passed
       through completely unchanged (spread the existing layout, replace one index).
    3. Header nav link: fetch `payload.findGlobal({ slug: 'header', locale: 'es' })`, capture its
       `navItems` (with real ids). Build `navItemsEs = [...existing, { link: { type: 'custom', url:
       '/services', label: 'Servicios', newTab: false } }]` (no id on the new entry).
       `payload.updateGlobal({ slug: 'header', locale: 'es', data: { navItems: navItemsEs } })`.
       Refetch (`findGlobal({slug:'header', locale:'es'})`) to capture the newly-assigned id of the
       last navItems entry. Fetch `findGlobal({ slug: 'header', locale: 'en' })` to get the 4 existing
       items' real EN labels + ids (should match the es-locale ids positionally since they're the
       same underlying rows). Build `navItemsEn = [...those 4 with their ids, { id: <captured id>,
       link: { type: 'custom', url: '/services', label: 'Services', newTab: false } }]`.
       `payload.updateGlobal({ slug: 'header', locale: 'en', data: { navItems: navItemsEn } })`.
    4. Log every step (mirroring the verbose logging style of `seed-phase13-home-content.ts` /
       `fix-header-navitems-es-labels.ts`) so a re-run's output is diagnosable.
    5. This script is NOT strictly idempotent on the nav-item-append step (re-running would append a
       SECOND "Servicios" item unless guarded) — add a guard: before appending, check whether an item
       with `link.url === '/services'` already exists in the fetched `navItems`; if so, skip the
       append (log "Services nav item already exists, skipping") and only run the aboutSection copy
       update (which IS naturally idempotent — always overwrites the same paragraph/feature by id).
  </action>
  <acceptance_criteria>
    - `grep -c "Next.js\|Payload" scripts/seed-phase21-home-optimization.ts` returns >= 2
    - `grep -c "navItems\|aboutSection" scripts/seed-phase21-home-optimization.ts` returns >= 2
    - `grep -c "already exists, skipping\|link.url === '/services'" scripts/seed-phase21-home-optimization.ts` returns >= 1 (idempotency guard present)
    - `npx tsc --noEmit` exits 0
    - After running against the real DB (with explicit human approval): `curl -s http://localhost:3000/ | grep -ic "next.js\|payload"` returns >= 1; `curl -s http://localhost:3000/ | grep -o "Servicios"` returns non-empty; `curl -s http://localhost:3000/en/ | grep -o "Services"` returns non-empty (among possibly multiple "Services" substring matches, at least one from the new nav item); `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/services` returns 200 (already true since Phase 19, re-confirmed here as the link target)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Home's aboutSection copy (both locales) explicitly reinforces the Next.js/Payload/SEO-in-the-code
    differentiator. Header nav has a working "Servicios"/"Services" link to `/services` in both
    locales, without disturbing any existing nav item's label, url, or id. Script is safe to re-run
    (aboutSection update is naturally idempotent; nav append is explicitly guarded).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local seed script -> Postgres | Same trusted, developer-run pattern as every prior phase's seed script, subject to the Database Safety human-approval rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | Header.navItems id-reuse across locale writes | mitigate | Existing items' ids always read back from the DB before being reused (never invented), exact pattern already proven correct by `fix-header-navitems-es-labels.ts` |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs |

No new trust boundary. Reuses already-vetted patterns end to end.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. Home page (both locales) content mentions Next.js/Payload explicitly
3. New "Servicios"/"Services" nav item present, links to a real 200 URL, in both locales
4. No regression to any existing nav item (labels/urls unchanged for Blog/Case Studies/Authors/Contact)
5. No new Postgres migration, no `payload.config.ts` changes
</verification>

<success_criteria>
- Home's aboutSection copy reinforces the Next.js/Payload/headless-CMS + SEO-technical differentiator, both locales
- At least one working link from Home to Services, no 404, both locales
- Zero schema changes
</success_criteria>

<output>
Create `.planning/phases/21-home-optimization-service-linking/21-01-SUMMARY.md` when done
</output>
