---
phase: 48.5-auditor-destacado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auditor.ts
  - src/blocks/AuditorHighlight/config.ts
  - src/blocks/AuditorHighlight/Component.tsx
  - src/collections/Pages/index.ts
  - src/blocks/blockRegistry.tsx
  - messages/es.json
  - messages/en.json
  - src/migrations/*_phase48_5_auditor_highlight.ts
  - src/migrations/*_phase48_5_auditor_highlight.json
  - src/migrations/index.ts
  - src/payload-types.ts
  - scripts/seed-phase48-5-home-auditor.ts
  - scripts/verify-phase48-5-home-auditor.ts
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-04, FEAT-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Home renderiza una seccion del auditor en ambos locales, con CTA a auditor.juan-tech.com, construida sobre Card/CardContent (el mismo primitivo de ServicesShowcase) — verificado por curl sobre el HTML renderizado"
    - "La seccion expone los 4 datos concretos (29 checks, 5 categorias, hasta 500 URLs, CWV via PageSpeed Insights) como hechos estructurados, no como adjetivos de marketing"
    - "El freeTierNote (1 auditoria por semana por email verificado) es texto visible, nunca sr-only, nunca solo-below-the-fold"
    - "El link al auditor no lleva rel=sponsored; git diff en sitemap-data.ts/canonical.ts/breadcrumbs.ts queda vacio; cero JS de cliente nuevo (sin 'use client' en los 2 archivos nuevos)"
    - "Toda la copy nueva existe en EN y ES, pasada por humanizer"
    - "AuditorHighlight se distingue de AuditOfferBlock: icono distinto (Gauge, no Search), heading sin substrings compartidos, AboutSection como bloque colchon entre ambos en el orden real del layout"
  artifacts:
    - src/lib/auditor.ts
    - src/blocks/AuditorHighlight/config.ts
    - src/blocks/AuditorHighlight/Component.tsx
    - "migracion aditiva generada por payload migrate:create (pages_blocks_auditor_highlight(+_locales) y sus contrapartes _pages_v_)"
  key_links:
    - "Pages/index.ts blocks[] -> blockRegistry.tsx blockComponents['auditorHighlight'] -> RenderBlocks -> AuditorHighlightComponent"
    - "content.layout[] del doc pages slug=home: ...servicesShowcase, auditorHighlight, aboutSection, auditOfferBlock... (orden exacto, AboutSection como colchon)"
    - "AuditorHighlightComponent -> src/lib/auditor.ts (AUDITOR_URL + AUDITOR_STATS, constantes de codigo, NUNCA campos CMS)"
---

<objective>
Construir de punta a punta la primera de las 3 superficies de la fase: el bloque Payload
`AuditorHighlight` en Home. Es el tracer de la fase 48.5 completa — prueba schema, migracion,
registro, render, contenido bilingue y verificacion end-to-end en una sola superficie antes de
replicar la forma en la landing de Auditoria SEO Tecnica (Plan 02) y en `/stack` (Plan 03).

Purpose: `auditor.juan-tech.com` deja de estar escondido. Home es la superficie de mayor trafico
y la que fija el patron visual (Card/CardContent, icono Gauge, stat grid) que las otras 2
superficies reutilizan por composicion, no por copia.
Output: bloque `AuditorHighlight` registrado y migrado contra Postgres real, seccion visible en
`/` y `/en` con los 4 datos verificables, CTA de salida conforme al Outbound Link Contract, y
buffer visual/semantico contra `AuditOfferBlock` confirmado.
</objective>

## Artifacts this phase produces (this plan's share)

- `src/lib/auditor.ts` — constantes compartidas `AUDITOR_URL`/`AUDITOR_STATS`, consumidas tambien
  por los Planes 02 y 03.
- Bloque Payload `AuditorHighlight` (`config.ts` + `Component.tsx`), registrado en `Pages/index.ts`
  y `blockRegistry.tsx`.
- Migracion aditiva `phase48_5_auditor_highlight` (`CREATE TABLE` unicamente).
- Namespace i18n `auditorHighlight` en `messages/es.json`/`en.json`.
- `AuditorHighlight` publicado en el doc `pages` slug `home` (es+en), entre `servicesShowcase` y
  `aboutSection`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.5-auditor-destacado/48.5-CONTEXT.md
@.planning/phases/48.5-auditor-destacado/48.5-UI-SPEC.md
@.planning/phases/48.5-auditor-destacado/48.5-RESEARCH.md

@src/blocks/ServicesShowcase/config.ts
@src/blocks/ServicesShowcase/Component.tsx
@src/blocks/AuditOfferBlock/config.ts
@src/blocks/AuditOfferBlock/Component.tsx
@src/collections/Pages/index.ts
@src/blocks/blockRegistry.tsx
@src/blocks/RenderBlocks.tsx
@src/components/ui/card.tsx
@src/components/ui/button-variants.ts
@src/components/Container.tsx
@src/lib/service-slugs.ts
@src/lib/canonical.ts
@src/lib/breadcrumbs.ts
@src/lib/sitemap-data.ts
@scripts/db/04-which-database.ts
@scripts/humanize-home-page.ts
@messages/es.json
@messages/en.json
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Bloque AuditorHighlight (schema+migracion+registro+i18n) + contenido real en Home, de punta a punta</name>
  <read_first>
    src/blocks/ServicesShowcase/config.ts, src/blocks/ServicesShowcase/Component.tsx,
    src/blocks/AuditOfferBlock/config.ts, src/blocks/AuditOfferBlock/Component.tsx,
    src/collections/Pages/index.ts, src/blocks/blockRegistry.tsx, src/components/ui/card.tsx,
    src/components/ui/button-variants.ts, src/components/Container.tsx, src/lib/service-slugs.ts,
    scripts/db/04-which-database.ts, scripts/humanize-home-page.ts (patron reapplyIds +
    findByID(depth:0) + payload.update con _status:'published' explicito)
  </read_first>
  <precondition>node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts confirma que DATABASE_URI apunta al Postgres de Dokploy (no a una Neon abandonada, per el incidente 2026-08-17 de CLAUDE.md) antes de generar/aplicar la migracion</precondition>
  <reversibility rating="reversible">AuditorHighlight es un bloque nuevo de 4 campos editoriales sin contenido previo que migrar ni relaciones que romper — si el shape necesita cambiar despues, es una migracion aditiva mas, no una reescritura de datos existentes.</reversibility>
  <files>
    src/lib/auditor.ts, src/blocks/AuditorHighlight/config.ts, src/blocks/AuditorHighlight/Component.tsx,
    src/collections/Pages/index.ts, src/blocks/blockRegistry.tsx, messages/es.json, messages/en.json,
    src/migrations/*_phase48_5_auditor_highlight.ts, scripts/seed-phase48-5-home-auditor.ts
  </files>
  <action>
    Paso 1 — Constantes compartidas. Crear `src/lib/auditor.ts` (modulo puro, sin imports de
    `payload`/`@payload-config`, mismo principio que `src/lib/service-slugs.ts`): exportar
    `AUDITOR_URL = 'https://auditor.juan-tech.com'` y `AUDITOR_STATS` como un array `as const` de 4
    objetos `{ value, labelKey }`: `{ value: '29', labelKey: 'statsChecks' }`,
    `{ value: '5', labelKey: 'statsCategories' }`, `{ value: '500', labelKey: 'statsUrls' }`,
    `{ value: 'CWV', labelKey: 'statsCwv' }`. Estos 4 valores son hechos verificables del repo del
    auditor (per ROADMAP/FEAT-02), NUNCA campos CMS — un editor no debe poder inflar un numero que
    esta fase exige mantener literalmente cierto.

    Paso 2 — `src/blocks/AuditorHighlight/config.ts`. `Block` de Payload, slug `auditorHighlight`,
    interfaceName `AuditorHighlightBlock`, labels `{ singular: 'Auditor Highlight', plural: 'Auditor
    Highlight Blocks' }`. Campos: `title` (text, localized, NO required — admin.description: "Vacio
    -> heading sr-only, mismo patron SEO-10.2 que ServicesShowcase.title, nunca saltar un nivel de
    heading"); `description` (textarea, localized, required — admin.description: "1-2 oraciones, sin
    line-clamp, este es un bloque de feature unico, no una grilla de 4"); `ctaLabel` (text, localized,
    NO required — admin.description: "Vacio -> fallback i18n auditorHighlight.cta"); `freeTierNote`
    (textarea, localized, required — admin.description: "La disclosure honesta del plan gratuito.
    Renderiza como texto visible, NUNCA sr-only, NUNCA solo debajo del fold").

    Paso 3 — `src/blocks/AuditorHighlight/Component.tsx`. Server Component puro (sin 'use client',
    sin fetch a auditor.juan-tech.com — los 4 stats son constantes locales). Firma: recibe las props
    del bloque (`title`, `description`, `ctaLabel`, `freeTierNote`) tipadas con
    `AuditorHighlightBlock` de `@/payload-types`. Usa `getTranslations('auditorHighlight')` (patron
    `ServicesShowcaseComponent`). Estructura exacta: `<Container className="py-16">` (NO `py-12` —
    este es un bloque feature-weight, no la grilla de 4 de ServicesShowcase) envolviendo un `Card`
    (mismas clases que ServicesShowcase: `rounded-2xl border bg-card shadow-sm`, sin `<Link>`
    envolviendo toda la card) con `CardContent`. Dentro: heading `title ? <h2
    className="font-heading text-heading">{title}</h2> : <h2 className="sr-only">{t('sectionHeading')}</h2>`.
    Layout interno de dos columnas en `md:` (`md:grid-cols-2 gap-8`), una columna en mobile, en este
    orden mobile: icono+titulo+description, luego el stat grid, luego el CTA, luego el freeTierNote
    al final. Icono: chip `flex size-10 items-center justify-center rounded-md bg-primary/10` con
    `Gauge` de `lucide-react` (`className="size-5 text-primary"`) — NUNCA `Search` (ya usado por la
    card de "Auditoria SEO Tecnica" en `ServicesShowcase`'s `iconBySlug`; los dos bloques de auditoria
    no deben compartir icono). Stat grid: `grid grid-cols-2 gap-6`, mapeando `AUDITOR_STATS`, cada
    stat como `<div><p className="font-heading text-display font-semibold tracking-tight tabular-nums
    text-primary-text">{stat.value}</p><p className="text-label uppercase tracking-wide
    opacity-70">{t(stat.labelKey)}</p></div>` (identico al tratamiento KPI de `ResultsSection`, no
    inventar tipografia nueva). CTA: `<a href={AUDITOR_URL} target="_blank" rel="noopener"
    className={buttonVariants()}>{ctaLabel || t('cta')}<span className="sr-only">
    {' '}({t('opensInNewTab')})</span></a>` — NUNCA `next/link`, NUNCA `@/i18n/navigation`'s `Link`,
    NUNCA `CMSLink` (0 KB de JS de cliente exige el `<a>` nativo). freeTierNote: `<p
    className="text-label text-muted-foreground mt-6 md:col-span-2">{freeTierNote}</p>` — visible,
    nunca `sr-only`.

    Paso 4 — Registro. En `src/collections/Pages/index.ts`: importar `AuditorHighlight` desde
    `@/blocks/AuditorHighlight/config` y agregarlo al final del array `blocks:` (mismo patron que los
    27 bloques ya existentes, no reordenar los existentes). En `src/blocks/blockRegistry.tsx`:
    importar `AuditorHighlightComponent` desde `@/blocks/AuditorHighlight/Component` y agregar la
    entrada `auditorHighlight: AuditorHighlightComponent` al objeto `blockComponents` (key = slug del
    bloque en camelCase).

    Paso 5 — i18n. Agregar el namespace `auditorHighlight` a `messages/es.json` y `messages/en.json`
    con las keys: `statsChecks` (ES "checks técnicos" / EN "technical checks"), `statsCategories`
    ("categorías"/"categories"), `statsUrls` ("URLs por auditoría"/"URLs per audit"), `statsCwv`
    ("Core Web Vitals (PageSpeed Insights)" identico en ambos locales), `cta` ("Probar el
    auditor"/"Try the auditor"), `sectionHeading` ("Mi auditor SEO"/"My SEO auditor"),
    `opensInNewTab` ("se abre en una pestaña nueva"/"opens in a new tab").

    Paso 6 — Migracion. Correr `node --env-file=.env node_modules/.bin/payload migrate:create`
    (nombre sugerido: `phase48_5_auditor_highlight`). Leer el archivo `.ts` generado COMPLETO antes
    de aplicar: debe contener unicamente `CREATE TABLE`/`ADD CONSTRAINT`/`CREATE INDEX` para
    `pages_blocks_auditor_highlight`, `pages_blocks_auditor_highlight_locales`, y sus contrapartes de
    borrador `_pages_v_blocks_auditor_highlight(+_locales)` — CERO `DROP TABLE`/`DROP
    COLUMN`/`ALTER COLUMN`. Es aditiva: aplicar directamente con
    `node --env-file=.env node_modules/.bin/payload migrate`, sin pausar para aprobacion de Juan, per
    la regla de Database Safety de CLAUDE.md. Correr
    `node --env-file=.env node_modules/.bin/payload generate:types` despues para que
    `payload-types.ts` incluya `AuditorHighlightBlock`.

    Paso 7 — Contenido real. Crear `scripts/seed-phase48-5-home-auditor.ts` (patron
    `scripts/humanize-home-page.ts`: `getPayload({config})`, `LOCALES = ['es','en'] as const`).
    Antes de escribir, pasar la copy borrador de la tabla de abajo por el skill humanizer
    (`~/.claude/skills/humanizer/SKILL.md`, calibrado contra `research/voice-sample-juan.md`,
    primera persona, sin em dash, espanol neutro sin voceo) — la tabla es punto de partida, no texto
    final. Para cada locale en orden `['es','en']`: `payload.findByID({collection:'pages', id:
    homeDoc.id, locale, depth: 0})` para traer el `content.layout` completo sin resolver relaciones;
    ubicar el indice del bloque cuyo `blockType === 'aboutSection'`; construir el nuevo bloque
    `{ blockType: 'auditorHighlight', title, description, ctaLabel: undefined, freeTierNote }` con la
    copy humanizada de ese locale; insertar (splice) el nuevo bloque en ese indice (queda
    inmediatamente despues de `servicesShowcase` e inmediatamente antes de `aboutSection` — NUNCA
    adyacente a `auditOfferBlock`); en la escritura de `es` no hay id previo que reusar para el
    bloque nuevo (Payload le asigna uno); refetch con `depth:0` tras esa escritura para capturar el id
    asignado y reusarlo (mismo campo `id`) en la escritura de `en`, dejando el resto del array
    identico via el patron `reapplyIds` de `scripts/humanize-home-page.ts` (bloques existentes pasan
    con su `id` intacto, sin tocar sus campos). Escribir con
    `payload.update({collection:'pages', id, locale, data:{content:{layout:...}, _status:
    'published'}})` — `_status: 'published'` explicito en cada write (bug recurrente documentado en
    memoria del proyecto: 2026-08-17, Websites draft-publish fix). Draft copy (humanizar antes de
    escribir): `title` ES "Construí mi propio auditor SEO" / EN "I built my own SEO auditor";
    `description` ES "Es la misma herramienta que uso en mis propios proyectos y en los de mis
    clientes. No es un servicio que vendo, es software que construí y mantengo en producción." / EN
    "It's the same tool I use on my own projects and my clients'. It's not a service I sell, it's
    software I built and run in production."; `freeTierNote` ES "Plan gratuito: 1 auditoría por
    semana, por email verificado." / EN "Free tier: 1 audit per week, per verified email."; `ctaLabel`
    queda vacio (usa el fallback i18n).

    Run del seed: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-5-home-auditor.ts`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; MIGFILE=$(ls src/migrations/*phase48_5_auditor_highlight*.ts 2>/dev/null | head -1); test -n "$MIGFILE"; ! grep -qiE "DROP TABLE|DROP COLUMN|ALTER COLUMN.*DROP" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); grep -q "AuditorHighlightBlock" src/payload-types.ts; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-5-home-auditor.ts; npm run dev > /tmp/dev-48-5-01.log 2>&1 & echo $! > /tmp/dev-48-5-01.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; curl -s http://localhost:3000/ | grep -q "auditor.juan-tech.com"; curl -s http://localhost:3000/en | grep -q "auditor.juan-tech.com"; kill "$(cat /tmp/dev-48-5-01.pid)" 2>/dev/null || true; npx tsc --noEmit; echo TRACER_OK</automated>
    <fails_when>el archivo de migracion no existe o contiene DROP; payload-types.ts no tiene AuditorHighlightBlock; el seed script falla; / o /en no incluyen un href a auditor.juan-tech.com; tsc reporta errores; TRACER_OK no se imprime</fails_when>
  </verify>
  <done>AuditorHighlight registrado y migrado contra Postgres real; Home ('/' y '/en') renderiza la seccion con contenido bilingue humanizado, posicionada entre ServicesShowcase y AboutSection; tsc limpio.</done>
</task>

<!-- planner-region-allow: 'use client' -->
<!-- Task 1's action prose says "sin 'use client'" (a negative instruction, not a usage) in the
     same 2 files Task 2's file-wide gate bans it from — no real conflict, both tasks agree these
     files must never contain the directive. -->
<task type="auto" tdd="false">
  <name>Task 2: Barrido de aceptacion — datos verificables, contrato de link, y no-confusion con AuditOfferBlock</name>
  <read_first>
    src/blocks/AuditOfferBlock/Component.tsx (heading real "Empecemos por una auditoría técnica"),
    src/lib/canonical.ts, src/lib/breadcrumbs.ts, src/lib/sitemap-data.ts
  </read_first>
  <files>scripts/verify-phase48-5-home-auditor.ts</files>
  <action>
    Crear `scripts/verify-phase48-5-home-auditor.ts` (patron `scripts/verify-stack-page.ts`: fetch
    HTML real contra `http://localhost:3000`, sin mockear nada, imprime `PASS`/`FAIL` + lista de
    fallos). El script corre por cada locale (`/` y `/en`) y verifica: (1) los 4 valores exactos
    aparecen en el HTML — "29", "5", "500", "CWV" junto a sus labels traducidos (no basta con que el
    numero aparezca suelto, debe estar en el contexto del stat grid, verificar contra el texto
    completo del bloque aislado por el marcador `auditor.juan-tech.com` +/- una ventana de
    caracteres razonable); (2) el `freeTierNote` (o su traduccion) aparece como texto visible del
    documento (no dentro de una clase `sr-only`) — extraer el fragmento HTML alrededor del texto y
    confirmar que su className NO contiene `sr-only`; (3) el anchor hacia `auditor.juan-tech.com`
    tiene `rel="noopener"` y NUNCA `sponsored` ni `nofollow`, tiene `target="_blank"`, y el texto del
    link incluye un `<span class="sr-only">` con el sufijo de nueva pestana; (4) el heading real de
    `AuditOfferBlock` ("Empecemos por una auditoría técnica" en es / su equivalente en) y el heading
    de `AuditorHighlight` no comparten ninguna substring de mas de 3 palabras (chequeo simple de
    solapamiento de tokens), y el heading de `AboutSection` ("Mi enfoque en Consultoría Técnica")
    aparece en el HTML EN UN INDICE entre el de `ServicesShowcase`/`AuditorHighlight` y el de
    `AuditOfferBlock`, confirmando el buffer; (5) ningun href renderizado contiene la palabra literal
    "undefined". Terminar con `git diff --stat` vacio sobre `src/lib/sitemap-data.ts`,
    `src/lib/canonical.ts`, `src/lib/breadcrumbs.ts`, y un grep que confirme CERO ocurrencias de
    `'use client'` en `src/lib/auditor.ts` y `src/blocks/AuditorHighlight/Component.tsx`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npm run dev > /tmp/dev-48-5-01b.log 2>&1 & echo $! > /tmp/dev-48-5-01b.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-phase48-5-home-auditor.ts | tee /tmp/verify-48-5-01.log; grep -q "^PASS$" /tmp/verify-48-5-01.log; kill "$(cat /tmp/dev-48-5-01b.pid)" 2>/dev/null || true; git diff --stat src/lib/sitemap-data.ts src/lib/canonical.ts src/lib/breadcrumbs.ts | tee /tmp/diff-48-5-01.log; test ! -s /tmp/diff-48-5-01.log; ! grep -l "'use client'" src/lib/auditor.ts src/blocks/AuditorHighlight/Component.tsx; npx tsc --noEmit; echo ACCEPTANCE_OK</automated>
    <fails_when>verify-phase48-5-home-auditor.ts imprime FAIL o falla al correr; el git diff de los 3 archivos protegidos no esta vacio; alguno de los 2 archivos nuevos contiene 'use client'; tsc reporta errores</fails_when>
  </verify>
  <done>Los 4 datos verificables, el freeTierNote visible, el contrato de link (rel=noopener unico, sr-only new-tab), y el buffer contra AuditOfferBlock quedan confirmados por script contra el dev server real; canonical/hreflang/sitemap sin diff; cero JS de cliente nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Editor (Payload admin) -> Postgres | Contenido editorial de `AuditorHighlight` entra por campos `text`/`textarea` sin rich text libre |
| Visitante -> auditor.juan-tech.com | Salida cross-subdominio desde un `<a>` nativo, sin llamada de red desde este servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-48.5-01 | Tampering | AuditorHighlight.description/freeTierNote (campos editoriales) | low | accept | React escapa por defecto todo `{children}`; ningun campo es rich text ni pasa por `dangerouslySetInnerHTML` — sin superficie de inyeccion de markup |
| T-48.5-02 | Tampering/Spoofing | CTA `<a target="_blank">` hacia auditor.juan-tech.com | medium | mitigate | `rel="noopener"` obligatorio (nunca `sponsored`/`nofollow`), verificado por Task 2's script contra el HTML real |
| T-48.5-03 | Tampering | `AUDITOR_URL` como destino de salida | low | mitigate | Constante hardcodeada en `src/lib/auditor.ts`, no un campo de texto libre editable — ningun editor puede redirigir el link sin tocar codigo |
| T-48.5-04 | Tampering (integridad de schema) | Migracion `phase48_5_auditor_highlight` contra Postgres de Dokploy (produccion real) | medium | mitigate | Generada via `payload migrate:create`, leida completa antes de aplicar (confirmar CERO `DROP`), aplicada solo tras confirmar target DB con `scripts/db/04-which-database.ts` — reversible sin perdida de datos (tabla nueva, sin contenido previo) |
| T-48.5-05 | Spoofing (confusion de intencion, no de identidad tecnica) | AuditorHighlight adyacente a AuditOfferBlock (auditoria pagada 600 USD) en el layout de Home | medium | mitigate | Icono distinto (Gauge vs Search), heading sin substrings compartidos, AboutSection como bloque colchon obligatorio entre ambos — verificado por script en Task 2, no solo por lectura de codigo |

</threat_model>

<verification>
`npx tsc --noEmit` limpio en ambas tareas. Migracion generada y confirmada 100% aditiva por
lectura directa antes de aplicar. `/` y `/en` renderizan `AuditorHighlight` con contenido bilingue
real, posicionado correctamente. `git diff` vacio en los 3 archivos protegidos de SEO tecnico.
</verification>

<success_criteria>
FEAT-01: seccion dedicada en Home, patron ServicesShowcase, CTA a auditor.juan-tech.com — cumplido
y verificado por curl. FEAT-02: 4 datos concretos como hechos estructurados, no adjetivos —
cumplido y verificado por script. FEAT-04 (parcial, esta superficie): sin `rel="sponsored"`,
canonical/hreflang/sitemap sin diff, 0 KB JS nuevo — cumplido y verificado. FEAT-05 (parcial, esta
superficie): copy bilingue humanizada, honesta sobre el limite gratuito — cumplido.
</success_criteria>

<output>
Create `.planning/phases/48.5-auditor-destacado/48.5-01-SUMMARY.md` when done
</output>
