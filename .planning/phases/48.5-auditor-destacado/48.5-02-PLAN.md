---
phase: 48.5-auditor-destacado
plan: 02
type: execute
wave: 2
depends_on: ["48.5-01"]
files_modified:
  - src/components/StackHighlightCallout.tsx
  - src/blocks/ToolStack/Component.tsx
  - src/blocks/AuditorCallout/config.ts
  - src/blocks/AuditorCallout/Component.tsx
  - src/collections/Pages/index.ts
  - src/blocks/blockRegistry.tsx
  - messages/es.json
  - messages/en.json
  - src/migrations/*_phase48_5_auditor_callout.ts
  - src/migrations/*_phase48_5_auditor_callout.json
  - src/migrations/index.ts
  - src/payload-types.ts
  - scripts/seed-phase48-5-landing-auditor.ts
  - scripts/verify-phase48-5-landing-auditor.ts
autonomous: true
requirements: [FEAT-02, FEAT-03, FEAT-04, FEAT-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "StackHighlightCallout generalizado a linkHref/linkLabel (de gscHref/gscLabel) sin romper su unico call site existente (el callout no-commission-pick de /stack, que sigue mostrando el link de GSC correctamente)"
    - "La landing de Auditoria SEO Tecnica enlaza al auditor en ambos locales, inmediatamente despues de ServiceScopeCard, antes de ClientLogosBlock"
    - "ServiceScopeCard (compartido por las otras 3 landings de servicio) queda sin tocar — cero cambios de schema/admin en ese bloque"
    - "El link al auditor en la landing no lleva rel=sponsored ni badge de no-commission (taxonomia reservada al sistema de afiliados); git diff en sitemap-data.ts/canonical.ts/breadcrumbs.ts queda vacio; cero JS de cliente nuevo"
    - "La copy de la landing (heading+narrative) es bilingue, humanizada, y menciona los 4 datos verificables + el limite del plan gratuito"
  artifacts:
    - src/blocks/AuditorCallout/config.ts
    - src/blocks/AuditorCallout/Component.tsx
    - "StackHighlightCallout.tsx con props linkHref/linkLabel/linkOpensInNewTabLabel (generalizado)"
    - "migracion aditiva generada por payload migrate:create (pages_blocks_auditor_callout(+_locales) y sus contrapartes _pages_v_)"
  key_links:
    - "Pages/index.ts blocks[] -> blockRegistry.tsx blockComponents['auditorCallout'] -> RenderBlocks -> AuditorCalloutComponent -> StackHighlightCallout (generalizado)"
    - "content.layout[] del doc pages slug=seo-technical-audit: Hero, intro, serviceScopeCard, auditorCallout, clientLogosBlock, ... (orden exacto)"
    - "ToolStackComponent.tsx call site existente (no-commission pick) migrado de gscHref/gscLabel a linkHref/linkLabel sin cambiar el JSX del <a> renderizado"
---

<objective>
Generalizar `StackHighlightCallout` (su unico prop de link hoy esta nombrado por su unico caller,
Google Search Console) y construir sobre esa base el bloque `AuditorCallout` para la landing de
Auditoria SEO Tecnica — la segunda superficie de enlace que exige FEAT-03.

Purpose: dos callers no-afiliados (GSC y el auditor) necesitan el mismo componente visual sin que
su firma siga hablando de un solo caso de uso; construir `AuditorCallout` como bloque de un solo
uso evita contaminar `ServiceScopeCard` (compartido por las otras 3 landings) con un campo
irrelevante.
Output: `StackHighlightCallout` generalizado sin regresion en `/stack`, bloque `AuditorCallout`
registrado y migrado, landing de Auditoria SEO Tecnica enlazando al auditor en ambos locales.
</objective>

## Artifacts this phase produces (this plan's share)

- `src/components/StackHighlightCallout.tsx` generalizado (`linkHref`/`linkLabel`/
  `linkOpensInNewTabLabel`), reusado sin regresion por el call site existente de `/stack`.
- Bloque Payload `AuditorCallout` (`config.ts` + `Component.tsx`), registrado en `Pages/index.ts`
  y `blockRegistry.tsx`.
- Migracion aditiva `phase48_5_auditor_callout` (`CREATE TABLE` unicamente).
- Namespace i18n `auditorCallout` en `messages/es.json`/`en.json`.
- `AuditorCallout` publicado en el doc `pages` slug `seo-technical-audit` (es+en), inmediatamente
  despues de `serviceScopeCard`.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.5-auditor-destacado/48.5-CONTEXT.md
@.planning/phases/48.5-auditor-destacado/48.5-UI-SPEC.md
@.planning/phases/48.5-auditor-destacado/48.5-RESEARCH.md
@.planning/phases/48.5-auditor-destacado/48.5-01-SUMMARY.md

@src/components/StackHighlightCallout.tsx
@src/blocks/ToolStack/Component.tsx
@src/blocks/ServiceScopeCard/config.ts
@src/blocks/RelatedCaseStudyBlock/config.ts
@src/blocks/RelatedCaseStudyBlock/Component.tsx
@src/collections/Pages/index.ts
@src/blocks/blockRegistry.tsx
@src/lib/auditor.ts
@src/components/Prose.tsx
@src/components/ui/badge.tsx
@src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
@scripts/db/04-which-database.ts
@scripts/seed-phase48-5-home-auditor.ts
@messages/es.json
@messages/en.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generalizar StackHighlightCallout (gscHref/gscLabel -&gt; linkHref/linkLabel) sin regresion</name>
  <read_first>
    src/components/StackHighlightCallout.tsx, src/blocks/ToolStack/Component.tsx (call site actual,
    lineas 86-99), src/components/ui/badge.tsx
  </read_first>
  <files>src/components/StackHighlightCallout.tsx, src/blocks/ToolStack/Component.tsx</files>
  <action>
    En `src/components/StackHighlightCallout.tsx`: renombrar la firma de props de `gscHref`/`gscLabel`
    a `linkHref`/`linkLabel` (ambos siguen `string | undefined`), y agregar un tercer prop nuevo
    `linkOpensInNewTabLabel?: string`. El `<a>` renderizado sigue siendo `href={linkHref} target="_blank"
    rel="noopener"` (elemento, `target`, `rel` sin cambios — nunca `noreferrer`, nunca `sponsored`),
    pero ahora, cuando `linkOpensInNewTabLabel` esta presente, agrega
    `<span className="sr-only"> ({linkOpensInNewTabLabel})</span>` despues de `{linkLabel}`; cuando
    esta ausente (el caso del call site existente de GSC, que NO debe cambiar de comportamiento
    visible), no renderiza ningun `sr-only` extra — este proyecto NO retrofittea el aviso de "se
    abre en pestana nueva" a links `target="_blank"` preexistentes, solo a los 3 nuevos de esta fase
    (per 48.5-UI-SPEC.md, Outbound Link Contract). Actualizar el comentario de cabecera del archivo
    para reflejar que ahora hay 2+ callers no-afiliados (GSC y el auditor), no solo GSC. Actualizar
    el UNICO call site existente en `src/blocks/ToolStack/Component.tsx` (el callout
    `noCommissionPick`): renombrar `gscHref`/`gscLabel` a `linkHref`/`linkLabel` en esa llamada, SIN
    agregar `linkOpensInNewTabLabel` (preserva el comportamiento actual exacto, sin sr-only nuevo en
    el link de GSC). Confirmar con un grep repo-wide que no queda ninguna referencia a `gscHref` ni
    `gscLabel` fuera de este archivo y su call site.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; ! grep -rn "gscHref\|gscLabel" src/; npx tsc --noEmit; npm run dev > /tmp/dev-48-5-02a.log 2>&1 & echo $! > /tmp/dev-48-5-02a.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/stack > /dev/null 2>&1 && break; sleep 1; done; curl -s http://localhost:3000/stack | grep -q "search.google.com/search-console"; kill "$(cat /tmp/dev-48-5-02a.pid)" 2>/dev/null || true; echo RENAME_OK</automated>
    <fails_when>queda alguna referencia a gscHref/gscLabel en src/; tsc reporta errores; /stack deja de mostrar el link de GSC (no-commission callout) tras el rename; RENAME_OK no se imprime</fails_when>
  </verify>
  <done>StackHighlightCallout usa linkHref/linkLabel/linkOpensInNewTabLabel; el unico call site existente (no-commission pick en /stack) sigue funcionando identico, sin sr-only nuevo; tsc limpio.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bloque AuditorCallout (schema+migracion+registro+i18n) sobre StackHighlightCallout generalizado</name>
  <read_first>
    src/blocks/RelatedCaseStudyBlock/config.ts, src/blocks/RelatedCaseStudyBlock/Component.tsx
    (patron de bloque de un solo uso), src/blocks/ServiceScopeCard/config.ts (confirmar que NO se
    toca), src/components/StackHighlightCallout.tsx (ya generalizado por Task 1), src/lib/auditor.ts,
    src/collections/Pages/index.ts, src/blocks/blockRegistry.tsx
  </read_first>
  <precondition>node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts confirma que DATABASE_URI apunta al Postgres de Dokploy antes de generar/aplicar la migracion</precondition>
  <reversibility rating="reversible">AuditorCallout es un bloque nuevo de 2 campos editoriales sin contenido previo — una migracion aditiva mas si el shape cambia despues, sin perdida de datos.</reversibility>
  <files>
    src/blocks/AuditorCallout/config.ts, src/blocks/AuditorCallout/Component.tsx,
    src/collections/Pages/index.ts, src/blocks/blockRegistry.tsx, messages/es.json, messages/en.json,
    src/migrations/*_phase48_5_auditor_callout.ts
  </files>
  <action>
    Paso 1 — `src/blocks/AuditorCallout/config.ts`. `Block` de Payload, slug `auditorCallout`,
    interfaceName `AuditorCalloutBlock`, labels `{ singular: 'Auditor Callout', plural: 'Auditor
    Callout Blocks' }`. Campos: `heading` (text, localized, NO required — admin.description: "Vacio
    -> fallback i18n auditorCallout.heading"); `narrative` (textarea, localized, required —
    admin.description: "El pitch completo. Este bloque no tiene fila de stats separada, los 4 datos
    van tejidos en la oracion"). NO agregar ningun campo a `ServiceScopeCard` — este es
    deliberadamente un bloque nuevo de un solo uso, mismo patron que `RelatedCaseStudyBlock`, para no
    contaminar el admin de las otras 3 landings de servicio que comparten `ServiceScopeCard`.

    Paso 2 — `src/blocks/AuditorCallout/Component.tsx`. Server Component puro (sin 'use client').
    Recibe `heading`/`narrative` tipadas con `AuditorCalloutBlock` de `@/payload-types`. Usa
    `getTranslations('auditorCallout')`. Envuelve el ya-generalizado `StackHighlightCallout` pasando:
    `heading={heading || t('heading')}`, `narrative={narrative}`, `linkHref={AUDITOR_URL}` (import
    desde `@/lib/auditor`), `linkLabel={t('cta')}`, `linkOpensInNewTabLabel={t('opensInNewTab')}`. NO
    pasar `badge` — ese prop existe especificamente para marcar "no-commission pick" dentro de la
    taxonomia de afiliados de `/stack`; usarlo aca implicaria falsamente que este callout pertenece
    a esa taxonomia, cuando es producto propio de Juan (constraint dura de CONTEXT.md/FEAT-04).

    Paso 3 — Registro. En `src/collections/Pages/index.ts`: importar `AuditorCallout` desde
    `@/blocks/AuditorCallout/config` y agregarlo al array `blocks:` (junto a `AuditorHighlight`, sin
    reordenar los bloques existentes). En `src/blocks/blockRegistry.tsx`: importar
    `AuditorCalloutComponent` desde `@/blocks/AuditorCallout/Component` y agregar la entrada
    `auditorCallout: AuditorCalloutComponent`.

    Paso 4 — i18n. Agregar el namespace `auditorCallout` a `messages/es.json`/`messages/en.json` con
    las keys: `heading` (ES "También tengo un auditor automatizado, gratis" / EN "I also built a
    free automated auditor"), `cta` (ES "Ir al auditor" / EN "Go to the auditor"), `opensInNewTab`
    (ES "se abre en una pestaña nueva" / EN "opens in a new tab").

    Paso 5 — Migracion. Correr `node --env-file=.env node_modules/.bin/payload migrate:create`
    (nombre sugerido: `phase48_5_auditor_callout`). Leer el `.ts` generado completo: debe contener
    unicamente `CREATE TABLE`/`ADD CONSTRAINT`/`CREATE INDEX` para `pages_blocks_auditor_callout`,
    `pages_blocks_auditor_callout_locales`, y sus contrapartes `_pages_v_` — CERO `DROP`. Aplicar con
    `node --env-file=.env node_modules/.bin/payload migrate` (aditiva, sin aprobacion de Juan per
    Database Safety de CLAUDE.md). Correr
    `node --env-file=.env node_modules/.bin/payload generate:types`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; MIGFILE=$(ls src/migrations/*phase48_5_auditor_callout*.ts 2>/dev/null | head -1); test -n "$MIGFILE"; ! grep -qiE "DROP TABLE|DROP COLUMN|ALTER COLUMN.*DROP" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); grep -q "AuditorCalloutBlock" src/payload-types.ts; npx tsc --noEmit; echo BLOCK_OK</automated>
    <fails_when>el archivo de migracion no existe o contiene DROP; payload-types.ts no tiene AuditorCalloutBlock; tsc reporta errores</fails_when>
  </verify>
  <done>AuditorCallout registrado y migrado contra Postgres real; ServiceScopeCard sin tocar; tsc limpio.</done>
</task>

<!-- planner-region-allow: 'use client' -->
<!-- Task 2's action prose says "sin 'use client'" (a negative instruction, not a usage) in the
     same file Task 3's file-wide gate bans it from — no real conflict, both tasks agree this file
     must never contain the directive. -->
<task type="auto" tdd="false">
  <name>Task 3: Contenido real en la landing + verificacion end-to-end</name>
  <read_first>
    scripts/seed-phase48-5-home-auditor.ts (patron a replicar), scripts/verify-stack-page.ts (patron
    de verify script), src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
  </read_first>
  <files>scripts/seed-phase48-5-landing-auditor.ts, scripts/verify-phase48-5-landing-auditor.ts</files>
  <action>
    Paso 1 — Contenido. Crear `scripts/seed-phase48-5-landing-auditor.ts` (mismo patron que
    `scripts/seed-phase48-5-home-auditor.ts`: `LOCALES=['es','en'] as const`, `findByID({locale,
    depth:0})`, splice, reapplyIds, `payload.update({..., data:{content:{layout:...}, _status:
    'published'}})`). Antes de escribir, pasar la copy borrador por el skill humanizer calibrado
    contra `research/voice-sample-juan.md`. Ubicar el doc `pages` con `slug: 'seo-technical-audit'`;
    encontrar el indice del bloque `blockType === 'clientLogosBlock'` en `content.layout`; insertar
    (splice) el nuevo bloque `{ blockType: 'auditorCallout', heading, narrative }` en ese indice
    (queda inmediatamente despues de `serviceScopeCard` e inmediatamente antes de `clientLogosBlock`).
    Draft copy a humanizar: `heading` ES "También tengo un auditor automatizado, gratis" / EN "I
    also built a free automated auditor"; `narrative` ES "Si querés un primer diagnóstico antes de
    contratar la auditoría completa, corré mi auditor automatizado: reviso 29 checks técnicos en 5
    categorías, hasta 500 URLs por corrida, con Core Web Vitals vía PageSpeed Insights. Es gratis,
    con un límite honesto: 1 auditoría por semana por email verificado." / EN "If you want a first
    read before hiring the full audit, run my automated auditor: 29 technical checks across 5
    categories, up to 500 URLs per run, with Core Web Vitals via PageSpeed Insights. It's free, with
    an honest limit: 1 audit per week per verified email."

    Paso 2 — Verificacion. Crear `scripts/verify-phase48-5-landing-auditor.ts` (patron
    `scripts/verify-stack-page.ts`) que verifique contra `/servicios/seo-technical-audit` y
    `/en/services/seo-technical-audit`: (1) el heading y narrative renderizan con los 4 datos
    (29/5/500/CWV via PageSpeed Insights) tejidos en el texto; (2) el anchor hacia
    `auditor.juan-tech.com` tiene `rel="noopener"` unicamente (nunca `sponsored`, nunca `nofollow`),
    `target="_blank"`, y un `<span class="sr-only">` con el sufijo de nueva pestana; (3) NO aparece
    ningun `Badge`/chip de "no-commission" alrededor de este callout (buscar la clase de badge
    inmediatamente antes/despues del bloque de texto del auditor y confirmar ausencia); (4) el bloque
    aparece en el HTML en un indice posterior al heading de `ServiceScopeCard` ("Alcance de esta
    auditoría"/su equivalente en) y anterior al heading de `ClientLogosBlock` ("Clientes"), en ese
    orden exacto; (5) ningun href contiene "undefined". Terminar con `git diff --stat` vacio sobre
    `src/lib/sitemap-data.ts`, `src/lib/canonical.ts`, `src/lib/breadcrumbs.ts`, y grep confirmando
    CERO `'use client'` en `src/blocks/AuditorCallout/Component.tsx`.

    Run: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-5-landing-auditor.ts`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-5-landing-auditor.ts; npm run dev > /tmp/dev-48-5-02b.log 2>&1 & echo $! > /tmp/dev-48-5-02b.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/servicios/seo-technical-audit > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-phase48-5-landing-auditor.ts | tee /tmp/verify-48-5-02.log; grep -q "^PASS$" /tmp/verify-48-5-02.log; kill "$(cat /tmp/dev-48-5-02b.pid)" 2>/dev/null || true; git diff --stat src/lib/sitemap-data.ts src/lib/canonical.ts src/lib/breadcrumbs.ts | tee /tmp/diff-48-5-02.log; test ! -s /tmp/diff-48-5-02.log; ! grep -l "'use client'" src/blocks/AuditorCallout/Component.tsx; npx tsc --noEmit; echo LANDING_OK</automated>
    <fails_when>el seed script falla; verify-phase48-5-landing-auditor.ts imprime FAIL; el git diff de los 3 archivos protegidos no esta vacio; AuditorCallout/Component.tsx contiene 'use client'; tsc reporta errores</fails_when>
  </verify>
  <done>La landing de Auditoria SEO Tecnica enlaza al auditor en ambos locales, inmediatamente despues de ServiceScopeCard, sin badge ni rel=sponsored; canonical/hreflang/sitemap sin diff; cero JS de cliente nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Editor (Payload admin) -> Postgres | Contenido editorial de `AuditorCallout` entra por campos `text`/`textarea` sin rich text libre |
| Visitante -> auditor.juan-tech.com | Salida cross-subdominio desde un `<a>` nativo dentro de `StackHighlightCallout`, sin llamada de red desde este servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-48.5-06 | Tampering | AuditorCallout.narrative (campo editorial) | low | accept | React escapa por defecto; `Prose` (usado por `StackHighlightCallout`) no usa `dangerouslySetInnerHTML`, confirmado en 48.5-RESEARCH.md |
| T-48.5-07 | Tampering/Spoofing | CTA generalizado de StackHighlightCallout hacia auditor.juan-tech.com | medium | mitigate | `rel="noopener"` unico, verificado por Task 3's script; el rename de props (Task 1) no cambia el JSX del `<a>` renderizado para el call site existente de GSC |
| T-48.5-08 | Tampering (integridad de schema) | Migracion `phase48_5_auditor_callout` contra Postgres de Dokploy | medium | mitigate | Generada via `payload migrate:create`, leida completa antes de aplicar (confirmar CERO `DROP`), aplicada tras confirmar target DB — tabla nueva sin contenido previo, reversible sin perdida de datos |
| T-48.5-09 | Spoofing (bleed de taxonomia de afiliados) | AuditorCallout podria heredar visualmente el `badge` "no-commission pick" de StackHighlightCallout | medium | mitigate | El bloque `AuditorCallout` NUNCA pasa el prop `badge` — verificado por Task 3's script confirmando ausencia del chip |
| T-48.5-10 | Repudiation (regresion silenciosa) | Generalizar StackHighlightCallout podria romper el call site de GSC sin que nadie lo note | low | mitigate | Task 1 verifica explicitamente contra `/stack` en vivo que el link de GSC sigue renderizando correctamente tras el rename |

</threat_model>

<verification>
`npx tsc --noEmit` limpio en las 3 tareas. `/stack` no sufre regresion tras el rename de props
(verificado en Task 1). Migracion de `AuditorCallout` generada y confirmada aditiva antes de
aplicar. La landing de Auditoria SEO Tecnica renderiza el callout con contenido bilingue real,
correctamente posicionado, sin badge ni rel=sponsored.
</verification>

<success_criteria>
FEAT-03 (parcial, esta superficie): la landing de Auditoria SEO Tecnica enlaza al auditor en ambos
locales — cumplido y verificado. FEAT-02 (reforzado): los 4 datos verificables aparecen tambien en
esta narrativa. FEAT-04 (parcial, esta superficie): sin `rel="sponsored"`, sin badge de afiliado,
canonical/hreflang/sitemap sin diff, 0 KB JS nuevo — cumplido. FEAT-05 (parcial, esta superficie):
copy bilingue humanizada, honesta sobre el limite gratuito — cumplido.
</success_criteria>

<output>
Create `.planning/phases/48.5-auditor-destacado/48.5-02-SUMMARY.md` when done
</output>
