---
phase: 48.5-auditor-destacado
plan: 03
type: execute
wave: 3
depends_on: ["48.5-02"]
files_modified:
  - src/blocks/ToolStack/config.ts
  - src/blocks/ToolStack/Component.tsx
  - messages/es.json
  - messages/en.json
  - src/migrations/*_phase48_5_tool_stack_auditor_highlight.ts
  - src/migrations/*_phase48_5_tool_stack_auditor_highlight.json
  - src/migrations/index.ts
  - src/payload-types.ts
  - scripts/seed-phase48-5-stack-auditor.ts
  - scripts/verify-phase48-5-stack-auditor.ts
autonomous: true
requirements: [FEAT-02, FEAT-03, FEAT-04, FEAT-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "/stack enlaza al auditor en ambos locales via una tercera instancia de StackHighlightCallout, en el orden elegiriaHoy -> noCommissionPick -> auditorHighlight"
    - "El campo nuevo (auditorHighlight, group) es una columna nueva sobre la tabla de locales YA EXISTENTE de ToolStack (pages_blocks_tool_stack_locales), no una tabla nueva — ADD COLUMN unicamente"
    - "La 3ra instancia de StackHighlightCallout no lleva badge ni rel=sponsored; canonical/hreflang de /stack sin diff; cero JS de cliente nuevo"
    - "La narrative del auditor en /stack es bilingue, humanizada, y menciona los 4 datos verificables + el limite del plan gratuito"
    - "Verificacion corrida contra el dev server LOCAL (no https://juan-tech.com/stack, que hoy 404 porque Phase 48 no llego a master todavia)"
  artifacts:
    - "src/blocks/ToolStack/config.ts con el group field auditorHighlight (subcampo narrative)"
    - "migracion aditiva generada por payload migrate:create (ADD COLUMN auditor_highlight_narrative en pages_blocks_tool_stack_locales y su contraparte _pages_v_)"
  key_links:
    - "ToolStack/config.ts auditorHighlight.narrative -> ToolStackComponent.tsx 3ra StackHighlightCallout -> linkHref=AUDITOR_URL (src/lib/auditor.ts)"
    - "content.layout[toolStack] del doc pages slug=stack: elegiriaHoy callout, noCommissionPick callout, auditorHighlight callout (orden exacto de render)"
---

<objective>
Cerrar la tercera y ultima superficie de enlace de la fase: agregar el campo `auditorHighlight`
(group, un subcampo `narrative`) al bloque `ToolStack` ya existente, y renderizar una tercera
instancia de `StackHighlightCallout` (ya generalizado por el Plan 02) en `/stack`.

Purpose: `/stack` ya tiene el patron de callout secundario (`elegiríaHoy`/`noCommissionPick`); el
auditor cierra esa progresion (recomendaciones de experiencia -> pick sin comision -> algo que Juan
realmente construyo).
Output: `ToolStack` con el campo nuevo migrado, `/stack` renderizando 3 callouts en el orden
correcto, con el link al auditor conforme al Outbound Link Contract.
</objective>

## Artifacts this phase produces (this plan's share, cierra la fase)

- `src/blocks/ToolStack/config.ts` con el nuevo campo `auditorHighlight` (group, subcampo
  `narrative`) sobre el bloque existente de Phase 48.
- Migracion aditiva `phase48_5_tool_stack_auditor_highlight` (`ADD COLUMN` unicamente, sobre tabla
  ya existente).
- `ToolStackComponent.tsx` con la 3ra instancia de `StackHighlightCallout` (elegiríaHoy ->
  noCommissionPick -> auditorHighlight).
- Namespace i18n `stackPage` extendido (`auditorHeading`/`auditorCta`/`opensInNewTab`).
- `auditorHighlight.narrative` publicado en el doc `pages` slug `stack` (es+en), sin regresion sobre
  el contenido real de Phase 48.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.5-auditor-destacado/48.5-CONTEXT.md
@.planning/phases/48.5-auditor-destacado/48.5-UI-SPEC.md
@.planning/phases/48.5-auditor-destacado/48.5-RESEARCH.md
@.planning/phases/48.5-auditor-destacado/48.5-02-SUMMARY.md

@src/blocks/ToolStack/config.ts
@src/blocks/ToolStack/Component.tsx
@src/components/StackHighlightCallout.tsx
@src/lib/auditor.ts
@src/migrations/20260905_060823_phase48_tool_stack_block.ts
@scripts/db/04-which-database.ts
@scripts/seed-phase48-stack-content.ts
@scripts/verify-stack-page.ts
@messages/es.json
@messages/en.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Campo auditorHighlight (group) en ToolStack + migracion aditiva (ADD COLUMN)</name>
  <read_first>
    src/blocks/ToolStack/config.ts (campos top-level elegiriaHoy/noCommissionPick, lineas 154-171 —
    el nuevo campo va al mismo nivel de anidamiento, NO dentro de categoryGroups),
    src/migrations/20260905_060823_phase48_tool_stack_block.ts (nombres reales de columnas en
    pages_blocks_tool_stack_locales: intro, gear_intro, elegiria_hoy)
  </read_first>
  <precondition>node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts confirma que DATABASE_URI apunta al Postgres de Dokploy antes de generar/aplicar la migracion</precondition>
  <reversibility rating="reversible">Es una columna ADD COLUMN sobre una tabla existente sin backfill necesario (campo nuevo, sin dato previo que perder) — deshacerla despues es otra migracion aditiva, no una reescritura de contenido ya cargado (los 9 tools/13 gear items existentes de Phase 48 no se tocan).</reversibility>
  <files>src/blocks/ToolStack/config.ts</files>
  <action>
    En `src/blocks/ToolStack/config.ts`, agregar un tercer campo top-level DESPUES de
    `noCommissionPick` (mismo nivel que `elegiriaHoy`/`noCommissionPick`, NO dentro de
    `categoryGroups` — evita el problema de longitud de identificador de Postgres que ya forzo
    `dbName: 'groups'` en `categoryGroups`, comentario en lineas 32-38 del propio archivo):
    `auditorHighlight`, `type: 'group'`, con un unico subcampo `narrative` (`textarea`, `localized:
    true`, `required: true`, `admin.description`: "Feed de la 3ra instancia de StackHighlightCallout
    ('El auditor que construí'). Debe mencionar los 4 datos verificables (29 checks, 5 categorías,
    500 URLs, CWV) y el límite del plan gratuito."). NO agregar `dbName` a este group — es escalar,
    no un array, sin riesgo del limite de 63 caracteres.

    Migracion: correr `node --env-file=.env node_modules/.bin/payload migrate:create` (nombre
    sugerido: `phase48_5_tool_stack_auditor_highlight`). Leer el `.ts` generado completo: debe
    contener UNICAMENTE `ALTER TABLE "pages_blocks_tool_stack_locales" ADD COLUMN
    "auditor_highlight_narrative" varchar` (o el nombre de columna real que Payload genere — no
    asumir el nombre exacto de antemano, confirmarlo leyendo el archivo) y su contraparte sobre
    `_pages_v_blocks_tool_stack_locales` (la tabla de versiones/borradores) — CERO `DROP`, CERO
    `CREATE TABLE` nueva (es una columna sobre tablas YA EXISTENTES de Phase 48, no una tabla
    nueva). Aplicar con `node --env-file=.env node_modules/.bin/payload migrate` (aditiva, sin
    aprobacion de Juan per Database Safety de CLAUDE.md). Correr
    `node --env-file=.env node_modules/.bin/payload generate:types`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; MIGFILE=$(ls src/migrations/*phase48_5_tool_stack_auditor_highlight*.ts 2>/dev/null | head -1); test -n "$MIGFILE"; ! grep -qiE "DROP TABLE|DROP COLUMN|ALTER COLUMN.*DROP" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); ! grep -qi "CREATE TABLE" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); grep -qi "ADD COLUMN" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); grep -q "auditorHighlight" src/payload-types.ts; npx tsc --noEmit; echo COLUMN_OK</automated>
    <fails_when>el archivo de migracion no existe, contiene DROP, o contiene CREATE TABLE (deberia ser solo ADD COLUMN); payload-types.ts no referencia auditorHighlight; tsc reporta errores</fails_when>
  </verify>
  <done>ToolStack tiene el campo auditorHighlight.narrative migrado como columna nueva sobre la tabla de locales existente, confirmado por lectura directa del SQL generado (ADD COLUMN unicamente); tsc limpio.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 3ra instancia de StackHighlightCallout en ToolStackComponent + i18n</name>
  <read_first>
    src/blocks/ToolStack/Component.tsx (lineas 80-100, las 2 instancias existentes de
    StackHighlightCallout), src/components/StackHighlightCallout.tsx (ya generalizado por el Plan 02
    — linkHref/linkLabel/linkOpensInNewTabLabel), src/lib/auditor.ts
  </read_first>
  <files>src/blocks/ToolStack/Component.tsx, messages/es.json, messages/en.json</files>
  <action>
    En `src/blocks/ToolStack/Component.tsx`, agregar `auditorHighlight` a la desestructuracion de
    props del componente (viene del bloque `ToolStackBlockProps`). Despues del bloque
    `resolvedNoCommissionPick && (...)` existente (el 2do callout), agregar un tercer bloque
    condicional a `auditorHighlight?.narrative` con el mismo wrapper `<div className="mt-12">` que
    los otros 2, renderizando: `<StackHighlightCallout heading={t('auditorHeading')}
    narrative={auditorHighlight.narrative} linkHref={AUDITOR_URL} linkLabel={t('auditorCta')}
    linkOpensInNewTabLabel={t('opensInNewTab')} />` — importar `AUDITOR_URL` desde `@/lib/auditor`.
    NO pasar `badge` (mismo razonamiento que `AuditorCallout`: este no es un pick sin comision del
    sistema de afiliados, es producto propio). El orden final de render queda: `elegiriaHoy` ->
    `noCommissionPick` -> `auditorHighlight` (orden deliberado, ya fijado por el UI-SPEC:
    recomendaciones de experiencia, luego un pick imparcial, cerrando con algo que Juan construyo).

    i18n: agregar a `messages/es.json`/`messages/en.json`, dentro del namespace `stackPage` ya
    existente, las keys: `auditorHeading` (ES "El auditor que construí" / EN "The auditor I built"),
    `auditorCta` (ES "Ir al auditor" / EN "Go to the auditor"), `opensInNewTab` (ES "se abre en una
    pestaña nueva" / EN "opens in a new tab" — si `stackPage` ya tuviera esta key de otro uso previo,
    reusarla en vez de duplicarla).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npx tsc --noEmit; grep -q "auditorHeading" messages/es.json messages/en.json; grep -q "auditorCta" messages/es.json messages/en.json; echo COMPONENT_OK</automated>
    <fails_when>tsc reporta errores; falta alguna de las 2 keys nuevas en alguno de los 2 archivos de mensajes</fails_when>
  </verify>
  <done>ToolStackComponent.tsx renderiza 3 StackHighlightCallout en el orden correcto; i18n completo en ambos locales; tsc limpio.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Contenido real en /stack + verificacion end-to-end contra dev LOCAL</name>
  <read_first>
    scripts/seed-phase48-stack-content.ts (patron reapplyIds completo, especificamente el manejo del
    bloque toolStack — NO reescribir categoryGroups/gearItems, solo agregar auditorHighlight),
    scripts/verify-stack-page.ts (patron de verify script existente para /stack, extenderlo, no
    reemplazarlo)
  </read_first>
  <files>scripts/seed-phase48-5-stack-auditor.ts, scripts/verify-phase48-5-stack-auditor.ts</files>
  <action>
    Paso 1 — Contenido. Crear `scripts/seed-phase48-5-stack-auditor.ts`
    (`LOCALES=['es','en'] as const`). Antes de escribir, pasar la copy borrador por el skill
    humanizer calibrado contra `research/voice-sample-juan.md`. Para cada locale: `findByID({locale,
    depth:0})` el doc `pages` slug `stack`; ubicar el bloque cuyo `blockType === 'toolStack'` en
    `content.layout`; construir una copia de ESE bloque con TODOS sus campos existentes intactos
    (`intro`, `categoryGroups`, `gearIntro`, `gearItems`, `elegiriaHoy`, `noCommissionPick` — sin
    tocar ninguno, deben viajar exactamente como llegaron de `depth:0`, incluyendo relaciones como
    IDs crudos) mas el campo nuevo `auditorHighlight: { narrative }` con la copy humanizada de ese
    locale; reemplazar solo ese indice del array `content.layout`, dejando cualquier otro bloque del
    layout (Hero, etc.) exactamente como esta. Escribir con `payload.update({..., locale, data:
    {content:{layout:...}, _status:'published'}})` — `_status:'published'` explicito. NO hace falta
    reapplyIds cross-locale para el `toolStack` block en si (su `id` ya existe y no cambia, per
    `depth:0` trae el `id` real del bloque), pero SI hay que preservar el `id` del propio bloque
    `toolStack` en la escritura (viene incluido en el fetch, no generarlo de nuevo). Draft copy a
    humanizar: `narrative` ES "Además de recomendar herramientas de terceros, construí la mía: un
    auditor SEO que corre 29 checks en 5 categorías, hasta 500 URLs por auditoría, con Core Web
    Vitals vía PageSpeed Insights. Lo uso en mis propios proyectos y en los de mis clientes. El plan
    gratuito permite 1 auditoría por semana por email verificado." / EN "Besides recommending other
    people's tools, I built my own: an SEO auditor that runs 29 checks across 5 categories, up to
    500 URLs per audit, with Core Web Vitals via PageSpeed Insights. I use it on my own projects and
    my clients'. The free tier allows 1 audit per week per verified email."

    Paso 2 — Verificacion. Crear `scripts/verify-phase48-5-stack-auditor.ts` (patron
    `scripts/verify-stack-page.ts`, corre contra `VERIFY_BASE_URL ?? 'http://localhost:3000'` — NUNCA
    contra `https://juan-tech.com`, que hoy 404 en `/stack` porque Phase 48 no llego a `master`
    todavia, per 48.5-RESEARCH.md Pitfall 3) verificando `/stack` y `/en/stack`: (1) aparecen 3
    callouts, y el heading "El auditor que construí"/"The auditor I built" aparece DESPUES en el
    documento que el heading de `noCommissionPick` ("Mi recomendación sin comisión"/su equivalente
    en) y ese a su vez despues de "Qué elegiría hoy..."; (2) el anchor hacia `auditor.juan-tech.com`
    tiene `rel="noopener"` unicamente, `target="_blank"`, y `<span class="sr-only">` con el sufijo de
    nueva pestana; (3) NO hay ningun chip/badge de "no-commission" alrededor de este 3er callout; (4)
    la narrative menciona 29, 5, 500 y CWV; (5) ningun href contiene "undefined"; (6) el resto de
    `/stack` (los 9 tools + 13 gear items de Phase 48) sigue presente sin cambios — reusar los
    checks de `verify-stack-page.ts` (DinoRANK CTA -> `/go/dinorank`, disclosure antes del primer
    link sponsored) como regresion. Terminar con `git diff --stat` vacio sobre
    `src/lib/sitemap-data.ts`, `src/lib/canonical.ts`, `src/lib/breadcrumbs.ts`, y grep confirmando
    que `src/blocks/ToolStack/Component.tsx` sigue sin `'use client'`.

    Run: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-5-stack-auditor.ts`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-5-stack-auditor.ts; npm run dev > /tmp/dev-48-5-03.log 2>&1 & echo $! > /tmp/dev-48-5-03.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/stack > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-stack-page.ts | tee /tmp/verify-stack-regr-48-5-03.log; grep -q "^PASS$" /tmp/verify-stack-regr-48-5-03.log; node --env-file=.env node_modules/.bin/tsx scripts/verify-phase48-5-stack-auditor.ts | tee /tmp/verify-48-5-03.log; grep -q "^PASS$" /tmp/verify-48-5-03.log; kill "$(cat /tmp/dev-48-5-03.pid)" 2>/dev/null || true; git diff --stat src/lib/sitemap-data.ts src/lib/canonical.ts src/lib/breadcrumbs.ts | tee /tmp/diff-48-5-03.log; test ! -s /tmp/diff-48-5-03.log; ! grep -l "'use client'" src/blocks/ToolStack/Component.tsx; npx tsc --noEmit; echo STACK_OK</automated>
    <fails_when>el seed script falla; verify-stack-page.ts (regresion) o verify-phase48-5-stack-auditor.ts imprimen FAIL; el git diff de los 3 archivos protegidos no esta vacio; ToolStack/Component.tsx contiene 'use client'; tsc reporta errores</fails_when>
  </verify>
  <done>/stack renderiza 3 callouts en el orden correcto contra el dev server local, con el link al auditor conforme al Outbound Link Contract, sin regresion en el contenido de Phase 48; canonical/hreflang/sitemap sin diff; cero JS de cliente nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Editor (Payload admin) -> Postgres | Contenido editorial de `auditorHighlight.narrative` entra por un campo `textarea` sin rich text libre, sobre una tabla ya existente de Phase 48 |
| Visitante -> auditor.juan-tech.com | Salida cross-subdominio desde un `<a>` nativo dentro de `StackHighlightCallout`, sin llamada de red desde este servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-48.5-11 | Tampering | auditorHighlight.narrative (campo editorial) | low | accept | React escapa por defecto; `Prose` sin `dangerouslySetInnerHTML` |
| T-48.5-12 | Tampering/Spoofing | 3ra instancia de StackHighlightCallout hacia auditor.juan-tech.com | medium | mitigate | `rel="noopener"` unico, verificado por Task 3's script |
| T-48.5-13 | Tampering (integridad de schema, mayor severidad que Plan 01/02) | Migracion `phase48_5_tool_stack_auditor_highlight` — ADD COLUMN sobre `pages_blocks_tool_stack_locales`, tabla que YA tiene contenido real de Phase 48 (9 tools, 13 gear items, en ambos locales) | high | mitigate | Confirmar por lectura directa que el SQL generado es EXCLUSIVAMENTE `ADD COLUMN` (nunca `ALTER COLUMN` sobre `intro`/`gear_intro`/`elegiria_hoy`, nunca `DROP`) antes de aplicar contra Dokploy; el precedente exacto de riesgo es el incidente 2026-07-12 (DROP COLUMN sin backfill en una migracion de localizacion) — este caso es estructuralmente distinto (columna nueva, sin backfill necesario) pero exige la misma disciplina de lectura previa |
| T-48.5-14 | Spoofing (bleed de taxonomia de afiliados) | La 3ra instancia podria heredar visualmente el badge "no-commission pick" | medium | mitigate | El call site nunca pasa `badge` para esta instancia — verificado por Task 3's script |
| T-48.5-15 | Repudiation (regresion silenciosa sobre contenido real) | El seed script de Task 3 reescribe el bloque `toolStack` completo por locale; un error de mapeo podria clobber `categoryGroups`/`gearItems` ya cargados | high | mitigate | El script preserva TODOS los campos existentes del bloque leidos con `depth:0` sin remapearlos, solo agrega `auditorHighlight`; Task 3 corre `verify-stack-page.ts` (el verify de Phase 48) como chequeo de regresion explicito antes de dar la tarea por cerrada |

</threat_model>

<verification>
`npx tsc --noEmit` limpio en las 3 tareas. Migracion confirmada `ADD COLUMN` unicamente (nunca
`CREATE TABLE`, nunca `DROP`) antes de aplicar. `/stack` (dev local) renderiza 3 callouts en el
orden correcto, sin regresion sobre el contenido real de Phase 48 (verificado con el propio
`verify-stack-page.ts` de esa fase).
</verification>

<success_criteria>
FEAT-03 (completa la cobertura de la fase): `/stack` enlaza al auditor en ambos locales. FEAT-02
(reforzado): los 4 datos verificables aparecen tambien en esta narrativa. FEAT-04 (completa la
fase): sin `rel="sponsored"` en ninguna de las 3 superficies, canonical/hreflang/sitemap sin diff
en las 3 fases del milestone, 0 KB JS de cliente agregado en total. FEAT-05 (completa la fase):
copy bilingue humanizada, honesta sobre el limite gratuito, en las 3 superficies.
</success_criteria>

<output>
Create `.planning/phases/48.5-auditor-destacado/48.5-03-SUMMARY.md` when done
</output>
