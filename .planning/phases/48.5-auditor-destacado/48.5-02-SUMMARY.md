---
phase: 48.5-auditor-destacado
plan: "02"
subsystem: ui
tags: [payload, nextjs, blocks, i18n, postgres-migration, seo, accessibility]

requires:
  - phase: "48.5-01"
    provides: "src/lib/auditor.ts (AUDITOR_URL/AUDITOR_STATS), patron de link outbound sr-only, bloque AuditorHighlight en Home"
provides:
  - "StackHighlightCallout generalizado (linkHref/linkLabel/linkOpensInNewTabLabel), reusable por cualquier caller no-afiliado"
  - "Bloque Payload AuditorCallout (config + Component) registrado en Pages/blockRegistry, de un solo uso para la landing seo-technical-audit"
  - "Namespace i18n auditorCallout en messages/es.json y en.json"
  - "Migracion aditiva phase48_5_auditor_callout aplicada contra Postgres real de Dokploy"
  - "Contenido real bilingue publicado en la landing Auditoria SEO Tecnica, entre ServiceScopeCard y ClientLogosBlock"
affects: ["48.5-03"]

actuals:
  tokens: 6500
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Generalizacion de props de un componente compartido (gscHref/gscLabel -> linkHref/linkLabel) preservando el comportamiento exacto del call site existente cuando el prop opcional nuevo no se pasa"
    - "Bloque Payload de un solo uso (mismo patron que RelatedCaseStudyBlock) para no contaminar el admin de landings hermanas que comparten un bloque generico (ServiceScopeCard)"
    - "id-reuse discipline entre locales, reutilizada identica a 48.5-01, ahora sobre el doc seo-technical-audit"

key-files:
  created:
    - src/blocks/AuditorCallout/config.ts
    - src/blocks/AuditorCallout/Component.tsx
    - src/migrations/20260907_040039_phase48_5_auditor_callout.ts
    - scripts/seed-phase48-5-landing-auditor.ts
    - scripts/verify-phase48-5-landing-auditor.ts
  modified:
    - src/components/StackHighlightCallout.tsx
    - src/blocks/ToolStack/Component.tsx
    - src/collections/Pages/index.ts
    - src/blocks/blockRegistry.tsx
    - messages/es.json
    - messages/en.json
    - src/migrations/index.ts
    - src/payload-types.ts

key-decisions:
  - "Worktree resincronizado por fast-forward a docs/seo-handoff antes de empezar: el worktree se creo desde un commit anterior a 48.5-01 (mergeado ya en esa rama), sin commits propios que perder — mismo patron ya documentado en 48.5-01-SUMMARY.md"
  - "node_modules instalado via npm install (no symlinkeado) porque el worktree no tenia node_modules ni lockfile compartido resoluble en este arranque; .env copiado (gitignored, no viaja con git worktree add)"
  - "Tunel SSH recreado desde cero a mitad de Task 3 tras degradarse (Connection terminated due to connection timeout) — mismo riesgo operativo ya documentado en 48.5-01-SUMMARY.md como conocido, no investigado a fondo, resuelto recreando tunel+relay"
  - "Copy de la landing reescrita a Spanish neutral (sin voceo) en vez del draft literal del plan (que usaba 'querés'/'corré') — CLAUDE.md exige neutral Spanish sin voceo nunca; se preservaron los 4 datos verificables y el tono de research/voice-sample-juan.md"

requirements-completed: [FEAT-02, FEAT-03, FEAT-04, FEAT-05]

coverage:
  - id: D1
    description: "StackHighlightCallout generalizado a linkHref/linkLabel/linkOpensInNewTabLabel sin regresion en su unico call site existente (no-commission pick en /stack)"
    requirement: "FEAT-04"
    verification:
      - kind: automated_ui
        ref: "grep repo-wide sin gscHref/gscLabel + curl contra /stack (dev server real, Postgres real de Dokploy) confirmando el link de GSC sigue renderizando identico"
        status: pass
    human_judgment: false
  - id: D2
    description: "La landing de Auditoria SEO Tecnica enlaza al auditor en ambos locales, inmediatamente despues de ServiceScopeCard y antes de ClientLogosBlock"
    requirement: "FEAT-03"
    verification:
      - kind: automated_ui
        ref: "scripts/verify-phase48-5-landing-auditor.ts (PASS, ambos locales) -- chequeo de orden exacto entre los 3 headings"
        status: pass
    human_judgment: false
  - id: D3
    description: "Los 4 datos verificables (29 checks, 5 categorias, 500 URLs, CWV via PageSpeed Insights) tejidos en la narrativa del callout, sin fila de stats separada"
    requirement: "FEAT-02"
    verification:
      - kind: automated_ui
        ref: "scripts/verify-phase48-5-landing-auditor.ts (chequeo de ventana de texto alrededor del heading del callout, ambos locales)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Contrato de link (rel=noopener unico, nunca sponsored/nofollow, target=_blank, sr-only de nueva pestana), sin badge de no-commission, cero JS de cliente nuevo, canonical/breadcrumbs/sitemap sin diff"
    requirement: "FEAT-04"
    verification:
      - kind: automated_ui
        ref: "scripts/verify-phase48-5-landing-auditor.ts + grep de 'use client' + git diff --stat vacio sobre sitemap-data.ts/canonical.ts/breadcrumbs.ts"
        status: pass
    human_judgment: false
  - id: D5
    description: "Copy bilingue humanizada, neutral Spanish (sin voceo), honesta sobre el limite gratuito"
    requirement: "FEAT-05"
    verification:
      - kind: manual_procedural
        ref: "Copy reescrita desde el draft del plan para eliminar voceo y calibrada contra research/voice-sample-juan.md; no existe un test automatizado de 'suena humano'"
        status: pass
    human_judgment: true
    rationale: "La calidad de voz/tono y el registro neutral son juicio editorial -- el script automatizado solo confirma que el texto EXISTE, es visible y contiene los 4 datos, no que 'suene como Juan'. Juan deberia dar un vistazo rapido al copy renderizado."

duration: 50min
completed: 2026-09-07
status: complete
---

# Phase 48.5 Plan 02: Auditoría SEO Técnica landing — AuditorCallout Summary

**`StackHighlightCallout` generalizado a `linkHref`/`linkLabel`/`linkOpensInNewTabLabel` y nuevo bloque `AuditorCallout` (schema, migración aditiva, registro, i18n, contenido real bilingüe) publicado en la landing "Auditoría SEO Técnica", inmediatamente después de `ServiceScopeCard`.**

## Performance

- **Duration:** ~50 min (incluye resincronizar el worktree, `npm install`, y recrear el túnel SSH una vez a mitad de Task 3)
- **Tasks:** 3
- **Files modified:** 13 (5 nuevos, 8 editados; excluye el snapshot JSON de la migración, autogenerado por `payload migrate:create`)

## Accomplishments

- `StackHighlightCallout` generalizado: `gscHref`/`gscLabel` renombrados a `linkHref`/`linkLabel`, con un tercer prop opcional `linkOpensInNewTabLabel` que agrega un sufijo `sr-only` solo cuando se pasa explícitamente (el call site de GSC en `/stack` no lo pasa, así que su comportamiento visible queda idéntico). Verificado sin regresión contra el dev server real conectado al Postgres real de Dokploy.
- Bloque Payload `AuditorCallout` (`config.ts` + `Component.tsx`) construido como bloque de un solo uso (mismo patrón que `RelatedCaseStudyBlock`), envolviendo el `StackHighlightCallout` ya generalizado — deliberadamente sin campo en `ServiceScopeCard` (compartido por las otras 3 landings de servicio) y sin prop `badge` (reservado a la taxonomía de afiliados de `/stack`).
- Migración aditiva `phase48_5_auditor_callout` (únicamente `CREATE TABLE`/`ADD CONSTRAINT`/`CREATE INDEX`, confirmada por lectura directa del `up()` antes de aplicar) generada y aplicada contra el Postgres real de Dokploy vía túnel SSH, tras confirmar el target DB con `scripts/db/04-which-database.ts`.
- Contenido real bilingüe publicado en el doc `seo-technical-audit` (es+en), insertado exactamente entre `serviceScopeCard` y `clientLogosBlock` — usando la misma fila de bloque para ambos locales (id-reuse discipline). Copy reescrita a español neutral (sin voceo) tejiendo los 4 datos verificables (29 checks, 5 categorías, 500 URLs, Core Web Vitals vía PageSpeed Insights).
- Barrido de aceptación automatizado (`scripts/verify-phase48-5-landing-auditor.ts`) confirma contra el dev server real: heading + narrativa con los 4 datos en contexto, contrato de link (`rel="noopener"` único, nunca sponsored/nofollow, `target="_blank"`, sufijo `sr-only` de nueva pestaña), ausencia de badge/chip de no-commission, orden exacto entre `ServiceScopeCard` → `AuditorCallout` → `ClientLogosBlock`, y cero hrefs con "undefined" — PASS en ambos locales.

## Task Commits

Cada tarea se comiteó atómicamente:

1. **Task 1: Generalizar StackHighlightCallout** - `21e29e9` (feat)
2. **Task 2: Bloque AuditorCallout (schema+migración+registro+i18n)** - `d5314a2` (feat)
3. **Task 3: Contenido real en la landing + verificación end-to-end** - `701e44c` (test)

## Files Created/Modified

- `src/components/StackHighlightCallout.tsx` - Props generalizados a `linkHref`/`linkLabel`/`linkOpensInNewTabLabel`
- `src/blocks/ToolStack/Component.tsx` - Call site del no-commission pick actualizado al nuevo nombre de props
- `src/blocks/AuditorCallout/config.ts` - Bloque Payload de un solo uso (heading opcional, narrative requerido)
- `src/blocks/AuditorCallout/Component.tsx` - Server Component: envuelve `StackHighlightCallout` con `AUDITOR_URL`
- `src/collections/Pages/index.ts` - Registro del bloque en el array `blocks`
- `src/blocks/blockRegistry.tsx` - Registro del componente en `blockComponents`
- `messages/es.json` / `messages/en.json` - Namespace `auditorCallout` (heading fallback, cta, opensInNewTab)
- `src/migrations/20260907_040039_phase48_5_auditor_callout.ts` (+`.json`) - Migración aditiva
- `src/migrations/index.ts` - Registro de la migración
- `src/payload-types.ts` - Regenerado con `AuditorCalloutBlock`
- `scripts/seed-phase48-5-landing-auditor.ts` - Contenido real bilingüe en la landing
- `scripts/verify-phase48-5-landing-auditor.ts` - Barrido de aceptación automatizado

## Decisions Made

- **Worktree resincronizado por fast-forward.** El worktree se había creado desde un commit anterior a `docs/seo-handoff` (que ya incluía todo 48.5-01). Confirmado que la rama del worktree no tenía commits propios y era ancestro directo, se hizo `git merge --ff-only docs/seo-handoff` — operación segura, sin pérdida de trabajo posible.
- **`node_modules` instalado con `npm install`, `.env` copiado.** Ninguno de los dos viaja con `git worktree add` (gitignored). `npm install` corrió limpio (805 paquetes) contra el `package-lock.json` del worktree.
- **Túnel SSH recreado una vez a mitad de Task 3.** El túnel abierto para Task 2 se degradó (`Connection terminated due to connection timeout`) al iniciar el dev server para Task 3 — mismo riesgo operativo ya documentado en 48.5-01-SUMMARY.md como conocido y no resuelto de raíz. Se recreó el túnel y el relay socat desde cero (`scripts/db/tunnel.sh`), se re-confirmó el target DB, y el resto de Task 3 corrió sin incidentes.
- **Copy reescrita a español neutral, sin voceo.** El draft de copy del plan (`48.5-02-PLAN.md` Task 3) usaba formas de voceo rioplatense ("querés", "corré"), que contradicen la instrucción global de Juan de hablar siempre en español neutral sin voceo. Se reescribió la narrativa preservando los 4 datos verificables y el tono directo/técnico de `research/voice-sample-juan.md`, evitando la segunda persona conjugada en voceo. Documentado como deviation (CLAUDE.md enforcement, ver abajo).

## Deviations from Plan

### Auto-fixed Issues

**1. [CLAUDE.md enforcement] Copy de la landing reescrita para eliminar voceo**
- **Found during:** Task 3, Paso 1 (antes de escribir el seed script)
- **Issue:** El draft `narrative` en `48.5-02-PLAN.md` usaba formas de voceo rioplatense ("Si querés...", "corré mi auditor automatizado") — CLAUDE.md global de Juan exige "neutral Spanish. No voceo nunca" para cualquier output en español de este proyecto, y el resto del contenido ya publicado en el sitio (ej. `scripts/seed-phase48-stack-content.ts`) tampoco usa voceo.
- **Fix:** Reescrita la narrativa completa en ES a español neutral, sin segunda persona conjugada en voceo, preservando los 4 datos verificables y calibrando el ritmo/vocabulario contra `research/voice-sample-juan.md` (frases largas encadenadas por comas, primera persona, sin em dash).
- **Files modified:** scripts/seed-phase48-5-landing-auditor.ts (la copy nunca llegó a escribirse en la DB con voceo — se corrigió antes de correr el seed)
- **Verification:** `scripts/verify-phase48-5-landing-auditor.ts` PASS confirmando los 4 datos siguen presentes; lectura manual de la copy final confirmando ausencia de "vos"/"querés"/"corré" u otras formas de voceo.
- **Committed in:** `701e44c` (Task 3)

---

**Total deviations:** 1 auto-fixed (corrección de registro lingüístico per CLAUDE.md, sin impacto en el contrato de datos/link del bloque)
**Impact on plan:** Cero scope creep — misma estructura y los mismos 4 datos verificables del plan original, solo cambia el registro gramatical de la copy en español.

## Issues Encountered

- **Túnel SSH degradado a mitad de ejecución** (ver "Decisions Made" arriba) — mismo riesgo operativo ya conocido de 48.5-01, resuelto recreando el túnel desde cero sin pérdida de datos ni reintentos fallidos de migración (la migración de Task 2 ya había aplicado limpio antes de que el túnel se degradara).

## User Setup Required

None - no se requiere configuración externa. La única pieza operativa (túnel SSH + credencial de Dokploy) fue manejada end-to-end por el ejecutor y cerrada al finalizar (túnel y relay remoto eliminados, verificado sin procesos residuales).

## Next Phase Readiness

- `StackHighlightCallout` generalizado queda listo para su tercera instancia en Plan 03 (`/stack`), que puede pasar `linkHref`/`linkLabel`/`linkOpensInNewTabLabel` directamente sin más cambios al componente.
- `src/lib/auditor.ts` (de 48.5-01) y el patrón de bloque de un solo uso (`AuditorCallout`, mismo patrón para lo que Plan 03 necesite en `ToolStack/config.ts`) quedan como precedente reusable.
- Sin bloqueos. `git diff` vacío confirmado sobre los 3 archivos protegidos de SEO técnico (`sitemap-data.ts`/`canonical.ts`/`breadcrumbs.ts`).

---
*Phase: 48.5-auditor-destacado*
*Completed: 2026-09-07*

## Self-Check: PASSED

- Todos los archivos creados verificados con `[ -f ... ]`: `src/blocks/AuditorCallout/config.ts`, `src/blocks/AuditorCallout/Component.tsx`, `src/migrations/20260907_040039_phase48_5_auditor_callout.ts`, `scripts/seed-phase48-5-landing-auditor.ts`, `scripts/verify-phase48-5-landing-auditor.ts` — todos `FOUND`.
- Los 3 commits de tarea (`21e29e9`, `d5314a2`, `701e44c`) confirmados presentes en `git log`.
