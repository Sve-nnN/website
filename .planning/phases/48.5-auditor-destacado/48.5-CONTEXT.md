# Phase 48.5: Auditor Destacado - Context

**Gathered:** 2026-09-05
**Status:** Ready for planning
**Mode:** Smart discuss (autonomous) — 1 área presentada, aceptada por Juan sin cambios

<domain>
## Phase Boundary

`auditor.juan-tech.com` — herramienta propia de Juan, en producción, con usuarios reales — deja de estar escondida: sección propia en Home, enlaces desde la landing de Auditoría SEO Técnica y desde `/stack` (Phase 48, ya construida). Es producto propio, no afiliación: sin `rel="sponsored"`, sin colección `affiliate-links`, sin `/go/`, sin disclosure.

Queda fuera: monetizar el auditor (v2.2), cualquier cambio al propio repo del auditor, links desde otras landings de servicio más allá de Auditoría SEO Técnica y `/stack`.

</domain>

<decisions>
## Alcance de enlaces
- Landings enlazadas: **Auditoría SEO Técnica** (la relevante de verdad, per ROADMAP) y `/stack` (ya construida en Phase 48). Ninguna otra landing de servicio — evita diluir el link sin aportar contexto real.

### Claude's Discretion
- Estructura visual exacta de la sección de Home (siguiendo el patrón de bloque de `ServicesShowcase`, per FEAT-01) — layout, iconografía, orden respecto a otras secciones de Home.
- Redacción exacta de la copy bilingüe (debe ser honesta sobre el límite gratuito: 1 auditoría/semana por email verificado) — a definir en UI-SPEC/planning.
- Dónde exactamente en la landing de Auditoría SEO Técnica y en `/stack` va el link (inline en la narrativa vs. bloque separado).

</decisions>

<code_context>
## Existing Code Insights

- `src/blocks/ServicesShowcase` (o equivalente) — patrón de bloque de referencia para la nueva sección de Home (FEAT-01 lo exige explícitamente).
- `packages/quota/src/canRunAudit.ts` (repo del auditor, no de este monorepo — solo referencia para verificar los números): `FREE_WEEKLY_LIMIT = 1`, `FREE_URL_LIMIT = 500`, comentario "Free-tier limits (v1, no billing — see PRD deferred: PAY in v2)" — confirma que la copy sobre el plan gratuito debe ser honesta y verificable.
- Landing de "Auditoría SEO Técnica" — ruta real a confirmar en research (`/servicios/seo-technical-audit` según las rutas ya vistas en Phase 48).
- `/stack` (Phase 48) — ya tiene el patrón de card/sección; el auditor no encaja como "tool" de afiliado (no tiene AffiliateLink), necesita su propio tratamiento sin `rel="sponsored"`.

</code_context>

<specifics>
## Specific Ideas

- Datos concretos y verificables a exponer (FEAT-02, ya fijados por el ROADMAP, no inventar otros): 29 checks, 5 categorías, hasta 500 URLs por auditoría, Core Web Vitals vía PageSpeed Insights.
- Constraint dura: cero JavaScript de cliente agregado — mismo presupuesto que el resto del milestone.
- Constraint dura: canonical y hreflang de las páginas tocadas (Home, landing de Auditoría SEO Técnica, `/stack`) deben quedar idénticos al estado antes de esta fase.

</specifics>

<deferred>
## Deferred Ideas

- Monetizar el auditor — v2.2, fuera de este milestone.
- Enlazar el auditor desde otras landings de servicio (ej. SEO Consulting) — decisión explícita de Juan de no ampliar el alcance.

</deferred>
