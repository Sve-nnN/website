---
phase: 08-shadcn-primitives-global-chrome
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/SiteHeader.tsx
  - src/components/SiteFooter.tsx
  - scripts/smoke-check-phase8.mjs
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "SiteHeader shows a clear visual hierarchy: logo/wordmark, nav links with an accent-colored active/hover indicator, and the primary CTA button visually dominant over secondary nav links"
    - "SiteFooter groups columns/social/legal with clear visual separation (a token-driven divider, not a hardcoded white/10 border) and consistent spacing per the UI-SPEC scale"
    - "All 16 Payload blocks still render without errors across at least one representative page per block type, verified by an automated smoke check against a running dev server"
    - "Zero diffs in any src/blocks/*/config.ts file or src/payload-types.ts as a result of this phase (both 08-01 and 08-02)"
  artifacts:
    - path: src/components/SiteHeader.tsx
      provides: "restyled header consuming the refined NavigationMenu/Button/Sheet primitives from 08-01, with a token-driven active nav-link indicator"
    - path: src/components/SiteFooter.tsx
      provides: "restyled footer using the refined Separator primitive from 08-01 for its section dividers, replacing the hardcoded border-white/10"
    - path: scripts/smoke-check-phase8.mjs
      provides: "automated curl-based smoke check across representative routes for all 16 block types, exits non-zero on any non-200 or error-boundary response"
  key_links:
    - from: src/components/SiteFooter.tsx
      to: src/components/ui/separator.tsx
      via: "<Separator /> import replacing raw border-t border-white/10"
      pattern: "import.*Separator.*from.*ui/separator"
    - from: scripts/smoke-check-phase8.mjs
      to: "http://localhost:3000/{route}"
      via: "fetch() against next dev"
      pattern: "fetch\\(.*localhost:3000"
---

<objective>
Restyle `SiteHeader`/`SiteFooter` for clear editorial-tech visual hierarchy (UI-05), consuming the token-refined primitives from `08-01-PLAN.md`, then close out the phase with the two hard-gate success criteria from ROADMAP.md: zero `config.ts`/`payload-types.ts` diffs (#3) and a smoke-tested confirmation that all 16 blocks still render (#4).

Purpose: header/footer are the highest-visibility, lowest-content-risk surface (no per-page dynamic content) — the safe second target after primitives per `RESEARCH.md`'s "primitives-then-chrome" build order. The verification task exists because #3/#4 are hard technical gates, not visual judgment calls — they need an automated, repeatable check, not a one-time manual glance.

Output: 2 restyled chrome components + 1 new smoke-check script. Zero diffs in `src/blocks/*/config.ts`/`src/payload-types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-shadcn-primitives-global-chrome/08-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/phases/08-shadcn-primitives-global-chrome/08-01-SUMMARY.md

<interfaces>
<!-- Refined primitives from 08-01, ready to consume as-is — no further prop/API changes, only styling landed. -->

From `src/components/ui/separator.tsx` (unchanged export surface):
```
export { Separator } // <Separator orientation="horizontal" className="..." />
```

From `src/components/ui/navigation-menu.tsx` (unchanged export surface, styling refined in 08-01):
```
export { NavigationMenu, NavigationMenuList, NavigationMenuItem, NavigationMenuLink, navigationMenuTriggerStyle, ... }
```

Both `SiteHeader.tsx` and `SiteFooter.tsx` are async Server Components reading `header`/`footer` Payload globals via `payload.findGlobal()` — no client-side state, no new props needed. Current structure (verified by direct read): `SiteHeader` renders `logo`/`navItems`/`ctaButton` from the `header` global via `NavigationMenu`+`Button`+`Sheet` (mobile); `SiteFooter` renders `columns`/`socialLinks`/`legalLinks`/`copyrightText` from the `footer` global, with a raw `border-t border-white/10` divider between the column grid and the social/legal row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SiteHeader and SiteFooter for editorial-tech hierarchy</name>
  <files>src/components/SiteHeader.tsx, src/components/SiteFooter.tsx</files>
  <action>
Per D (UI-05) and `05-UI-SPEC.md`'s reserved accent usage ("active nav-link underline/indicator" is one of the explicit accent-color use cases), refine both components' visual hierarchy without touching data-fetching logic, prop signatures, or the Payload global schema they read from.

`SiteHeader.tsx`: add a `shadow-md` to the `<header>` root (elevation separating it from page content, since it's `sticky top-0 z-50` — a flat sticky header with no shadow reads as visually unanchored) and `transition-shadow duration-base ease-standard`. On each `NavigationMenuLink`/`CMSLink` nav item, add an accent-colored bottom-border hover/focus indicator: wrap the existing `text-body` className with `relative pb-1 border-b-2 border-transparent hover:border-primary focus-visible:border-primary transition-colors duration-fast ease-out` (CSS-only, no active-route JS logic needed — this is a hover/focus indicator per the UI-SPEC contract, not an active-route highlighter, since no route-matching logic exists in this Server Component today). Keep the CTA `<Button asChild>` exactly as-is (it already inherits 08-01's refined button treatment automatically). Do not change the `header.navItems`/`header.ctaButton`/`header.logo` data shape or the `payload.findGlobal` call.

`SiteFooter.tsx`: replace the hardcoded `border-t border-white/10` divider (a non-token color, invisible in the audit trail of "what changed when tokens changed") with `<Separator className="opacity-30" />` imported from `@/components/ui/separator` — this both fixes the hardcoded-color anti-pattern and makes the footer consume a refined 08-01 primitive per the phase's own "chrome consumes primitives" premise. Strengthen column-title hierarchy: change `<h3 className="text-label mb-3">` to `<h3 className="text-label mb-3 uppercase tracking-wide text-secondary-foreground/70">` (label-role text per UI-SPEC Typography table, now visually distinct from the link list below it via letter-spacing + reduced opacity, not a new size token). Increase the gap between the column grid and the social/legal row from `mt-10`/`pt-6` to `mt-12`/`pt-8` (rounds to the UI-SPEC `xl`=32px spacing token instead of the current off-scale `mt-10`=40px/`pt-6`=24px mismatch — both become 4px-multiple-aligned `mt-12`=48px `pt-8`=32px, hitting the `2xl`/`xl` tokens from the Spacing Scale table). Do not change the `footer.columns`/`footer.socialLinks`/`footer.legalLinks`/`footer.copyrightText` data shape or the `payload.findGlobal` call.
  </action>
  <verify>
    <automated>grep -q "from '@/components/ui/separator'" src/components/SiteFooter.tsx && ! grep -q "border-white/10" src/components/SiteFooter.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>SiteHeader shows a shadow-elevated sticky header with accent hover/focus nav indicators; SiteFooter uses the Separator primitive instead of a hardcoded color and has 4px-scale-aligned spacing; both compile with no new TS errors; neither `payload.findGlobal` call nor any prop/data shape changed.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-close verification — block-render smoke test and schema-diff gate</name>
  <files>scripts/smoke-check-phase8.mjs</files>
  <action>
Write a standalone Node smoke-check script (no new npm dependency — use global `fetch`, available in Node 20+ per the project's stack requirement) that curls a representative route per block type against a running `next dev` server and asserts each returns HTTP 200 with no Next.js error-boundary marker in the body (grep the response text for `"Application error"` / a React error-digest string). Cover all 16 blocks via these routes (per `05-UI-SPEC.md`'s Page Inventory, home + blog composes most blocks; case-study/post detail covers the remainder):
  - `/` (or `/en`) — Hero, FeaturedCaseStudiesBlock, ClientLogosBlock, FeaturedPostsBlock, TestimonialsCarousel, CallToAction, Section
  - `/blog` — ArchiveBlock
  - `/blog/{seeded-post-slug}` — Content, RelatedPosts, TableOfContentsBlock, Code, FAQ, MediaBlock (use the seeded test post from `scripts/seed-phase2.ts`/`scripts/seed-blog-page.ts` if present; otherwise fetch `/blog` first, extract the first post link via regex, and follow it — do not hardcode a slug that may not exist in the target environment)
  - `/case-studies/{seeded-case-study-slug}` — ResultsSection (same discovery-by-following-a-link approach as above via `/case-studies`)
  - `/contact` — ContactFormBlock
  - `/privacy` or `/terms` — Content/Section (legal pages)

The script exits `1` and prints which route/block failed if any check fails; exits `0` and prints a pass table otherwise. Add an npm script alias is not required — document the run command in the script's header comment (`node scripts/smoke-check-phase8.mjs`, requires `npm run dev` already running on port 3000).

Then run the two phase-close gates and report their exact output in the task's completion notes (do not just claim pass — paste the command output):
1. `npm run dev` (background) then `node scripts/smoke-check-phase8.mjs` — must exit 0.
2. `git diff --stat -- 'src/blocks/*/config.ts' src/payload-types.ts` (diffed against the commit before 08-01 started) — must produce empty output, proving ROADMAP success criterion #3.
  </action>
  <verify>
    <automated>test -f scripts/smoke-check-phase8.mjs && node --check scripts/smoke-check-phase8.mjs</automated>
  </verify>
  <done>`scripts/smoke-check-phase8.mjs` exists, is syntactically valid, and was actually run against a live `next dev` server with a pasted 0-exit pass table covering all 16 blocks; `git diff --stat -- 'src/blocks/*/config.ts' src/payload-types.ts` was run and its empty output pasted into the task completion notes, proving zero schema drift for the whole phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dev-server smoke script -> localhost:3000 | The new script makes local HTTP requests to the developer's own `next dev` instance; no network egress, no secrets, no production endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-08-03 | Tampering | src/components/SiteHeader.tsx, SiteFooter.tsx | accept | Styling-only edit to Server Components that already read trusted Payload global data via the existing `getPayload()`/`findGlobal` call; no new input surface |
| T-08-04 | Denial of Service | scripts/smoke-check-phase8.mjs | accept | Script only targets `localhost:3000` (developer machine), a fixed small set of routes, run manually/locally — not deployed, not scheduled, no production target |
</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` passes with no new errors.
2. `node scripts/smoke-check-phase8.mjs` run against `npm run dev` — exit 0, all 16 blocks' representative route confirmed 200/no-error-boundary.
3. `git diff --stat -- 'src/blocks/*/config.ts' src/payload-types.ts` (phase-wide, both 08-01 and 08-02) — empty output.
4. Manual visual check (per `workflow.human_verify_mode: end-of-phase`): open `/` and `/blog` in a browser, confirm header shows the accent hover/focus nav indicator and elevated shadow, footer shows the Separator divider and clearer column-title hierarchy.
</verification>

<success_criteria>
- SiteHeader/SiteFooter show a visibly clearer hierarchy consistent with `05-UI-SPEC.md`'s editorial-tech direction (ROADMAP criterion #2).
- Zero diffs in any `src/blocks/*/config.ts` or `src/payload-types.ts` across the whole phase (ROADMAP criterion #3), proven by a pasted empty `git diff --stat` output.
- All 16 blocks confirmed rendering without errors via the automated smoke script (ROADMAP criterion #4).
- No new npm dependency added.
</success_criteria>

<output>
Create `.planning/phases/08-shadcn-primitives-global-chrome/08-02-SUMMARY.md` when done
</output>
