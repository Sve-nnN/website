---
phase: 45-baseline-de-regresi-n
plan: 03
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - .planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json
  - .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  - .planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md
autonomous: true
requirements: [BASE-02, BASE-03]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "45-gsc-snapshot.json existe con p1_* y p2_* crudos (nunca los *_diff invertidos) keyed por URL, con la ventana temporal declarada"
    - "La tabla ## Search Console del baseline tiene las 14 rutas criticas (con 'sin datos en la ventana' explicito donde no hay datos)"
    - "El dato de canibalizacion pilas-y-colas (cerca de 150 vs cerca de 48 impresiones) queda confirmado y anotado en el baseline"
    - "El numero de trafico organico mensual real esta escrito con digitos, con metodo (suma de p1 sobre dimensions=date) y ventana declarados"
    - "V(stack) = 0 esta escrito por separado del trafico organico, como dato y no como medicion"
    - "DECISIONS.md de Phase 44 ya no contiene la marca de incognita; la formula parametrica y la tabla de tramos quedan textualmente intactas, con una nota del tramo actual agregada"
    - "45-REGRESSION-BASELINE.md esta humanizado (0 em/en dash, 0 voceo, espanol neutro) y cerrado con veredicto"
    - "git diff sobre src/ vacio en toda la fase"
  artifacts:
    - .planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json
    - .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
    - .planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md
  key_links:
    - "dimensions es un string ('page' / 'date'), no un array — pasar ['page'] devuelve error de validacion"
    - "Los campos *_diff vienen como p2 menos p1 (periodo viejo menos nuevo) — guardar p1 y p2 crudos y calcular deltas por cuenta propia"
    - "Verificar showing == total_items antes de usar los numeros — limit trunca en silencio"
    - "Si las tools gsc-juan-* no responden: surface un blocker, no fabricar numeros"
    - "CONTEXT.md gana sobre la linea 'recalcular la tabla de tramos' de DECISIONS.md: la tabla no se recalcula, se le anota el tramo actual"
    - "Normalizar la barra final entre el canonical de / (sin barra) y GSC (con barra) al cruzar los dos snapshots"
    - "La credencial del hub MCP se lee de ~/.claude.json en tiempo de ejecucion y nunca se escribe a un artefacto, log ni mensaje"
---

<objective>
Cerrar el baseline de la Phase 45: el snapshot de Search Console por pagina de las 14 rutas criticas (BASE-02), el numero de trafico organico mensual real con metodo declarado (BASE-03), la resolucion de la incognita `V` en el documento de decisiones de la Phase 44, y el veredicto final del documento entregable pasado por el humanizer.

Purpose: la Phase 50 va a comparar contra este snapshot para distinguir "la monetizacion diluyo el cluster de servicios" de estacionalidad, y el modelo de ingresos de DEC-02 se parametriza sobre el numero de trafico. Un numero de trafico sin metodo declarado no es comparable en la Phase 50; un snapshot que copia las columnas `*_diff` invierte la lectura de toda la tabla (vienen calculadas como periodo viejo menos periodo nuevo).

Output:
- `.planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json` (p1/p2 crudos, ventana declarada)
- `45-REGRESSION-BASELINE.md` con las secciones `## Search Console`, `## Trafico real y V` y el veredicto, humanizado
- `.planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md` con `V` resuelto y la tabla parametrica intacta
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md
@.planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md
@.planning/phases/45-baseline-de-regresi-n/45-CONTEXT.md
@.planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md
@.planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar el snapshot de Search Console por pagina de las 14 rutas</name>
  <files>
    .planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json,
    .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (seccion "Acceso a Search Console" completa — helper hub.py, contrato de las tools, "Cuatro trampas verificadas", "Confirmacion del dato de canibalizacion")
    - .planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json (los canonicals, para normalizar URLs entre HTML y GSC)
  </read_first>
  <action>
    Precondicion dura: las herramientas `gsc-juan-*` tienen que estar disponibles, ya sea como tools MCP nativas (si Juan reconecto el hub con `/mcp` — camino preferible) o via JSON-RPC directo con el helper `hub.py` que documenta RESEARCH. Si NINGUNA de las dos vias responde, surface un blocker al orquestador con el error exacto de conexion y detener el plan. No fabricar numeros, no estimar, no dejar celdas inventadas.

    Llamar `gsc-juan-compare-search-periods` con: `site_url` = `sc-domain:juan-tech.com`, `period1_start` = `2026-07-31`, `period1_end` = `2026-08-27`, `period2_start` = `2026-07-03`, `period2_end` = `2026-07-30`, `dimensions` = `page` (STRING, no array — pasar `['page']` devuelve error de validacion), `limit` = `250`.

    Trampas a respetar (RESEARCH "Cuatro trampas verificadas"):
    - La respuesta viene como texto plano dentro de `result.content[0].text`; envolver el parse en try/except e imprimir el texto crudo si falla (un error de parseo aca suele ser un mensaje de validacion disfrazado).
    - Verificar que `showing == total_items` (se esperan cerca de 190 paginas) ANTES de usar los numeros; si `showing` es menor, subir `limit` y repetir. `limit` trunca en silencio.
    - NO copiar los campos `*_diff` ni `*_pct`: estan calculados como periodo 2 menos periodo 1 (viejo menos nuevo), el signo es el inverso del intuitivo. Guardar los `p1_*` y `p2_*` crudos (clicks, impressions, ctr, position) y calcular cualquier delta por cuenta propia.

    Escribir `.planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json`: objeto keyed por URL absoluta normalizada, cada valor con `p1_clicks` / `p1_impressions` / `p1_ctr` / `p1_position` y `p2_*` crudos, mas un encabezado con la ventana declarada (periodo 1 = 28 dias 2026-07-31 -> 2026-08-27, periodo 2 = los 28 dias previos 2026-07-03 -> 2026-07-30) y los `total_items` / `showing` observados. Ningun artefacto de esta fase debe incluir la credencial del hub MCP ni encabezados de autenticacion HTTP — el helper la toma de `~/.claude.json` en tiempo de ejecucion y no la escribe en ningun archivo, log ni mensaje.

    Cruzar con las 14 rutas criticas: normalizar la barra final (el canonical de `/` sale como `https://juan-tech.com` sin barra; GSC reporta `https://juan-tech.com/` con barra — elegir una forma y aplicarla a las dos puntas). Llenar la tabla `## Search Console` del baseline (columnas `Route | Impresiones | Clics | Posicion media`, periodo 1) con una fila por cada una de las 14 rutas; las rutas que no aparezcan en GSC se marcan `sin datos en la ventana` explicitamente, nunca celda vacia. Declarar la ventana en el encabezado de la seccion.

    Confirmar y anotar el dato de canibalizacion (RESEARCH "Confirmacion del dato de canibalizacion"): en la ventana, `https://juan-tech.com/blog/pilas-y-colas` (URL vieja) tiene cerca de 150 impresiones contra cerca de 48 de `/blog/cs-fundamentals/pilas-y-colas` (la canonica) — sale con `limit` 250. Es un dato que el baseline debe confirmar, no un defecto de esta fase.

    `git diff --stat src/` vacio.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; test -f .planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json; node -e "const g=require('./.planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json');const s=JSON.stringify(g);if(/Bearer|Authorization|x-api-key/i.test(s)){console.error('credential-like string in snapshot');process.exit(1)}const rows=Object.values(g).filter(v=>v&&typeof v==='object'&&('p1_impressions'in v));if(rows.length<10){console.error('too few GSC rows: '+rows.length);process.exit(1)}if(rows.some(r=>('imp_diff'in r)||('impressions_diff'in r)||('clicks_diff'in r))){console.error('raw *_diff fields leaked into snapshot');process.exit(1)}"; f=.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md; grep -qF '## Search Console' "$f"; grep -qF '2026-07-31' "$f"; grep -qiE 'pilas-y-colas|canibaliz' "$f"; test -z "$(git diff --stat src/)"; echo GSC_OK</automated>
    <fails_when>45-gsc-snapshot.json no existe, contiene una cadena tipo credencial, trae campos *_diff crudos, o tiene menos de 10 filas con metricas; al baseline le falta la seccion Search Console con la ventana declarada o la nota de canibalizacion; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <acceptance_criteria>
    - 45-gsc-snapshot.json existe con p1_* y p2_* crudos keyed por URL, ventana declarada, `showing == total_items` anotado, y cero material de credencial
    - La tabla `## Search Console` del baseline tiene las 14 filas (con `sin datos en la ventana` donde corresponda)
    - El dato de canibalizacion pilas-y-colas (cerca de 150 vs cerca de 48) esta confirmado y anotado
    - git diff src/ vacio
  </acceptance_criteria>
  <precondition>Las tools gsc-juan-* responden, nativas via /mcp o via JSON-RPC con hub.py. Si no responde ninguna via, el plan se detiene con un blocker — no se fabrican datos.</precondition>
  <done>El snapshot de Search Console por pagina de las 14 rutas esta guardado con datos crudos p1/p2 y la seccion Search Console del baseline esta completa.</done>
</task>

<task type="auto">
  <name>Task 2: Derivar el numero de trafico mensual real y resolver V en DECISIONS.md</name>
  <files>
    .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md,
    .planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md
  </files>
  <read_first>
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (secciones "De donde sale el numero de trafico" y "Donde vive V" — la tabla de 5 lineas verbatim y la nota "Tension a resolver en el plan, no en la ejecucion")
    - .planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md (DEC-02 completo: la linea que define `V` y la marca como pendiente de Phase 45, la formula parametrica `Ventas/mes = V x CTR x Conversion`, la tabla de tramos V=1.000 / V=10.000, la subseccion "Que actualizar en Phase 45", y la linea de riesgo "se dimensiona recien cuando Phase 45 entregue el numero")
    - .planning/phases/45-baseline-de-regresi-n/45-CONTEXT.md (decision "El numero V y los artefactos": los dos numeros van separados y sin mezclarse; la tabla parametrica queda intacta)
  </read_first>
  <action>
    Numero total del sitio (BASE-03): llamar `gsc-juan-compare-search-periods` con `site_url` = `sc-domain:juan-tech.com`, los mismos cuatro bordes de fecha de la Task 1, `dimensions` = `date` (string), `limit` = `60`. Devuelve 56 filas (28 dias de cada periodo). Las filas cuya fecha cae en el periodo 2 traen `p1_clicks` / `p1_impressions` en cero estructural, asi que sumar `p1_clicks` y `p1_impressions` sobre las 56 filas da el total exacto del periodo 1 (ventana 2026-07-31 -> 2026-08-27). Este camino es preferible a sumar las 190 filas de `dimensions=page`, que esta sujeto al filtrado por anonimizacion de Google y suma por debajo del total real. Verificar `showing == total_items` tambien aca.

    Escribir la seccion `## Trafico real y V` del baseline con los DOS numeros separados y sin mezclarlos (decision de CONTEXT.md):
    (a) Trafico organico real del sitio: clics e impresiones del periodo 2026-07-31 -> 2026-08-27, con el metodo declarado (suma de `p1` sobre `dimensions=date`, 56 filas) y la fecha de medicion. Un numero, con digitos, no un adjetivo.
    (b) `V(stack) = 0` a la fecha de medicion, porque la pagina `/stack` no existe todavia (la construye la Phase 48). Cualquier proyeccion de que fraccion del trafico llegaria ahi va etiquetada como supuesto, nunca como medicion.

    Actualizar `.planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md`, edicion dirigida por texto ancla (no por numero de linea, que puede haber corrido), respetando la tension que RESEARCH ya resolvio (CONTEXT.md gana sobre la linea que pide "recalcular la tabla de tramos": la tabla NO se recalcula ni se borra, se le anota el tramo actual):
    - La frase "La unica entrada real que falta es `V`, y llega en Phase 45." -> reescribir: `V` ya no falta; referenciar `45-REGRESSION-BASELINE.md` y la fecha.
    - La linea que define `V` como "visitas mensuales a la pagina de stack" y la marca entre corchetes como pendiente de Phase 45 -> reemplazar esa marca por: `V(stack) = 0 a <fecha> (la pagina no existe; se construye en Phase 48)`.
    - La linea de la formula parametrica (`Ventas/mes = V x CTR x Conversion = ...`) -> NO TOCAR.
    - La tabla de tramos (filas `**1.000**` y `**10.000**`) -> intacta. Agregar DEBAJO de la tabla una nota (no una fila que altere la tabla) indicando en que tramo cae el sitio hoy segun el trafico organico real recien medido, y que con `V(stack) = 0` el modelo de ingresos de la pagina de stack rinde 0 hasta que la Phase 48 la publique.
    - La subseccion "### Que actualizar en Phase 45" y su instruccion "Sustituir `V` por el numero real y recalcular la tabla de tramos. Nada mas de este documento cambia." -> cerrarla marcando que se hizo y con que fecha, y reconciliando de forma explicita: la tabla NO se recalculo porque CONTEXT.md la declaro intacta; se anoto el tramo actual.
    - La linea de riesgo ("es un riesgo real que se dimensiona recien cuando Phase 45 entregue el numero de trafico") -> actualizar con el numero; senalar que con `V(stack) = 0` el riesgo del plazo de los 180 dias de Amazon es mayor que el modelado, no menor.
    - La linea de DEC-04 ("Confirmar el numero real en Phase 45 antes de postular") -> anotar con el numero confirmado y la fecha.

    Nada mas de DECISIONS.md cambia. `git diff --stat src/` vacio (DECISIONS.md vive bajo `.planning/`, no bajo `src/`).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; f=.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md; d=.planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md; grep -qF '## Trafico real y V' "$f"; grep -qE 'V\(stack\) *= *0' "$f"; grep -A 25 '## Trafico real y V' "$f" | grep -qE '[0-9]{2,}'; ! grep -qF 'INCOGNITA' "$d"; ! grep -qF "$(printf 'INC\xc3\x93GNITA')" "$d"; grep -qF 'Ventas/mes' "$d"; grep -qF '**1.000**' "$d"; grep -qF '**10.000**' "$d"; grep -qF 'V(stack) = 0' "$d"; test -z "$(git diff --stat src/)"; echo V_OK</automated>
    <fails_when>Falta la seccion Trafico real y V con V(stack)=0 y un numero con al menos dos digitos; DECISIONS.md todavia contiene la marca de incognita; se perdio la linea de la formula parametrica o alguna de las dos filas de la tabla de tramos; DECISIONS.md no tiene el reemplazo V(stack)=0; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <acceptance_criteria>
    - `## Trafico real y V` tiene los dos numeros separados: trafico organico real (con digitos, metodo y ventana declarados) y `V(stack) = 0` como dato aparte
    - DECISIONS.md ya no contiene la marca de incognita; la formula parametrica y las filas `**1.000**` / `**10.000**` de la tabla de tramos quedan textualmente intactas
    - DECISIONS.md tiene la nota del tramo actual debajo de la tabla, el cierre de "### Que actualizar en Phase 45" con fecha, y las lineas de riesgo y DEC-04 anotadas con el numero
    - git diff src/ vacio
  </acceptance_criteria>
  <reversibility rating="reversible">Edicion dirigida de DECISIONS.md por texto ancla, versionada en git, con la formula y la tabla parametrica preservadas verbatim. Reversible con `git revert` del commit de docs.</reversibility>
  <done>El numero de trafico mensual real esta escrito con digitos y metodo declarado, y `V` queda resuelto en DECISIONS.md sin tocar la formula ni la tabla parametrica.</done>
</task>

<task type="auto">
  <name>Task 3: Cerrar el veredicto del baseline y humanizar el documento</name>
  <files>.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md lineas 35-37 (formato del veredicto en el propio ## + parrafo de cierre)
    - ~/.claude/skills/humanizer/SKILL.md
    - research/voice-sample-juan.md (calibracion de voz, ES y EN)
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (secciones "Formato del artefacto a replicar" y "Trampas conocidas")
  </read_first>
  <action>
    Cerrar `45-REGRESSION-BASELINE.md`:
    - Veredicto en el propio encabezado: `## Phase 45 Verdict: Baseline captured, 14/14 routes clean` (ajustar el conteo si alguna ruta no quedo limpia, con la explicacion).
    - Parrafo final: medicion unicamente, ni codigo ni contenido ni schema se modificaron durante la fase, `git diff` sobre `src/` vacio de principio a fin, y este archivo mas sus JSON son el punto de comparacion del gate de la Phase 50.
    - Nota de continuidad del redirect: Phase 32 registro `/es` como 307 contra localhost; produccion emite 308 permanente. La diferencia es esperada (Next emite permanente en produccion) y se anota para que la Phase 50 no la lea como cambio.
    - Confirmar que estan presentes las notas de interpretacion que ya deberian haber quedado de 45-02 y de las Tasks 1 y 2 de este plan: datos de campo de CrUX ausentes (todos los CWV son de laboratorio), serie de laboratorio local no comparable directo con PageSpeed Insights, y la senal de alarma de la varianza de Lighthouse.

    Pasar TODO el documento por el skill humanizer (espanol neutro, sin voceo, sin em dash ni en dash, sin marcas de escritura de IA), calibrando contra `research/voice-sample-juan.md`. Los identificadores, nombres de archivo, comandos y valores numericos no se tocan — solo la prosa. Auto-chequeo antes de dar por terminado: cero em dash, cero en dash, cero voceo.

    Revisar que ningun JSON de la fase (`45-baseline-content.json`, `45-baseline-headlinks.json`, `45-gsc-snapshot.json`, `lh-phase45-*.json`) haya arrastrado encabezados, credenciales ni URLs firmadas antes del commit final.

    `git diff --stat src/` vacio.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; f=.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md; grep -qF 'Phase 45 Verdict' "$f"; grep -qiE '14/14|routes clean|rutas limpias' "$f"; grep -qF '308' "$f"; grep -qi 'PageSpeed Insights' "$f"; if grep -nP "\xe2\x80\x94|\xe2\x80\x93" "$f"; then echo 'em/en dash encontrado en el baseline'; exit 1; fi; if grep -rlE 'Bearer|Authorization|x-api-key' .planning/phases/45-baseline-de-regresi-n/*.json; then echo 'cadena tipo credencial en un JSON de la fase'; exit 1; fi; test -z "$(git diff --stat src/)"; echo FINALIZE_OK</automated>
    <fails_when>Al baseline le falta el veredicto Phase 45, la nota del 308, o la nota de PSI; el documento contiene em dash o en dash; algun JSON de la fase contiene una cadena tipo credencial; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <human-check>Juan revisa `45-REGRESSION-BASELINE.md`: los numeros de Lighthouse son plausibles (la mediana de `/` cerca de 85-90, no cerca de 47), el numero de trafico organico coincide con su modelo mental del sitio, y la prosa suena a el y no a IA. Verificacion de fin de fase (human_verify_mode = end-of-phase).</human-check>
  <acceptance_criteria>
    - El veredicto y el parrafo de cierre estan escritos en el formato de 32-REGRESSION-BASELINE.md
    - La nota de continuidad del 308 y las notas de interpretacion (CrUX, PSI, varianza) estan presentes
    - El documento paso el humanizer: 0 em/en dash, 0 voceo, espanol neutro
    - Ningun JSON de la fase contiene credenciales ni encabezados
    - git diff src/ vacio
  </acceptance_criteria>
  <done>45-REGRESSION-BASELINE.md esta completo, humanizado y cerrado, y es un artefacto de comparacion listo para el gate de la Phase 50.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop -> Google Search Console via hub MCP | JSON-RPC sobre HTTP; la credencial del hub vive en `~/.claude.json`, fuera del repo |
| datos de negocio (impresiones, clics, queries) -> git | El snapshot de GSC se commitea (`commit_docs: true`) |
| DECISIONS.md de la Phase 44 -> edicion cross-fase | Este plan edita un documento de otra fase |
| arbol `src/` | Debe quedar intacto — es la razon de ser de la fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-07 | Information Disclosure | Credencial del hub MCP filtrandose a `45-gsc-snapshot.json`, al `.md`, a un log o al commit | high | mitigate | El helper lee la credencial de `~/.claude.json` en runtime y nunca la escribe; el `<verify>` de la Task 1 y de la Task 3 escanea los artefactos commiteados en busca de cadenas tipo credencial y exige cero coincidencias |
| T-45-08 | Information Disclosure | Analitica de negocio (impresiones/clics/queries) commiteada al repo | low | accept | Es data de analitica, no secreto; se revisa que ningun JSON arrastre URLs firmadas ni encabezados de autenticacion antes del commit |
| T-45-09 | Tampering | El modelo parametrico de DEC-02 (formula + tabla de tramos) corrompido por la edicion | medium | mitigate | Edicion restringida a textos ancla; la linea de la formula y las filas `**1.000**` / `**10.000**` afirmadas presentes verbatim en el `<verify>`; nada se recalcula ni se borra |
| T-45-10 | Tampering | Arbol `src/` durante una fase de solo medicion | medium | mitigate | `git diff --stat src/` afirmado vacio en el `<verify>` de las 3 tasks |
| T-45-SC | Tampering | Instalaciones npm/pip/cargo | n/a | accept | Cero instalaciones en esta fase (45-RESEARCH.md "Package Legitimacy Audit"); `python3` (helper del hub) es del sistema |
</threat_model>

<verification>
- `45-gsc-snapshot.json` existe con `p1_*` / `p2_*` crudos keyed por URL, ventana declarada, sin campos `*_diff`, sin material de credencial
- La seccion `## Search Console` del baseline tiene las 14 rutas criticas (con `sin datos en la ventana` donde aplique)
- El dato de canibalizacion pilas-y-colas queda confirmado y anotado
- `## Trafico real y V` tiene el numero de trafico organico con digitos + metodo + ventana, y `V(stack) = 0` por separado
- `DECISIONS.md` ya no contiene la marca de incognita; formula y tabla de tramos intactas; nota del tramo actual agregada
- `45-REGRESSION-BASELINE.md` cerrado con veredicto, humanizado (0 em/en dash, 0 voceo)
- `git diff --stat src/` vacio en toda la ejecucion del plan
</verification>

<artifacts>
## Artifacts this phase produces (Plan 45-03)

| Artefacto | Rol |
|-----------|-----|
| `.planning/phases/45-baseline-de-regresi-n/45-gsc-snapshot.json` | Snapshot de Search Console por pagina, `p1_*` / `p2_*` crudos keyed por URL absoluta normalizada, ventana temporal en el encabezado — contra este archivo compara la Phase 50 (BASE-02) |
| `.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md` | Se le agregan `## Search Console`, `## Trafico real y V`, la nota de canibalizacion, la nota de continuidad del 308 y el veredicto final; se pasa entero por el humanizer |
| `.planning/phases/44-decisiones-de-monetizaci-n/DECISIONS.md` | `V` resuelto: `V(stack) = 0` con fecha, formula parametrica y tabla de tramos intactas, nota del tramo actual, lineas de riesgo y DEC-04 anotadas (BASE-03) |
</artifacts>

<success_criteria>
- BASE-02: existe un snapshot guardado de Search Console (impresiones, clics, posicion media, ventana declarada) de las 14 rutas criticas
- BASE-03: el trafico mensual real del sitio queda escrito con numero y metodo, y DECISIONS.md de la Phase 44 reemplaza la incognita `V` por el valor real
- Criterio 4: `git diff` sobre `src/` vacio en toda la fase
- El documento entregable esta humanizado y cerrado con veredicto, listo como referencia del gate de la Phase 50
</success_criteria>

<output>
Actualizar `.planning/phases/45-baseline-de-regresi-n/45-03-SUMMARY.md` al terminar, con el numero de trafico organico medido, su metodo, y la lista de textos ancla editados en DECISIONS.md.
</output>
