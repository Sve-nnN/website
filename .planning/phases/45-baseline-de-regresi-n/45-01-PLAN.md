---
phase: 45-baseline-de-regresi-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture-head-links-snapshot.mjs
  - .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  - .planning/phases/45-baseline-de-regresi-n/45-baseline-content.json
  - .planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json
  - .planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json
autonomous: false
requirements: [BASE-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Las 14 rutas críticas resuelven 200 directo contra https://juan-tech.com, verificado por curl y anotado en el baseline (con el 308 de /es documentado)"
    - "45-baseline-content.json existe con H1 (count + texto) y tipos JSON-LD de las 14 rutas, en la forma condensada de 32-baseline-content.json"
    - "45-baseline-headlinks.json existe con canonical y hreflang (lang -> href) de las 14 rutas"
    - "45-REGRESSION-BASELINE.md existe con todas las secciones del formato de Phase 32 mas Canonical/hreflang, Search Console y Trafico real y V, con la fila de / poblada de punta a punta"
    - "scripts/capture-service-page-snapshot.mjs queda sin modificar (serie 32 -> 50 comparable byte a byte)"
    - "git diff sobre src/ vacio"
  artifacts:
    - scripts/capture-head-links-snapshot.mjs
    - .planning/phases/45-baseline-de-regresi-n/45-baseline-content.json
    - .planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json
    - .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
    - .planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json
  key_links:
    - "El script nuevo emite un objeto keyed por path (como capture-service-page-snapshot.mjs), NO un array (como verify-live-jsonld-meta.mjs) — el baseline tiene que ser diffeable clave a clave"
    - "La regex de hreflang necesita el flag i: produccion emite hrefLang en camelCase; sin i devuelve cero resultados en las 14 rutas"
    - "El canonical de / sale sin barra final; GSC lo reporta con barra — la normalizacion se declara aca y la consume 45-03"
---

<objective>
Probar y armar el pipeline del baseline de regresion de la Phase 45: resolucion de las 14 rutas criticas, captura de H1 y JSON-LD con el script existente, captura de canonical y hreflang con un script hermano nuevo, una pasada de Lighthouse de calentamiento, y el esqueleto del documento entregable con la estructura 1:1 de `32-REGRESSION-BASELINE.md`. Cubre BASE-01 en su mitad de HTML renderizado.

Purpose: esta es la foto previa contra la que el gate de la Phase 50 va a comparar. Su valor entero es ser una medicion tomada ANTES de que ninguna fase toque un byte renderizado, asi que el gate de `git diff src/` vacio no es una formalidad. El plan arranca con un tracer de una sola ruta para que cualquier callejon sin salida del enfoque (script nuevo, formato del doc, cadena de Lighthouse) salga a la luz con una ruta medida y no con catorce.

Output:
- `scripts/capture-head-links-snapshot.mjs` (script hermano nuevo, mismo contrato CLI que `capture-service-page-snapshot.mjs`)
- `.planning/phases/45-baseline-de-regresi-n/45-baseline-content.json` (H1 + JSON-LD, 14 rutas, forma condensada)
- `.planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json` (canonical + hreflang, 14 rutas)
- `.planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json` (pasada de calentamiento sobre /)
- `.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md` (esqueleto con todas las secciones + tablas de H1/JSON-LD y Canonical/hreflang completas)
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md
@.planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md
@.planning/phases/45-baseline-de-regresi-n/45-CONTEXT.md
@.planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md
@.planning/milestones/v1.7-phases/32-regression-baseline/32-baseline-content.json
@scripts/capture-service-page-snapshot.mjs
@scripts/verify-live-jsonld-meta.mjs
@scripts/lighthouse-mobile.mjs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decision — script hermano para canonical/hreflang vs extender el capturador existente</name>
  <decision>Como capturar canonical y hreflang de las 14 rutas: script hermano nuevo (`scripts/capture-head-links-snapshot.mjs`) o extender el capturador existente (`scripts/capture-service-page-snapshot.mjs`).</decision>
  <context>
    El criterio 1 de la fase y BASE-01 piden canonical y hreflang explicitamente. Ningun script del repo los extrae hoy — `capture-service-page-snapshot.mjs` solo saca H1 y JSON-LD (verificado en 45-RESEARCH.md "El hueco real"). CONTEXT.md dice literalmente "se reusan los scripts que ya existen ... No se escribe un capturador nuevo", pero esa frase apuntaba a no reinventar la rueda, no a prohibir cerrar un hueco del criterio 1. El repo ya establecio el precedente opuesto y lo dejo escrito: `verify-live-jsonld-meta.mjs` (lineas 8-11) se creo como script hermano con la misma forma de CLI y con el original intacto a proposito, para que las capturas viejas sigan comparables byte a byte. Esta decision se aparta de una frase literal de CONTEXT.md, por eso para y la ve Juan (45-RESEARCH.md Assumption A4, Open Question 1). Read first: 45-RESEARCH.md "El hueco real: canonical y hreflang", 45-CONTEXT.md decision "Alcance y metodo del baseline", scripts/verify-live-jsonld-meta.mjs lineas 1-14.
  </context>
  <options>
    <option id="sibling-script">
      <name>Script hermano nuevo `scripts/capture-head-links-snapshot.mjs` (recomendada)</name>
      <pros>`capture-service-page-snapshot.mjs` queda intacto y la serie Phase 32 -> 36 -> 45 -> 50 sigue comparable byte a byte. Sigue el precedente ya escrito del repo. Archivo aditivo bajo scripts/, borrable sin impacto en nada renderizado.</pros>
      <cons>Un archivo `.mjs` nuevo mas en scripts/. Se aparta de la frase literal de CONTEXT.md "no se escribe un capturador nuevo".</cons>
    </option>
    <option id="extend-existing">
      <name>Extender `scripts/capture-service-page-snapshot.mjs` con los dos extractores</name>
      <pros>Menos codigo, un solo script de captura de HTML.</pros>
      <cons>Cambia el shape del JSON que produjeron Phase 25/30/32 y rompe el diff directo con `32-baseline-content.json`. Toca un archivo que es ancla de comparabilidad de 4 fases.</cons>
    </option>
  </options>
  <resume-signal>Elegir: `sibling-script` o `extend-existing`. Se registra la eleccion y su justificacion en 45-01-SUMMARY.md.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Tracer — baseline de punta a punta de la ruta / (todas las capas, una sola ruta)</name>
  <files>
    scripts/capture-head-links-snapshot.mjs,
    .planning/phases/45-baseline-de-regresi-n/45-baseline-content.json,
    .planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json,
    .planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json,
    .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  </files>
  <read_first>
    - scripts/capture-service-page-snapshot.mjs (completo — forma de CLI, parseArgs, extractH1s/extractJsonLd, loop de main, escritura de --out)
    - scripts/verify-live-jsonld-meta.mjs lineas 1-14 y 109-145 (encabezado del precedente + extractores con match simple y null como no-encontrado)
    - .planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md (seccion "scripts/capture-head-links-snapshot.mjs" — bloques 1 a 7, shape de salida sugerido)
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (secciones "Lo que produccion realmente emite", "Fix minimo propuesto", "Contrato de los scripts")
    - .planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md (completo — estructura exacta a replicar)
    - scripts/lighthouse-mobile.mjs lineas 39-57 y 84-108 (flag --routes-only, forma de salida por ruta)
  </read_first>
  <action>
    Probar el pipeline completo del baseline contra UNA sola ruta (`/`) antes de gastar la hora de Lighthouse en 45-02. Cinco pasos, todos sobre `/`:

    1. Resolver: `curl -s -o /dev/null -w "%{http_code}" https://juan-tech.com/` debe dar 200 directo. Confirmar tambien que `/es` responde 308 hacia `https://juan-tech.com/` (Phase 32 lo registro como 307 contra localhost; produccion emite 308 permanente — anotar la diferencia como continuidad esperada, no como cambio).

    2. Crear `scripts/capture-head-links-snapshot.mjs` siguiendo la estructura comun de los tres scripts de captura del repo (ver "Shared Patterns" en 45-PATTERNS.md): shebang `#!/usr/bin/env node`; docblock que nombra Phase 45 Plan 01, con bloque `Usage:` y la frase que declara que tiene la misma forma de CLI que `capture-service-page-snapshot.mjs` y que ese archivo no se modifica; ESM puro con `fetch` nativo y `writeFile` de `node:fs/promises`, cero dependencias, cero import de Payload, cero contacto con la DB; `parseArgs` manual sobre `process.argv.slice(2)` con `--base-url`, `--out`, `--routes` (valor vacio o ausente vuelve a `null` a proposito); una constante con las 14 rutas del criterio 1 hardcodeada arriba como default, con comentario que explica de donde salio la lista; error por ruta capturado y guardado en el resultado sin abortar el loop; `process.exitCode = 1` al final si hubo fallos, nunca `process.exit(1)` en medio; `main().catch(...)` al pie. Agregar dos extractores con la forma `match` simple + `null` como no-encontrado: uno para `<link rel="canonical">` que devuelve el href o `null`, y uno para todos los `<link rel="alternate" hreflang="...">` que devuelve un array de `{ lang, href }`. Las dos regex llevan el flag `i` obligatorio, con un comentario que explique que produccion emite el atributo como `hrefLang` en camelCase y que sin la `i` la regex devuelve cero resultados. Salida por ruta: `{ canonical, hreflang }`. Condicion de fallo: `canonical === null` en una ruta marca `hadFailure`. Si Juan eligio `extend-existing` en Task 1, en su lugar extender `capture-service-page-snapshot.mjs` con esos dos extractores y su objeto de salida ampliado.

    3. Correr los dos capturadores scoped a `/`: `node scripts/capture-service-page-snapshot.mjs --base-url https://juan-tech.com --routes "/" --out /tmp/content-tracer.json` y `node scripts/capture-head-links-snapshot.mjs --base-url https://juan-tech.com --routes "/" --out /tmp/headlinks-tracer.json`. Escribir la entrada de `/` en `.planning/phases/45-baseline-de-regresi-n/45-baseline-content.json` en la forma condensada de `32-baseline-content.json` (`h1Count`, `h1` array, `jsonLdCount`, `jsonLdTypes` array; conservar entidades HTML sin decodificar, como quedaron en el analog — no "arreglarlas") y la entrada de `/` en `.planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json` en la forma cruda del script (`{ canonical, hreflang }`), objeto keyed por path.

    4. Correr una pasada de Lighthouse mobile sobre `/`: `node scripts/lighthouse-mobile.mjs --base-url https://juan-tech.com --routes-only "/" --out .planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json`. Esta pasada es el calentamiento que pide RESEARCH (fuerza la resolucion o descarga de Chrome-for-Testing y calienta la cache de produccion) y prueba que la cadena de Lighthouse sigue viva. No cuenta como una de las 3 corridas de 45-02.

    5. Crear `.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md` replicando la estructura de `32-REGRESSION-BASELINE.md` (H1 del documento, linea `**Captured:**`, parrafo `Purpose:`, parrafo `Route set:`) traducida a espanol neutro, con TODAS las secciones presentes como esqueleto: `## Lighthouse Mobile` (con la linea de bandas de CWV verbatim del analog debajo de la tabla), `## H1 / JSON-LD Snapshot`, `## Canonical / hreflang` (columnas `Route | Canonical | hreflang (lang -> href)`), `## Search Console` (con la ventana 2026-07-31 -> 2026-08-27 declarada en el encabezado de la seccion), `## Trafico real y V`, mas un placeholder para la nota de datos de campo (CrUX) y para el veredicto `## Phase 45 Verdict`. Poblar SOLO la fila de `/` en cada tabla con los datos de los pasos 3 y 4 (la de Lighthouse marcada como "[pasada tracer — reemplazada por la mediana de 3 corridas en 45-02]"). Las otras 13 rutas quedan como filas TBD que llenan la Task 3, 45-02 y 45-03.

    En todo momento: `git diff --stat src/` vacio. El unico archivo nuevo de codigo vive bajo `scripts/`, nunca bajo `src/`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; test -f scripts/capture-head-links-snapshot.mjs; node --check scripts/capture-head-links-snapshot.mjs; node scripts/capture-head-links-snapshot.mjs --base-url https://juan-tech.com --routes "/" --out /tmp/hl-tracer-verify.json; node -e "const d=require('/tmp/hl-tracer-verify.json');const r=d['/'];if(!r||typeof r.canonical!=='string'||!Array.isArray(r.hreflang)||r.hreflang.length===0){console.error('headlinks bad for /');process.exit(1)}"; node -e "const c=require('./.planning/phases/45-baseline-de-regresi-n/45-baseline-content.json');if(!c['/']||typeof c['/'].h1Count!=='number'){process.exit(1)}"; node -e "const h=require('./.planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json');if(!h['/']||typeof h['/'].canonical!=='string'){process.exit(1)}"; node -e "const b=require('./.planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json');if(typeof b['/'].performance!=='number'||typeof b['/'].lcpMs!=='number'){process.exit(1)}"; for s in '## Lighthouse Mobile' '## H1 / JSON-LD' '## Canonical / hreflang' '## Search Console' '## Trafico real y V' '## Phase 45 Verdict'; do grep -qF "$s" .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md || { echo "missing section: $s"; exit 1; }; done; test -z "$(git diff --stat src/)"; echo TRACER_OK</automated>
    <fails_when>El script nuevo no pasa node --check; no emite canonical string + hreflang array no vacio para /; falta cualquiera de los 3 JSON o el lh-phase45-tracer.json no tiene metricas numericas para /; al baseline .md le falta alguna de las 6 secciones; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <acceptance_criteria>
    - scripts/capture-head-links-snapshot.mjs existe, pasa node --check, y devuelve `{ canonical, hreflang }` para / contra produccion
    - 45-baseline-content.json y 45-baseline-headlinks.json tienen la entrada de / en la forma correcta (condensada / cruda respectivamente)
    - lh-phase45-tracer.json tiene performance, accessibility, best-practices, seo, lcpMs, cls, tbtMs para /
    - 45-REGRESSION-BASELINE.md tiene las 6 secciones y la fila de / poblada en cada tabla
    - Si Juan eligio `sibling-script`: `git diff --quiet -- scripts/capture-service-page-snapshot.mjs` (original sin tocar)
    - git diff src/ vacio
  </acceptance_criteria>
  <precondition>https://juan-tech.com responde 200 en / (verificado por curl); Chrome-for-Testing resuelve via @puppeteer/browsers (cache en .lighthouse-chrome/ o autodescarga).</precondition>
  <done>El pipeline completo del baseline — resolucion de ruta, captura de H1/JSON-LD, captura de canonical/hreflang con el script nuevo, pasada de Lighthouse y esqueleto del documento — funciona de punta a punta sobre / y esta commiteado. Cualquier fallo arquitectonico del enfoque ya salio a la luz con una sola ruta.</done>
</task>

<task type="auto">
  <name>Task 3: Expandir la captura de H1/JSON-LD y canonical/hreflang a las 14 rutas</name>
  <files>
    .planning/phases/45-baseline-de-regresi-n/45-baseline-content.json,
    .planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json,
    .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  </files>
  <read_first>
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (secciones "Lista final de rutas" — la cadena copiable de 14 rutas — y "Trampa 4: extractJsonLd no tolera JSON malformado")
    - .planning/milestones/v1.7-phases/32-regression-baseline/32-baseline-content.json (forma condensada exacta a replicar)
    - .planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md (seccion "45-REGRESSION-BASELINE.md" — convenciones no obvias de las tablas)
  </read_first>
  <action>
    Correr los dos capturadores contra las 14 rutas del criterio 1 usando la cadena verbatim de RESEARCH "Lista final de rutas":
    `/,/en,/servicios/seo-technical-audit,/servicios/seo-consulting,/servicios/fullstack-development,/servicios/ai-seo-geo,/en/services/seo-technical-audit,/en/services/seo-consulting,/en/services/fullstack-development,/en/services/ai-seo-geo,/seo-tecnico-madrid,/seo-tecnico-lima,/en/seo-tecnico-madrid,/en/seo-tecnico-lima`

    Primero curl-verificar las 14 rutas: todas deben dar 200 directo, sin redirect intermedio. Anotar el resultado en el parrafo `Route set:` del baseline, incluyendo el 308 de `/es` documentado en la Task 2.

    `node scripts/capture-service-page-snapshot.mjs --base-url https://juan-tech.com --routes "<cadena>" --out /tmp/content-14.json`, luego condensar a `45-baseline-content.json` en la forma de `32-baseline-content.json` (`h1Count`, `h1` array, `jsonLdCount`, `jsonLdTypes` array). Conservar entidades HTML sin decodificar, no normalizarlas, o Phase 50 lee un cambio donde no lo hay.

    `node scripts/capture-head-links-snapshot.mjs --base-url https://juan-tech.com --routes "<cadena>" --out .planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json` (forma cruda del script, objeto keyed por path).

    Si alguna ruta vuelve `{ error }`, el primer sospechoso es un bloque JSON-LD malformado (el `JSON.parse` de `capture-service-page-snapshot.mjs` esta fuera de try/catch), no la red — inspeccionar el HTML de esa ruta con curl antes de reintentar.

    Llenar en `45-REGRESSION-BASELINE.md`:
    - Tabla `## H1 / JSON-LD Snapshot` (columnas `Route | H1 (count, text) | JSON-LD types`; H1 como `count, "texto"` en una sola celda; `none` en minuscula para JSON-LD ausente), 14 filas.
    - Tabla `## Canonical / hreflang` (columnas `Route | Canonical | hreflang (lang -> href)`), 14 filas.
    - Cerrar cada seccion con un parrafo que nombre el JSON crudo (`45-baseline-content.json`, `45-baseline-headlinks.json`) y aclare que no es defecto sino estado de base (por ejemplo: home y /en emiten 3 bloques JSON-LD, las geo 1, las landings de servicio 1 — anotarlo, no marcarlo como hallazgo).

    `git diff --stat src/` vacio en todo momento.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node -e "const routes=['/','/en','/servicios/seo-technical-audit','/servicios/seo-consulting','/servicios/fullstack-development','/servicios/ai-seo-geo','/en/services/seo-technical-audit','/en/services/seo-consulting','/en/services/fullstack-development','/en/services/ai-seo-geo','/seo-tecnico-madrid','/seo-tecnico-lima','/en/seo-tecnico-madrid','/en/seo-tecnico-lima'];const c=require('./.planning/phases/45-baseline-de-regresi-n/45-baseline-content.json');const h=require('./.planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json');for(const r of routes){const cr=c[r];if(!cr||cr.error||typeof cr.h1Count!=='number'||cr.h1Count<1){console.error('content bad: '+r);process.exit(1)}const hr=h[r];if(!hr||hr.error||typeof hr.canonical!=='string'||!Array.isArray(hr.hreflang)){console.error('headlinks bad: '+r);process.exit(1)}}"; f=.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md; for slug in seo-technical-audit seo-consulting fullstack-development ai-seo-geo seo-tecnico-madrid seo-tecnico-lima; do grep -qF "$slug" "$f" || { echo "route missing from baseline doc: $slug"; exit 1; }; done; test -z "$(git diff --stat src/)"; echo EXPANSION_OK</automated>
    <fails_when>Cualquiera de las 14 rutas falta o trae {error} en content.json o headlinks.json; canonical no es string en alguna ruta; el baseline no menciona las 4 slugs de servicio y las 2 geo; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <acceptance_criteria>
    - 45-baseline-content.json tiene las 14 rutas con h1Count >= 1 y jsonLdTypes array
    - 45-baseline-headlinks.json tiene las 14 rutas con canonical string y hreflang array
    - Las tablas `## H1 / JSON-LD Snapshot` y `## Canonical / hreflang` tienen 14 filas cada una
    - El parrafo `Route set:` documenta la resolucion curl de las 14 rutas y el 308 de /es
    - git diff src/ vacio
  </acceptance_criteria>
  <done>Las dos capturas de HTML cubren las 14 rutas, los dos JSON estan commiteados en la forma diffeable correcta, y las secciones H1/JSON-LD y Canonical/hreflang del baseline estan completas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop -> produccion HTTP (`https://juan-tech.com`) | Solo lectura. Se hace `fetch`/`curl` del HTML publico; no se envia entrada de usuario |
| script nuevo -> repo | `scripts/capture-head-links-snapshot.mjs` entra al arbol versionado y se commitea |
| artefactos de fase -> git | Los JSON y el .md se commitean (`commit_docs: true`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering | Arbol `src/` durante una fase de solo medicion | medium | mitigate | `git diff --stat src/` afirmado vacio en el `<verify>` de cada task y al cierre del plan; ningun archivo nuevo vive bajo `src/` |
| T-45-02 | Tampering | `scripts/capture-service-page-snapshot.mjs` (frozen — ancla de la serie 32 -> 50) | medium | mitigate | El script nuevo es hermano y deja el original intacto; si Juan eligio `sibling-script`, `git diff --quiet` sobre el original es criterio de aceptacion; misma forma de CLI y salida keyed por path |
| T-45-03 | Information Disclosure | JSON y .md commiteados | low | accept | Los snapshots de HTML son data publica; se revisa que ningun artefacto arrastre rutas locales absolutas ni tokens antes del commit |
| T-45-SC | Tampering | Instalaciones npm/pip/cargo | n/a | accept | Esta fase no instala ningun paquete — `lighthouse`, `chrome-launcher`, `@puppeteer/browsers` ya estan en `package.json` (ver 45-RESEARCH.md "Package Legitimacy Audit"). Sin superficie de cadena de suministro |
</threat_model>

<verification>
- `node --check scripts/capture-head-links-snapshot.mjs` pasa
- El script nuevo corrido contra `/` devuelve `{ canonical: <string>, hreflang: [<>=1 items>] }`
- `45-baseline-content.json` y `45-baseline-headlinks.json` cubren exactamente las 14 rutas del criterio 1, cero `{ error }`
- `45-REGRESSION-BASELINE.md` tiene las 6 secciones y las tablas de H1/JSON-LD y Canonical/hreflang con 14 filas
- `lh-phase45-tracer.json` tiene las 7 metricas para `/`
- `git diff --stat src/` vacio en toda la ejecucion del plan
- Si `sibling-script`: `capture-service-page-snapshot.mjs` sin modificar
</verification>

<artifacts>
## Artifacts this phase produces (Plan 45-01)

| Artefacto | Rol |
|-----------|-----|
| `scripts/capture-head-links-snapshot.mjs` | Script hermano nuevo — extrae canonical + hreflang del HTML renderizado, mismo contrato CLI que `capture-service-page-snapshot.mjs` |
| `.planning/phases/45-baseline-de-regresi-n/45-baseline-content.json` | H1 (count + texto) + tipos JSON-LD de las 14 rutas, forma condensada de `32-baseline-content.json` |
| `.planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json` | canonical + hreflang (lang -> href) de las 14 rutas, objeto keyed por path |
| `.planning/phases/45-baseline-de-regresi-n/lh-phase45-tracer.json` | Pasada de calentamiento de Lighthouse sobre `/` (no cuenta como una de las 3 corridas de 45-02) |
| `.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md` | Documento entregable — esqueleto con las 6 secciones + tablas de H1/JSON-LD y Canonical/hreflang completas; 45-02 y 45-03 llenan el resto |
</artifacts>

<success_criteria>
- BASE-01 (mitad HTML): existe un baseline con H1 + JSON-LD + canonical/hreflang de las 14 rutas criticas, capturado sin ningun cambio renderizado previo
- El documento sigue la estructura 1:1 de `32-REGRESSION-BASELINE.md`, con las secciones nuevas que exige el alcance de esta fase
- El script nuevo respeta la convencion de "script hermano" del repo y deja el original intacto (si `sibling-script`)
- `git diff src/` vacio de principio a fin
</success_criteria>

<output>
Actualizar `.planning/phases/45-baseline-de-regresi-n/45-01-SUMMARY.md` al terminar, incluyendo la decision de la Task 1 (`sibling-script` o `extend-existing`) y su justificacion.
</output>
