---
phase: 45-baseline-de-regresi-n
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - .planning/phases/45-baseline-de-regresi-n/lh-phase45-run1.json
  - .planning/phases/45-baseline-de-regresi-n/lh-phase45-run2.json
  - .planning/phases/45-baseline-de-regresi-n/lh-phase45-run3.json
  - .planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json
  - .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
autonomous: true
requirements: [BASE-01]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "lh-phase45-run1/2/3.json existen, cada uno con las 14 rutas y las 7 metricas numericas por ruta"
    - "lh-phase45-baseline.json es la mediana por ruta y metrica, con el mismo shape plano que lh-phase32-baseline.json"
    - "La tabla ## Lighthouse Mobile del baseline tiene 14 filas con CWV en formato numero + banda y la linea de bandas verbatim debajo"
    - "El baseline declara que los CWV son de laboratorio (sin datos de campo CrUX) y que la serie no es comparable directo con PageSpeed Insights"
    - "El comando node -e de la mediana esta copiado verbatim en la linea Captured del baseline"
    - "git diff sobre src/ vacio"
  artifacts:
    - .planning/phases/45-baseline-de-regresi-n/lh-phase45-run1.json
    - .planning/phases/45-baseline-de-regresi-n/lh-phase45-run2.json
    - .planning/phases/45-baseline-de-regresi-n/lh-phase45-run3.json
    - .planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json
    - .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  key_links:
    - "lh-phase45-baseline.json debe tener shape identico a lh-phase32-baseline.json o Phase 50 no puede diffear archivo contra archivo"
    - "En lighthouse-mobile.mjs el flag es --routes-only, no --routes (unica inconsistencia de CLI entre los 3 scripts de captura)"
    - "Varianza de CPU: mediana de 3; si el spread de performance de una ruta supera 15 puntos, escalar a 5 lecturas; conservar siempre las corridas crudas"
---

<objective>
Capturar la mitad de rendimiento del baseline BASE-01: Lighthouse mobile de las 14 rutas criticas contra produccion live, 3 corridas por ruta, mediana guardada, con las corridas crudas conservadas como evidencia de la varianza.

Purpose: la Phase 50 va a diffear su medicion contra `lh-phase45-baseline.json` archivo contra archivo, igual que Phase 36 hizo contra `lh-phase32-baseline.json`. Para que eso funcione, el shape tiene que ser identico y el numero tiene que estar filtrado de ruido: la varianza de Lighthouse contra produccion es brutal (RESEARCH observo 47 vs 86 en dos corridas de `/` separadas por minutos, por contencion de CPU en la laptop, no por el servidor). Esta captura vive en su propia tarea porque son 70-90 minutos de reloj y un fallo tardio no debe obligar a repetir la hora entera.

Output:
- `lh-phase45-run1.json`, `lh-phase45-run2.json`, `lh-phase45-run3.json` (corridas crudas)
- `lh-phase45-baseline.json` (mediana, shape plano de `lh-phase32-baseline.json`)
- `45-REGRESSION-BASELINE.md` con la seccion `## Lighthouse Mobile` completa + notas de interpretacion (CrUX, laboratorio vs PSI)
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md
@.planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md
@.planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md
@.planning/milestones/v1.7-phases/32-regression-baseline/lh-phase32-baseline.json
@scripts/lighthouse-mobile.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correr Lighthouse mobile 3 veces sobre las 14 rutas contra produccion</name>
  <files>
    .planning/phases/45-baseline-de-regresi-n/lh-phase45-run1.json,
    .planning/phases/45-baseline-de-regresi-n/lh-phase45-run2.json,
    .planning/phases/45-baseline-de-regresi-n/lh-phase45-run3.json
  </files>
  <read_first>
    - scripts/lighthouse-mobile.mjs (completo — el flag es --routes-only NO --routes; una instancia de Chrome se lanza y se mata por ruta; cada --out sobrescribe)
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (secciones "Trampa 1: la varianza de Lighthouse contra produccion es enorme", "Trampa 5", "Sizing de la captura", "Contrato de los scripts")
  </read_first>
  <action>
    Con la maquina lo mas ociosa posible y `caffeinate -u` sostenido (patron de Phase 32), correr 3 pasadas completas de Lighthouse mobile sobre las 14 rutas contra `https://juan-tech.com`. La pasada de calentamiento ya la hizo el tracer de 45-01 (`lh-phase45-tracer.json`) — estas 3 son las que cuentan.

    Para cada n en 1, 2, 3:
    `node scripts/lighthouse-mobile.mjs --base-url https://juan-tech.com --routes-only "<cadena de 14 rutas>" --out .planning/phases/45-baseline-de-regresi-n/lh-phase45-run{n}.json`

    Cadena de rutas (verbatim de RESEARCH "Lista final de rutas", ojo que aca el flag es `--routes-only`):
    `/,/en,/servicios/seo-technical-audit,/servicios/seo-consulting,/servicios/fullstack-development,/servicios/ai-seo-geo,/en/services/seo-technical-audit,/en/services/seo-consulting,/en/services/fullstack-development,/en/services/ai-seo-geo,/seo-tecnico-madrid,/seo-tecnico-lima,/en/seo-tecnico-madrid,/en/seo-tecnico-lima`

    Presupuesto: cerca de 82s por ruta (Chrome se lanza y se mata por ruta), 14 x 3 = 42 corridas, cerca de 55-60 min de Lighthouse puro; planificar 70-90 min de reloj con la maquina dedicada. Si el proceso se corta a mitad, se puede reanudar con `--routes-only` sobre las rutas que falten a un archivo aparte y fusionar por clave con el `run{n}.json` parcial.

    Regla de escalado (RESEARCH Open Question 2): tras las 3 corridas, para cada ruta calcular `max - min` de performance sobre las 3 lecturas. Si en alguna ruta ese spread supera 15 puntos, correr esa ruta 2 veces mas (`--routes-only "<esa ruta>"` a archivos temporales) y usar la mediana de las 5 lecturas para esa ruta en 45-02 Task 2. Anotar en el SUMMARY que rutas se escalaron y sus 5 valores.

    Senal de alarma (RESEARCH Trampa 1): si la mediana de `/` cae cerca de 47 y no cerca de 85-90, la medicion esta contaminada por contencion de CPU, no es una regresion (la auditoria del 2026-08-25 dejo `/` en 94 en PSI mobile). Repetir las 3 corridas con la maquina realmente dedicada.

    Cero contacto con `src/`. Los archivos de `.lighthouse-chrome/` estan gitignoreados.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node -e "for(const n of [1,2,3]){const d=require('./.planning/phases/45-baseline-de-regresi-n/lh-phase45-run'+n+'.json');const ks=Object.keys(d);if(ks.length<14){console.error('run'+n+' has '+ks.length+' routes');process.exit(1)}for(const k of ks){const v=d[k];if(v.error||typeof v.performance!=='number'||typeof v.lcpMs!=='number'||typeof v.tbtMs!=='number'||typeof v.cls!=='number'){console.error('run'+n+' bad route '+k);process.exit(1)}}}"; test -z "$(git diff --stat src/)"; echo RUNS_OK</automated>
    <fails_when>Falta alguno de los 3 archivos de corrida; alguna corrida tiene menos de 14 rutas o alguna ruta con {error} o metricas no numericas; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <acceptance_criteria>
    - lh-phase45-run1/2/3.json existen, cada uno con las 14 rutas y las 7 metricas numericas por ruta
    - Toda ruta con spread de performance mayor a 15 puntos entre corridas tiene 2 lecturas extra registradas en el SUMMARY
    - La performance de `/` a lo largo de las corridas esta en el rango esperado (cerca de 80-95), no cerca de 47; si no, se repitio la medicion
    - git diff src/ vacio
  </acceptance_criteria>
  <precondition>https://juan-tech.com responde 200 en las 14 rutas; la maquina puede dedicarse cerca de 90 min sin carga pesada concurrente.</precondition>
  <done>Las 3 corridas crudas de Lighthouse sobre las 14 rutas estan capturadas y commiteadas, con las rutas de alta varianza escaladas a 5 lecturas.</done>
</task>

<task type="auto">
  <name>Task 2: Computar la mediana por ruta y metrica y llenar la tabla de Lighthouse del baseline</name>
  <files>
    .planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json,
    .planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
  </files>
  <read_first>
    - .planning/milestones/v1.7-phases/32-regression-baseline/lh-phase32-baseline.json (shape exacto: objeto keyed por path, 7 metricas planas, sin envoltorio ni metadata)
    - .planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md lineas 9-20 (tabla de 8 columnas + linea de bandas de CWV verbatim)
    - .planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md (secciones "Recomendacion sobre la mediana" — inline node -e, bloque copiado tambien al baseline — y "lh-phase45-baseline.json — shape exacto")
    - .planning/phases/45-baseline-de-regresi-n/45-RESEARCH.md (seccion "Trampa 2: CrUX no tiene datos de campo para este dominio")
  </read_first>
  <action>
    Escribir un one-liner inline `node -e` (no un script versionado — es el precedente explicito de la serie 28 -> 32 y son cerca de 15 lineas de logica) que lea `lh-phase45-run1/2/3.json` y, para cada ruta (salteando claves con `.error`) y cada una de las 7 metricas (`performance`, `accessibility`, `best-practices`, `seo`, `lcpMs`, `cls`, `tbtMs`), tome la mediana de las 3 lecturas (mediana de 3 = el valor del medio tras ordenar numericamente). Para las rutas escaladas en la Task 1, usar la mediana de las 5 lecturas. Escribir `.planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json` con exactamente la misma forma plana que `lh-phase32-baseline.json` (objeto keyed por path, valores numericos planos, sin envoltorio), para que Phase 50 pueda diffear archivo contra archivo.

    Copiar el comando `node -e` usado, verbatim, a la linea `**Captured:**` de `45-REGRESSION-BASELINE.md` — es la reproducibilidad para Phase 50.

    Llenar la tabla `## Lighthouse Mobile` del baseline: columnas `Route | Performance | Accessibility | Best Practices | SEO | LCP | CLS | TBT`, 14 filas. Los valores de CWV van como numero + banda entre parentesis en la misma celda (por ejemplo `3249ms (needs-improvement)`, `0 (good)`, `224ms (good)`) — nunca el numero solo. Bandas: LCP good hasta 2500ms / needs-improvement hasta 4000ms / poor arriba; CLS good hasta 0.1 / needs-improvement hasta 0.25 / poor arriba; TBT good hasta 200ms / needs-improvement hasta 600ms / poor arriba. Poner la linea de bandas verbatim inmediatamente debajo de la tabla, empezando con `Raw data:` seguido de los nombres de los 4 archivos JSON (`lh-phase45-baseline.json` como mediana; `lh-phase45-run1.json`, `run2`, `run3` como crudos).

    Ajustar la linea `**Captured:**` para reflejar el procedimiento real de esta fase: produccion live `https://juan-tech.com`, mediana de 3 corridas de `scripts/lighthouse-mobile.mjs` form factor mobile, los 3 archivos crudos nombrados, mas la nota de que es una serie de laboratorio local contra produccion y por lo tanto NO es directamente comparable con un score de PageSpeed Insights (que corre desde infraestructura de Google).

    Agregar la nota de datos de campo (RESEARCH Trampa 2): `juan-tech.com` no tiene datos de campo en Chrome UX Report por falta de trafico; todos los CWV del baseline son de laboratorio (Lighthouse simulado) y el TBT es el proxy de laboratorio de INP, no INP real. Escribirlo como frase explicita para que en Phase 50 la ausencia de datos de campo no se lea como regresion.

    Cero cambios en `src/`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node -e "const b=require('./.planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json');const ref=require('./.planning/milestones/v1.7-phases/32-regression-baseline/lh-phase32-baseline.json');const keys=Object.keys(Object.values(ref)[0]).sort();const routes=['/','/en','/servicios/seo-technical-audit','/servicios/seo-consulting','/servicios/fullstack-development','/servicios/ai-seo-geo','/en/services/seo-technical-audit','/en/services/seo-consulting','/en/services/fullstack-development','/en/services/ai-seo-geo','/seo-tecnico-madrid','/seo-tecnico-lima','/en/seo-tecnico-madrid','/en/seo-tecnico-lima'];for(const r of routes){const v=b[r];if(!v){console.error('missing '+r);process.exit(1)}if(JSON.stringify(Object.keys(v).sort())!==JSON.stringify(keys)){console.error('shape mismatch '+r);process.exit(1)}if(typeof v.performance!=='number'||typeof v.lcpMs!=='number'){process.exit(1)}}"; f=.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md; grep -qi 'PageSpeed Insights' "$f"; grep -qiE 'CrUX|Chrome UX Report|datos de campo' "$f"; grep -qF 'lh-phase45-baseline.json' "$f"; grep -qF 'lh-phase45-run1.json' "$f"; test -z "$(git diff --stat src/)"; echo MEDIAN_OK</automated>
    <fails_when>lh-phase45-baseline.json no tiene las 14 rutas, o su shape por ruta no coincide exactamente (mismas 7 claves) con lh-phase32-baseline.json; al baseline .md le falta la nota de CrUX, la nota de no-comparable-con-PSI, o la referencia a los JSON crudos; o git diff src/ no esta vacio.</fails_when>
  </verify>
  <acceptance_criteria>
    - lh-phase45-baseline.json existe, 14 rutas, shape por ruta identico a lh-phase32-baseline.json (7 claves, valores numericos planos)
    - El comando node -e de la mediana esta copiado verbatim en la linea `**Captured:**` del baseline
    - La tabla `## Lighthouse Mobile` tiene 14 filas con CWV en formato `numero (banda)` y la linea de bandas verbatim debajo
    - La nota de CrUX y la nota de laboratorio vs PSI estan escritas como frases explicitas
    - git diff src/ vacio
  </acceptance_criteria>
  <done>lh-phase45-baseline.json es la mediana diffeable de las 3 corridas y la seccion Lighthouse del baseline esta completa con sus notas de interpretacion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop (Chrome headless) -> produccion HTTP | Lighthouse audita el HTML servido por produccion; solo lectura, sin codigo de sitio involucrado |
| artefactos de fase -> git | Los JSON de corridas + la mediana + el .md se commitean |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-04 | Tampering | Arbol `src/` durante una fase de solo medicion | medium | mitigate | `git diff --stat src/` afirmado vacio en el `<verify>` de cada task; `.lighthouse-chrome/` esta gitignoreado |
| T-45-05 | Denial of Service (integridad de la medicion) | Contencion de CPU en la laptop contamina el baseline y Phase 50 no puede confiar en el | medium | mitigate | Pasada de calentamiento + 3 corridas + mediana + regla de escalado a 5 lecturas cuando el spread supera 15 puntos + corridas crudas conservadas + senal de alarma documentada (mediana de `/` cerca de 47 = repetir) |
| T-45-06 | Information Disclosure | JSON + .md commiteados | low | accept | Scores de Lighthouse y timings, data no sensible; se revisa que no se cuele una ruta local absoluta en la linea Captured |
| T-45-SC | Tampering | Instalaciones npm/pip/cargo | n/a | accept | Cero instalaciones — `lighthouse`, `chrome-launcher`, `@puppeteer/browsers` ya preexistentes (45-RESEARCH.md "Package Legitimacy Audit") |
</threat_model>

<verification>
- `lh-phase45-run1/2/3.json` cubren las 14 rutas con 7 metricas numericas por ruta, cero `{ error }`
- `lh-phase45-baseline.json` tiene shape por ruta identico a `lh-phase32-baseline.json`
- La seccion `## Lighthouse Mobile` del baseline tiene 14 filas con CWV en formato `numero (banda)` y la linea de bandas verbatim
- El comando de la mediana esta copiado verbatim en la linea `**Captured:**`
- Notas de CrUX y de laboratorio-vs-PSI presentes
- `git diff --stat src/` vacio en toda la ejecucion del plan
</verification>

<artifacts>
## Artifacts this phase produces (Plan 45-02)

| Artefacto | Rol |
|-----------|-----|
| `.planning/phases/45-baseline-de-regresi-n/lh-phase45-run1.json` | Corrida cruda 1 de Lighthouse mobile, 14 rutas |
| `.planning/phases/45-baseline-de-regresi-n/lh-phase45-run2.json` | Corrida cruda 2 |
| `.planning/phases/45-baseline-de-regresi-n/lh-phase45-run3.json` | Corrida cruda 3 |
| `.planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json` | Mediana por ruta y metrica, shape plano de `lh-phase32-baseline.json` — es el archivo contra el que diffea Phase 50 |
| `.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md` | Se le agrega la seccion `## Lighthouse Mobile` completa + linea `**Captured:**` real + notas de CrUX y laboratorio-vs-PSI |
</artifacts>

<success_criteria>
- BASE-01 (mitad rendimiento): existe un baseline de Lighthouse/CWV mobile de las 14 rutas criticas, mediana de 3 corridas contra produccion live, capturado sin ningun cambio renderizado previo
- El shape de `lh-phase45-baseline.json` es diffeable archivo contra archivo por Phase 50
- La interpretacion del baseline esta protegida: CrUX ausente declarado, serie de laboratorio marcada como no comparable con PSI
- `git diff src/` vacio de principio a fin
</success_criteria>

<output>
Actualizar `.planning/phases/45-baseline-de-regresi-n/45-02-SUMMARY.md` al terminar, listando que rutas se escalaron a 5 lecturas y por que.
</output>
