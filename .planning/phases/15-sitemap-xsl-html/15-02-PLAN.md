---
phase: 15-sitemap-xsl-html
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/app/sitemap.html/route.ts
  - scripts/seed-phase15-sitemap-footer-link.ts
autonomous: true
requirements: [SITEMAP-02]

must_haves:
  truths:
    - "Visiting /sitemap.html shows a real navigable HTML page listing every published URL, grouped by section (Pages/Blog/Case Studies/Authors/Categories), with groups that have zero entries omitted"
    - "The footer's legal links row has a 'Sitemap' entry, in both ES and EN, pointing to /sitemap.html"
  artifacts:
    - path: "src/app/sitemap.html/route.ts"
      provides: "Route handler serving the literal /sitemap.html path with a full HTML document grouped by section, reusing getSitemapEntries() from src/lib/sitemap-data.ts"
      contains: "getSitemapEntries"
    - path: "scripts/seed-phase15-sitemap-footer-link.ts"
      provides: "Idempotent seed adding a 'Sitemap' entry to the Footer global's legalLinks array (ES + EN) pointing to /sitemap.html"
      contains: "legalLinks"
  key_links:
    - from: "src/app/sitemap.html/route.ts"
      to: "src/lib/sitemap-data.ts"
      via: "import { getSitemapEntries, SITEMAP_GROUP_LABELS }"
      pattern: "getSitemapEntries"
    - from: "src/components/SiteFooter.tsx"
      to: "/sitemap.html"
      via: "footer.legalLinks[].href rendered as <Link href={legal.href}>"
      pattern: "sitemap.html"
---

<objective>
Add a real navigable `/sitemap.html` route (grouped by section: Pages/Blog/Case Studies/Authors/Categories) reusing the shared query from plan 15-01, and wire it into the footer's `legalLinks` array via an idempotent seed script so the "Sitemap" link the footer is supposed to have actually exists and points somewhere real.

Purpose: SITEMAP-02 — the footer currently has no "Sitemap" link at all (15-CONTEXT.md corrected the wrong assumption from REQUIREMENTS.md that it already existed). This plan closes both halves: the destination page, and the link to it.

Output: `src/app/sitemap.html/route.ts`, `scripts/seed-phase15-sitemap-footer-link.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-sitemap-xsl-html/15-CONTEXT.md
@.planning/phases/15-sitemap-xsl-html/15-UI-SPEC.md
@src/components/SiteFooter.tsx
@src/globals/Footer/index.ts
@scripts/seed-phase14-target-keyword.ts
@src/app/globals.css
</context>

<interfaces>
<!-- Shared data module from plan 15-01 (src/lib/sitemap-data.ts) — consume directly, do not re-implement the query: -->

```typescript
export type SitemapEntry = {
  url: string
  lastModified: string | Date
  group: 'pages' | 'blog' | 'case-studies' | 'authors' | 'categories'
  alternates: { es: string; en: string }
}

export const SITE_URL: string
export const SITEMAP_GROUP_LABELS: Record<SitemapEntry['group'], string> // 'pages'->'Pages', 'blog'->'Blog', 'case-studies'->'Case Studies', 'authors'->'Authors', 'categories'->'Categories'
export async function getSitemapEntries(): Promise<SitemapEntry[]>
```

<!-- Footer global's legalLinks array shape, from src/globals/Footer/index.ts: -->
<!-- array field, items: { label: string (localized: true), href: string (not localized) } -->

<!-- SiteFooter.tsx renders it (line ~154): footer.legalLinks?.map((legal, i) => <Link key={legal.id ?? i} href={legal.href}>{legal.label}</Link>) -->

<!-- globals.css light/dark tokens (for the "nice-to-have" token-aware nice-to-have per 15-UI-SPEC.md): -->
<!-- Light: --background:#FAFAF7 --foreground:#12141C --primary:#F7581E --border:oklch(0.63 0 0) --muted-foreground:oklch(0.54 0 0) -->
<!-- Dark:  --background:#12141C --foreground:#FAFAF7 --primary:#FF7A45 --border:rgba(250,250,247,0.35) --muted-foreground:#A8ACBB -->

<!-- Seed script runner pattern, from scripts/seed-phase14-target-keyword.ts: -->

```typescript
import { getPayload } from 'payload'
import config from '../src/payload.config'
async function main() {
  const payload = await getPayload({ config })
  // payload.findGlobal({ slug: 'footer', locale: 'es' }) / updateGlobal(...)
}
main().catch((e) => { console.error(e); process.exit(1) })
```
Run with: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase15-sitemap-footer-link.ts`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: sitemap.html route handler grouped by section</name>
  <files>src/app/sitemap.html/route.ts</files>
  <action>
Create `src/app/sitemap.html/route.ts` as a Next.js route handler (literal `.html` path segment as a directory name — same convention already used for `src/app/llms.txt/route.ts` and `src/app/llms-full.txt/route.ts` in this codebase). `export const dynamic = 'force-dynamic'`, `export async function GET()`.

Import `getSitemapEntries`, `SITEMAP_GROUP_LABELS`, `SitemapEntry` from `@/lib/sitemap-data`. Fetch entries, group them by `entry.group` in the fixed order Pages → Blog → Case Studies → Authors → Categories (iterate `Object.keys(SITEMAP_GROUP_LABELS)` in that declared order, not by first-seen order in the array). Skip any group with zero entries entirely (no empty `<section>`).

Build a full HTML document string:
- `<title>Sitemap</title>` (per Copywriting Contract — page title stays "Sitemap", not localized, since this route is locale-neutral/crawler-facing per 15-UI-SPEC.md's copy contract note)
- `<h1>Sitemap</h1>`
- One `<section>` per non-empty group: `<h2>{SITEMAP_GROUP_LABELS[group]}</h2>` + `<ul>` of `<li>` items, each item: the URL as link text (`<a href="{entry.url}">{entry.url}</a>`), plus, where the entry has ES/EN alternates that differ, a small inline language switcher `<span class="lang-tag">EN · ES</span>` linking each label to its respective `entry.alternates.en` / `entry.alternates.es` URL (`<a href="{alternates.en}">EN</a> · <a href="{alternates.es}">ES</a>`)
- No "link back" element (this route IS the page the footer links to)

Inline a `<style>` block implementing the token-aware nice-to-have from 15-UI-SPEC.md: define CSS custom properties matching `src/app/globals.css` (light: `--background:#FAFAF7 --foreground:#12141C --primary:#F7581E --border:#E5E5DE --muted-foreground:#71717A`; dark, inside `@media (prefers-color-scheme: dark)`: `--background:#12141C --foreground:#FAFAF7 --primary:#FF7A45 --border:rgba(250,250,247,0.35) --muted-foreground:#A8ACBB`), apply `background:var(--background)` / `color:var(--foreground)` to body, links use `color:inherit` by default and `color:var(--primary)` only on `:hover`/`:focus` (accent reserved for hover/focus only, per UI-SPEC — never on headings/backgrounds/borders), section dividers/`h2` use `border-bottom:1px solid var(--border)`, language tags use `color:var(--muted-foreground)`. Font stack: system stack (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif`) — do not attempt to reference `var(--font-geist-sans)` since this route handler renders outside the `(frontend)/[locale]/layout.tsx` tree that defines that variable, so it would resolve to nothing; system stack is the documented fallback in 15-UI-SPEC.md. Typography: H1 28px/600/1.2, H2 20px/600/1.2, list item 16px/400/1.5, language tag 13px/400/1.4. Spacing: 16px between list items, 32px between sections, 48px page top/bottom margin, max-width 720px centered.

Return via `new Response(html, { headers: { 'content-type': 'text/html; charset=utf-8', 'cache-control': 'public, max-age=0, s-maxage=3600, stale-while-revalidate=86400' } })`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>`npm run build` succeeds; `src/app/sitemap.html/route.ts` exists and exports `GET`; imports `getSitemapEntries`/`SITEMAP_GROUP_LABELS` from `@/lib/sitemap-data` (no duplicated query logic).</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent footer "Sitemap" link seed</name>
  <files>scripts/seed-phase15-sitemap-footer-link.ts</files>
  <action>
Create `scripts/seed-phase15-sitemap-footer-link.ts` following the pattern in `scripts/seed-phase14-target-keyword.ts` (`getPayload({ config })` from `../src/payload.config`, `main().catch(...)`, exit 0 on success).

For each locale (`'es'`, `'en'`): `payload.findGlobal({ slug: 'footer', locale })`, read the current `legalLinks` array. Check idempotently for an existing entry with `href === '/sitemap.html'` (do not match by label, since label is localized and could theoretically differ — match by href, the non-localized field). If no such entry exists, append `{ label: 'Sitemap', href: '/sitemap.html' }` (D from 15-CONTEXT.md: same literal word "Sitemap" in both ES and EN, set explicitly per-locale rather than relying on a shared fallback) to the existing array and call `payload.updateGlobal({ slug: 'footer', locale, data: { legalLinks: <updated array> } })`. If the entry already exists, skip the update for that locale and log that it was already present — running the script twice must not create duplicate "Sitemap" entries.

Log a summary line per locale (`Sitemap link added to footer.legalLinks (es)` / `... already present, skipped (es)`) and the equivalent for `en`.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/seed-phase15-sitemap-footer-link.ts && node --env-file=.env node_modules/.bin/tsx scripts/seed-phase15-sitemap-footer-link.ts 2>&1 | grep -qi 'already present'</automated>
  </verify>
  <done>Running the script twice in a row shows the entry being added on the first run and skipped as already-present on the second (idempotency proven); `Footer.legalLinks` for both `es` and `en` locales contains `{ label: 'Sitemap', href: '/sitemap.html' }`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload DB -> HTML response | Doc `slug`/`updatedAt` values flow into HTML text nodes and `href` attributes served publicly |
| Seed script -> Postgres | One-time idempotent write to the `footer` global, run manually against the project's DB, not exposed as an HTTP endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering/Injection | src/app/sitemap.html/route.ts | mitigate | Interpolated `url`/`alternates` values come from the same escaped/normalized `SitemapEntry` objects produced by `getSitemapEntries()` (plan 15-01); apply the same defensive escaping for `<`, `&`, `"` when interpolating into HTML text/attributes to prevent stored-XSS via a crafted slug |
| T-15-05 | Information Disclosure | src/app/sitemap.html/route.ts | accept | Same published-URL data already exposed by `/sitemap.xml`; no new disclosure |
| T-15-06 | Repudiation/Tampering | scripts/seed-phase15-sitemap-footer-link.ts | accept | Script requires direct DB/env access to run (not an HTTP-exposed endpoint), matches the trust model of every other `seed-*.ts` script already in this repo |
</threat_model>

<verification>
1. `npm run build` succeeds.
2. `curl -s http://localhost:3000/sitemap.html | grep -c '<h2>'` returns between 1 and 5 (only non-empty groups render).
3. `curl -s http://localhost:3000/sitemap.html | grep -o '<h2>[^<]*</h2>'` shows section labels only in the order Pages, Blog, Case Studies, Authors, Categories (subset if some are empty).
4. After running the seed script, load the site footer (`/` and `/en`) and confirm a "Sitemap" link is present pointing to `/sitemap.html`, in both locales.
5. Re-run the seed script — must log "already present" for both locales and must NOT create a duplicate legal link.
</verification>

<success_criteria>
- `/sitemap.html` is a real navigable page grouped by section, empty groups omitted (SITEMAP-02 truth, half 1).
- Footer's `legalLinks` (ES + EN) has exactly one "Sitemap" entry pointing to `/sitemap.html` (SITEMAP-02 truth, half 2).
- Re-running the seed script is a no-op on the second run.
</success_criteria>

<output>
Create `.planning/phases/15-sitemap-xsl-html/15-02-SUMMARY.md` when done
</output>
