---
phase: 16-hero-grainy-gradient-implementation
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [src/components/HeroGrainGradient.tsx, src/blocks/Hero/Component.tsx]
autonomous: true
requirements: [HERO-ANIM-01, HERO-ANIM-02, HERO-ANIM-03, HERO-ANIM-04]

must_haves:
  truths:
    - "Home Hero (variant: 'home') renders the GrainGradient WebGL shader as its background instead of the previous solid bg-secondary panel"
    - "The shader's colors and colorBack derive exclusively from hex values already declared in globals.css (--secondary, --chart-3, --primary, both :root and .dark) — no new hex values invented"
    - "Title, subtitle, CTAs, and breadcrumbs render with the exact same DOM structure, classes, and copy as before this change — only the background layer changes"
    - "With prefers-reduced-motion: reduce active, the shader's speed prop is 0 (static/paused) instead of animating live, and this updates live if the media query changes without a reload"
    - "Non-home Hero variants (listing/post-header/case-study-header) keep their existing bg-secondary + optional media-image-at-30%-opacity behavior completely unchanged"
    - "If GrainGradient fails to mount (e.g. WebGL unavailable), the Hero falls back to the plain bg-secondary background silently, with no error shown to the visitor"
  artifacts:
    - path: "src/components/HeroGrainGradient.tsx"
      provides: "Client Component wrapping GrainGradient, sized via absolute inset-0, theme-aware colors, prefers-reduced-motion handling, error-boundary fallback"
      exports: ["HeroGrainGradient"]
    - path: "src/blocks/Hero/Component.tsx"
      provides: "isHome branch renders <HeroGrainGradient /> instead of the media-image overlay; non-home variants unchanged"
  key_links:
    - from: "src/blocks/Hero/Component.tsx"
      to: "src/components/HeroGrainGradient.tsx"
      via: "import + isHome-gated render"
      pattern: "isHome.*HeroGrainGradient"
    - from: "src/components/HeroGrainGradient.tsx"
      to: "@paper-design/shaders-react"
      via: "GrainGradient import"
      pattern: "import.*GrainGradient.*@paper-design/shaders-react"
    - from: "src/components/HeroGrainGradient.tsx"
      to: "prefers-reduced-motion"
      via: "window.matchMedia"
      pattern: "matchMedia\\('\\(prefers-reduced-motion: reduce\\)'\\)"
---

<objective>
Build the `HeroGrainGradient` Client Component (wrapping the real, verified `GrainGradient` API from `@paper-design/shaders-react`) and wire it into the Hero block's `isHome` branch, replacing the current solid `bg-secondary` + unused 30%-opacity image overlay for the home variant only.

Purpose: HERO-ANIM-01/02/03/04 — the phase's actual visual deliverable. Colors must derive from Phase 7's ember/navy tokens (no new hex invented), title/subtitle/CTAs/breadcrumbs must stay byte-identical, and `prefers-reduced-motion` must be respected via the shader's own `speed=0` mechanism (no separate CSS fallback, no unmounting).
Output: `src/components/HeroGrainGradient.tsx` (new) + `src/blocks/Hero/Component.tsx` (modified `isHome` branch only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-hero-grainy-gradient-implementation/16-CONTEXT.md
@.planning/phases/16-hero-grainy-gradient-implementation/16-UI-SPEC.md
@src/blocks/Hero/Component.tsx
@src/app/globals.css

<interfaces>
<!-- Current Hero/Component.tsx (Server Component) — the file this plan modifies.
     Full current contents, read once, do not re-read: -->

```tsx
import Image from 'next/image'
import Link from 'next/link'

import type { HeroBlock as HeroBlockProps } from '@/payload-types'

import { Container } from '@/components/Container'
import { CMSLink } from '@/components/CMSLink'

export function HeroComponent(props: HeroBlockProps) {
  const { variant, title, subtitle, media, links, breadcrumbs } = props
  const image = typeof media === 'object' ? media : null

  const isHome = variant === 'home'
  const isListing = variant === 'listing'

  return (
    <section
      className={
        isHome
          ? 'relative bg-secondary text-secondary-foreground py-16 md:py-24'
          : 'relative bg-secondary text-secondary-foreground py-12 md:py-16'
      }
    >
      {image?.url && (
        <div className="absolute inset-0 opacity-30">
          <Image src={image.url} alt={image.alt ?? ''} fill className="object-cover" priority />
        </div>
      )}
      <Container className="relative z-10">
        {/* breadcrumbs, title, subtitle, links — all unchanged, see full file */}
      </Container>
    </section>
  )
}
```

Only the `image?.url &&` block needs gating so it renders for non-home variants only, and a
new `{isHome && <HeroGrainGradient />}` render is added in its place for the home variant.
Everything inside `<Container className="relative z-10">` (breadcrumbs/title/subtitle/links)
is untouched — do not reformat or restructure it.

Confirmed CSS tokens this plan must reuse verbatim (from `src/app/globals.css`, already
matching 16-UI-SPEC.md's resolved hex table):

```
:root  (light): --primary: #F7581E;  --secondary: #12141C;  --chart-3: oklch(...) [unused light chart-3, NOT part of this gradient — light gradient's mid-stop reuses the light --secondary #12141C plus the .dark --secondary #23283A per UI-SPEC's "Secondary tonal stop" row]
.dark  (dark):  --primary: #FF7A45;  --secondary: #23283A;  --chart-3: #3A4159;
```

Per 16-UI-SPEC.md's Color/Shader Parameters tables (already resolved, do not re-derive):
- Light `colors`: `["#12141C", "#23283A", "#F7581E"]`, `colorBack`: `#12141C`
- Dark `colors`: `["#23283A", "#3A4159", "#FF7A45"]`, `colorBack`: `#23283A`
- `shape: "wave"`, `softness: 0.8`, `intensity: 0.25`, `noise: 0.12`, `speed: 0.3` (live) /
  `0` (reduced motion), `scale: 1`, `fit: "cover"`, `width`/`height`: `"100%"`/`"100%"`

Dark-theme reachability note (Claude's Discretion territory per 16-CONTEXT.md — how exactly to
select light vs dark colors): `tailwind.config.ts` has `darkMode: ['class']`, and grepping the
full `src/` tree found ZERO places that ever add a `dark` class to `<html>`/`<body>` — there is
no toggle and no OS-preference wiring (confirms ROADMAP's "paleta dark branded (sin toggle)").
The dark palette is therefore currently unreachable on the live site, but the component must
still support it defensively (forward-compatible, low cost) by checking
`document.documentElement.classList.contains('dark')` once on mount — do NOT wire a
`prefers-color-scheme` media query check, since this project's dark mode is class-based, not
OS-preference-based, and would otherwise apply dark colors independent of the actual site theme.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build HeroGrainGradient Client Component</name>
  <files>src/components/HeroGrainGradient.tsx</files>
  <behavior>
    - On mount, if `document.documentElement.classList.contains('dark')` is true, the shader
      receives the DARK `colors`/`colorBack` values from 16-UI-SPEC.md; otherwise the LIGHT
      values.
    - On mount, if `window.matchMedia('(prefers-reduced-motion: reduce)').matches` is true, the
      shader receives `speed={0}` (and a fixed `frame={0}`); otherwise `speed={0.3}` with no
      `frame` prop.
    - If the media query's `change` event fires after mount (user toggles OS setting without
      reload), the component re-renders with the updated `speed` value.
    - The rendered wrapper carries `data-testid="hero-grain-gradient"` and
      `data-motion="reduced"` / `data-motion="live"` reflecting the current reduced-motion
      state, for automated verification in Plan 3.
    - If `GrainGradient` throws during render (e.g. WebGL unavailable), the component falls
      back to a plain `<div className="absolute inset-0 bg-secondary" aria-hidden="true" />`
      with no visible error to the visitor.
  </behavior>
  <action>
    Create `'use client'` component `src/components/HeroGrainGradient.tsx` exporting
    `HeroGrainGradient`. Structure:
    1. Two constant color arrays/back-colors (`LIGHT_COLORS`/`DARK_COLORS`,
       `LIGHT_COLOR_BACK`/`DARK_COLOR_BACK`) hardcoded to the exact hex values from
       16-UI-SPEC.md's Shader Parameters table (quoted in context above) — these are direct
       copies of `globals.css` token values, not re-derived at runtime, since a build-time
       constant is simpler and equally token-faithful for a component with only two possible
       states.
    2. A small class-based `ShaderErrorBoundary` (React error boundaries must be classes) that
       catches render errors from its children and renders the `bg-secondary` fallback div
       instead, logging the error via `console.error` (not surfaced to the visitor).
    3. The `HeroGrainGradient` function component: `useState` for `reducedMotion` (init from
       `window.matchMedia` inside a lazy initializer guarded by `typeof window !== 'undefined'`
       — this is a Client Component so it only renders in the browser after hydration, but
       guard anyway for safety) and `isDark` (init from `document.documentElement.classList`).
       `useEffect` attaches a `change` listener on the `matchMedia` result to keep
       `reducedMotion` live-updated; cleanup removes the listener on unmount. No effect needed
       for `isDark` beyond the initial read (theme never changes at runtime on this site today,
       per the interfaces note above — do not build a `MutationObserver` for an unreachable
       state change).
    4. Render: outer `<div className="absolute inset-0 overflow-hidden" aria-hidden="true"
       data-testid="hero-grain-gradient" data-motion={reducedMotion ? 'reduced' : 'live'}>`
       wrapping `<ShaderErrorBoundary>` wrapping `<GrainGradient>` from
       `@paper-design/shaders-react`, imported as
       `import { GrainGradient } from '@paper-design/shaders-react'`. Pass exactly the props
       from 16-UI-SPEC.md's Shader Parameters table: `colors`, `colorBack` (from the
       light/dark constants based on `isDark`), `shape="wave"`, `softness={0.8}`,
       `intensity={0.25}`, `noise={0.12}`, `scale={1}`, `fit="cover"`, `width="100%"`,
       `height="100%"`, and `speed={reducedMotion ? 0 : 0.3}` plus `frame={0}` only when
       `reducedMotion` is true (build the props as a plain object and spread conditionally, or
       use a ternary on the whole props object — either is fine, just don't pass `frame` when
       motion is live since the shader should be free-running then).
    `aria-hidden="true"` on the outer wrapper because this is a decorative background layer —
    no semantic content, matches how the removed `<Image>` overlay was purely decorative too.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "HeroGrainGradient" || true</automated>
  </verify>
  <done>File exists, exports `HeroGrainGradient`, `npx tsc --noEmit` reports zero errors referencing this file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HeroGrainGradient into the Hero block's home variant</name>
  <files>src/blocks/Hero/Component.tsx</files>
  <action>
    Modify only the background-rendering logic inside `HeroComponent`, per D-01/D-02/D-03/D-04
    (HERO-ANIM-01..04):
    1. Import `HeroGrainGradient` from `@/components/HeroGrainGradient`.
    2. Keep the `<section>`'s `bg-secondary` class on BOTH the `isHome` and non-home branches
       (do not remove it from the home branch) — this is the SSR/pre-hydration background so
       there is zero flash-of-unstyled-background before the Client Component mounts, since
       `HeroGrainGradient`'s `colorBack` matches `--secondary` exactly. Only difference: add
       `overflow-hidden` to the `isHome` className (the shader's canvas should never cause the
       section to scroll/overflow).
    3. Change the existing `{image?.url && (<div className="absolute inset-0 opacity-30">...
       </div>)}` block to only render for the non-home branches: wrap its condition with
       `!isHome &&` (i.e. `{!isHome && image?.url && (...)}`). This preserves the exact
       existing behavior for `listing`/`post-header`/`case-study-header` variants (image
       fallback logic, opacity, `Image` component — completely untouched) while removing it
       from `home`.
    4. Add `{isHome && <HeroGrainGradient />}` immediately after (or in place of) that block,
       so it only renders on the home variant.
    5. Do NOT touch anything inside `<Container className="relative z-10">` — breadcrumbs,
       title, subtitle, links markup, classes, and copy stay byte-identical (HERO-ANIM-03).
    6. The `media` field stays in the Hero schema/props destructuring (`const { ..., media,
       ... } = props`) — do not remove it, per 16-CONTEXT.md ("se mantiene en el schema").
  </action>
  <verify>
    <automated>grep -c "HeroGrainGradient" src/blocks/Hero/Component.tsx && grep -c "!isHome && image" src/blocks/Hero/Component.tsx</automated>
  </verify>
  <done>`isHome` branch renders `<HeroGrainGradient />`; non-home branches still render the unchanged image-overlay block; title/subtitle/CTAs/breadcrumbs markup is unmodified; `npx tsc --noEmit` passes with zero errors in this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component -> Client Component boundary | `Component.tsx` (server) passes no untrusted data into `HeroGrainGradient` (no props at all — it's a self-contained decorative layer), so no data crosses this boundary |
| Browser WebGL context | `GrainGradient` renders via WebGL; a hostile/unsupported browser environment could throw during context creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Denial of Service | `HeroGrainGradient.tsx` (WebGL context creation failure) | mitigate | `ShaderErrorBoundary` catches render errors and falls back to the plain `bg-secondary` div — the Hero section never breaks or shows a blank/broken area even if WebGL is unavailable |
| T-16-02 | Information Disclosure | `console.error` call inside the error boundary | accept | Logs only the caught render error object (React/library internals), never user data — acceptable for a client-side decorative component with no user input |
</threat_model>

<verification>
`npx tsc --noEmit` passes with zero errors across both modified/created files. `grep` confirms `HeroGrainGradient` is imported and rendered only inside the `isHome` branch, and the non-home image-overlay block is gated with `!isHome`.
</verification>

<success_criteria>
Home Hero renders the `GrainGradient` shader as its background; non-home Hero variants are pixel-identical to their pre-change state; title/subtitle/CTAs/breadcrumbs unchanged; reduced-motion state is exposed via `data-motion` for Plan 3's automated verification.
</success_criteria>

<output>
Create `.planning/phases/16-hero-grainy-gradient-implementation/16-02-SUMMARY.md` when done
</output>
