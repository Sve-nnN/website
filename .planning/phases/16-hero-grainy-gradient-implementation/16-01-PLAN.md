---
phase: 16-hero-grainy-gradient-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json]
autonomous: false
requirements: [HERO-ANIM-01]

must_haves:
  truths:
    - "@paper-design/shaders-react is installed and resolvable from application code"
    - "Juan has explicitly confirmed the package's legitimacy before any later plan imports it"
  artifacts:
    - path: "package.json"
      provides: "@paper-design/shaders-react as an installed dependency"
      contains: "\"@paper-design/shaders-react\""
  key_links: []
---

<objective>
Install `@paper-design/shaders-react` (the `GrainGradient` shader component chosen in 16-CONTEXT.md) and gate its use behind a blocking human legitimacy checkpoint, per the Package Legitimacy Gate protocol — no `RESEARCH.md` exists for this phase (no research step ran), so the fallback policy applies: treat the package as `[ASSUMED]` until confirmed.

Purpose: unblock Wave 2 (`HeroGrainGradient.tsx` + Hero wiring), which imports this package and cannot be built without it installed and cleared first.
Output: `@paper-design/shaders-react` installed and locked in `package-lock.json`, with Juan's explicit legitimacy confirmation recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-hero-grainy-gradient-implementation/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @paper-design/shaders-react</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    16-CONTEXT.md "Parámetros técnicos" — package chosen after discussion, rejecting
    `three.js`/`ShaderGradient` (~150KB+, drags in `three` + `@react-three/fiber`) and
    `anime.js` (wrong tool — tweening, not shader/grain generation). No `RESEARCH.md` exists
    for Phase 16 (no research phase ran for this milestone insertion), so per the Package
    Legitimacy Gate's fallback policy this package is treated as `[ASSUMED]` until the
    checkpoint below is confirmed.
  </read_first>
  <action>
    Run `npm install @paper-design/shaders-react`. Do NOT pin a specific version — let npm
    resolve latest (registry currently shows `0.0.77`; this is a pre-1.0 package, expect no
    semver-major guarantees, which is fine for a single presentational component). After
    install, run `npm ls @paper-design/shaders-react` and confirm it resolves with no
    `UNMET DEPENDENCY` errors, and inspect `package.json`'s new dependency tree for
    `@paper-design/shaders-react` to confirm its only runtime dependency is
    `@paper-design/shaders` (no `three`, no `@react-three/*`, no unexpected transitive
    dependencies) — per 16-UI-SPEC.md's Registry Safety note, this confirmation must happen
    before committing the lockfile.
  </action>
  <verify>
    <automated>grep -c '"@paper-design/shaders-react"' package.json</automated>
  </verify>
  <done>`@paper-design/shaders-react` appears in package.json dependencies, package-lock.json updated, `npm ls @paper-design/shaders-react` resolves cleanly with only `@paper-design/shaders` as its own dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirm @paper-design/shaders-react legitimacy before relying on it</name>
  <files></files>
  <what-built>
    Installed `@paper-design/shaders-react` from the public npm registry. No `RESEARCH.md`
    exists for this phase (this milestone's two phases — 16 and 17 — were inserted directly
    from a `/gsd:discuss-phase` conversation, skipping the research step), so per the Package
    Legitimacy Gate's fallback policy this package is tagged `[ASSUMED]` and requires a
    blocking human-verify checkpoint before any later plan imports it — not an automated
    approval, even though `workflow.mode` is `yolo`.

    Registry signals gathered during Task 1 for Juan's review:
    - Package created 2024-10-15, current version `0.0.77` (actively maintained — 4 tags:
      `latest`, `next`, `canary`, `debug`)
    - ~1.78M downloads in the trailing month (per `api.npmjs.org/downloads/point/last-month`)
    - License: Apache-2.0
    - Maintainers include `vladmoroz` (Paper design team) — matches the `@paper-design/*`
      npm scope, no name-squatting signal
    - Zero unexpected transitive dependencies: only depends on `@paper-design/shaders`
      (same publisher, sibling package), no `three.js`/`@react-three/*` chain
  </what-built>
  <action>
    Present the npm registry stats gathered during Task 1 to Juan and wait for explicit
    confirmation before Wave 2 relies on this package.
  </action>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@paper-design/shaders-react and confirm:
       - Weekly/monthly download volume is in the same range as gathered above (roughly
         hundreds of thousands to low millions)
       - Latest published version is `0.0.77` or a newer patch published after 2026-07-11
       - Maintainer/publisher is the `@paper-design` npm scope, not an unrelated account
    2. Run `npm ls @paper-design/shaders-react` locally and confirm it resolves with no
       `UNMET DEPENDENCY` errors and no unexpected `three`/`@react-three/*` entries in the
       dependency tree.
  </how-to-verify>
  <verify>
    <automated>npm ls @paper-design/shaders-react 2>&1 | grep -c UNMET || true</automated>
  </verify>
  <done>Juan has typed "approved" confirming the package's legitimacy per the checkpoint verification steps above.</done>
  <resume-signal>Type "approved" to continue, or describe concerns to halt and reassess.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Untrusted third-party package code enters the build/runtime supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering | `npm install @paper-design/shaders-react` (flagged `[ASSUMED]`, no RESEARCH.md audit table exists for this phase) | mitigate | Blocking `checkpoint:human-verify` (Task 2) before this package is imported by any Wave 2 code — human confirms npmjs.com listing, publisher scope, download stats, and clean dependency tree before proceeding |
</threat_model>

<verification>
`npm ls @paper-design/shaders-react` shows it resolved with no errors; `package.json`/`package-lock.json` reflect the installed version; Juan has typed "approved" at the Task 2 checkpoint.
</verification>

<success_criteria>
`@paper-design/shaders-react` installed, human has confirmed its legitimacy per the checkpoint, Wave 2 can safely `import { GrainGradient } from '@paper-design/shaders-react'`.
</success_criteria>

<output>
Create `.planning/phases/16-hero-grainy-gradient-implementation/16-01-SUMMARY.md` when done
</output>
