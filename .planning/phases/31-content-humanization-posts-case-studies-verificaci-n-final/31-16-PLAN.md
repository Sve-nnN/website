---
phase: 31-content-humanization-posts-case-studies-verificaci-n-final
plan: 16
type: execute
wave: 3
depends_on: ["31-02", "31-03", "31-04", "31-05", "31-06", "31-07", "31-08", "31-09", "31-10", "31-11", "31-12", "31-13", "31-14", "31-15"]
files_modified:
  - scripts/diff-humanization-snapshots.ts
  - scripts/verify-locale-parity.ts
  - scripts/verify-live-jsonld-meta.mjs
  - .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-HISTORICAL-DIFF.md
  - .planning/phases/29-content-humanization-safety-net/content-snapshots/post-sweep-phase31-*.json (generated, gitignored, not hand-written)
autonomous: true
requirements: [VOICE-07]

must_haves:
  truths:
    - "A post-sweep snapshot (tag post-sweep-phase31) exists, captured after all 13 Posts batches and the CaseStudies plan have completed"
    - "A historical diff document shows the full before/after picture: this phase's own pre/post pair, AND a diff against the original pre-sweep-phase30 snapshot (predating any Track B rewrite, satisfying VOICE-04's 'diffable rollback base' purpose) — confirming document counts unchanged everywhere and only the expected collections show content deltas"
    - "reindex-search.ts has been re-run after the full sweep, so /search reflects the rewritten Posts/CaseStudies content"
    - "verify-locale-parity.ts now includes 'posts' and 'case-studies' in its COLLECTIONS array and reports zero asymmetric parity failures across all 72 posts + 7 case-studies"
    - "verify-live-jsonld-meta.mjs now covers every Track B route (Phase 30's original 22 + all 72 posts x2 locales + all 7 case-studies x2 locales + the 4 index routes) and reports zero JSON-LD parse failures / zero missing-expected-type failures across all of them"
  artifacts:
    - path: "scripts/diff-humanization-snapshots.ts"
      provides: "New small tool: diffs two content-humanization-snapshot.ts JSON outputs, reports per-collection/per-doc text deltas"
      contains: "extractText"
    - path: ".planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-HISTORICAL-DIFF.md"
      provides: "Human-readable before/after summary for Juan to read before considering Track B closed"
      contains: "pre-sweep-phase30"
    - path: "scripts/verify-locale-parity.ts"
      provides: "Extended COLLECTIONS array now includes posts/case-studies"
      contains: "'posts'"
    - path: "scripts/verify-live-jsonld-meta.mjs"
      provides: "Extended with dynamic blog/case-studies route generation from the live sitemap"
      contains: "getDynamicBlogCaseStudyRoutes"
  key_links:
    - from: "scripts/diff-humanization-snapshots.ts"
      to: ".planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase30-2026-07-14T20:38:02.242Z.json"
      via: "the ORIGINAL pre-Track-B snapshot (VOICE-04's diffable rollback base)"
      pattern: "pre-sweep-phase30-2026-07-14T20:38:02.242Z.json"
    - from: "scripts/verify-live-jsonld-meta.mjs"
      to: "/sitemap.xml"
      via: "fetch(baseUrl + '/sitemap.xml') to enumerate all real blog/case-studies URLs dynamically"
      pattern: "sitemap.xml"
---

<objective>
Close out Phase 31's verification requirements: capture the post-sweep snapshot and produce the full historical before/after diff Juan reads before considering Track B closed; re-run search reindexing; extend `verify-locale-parity.ts` and `verify-live-jsonld-meta.mjs` to cover Posts/CaseStudies and run them over every Track B route (Phase 30's 22 + this phase's ~162 new routes).

Purpose: Success criteria 2, 3, and 4 of Phase 31 all live here — this is where the phase proves the 13 Posts batches + 1 CaseStudies plan didn't collapse any locale, didn't break any JSON-LD, and is reflected in search.
Output: `post-sweep-phase31-<timestamp>.json` snapshot; `31-HISTORICAL-DIFF.md`; extended and passing `verify-locale-parity.ts`/`verify-live-jsonld-meta.mjs`; reindexed search.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-CONTEXT.md
@.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- verify-locale-parity.ts (full file, 206 lines, unmodified logic) — the -->
<!-- only change needed is adding 2 strings to the COLLECTIONS tuple. -->

From `scripts/verify-locale-parity.ts` (line 40, current state):
```typescript
const COLLECTIONS = ['pages', 'authors', 'testimonials', 'speaking-events', 'categories'] as const
```
Change to:
```typescript
const COLLECTIONS = ['pages', 'authors', 'testimonials', 'speaking-events', 'categories', 'posts', 'case-studies'] as const
```
The generic `walk()`/`isLocalizedPair()`/`extractText()` machinery (lines 55-118) needs ZERO changes — it already treats any `{es,en}`-shaped node as one localized field, which is exactly what `posts.content`/`case-studies.clientContext`/`case-studies.conclusion` return under `locale: 'all'`, richText trees included (the recursive `extractText` already walks Lexical `children` generically).

<!-- verify-live-jsonld-meta.mjs — confirmed real JSON-LD shapes for blog/ -->
<!-- case-studies routes, read directly from the page source during planning -->
<!-- (src/app/(frontend)/[locale]/blog/[slug]/page.tsx and -->
<!-- case-studies/[slug]/page.tsx and their listing pages), NOT guessed: -->

```
/blog, /en/blog                          -> expectedJsonLd: []          (no JsonLd component rendered)
/blog/{slug}, /en/blog/{slug}            -> expectedJsonLd: ['Article']  (single script, '@type': 'Article')
/case-studies, /en/case-studies          -> expectedJsonLd: ['BreadcrumbList']
/case-studies/{slug}, /en/case-studies/{slug} -> expectedJsonLd: ['CreativeWork', 'BreadcrumbList']  (2 separate <script> blocks)
```

Dynamic route discovery (do NOT hardcode 162 routes by hand — fetch and parse the live sitemap instead, matching this script's existing "pure fetch, no DB import" design):
```javascript
async function getDynamicBlogCaseStudyRoutes(baseUrl) {
  const res = await fetch(`${baseUrl}/sitemap.xml`)
  const xml = await res.text()
  // Each <url> block has <loc> (default-locale path) plus 2
  // <xhtml:link hreflang="es|en" href="...">  alternates. Parse both
  // alternates per <url> block (regex or a minimal XML parse), filter to
  // paths matching /blog or /case-studies (index or detail), and produce
  // one route entry per alternate href with expectedJsonLd assigned by the
  // table above (index path === exactly '/blog'|'/en/blog'|'/case-studies'|
  // '/en/case-studies' vs. detail path === anything nested under those).
}
```
Merge the result with the existing hardcoded `ROUTES` constant in `main()` before iterating, when no `--routes` CLI filter is passed (the default full run for this task).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Post-sweep snapshot + historical diff tool + diff report</name>
  <files>scripts/diff-humanization-snapshots.ts, .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-HISTORICAL-DIFF.md, .planning/phases/29-content-humanization-safety-net/content-snapshots/post-sweep-phase31-*.json</files>
  <read_first>
    scripts/content-humanization-snapshot.ts (full file — run as-is, do not modify)
    scripts/verify-locale-parity.ts (extractText/isLocalizedPair pattern, lines 55-118 — reuse the walk shape for the diff tool)
    .planning/phases/29-content-humanization-safety-net/content-snapshots/ (confirm the exact original filename via `ls`: `pre-sweep-phase30-2026-07-14T20:38:02.242Z.json` — this is the true pre-Track-B baseline, captured before Phase 30 touched anything, and is what CONTEXT.md/PATTERNS.md refer to as "the VOICE-04 original snapshot")
  </read_first>
  <action>
    Run the existing snapshot script, tagged for this phase's close: `node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag post-sweep-phase31`. Confirm it exits 0 and produces a file under `.planning/phases/29-content-humanization-safety-net/content-snapshots/`.

    Create `scripts/diff-humanization-snapshots.ts` — a small standalone tool (not modifying `content-humanization-snapshot.ts` itself) that takes two snapshot file paths as CLI args (`--before <path> --after <path>`), loads both, and for every collection present in both files: walks each doc's fields with the same recursive `{es,en}`-shape detection as `verify-locale-parity.ts` (replicate the ~15-line `extractText`/`isLocalizedPair` pattern locally, do not import), and for every localized field found, computes whether the extracted plain text changed between before/after (a simple string inequality check per locale) plus a character-count delta. Also compute and report each collection's document count in both snapshots (flagging any mismatch as an error, not a silent note — a changed doc count would mean something was created/deleted, which this phase must never do).

    Run the diff tool twice and combine both results into one report:
    1. `pre-sweep-phase31-<timestamp>.json` (from 31-01) vs `post-sweep-phase31-<timestamp>.json` (from this task) — confirms ONLY `posts` and `case-studies` show content deltas; every other collection/global (pages, authors, testimonials, speaking-events, categories, clientes, websites, header, footer, llms) shows ZERO deltas, since nothing else was touched this phase.
    2. `pre-sweep-phase30-2026-07-14T20:38:02.242Z.json` (the original, pre-dating Phase 30 entirely — this is what CONTEXT.md calls "the VOICE-04 original snapshot") vs this task's fresh `post-sweep-phase31-<timestamp>.json` — the full historical picture across BOTH Track B phases (30 and 31) combined, for Juan to read before considering the whole track closed.

    Write `.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-HISTORICAL-DIFF.md`: for diff #1, a summary table of collections with a changed/unchanged flag and doc-count-match confirmation; for diff #2, the same table plus 3-5 concrete before/after text snippets (one sentence each, not full posts) from a representative sample of Posts and CaseStudies docs, so Juan has something readable rather than just a pass/fail line. State explicitly which file is the "before" reference for diff #2 and why (the original pre-Track-B snapshot, predating any humanization work in this milestone).
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag post-sweep-phase31 && ls .planning/phases/29-content-humanization-safety-net/content-snapshots/post-sweep-phase31-*.json && test -f .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-HISTORICAL-DIFF.md</automated>
  </verify>
  <done>post-sweep-phase31 snapshot exists; diff-humanization-snapshots.ts correctly reports doc counts unchanged in both comparisons; 31-HISTORICAL-DIFF.md exists with both diffs summarized and concrete before/after samples for Juan to read.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run search reindex + extend and run locale-parity verification</name>
  <files>scripts/verify-locale-parity.ts</files>
  <read_first>
    scripts/reindex-search.ts (full file — run as-is, do not modify)
    scripts/verify-locale-parity.ts (full file, 206 lines)
  </read_first>
  <action>
    Run `node --env-file=.env node_modules/.bin/tsx scripts/reindex-search.ts` — confirms it exits 0 and reports 72 posts + 7 case-studies + N authors reindexed (already covers these 3 collections, no modification needed).

    Edit `scripts/verify-locale-parity.ts` line 40's `COLLECTIONS` tuple: add `'posts'` and `'case-studies'` to the existing array (`['pages', 'authors', 'testimonials', 'speaking-events', 'categories', 'posts', 'case-studies']`). Make no other change to this file — the generic walk/extract logic already handles richText trees and needs no modification.

    Run `node --env-file=.env node_modules/.bin/tsx scripts/verify-locale-parity.ts` and confirm it exits 0 with zero asymmetric parity failures across all collections including the newly added `posts` (72 docs) and `case-studies` (7 docs). If any failure is reported, investigate whether it's a real regression from one of the 13 posts-batch plans or Plan 31-15 (in which case: identify which batch, re-run that batch's script — it is idempotent/checkpointed and safe to re-invoke — then re-run this verification) versus a pre-existing issue unrelated to this phase's writes (in which case: document it, do not silently patch).
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/reindex-search.ts && grep -c "'posts', 'case-studies'" scripts/verify-locale-parity.ts | grep -q '^1$' && node --env-file=.env node_modules/.bin/tsx scripts/verify-locale-parity.ts</automated>
  </verify>
  <done>reindex-search.ts run successfully; verify-locale-parity.ts's COLLECTIONS array includes posts and case-studies; running it reports RESULT: PASS with zero asymmetric parity failures across all 7 collections + 2 globals.</done>
</task>

<task type="auto">
  <name>Task 3: Extend and run live JSON-LD/meta verification over all Track B routes</name>
  <files>scripts/verify-live-jsonld-meta.mjs</files>
  <read_first>
    scripts/verify-live-jsonld-meta.mjs (full file, 181 lines)
    src/app/sitemap.xml/route.ts (full file — confirms sitemap shape: <loc> + 2 <xhtml:link hreflang="es|en"> alternates per entry)
  </read_first>
  <action>
    Extend `scripts/verify-live-jsonld-meta.mjs` with a new function that fetches the live `/sitemap.xml`, parses every `<url>` block's `<xhtml:link hreflang="es">` and `hreflang="en"` `href` values, and filters to those whose path is `/blog`, `/en/blog`, `/case-studies`, `/en/case-studies`, or anything nested under `/blog/...`/`/case-studies/...`/`/en/blog/...`/`/en/case-studies/...`. For each matched path, assign `expectedJsonLd` per the exact table confirmed from source in `<interfaces>` (index paths get `[]`, blog detail paths get `['Article']`, case-studies detail paths get `['CreativeWork', 'BreadcrumbList']`). Merge this dynamically-built list with the existing hardcoded `ROUTES` constant (Phase 30's 22 routes, unchanged) in `main()` when no `--routes` filter is passed on the CLI, so a default run covers everything.

    Start the dev server if not already running (`npm run dev`, confirm port 3000 responds) and run: `node scripts/verify-live-jsonld-meta.mjs --base-url http://localhost:3000 --out .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-jsonld-meta-results.json`. This will curl roughly 184 routes total (22 existing + ~162 new) — expect this to take several minutes; it is mechanically simple (sequential fetch + regex parse), not context-heavy.

    Post-process the `--out` JSON: separately tally (a) routes with a JSON-LD JSON.parse failure, (b) routes missing an expected `@type`, (c) routes with an empty/missing `<title>`, and (d) routes with an empty/missing meta description. Treat (a), (b), and (c) as blocking — Phase 31's success criterion 4 is specifically about "cero structured data roto" and title integrity. Treat (d) as informational only: per Phase 30's own precedent (30-04-SUMMARY.md), `meta.description` being empty is a pre-existing, out-of-scope SEO content gap (CONTEXT.md explicitly scopes this phase to body/content, not meta) — do not fail the phase on (d) alone, but do report the count in the SUMMARY so it stays visible. If ANY route in category (a) or (b) is found, investigate which batch/doc is responsible before declaring this task done — a real broken JSON-LD is a genuine regression, not a known gap. Stop the dev server if you started it fresh.
  </action>
  <verify>
    <automated>node scripts/verify-live-jsonld-meta.mjs --base-url http://localhost:3000 --out .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-jsonld-meta-results.json; node -e "const r=require('./.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-jsonld-meta-results.json'); const bad=r.filter(x=>x.jsonLdParseErrors?.length || x.problems?.some(p=>p.includes('expected JSON-LD') || p.includes('title'))); if(bad.length) { console.error('BLOCKING failures:', bad.length); process.exit(1);} console.log('OK: 0 blocking JSON-LD/title failures across', r.length, 'routes')"</automated>
  </verify>
  <done>verify-live-jsonld-meta.mjs dynamically discovers all blog/case-studies routes from the live sitemap and merges them with Phase 30's existing 22; running the full sweep (~184 routes) reports zero JSON-LD parse failures, zero missing-expected-type failures, and zero empty-title failures; any empty meta.description findings are reported as the known pre-existing gap, not a blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| script -> Local API -> Neon Postgres | Snapshot/reindex scripts read/write the real production database via `payload.find`/`payload.update` |
| script -> live dev server -> HTML responses | The JSON-LD/meta verification curls a locally-bound dev server serving real content; never exposed beyond localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-31-08 | Information Disclosure | content-humanization-snapshot.ts / diff tool reading full production content | low | accept | Read-only; output stays gitignored/local, same as Phase 29/30 precedent |
| T-31-09 | Tampering | reindex-search.ts's no-op update touching every posts/case-studies/authors doc | low | accept | Existing, already-vetted script (Phase 5), `data: {}` update cannot alter any field |
| T-31-10 | Repudiation | A real JSON-LD/title regression going unnoticed among ~184 routes | high | mitigate | Verify command programmatically tallies parse failures and missing-type failures across the full output; any hit blocks task completion until root-caused |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installed this plan — reuses existing scripts/tooling |
</threat_model>

<verification>
1. `post-sweep-phase31-<timestamp>.json` exists with `collections.posts.count === 72` and `collections['case-studies'].count === 7`, matching the pre-sweep count exactly
2. `31-HISTORICAL-DIFF.md` exists, documents both diffs (this-phase pre/post, and full-milestone original-vs-now), with doc counts confirmed unchanged in every collection
3. `verify-locale-parity.ts` includes `posts`/`case-studies` and exits 0 (RESULT: PASS)
4. `verify-live-jsonld-meta.mjs` covers all ~184 Track B routes and reports zero JSON-LD parse failures / zero missing-expected-type failures / zero empty-title failures
5. `reindex-search.ts` has been re-run after the full sweep
</verification>

<success_criteria>
- Post-sweep snapshot captured, historical diff documented for Juan
- Locale parity verified across all Posts/CaseStudies with zero asymmetric failures
- Search reindexed to reflect rewritten content
- Zero broken structured data across every Track B route (Phase 30 + Phase 31 combined)
</success_criteria>

<output>
Create `.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-16-SUMMARY.md` when done
</output>
