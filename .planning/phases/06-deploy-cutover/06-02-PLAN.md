---
phase: 06-deploy-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/payload.config.ts, scripts/verify-db-pool.ts, .planning/phases/06-deploy-cutover/NEON-POOL-SIZING.md]
autonomous: false
requirements: [DEPLOY-03]

must_haves:
  truths:
    - "El pool de Postgres en payload.config.ts tiene un tamaño máximo explícito, no el default implícito de la librería"
    - "Ese tamaño está verificado contra el límite real de conexiones del plan Neon que Juan tiene contratado (Neon es el Postgres externo real de este proyecto, no un Postgres self-hosted en Hostinger — DEPLOY-03 se interpreta contra el límite de Neon per 06-CONTEXT.md)"
    - "Un script local, corrido contra la DB real, confirma N queries concurrentes (N = pool max) sin errores de conexión"
  artifacts:
    - path: "scripts/verify-db-pool.ts"
      provides: "Prueba de concurrencia real contra Neon usando el pool configurado"
    - path: ".planning/phases/06-deploy-cutover/NEON-POOL-SIZING.md"
      provides: "Documentación del límite de Neon confirmado por Juan y la decisión sobre connection string pooled vs unpooled"
  key_links:
    - from: "src/payload.config.ts"
      to: "process.env.DATABASE_URI"
      via: "postgresAdapter({ pool: { connectionString, max } })"
      pattern: "pool:\\s*\\{[^}]*max"
---

<objective>
Dimensionar y verificar explícitamente el pool de conexiones Postgres contra el límite real del proveedor Postgres de este proyecto (Neon, externo — no un Postgres self-hosted en Hostinger, confirmado en 06-CONTEXT.md), y decidir junto con Juan si el proceso en producción debe usar la connection string *pooled* (`-pooler`) de Neon en vez de la *unpooled* que hoy se usa también para runtime.

Purpose: Satisfacer DEPLOY-03 sin asumir que "el plan de Hostinger" implica Postgres hosteado ahí — la DB real es Neon.
Output: `src/payload.config.ts` con pool.max explícito, `scripts/verify-db-pool.ts`, `NEON-POOL-SIZING.md` con la decisión documentada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-cutover/06-CONTEXT.md
@src/payload.config.ts

<interfaces>
Patrón real ya usado en `scripts/backfill-case-study-author.ts` y otros scripts del repo para hablar con Payload fuera del server Next:

```
import { getPayload } from 'payload'
import config from '../src/payload.config'
const payload = await getPayload({ config })
```

Se corre con: `node --env-file=.env node_modules/.bin/tsx scripts/<archivo>.ts`

`src/payload.config.ts` hoy configura `db: postgresAdapter({ pool: { connectionString: process.env.DATABASE_URI }, push: false })` — sin `max` explícito (el default de `pg`/drizzle es 10). El comentario existente aclara que `DATABASE_URI` apunta a la connection string UNPOOLED de Neon porque la pooled rompe los prepared statements de `payload migrate`. Ese mismo `DATABASE_URI` se usa hoy también para el proceso runtime (no solo para migrate) — este plan revisa si eso es correcto para producción.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de verificación de concurrencia real + documento base de análisis</name>
  <files>scripts/verify-db-pool.ts, .planning/phases/06-deploy-cutover/NEON-POOL-SIZING.md</files>
  <action>
Crear `scripts/verify-db-pool.ts` siguiendo el patrón `getPayload({config})` de los scripts existentes: lee un `POOL_TEST_CONCURRENCY` de `process.env` (default 10), dispara esa cantidad de `payload.find()` concurrentes con `Promise.allSettled` repartidos entre 3-4 colecciones reales (pages, posts, authors, case-studies), mide cuántos resuelven OK vs cuántos fallan por error de conexión, e imprime un resumen `X/Y queries succeeded` con `process.exit(1)` si hubo algún fallo.

Correr el script contra la DB real (`node --env-file=.env node_modules/.bin/tsx scripts/verify-db-pool.ts`) con el pool default actual (sin `max` explícito) como baseline, y anotar el resultado.

Escribir `NEON-POOL-SIZING.md` documentando: (1) el hallazgo de que hoy `DATABASE_URI` es la string UNPOOLED de Neon usada tanto para `payload migrate` como para el proceso runtime, (2) que Neon documenta límites de conexión directa (unpooled) sensiblemente más bajos que los de conexión pooled (vía PgBouncer) — el número exacto depende del plan Neon contratado, que no está confirmado, (3) dos opciones a decidir con Juan en la Task 2: Opción A — mantener una sola `DATABASE_URI` unpooled también en runtime (más simple, riesgo de agotar el límite bajo si hay tráfico admin + frontend simultáneo); Opción B — separar `DATABASE_URI` (pooled, usada por el proceso runtime en producción) de `DATABASE_URI_MIGRATE` (unpooled, usada solo por `payload migrate`/`migrate:create`), siguiendo la recomendación de Neon de usar el connection pooler para tráfico de aplicación.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/verify-db-pool.ts</automated>
  </verify>
  <done>El script corre contra la DB real y reporta explícitamente cuántas queries concurrentes tuvieron éxito; NEON-POOL-SIZING.md documenta el hallazgo del connection-string único y las dos opciones para decidir.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decidir esquema de connection string Neon (pooled vs unpooled) tras confirmar el límite real</name>
  <action>Juan confirma en el dashboard de Neon el plan y límite de conexiones real, y elige entre las dos opciones descritas abajo; Claude no tiene acceso al dashboard de Neon.</action>
  <decision>Límite de conexiones Neon confirmado + esquema de connection string para producción (una sola unpooled vs pooled+unpooled separadas)</decision>
  <context>
    El pool de Postgres de este proyecto corre contra Neon (externo), no contra un Postgres hosteado en Hostinger — 06-CONTEXT.md ya aclaró esta interpretación de DEPLOY-03. Hoy la misma connection string UNPOOLED sirve tanto para `payload migrate` (necesario, la pooled rompe sus prepared statements) como para el proceso runtime en producción (no necesariamente correcto — Neon recomienda la pooled para tráfico de aplicación, que soporta muchas más conexiones concurrentes que la conexión directa).

    Para decidir el `pool.max` final necesito que confirmes en el dashboard de Neon (Project → Settings → o la página de tu plan):
    1. Qué plan de Neon tienes (Free / Launch / Scale / otro) y el límite de conexiones directas (unpooled) y pooled que muestra.
  </context>
  <options>
    <option id="single-unpooled">
      <name>Mantener una sola DATABASE_URI unpooled para todo (status quo)</name>
      <pros>Cero cambios de config además del pool.max; menos superficie de error</pros>
      <cons>El límite de conexiones directas de Neon suele ser bajo (puede rondar decenas incluso en planes pagos); un proceso PM2 con pool.max alto + `payload migrate` corriendo en paralelo durante un deploy podría agotarlo bajo tráfico real</cons>
    </option>
    <option id="split-pooled-migrate">
      <name>Separar DATABASE_URI (pooled, runtime) de DATABASE_URI_MIGRATE (unpooled, solo migrate)</name>
      <pros>Sigue la recomendación oficial de Neon; el proceso de producción usa el pooler (soporta muchas más conexiones concurrentes); reduce el riesgo de DEPLOY-03 real</pros>
      <cons>Requiere una env var adicional en el servidor y ajustar payload.config.ts para leer una u otra según el comando (`migrate` vs runtime normal)</cons>
    </option>
  </options>
  <resume-signal>Confirma el plan/límite real de Neon y responde: single-unpooled o split-pooled-migrate</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Aplicar la decisión al pool config y re-verificar</name>
  <files>src/payload.config.ts, .planning/phases/06-deploy-cutover/NEON-POOL-SIZING.md</files>
  <action>
Según lo decidido en Task 2: añadir `max` explícito al `pool` de `postgresAdapter` en `src/payload.config.ts` (valor conservador, por ejemplo 5-10, justificado contra el límite real confirmado por Juan — dejar el número final atado al límite real, nunca mayor a la mitad del límite confirmado para dejar margen a `payload migrate`/dev local corriendo en paralelo). Mantener intacto el comentario existente sobre por qué `payload migrate` necesita la string unpooled.

Si se eligió `split-pooled-migrate`: introducir una segunda env var (`DATABASE_URI_MIGRATE`) documentada en `NEON-POOL-SIZING.md` y en `DEPLOY-ENV-CHECKLIST.md` (no editar ese archivo directamente si pertenece a otro plan — en su lugar, añadir la nota de la nueva key solo en `NEON-POOL-SIZING.md` y mencionar en el resumen del plan que `DEPLOY-ENV-CHECKLIST.md` del Plan 06-01 debe actualizarse manualmente por Juan o en un pase posterior si ya fue commiteado). Ajustar `payload.config.ts` para que `pool.connectionString` use `DATABASE_URI` (pooled) en runtime normal, pero seguir usando `DATABASE_URI` sin split de comando si Payload no ofrece un hook limpio para diferenciar `migrate` de `runtime` sin código adicional frágil — documentar la limitación real encontrada si el split resulta impracticable con la API actual de Payload, en vez de forzar una solución fragil.

Actualizar `NEON-POOL-SIZING.md` con el límite real confirmado por Juan, el valor final de `pool.max`, y el resultado de re-correr `scripts/verify-db-pool.ts` con `POOL_TEST_CONCURRENCY` igual al `pool.max` elegido.
  </action>
  <verify>
    <automated>grep -q "max" src/payload.config.ts && POOL_TEST_CONCURRENCY=$(grep -oE 'max:\s*[0-9]+' src/payload.config.ts | grep -oE '[0-9]+') node --env-file=.env node_modules/.bin/tsx scripts/verify-db-pool.ts</automated>
  </verify>
  <done>payload.config.ts tiene pool.max explícito justificado contra el límite real de Neon confirmado por Juan, y verify-db-pool.ts confirma N/N queries exitosas con esa concurrencia contra la DB real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proceso Node (Hostinger) → Neon (internet) | Conexión TLS a un Postgres gestionado externo |
| .env local/servidor → DATABASE_URI | Secreto de conexión con credenciales de escritura completas a la DB de producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | Pool sin `max` explícito | mitigate | Fijar `pool.max` conservador y verificado contra el límite real de Neon (Task 3) |
| T-06-05 | Information Disclosure | `DATABASE_URI` en tránsito | accept | Neon fuerza TLS (`sslmode=require` implícito en su connection string); no se requiere config adicional |
| T-06-06 | Denial of Service | `payload migrate` + proceso runtime compitiendo por el límite de conexiones directas durante un deploy | mitigate | Split pooled/unpooled si Neon lo soporta bien (Task 2/3), o pool.max reducido con margen documentado si se mantiene una sola string |
</threat_model>

<verification>
`scripts/verify-db-pool.ts` corrido contra la DB real de Neon, con concurrencia igual al `pool.max` final, reporta 0 fallos.
</verification>

<success_criteria>
`src/payload.config.ts` tiene `pool.max` explícito. Juan confirmó el límite real de conexiones de su plan Neon. `NEON-POOL-SIZING.md` documenta el límite confirmado, la decisión tomada, y el resultado de la verificación real.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-cutover/06-02-SUMMARY.md` when done
</output>
