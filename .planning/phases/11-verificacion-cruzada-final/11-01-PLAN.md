---
phase: 11-verificacion-cruzada-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-wcag-contrast-full.ts
  - src/app/globals.css
  - .planning/phases/11-verificacion-cruzada-final/11-01-contrast-report.md
autonomous: true
requirements: [UI-11, UI-13]

must_haves:
  truths:
    - "Every token pair actually consumed by a component (text-on-background, text-on-card, text-on-primary/secondary/accent, destructive-on-background, primary/border as large-UI elements) passes WCAG AA (4.5:1 normal text / 3:1 large text or UI) in BOTH the light `:root` palette and the `.dark` palette, on the final accumulated globals.css — not just the 10 dark-only pairs Phase 7 checked."
    - "A grep sweep across every file touched by Phase 7-10 commits finds zero hardcoded content strings (literal EN/ES sentences that should be a Payload field or next-intl message) — only genuine UI chrome/i18n-catalog strings remain."
    - "src/blocks/*/config.ts and src/payload-types.ts have zero diff across the full milestone range (Phase 5 close through current HEAD), confirmed by a fresh git diff, not by trusting per-phase VERIFICATION.md claims."
  artifacts:
    - path: "scripts/check-wcag-contrast-full.ts"
      provides: "Automated, re-runnable WCAG AA contrast check covering both :root (light) and .dark token palettes, with oklch color support"
      exports: []
    - path: ".planning/phases/11-verificacion-cruzada-final/11-01-contrast-report.md"
      provides: "Documented pass/fail table for every checked pair, both themes, plus the hardcoded-content grep results and the full-range config/types diff output"
  key_links:
    - from: "scripts/check-wcag-contrast-full.ts"
      to: "src/app/globals.css"
      via: "hardcoded token mirror (light :root + .dark blocks), kept in sync per inline comment"
      pattern: "background|foreground|primary|secondary|accent|destructive|border"
---

<objective>
Re-verify WCAG AA contrast across BOTH themes on the fully accumulated milestone state, and run the final hardcoded-content + schema-drift sweep across the whole Phase 7-10 diff. This is the phase's fast, deterministic, script-driven half of the audit — no visual/browser check needed here (that's Plan 11-02).

Purpose: Phase 7's `check-dark-contrast.ts` only checked 10 dark-mode pairs at the moment `.dark` was rebranded — it never checked the light theme, and it predates every component change from Phases 8-10 (Card hover/press states, Badge, nav accent borders, AuthorCard credential badges) that could compose tokens in new ways. UI-13's hardcoded-content grep and config/types zero-diff check were only ever run per-phase (narrow diffs); this phase's job is the FULL milestone range (Phase 5 close -> current HEAD), per 11-CONTEXT.md.
Output: `scripts/check-wcag-contrast-full.ts` (reusable, both-theme contrast checker) + a written report documenting every pair's ratio, the grep findings, and the full-range schema-diff result. Any genuine contrast/hardcoded-content regression found gets fixed in this same plan (token value adjustment or field/message-catalog wiring), per 11-CONTEXT.md's "fixing a genuine regression stays in scope" rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-verificacion-cruzada-final/11-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Full current token set the executor must check. Both palettes live in
     src/app/globals.css. Light :root uses a MIX of hex/oklch; .dark uses
     hex/rgba exclusively (Phase 7 rebrand). The new script must handle both
     color formats. -->

From src/app/globals.css `:root` (light theme):
```
--background: #FAFAF7;        --foreground: #12141C;
--card: #FAFAF7;               --card-foreground: #12141C;
--popover: #FAFAF7;            --popover-foreground: #12141C;
--primary: #FF5B1F;            --primary-foreground: #FAFAF7;
--secondary: #12141C;          --secondary-foreground: #FAFAF7;
--muted: oklch(0.97 0 0);      --muted-foreground: oklch(0.556 0 0);
--accent: oklch(0.97 0 0);     --accent-foreground: oklch(0.205 0 0);
--destructive: #DC2626;
--border: oklch(0.922 0 0);    --input: oklch(0.922 0 0);   --ring: oklch(0.708 0 0);
```

From src/app/globals.css `.dark` (dark theme — mirror of scripts/check-dark-contrast.ts's hardcoded `dark` object, already WCAG-verified by Phase 7):
```
--background: #12141C;         --foreground: #FAFAF7;
--card: #1B1E29;                --card-foreground: #FAFAF7;
--primary: #FF7A45;             --primary-foreground: #12141C;
--secondary: #23283A;           --secondary-foreground: #FAFAF7;
--muted: #1F2230;               --muted-foreground: #A8ACBB;
--accent: #23283A;              --accent-foreground: #FAFAF7;
--destructive: #F87171;
--border: rgba(250, 250, 247, 0.35);
```

Existing color-math functions to reuse verbatim (do not reinvent) — from
`scripts/check-dark-contrast.ts`: `parseHex`, `parseRgba`, `compositeOverBackground`,
`channelToLinear`, `relativeLuminance`, `contrastRatio`. The new script must ALSO add an
`parseOklch` (or equivalent) function converting `oklch(L C H)` -> sRGB 0-255, since the
light theme uses oklch for `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`,
`--border`, `--input`, `--ring` — these were never hex before and were never contrast-checked
(they're shadcn defaults, unmodified since scaffold). Use the standard CSS Color 4 OKLab ->
linear-sRGB matrix conversion (Björn Ottosson's formulas), gamma-encode via the existing sRGB
transfer function, then reuse `relativeLuminance`/`contrastRatio` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build and run the both-theme WCAG contrast checker</name>
  <files>scripts/check-wcag-contrast-full.ts, src/app/globals.css (only if a genuine failure requires a token fix)</files>
  <action>
Create `scripts/check-wcag-contrast-full.ts`, modeled directly on `scripts/check-dark-contrast.ts` (read it first — reuse its `parseHex`/`parseRgba`/`compositeOverBackground`/`channelToLinear`/`relativeLuminance`/`contrastRatio` functions verbatim, do not rewrite the WCAG math). Add:
1. A `parseOklch(oklch: string): RGB` function implementing the CSS Color 4 OKLab->linear-sRGB conversion for `oklch(L C H)` strings (L 0-1, C typically 0-0.4, H in degrees; note `oklch(0.97 0 0)` has C=0 meaning pure grayscale — verify the math collapses correctly to a gray RGB triple for C=0 cases, which most of this project's oklch tokens are).
2. Extend `parseColor` to dispatch to `parseOklch` when the string starts with `oklch(`.
3. Two token objects mirroring the CURRENT `src/app/globals.css` (read the file fresh, do not trust this plan's interface block if globals.css has changed since planning) — `light` (from `:root`) and `dark` (from `.dark`, can copy from the existing script's `dark` object).
4. The SAME 10-pair check list as `check-dark-contrast.ts` (foreground/background, muted-foreground/background, muted-foreground/muted, card-foreground/card, primary-foreground/primary, secondary-foreground/secondary, accent-foreground/accent, destructive/background, primary/background at 3:1, border/background at 3:1), run against BOTH `light` and `dark` objects — 20 total checks.
5. A runner that prints a table per theme (reuse the existing script's table-printing pattern) and exits 1 if any pair fails either theme.

Run `node_modules/.bin/tsx scripts/check-wcag-contrast-full.ts`. If any LIGHT-theme pair fails (the dark pairs are already Phase-7-verified and should still pass unchanged), this is a genuine regression: adjust the specific failing CSS custom property value in `src/app/globals.css` `:root` block (smallest possible change — e.g. darken/lighten the oklch lightness channel), re-run the script until all 20 checks pass, and note the adjustment. Do not touch `.dark` values unless the script's dark-theme checks (which should be unchanged from Phase 7) somehow fail — if they do, that is unexpected and must be investigated as a possible Phase 8-10 regression to a `.dark` value, not silently patched.
  </action>
  <verify>
    <automated>node_modules/.bin/tsx scripts/check-wcag-contrast-full.ts</automated>
  </verify>
  <done>Script exists, runs, exits 0, and prints 20/20 PASS (10 pairs x 2 themes) after any necessary token fix.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final hardcoded-content grep + full-milestone-range config/types diff</name>
  <files>.planning/phases/11-verificacion-cruzada-final/11-01-contrast-report.md</files>
  <action>
Two independent checks, both written up in `.planning/phases/11-verificacion-cruzada-final/11-01-contrast-report.md`:

**(a) Hardcoded-content grep.** Get the full file list touched across Phase 7-10 via `git diff --stat 0812dc4..HEAD --name-only -- src/ scripts/` (excludes `.planning/*`). For every `.tsx`/`.ts` file in that list (expect: `src/app/globals.css`, `src/blocks/Hero/Component.tsx`, `src/blocks/ResultsSection/Component.tsx`, `src/components/AuthorCard.tsx`, `src/components/CaseStudyCard.tsx`, `src/components/PostCard.tsx`, `src/components/Prose.tsx`, `src/components/SiteFooter.tsx`, `src/components/SiteHeader.tsx`, `src/components/ui/*.tsx`, `src/app/(frontend)/[locale]/blog/[slug]/page.tsx`, `src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx`), grep for JSX text nodes and string literals that look like real sentences (contain a space and are not inside `className`, not a Tailwind token, not an aria-label pulled from a variable) that are NOT sourced from a Payload field/prop or a `next-intl` `t(...)`/message-catalog call. Per Pitfall 5's guidance: a literal is acceptable only if it's genuinely static, non-content, non-localized UI chrome (e.g. a bare "/" separator or decorative rule). Read each flagged file directly (not grep-only) to confirm each match is either (1) already prop/field-sourced (false positive), (2) a legitimate next-intl-driven string, or (3) a genuine hardcoded-content violation. If (3) is found, fix it in this task: wire it to the correct Payload field (if the block already has one) or a new `next-intl` message-catalog entry in both `en.json`/`es.json` (if it's UI chrome) — do not add a new Payload field in this audit-only phase (that would be new schema work out of scope; if a genuine new-field need is discovered, flag it in the report as a follow-up rather than silently expanding scope).

**(b) Full-range config/types diff.** Run `git diff --stat 2e22e9b..HEAD -- 'src/blocks/*/config.ts' src/payload-types.ts` (2e22e9b = Phase 5 close, the actual milestone start per 11-CONTEXT.md, covering Phase 7-10 in one pass rather than four separate per-phase diffs). Expect empty output (each phase's own VERIFICATION.md already confirmed this per-phase). Record the exact command and its (expected empty) output in the report as the FULL-milestone-range confirmation UI-13 requires, distinct from any single phase's narrower check.

Write both results into the report file with a clear PASS/FAIL per check and the file list actually scanned.
  </action>
  <verify>
    <automated>git diff --stat 2e22e9b..HEAD -- 'src/blocks/*/config.ts' src/payload-types.ts | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>Report file exists documenting zero hardcoded-content findings (or documents fixes applied for any genuine finding) and confirms zero diff in config.ts/payload-types.ts across the full 2e22e9b..HEAD range.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev/CI environment -> repo source files | This plan only reads/greps source and writes a script + report; no user input, no network, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-11-01 | Tampering | `scripts/check-wcag-contrast-full.ts` (new script, no external deps) | accept | Pure computation over hardcoded string literals mirrored from `globals.css`; no external input, no network, no package install — no attack surface introduced. |
| T-11-02 | Information Disclosure | `.planning/.../11-01-contrast-report.md` | accept | Report contains only design-token values and file-scan results already public in the repo; nothing sensitive. |
</threat_model>

<verification>
- `node_modules/.bin/tsx scripts/check-wcag-contrast-full.ts` exits 0 with 20/20 PASS.
- `git diff --stat 2e22e9b..HEAD -- 'src/blocks/*/config.ts' src/payload-types.ts` returns empty.
- Report file documents both results plus the hardcoded-content grep outcome (zero findings, or fixes applied and described).
</verification>

<success_criteria>
Both themes pass WCAG AA on every checked token pair on the current codebase state; the full Phase 5-close-to-HEAD diff shows zero schema/type drift; no hardcoded content remains in any Phase 7-10 touched file (or any found instance was fixed).
</success_criteria>

<output>
Create `.planning/phases/11-verificacion-cruzada-final/11-01-SUMMARY.md` when done
</output>
</plan>
