---
phase: 11-verificacion-cruzada-final
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-es-layout-final.mjs
  - .planning/phases/11-verificacion-cruzada-final/11-02-es-layout-report.md
autonomous: false
requirements: [UI-12]

must_haves:
  truths:
    - "Home (/es), authors list (/es/authors), an author detail (/es/authors/[real-author-slug]), and case-studies list (/es/case-studies) all render without overflow/clipping/broken layout against the longest real ES content available in the migrated data, at mobile/tablet/desktop widths."
    - "Where real content is genuinely absent (0 case studies, minimal author credentials — the already-accepted Phase 4/10 content gap), the page type is still confirmed to render a clean, non-broken structural/empty state in ES, and the gap is documented rather than silently skipped."
  artifacts:
    - path: "scripts/verify-es-layout-final.mjs"
      provides: "Automated route-level check (HTTP 200 + verbatim longest-title substring match) for the remaining untested ES page types"
    - path: ".planning/phases/11-verificacion-cruzada-final/11-02-es-layout-report.md"
      provides: "Documented longest-content findings per page type + human visual-verification outcome"
  key_links:
    - from: "scripts/verify-es-layout-final.mjs"
      to: "running next dev/production server"
      via: "fetch()"
      pattern: "fetch\\(.*locale.*(authors|case-studies)"
---

<objective>
Verify `/es` layout against the longest real migrated ES content for the page types Phase 10 did NOT already cover (Phase 10 covered blog post cards/detail specifically). This phase covers: home, authors list, an author detail, case-studies list. Case-study detail is structurally checked only (0 real case studies exist — accepted content gap per 11-CONTEXT.md/Phase 4/10 findings), not stress-tested with real long content.

Purpose: UI-12 requires ES layout verification against real longest titles across ALL touched page types, not just the blog cards Phase 10 spot-checked. Spanish runs 15-25% longer than English (Pitfall 3) — the remaining untested surfaces (home hero/blocks, authors bio/credentials, case-studies list empty-state) are exactly where an untested layout could silently break for ES users.
Output: `scripts/verify-es-layout-final.mjs` (route-level automated check, same pattern as Phase 10's `verify-phase10-cards-eeat.mjs`) + a written report + a human visual-verification checkpoint (Phase 10 established that pixel-level overflow judgment requires a real browser, since no viewport-resize tool exists in this environment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-verificacion-cruzada-final/11-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md

<interfaces>
<!-- Known content-gap facts from Phase 10's independent DB verification, do not
     re-derive from scratch — confirm still true, then proceed accordingly. -->

- `case_studies` table: 0 rows (confirmed by Phase 8 AND Phase 10 verification via direct query/API — `totalDocs: 0`). Case-studies list page (`/es/case-studies`) will render its genuine empty state; case-study detail page cannot be tested with real content at all (no real slug exists).
- `authors` table: exactly 1 row (id=1, "Juan Carlos Angulo"), with `years_experience: null` and zero rows in `authors_credentials`/`authors_social_links` (confirmed by Phase 10). This is the ONLY real author detail page (`/es/authors/{slug-of-id-1}`) testable — it will render with bio present (if populated) but without credentials/years/social — an accepted content gap, not a code defect.
- Posts: 72 real migrated posts exist; longest ES title precedent from Phase 10 is exactly 75 characters (ids 53, 66), pre-truncated at the DATA level (not CSS truncation).

Route paths (App Router, confirmed to exist):
`src/app/(frontend)/[locale]/authors/page.tsx`, `src/app/(frontend)/[locale]/authors/[slug]/page.tsx`,
`src/app/(frontend)/[locale]/case-studies/page.tsx`, `src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx`,
`src/app/(frontend)/[locale]/page.tsx` (home).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Identify longest real ES content per page type + automated route check</name>
  <files>scripts/verify-es-layout-final.mjs, .planning/phases/11-verificacion-cruzada-final/11-02-es-layout-report.md</files>
  <action>
Using Payload's Local API (same pattern as `scripts/verify-phase10-cards-eeat.mjs` — `getPayload({ config })` importing `src/payload.config.ts`), query for ES-locale content driving each remaining untested page type:
1. Home page: read the Pages collection doc used as home (or the relevant global/page config rendering `/`) and identify its longest ES text fields (hero title/subtitle, any block heading/body text) actually rendered on the page.
2. Authors list: confirm it lists the single real author (id=1) plus whatever seed/test authors may exist; note the real author's ES `bio`/`jobTitle` length.
3. Author detail (`/es/authors/{id-1-slug}`): confirm the ES bio field's actual character count (this is the longest real ES author content available — there is only one row).
4. Case-studies list: confirm `totalDocs: 0` still holds (re-run the same API check Phase 8/10 used) — if it does, this page type is verified only at the structural/empty-state level, documented as such, not a gap to chase further (already accepted per 11-CONTEXT.md known content gaps).

Write `scripts/verify-es-layout-final.mjs` (zero new dependencies, `fetch()`-based against `SMOKE_BASE_URL`/`BASE_URL` env var defaulting to `http://localhost:3000`, matching the Phase 8/10 script pattern) that: fetches `/es`, `/es/authors`, `/es/authors/{real-slug}`, `/es/case-studies` and asserts (a) HTTP 200, (b) no error markers (reuse the `ERROR_MARKERS` list pattern from `scripts/verify-phase10-cards-eeat.mjs`), (c) for the author detail page, the full real ES bio text (if present) appears verbatim in the response body (not truncated/cut by a server-side error). Run it against a live `npm run dev` server you start for this check. Document all findings (content lengths per page type, script pass/fail, the case-studies empty-state note) in `.planning/phases/11-verificacion-cruzada-final/11-02-es-layout-report.md`.
  </action>
  <verify>
    <automated>node scripts/verify-es-layout-final.mjs</automated>
  </verify>
  <done>Script exists and passes against a live dev server; report documents the real ES content lengths found per page type and the case-studies/authors content-gap notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of ES layout across home/authors/case-studies</name>
  <files></files>
  <what-built>
Automated route-level checks (Task 1) confirmed all four page types return 200 with no errors and the real ES content renders verbatim server-side. What automation CANNOT confirm is pixel-level visual layout — whether the longest real ES strings found in Task 1 wrap, overflow, or break card/grid alignment in an actual browser viewport.
  </what-built>
  <how-to-verify>
With `npm run dev` running, open a real browser and check these URLs at three widths (~375px mobile, ~768px tablet, ~1280px desktop):
1. `http://localhost:3000/es` — home page, hero and any block text
2. `http://localhost:3000/es/authors` — authors list card(s)
3. `http://localhost:3000/es/authors/{real-author-slug}` — author detail (bio, and if populated, credentials/years/social — note if these are empty due to the known content gap, that's expected, not a bug)
4. `http://localhost:3000/es/case-studies` — case-studies list, should show a clean empty state (0 real case studies is expected — confirm the empty state itself looks intentional, not broken/blank-with-leftover-grid-artifacts)

Confirm: no horizontal overflow, no clipped/cut-off text, no broken card height/alignment, headings wrap onto multiple lines cleanly where they're long, empty states (case-studies list) look deliberate rather than broken.
  </how-to-verify>
  <action>
Present the four URLs and breakpoints above to Juan and wait for explicit confirmation (or a described issue) before this plan closes. If an issue is described, fix it (adjust the relevant component's CSS — min-height/line-clamp/flex wrap per Pitfall 3's guidance, not a hardcoded truncation) and re-request verification.
  </action>
  <verify>
    <automated>node scripts/verify-es-layout-final.mjs</automated>
  </verify>
  <done>Juan has typed "approved" confirming no visual overflow/clipping/broken-layout issue across the four page types and three breakpoints, or any described issue has been fixed and re-confirmed.</done>
  <resume-signal>Type "approved" or describe any layout issue found (will be fixed before this plan closes)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev server -> browser | Read-only visual check against locally rendered pages; no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-11-03 | Tampering | `scripts/verify-es-layout-final.mjs` (fetch-only, no writes) | accept | Read-only HTTP GETs against a local dev server; no external network calls, no package installs. |
</threat_model>

<verification>
- `node scripts/verify-es-layout-final.mjs` passes against a live dev server.
- Human checkpoint confirms no visual overflow/clipping issue across all four page types at three breakpoints, or any found issue is fixed and re-verified before this plan closes.
</verification>

<success_criteria>
All ES page types not covered by Phase 10 (home, authors list/detail, case-studies list) are confirmed to render cleanly with the longest real available content, and the case-studies-detail/authors-credentials content gap is explicitly documented as accepted (not silently skipped).
</success_criteria>

<output>
Create `.planning/phases/11-verificacion-cruzada-final/11-02-SUMMARY.md` when done
</output>
