---
phase: 43-performance-response-time-html-size
plan: 03
type: execute
wave: 3
depends_on: ["43-02"]
files_modified:
  - "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
  - "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Un post de detalle real (/blog/tech-seo-guide o equivalente ES) sirve su query desde cache — 2do request al mismo slug+locale es más rápido que el primero."
    - "Un case-study de detalle real (/case-studies/migracion-ecommerce-nextjs-seo-tecnico o equivalente ES) sirve su query desde cache."
    - "Medición real post-fix (curl timing) muestra mejora de tiempo de respuesta de Home respecto al baseline de 1.58-2.4s documentado en 43-CONTEXT.md (PERF-01, ROADMAP Success Criteria #3)."
    - "Medición real (curl | wc -c) muestra el HTML de Home reducido de forma significativa respecto al baseline de 271KB/276-283KB (PERF-02, ROADMAP Success Criteria #4)."
    - "Sin regresión de Lighthouse/CWV mobile en las rutas representativas del sitio, comparado contra el baseline más reciente del proyecto (ROADMAP Success Criteria #5)."
  artifacts:
    - ".planning/phases/43-performance-response-time-html-size/lh-phase43-post.json"
  key_links:
    - "getPost (blog/[slug]/page.tsx) -> getCachedPost(slug, locale), ya construido en 43-01"
    - "getCaseStudy (case-studies/[slug]/page.tsx) -> getCachedCaseStudy(slug, locale), ya construido en 43-01"
---

<objective>
Cerrar el tracer de 43-CONTEXT.md cacheando las 2 rutas de detalle restantes (post individual, case-study individual) con el mismo patrón de las 3 plans anteriores, y correr la verificación final de fase: medición real de tiempo de respuesta + tamaño de HTML de Home, y el gate de regresión Lighthouse/CWV contra el baseline más reciente del proyecto.

Purpose: Cierra PERF-01/PERF-02 con las 5 rutas nombradas en 43-CONTEXT.md cubiertas, y produce la evidencia real que ROADMAP Phase 43 exige (Success Criteria #3/#4/#5) — no solo teórica.
Output: Post detail + Case-study detail sirviendo desde cache; medición real de mejora en Home; JSON de Lighthouse post-fix comparado contra el baseline de Phase 36.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-performance-response-time-html-size/43-CONTEXT.md
@.planning/phases/43-performance-response-time-html-size/43-01-SUMMARY.md
@.planning/phases/43-performance-response-time-html-size/43-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Post detail — cachear getPost</name>
  <read_first>
    "src/app/(frontend)/[locale]/blog/[slug]/page.tsx":27-37,94-95 (getPost: payload.find sobre 'posts', where slug, locale, depth:1, sin overrideAccess, llamado 2 veces por request — generateMetadata + body)
    src/lib/cache.ts (getCachedPost ya construido en 43-01 — firma: slug, locale, sin select, depth:1 preservado, tag CACHE_TAGS.post(slug))
  </read_first>
  <files>"src/app/(frontend)/[locale]/blog/[slug]/page.tsx"</files>
  <action>
    En `"src/app/(frontend)/[locale]/blog/[slug]/page.tsx"`, reemplazar el cuerpo de `getPost(locale, slug)` por `return getCachedPost(slug, locale)` (import desde `@/lib/cache`) — preserva `depth:1` exacto, agrega `overrideAccess:false` (ya incluido en el fetcher de 43-01, un post en borrador nunca debería renderizar públicamente — mismo patrón de seguridad que Home). No se aplica select scoping acá: la vista de detalle necesita `content` (richText) completo para renderizar el artículo, a diferencia del grid/card.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio.
    - `grep -n "getCachedPost" "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"` confirma que `getPost` ya no arma su propia query.
    - MEJOR ESFUERZO: `curl -o /dev/null -s -w '%{time_total}\n' http://localhost:3000/blog/tech-seo-guide` (o el slug real equivalente confirmado en el dev server) dos veces seguidas — el 2do debe ser más rápido. Diferir a producción si la DB local no conecta.
  </acceptance_criteria>
  <done>Post detail sirve desde cache con el mismo TTL/invalidación que el resto del tracer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Case-study detail — cachear getCaseStudy + verificación final de fase</name>
  <read_first>
    "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx":26-36,104-105 (getCaseStudy: payload.find sobre 'case-studies', where slug, locale, depth:1, sin overrideAccess, llamado 2 veces por request)
    src/lib/cache.ts (getCachedCaseStudy ya construido en 43-01 — firma: slug, locale, sin select, depth:1 preservado, tag CACHE_TAGS.caseStudy(slug))
    scripts/lighthouse-mobile.mjs (runner reusable ya usado en Phases 11/17/25/28/32/36 — `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3000 --out <path>`, rutas default: /en, /es, /en/blog, /en/blog/tech-seo-guide, /en/case-studies, /en/case-studies/migracion-ecommerce-nextjs-seo-tecnico — cubren Home/Blog listing/Post detail/Case-study listing+detail, las mismas rutas de este tracer salvo Servicios)
    .planning/milestones/v1.7-phases/36-regression-gate/lh-phase36-post.json (baseline Lighthouse más reciente del proyecto — no hay captura posterior en Phases 37-42, que fueron de contenido/meta tags, no de UI/performance)
  </read_first>
  <files>"src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx", .planning/phases/43-performance-response-time-html-size/lh-phase43-post.json</files>
  <action>
    En `"src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"`, reemplazar el cuerpo de `getCaseStudy(locale, slug)` por `return getCachedCaseStudy(slug, locale)` (import desde `@/lib/cache`) — mismo patrón que Task 1.

    Verificación final de fase (best-effort, con fallback a producción documentado si la DB local no conecta — mismo patrón intermitente que Phase 41/42, no bloquea el cierre):
    1. Con `npm run build && npm run start` corriendo en `localhost:3000`: medir Home con `curl -o /dev/null -s -w '%{time_total}\n' http://localhost:3000/` (warm, 2do request) y comparar contra el baseline de 1.58-2.4s de 43-CONTEXT.md; medir `curl -s http://localhost:3000/ | wc -c` y comparar contra el baseline de 271KB/276-283KB.
    2. Correr `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3000 --out .planning/phases/43-performance-response-time-html-size/lh-phase43-post.json` contra el server local recién buildeado (mobile form-factor, mismas 6 rutas default del script). Comparar cada score (`performance`, `accessibility`, `best-practices`, `seo`, `lcpMs`, `cls`, `tbtMs`) contra `.planning/milestones/v1.7-phases/36-regression-gate/lh-phase36-post.json` — ninguna categoría debe caer más de ~5 puntos ni entrar en banda "poor" de CWV (mismo umbral usado en Phase 17/25/28/32/36).
    3. Si la conectividad local a Neon falla (ECONNRESET u otro error, patrón ya documentado en sesiones previas de este proyecto), documentar el intento fallido explícitamente en el SUMMARY y diferir la confirmación en vivo a producción (https://juan-tech.com) post-deploy — la evidencia estática de las 3 plans (dedup por grep, select/populate scoping por lectura de código, hooks de invalidación reales, tsc limpio en las 6 tareas) es la evidencia primaria aceptada para cerrar esta fase si la medición en vivo queda bloqueada por el entorno, no por el código.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio.
    - `grep -n "getCachedCaseStudy" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"` confirma que `getCaseStudy` ya no arma su propia query.
    - `.planning/phases/43-performance-response-time-html-size/lh-phase43-post.json` existe y cada categoría se compara explícitamente contra `lh-phase36-post.json` en el SUMMARY (números reales lado a lado, no solo "sin regresión" sin evidencia) — O, si bloqueado por conectividad local, el SUMMARY documenta el error real recibido y el plan de confirmación diferida a producción.
    - El SUMMARY reporta el timing/tamaño de Home antes/después con números reales (no solo "mejoró") — O el mismo fallback documentado arriba.
  </acceptance_criteria>
  <done>Case-study detail sirve desde cache; las 5 rutas del tracer de 43-CONTEXT.md (Home, Servicios índice, Blog listing, post detail, case-study detail) están cubiertas; evidencia real (o fallback documentado) de mejora de tiempo de respuesta, tamaño de HTML, y ausencia de regresión Lighthouse/CWV queda registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Mismas boundaries que 43-01/43-02. Esta plan solo agrega 2 call sites más sobre fetchers ya mitigados.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-07 | Information Disclosure | `getCachedPost`/`getCachedCaseStudy` aplicados a las rutas de detalle | high | mitigate | Reusa los fetchers de 43-01 (`overrideAccess:false` ya incluido) — un post/case-study en borrador servido vía URL directa (`/blog/<draft-slug>`) nunca queda cacheado ni servido públicamente. Sin este guard, cachear la query habría sido estrictamente peor que el bug actual (que ya existía sin cache, per el mismo patrón WR-02 de Phase 24) porque amplificaría la exposición a TODOS los visitantes por hasta 60s. |
| T-43-08 | Repudiation (n/a, informativo) | Lighthouse regression gate | n/a | accept | El gate compara contra un baseline capturado en Phase 36 (2026-07-14); cualquier drift de contenido real entre esa fecha y hoy (nuevo contenido, imágenes) puede introducir ruido no atribuible a esta fase — mismo criterio de "ruido de medición" ya usado y documentado en Phase 25/36 (reproducibilidad de 3 corridas antes de aceptar una lectura anómala). |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio.
- Las 5 rutas del tracer sirven desde cache con `overrideAccess:false` preservado.
- Evidencia real (o fallback documentado con causa exacta) de: tiempo de respuesta de Home, tamaño de HTML de Home, comparación Lighthouse contra `lh-phase36-post.json`.
</verification>

<success_criteria>
Las 5 rutas nombradas en 43-CONTEXT.md (Home, Servicios índice, Blog listing, post detail, case-study detail) sirven desde `unstable_cache` con invalidación real vía hooks; PERF-01 y PERF-02 cuentan con evidencia real de mejora (medida localmente o diferida a producción con causa documentada); sin regresión Lighthouse/CWV frente al baseline de Phase 36.
</success_criteria>

## Artifacts this phase produces

- `"src/app/(frontend)/[locale]/blog/[slug]/page.tsx"` (modificado) — `getPost` delega a `getCachedPost`.
- `"src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"` (modificado) — `getCaseStudy` delega a `getCachedCaseStudy`.
- `.planning/phases/43-performance-response-time-html-size/lh-phase43-post.json` (nuevo) — captura Lighthouse post-fix para comparación contra `lh-phase36-post.json`.

<output>
Create `.planning/phases/43-performance-response-time-html-size/43-03-SUMMARY.md` when done
</output>
