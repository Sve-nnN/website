---
phase: 12-author-page-e-e-a-t-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Authors/index.ts
  - src/migrations/index.ts
  - src/payload-types.ts
autonomous: true
requirements: [AUTHOR-01]

must_haves:
  truths:
    - "Un editor puede completar expertise[], education[] y experience[] en la colección Authors desde /admin"
    - "El cambio de schema está commiteado como migración generada por payload migrate:create, con push:false respetado (sin push directo a Postgres)"
  artifacts:
    - path: "src/collections/Authors/index.ts"
      provides: "Campos expertise (array de topic localizado), education (array degree/institution/logo/startDate/endDate/description) y experience (array company/role/startDate/endDate/description), localizados donde corresponde"
      contains: "expertise"
    - path: "src/migrations/index.ts"
      provides: "Nueva entrada de migración que agrega las columnas de Postgres para expertise/education/experience"
    - path: "src/payload-types.ts"
      provides: "Tipo Author actualizado con expertise/education/experience tras payload generate:types"
      contains: "expertise"
  key_links:
    - from: "src/collections/Authors/index.ts"
      to: "src/migrations"
      via: "payload migrate:create genera el diff de schema para los 3 arrays nuevos"
      pattern: "expertise|education|experience"
---

<objective>
Recuperar en la colección `Authors` los tres arrays recortados intencionalmente en Phase 1 — `expertise[]`, `education[]` y `experience[]` — per AUTHOR-01, y generar/aplicar/commitear la migración de Postgres correspondiente respetando `push:false`.

Purpose: Este plan es la base de contrato de datos para los otros dos plans de la fase (UI del author page y seed de contenido real) — ambos dependen de que estos campos existan en el schema y en `payload-types.ts` antes de poder escribir contra ellos.
Output: `src/collections/Authors/index.ts` con los 3 campos nuevos, migración commiteada bajo `src/migrations/`, `payload-types.ts` regenerado con el tipo `Author` actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-author-page-e-e-a-t-expansion/12-CONTEXT.md
@src/collections/Authors/index.ts
@src/fields/slug.ts
</context>

<interfaces>
Patrón existente de array field localizado en este proyecto (de `credentials` en `src/collections/Authors/index.ts`), a seguir para los 3 arrays nuevos:

    {
      name: 'credentials',
      type: 'array',
      label: { en: 'Credentials', es: 'Credenciales' },
      fields: [
        { name: 'label', type: 'text', localized: true, required: true },
      ],
    },

Patrón de campo `date` ya usado en este proyecto (de `src/collections/Posts/index.ts`, campo `publishedAt`):

    { name: 'publishedAt', type: 'date', admin: { position: 'sidebar' } },

Nota importante (decisión CONTEXT.md, NO agregar): el sitio de referencia (JuanPortfolio) tiene un campo `certificate` (upload) en `education[]`. CONTEXT.md decide explícitamente NO portarlo — "no hay archivos reales de certificados disponibles". No agregues `certificate` a `education`; el nombre de la sección ("Educación y Certificaciones") es solo copy de UI, no implica un campo de upload adicional.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar campos expertise/education/experience a la colección Authors</name>
  <files>src/collections/Authors/index.ts</files>
  <action>
    Agrega tres campos nuevos tipo `array` a `Authors.fields`, ubicados después de `credentials` y antes de `yearsExperience` (agrupando los campos E-E-A-T juntos):

    1. `expertise` — label `{ en: 'Expertise', es: 'Expertise' }` (mismo loanword en ambos locales, per 12-UI-SPEC.md), `admin.description` explicando "Temas de especialización técnica, renderizados como tags en el author page". Sub-campo:
       - `topic` (text, `localized: true`, `required: true`)

    2. `education` — label `{ en: 'Education & Certifications', es: 'Educación y Certificaciones' }`, `admin.initCollapsed: true`. Sub-campos:
       - `degree` (text, `localized: true`, `required: true`)
       - `institution` (text, `localized: true`, `required: true`) — nombre propio de institución, aunque el campo esté marcado localized (para permitir excepciones futuras), en la práctica el mismo valor se escribe en ambos locales
       - `logo` (upload, `relationTo: 'media'`, opcional — sin `required`)
       - `startDate` (date, `admin: { date: { pickerAppearance: 'monthOnly' } }`)
       - `endDate` (date, `admin: { date: { pickerAppearance: 'monthOnly' } }`, opcional — ausente/null significa "en curso", el author page lo debe renderizar como "Presente"/"Present")
       - `description` (textarea, `localized: true`, opcional)

    3. `experience` — label `{ en: 'Experience', es: 'Experiencia' }`, `admin.initCollapsed: true`. Sub-campos:
       - `company` (text, `localized: true`, `required: true`)
       - `role` (text, `localized: true`, `required: true`)
       - `startDate` (date, `admin: { date: { pickerAppearance: 'monthOnly' } }`)
       - `endDate` (date, `admin: { date: { pickerAppearance: 'monthOnly' } }`, opcional)
       - `description` (textarea, `localized: true`, opcional)

    No agregues `certificate` a `education` (ver `<interfaces>` — decisión CONTEXT.md explícita). No toques `name`, `jobTitle`, `bio`, `avatar`, `credentials`, `yearsExperience`, `socialLinks` ni `slugField('name')` — deben permanecer exactamente como están. Actualiza también el comment de cabecera del archivo (líneas 5-19) que actualmente dice "Trimmed... does NOT port the heavy education/experience/socialMedia/expertise arrays" — ya no es cierto tras este plan; corrígelo para reflejar que `expertise`/`education`/`experience` fueron recuperados en Phase 12 (AUTHOR-01), sin borrar el resto del comment (contexto histórico de Phase 1 sigue siendo válido para `name`/`jobTitle`/`bio`/`avatar`/`credentials`/`yearsExperience`/`socialLinks`).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && grep -q "name: 'expertise'" src/collections/Authors/index.ts && grep -q "name: 'education'" src/collections/Authors/index.ts && grep -q "name: 'experience'" src/collections/Authors/index.ts && ! grep -q "name: 'certificate'" src/collections/Authors/index.ts && echo OK</automated>
  </verify>
  <done>La colección Authors tiene los 3 campos array nuevos con los sub-campos especificados, certificate no fue portado, y los campos preexistentes no cambiaron.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generar migración, aplicarla y regenerar payload-types.ts</name>
  <files>src/migrations/index.ts, src/payload-types.ts</files>
  <action>
    Corre `payload migrate:create` con un nombre descriptivo (por ejemplo `phase12_author_eeat_fields`) para que Payload introspeccione el diff de schema entre el estado actual de Postgres y los 3 campos nuevos del Task 1, y genere el archivo de migración up/down bajo `src/migrations/`. Confirma que el archivo se registró automáticamente en `src/migrations/index.ts`, siguiendo el patrón ya usado por las migraciones previas de este proyecto (ver el array de migraciones al final de ese archivo). Aplica la migración contra la base de datos de desarrollo con `payload migrate`. No uses `push: true` en ningún entorno — `src/payload.config.ts` ya tiene `push: false` fijo, no lo cambies.

    Después de aplicar la migración, corre `payload generate:types` para regenerar `src/payload-types.ts` y confirmar que la interfaz `Author` incluye ahora `expertise`, `education` y `experience` con la forma correcta (arrays de objetos con los sub-campos del Task 1, `logo` tipado como relación opcional a `Media`, fechas como `string | null`).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && ls src/migrations | grep -i "phase12\|author_eeat" && grep -q "expertise" src/payload-types.ts && grep -q "education" src/payload-types.ts && grep -q "experience" src/payload-types.ts && echo OK</automated>
  </verify>
  <done>Existe un archivo de migración nuevo bajo src/migrations/ referenciando el cambio de Authors, registrado en src/migrations/index.ts, la migración se aplicó exitosamente contra Postgres, y src/payload-types.ts refleja expertise/education/experience en la interfaz Author.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin de Payload -> Postgres | Un editor autenticado completa expertise/education/experience desde /admin; los valores se persisten sin validación adicional más allá de los constraints declarados en el schema (required en topic/degree/institution/company/role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-01 | Tampering | Migración generada por payload migrate:create | mitigate | Migración se revisa (diff up/down) antes de commitear y se aplica con push:false, nunca con push directo |
| T-12-02 | Information Disclosure | education[].logo como relationTo:media | accept | Media es una colección de lectura pública (access.read: () => true) desde Phase 1; no introduce un nuevo canal de exposición — el logo institucional no es información sensible |

</threat_model>

<verification>
- `payload migrate` corre sin errores contra la base de datos de desarrollo.
- `src/payload-types.ts` incluye `expertise`/`education`/`experience` en la interfaz `Author`.
- El admin de Payload muestra los 3 campos nuevos dentro de la colección Authors sin romper los campos existentes (`name`/`jobTitle`/`bio`/`avatar`/`credentials`/`yearsExperience`/`socialLinks`/`slug`).
</verification>

<success_criteria>
La colección Authors tiene el schema completo para expertise/education/experience, respaldado por una migración commiteada con push:false respetado, y payload-types.ts refleja los 3 campos nuevos. Cumple ROADMAP Phase 12 Success Criteria #1 (AUTHOR-01).
</success_criteria>

<output>
Create `.planning/phases/12-author-page-e-e-a-t-expansion/12-01-SUMMARY.md` when done
</output>
</content>
