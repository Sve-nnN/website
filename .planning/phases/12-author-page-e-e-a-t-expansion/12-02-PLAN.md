---
phase: 12-author-page-e-e-a-t-expansion
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
autonomous: true
requirements: [AUTHOR-02, AUTHOR-03, AUTHOR-04, AUTHOR-05]

must_haves:
  truths:
    - "El author page renderiza una sección Expertise (tags) cuando expertise[] tiene datos, diseñada con ui-ux-pro-max"
    - "El author page renderiza una sección Educación y Certificaciones (grid con logo/institución/fechas) cuando education[] tiene datos, diseñada con ui-ux-pro-max"
    - "El author page renderiza una sección Experiencia (timeline laboral) cuando experience[] tiene datos, diseñada con ui-ux-pro-max"
    - "El Person JSON-LD del author page incluye sameAs (redes sociales), knowsAbout (desde expertise[]) y hasCredential (desde education[])"
  artifacts:
    - path: "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"
      provides: "3 secciones nuevas condicionales (Expertise/Educación/Experiencia) + personData JSON-LD enriquecido con sameAs/knowsAbout/hasCredential"
      contains: "knowsAbout"
  key_links:
    - from: "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"
      to: "doc.expertise / doc.education / doc.experience"
      via: "Local API payload.find ya trae estos campos porque forman parte del doc Author tras 12-01"
      pattern: "doc\\.(expertise|education|experience)"
---

<objective>
Agregar al author page (`src/app/(frontend)/[locale]/authors/[slug]/page.tsx`) las 3 secciones nuevas especificadas en `12-UI-SPEC.md` — Expertise (tag cloud), Educación y Certificaciones (grid de tarjetas) y Experiencia (timeline vertical) — y enriquecer el objeto `personData` del Person JSON-LD existente con `sameAs`, `knowsAbout` y `hasCredential`.

Purpose: Estas son las secciones visibles que demuestran E-E-A-T real en el author page (AUTHOR-02 a AUTHOR-05) — el schema de 12-01 no tiene valor si no se renderiza.
Output: `page.tsx` con las 3 secciones condicionales insertadas entre `AuthorCard` y los grids de posts/case studies, y `personData` enriquecido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-author-page-e-e-a-t-expansion/12-CONTEXT.md
@.planning/phases/12-author-page-e-e-a-t-expansion/12-UI-SPEC.md
@src/app/(frontend)/[locale]/authors/[slug]/page.tsx
@src/components/AuthorCard.tsx
</context>

<interfaces>
Tipo `Author` (tras 12-01, campos relevantes — usar directamente, no re-explorar el schema):

    interface Author {
      id: number
      name: string
      jobTitle?: string | null
      bio?: string | null
      avatar?: (number | null) | Media
      credentials?: { label: string; id?: string | null }[] | null
      yearsExperience?: number | null
      socialLinks?: { platform: 'linkedin' | 'github' | 'x' | 'website'; url: string; id?: string | null }[] | null
      expertise?: { topic: string; id?: string | null }[] | null
      education?: {
        degree: string
        institution: string
        logo?: (number | null) | Media
        startDate?: string | null
        endDate?: string | null
        description?: string | null
        id?: string | null
      }[] | null
      experience?: {
        company: string
        role: string
        startDate?: string | null
        endDate?: string | null
        description?: string | null
        id?: string | null
      }[] | null
      slug?: string | null
    }

Confirma la forma exacta leyendo `src/payload-types.ts` (generado por 12-01) antes de escribir — los nombres arriba son los mismos definidos en 12-01-PLAN.md, pero el tipo generado es la fuente de verdad.

JSX exacto de las 3 secciones (de `12-UI-SPEC.md` "Section Specs" — usar como base, ajustar solo lo necesario para integrarse con el resto de `page.tsx`, p.ej. `Image` de `next/image` para el logo, no `<img>`):

- Expertise: `Badge variant="secondary"` en `flex flex-wrap gap-2`, ver 12-UI-SPEC.md sección "1. Expertise".
- Educación: `Card` con logo/fallback `GraduationCap`, grid `grid-cols-1 md:grid-cols-2 gap-6`, ver 12-UI-SPEC.md sección "2. Educación y Certificaciones".
- Experiencia: rail vertical `bg-border` + dots `bg-primary`, ver 12-UI-SPEC.md sección "3. Experiencia" — el executor DEBE verificar visualmente que el dot quede centrado en la línea en 375/768/1280px y ajustar el offset unos px si `size-3.5` no queda centrado (tolerancia de implementación explícita en el spec, no una violación).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar las 3 secciones nuevas (Expertise, Educación y Certificaciones, Experiencia) al author page</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx</files>
  <action>
    Extiende el diccionario `copy` existente (líneas 40-43) con las claves nuevas por locale, exactamente per 12-UI-SPEC.md "Copywriting Contract":
    - `expertise`: ES "Expertise" / EN "Expertise"
    - `education`: ES "Educación y Certificaciones" / EN "Education & Certifications"
    - `experience`: ES "Experiencia" / EN "Experience"
    - `present`: ES "Presente" / EN "Present"

    Importa `Badge` de `@/components/ui/badge`, `Card` de `@/components/ui/card`, `GraduationCap` de `lucide-react`, e `Image` de `next/image`.

    Agrega una función helper `formatDateRange(startDate, endDate, locale, presentLabel)` (module-level, arriba del componente) que use `Intl.DateTimeFormat(locale, { month: 'short', year: 'numeric' })` para formatear `startDate` y (si existe) `endDate`, uniéndolos con un en dash (` – `). Si `endDate` es null/undefined, usa `presentLabel` en su lugar. Maneja `startDate` null/undefined devolviendo string vacío (no debería ocurrir con datos reales, pero el UI-SPEC exige degradación elegante).

    Inserta las 3 secciones nuevas, en este orden, después de `<AuthorCard author={doc} />` y antes de la sección de `posts` (Expertise → Educación y Certificaciones → Experiencia — orden lógico E-E-A-T, locked en 12-UI-SPEC.md). Cada sección envuelta en `<section className="mt-12">` (mismo patrón que las secciones existentes de posts/caseStudies) y renderizada solo si el array correspondiente tiene `.length > 0` (mismo contrato de "omitir si vacío", sin empty state público — per 12-UI-SPEC.md "Copywriting Contract").

    Usa el JSX de `12-UI-SPEC.md` "Section Specs" 1/2/3 como base literal, con estos ajustes de integración: usa `next/image` (`<Image src={logo.url} alt={...} width={48} height={48} className="rounded-md object-contain" />`) para el logo de educación en vez de `<img>`; usa `formatDateRange` para las 3 fechas (`dateRange` en el spec); usa `doc.expertise`/`doc.education`/`doc.experience` como fuente de datos (no `Author.expertise` genérico); usa `t.expertise`/`t.education`/`t.experience` para los headings.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && f='src/app/(frontend)/[locale]/authors/[slug]/page.tsx' && grep -q "doc.expertise" "$f" && grep -q "doc.education" "$f" && grep -q "doc.experience" "$f" && grep -q "GraduationCap" "$f" && grep -q "formatDateRange" "$f" && echo OK</automated>
  </verify>
  <done>Las 3 secciones (Expertise/Educación y Certificaciones/Experiencia) están insertadas entre AuthorCard y la sección de posts, cada una condicionada a length > 0, con formatDateRange manejando el fallback "Presente"/"Present".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enriquecer el Person JSON-LD con sameAs, knowsAbout y hasCredential</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx</files>
  <action>
    Extiende el objeto `personData` existente (líneas 75-81) agregando 3 propiedades nuevas, calculadas a partir del `doc` ya cargado, per CONTEXT.md ("JSON-LD generado server-side... agregar sameAs..."):

    - `sameAs`: `doc.socialLinks?.map((s) => s.url) ?? []` — array de URLs de redes sociales. Si `doc.socialLinks` está vacío, omite la propiedad por completo (no emitas `sameAs: []` — Google recomienda omitir propiedades vacías en JSON-LD, mismo criterio que el resto del objeto ya usa implícitamente para `jobTitle` opcional).
    - `knowsAbout`: `doc.expertise?.map((e) => e.topic) ?? []` — array de strings desde expertise[].topic. Omite la propiedad si el array resultante está vacío.
    - `hasCredential`: `doc.education?.map((ed) => ({ '@type': 'EducationalOccupationalCredential', name: ed.degree, organization: ed.institution, datePublished: ed.endDate }))` — per CONTEXT.md ("hasCredential desde education[]: name=degree, organization=institution, datePublished=endDate", literal, no agregues campos JSON-LD adicionales no pedidos como `recognizedBy` anidado). Omite la propiedad si `doc.education` está vacío o ausente.

    Implementa la omisión condicional construyendo `personData` con spread condicional (`...(doc.socialLinks?.length ? { sameAs: [...] } : {})`, mismo patrón para los otros 2) en vez de asignar arrays vacíos.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && f='src/app/(frontend)/[locale]/authors/[slug]/page.tsx' && grep -q "sameAs" "$f" && grep -q "knowsAbout" "$f" && grep -q "hasCredential" "$f" && grep -q "EducationalOccupationalCredential" "$f" && echo OK</automated>
  </verify>
  <done>personData incluye sameAs/knowsAbout/hasCredential calculados desde doc.socialLinks/doc.expertise/doc.education, cada uno omitido (no como array vacío) cuando el array fuente está vacío.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload Local API -> JSON-LD público | Los datos de expertise/education/experience/socialLinks se sirven sin sanitización adicional dentro de un `<script type="application/ld+json">` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-03 | Tampering / XSS | JsonLd component renderizando personData enriquecido | accept | El componente JsonLd ya existe y serializa con JSON.stringify dentro de un script tag (patrón pre-existente, no modificado por este plan); el contenido proviene de campos de texto admin-only (no input público), mismo nivel de confianza que jobTitle/name ya en producción |
| T-12-04 | Information Disclosure | education[].logo servido públicamente vía next/image | accept | Media ya es de lectura pública desde Phase 1; un logo institucional no es dato sensible |

</threat_model>

<verification>
- `npm run build` compila sin errores de tipos en `page.tsx` (los tipos de `Author` ya incluyen expertise/education/experience tras 12-01).
- Las 3 secciones nuevas solo aparecen cuando su array correspondiente tiene datos (verificar contra un Author sin esos campos poblados, si existe alguno en dev, o inspeccionar el JSX condicional directamente).
- El `<script type="application/ld+json">` del Person renderizado incluye `sameAs`/`knowsAbout`/`hasCredential` cuando el Author real (post-12-03 seed) los tiene poblados.
</verification>

<success_criteria>
El author page renderiza las 3 secciones E-E-A-T condicionalmente y el Person JSON-LD está enriquecido con sameAs/knowsAbout/hasCredential. Cumple ROADMAP Phase 12 Success Criteria #2-5 (AUTHOR-02 a AUTHOR-05).
</success_criteria>

<output>
Create `.planning/phases/12-author-page-e-e-a-t-expansion/12-02-SUMMARY.md` when done
</output>
</content>
