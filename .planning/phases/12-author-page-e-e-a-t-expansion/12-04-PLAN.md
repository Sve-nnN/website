---
phase: 12-author-page-e-e-a-t-expansion
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - scripts/verify-phase12-author-eeat.mjs
autonomous: false
requirements: [AUTHOR-02, AUTHOR-03, AUTHOR-04, AUTHOR-05, AUTHOR-06]

must_haves:
  truths:
    - "Las 3 secciones nuevas se ven correctamente en 375/768/1280px, en ambos temas (claro/oscuro) y ambos locales, con el contenido real seedeado en 12-03"
    - "El Person JSON-LD renderizado en producción-local incluye sameAs/knowsAbout/hasCredential con datos reales, no placeholders"
  artifacts:
    - path: "scripts/verify-phase12-author-eeat.mjs"
      provides: "Verificación automatizada headless (Playwright) del author page en 3 breakpoints, sin overflow horizontal, con screenshots"
      contains: "authors/juan-carlos-angulo"
  key_links:
    - from: "scripts/verify-phase12-author-eeat.mjs"
      to: "/es/authors/juan-carlos-angulo y /en/authors/juan-carlos-angulo"
      via: "fetch/goto contra un dev server local corriendo"
      pattern: "juan-carlos-angulo"
---

<objective>
Cierre de verificación de la Fase 12: correr un chequeo automatizado headless (mismo patrón que `scripts/verify-phase11-real-content-mobile.mjs`) contra el author page real con el contenido seedeado en 12-03, y un checkpoint humano para confirmar visualmente las 3 secciones nuevas (Expertise/Educación y Certificaciones/Experiencia) y el JSON-LD enriquecido.

Purpose: Cierra el ciclo goal-backward de la fase — confirma que AUTHOR-02 a AUTHOR-06 son observablemente ciertos en el sitio real, no solo en el código.
Output: `scripts/verify-phase12-author-eeat.mjs` commiteado, screenshots generados, aprobación humana registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-author-page-e-e-a-t-expansion/12-CONTEXT.md
@.planning/phases/12-author-page-e-e-a-t-expansion/12-UI-SPEC.md
@scripts/verify-phase11-real-content-mobile.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verificación automatizada headless del author page en 3 breakpoints</name>
  <files>scripts/verify-phase12-author-eeat.mjs</files>
  <action>
    Crea `scripts/verify-phase12-author-eeat.mjs` copiando el patrón exacto de `scripts/verify-phase11-real-content-mobile.mjs` (Playwright `chromium`, mismos 3 viewports 375/768/1280, `OVERFLOW_MARGIN_PX`, screenshots a `.mobile-verify-screenshots/`), con estas rutas:
    - `/es/authors/juan-carlos-angulo` (label "Author page ES — Expertise/Educación/Experiencia")
    - `/en/authors/juan-carlos-angulo` (label "Author page EN — Expertise/Education/Experience")

    Además del chequeo de overflow horizontal ya presente en el patrón base, agrega por ruta una verificación de contenido vía `page.locator` o `page.content()`:
    - Al menos 1 elemento con texto de badge de expertise visible (busca el texto del primer topic seedeado, ES: "SEO Técnico Avanzado", EN: "Advanced Technical SEO").
    - Al menos 1 tarjeta de educación visible (busca el texto de la institución, "Universidad Peruana de Ciencias Aplicadas").
    - Al menos 1 item de experiencia timeline visible (busca "AprendoSEO").
    - El `<script type="application/ld+json">` de la página contiene las substrings `"knowsAbout"`, `"hasCredential"` y `"sameAs"` (usar `page.locator('script[type="application/ld+json"]').allTextContents()` y verificar sobre el que contiene `"@type":"Person"`).

    Reporta cada fallo como en el script base (`failures`/`notes` por ruta+viewport), exit code 1 si algo falla.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && test -f scripts/verify-phase12-author-eeat.mjs && grep -q "juan-carlos-angulo" scripts/verify-phase12-author-eeat.mjs && grep -q "knowsAbout" scripts/verify-phase12-author-eeat.mjs && echo OK</automated>
  </verify>
  <done>scripts/verify-phase12-author-eeat.mjs existe y, corrido contra un dev server local (npm run dev en otra terminal), reporta RESULT: PASS para las 2 rutas en los 3 breakpoints, sin overflow horizontal y con las 3 secciones + JSON-LD confirmados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirmación visual humana de las 3 secciones E-E-A-T y el JSON-LD enriquecido</name>
  <files></files>
  <what-built>
    3 secciones nuevas en el author page (Expertise, Educación y Certificaciones, Experiencia) con contenido real de Juan, diseñadas per 12-UI-SPEC.md sobre los primitivos shadcn tokenizados, más el Person JSON-LD enriquecido con sameAs/knowsAbout/hasCredential. Verificación automatizada (Task 1) ya confirmó ausencia de overflow y presencia de contenido/JSON-LD en 375/768/1280px.
  </what-built>
  <action>
    Levanta `npm run dev` y presenta a Juan los pasos de `<how-to-verify>` abajo; espera confirmación explícita antes de cerrar la fase.
  </action>
  <how-to-verify>
    1. Corre `npm run dev` y visita `http://localhost:3000/es/authors/juan-carlos-angulo` y `http://localhost:3000/en/authors/juan-carlos-angulo`.
    2. Confirma visualmente las 3 secciones nuevas debajo del AuthorCard, en este orden: Expertise (tags) → Educación y Certificaciones (grid 2 columnas en desktop, 1 en mobile) → Experiencia (timeline vertical con línea y puntos conectores en color primary/ember).
    3. Redimensiona la ventana (o usa devtools) a 375px, 768px y 1280px — confirma que la grid de educación pasa de 1 a 2 columnas en el breakpoint md (768px), y que el timeline se ve como una sola columna con rail izquierdo en todos los anchos (sin layout alternado).
    4. Confirma que el punto (dot) del timeline queda visualmente centrado sobre la línea vertical, no descuadrado.
    5. Activa el modo oscuro del sitio (si existe toggle) y repite la revisión visual — confirma contraste legible de `muted-foreground` sobre `card` y visibilidad del rail (`border-border`) en dark.
    6. Abre las devtools, pestaña Elements/Network, busca el `<script type="application/ld+json">` con `"@type":"Person"` y confirma que contiene `sameAs` con las 3 URLs reales, `knowsAbout` con los 4 topics, y `hasCredential` con los 2 degrees.
    7. Confirma que ningún texto es placeholder genérico (Lorem ipsum, "TODO", etc.) — todo el contenido debe ser el real de Juan (SEO Técnico Avanzado, UPC, AprendoSEO, etc.).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si todo se ve correcto, o describe qué ajustar (por ejemplo desalineación del dot, contraste insuficiente en dark, texto faltante).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev server local -> navegador humano | Verificación puramente visual/manual, sin exposición de datos sensibles nuevos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-07 | N/A | scripts/verify-phase12-author-eeat.mjs | accept | Script de verificación read-only (Playwright goto + locators), no escribe estado ni credenciales, mismo perfil de riesgo que los scripts verify-* previos ya en el repo |

</threat_model>

<verification>
- `node scripts/verify-phase12-author-eeat.mjs` (con `npm run dev` corriendo en paralelo) reporta `RESULT: PASS`.
- Aprobación humana explícita registrada en el checkpoint (Task 2).
</verification>

<success_criteria>
Las 3 secciones E-E-A-T y el JSON-LD enriquecido están verificados automatizada y visualmente contra el sitio real con contenido real, en los 3 breakpoints y ambos temas. Cierra ROADMAP Phase 12 Success Criteria #2-6 (AUTHOR-02 a AUTHOR-06).
</success_criteria>

<output>
Create `.planning/phases/12-author-page-e-e-a-t-expansion/12-04-SUMMARY.md` when done
</output>
</content>
