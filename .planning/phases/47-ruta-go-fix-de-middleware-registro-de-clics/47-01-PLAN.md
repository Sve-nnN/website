---
phase: 47-ruta-go-fix-de-middleware-registro-de-clics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/app/robots.ts
  - src/app/go/[slug]/route.ts
  - src/collections/AffiliateClicks/index.ts
  - src/payload.config.ts
  - src/migrations/*_affiliate_clicks_collection.ts (generado por payload migrate:create)
  - src/payload-types.ts (regenerado)
  - src/lib/bot-detection.ts
  - src/lib/ip-throttle.ts
  - scripts/verify-go-route.ts
  - scripts/verify-affiliate-clicks-schema.ts
  - scripts/verify-affiliate-clicks-write.ts
autonomous: true
requirements: [GO-01, GO-02, GO-03, GO-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "curl -I a /go/{slug} de un link activo devuelve 302 con Location al destino admin-autorado y Cache-Control: no-store, en runtime Node; un ?to= en la query se ignora completamente (el handler nunca lee searchParams para resolver destino) y un slug inexistente vs. un slug inactivo devuelven exactamente el mismo 404 genérico, sin distinguir cuál fue"
    - "Con el matcher corregido (go/ con barra, ancla de segmento), las 5 rutas control (/, /en, /servicios, /en/services, /blog) responden 200 igual que antes del cambio, /golang-para-seo sigue devolviendo su 404 normal sin que la exclusión lo trague, y ningún request a /go/* dispara el fetch loopback a /api/redirects-lookup (el Location del 302 apunta directo al destino, nunca a /es/go/... ni /en/go/...)"
    - "/go pelado (sin slug) responde 404, per la decisión bloqueada de 47-CONTEXT.md — sin código nuevo para esto: cae de forma natural fuera de src/app/go/[slug] y next-intl lo reescribe hacia una página inexistente bajo [locale]/go"
    - "/robots.txt incluye Disallow: /go junto a los Disallow ya existentes de /admin y /api"
    - "Un clic real (User-Agent de navegador normal, dentro del throttle) deja exactamente una fila nueva en affiliate-clicks, escrita vía after() después de emitir el redirect; un User-Agent de bot y un request que excede el throttle por IP siguen recibiendo el 302 correcto pero no dejan fila"
  artifacts:
    - src/app/go/[slug]/route.ts
    - src/middleware.ts (matcher con go/ insertado)
    - src/app/robots.ts (Disallow /go)
    - src/collections/AffiliateClicks/index.ts
    - src/migrations/*_affiliate_clicks_collection.ts
    - src/lib/bot-detection.ts
    - src/lib/ip-throttle.ts
    - scripts/verify-go-route.ts
    - scripts/verify-affiliate-clicks-schema.ts
    - scripts/verify-affiliate-clicks-write.ts
  key_links:
    - "src/middleware.ts matcher excluye go/ -> /go/[slug] nunca ejecuta la función middleware() -> nunca pasa por createIntlMiddleware ni por el fetch loopback a /api/redirects-lookup"
    - "/go/[slug]/route.ts -> getCachedAffiliateLinks('es') + pickDestination() (ambos de Phase 46, sin modificar) -> único origen posible del destino; ?to= nunca se lee"
    - "/go/[slug]/route.ts -> after() -> payload.create({collection:'affiliate-clicks'}) -> escritura diferida que corre después de que el 302 ya salió, nunca antes"
    - "bot-detection.ts + ip-throttle.ts deciden únicamente si after() se ejecuta — nunca si el redirect se emite; un bot o un IP throttled igual llegan a destino"
---

<objective>
Cerrar la Phase 47 completa en un solo plan: el fix de una línea del matcher de `src/middleware.ts` que hoy traga `/go/*` (GO-02), el route handler `/go/[slug]` en runtime Node que resuelve el destino exclusivamente desde `affiliate-links` (GO-01), `Disallow: /go` en `robots.ts` (GO-03), y la colección `affiliate-clicks` append-only escrita vía `after()` con descarte de bots y throttle por IP (GO-04). Todo verificado por curl/fetch contra el dev server real conectado al Postgres real de Dokploy — no hay UI que emita un href `/go/` todavía (eso es Phase 48), así que esta fase se prueba sola.

Purpose: sin el fix del matcher, cada clic de afiliado real devuelve 404 hoy mismo — confirmado por dos investigadores independientes. El fix es una línea con radio de impacto sitewide (`/`, `/en`, `/servicios`, `/en/services`, `/blog` pasan por el mismo matcher), así que se aísla, se verifica contra rutas control, y se cierra con el registro de clics ya wireado para que Phase 48 solo tenga que renderizar el `href` y todo lo demás ya funcione.

Output:
- `src/middleware.ts` con `go/` insertado en la alternancia real (no la del ROADMAP)
- `src/app/go/[slug]/route.ts` (route handler nuevo, runtime Node)
- `src/app/robots.ts` con `Disallow: /go`
- `src/collections/AffiliateClicks/index.ts` (colección nueva, append-only por access control)
- Migración aditiva aplicada contra Dokploy, leída antes de correr
- `src/lib/bot-detection.ts` + `src/lib/ip-throttle.ts` (utils nuevos)
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-ruta-go-fix-de-middleware-registro-de-clics/47-CONTEXT.md
@.planning/phases/47-ruta-go-fix-de-middleware-registro-de-clics/47-RESEARCH.md
@CLAUDE.md
@src/middleware.ts
@src/lib/affiliate.ts
@src/lib/cache.ts
@src/app/api/newsletter/confirm/route.ts
@src/app/actions/contact.ts
@src/collections/Subscribers/index.ts
@src/migrations/20260820_155934_subscribers_collection.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fix del matcher + route handler /go/[slug] + robots.ts — un clic llega a destino</name>
  <files>
    src/middleware.ts,
    src/app/robots.ts,
    src/app/go/[slug]/route.ts,
    scripts/verify-go-route.ts
  </files>
  <read_first>
    - src/middleware.ts completo, en especial líneas 118-147 (comentario "SEO-39" + el `matcher` real) — este es el archivo que se edita, NO el ejemplo citado en el ROADMAP
    - src/i18n/navigation.ts (completo) — comentario de `isPrefixableHref`, mismo trap de prefijo-vs-segmento que resuelve `go/` con barra
    - src/app/api/newsletter/confirm/route.ts (completo) — forma exacta del route handler Node-runtime a replicar (`runtime = 'nodejs'`, `dynamic = 'force-dynamic'`, `NextResponse`)
    - src/lib/cache.ts líneas 320-346 (`getCachedAffiliateLinks` + `export type Locale`)
    - src/lib/affiliate.ts (completo — `pickDestination`, función pura, sin tocar)
    - src/app/robots.ts (completo — `disallow` actual)
    - 47-RESEARCH.md secciones "Pitfall 1", "Pitfall 3", "Pattern 1", "Pattern 2", "Pattern 3"
  </read_first>
  <action>
    1. En `src/middleware.ts`: localizar la alternancia real dentro de `export const config = { matcher: [...] }` (la lista explícita y finita que reemplazó la exclusión genérica anterior, documentada en el comentario "SEO-39" inmediatamente arriba). Insertar el token `go/` (con barra) justo después de `admin`, sin modificar ningún otro token de la lista ni el comentario existente, y sin tocar `api`/`admin` (comparten el mismo bug latente de prefijo pero quedan fuera de esta fase a propósito, per 47-CONTEXT.md). Agregar un comentario corto e independiente encima del `matcher` (no dentro del bloque "SEO-39" ya escrito) citando GO-02: `go/` ancla por segmento completo, no por prefijo — sin la barra, `/golang-para-seo` quedaría excluido igual que `/go/notion`; referenciar `isPrefixableHref` en `src/i18n/navigation.ts` como el mismo criterio ya aplicado ahí. No pegar ni reproducir la alternancia vieja que ese comentario "SEO-39" describe — solo se referencia por nombre, no se transcribe.

    2. En `src/app/robots.ts`: cambiar `disallow: ['/admin', '/api']` por `disallow: ['/admin', '/api', '/go']`. Sin ningún otro cambio al archivo.

    3. Crear `src/app/go/[slug]/route.ts` (Node runtime, fuera de `(frontend)/[locale]` y de `(payload)`, sibling de `src/app/api/`): `export const runtime = 'nodejs'`, `export const dynamic = 'force-dynamic'`. `GET(request: NextRequest, { params }: { params: Promise<{ slug: string }> })`: obtener `slug` con `await params`. Llamar `getCachedAffiliateLinks('es')` — locale fijo a propósito, con un comentario explicando que `slug`/`active`/`destinations`/`program` NO están localizados (matriz congelada de Phase 46), así que el locale del visitante no cambia qué documento resuelve. Buscar `doc = docs.find((d) => d.slug === slug)`. Si `!doc || !doc.active`, responder `new NextResponse('Not Found', { status: 404 })` — el MISMO camino de código exacto para "no existe" y para "existe pero inactivo" (GO-01, Pitfall 3): un comentario debe prohibir explícitamente una segunda consulta con `overrideAccess: true` "para saber cuál de los dos pasó". Si `doc.active`, resolver `destination = pickDestination(doc.destinations ?? [], 'default')` — `'default'` es un valor fijo de bajo riesgo (ver 47-RESEARCH.md Open Question 1 / Assumption A2): si ningún `destinations[].marketplace` coincide, `pickDestination` ya cae al primer elemento, así que cualquier valor fijo es seguro. Si `!destination`, mismo 404 genérico (link mal configurado en el admin, nunca un error 500). Si hay destino, responder `NextResponse.redirect(destination.url, { status: 302, headers: { 'Cache-Control': 'no-store' } })`. Comentario de cabecera del archivo (CONSTRAINT, GO-01): el handler no debe leer `request.nextUrl.searchParams` en ningún punto de la resolución del destino — cualquier query param que llegue se ignora por el simple hecho de no ser código alcanzable, no por un chequeo explícito. NO agregar todavía el registro de clics (`after()`/`affiliate-clicks`) — eso llega en Task 3, después de que la colección exista (Task 2); agregarlo ahora dejaría un import roto.

    4. Escribir `scripts/verify-go-route.ts` (ejecutado con el dev server real ya corriendo, contra el Postgres real de Dokploy): (a) vía `getPayload({config})`, crear un doc de prueba ACTIVO en `affiliate-links` (`slug: 'go-verify-active'`, `program: 'other'`, `active: true`, `destinations: [{ marketplace: 'default', url: 'https://example.com/go-verify-target?keep=1' }]`) y un doc de prueba INACTIVO (`slug: 'go-verify-inactive'`, `active: false`, misma forma) — crear AMBOS antes de la primera request HTTP a `/go/*` de este proceso del dev server, para que la primera lectura de `getCachedAffiliateLinks('es')` (todavía sin cachear en este arranque) los vea frescos y no haga falta esperar el TTL de 60s. (b) Con `fetch(..., { redirect: 'manual' })` contra `http://localhost:3000`: `/go/go-verify-active` → esperar `status === 302`, `Location === 'https://example.com/go-verify-target?keep=1'` verbatim, header `cache-control === 'no-store'`; `/go/go-verify-active?to=https://evil.example.com` → mismo `Location` exacto (prueba que el query param se ignora); `/go/go-verify-inactive` → `status === 404`; `/go/does-not-exist-slug` → `status === 404` con el MISMO body de texto que la respuesta anterior (prueba que inactivo e inexistente no se distinguen); `/go` (sin slug) → `status === 404`. (c) Matriz de control: `/`, `/en`, `/servicios`, `/en/services`, `/blog` → todas `200`; `/golang-para-seo` → `404` (decoy que empieza con "go", debe seguir devolviendo su 404 normal, nunca ser tragado por la exclusión). (d) `/robots.txt` → el body contiene la subcadena `Disallow: /go`. (e) Borrar ambos docs de prueba (`payload.delete`) y confirmar `payload.count({collection:'affiliate-links'}) === 0`. Imprimir `PASS` si todo se cumple, o `FAIL: <motivo>` y `process.exitCode = 1` si algo falla; terminar siempre con `process.exit()` explícito en cada rama (el pool de Postgres no cierra solo, mismo patrón que `scripts/db/04-which-database.ts`).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; grep -qE "admin\|go/\|_next" src/middleware.ts; grep -q "robots\\\\.txt" src/middleware.ts; grep -q "sitemap\\\\.xml" src/middleware.ts; test -f "src/app/go/[slug]/route.ts"; grep -q "runtime = 'nodejs'" "src/app/go/[slug]/route.ts"; grep -qF "'/go'" src/app/robots.ts; npm run dev > /tmp/dev-47-01.log 2>&1 & echo $! > /tmp/dev-47-01.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-go-route.ts | tee /tmp/verify-go-47-01.log; grep -q "^PASS$" /tmp/verify-go-47-01.log; kill "$(cat /tmp/dev-47-01.pid)" 2>/dev/null || true; npx tsc --noEmit; echo TRACER_OK</automated>
    <fails_when>El matcher no contiene `go/` insertado tras `admin`, o perdió alguna de las exclusiones existentes (`robots.txt`/`sitemap.xml`); el route handler no existe o no declara runtime Node; `robots.ts` no incluye `/go`; el script de verificación no imprime `PASS` (algún status/Location/header no coincide, el 404 de inactivo difiere del de inexistente, alguna ruta control cambió de comportamiento, `/golang-para-seo` fue tragado por la exclusión, o `affiliate-links` no queda en `COUNT(*) = 0` al final); o `tsc --noEmit` falla.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/middleware.ts` matcher contiene `go/` inmediatamente después de `admin`, con las demás exclusiones intactas
    - `src/app/go/[slug]/route.ts` existe, declara `runtime = 'nodejs'` y `dynamic = 'force-dynamic'`, y no lee `searchParams` en ningún punto de la resolución del destino
    - El 404 de un slug inexistente y el de un slug inactivo son indistinguibles (mismo status, mismo body)
    - `src/app/robots.ts` incluye `/go` en `disallow`
    - Las 5 rutas control responden 200 y el decoy `/golang-para-seo` responde 404, sin cambios de comportamiento respecto a antes del fix
  </acceptance_criteria>
  <precondition>DATABASE_URI en .env resuelve al Postgres real de Dokploy (confirmado por scripts/db/04-which-database.ts) — no existe entorno de staging/sandbox; el dev server necesita esa misma conexión para resolver /go/* contra datos reales.</precondition>
  <reversibility rating="costly">El matcher de `src/middleware.ts` tiene radio de impacto sitewide — un error en la alternancia puede devolver 404/500 en rutas núcleo (`/`, `/en`, `/servicios`) hasta el próximo deploy. Se mitiga verificando por curl las 5 rutas control más el decoy ANTES de commitear, no después.</reversibility>
  <done>/go/{slug} de un link activo redirige 302 con no-store al destino admin-autorado, ?to= se ignora, inactivo e inexistente devuelven el mismo 404, /go pelado es 404, las 5 rutas control y el decoy no cambiaron de comportamiento, y robots.txt bloquea /go.</done>
</task>

<task type="auto">
  <name>Task 2: Colección affiliate-clicks (append-only por access control) + migración aditiva contra Dokploy</name>
  <files>
    src/collections/AffiliateClicks/index.ts,
    src/payload.config.ts,
    src/migrations/*_affiliate_clicks_collection.ts,
    src/payload-types.ts,
    scripts/verify-affiliate-clicks-schema.ts
  </files>
  <read_first>
    - src/collections/Subscribers/index.ts (completo — forma estructural de una colección plana, sin `versions`, `admin.group: 'Site'`)
    - src/migrations/20260820_155934_subscribers_collection.ts (completo — forma de una migración aditiva SIN tabla hija, porque `Subscribers` no tiene ningún campo `array`, igual que `affiliate-clicks`)
    - src/payload.config.ts líneas 98-115 (punto de inserción en `collections:`, comentario existente sobre colecciones fuera de los plugins de contenido)
    - CLAUDE.md sección "Database Safety"
    - 47-RESEARCH.md sección "Pitfall 4" y "Open Questions #2"
  </read_first>
  <action>
    1. Confirmar contra qué base se va a escribir: `node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts` — debe resolver al Postgres real de Dokploy.

    2. Crear `src/collections/AffiliateClicks/index.ts` con slug `affiliate-clicks`, `labels: { singular: 'Affiliate Click', plural: 'Affiliate Clicks' }`. Access: `{ create: () => false, read: authenticated, update: () => false, delete: authenticated }` — `create`/`update` en `() => false` a propósito (GO-04: append-only estructural, nunca un `UPDATE ... clicks + 1`); el route handler escribe igual porque el Local API por defecto corre con `overrideAccess: true` cuando no se pasa `overrideAccess: false` explícito (mismo mecanismo verbatim que ya usa `src/app/actions/subscribe.tsx` contra `Subscribers`, que también tiene `create: authenticated`). `admin: { group: 'Site', useAsTitle: 'slug', defaultColumns: ['slug', 'userAgent', 'createdAt'] }`. Fields, EN ESTE ORDEN: `slug` (text, required, `index: true` — a qué `affiliate-links.slug` corresponde el clic, guardado como string plano y no como `relationship`, para que el log sobreviva si el doc de `affiliate-links` se borra después); `userAgent` (text, opcional — sin IP cruda, per LEG-04/RESEARCH Open Question 2: la IP solo vive en memoria del proceso para el throttle de Task 3, nunca se persiste en esta tabla). NO agregar un campo `clickedAt`: el `createdAt` automático de Payload ya es el timestamp del clic, agregar uno redundante duplicaría el dato. NO agregar `versions` ni ningún campo de conteo mutable. Comentario de cabecera del archivo explicando el diseño append-only (acceso `() => false` en `update`, no solo convención).

    3. Registrar `AffiliateClicks` en `src/payload.config.ts`: importar junto a `AffiliateLinks` y agregarlo al array `collections:` inmediatamente después de `AffiliateLinks`. NO agregarlo a `seoPlugin`/`redirectsPlugin`/`searchPlugin`/`mcpPlugin` — no es contenido público ni debe exponerse por MCP (REQUIREMENTS.md "Out of Scope" lo nombra explícitamente junto a `subscribers`/`lead-magnets`).

    4. Generar la migración: `node --env-file=.env node_modules/.bin/payload migrate:create affiliate_clicks_collection`. LEER el archivo generado bajo `src/migrations/` completo antes de continuar — al no tener `affiliate-clicks` ningún campo `array`, debe generar SOLO `CREATE TABLE "affiliate_clicks"` (sin tabla hija, a diferencia de `affiliate_links_collection` que sí tuvo una por su array `destinations`), índices, y `ALTER TABLE payload_locked_documents_rels ADD COLUMN`/`ADD CONSTRAINT` — exactamente la forma de `20260820_155934_subscribers_collection.ts`. Si aparece cualquier `ALTER COLUMN`, `DROP TABLE` o `DROP COLUMN` fuera del bloque `down()`, PARAR y escalar a Juan por nombre antes de aplicar.

    5. Aplicar: `node --env-file=.env node_modules/.bin/payload migrate`.

    6. Regenerar tipos: `npm run generate:types` (agrega el tipo `AffiliateClick` a `src/payload-types.ts`, consumido por Task 3).

    7. Escribir `scripts/verify-affiliate-clicks-schema.ts`: (a) `payload.count({collection:'affiliate-clicks'})` debe ser `0`; (b) `payload.create()` un doc de prueba (`slug: 'schema-verify-test'`, `userAgent: 'verify-script'`); (c) `payload.findByID()` confirma que persistió con ambos campos; (d) intentar `payload.update()` ese mismo doc pasando `overrideAccess: false` explícito y confirmar que la llamada es RECHAZADA (lanza o devuelve error de acceso) — prueba que el append-only es estructural, no solo convención; (e) `payload.delete()` el doc de prueba; (f) `payload.count()` vuelve a ser `0`. Imprimir `PASS`/`FAIL: <motivo>` y `process.exit()` explícito en cada rama, igual que en Task 1.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; MIGFILE=$(ls src/migrations/*affiliate_clicks_collection*.ts 2>/dev/null | head -1); test -n "$MIGFILE"; ! grep -qiE "ALTER COLUMN|DROP TABLE|DROP COLUMN" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); grep -q "AffiliateClick" src/payload-types.ts; node --env-file=.env node_modules/.bin/tsx scripts/verify-affiliate-clicks-schema.ts | tee /tmp/verify-clicks-schema-47-02.log; grep -q "^PASS$" /tmp/verify-clicks-schema-47-02.log; npx tsc --noEmit; echo SCHEMA_OK</automated>
    <fails_when>El archivo de migración no existe o contiene ALTER COLUMN/DROP TABLE/DROP COLUMN fuera de `down()`; `payload-types.ts` no tiene el tipo `AffiliateClick`; el script no imprime `PASS` (count distinto de 0 al inicio/final, el update no fue rechazado, o create/read/delete falla); o `tsc --noEmit` falla.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/collections/AffiliateClicks/index.ts` existe con `create`/`update` en `() => false` y exactamente los campos `slug`/`userAgent` (más los automáticos de Payload)
    - `AffiliateClicks` está registrado en `payload.config.ts` `collections:` únicamente (no en ningún plugin)
    - La migración generada es puramente aditiva (leída completa, confirmado por el grep del verify) y no genera tabla hija (sin campos `array`)
    - `payload.count({collection:'affiliate-clicks'})` es 0 tanto antes como después del round-trip de prueba
    - Un `payload.update()` con `overrideAccess: false` sobre `affiliate-clicks` es rechazado
    - `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <precondition>DATABASE_URI en .env resuelve al Postgres real de Dokploy (confirmado por scripts/db/04-which-database.ts) — no existe entorno de staging/sandbox.</precondition>
  <reversibility rating="reversible">A diferencia de `affiliate-links`, `affiliate-clicks` es una tabla plana sin campos localizados ni contenido editorial — es un log append-only. Un campo mal diseñado se corrige con una migración aditiva de seguimiento (agregar columna), nunca exige un backfill con pérdida de datos.</reversibility>
  <done>La colección affiliate-clicks existe y está migrada contra el Postgres real de Dokploy, append-only por access control (update rechazado), y el round-trip de prueba deja la tabla en COUNT(*) = 0.</done>
</task>

<task type="auto">
  <name>Task 3: Descarte de bots + throttle por IP wireados en /go/[slug] vía after()</name>
  <files>
    src/lib/bot-detection.ts,
    src/lib/ip-throttle.ts,
    src/app/go/[slug]/route.ts,
    scripts/verify-affiliate-clicks-write.ts
  </files>
  <read_first>
    - src/app/actions/contact.ts completo, en especial líneas 27-48 (`isRateLimited`, Map a nivel de módulo, ventana de tiempo) y 33-34 (extracción de IP vía `x-forwarded-for`/`x-real-ip`)
    - src/app/go/[slug]/route.ts (tal como quedó de Task 1)
    - 47-RESEARCH.md secciones "Pitfall 2" (por qué el honeypot de `contact.ts` NO aplica acá), "Pattern 4" (`after()`) y "Pattern 5" (throttle)
  </read_first>
  <action>
    1. Crear `src/lib/bot-detection.ts` exportando `isBotUserAgent(userAgent: string | null): boolean`. Un User-Agent ausente o vacío cuenta como bot (un navegador real siempre manda uno). Contra un User-Agent presente, usar un regex case-insensitive con patrones estándar de la industria: `bot`, `crawl`, `spider`, `slurp`, `curl`, `wget`, `python-requests`, `python-urllib`, `scrapy`, `headless`, `phantomjs`, `facebookexternalhit`, `whatsapp`, `telegrambot`, `discordbot`, `slackbot`, `libwww-perl`, `node-fetch`, `axios`, `http-client`. Comentario de cabecera marcando esto como `[ASSUMED]` (47-RESEARCH.md Assumption A1) — ningún patrón de este repo lo respalda, es conocimiento general de industria; si algún crawler legítimo necesitara pasar por `/go/`, ajustar la lista es una decisión de Juan, no algo que resolver acá (además, GO-03 ya lo desalienta vía `Disallow: /go`).

    2. Crear `src/lib/ip-throttle.ts` exportando `createIpThrottle(windowMs: number, maxHits: number)` que devuelve `{ isThrottled(ip: string): boolean }`, y `extractClientIp(headers: Headers): string`. Misma FORMA exacta que `contact.ts` líneas 27-48/33-34 (`Map<string, number[]>` a nivel de módulo, filtro por ventana de tiempo, `x-forwarded-for` con fallback a `x-real-ip` y luego `'unknown'`) — extraída a util reusable per discreción de 47-CONTEXT.md, sin modificar `contact.ts`.

    3. Modificar `src/app/go/[slug]/route.ts`: importar `after` desde `'next/server'`, `getPayload` desde `'payload'`, `config` desde `'@payload-config'`, `isBotUserAgent` desde `'@/lib/bot-detection'`, `createIpThrottle`/`extractClientIp` desde `'@/lib/ip-throttle'`. Agregar constantes a nivel de módulo `CLICK_THROTTLE_WINDOW_MS = Number(process.env.GO_CLICK_THROTTLE_WINDOW_MS) || 10 * 60 * 1000` y `CLICK_THROTTLE_MAX_HITS = Number(process.env.GO_CLICK_THROTTLE_MAX_HITS) || 20` (los env vars permiten que el verify script use una ventana corta y determinística; en producción caen a los defaults), e instanciar `const clickThrottle = createIpThrottle(CLICK_THROTTLE_WINDOW_MS, CLICK_THROTTLE_MAX_HITS)` una sola vez a nivel de módulo. Justo antes del `return` de la respuesta 302 (la respuesta ya construida, nunca antes): leer `userAgent = request.headers.get('user-agent')` y `clientIp = extractClientIp(request.headers)`; calcular `shouldLog = !isBotUserAgent(userAgent) && !clickThrottle.isThrottled(clientIp)`; si `shouldLog`, llamar `after(async () => { const payload = await getPayload({ config }); await payload.create({ collection: 'affiliate-clicks', data: { slug, userAgent: userAgent ?? undefined } }) })`. El `return` de la respuesta 302 ocurre SIEMPRE, sin importar `shouldLog` — un bot o un IP throttled igual llegan a destino, solo se suprime la escritura (GO-04, Pattern 4 de RESEARCH.md). Comentario explícito de esta regla en el archivo.

    4. Escribir `scripts/verify-affiliate-clicks-write.ts` (corre con el dev server iniciado con `GO_CLICK_THROTTLE_WINDOW_MS=60000 GO_CLICK_THROTTLE_MAX_HITS=2` para que el throttle sea determinístico y rápido de probar): (a) crear un doc de prueba activo en `affiliate-links` (`slug: 'go-verify-clicks-writer'`, 1 destino) antes de la primera request; (b) confirmar `payload.count({collection:'affiliate-clicks', where:{slug:{equals:'go-verify-clicks-writer'}}}) === 0`; (c) `fetch('/go/go-verify-clicks-writer', { headers: { 'user-agent': 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) Verify/1.0' } })` DOS veces seguidas, esperando ~800ms después de cada una (tiempo para que `after()` escriba) y confirmando que el conteo llega a `2` (máximo permitido por `GO_CLICK_THROTTLE_MAX_HITS=2`); en ambas, confirmar también que la respuesta HTTP sigue siendo `302` con el `Location` correcto; (d) una TERCERA vez con el mismo User-Agent normal → confirmar `302` correcto pero el conteo se queda en `2` (throttled, sin fila nueva); (e) una CUARTA vez con `'user-agent': 'Mozilla/5.0 (compatible; SemrushBot/7~bl; +http://www.semrush.com/bot.html)'` → confirmar `302` correcto pero el conteo se queda en `2` (bot descartado, sin fila nueva); (f) borrar las filas de `affiliate-clicks` con ese `slug` y el doc de prueba de `affiliate-links`, confirmar que ambos conteos vuelven a su estado previo. Imprimir `PASS`/`FAIL: <motivo>` y `process.exit()` explícito, igual que las tasks anteriores.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; grep -q "isBotUserAgent" "src/app/go/[slug]/route.ts"; grep -q "after(" "src/app/go/[slug]/route.ts"; GO_CLICK_THROTTLE_WINDOW_MS=60000 GO_CLICK_THROTTLE_MAX_HITS=2 npm run dev > /tmp/dev-47-03.log 2>&1 & echo $! > /tmp/dev-47-03.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-affiliate-clicks-write.ts | tee /tmp/verify-clicks-write-47-03.log; grep -q "^PASS$" /tmp/verify-clicks-write-47-03.log; kill "$(cat /tmp/dev-47-03.pid)" 2>/dev/null || true; npx tsc --noEmit; echo WRITE_OK</automated>
    <fails_when>`route.ts` no referencia `isBotUserAgent`/`after(`; el script no imprime `PASS` (el conteo no llega a 2, sigue subiendo pasado el throttle, un bot deja fila, o cualquiera de los 4 requests no devuelve 302 con el Location correcto); la limpieza final no restaura los conteos previos; o `tsc --noEmit` falla.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/bot-detection.ts` exporta `isBotUserAgent`, sin ningún import de `payload`/`@payload-config`
    - `src/lib/ip-throttle.ts` exporta `createIpThrottle`/`extractClientIp` con la misma forma (Map + ventana) que `contact.ts`, sin modificar ese archivo
    - `route.ts` solo escribe a `affiliate-clicks` cuando `!isBotUserAgent(...) && !clickThrottle.isThrottled(...)`, siempre después de construir la respuesta 302, vía `after()`
    - Un User-Agent de bot y un request throttled reciben igual el 302 correcto, solo se suprime la escritura
  </acceptance_criteria>
  <precondition>Misma base real de Dokploy que Task 1/2; el dev server de este task debe arrancar con GO_CLICK_THROTTLE_MAX_HITS=2 para que el throttle sea verificable en segundos en vez de minutos (el default de producción real es 20, definido en la constante del route handler).</precondition>
  <reversibility rating="reversible">Bot-detection y throttle son heurísticas de negocio ajustables sin migración ni cambio de contrato público — cambiar la lista de patrones o los umbrales no rompe ningún call site.</reversibility>
  <done>Un clic real deja exactamente una fila en affiliate-clicks vía after(); un User-Agent de bot y un segundo request dentro del throttle por IP no dejan fila, pero ambos siguen recibiendo el 302 correcto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitante anónimo -> /go/[slug] | Ruta pública sin auth; el `slug` de la URL es la única entrada de usuario que participa en la resolución |
| Query string (`?to=`, cualquier otro param) -> resolución de destino | Debe ignorarse por completo — superficie clásica de open redirect (CWE-601) |
| Edge middleware (matcher) -> next-intl + fetch loopback a redirects-lookup | Radio de impacto sitewide — el mismo matcher gatea `/`, `/en`, `/servicios`, `/blog` |
| Route handler -> Postgres real de Dokploy | Tanto la lectura de `affiliate-links` como la escritura a `affiliate-clicks` van contra la única base que existe, sin sandbox |
| Visitante anónimo -> escritura en affiliate-clicks | Un actor externo no debe poder inflar el conteo de clics trivialmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering | `/go/[slug]/route.ts` — query param `?to=` | high | mitigate | GO-01: el destino se resuelve exclusivamente vía `getCachedAffiliateLinks()`/`pickDestination()`; el handler nunca lee `searchParams` en la ruta de resolución del destino (Task 1) |
| T-47-02 | Information Disclosure | Respuesta 404 de slug inexistente vs. inactivo | medium | mitigate | Un único camino de código para "no encontrado" (mismo status y body), verificado por comparación directa en `scripts/verify-go-route.ts` (Task 1) |
| T-47-03 | Tampering | `src/middleware.ts` matcher (radio de impacto sitewide) | high | mitigate | `go/` insertado con barra (ancla de segmento, no de prefijo) dentro de la alternancia real existente, nunca la del ROADMAP; curl-verificado contra 5 rutas control + 1 decoy antes de commitear (Task 1) |
| T-47-04 | Repudiation / abuso de lógica de negocio | `affiliate-clicks` (conteo de clics) | medium | mitigate | Descarte de bots por User-Agent + throttle por IP evaluados antes de decidir si se escribe, verificado con umbral bajo en `scripts/verify-affiliate-clicks-write.ts` (Task 3) |
| T-47-05 | Tampering | Migración de `affiliate-clicks` contra Postgres real (sin sandbox) | high | mitigate | Migración leída completa antes de aplicar, confirmado aditiva-only (sin tabla hija, sin `ALTER COLUMN`/`DROP`) por grep automatizado (Task 2) |
| T-47-06 | Tampering | Caché de un 302 stale en el borde/navegador | medium | mitigate | `Cache-Control: no-store` explícito en toda respuesta de `/go/[slug]/route.ts` (Task 1) |
| T-47-SC | Tampering | Instalaciones npm/pip/cargo | n/a | accept | Esta fase no instala ningún paquete nuevo (47-RESEARCH.md "Package Legitimacy Audit": no aplica) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `curl`/`fetch` a `/go/{slug}` de un link activo devuelve 302 + `Location` exacto + `Cache-Control: no-store`; `?to=` no cambia el destino
- Slug inactivo y slug inexistente devuelven el mismo 404 (status y body)
- Las 5 rutas control (`/`, `/en`, `/servicios`, `/en/services`, `/blog`) y el decoy `/golang-para-seo` no cambian de comportamiento tras el fix del matcher
- `/robots.txt` contiene `Disallow: /go`
- La migración de `affiliate-clicks` es puramente aditiva, leída antes de aplicarse; `payload.count()` es 0 antes/después del round-trip de Task 2
- Un `update()` sobre `affiliate-clicks` es rechazado (append-only estructural)
- Un clic normal deja exactamente una fila vía `after()`; un bot o un IP throttled no dejan fila pero sí reciben el 302
- `npx tsc --noEmit` pasa en todo el plan
</verification>

<success_criteria>
- GO-01: `/go/[slug]` responde 302 en runtime Node con `no-store`, leyendo el destino exclusivamente del documento admin-autorado, `?to=` ignorado, sin fuga de información entre slug inexistente e inactivo
- GO-02: el matcher excluye `go/` con barra (ancla de segmento), verificado por curl contra 5 rutas control + 1 decoy, sin tocar `api`/`admin`
- GO-03: `robots.ts` incluye `Disallow: /go`
- GO-04: los clics se registran en `affiliate-clicks` (append-only, estructural vía access control) escritos por `after()`, con descarte de bots y throttle por IP verificado con umbral determinístico
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 47-01)

| Artefacto | Rol |
|-----------|-----|
| `src/middleware.ts` | Matcher corregido — `go/` insertado con barra tras `admin`, resto de exclusiones intacto |
| `src/app/go/[slug]/route.ts` | Route handler nuevo, runtime Node, resuelve destino exclusivamente vía Phase 46, registra clics vía `after()` |
| `src/app/robots.ts` | `Disallow: /go` agregado junto a `/admin`/`/api` |
| `src/collections/AffiliateClicks/index.ts` | Colección nueva — append-only por access control (`create`/`update` en `() => false`) |
| `src/migrations/*_affiliate_clicks_collection.ts` | Migración aditiva aplicada contra Dokploy |
| `src/lib/bot-detection.ts` | `isBotUserAgent()` — heurística nueva por User-Agent, `[ASSUMED]` |
| `src/lib/ip-throttle.ts` | `createIpThrottle()`/`extractClientIp()` — misma forma que `contact.ts`, extraída a util reusable |
| `scripts/verify-go-route.ts` | Round-trip de `affiliate-links` + curl matrix (control + decoy + slug activo/inactivo/inexistente) |
| `scripts/verify-affiliate-clicks-schema.ts` | Round-trip de schema de `affiliate-clicks` + prueba de rechazo de `update()` |
| `scripts/verify-affiliate-clicks-write.ts` | Prueba de escritura vía `after()` con throttle/bot-detection bajo umbral determinístico |
</artifacts>

<output>
Crear `.planning/phases/47-ruta-go-fix-de-middleware-registro-de-clics/47-01-SUMMARY.md` al terminar.
</output>
