---
phase: 42-meta-tags-completion
plan: 03
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
  - "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
  - "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"
  - "src/app/(frontend)/[locale]/websites/[slug]/page.tsx"
  - "src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx"
  - "src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx"
autonomous: true
requirements: [META-01]

must_haves:
  truths:
    - "Every doc-backed detail route (blog post, case study, author, website) emits a locale-aware, self-referencing alternates.canonical whose path exactly mirrors that same page's already-live og:url"
    - "Both geo-pages (Lima, Madrid) emit alternates.canonical using the same URL segment across locales (only the /en prefix differs), matching the Phase 20 URL decision already baked into their routes"
    - "All 19 public route types now emit alternates.canonical — confirmed via live curl across a representative sample, not just static code reading"
    - "Re-running an opengraph.to-equivalent meta-tag inspection shows 0 remaining warnings across favicon/apple-touch-icon/theme-color/manifest/canonical (Plan 42-01 + 42-02 + 42-03 combined)"
  artifacts: []
  key_links:
    - "Detail-route canonical esPath/enPath must be built from the exact same route `slug` param expression already used by that file's existing buildOpenGraph({ url }) call — canonical and og:url can never diverge for the same page"
    - "Plan 42-01's favicon/manifest/theme-color tags on Home must still be present after this plan's edits (regression check, zero shared files but same overall <head> output)"
---

<objective>
Complete META-01 sitewide by wiring `alternates.canonical` into the last 6 route files — the 4 doc-backed detail templates (blog post, case study, author, website) and the 2 geo-landing pages — using the `buildAlternates` helper Plan 42-02 added to `src/lib/canonical.ts`. Close the phase with one comprehensive live-curl sweep across all 19 public route types (plus a regression check of Plan 42-01's favicon/manifest work), giving Phase 42 real evidence equivalent to re-running the opengraph.to audit.

Purpose: These are the highest-traffic content pages (every blog post, case study, author, and website portfolio entry) — canonical here also has the most SEO leverage. Reusing each file's already-live `og:url` computation (from Phase 41) guarantees canonical and og:url can never drift apart for the same page.
Output: 6 route files gain `alternates.canonical`; a full-sweep curl verification closes out ROADMAP Phase 42 Success Criteria #5 and #6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-meta-tags-completion/42-CONTEXT.md
@.planning/phases/42-meta-tags-completion/42-02-SUMMARY.md

@src/lib/canonical.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire the 4 doc-backed detail routes (Blog post, Case Study, Author, Website)</name>
  <files>src/app/(frontend)/[locale]/blog/[slug]/page.tsx, src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx, src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/(frontend)/[locale]/websites/[slug]/page.tsx</files>
  <read_first>
    src/lib/canonical.ts (buildAlternates, added by Plan 42-02 — do not re-add or redefine it)
    src/app/(frontend)/[locale]/blog/[slug]/page.tsx (generateMetadata)
    src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx (generateMetadata)
    src/app/(frontend)/[locale]/authors/[slug]/page.tsx (generateMetadata)
    src/app/(frontend)/[locale]/websites/[slug]/page.tsx (generateMetadata)
  </read_first>
  <action>
    All 4 files share the same shape: `generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> })` returns `{}` early when the doc isn't found, otherwise returns an object with `openGraph: buildOpenGraph({ ..., url: locale === 'en' ? \`/en/{route}/${'{'}slug{'}'}\` : \`/{route}/${'{'}slug{'}'}\`, ... })`. Add `import { buildAlternates } from '@/lib/canonical'` and add `alternates: buildAlternates(locale as 'es' | 'en', esPath, enPath)` as a sibling key to `openGraph`, computing `esPath`/`enPath` from the identical `slug` route param each file's existing `url` line already uses (not `doc.slug ?? slug` — match the `url:` computation exactly, which uses the raw param):
    - `blog/[slug]/page.tsx`: `esPath` is `` `/blog/${slug}` ``, `enPath` is `` `/en/blog/${slug}` `` — mirrors the existing `url: locale === 'en' ? \`/en/blog/${slug}\` : \`/blog/${slug}\`` line.
    - `case-studies/[slug]/page.tsx`: `esPath` `` `/case-studies/${slug}` ``, `enPath` `` `/en/case-studies/${slug}` `` — mirrors its existing `url` line.
    - `authors/[slug]/page.tsx`: `esPath` `` `/authors/${slug}` ``, `enPath` `` `/en/authors/${slug}` `` — mirrors its existing `url` line (note: the `slug:` param passed to `buildOpenGraph` uses `doc.slug ?? slug`, but the `url:`/canonical path uses the raw route `slug` — keep that same distinction, do not conflate the two).
    - `websites/[slug]/page.tsx`: `esPath` `` `/websites/${slug}` ``, `enPath` `` `/en/websites/${slug}` `` — mirrors its existing `url` line.
  </action>
  <verify>
    <automated>
npx tsc --noEmit -p tsconfig.json
for f in "src/app/(frontend)/[locale]/blog/[slug]/page.tsx" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" "src/app/(frontend)/[locale]/authors/[slug]/page.tsx" "src/app/(frontend)/[locale]/websites/[slug]/page.tsx"; do
  echo "-- $f --"
  grep -c "buildAlternates(" "$f"
done
    </automated>
  </verify>
  <acceptance_criteria>
    - each of the 4 files contains exactly one `buildAlternates(` call using the raw `slug` route param (same expression as its existing `url:` line, not `doc.slug ?? slug`)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Blog post, Case Study, Author, and Website detail templates each return a correct, slug-matched `alternates.canonical` that mirrors their existing `og:url`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the 2 geo-pages (Lima, Madrid) + sitewide zero-gap confirmation</name>
  <files>src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx, src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx</files>
  <read_first>
    src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx (generateMetadata)
    src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx (generateMetadata)
  </read_first>
  <action>
    Same mechanical pattern. Both files already compute `const url = locale === 'en' ? '/en/seo-tecnico-{city}' : '/seo-tecnico-{city}'` — same URL segment in both locales (only the `/en` prefix differs), per the Phase 20 decision already baked into these routes. Add `import { buildAlternates } from '@/lib/canonical'` and `alternates: buildAlternates(locale as 'es' | 'en', esPath, enPath)`:
    - `seo-tecnico-lima/page.tsx`: `esPath` `'/seo-tecnico-lima'`, `enPath` `'/en/seo-tecnico-lima'`.
    - `seo-tecnico-madrid/page.tsx`: `esPath` `'/seo-tecnico-madrid'`, `enPath` `'/en/seo-tecnico-madrid'`.

    After both files are edited, run the sitewide zero-gap confirmation: `grep -rl "generateMetadata" "src/app/(frontend)"` must list all 19 public route files, and cross-referencing against `grep -rl "alternates" "src/app/(frontend)"` plus the 4 Servicios/Services routes' indirect coverage via `src/lib/services-data.ts` (confirmed in Plan 42-02) must together account for all 19 — zero routes silently missed, same discipline Phase 41's 41-03 plan used for its own sitewide `buildOpenGraph` confirmation.
  </action>
  <verify>
    <automated>
npx tsc --noEmit -p tsconfig.json
grep -c "buildAlternates(" "src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx"
grep -c "buildAlternates(" "src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx"
echo "--- sitewide gap check ---"
comm -23 <(grep -rl "generateMetadata" "src/app/(frontend)" | sort) <(grep -rl "alternates" "src/app/(frontend)" | sort)
    </automated>
  </verify>
  <acceptance_criteria>
    - both geo-page files contain exactly one `buildAlternates(` call with the exact es/en literals above
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - the `comm -23` gap-check output lists only the 4 Servicios/Services route files (`services/page.tsx`, `servicios/page.tsx`, `services/[slug]/page.tsx`, `servicios/[slug]/page.tsx`) — these are the expected, already-covered-indirectly exceptions (their canonical comes from `services-data.ts`, not a literal `alternates` string in the route file itself); zero unexpected entries
  </acceptance_criteria>
  <done>Both geo-pages return a correct `alternates.canonical`; a sitewide grep confirms every one of the 19 public route types now has canonical coverage (15 direct + 4 indirect via services-data.ts).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sitewide live verification sweep (canonical + favicon/manifest regression check)</name>
  <files></files>
  <read_first>
    .planning/WINDOWS.md (confirms the session-wide Neon Postgres connectivity blocker from Phase 41 is now resolved — ids 1-3 marked fixed 2026-08-01 — so this task's live curl checks should now succeed where Phase 41's could not)
  </read_first>
  <action>
    Run one comprehensive live-curl session against a real `npm run dev` server covering a representative sample of every route type this phase touched, plus a regression check of Plan 42-01's favicon/manifest work (zero shared files with this plan, but same rendered `<head>`, so worth re-confirming nothing broke). Fetch one real post slug and one real case-study slug from `/sitemap.xml` rather than guessing a slug. No code changes in this task — verification only.
  </action>
  <verify>
    <automated>
npm run dev &
DEV_PID=$!
sleep 6
echo "--- Home (regression: canonical absent by design -- Home has no canonical per ROADMAP scope beyond OG; confirm favicon/manifest/theme-color from Plan 42-01 still present) ---"
curl -s http://localhost:3000/ | grep -o '<link rel="manifest"[^>]*>'
curl -s http://localhost:3000/ | grep -o '<meta name="theme-color"[^>]*>'
curl -s http://localhost:3000/ | grep -o '<link rel="canonical" href="[^"]*"'
echo "--- static Pages-collection route ---"
curl -s http://localhost:3000/contact | grep -o '<link rel="canonical" href="[^"]*"'
curl -s http://localhost:3000/en/contact | grep -o '<link rel="canonical" href="[^"]*"'
echo "--- geo pages ---"
curl -s http://localhost:3000/seo-tecnico-lima | grep -o '<link rel="canonical" href="[^"]*"'
curl -s http://localhost:3000/en/seo-tecnico-madrid | grep -o '<link rel="canonical" href="[^"]*"'
echo "--- doc-backed detail routes (real slugs from sitemap.xml) ---"
POST_URL=$(curl -s http://localhost:3000/sitemap.xml | grep -oE '<loc>[^<]*/blog/[^<]+</loc>' | head -1 | sed -E 's#<loc>(.*)</loc>#\1#')
echo "post: $POST_URL"
curl -s "$POST_URL" | grep -o '<link rel="canonical" href="[^"]*"'
CASE_URL=$(curl -s http://localhost:3000/sitemap.xml | grep -oE '<loc>[^<]*/case-studies/[^<]+</loc>' | head -1 | sed -E 's#<loc>(.*)</loc>#\1#')
echo "case study: $CASE_URL"
curl -s "$CASE_URL" | grep -o '<link rel="canonical" href="[^"]*"'
echo "--- Servicios regression (unchanged, must still resolve via buildServiceAlternates) ---"
curl -s http://localhost:3000/servicios | grep -o '<link rel="canonical" href="[^"]*"'
curl -s http://localhost:3000/en/services | grep -o '<link rel="canonical" href="[^"]*"'
kill $DEV_PID 2>/dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - Home still serves the Plan 42-01 `<link rel="manifest">` and `<meta name="theme-color">` tags (no regression)
    - `/contact` and `/en/contact` each render a `<link rel="canonical">` with the correct absolute, locale-correct URL
    - `/seo-tecnico-lima` and `/en/seo-tecnico-madrid` each render a correct canonical (same URL segment, only `/en` prefix differs)
    - the real post URL and real case-study URL fetched from `/sitemap.xml` each render a `<link rel="canonical">` whose path matches `/blog/{slug}` or `/case-studies/{slug}` exactly (or the `/en/...` equivalent)
    - `/servicios` and `/en/services` still render their pre-existing, unmodified `buildServiceAlternates` canonical correctly (zero regression from this phase's work)
  </acceptance_criteria>
  <done>Live curl evidence — not just static code reading — confirms canonical is correct across every public route type, and Plan 42-01's favicon/manifest/theme-color work has zero regression. Equivalent to a manual re-run of the opengraph.to audit's canonical + favicon/manifest/theme-color checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Route `slug` param (from the URL path, already resolved against a real Payload doc before use) → `buildAlternates` canonical output | Same boundary as Plan 42-02, extended to dynamic detail routes where `slug` is attacker-controllable in the URL but only ever used as a literal path-segment string, never interpolated into a query or reflected unescaped into non-URL contexts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Tampering | Detail-route canonical vs. og:url drift (`blog/[slug]`, `case-studies/[slug]`, `authors/[slug]`, `websites/[slug]`) | medium | mitigate | Task 1 requires `esPath`/`enPath` to be built from the exact same `slug` route-param expression each file's pre-existing, already-live `buildOpenGraph({ url: ... })` call already uses (confirmed by reading each file before editing) — canonical and `og:image`/`og:url` can never point at two different URLs for the same rendered page. |
</threat_model>

<verification>
`npx tsc --noEmit -p tsconfig.json` passes after Tasks 1-2. Task 3's live curl sweep (real dev server, real Postgres — WINDOWS.md ids 1-3 confirm the prior session's connectivity blocker is resolved) confirms canonical renders correctly across Home/static-page/geo-page/detail-page/Servicios route types, with zero regression to Plan 42-01's favicon/manifest work.
</verification>

<success_criteria>
All 19 public route types (15 direct via `buildAlternates`, 4 indirect via the pre-existing `buildServiceAlternates`) emit `alternates.canonical`, live-curl-verified. Combined with Plan 42-01, ROADMAP Phase 42's 6 Success Criteria are all closed with real evidence, not just static code reading.
</success_criteria>

<output>
Create `.planning/phases/42-meta-tags-completion/42-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `alternates.canonical`/`alternates.languages` wired into the 4 doc-backed detail templates (blog post, case study, author, website) and the 2 geo-pages (Lima, Madrid) — closing the last 6 of 15 direct-coverage routes
- Live curl evidence (Task 3) covering all public route types, serving as the phase's final "audit re-run" proof for ROADMAP Phase 42 Success Criterion #6
