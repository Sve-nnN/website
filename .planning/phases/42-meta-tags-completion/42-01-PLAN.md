---
phase: 42-meta-tags-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/favicon.ico
  - public/favicon.svg
  - public/favicon-32x32.png
  - public/apple-touch-icon.png
  - public/icon-192.png
  - public/icon-512.png
  - public/site.webmanifest
  - scripts/generate-favicon-pngs.ts
  - "src/app/(frontend)/[locale]/layout.tsx"
autonomous: true
requirements: [META-02, META-03, META-04, META-05]

must_haves:
  truths:
    - "The real Juan Carlos Angulo isotype ('J' white mark on a black rounded-square background) renders as the browser-tab favicon on Home in both locales — not the default Next.js icon"
    - "iOS 'Add to Home Screen' on any page uses a real, crisp 180x180 apple-touch-icon — not a missing/blurry default"
    - "Mobile browser chrome (address bar / task switcher) tints to brand ember (#F7581E) via theme-color"
    - "Android/desktop 'Install app' / PWA prompt reads name 'Juan Carlos Angulo', short_name 'Juan Angulo', and the correct icons from site.webmanifest"
  artifacts:
    - public/favicon.ico
    - public/favicon.svg
    - public/favicon-32x32.png
    - public/apple-touch-icon.png
    - public/icon-192.png
    - public/icon-512.png
    - public/site.webmanifest
    - scripts/generate-favicon-pngs.ts
  key_links:
    - "layout.tsx metadata.icons/metadata.manifest -> public/ static files (each href must resolve 200, never 404)"
    - "viewport.themeColor value (#F7581E) must byte-match globals.css's --primary token (src/app/globals.css:19) — no independent color pick"
    - "site.webmanifest background_color (#FAFAF7) must byte-match globals.css's --background token (src/app/globals.css:13)"
---

<objective>
Wire the real Juan Carlos Angulo brand favicon, apple-touch-icon, theme-color, and a PWA manifest sitewide from the single root layout (`src/app/(frontend)/[locale]/layout.tsx`), closing META-02, META-03, META-04, and META-05 from the opengraph.to audit in one vertical slice — asset generation through to a live, curl-verified render on Home.

Purpose: These 4 warnings are pure meta-tag/static-asset wiring with zero design work (Juan already confirmed reusing JuanPortfolio's real isotype and the existing brand tokens) — this plan is the phase's tracer, proving the full asset-generation-to-verified-render path once before Plan 02/03 expand `alternates.canonical` horizontally across ~19 routes.
Output: 7 static assets in `public/`, one one-off raster script, and `layout.tsx` gaining `metadata.icons`, `metadata.manifest`, and a new `viewport` export — all live-verified via curl against a real dev server.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-meta-tags-completion/42-CONTEXT.md

@src/app/(frontend)/[locale]/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Favicon + apple-touch-icon + theme-color + manifest — generated, wired, and live-verified on Home</name>
  <files>public/favicon.ico, public/favicon.svg, public/favicon-32x32.png, public/apple-touch-icon.png, public/icon-192.png, public/icon-512.png, public/site.webmanifest, scripts/generate-favicon-pngs.ts, src/app/(frontend)/[locale]/layout.tsx</files>
  <read_first>
    /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/public/favicon.ico
    /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/public/favicon.svg
    src/app/(frontend)/[locale]/layout.tsx (current `metadata` export — metadataBase + twitter card from Phase 41)
    src/app/globals.css (lines 13 and 19 — `--background: #FAFAF7`, `--primary: #F7581E`)
    src/payload.config.ts line 13 (`import sharp from 'sharp'` — confirms the exact default-import convention already used in this repo)
  </read_first>
  <action>
    Step 1 — copy the real brand assets verbatim, no modification: copy `/Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/public/favicon.ico` to `public/favicon.ico` and `/Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/public/favicon.svg` to `public/favicon.svg` (binary-safe copy — do not open/re-save the .ico, do not reformat the .svg).

    Step 2 — create `scripts/generate-favicon-pngs.ts`, a one-off Node script (kept in the repo afterward, matching the existing `scripts/check-dark-contrast.ts` one-off-script convention, run via `node_modules/.bin/tsx`). It must: import `sharp` as the default export (same convention as `src/payload.config.ts`) and Node's `node:fs`/`node:path`; read `public/favicon.svg` into a `Buffer`; define the 4 raster targets as `{ file: 'favicon-32x32.png', size: 32 }`, `{ file: 'apple-touch-icon.png', size: 180 }`, `{ file: 'icon-192.png', size: 192 }`, `{ file: 'icon-512.png', size: 512 }`; and for each, call `sharp(svgBuffer).resize(size, size).png().toFile(path.join('public', file))`. Note on the SVG's dual `#light-icon`/`#dark-icon` groups (both already render an identical black-rounded-square + white "J" — verified by inspection): the file's own embedded `<style>` sets `#dark-icon { display: none }` unconditionally and only overrides it inside a `prefers-color-scheme: dark` media query, which a static raster render never matches — so passing the whole SVG buffer straight into `sharp()` already renders exactly the `light-icon` variant CONTEXT.md calls for; no manual `<g>` extraction or XML surgery needed.

    Step 3 — run `node_modules/.bin/tsx scripts/generate-favicon-pngs.ts` to actually produce the 4 PNGs inside `public/`.

    Step 4 — create `public/site.webmanifest` (exact filename, this exact JSON, matching the audit's expected `/site.webmanifest` path per 42-CONTEXT.md): `name` "Juan Carlos Angulo", `short_name` "Juan Angulo", `start_url` "/", `display` "standalone", `background_color` "#FAFAF7", `theme_color` "#F7581E", `icons` an array of two entries — `{ src: "/icon-192.png", sizes: "192x192", type: "image/png" }` and `{ src: "/icon-512.png", sizes: "512x512", type: "image/png" }`.

    Step 5 — edit `src/app/(frontend)/[locale]/layout.tsx`: extend the `import type { Metadata } from 'next'` line to also import `Viewport`. Add an `icons` field to the existing `metadata` object: `icon` as an array of `{ url: '/favicon.ico', sizes: 'any' }`, `{ url: '/favicon.svg', type: 'image/svg+xml' }`, `{ url: '/favicon-32x32.png', sizes: '32x32', type: 'image/png' }`, and `apple: '/apple-touch-icon.png'`. Add `manifest: '/site.webmanifest'` as a sibling field on the same `metadata` object. Then add a new, separate top-level export `export const viewport: Viewport = { themeColor: '#F7581E' }` right after the `metadata` export — `themeColor` must NOT be placed inside the `metadata` object itself; Next.js 15 requires it on the dedicated `viewport`/`generateViewport` export (confirmed against the Next.js v15 docs), and putting it under `metadata` silently does nothing.

    Step 6 — live-verify: start the dev server, then curl Home in both locales for the 4 new tags, and curl each of the 7 new `public/` assets directly to confirm they are served with a 200 and a sane (non-`text/plain`, non-404-page) Content-Type.
  </action>
  <verify>
    <automated>
npm run dev &
DEV_PID=$!
sleep 6
echo "--- Home /  icons/manifest/theme-color ---"
curl -s http://localhost:3000/ | grep -o '<link rel="icon"[^>]*>'
curl -s http://localhost:3000/ | grep -o '<link rel="apple-touch-icon"[^>]*>'
curl -s http://localhost:3000/ | grep -o '<link rel="manifest"[^>]*>'
curl -s http://localhost:3000/ | grep -o '<meta name="theme-color"[^>]*>'
echo "--- Home /en/ same 4 tags (sitewide, not locale-conditional) ---"
curl -s http://localhost:3000/en/ | grep -o '<link rel="manifest"[^>]*>'
echo "--- static asset Content-Type/status checks ---"
for f in favicon.ico favicon.svg favicon-32x32.png apple-touch-icon.png icon-192.png icon-512.png site.webmanifest; do
  echo "-- /$f --"
  curl -sI "http://localhost:3000/$f" | grep -iE '^(HTTP|content-type)'
done
kill $DEV_PID 2>/dev/null
npx tsc --noEmit -p tsconfig.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `public/favicon.ico`, `public/favicon.svg` exist and are byte-identical to the JuanPortfolio source files (same file size)
    - `public/favicon-32x32.png`, `public/apple-touch-icon.png`, `public/icon-192.png`, `public/icon-512.png` exist and are valid PNGs at exactly 32x32, 180x180, 192x192, 512x512 respectively
    - `public/site.webmanifest` exists and is valid JSON containing all 7 required keys (name, short_name, start_url, display, background_color, theme_color, icons)
    - curl of `http://localhost:3000/` HTML contains a `<link rel="icon" ...>` tag, a `<link rel="apple-touch-icon" href="/apple-touch-icon.png">` tag, a `<link rel="manifest" href="/site.webmanifest">` tag, and a `<meta name="theme-color" content="#F7581E">` tag
    - the same 4 tags are present on `http://localhost:3000/en/` (sitewide, from the shared root layout — not locale-gated)
    - `curl -sI http://localhost:3000/apple-touch-icon.png` returns `200` with a `Content-Type` starting `image/png`
    - `curl -sI http://localhost:3000/site.webmanifest` returns `200` with a `Content-Type` that is NOT `text/plain` and NOT an HTML error-page type
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Home (both locales) serves the real isotype favicon, a working apple-touch-icon, brand-ember theme-color, and a linked site.webmanifest — all confirmed via live curl against a real dev server, not just static code reading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser/crawler request → Next.js static file server (`public/`) | Untrusted clients request favicon/manifest/PNG assets by fixed, hardcoded filename — zero user input is reflected into any served asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Information Disclosure | `public/site.webmanifest` | low | accept | Manifest contains only public brand fields (name, short_name, icons, theme_color, background_color, start_url) — no user data, API keys, or internal URLs; content is static, hand-authored in this task, never templated from request data. |
| T-42-02 | Tampering | Next.js static serving of `public/*.png`, `favicon.ico`/`.svg`, `site.webmanifest` | medium | mitigate | Task 1's `<verify>` runs `curl -I` against all 7 new assets and asserts a non-`text/plain` Content-Type per file — catches a misconfigured/incorrect MIME type (e.g. `.webmanifest` silently served as plain text, which some clients reject for PWA install) before this ships, rather than assuming Next's default static-file MIME detection is correct for the less-common `.webmanifest` extension. |
</threat_model>

<verification>
Run Task 1's `<automated>` block end-to-end against a real `npm run dev` server. All 7 static assets must resolve with correct status/Content-Type, and Home must serve all 4 new meta/link tags in both locales.
</verification>

<success_criteria>
ROADMAP Phase 42 Success Criteria #1-4 closed: favicon.ico/svg exist and are declared, apple-touch-icon is declared and real, theme-color is declared and matches the brand token, and site.webmanifest is served + linked from `<head>` — all confirmed via live curl.
</success_criteria>

<output>
Create `.planning/phases/42-meta-tags-completion/42-01-SUMMARY.md` when done
</output>
