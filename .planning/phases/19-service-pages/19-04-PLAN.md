---
phase: 19-service-pages
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - scripts/seed-phase19-data/group-b.ts
autonomous: true
requirements: [SEO-SVC-02, SEO-SVC-03]

must_haves:
  truths:
    - "Desarrollo Full-Stack copy explicitly names Next.js/Payload headless CMS development with SEO integrated into the code, not bolted on — the real differentiator no audited competitor offers"
    - "SEO para IA/GEO copy explicitly references llms.txt and llms-full.txt as concrete, already-live proof of the offering (not just 'GEO'/'SEO para IA' used as a label), via proofLinks pointing at /llms.txt and /llms-full.txt"
    - "Both services follow H1(pain-framed subtitle)→qué incluye→cómo trabajo→FAQ(3-5 real questions)→CTA, in both es and en, with zero pricing anywhere"
  artifacts:
    - path: "scripts/seed-phase19-data/group-b.ts"
      provides: "fullstackServiceCopy: BilingualServiceCopy, geoServiceCopy: BilingualServiceCopy"
      contains: "geoServiceCopy"
  key_links:
    - from: "scripts/seed-phase19-data/group-b.ts"
      to: "scripts/seed-phase19-data/types.ts"
      via: "import type { BilingualServiceCopy } from './types'"
      pattern: "from './types'"
    - from: "scripts/seed-phase19-data/group-b.ts"
      to: "src/lib/services-data.ts"
      via: "slug values in ServiceCopy.slug match SERVICE_SLUGS entries ('fullstack-development', 'ai-seo-geo')"
      pattern: "fullstack-development"
    - from: "scripts/seed-phase19-data/group-b.ts"
      to: "src/app/llms.txt"
      via: "geoServiceCopy proofLinks[].url === '/llms.txt' or '/llms-full.txt'"
      pattern: "llms.txt"
---

<objective>
Write the real, bilingual (es/en) marketing copy for the two most differentiated service
landings — Desarrollo Full-Stack con SEO integrado and SEO para IA/GEO — as a pure TypeScript data
module conforming to the contracts defined in 19-01 (SEO-SVC-02, SEO-SVC-03).

Purpose: these two services carry this phase's actual competitive differentiation (per the audit,
§2 and §6 item 2): full-stack Next.js/Payload development with SEO built into the code (no
competitor offers this at Juan's level), and naming "SEO para IA/GEO" as its own line while
proving it with already-live `llms.txt`/`llms-full.txt` infrastructure rather than just a label.

Output: `scripts/seed-phase19-data/group-b.ts`, exporting typed copy for 2 services, ready for
19-05's orchestrator to consume unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-service-pages/19-CONTEXT.md
@.planning/REQUIREMENTS.md
@research/SEO-COMPETITIVE-AUDIT-v1.4.md

@scripts/seed-phase19-data/types.ts
@src/app/llms.txt/route.ts
@src/app/llms-full.txt/route.ts

<interfaces>
<!-- scripts/seed-phase19-data/types.ts (created by 19-01) — same contract as 19-03's plan, repeated
here for this plan's independence:

export type Locale = 'es' | 'en'
export interface FaqItem { question: string; answer: string }
export interface ProofLink { url: string; label: string; description: string }
export interface ServiceCopy {
  slug: string
  hero: { title: string; subtitle: string }
  includes: { title: string; paragraphs: string[] }
  process: { title: string; paragraphs: string[] }
  faqs: FaqItem[]
  proofLinks?: ProofLink[]
  ctaText: string
  ctaLinkLabel: string
}
export type BilingualServiceCopy = Record<Locale, ServiceCopy>

`proofLinks` is optional on `ServiceCopy` and is used ONLY by `geoServiceCopy` in this plan — the
orchestrator (19-05) renders each `proofLink` as an extra Content-block column with a real,
working link (D-05 requires the SEO-para-IA/GEO page to link directly to `/llms.txt` and/or
`/llms-full.txt`, not just mention the filenames as inert text).

Audit finding this service must lean into (research/SEO-COMPETITIVE-AUDIT-v1.4.md §1.2/§6 item 2):
Juan already has the infrastructure (`llms.txt`/`llms-full.txt`, live since Phase 2) but has never
named "SEO para IA/GEO" as an explicit service line — 2 of 4 audited competitors already do
(Capitán SEO has `/seo-para-ia/` in its main nav). This is a positioning gap, not an infrastructure
gap — the copy must make that tangible ("así es como estructuro el contenido para que los agentes
de IA lo puedan citar — mirá /llms.txt en este mismo sitio", per 19-CONTEXT.md D-05's exact framing
example).

Real differentiator for Desarrollo Full-Stack (research/SEO-COMPETITIVE-AUDIT-v1.4.md §2): "ningún
[competidor] ofrece desarrollo full-stack con Next.js/Payload/CMS headless a ese nivel... este es
el ángulo a explotar... 'desarrollo real (Next.js/Payload) con SEO integrado desde el código', no
'SEO + un poco de WordPress'." Carlos Sánchez is the closest competitor (mentions React/Vue/Next/
Angular) but "sin case studies de proyectos reales construidos así" — Juan's advantage is that this
very site (juan-tech.com, this repo) plus the CaseStudies collection's real projects ARE that proof.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desarrollo Full-Stack + SEO para IA/GEO copy (bilingual)</name>
  <files>scripts/seed-phase19-data/group-b.ts</files>
  <read_first>
    scripts/seed-phase19-data/types.ts,
    research/SEO-COMPETITIVE-AUDIT-v1.4.md,
    src/app/llms.txt/route.ts,
    src/app/llms-full.txt/route.ts
  </read_first>
  <action>
    Create `scripts/seed-phase19-data/group-b.ts`, importing `type { BilingualServiceCopy }` from
    `'./types'`. Export 2 consts:

    `export const fullstackServiceCopy: BilingualServiceCopy` (slug: `fullstack-development`) — per
    D-03/D-06(b): `hero.subtitle` frames the pain of SEO bolted onto a slow/generic WordPress build
    versus SEO built into the code from day one (crawl-blocking rendering, unoptimized JS bundles,
    schema retrofitted instead of designed-in). `includes` must EXPLICITLY name Next.js and Payload
    (or "headless CMS") — this is the one non-negotiable per this plan's must_haves — covering
    things like server-rendered/streamed pages for crawlability, Core Web Vitals built into the
    architecture (not patched after launch), structured data designed into the schema/collections
    layer, not templated afterward. `process` = how Juan works (architecture/schema design →
    build → SEO-aware from the first commit, not an SEO pass at the end). `faqs`: 3-5 real
    questions, at least one addressing "¿por qué Next.js/Payload en vez de WordPress?" (D-07's exact
    example) with a real, specific answer (not "because it's better" — ground it in the audit's
    findings: rendering control, schema-level SEO fields, no plugin bloat). `ctaText`/
    `ctaLinkLabel`: no price.

    `export const geoServiceCopy: BilingualServiceCopy` (slug: `ai-seo-geo`) — per D-05/D-03:
    `hero.subtitle` frames the emerging pain (AI answer engines/agents citing competitors instead of
    you because your content isn't structured for machine consumption). `includes` = what this
    service covers conceptually (structuring content so AI agents can parse/cite it, machine-
    readable content manifests, semantic markup). `process` = how Juan works. `faqs`: 3-5 real
    questions (e.g. "¿qué es GEO y en qué se diferencia del SEO tradicional?", "¿cómo se mide el
    éxito de una estrategia GEO?"). `proofLinks`: exactly 2 entries — `{ url: '/llms.txt', label:
    (es: 'Ver /llms.txt' / en: 'View /llms.txt'), description: a real sentence explaining this file
    already exists on THIS site as a live example of the offering }` and the same shape for
    `{ url: '/llms-full.txt', ... }`. The `hero.subtitle` or `includes.paragraphs` text must also
    mention `llms.txt`/`llms-full.txt` by name in prose (not just via the `proofLinks` array) —
    satisfies this plan's must_have that the reference is explicit, not just a link with no framing.
    `ctaText`/`ctaLinkLabel`: no price.

    Every string field must have both an `es` and `en` value — no field left empty, no placeholder
    text in either locale.
  </action>
  <acceptance_criteria>
    - `grep -c "fullstackServiceCopy\|geoServiceCopy" scripts/seed-phase19-data/group-b.ts` returns >= 2
    - `grep -ic "next.js\|payload" scripts/seed-phase19-data/group-b.ts` returns >= 2 (Next.js/Payload named explicitly, not just implied)
    - `grep -c "llms.txt\|llms-full.txt" scripts/seed-phase19-data/group-b.ts` returns >= 4 (mentioned in prose AND in the 2 proofLinks urls, at minimum)
    - `grep -c "proofLinks" scripts/seed-phase19-data/group-b.ts` returns >= 1
    - `grep -c "fullstack-development\|ai-seo-geo" scripts/seed-phase19-data/group-b.ts` returns >= 2
    - `grep -ic "TODO\|lorem ipsum" scripts/seed-phase19-data/group-b.ts` returns 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `group-b.ts` exports fully-populated bilingual copy for both services, with Next.js/Payload
    named explicitly on the Full-Stack service and `llms.txt`/`llms-full.txt` both mentioned in
    prose and linked via `proofLinks` on the GEO service.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no pricing leakage and GEO proof-link correctness</name>
  <files>scripts/seed-phase19-data/group-b.ts</files>
  <action>
    Run the grep-based checks below. If any fails, fix `group-b.ts` within this same task before
    considering the plan done.
  </action>
  <acceptance_criteria>
    - `grep -iE '(€|\$|USD|EUR|/mes|/month|precio|price|tarifa|pricing)' scripts/seed-phase19-data/group-b.ts | grep -v -i "sin precio\|no price"` returns 0 matches
    - `grep -c "url: '/llms.txt'" scripts/seed-phase19-data/group-b.ts` returns >= 1 per locale (i.e. >= 2 total across es/en) — confirms the proof link URL is the real root-relative path, not a placeholder or wrong path
    - `grep -c "url: '/llms-full.txt'" scripts/seed-phase19-data/group-b.ts` returns >= 2
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `group-b.ts` contains zero pricing language, and both proof links use the correct real,
    root-relative `/llms.txt` and `/llms-full.txt` URLs for both locales.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Static, admin-authored content consumed only by a local seed script (19-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | `group-b.ts` content later rendered via Lexical/RenderBlocks | accept | Same as T-19-05 in 19-03 — plan-time authored content, no external input |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan |

No new trust boundary — this is a pure content-authoring plan.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. Zero pricing language anywhere in the file
3. `llms.txt`/`llms-full.txt` referenced both in prose and via `proofLinks` with the correct root-relative URLs
</verification>

<success_criteria>
- `fullstackServiceCopy` explicitly names Next.js/Payload as the real differentiator
- `geoServiceCopy` explicitly references and links `/llms.txt` and `/llms-full.txt` as tangible proof
- No pricing anywhere
- Each service's FAQ has 3-5 real, non-generic questions
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/19-service-pages/19-04-SUMMARY.md` when done
</output>
