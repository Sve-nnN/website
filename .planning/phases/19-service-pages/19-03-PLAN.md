---
phase: 19-service-pages
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - scripts/seed-phase19-data/group-a.ts
autonomous: true
requirements: [SEO-SVC-01, SEO-SVC-02]

must_haves:
  truths:
    - "Index page copy lists all 4 services (name + 1-line description each) with zero price/cost figures anywhere in either locale"
    - "Auditoría SEO Técnica and Consultoría SEO copy each follow H1(pain-framed subtitle)→qué incluye→cómo trabajo→FAQ(3-5 real questions)→CTA, in both es and en"
    - "Consultoría SEO's FAQ includes at least one question a genuine prospect would ask about ongoing consulting engagement structure (not a generic filler question)"
  artifacts:
    - path: "scripts/seed-phase19-data/group-a.ts"
      provides: "indexPageCopy: BilingualIndexCopy, auditServiceCopy: BilingualServiceCopy, consultingServiceCopy: BilingualServiceCopy"
      contains: "indexPageCopy"
  key_links:
    - from: "scripts/seed-phase19-data/group-a.ts"
      to: "scripts/seed-phase19-data/types.ts"
      via: "import type { BilingualServiceCopy, BilingualIndexCopy } from './types'"
      pattern: "from './types'"
    - from: "scripts/seed-phase19-data/group-a.ts"
      to: "src/lib/services-data.ts"
      via: "slug values in ServiceCopy.slug match SERVICE_SLUGS entries ('seo-technical-audit', 'seo-consulting')"
      pattern: "seo-technical-audit"
---

<objective>
Write the real, bilingual (es/en) marketing copy for the Services index page and 2 of the 4
individual service landings — Auditoría SEO Técnica and Consultoría SEO — as a pure TypeScript data
module conforming to the contracts defined in 19-01 (SEO-SVC-01, SEO-SVC-02).

Purpose: separate content authorship from the seed script's Payload-block-assembly logic (19-05),
so this plan is entirely about writing copy that sounds like Juan (technical, direct, no corporate
fluff) and follows the audit-validated structural pattern — not about wiring Lexical JSON or
Payload upsert mechanics.

Output: `scripts/seed-phase19-data/group-a.ts`, exporting typed copy for the index page + 2
services, ready for 19-05's orchestrator to consume unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-service-pages/19-CONTEXT.md
@.planning/REQUIREMENTS.md
@research/SEO-COMPETITIVE-AUDIT-v1.4.md

@scripts/seed-phase19-data/types.ts
@scripts/seed-phase13-home-content.ts

<interfaces>
<!-- scripts/seed-phase19-data/types.ts (created by 19-01):

export type Locale = 'es' | 'en'
export interface FaqItem { question: string; answer: string }
export interface ProofLink { url: string; label: string; description: string }
export interface ServiceCopy {
  slug: string
  hero: { title: string; subtitle: string }
  includes: { title: string; paragraphs: string[] }
  process: { title: string; paragraphs: string[] }
  faqs: FaqItem[]
  proofLinks?: ProofLink[]
  ctaText: string
  ctaLinkLabel: string
}
export type BilingualServiceCopy = Record<Locale, ServiceCopy>
export interface IndexPageCopy {
  hero: { title: string; subtitle: string }
  services: { slug: string; name: string; description: string }[]
  ctaText: string
  ctaLinkLabel: string
}
export type BilingualIndexCopy = Record<Locale, IndexPageCopy>

Existing site voice reference (Phase 13's Home aboutSection/FAQ copy, scripts/seed-phase13-home-content.ts)
— match this register exactly: technical, direct, first person, no corporate fluff, professional
neutral Spanish (no voceo/vos in the site's own copy register — Phase 13 uses neutral "tú/usted"-free
constructions like "Optimizo el Crawl Budget..."/"Cubro ambas fases...", not "vos"). English is
plain, confident, no marketing filler ("An obsession with 100/100 scores", not "We are passionate
about...").

Real differentiator to lean on (research/SEO-COMPETITIVE-AUDIT-v1.4.md §2): "desarrollo real
(Next.js/Payload) con SEO integrado desde el código" — mention this explicitly on the Consultoría
SEO page too (not just Desarrollo Full-Stack in 19-04) wherever relevant to differentiate from
generic SEO consultants who bolt SEO onto someone else's build.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index page copy + Auditoría SEO Técnica + Consultoría SEO (bilingual)</name>
  <files>scripts/seed-phase19-data/group-a.ts</files>
  <read_first>
    scripts/seed-phase19-data/types.ts,
    research/SEO-COMPETITIVE-AUDIT-v1.4.md
  </read_first>
  <action>
    Create `scripts/seed-phase19-data/group-a.ts`, importing `type { BilingualIndexCopy,
    BilingualServiceCopy }` from `'./types'`. Export 3 consts:

    `export const indexPageCopy: BilingualIndexCopy` — hero title "Servicios" (es) / "Services"
    (en), subtitle framing the offering around technical SEO + real full-stack development (not a
    generic "I do SEO" line — ground it in the audit's differentiator). `services` array: 4 entries
    (slugs `seo-technical-audit`, `seo-consulting`, `fullstack-development`, `ai-seo-geo`, matching
    `src/lib/services-data.ts`'s `SERVICE_SLUGS` exactly — do not invent different slug strings),
    each with a real service name (es/en) and a genuine 1-line description (not filler, must
    differentiate the 4 from each other — a reader should understand why they'd pick one over
    another from the description alone). `ctaText`/`ctaLinkLabel`: final CTA pointing toward
    contact (e.g. es: "¿No sabés cuál servicio necesitás? Hablemos." / en: "Not sure which service
    fits? Let's talk."), no price mentioned.

    `export const auditServiceCopy: BilingualServiceCopy` (slug: `seo-technical-audit`) — per D-03
    structure: `hero.title` = the service name, `hero.subtitle` = problem/pain framing (what's
    broken when a site needs a technical audit — crawl budget waste, rendering issues, indexing
    gaps — ground this in real technical SEO problems, not generic "improve your SEO"). `includes`
    = what a technical audit concretely covers (Core Web Vitals, crawl/render analysis, schema,
    information architecture — be specific, this is Juan's actual expertise per the audit's §2).
    `process` = how the engagement runs (audit → prioritized findings → optional implementation,
    matching the audit's finding that Juan "cubre ambas fases" — detects issues AND can implement
    fixes, unlike auditors who only hand off a report). `faqs`: 3-5 real questions (D-07 examples:
    "¿cuánto dura una auditoría técnica?", "¿entregan solo el informe o también implementan las
    correcciones?", "¿qué herramientas usás?" — write real, specific answers, not placeholders).
    `ctaText`/`ctaLinkLabel`: final CTA to contact, no price.

    `export const consultingServiceCopy: BilingualServiceCopy` (slug: `seo-consulting`) — same
    structure. `hero.subtitle` = pain framing for ongoing/recurring SEO needs (as opposed to a
    one-off audit — differentiate clearly from `auditServiceCopy` so the two pages don't read as
    duplicates). `includes` = what ongoing consulting covers (strategy, monitoring, roadmap,
    cross-team collaboration with dev/marketing). `process` = how an ongoing engagement is
    structured (cadence, reporting, collaboration model). `faqs`: 3-5 real questions, at least one
    about engagement structure/cadence (per this plan's must_haves). `ctaText`/`ctaLinkLabel`: no
    price, cotización-a-medida framing consistent with `research/SEO-COMPETITIVE-AUDIT-v1.4.md`
    §3's finding that 3/4 competitors hide price and use a "cotización a medida" pattern.

    Every string field must have both an `es` and `en` value — no field may be left empty, no
    placeholder text (e.g. "TODO", "Lorem ipsum", "[description]") anywhere in either locale.
  </action>
  <acceptance_criteria>
    - `grep -c "indexPageCopy\|auditServiceCopy\|consultingServiceCopy" scripts/seed-phase19-data/group-a.ts` returns >= 3
    - `grep -ic "TODO\|lorem ipsum\|\[.*description.*\]" scripts/seed-phase19-data/group-a.ts` returns 0
    - `grep -c "seo-technical-audit\|seo-consulting" scripts/seed-phase19-data/group-a.ts` returns >= 2
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `group-a.ts` exports fully-populated bilingual copy for the index page and 2 services,
    structurally conforming to `types.ts`'s contracts, with distinct (non-duplicate) pain framing
    between the audit and consulting services.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no pricing leakage and copy completeness</name>
  <files>scripts/seed-phase19-data/group-a.ts</files>
  <action>
    Run the grep-based checks below to confirm D-01/ROADMAP's "sin precios publicados" constraint
    holds and that every `ServiceCopy`/`IndexPageCopy` field required by `types.ts` is present with
    non-empty content for both locales. This is a read-only verification task — if any check fails,
    go back and fix `group-a.ts` in this same task (do not proceed to mark the plan done with a
    failing check).
  </action>
  <acceptance_criteria>
    - `grep -iE '(€|\$|USD|EUR|/mes|/month|precio|price|tarifa|pricing)' scripts/seed-phase19-data/group-a.ts | grep -v -i "sin precio\|no price"` returns 0 matches (any literal currency symbol, "/mes", "/month", or pricing-related word must be absent, except inside a sentence explicitly stating there is NO published price, if such a sentence exists)
    - `grep -c "faqs:" scripts/seed-phase19-data/group-a.ts` returns >= 2 (one per service)
    - `npx tsc --noEmit` exits 0 (re-confirms the file still compiles after any fixes made in this task)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `group-a.ts` contains zero pricing figures or pricing-table language in either locale, and both
    services have a populated `faqs` array.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan produces static, admin-authored TypeScript content consumed only by a build-time/local-only seed script (19-05) — no runtime input, no user-facing write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | `group-a.ts` content later rendered via Lexical/RenderBlocks | accept | Content is authored by Claude/Juan at plan-time and committed to the repo — same trust level as every other seed script's copy already in production (`seed-phase13-home-content.ts`, `seed-legal-pages.ts`), no external input |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan |

No new trust boundary — this is a pure content-authoring plan.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. Zero pricing language anywhere in the file (Task 2's grep)
3. Both services have distinct, non-templated pain framing (manual read confirms — this is a copy-quality check, not automatable beyond the placeholder/pricing greps already run)
</verification>

<success_criteria>
- `indexPageCopy`, `auditServiceCopy`, `consultingServiceCopy` are fully populated, bilingual, and conform to `types.ts`
- No pricing anywhere
- Each service's FAQ has 3-5 real, non-generic questions
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/19-service-pages/19-03-SUMMARY.md` when done
</output>
