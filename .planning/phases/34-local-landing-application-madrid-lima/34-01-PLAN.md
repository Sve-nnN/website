# Phase 34 Plan 01: Apply Local Landing Components to Madrid/Lima

**Status:** Complete

## Goal

Convert the 2 existing, live pages `seo-tecnico-madrid` and `seo-tecnico-lima` from generic `Hero`/`listing` structure to the Phase 33 `local-landing` variant + a new `LocalProofSection` block, with real structural differentiation between the two cities (per ROADMAP Phase 34 success criteria) and clearly-marked placeholder content where real data doesn't exist yet (Juan's explicit authorization, real GSC client data pending).

## Tasks

1. **Inspect current state**: read both page.tsx files (confirm 100% `RenderBlocks`-driven), read the Phase 33 Hero/LocalProofSection config+Component files in full, read Phase 33's CONTEXT/SUMMARY for exact field shapes, and query the live DB (`scripts/phase34-inspect-pages.ts`) to see both pages' actual current `hero` block (variant `listing`, empty `links`, real title/subtitle, unused local-landing fields already at schema defaults).
2. **Write `scripts/phase34-apply-local-landing.ts`**: fetches each page (both locales) via Local API, converts the existing hero block in place (`variant: 'local-landing'` + ring config + `cityName`/`inlineStat`/`links` per city), inserts a new `LocalProofSection` block right after it, leaves `content`/`faq`/`callToAction` blocks untouched. Writes locale `es` first, re-fetches to capture Payload-assigned ids for the new array rows, reapplies those ids (generic recursive `applyIds()` helper) before writing locale `en` — avoids duplicate rows across the two locale-scoped writes.
3. **Run the script** against the real Neon DB (additive content update via Local API — no schema change, no destructive statement; does not require pause-for-confirmation per project CLAUDE.md's DB safety rules).
4. **Functional test**: start `next dev` (port 3457), curl all 4 routes (`/seo-tecnico-madrid`, `/en/seo-tecnico-madrid`, `/seo-tecnico-lima`, `/en/seo-tecnico-lima`), confirm 200s, confirm exactly 1 `<h1>` per page, confirm correct per-city ring `opacity`/`transform`, confirm city badges, CTA labels/hrefs, and `[PLACEHOLDER]` markers render in the DOM. Kill the dev server.
5. **Regression spot-check**: compare H1 counts against `.planning/phases/32-regression-baseline/32-baseline-content.json` (all 4 routes: baseline says 1 H1 each, same titles — post-change matches).
6. **Document**: write 34-CONTEXT.md, 34-01-SUMMARY.md (with the full placeholder table), 34-VERIFICATION.md; update STATE.md/ROADMAP.md/REQUIREMENTS.md.

## Interfaces

- `scripts/phase34-apply-local-landing.ts` — one-off Local API script, safe to leave in repo (idempotent re-run: converts hero + inserts proof section again if run twice — not idempotent against double-insertion, intended as a single-use apply script, not a repeatable seed).
- `scripts/phase34-inspect-pages.ts` — read-only helper, safe to leave in repo.

## Verification

- Script output re-fetches and prints hero + localProofSection fields per locale per page — confirmed correct values.
- Dev server curl: 4/4 routes 200, 1 H1 each, correct ring styling, correct CTA copy/hrefs, `[PLACEHOLDER]` present (22 occurrences on Madrid pages, 15 on Lima pages, across both locales combined per page).
- No server errors in dev log during the functional test.
