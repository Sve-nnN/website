---
phase: 37-case-studies-content-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
autonomous: true
requirements: [CASE-07, CASE-08, CASE-11]

must_haves:
  truths:
    - "The case-study detail page renders AuthorCard exactly once (no AuthorByline stacked on top) — verified by reading the rendered author section"
    - "Each case study's JSON-LD CreativeWork reflects that specific doc's real author/client/kpis/dates — not a generic 2-field object shared across every doc"
    - "The case-study detail page ends with a closing CTA section (heading + 2 locale-aware buttons) placed after the conclusion and before the author section, matching the ariannalupi.com/casos structural reference"
  artifacts:
    - path: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      provides: "Deduplicated author section, dynamic per-doc creativeWorkData, new closing CTA section"
      contains: "additionalProperty"
  key_links:
    - from: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      to: "src/components/AuthorCard.tsx"
      via: "single <AuthorCard author={author} /> render, no AuthorByline"
      pattern: "AuthorCard author={author}"
    - from: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx creativeWorkData"
      to: "doc.kpis / doc.author / doc.client / doc.createdAt / doc.updatedAt"
      via: "additionalProperty / author / creator / dateCreated / dateModified fields composed from real doc data"
      pattern: "additionalProperty: doc.kpis"
    - from: "closing CTA buttons"
      to: "/contact and /case-studies routes"
      via: "locale-aware href (empty prefix for es, /en prefix for en, matching src/lib/breadcrumbs.ts homeHref pattern)"
      pattern: "locale === 'es'"
---

<objective>
Fix three code-level bugs on the case-study detail page, all confirmed in `src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx`: the author section renders both `AuthorByline` and `AuthorCard` stacked (CASE-07, visible duplication), the `creativeWorkData` JSON-LD object is generic/shallow with only 2 real fields (CASE-08, fails "dynamic per-doc" audit), and the page structure is missing a closing CTA section that the reference site (`ariannalupi.com/casos/ecommerce-vape/`) has before its footer (CASE-11).

Purpose: These are the 3 requirements from Phase 37 that have a genuine code root cause, independent of the 6 case-study docs' content — they can be fixed and verified without needing the content audit in Plan 37-03/37-04 to be done first.
Output: Updated `page.tsx` with a single-render author section, a real per-doc JSON-LD object, and a new closing CTA section with locale-aware buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-case-studies-content-audit-fix/37-CONTEXT.md
@.planning/phases/37-case-studies-content-audit-fix/37-UI-SPEC.md
@.planning/phases/37-case-studies-content-audit-fix/37-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Current state of the file to modify, and the two sibling analogs it must match. Executor should not need to explore the codebase further for these 3 fixes. -->

Current `src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx` (full file, 257 lines, already read in full during planning) — the 3 relevant excerpts:

```typescript
// Lines 91-96 — the CASE-08 bug: generic JSON-LD, only 2 real fields
const creativeWorkData = {
  '@context': 'https://schema.org',
  '@type': 'CreativeWork',
  name: doc.title,
  about: doc.heroSubtitle,
}
```

```typescript
// Lines 243-250 — the CASE-07 bug: AuthorByline + AuthorCard stacked
{author && (
  <section>
    <AuthorByline author={author} />
    <div className="mt-6">
      <AuthorCard author={author} />
    </div>
  </section>
)}
```

```typescript
// Lines 50-67 — the `copy` object the new CTA strings extend
const copy = {
  es: { client: 'El cliente', challenge: 'El reto', solution: 'La solución', results: 'Resultados', before: 'Antes', after: 'Después' },
  en: { client: 'The Client', challenge: 'The Challenge', solution: 'The Solution', results: 'Results', before: 'Before', after: 'After' },
}
```

Locale-aware href pattern already established in `src/lib/breadcrumbs.ts` (read in full during planning):
```typescript
function homeHref(locale: Locale): string {
  return locale === 'es' ? '/' : '/en'
}
```
This page does NOT currently import `homeHref` (it's not exported for reuse outside `breadcrumbs.ts` in a directly importable form beyond the trail builders) — inline the same 1-line ternary logic directly in this file rather than adding a new cross-file dependency for a single href prefix.

`Button` primitive (`src/components/ui/button.tsx`, already read in full): `import { Button } from '@/components/ui/button'`, props `variant` (`"default" | "outline" | "secondary" | ...`), `size`, `asChild`. Use `asChild` with a wrapped `<Link>` for internal navigation (Next.js `Link`, not a plain `<a>`), matching how `Button` + `Link` compose elsewhere in this codebase (`asChild` + `Slot` pattern already wired, no new dependency).

`doc.createdAt` / `doc.updatedAt` are present on every Payload document automatically (Payload's standard timestamp tracking, required by `CaseStudies`' `versions: { drafts: {...} }` config) — no schema change needed to read them.

`Author` type import already present (`import type { Author } from '@/payload-types'`) — `author.name` is the field to use for the JSON-LD `author` field.

`client` is already destructured in this file: `const client = typeof doc.client === 'object' ? doc.client : null` — `client.name` is the field for JSON-LD `creator`.

`doc.kpis` shape (from `src/collections/CaseStudies/index.ts`, already read in full): `{ label: string; value: string; id?: string }[]`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Deduplicate author section (CASE-07)</name>
  <files>src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx</files>
  <action>
    Remove the `AuthorByline author={author}` line and its now-redundant wrapping structure from the author section (currently lines ~243-250), leaving only `AuthorCard author={author}` directly inside the section — do NOT pass `asPageHeading` (that prop is reserved for the Author page's own H1; this page's H1 is the case-study title). Match the exact target shape from `37-UI-SPEC.md` Fix 1 contract: a section containing a single `AuthorCard` call, nothing else.

    Before removing the `import { AuthorByline } from '@/components/AuthorByline'` line, grep this file for any other `AuthorByline` usage — there should be none left after this edit, so remove the import too. Do NOT touch `src/components/AuthorByline.tsx` itself or any other page/component that renders it (listing cards, blog post header, etc. still use it correctly and are out of scope).
  </action>
  <verify>
    <automated>grep -c "AuthorByline" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | grep -q '^0$' && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The author section renders only AuthorCard (no AuthorByline), the AuthorByline import is removed, and tsc --noEmit passes with no errors referencing this file.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic per-doc JSON-LD (CASE-08)</name>
  <files>src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx</files>
  <action>
    Replace the generic `creativeWorkData` object with one that composes every field from real per-doc data already available in this file's scope (`doc`, `author`, `client` are all already fetched/destructured above this point — no new query needed). Target shape, following the same "every field traces to a real value" principle as `blog/[slug]/page.tsx`'s `articleData` (read in full during planning):

    - `name: doc.title` (unchanged)
    - `about: doc.heroSubtitle` (unchanged)
    - `description: doc.heroSubtitle` (new — CreativeWork's natural description field)
    - `author`: only included when `author` exists, shaped `{ '@type': 'Person', name: author.name }` (new)
    - `dateCreated: doc.createdAt` (new)
    - `dateModified: doc.updatedAt` (new)
    - `creator`: only included when `client?.name` exists, shaped `{ '@type': 'Organization', name: client.name }` (new)
    - `additionalProperty`: only included when `doc.kpis?.length`, mapping each kpi to `{ '@type': 'PropertyValue', name: kpi.label, value: kpi.value }` (new)

    Use conditional spread (`...(condition ? { key: value } : {})`) for each optional field rather than assigning literal `undefined`, so `JSON.stringify` (inside the `JsonLd` component) never emits a null/undefined-valued key for docs missing that data — keep the object shape valid schema.org even for the sparsest doc.
  </action>
  <verify>
    <automated>grep -c "additionalProperty" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | grep -q '^1$' && grep -c "dateCreated" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | grep -q '^1$' && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>creativeWorkData includes description/author/dateCreated/dateModified/creator/additionalProperty, each conditionally composed from doc/author/client/doc.kpis (no hardcoded/shared values), tsc --noEmit passes. If the dev server is running, curl a real case-study slug and confirm the ld+json script output contains additionalProperty with real kpi labels/values from that specific doc (not empty/generic) — do this spot-check if localhost:3000 responds, skip with a note if the dev server isn't up yet (Plan 37-03/37-04 will re-verify against fully-populated content).</done>
</task>

<task type="auto">
  <name>Task 3: Closing CTA section (CASE-11)</name>
  <files>src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx</files>
  <action>
    Per `37-UI-SPEC.md` Fix 3 ("New section contract: Closing CTA"), add a new section after the `conclusion` section and before the `author` section (matches the reference site's insight -> CTA -> footer/author-adjacent order).

    Extend the `copy` object (both `es`/`en`) with 3 new keys using this exact copy (already humanized, do not alter):
    - `ctaHeading`: ES "¿Quieres resultados como estos?" / EN "Want results like these?"
    - `ctaPrimary`: ES "Hablemos de tu proyecto" / EN "Let's talk about your project"
    - `ctaSecondary`: ES "Ver más casos de éxito" / EN "See more case studies"

    Import `Button` from `@/components/ui/button`. Build the locale-aware hrefs inline (do not import from `breadcrumbs.ts` — that module's `homeHref` isn't exported for standalone reuse outside its trail builders): compute a `localePrefix` constant that is empty string for `es` and `/en` for `en`, then build hrefs as `${localePrefix}/contact` and `${localePrefix}/case-studies`.

    Section structure: a centered `<section className="text-center mt-10">` containing an `<h2 className="font-heading text-heading mb-4">` with `t.ctaHeading`, and a `<div className="flex flex-wrap justify-center gap-4">` with two `Button` elements using `asChild` wrapping the existing `Link` import (this file already imports `Link` from `next/link` — reuse it, do not add a second import): one `variant="default"` linking to the contact href with `t.ctaPrimary` text, one `variant="outline"` linking to the case-studies href with `t.ctaSecondary` text.

    Place this section immediately after the `doc.conclusion` block and immediately before the `author` block, still inside the same `Container className="py-8 space-y-12"` that wraps every other section in this file.

    No new spacing/color/typography tokens (per UI-SPEC) — `mt-10`/`gap-4`/`font-heading text-heading` are all existing tokens already used elsewhere in this exact file.
  </action>
  <verify>
    <automated>grep -c "ctaHeading" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | grep -q '^1$' && grep -c "Button asChild" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | grep -q '^2$' && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>A new closing CTA section renders after conclusion and before author, with the exact ES/EN copy specified, 2 locale-aware buttons (primary -> /contact or /en/contact, secondary -> /case-studies or /en/case-studies), tsc --noEmit passes. If the dev server is running, curl both /case-studies/{a-real-slug} and /en/case-studies/{same-slug} and grep the HTML for both button labels in the correct language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload CMS content -> rendered page -> browser | `doc`/`author`/`client` are already-trusted CMS-authored content (not user input); this plan only changes what subset of that trusted data is composed into the rendered page and its JSON-LD, no new input surface is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-37-01 | Information Disclosure | JSON-LD `additionalProperty` (kpis) | accept | These kpi label/value pairs are already rendered visibly on the same page (KPI cards, results table) — the JSON-LD change surfaces no data that isn't already public on the page |
| T-37-02 | Tampering | `<script type="application/ld+json">` injection | accept | Uses the existing `JsonLd` component, which already escapes `<`/`>`/`&` before `dangerouslySetInnerHTML` (see `src/components/JsonLd.tsx` — unchanged by this plan) |
| T-37-SC | Tampering | npm/CLI installs during this phase | accept | No new packages installed — this plan only edits an existing page component using already-installed primitives (Button, Link, JsonLd) |
</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` exits 0
2. `grep -c "AuthorByline" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"` returns 0
3. `grep -c "additionalProperty" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"` returns 1
4. `grep -c "ctaHeading" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"` returns 1
5. If dev server is up: curl a real case-study slug in both locales, confirm single AuthorCard render, CTA buttons with correct-language copy, and populated JSON-LD additionalProperty
</verification>

<success_criteria>
- Author section renders `AuthorCard` exactly once, no `AuthorByline`, matching the Author page's established pattern
- `creativeWorkData` composes `description`/`author`/`dateCreated`/`dateModified`/`creator`/`additionalProperty` from real per-doc fields, conditionally omitting fields the doc lacks
- A closing CTA section (heading + primary/secondary buttons, both locale-aware) exists after `conclusion` and before `author`
- `tsc --noEmit` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/37-case-studies-content-audit-fix/37-01-SUMMARY.md` when done
</output>
