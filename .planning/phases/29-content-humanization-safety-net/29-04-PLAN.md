---
phase: 29-content-humanization-safety-net
plan: 04
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - src/collections/CaseStudies/index.ts
  - src/migrations/*.ts (generated, only if migration needed)
  - src/migrations/*.json (generated, only if migration needed)
  - src/payload-types.ts (generated, only after apply if migration needed)
  - .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md (final decision update)
autonomous: false
requirements: [VOICE-03]
user_setup: []

must_haves:
  truths:
    - "CaseStudies.services[].service is locale-safely resolved: either migrated to localized:true with correct backfill (same approval protocol as VOICE-02), or documented as an intentional non-localized decision — never left ambiguous"
  artifacts:
    - path: ".planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md"
      provides: "Final resolution recorded for CaseStudies.services[].service, replacing the pending-investigation placeholder"
      contains: "services"
  key_links:
    - from: ".planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md"
      to: "src/collections/CaseStudies/index.ts"
      via: "decision verdict from Plan 29-02's live-data investigation determines this plan's action"
      pattern: "MIGRATION REQUIRED\\|NO MIGRATION NEEDED"
---

<objective>
Resolve `CaseStudies.services[].service`'s localization status per Plan 29-02's evidence-based verdict: if the live data shows editorial content needing translation, migrate it with the same backfill-then-drop protocol as `TestimonialsCarousel.title`, under Juan's named approval; if the data is proper-noun-like (mirroring `Websites.stack[].tag`), close it out as a documented no-op.

Purpose: VOICE-03's success criterion explicitly allows either outcome ("fix de schema si aplica ... o decisión documentada de no tocarlo") — this plan executes whichever path Plan 29-02's real-data investigation concluded, without re-litigating the investigation.
Output: Either an applied, Juan-approved migration + updated collection config, or a documented "no change needed" decision — plus the final update to `29-FIELD-AUDIT.md` closing out this item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-content-humanization-safety-net/29-CONTEXT.md
@.planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md
@CLAUDE.md
</context>

<interfaces>
From src/collections/CaseStudies/index.ts (current `services` field, lines ~36-39):
```typescript
{
  name: 'services',
  type: 'array',
  fields: [{ name: 'service', type: 'text', required: true }],
},
```

If a migration is required, replicate the exact backfill-then-drop pattern from `src/migrations/20260712_202954_phase19_calltoaction_localized.ts` (see 29-03-PLAN.md's `<interfaces>` block for the full reference file content — same pattern applies here, table names will be `case_studies_services`/`_case_studies_v_services` or whatever `payload migrate:create` actually generates for this array-field table, column is `service` (text) instead of `rich_text` (jsonb)).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Read Plan 29-02's verdict and either apply the schema fix or document no-op</name>
  <files>src/collections/CaseStudies/index.ts, src/migrations/*.ts (generated, only if needed)</files>
  <read_first>
    .planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md (Plan 29-02's evidence-based verdict — authoritative for this task, do not re-investigate)
    src/collections/CaseStudies/index.ts (current services field)
    src/migrations/20260712_202954_phase19_calltoaction_localized.ts (backfill pattern, only needed if verdict is MIGRATION REQUIRED)
    CLAUDE.md — "Database Safety" section
  </read_first>
  <action>
    Read `29-CASESTUDIES-SERVICES-DECISION.md`'s verdict.

    **If verdict is "NO MIGRATION NEEDED":** No code change to `src/collections/CaseStudies/index.ts` is required. Skip directly to Task 2 (documentation update) — do not fabricate a migration or checkpoint that isn't needed.

    **If verdict is "MIGRATION REQUIRED":** Add `localized: true` to the `service` field in `src/collections/CaseStudies/index.ts`. Run `npx payload migrate:create`, read the generated migration in full, and manually insert the backfill `INSERT ... SELECT ... FROM <table>, unnest(ARRAY['es', 'en']) AS locale WHERE "service" IS NOT NULL` statements for both the live `services` array table and its `_v` shadow table, placed BEFORE the `DROP COLUMN "service"` statements — mirroring the Phase 19 CallToAction reference pattern exactly (see 29-03-PLAN.md's interfaces block for the full reference). Do NOT apply this migration in this task — proceed to Task 2's blocking checkpoint first.
  </action>
  <verify>
    <automated>cat .planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md | grep -q "NO MIGRATION NEEDED" && echo "no-op path confirmed" || (ls src/migrations/*.ts | tail -1 | xargs grep -c "unnest(ARRAY\['es', 'en'\])" | grep -qv '^0$')</automated>
  </verify>
  <done>If NO MIGRATION NEEDED: no schema change made, proceeding to documentation only. If MIGRATION REQUIRED: services field is localized:true, a migration file exists with the backfill INSERT before DROP COLUMN, not yet applied.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [BLOCKING] Present migration (or no-op reasoning) to Juan for named approval</name>
  <action>If Task 1 generated a migration, paste its full content (up and down functions) into the conversation for Juan to read, then ask for named approval before running `payload migrate` in Task 3. If Task 1 concluded no migration is needed, present the real service values found and the reasoning for Juan's confirmation instead.</action>
  <decision>If Task 1 concluded MIGRATION REQUIRED: apply the CaseStudies.services[].service localization migration against the real production Neon Postgres database. If Task 1 concluded NO MIGRATION NEEDED: confirm the documented no-op decision with Juan before closing out VOICE-03 (lighter-weight confirmation, not a schema-risk approval).</decision>
  <context>
    Per CLAUDE.md's Database Safety section, any migration touching an EXISTING column with real data requires Juan's named approval before applying — this is not an additive write. If Task 1 generated a migration, the executor must paste its full content (up/down) here for Juan to read before asking approval, same protocol as Plan 29-03's Task 2. If Task 1 concluded no migration is needed, present the real service values found (from 29-CASESTUDIES-SERVICES-DECISION.md) and the "proper-noun, non-localized is correct" reasoning for Juan's confirmation, since this closes out a phase success criterion.
  </context>
  <options>
    <option id="approve-migration">
      <name>Approve and apply the migration (if one was generated)</name>
      <pros>Closes VOICE-03 with a locale-safe schema; case study service tags become correctly translatable</pros>
      <cons>Irreversible without a further migration or point-in-time restore once applied</cons>
    </option>
    <option id="confirm-no-op">
      <name>Confirm the no-migration-needed decision (if no migration was generated)</name>
      <pros>Closes VOICE-03 with documented reasoning, zero schema risk</pros>
      <cons>None — this path only applies when the investigation found proper-noun-like values</cons>
    </option>
    <option id="reject">
      <name>Reject — revise the migration or the reasoning</name>
      <pros>Stops if either the migration's backfill logic or the no-op reasoning looks wrong on inspection</pros>
      <cons>VOICE-03 stays open pending revision</cons>
    </option>
  </options>
  <resume-signal>Juan debe responder aprobando por nombre (ej. "Juan aprueba") antes de correr `payload migrate`, o confirmar por nombre la decisión de no-migración si ese fue el camino. Cualquier otra respuesta detiene la tarea sin aplicar nada.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply (if approved) and close out the field audit</name>
  <files>src/payload-types.ts, .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md</files>
  <read_first>
    (none beyond Task 2's approval outcome)
  </read_first>
  <action>
    If Task 2 approved a migration: run `npx payload migrate` (or `CI=true npx payload migrate` if non-interactive) against the real Neon Postgres DB, then `npx payload generate:types` to regenerate `src/payload-types.ts` reflecting the localized `service` field.

    If Task 2 confirmed a no-op decision, or Task 1 already concluded no migration was needed: skip the migrate/generate:types steps entirely.

    In either case, update `.planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md`'s "Action Needed" section: replace the `CaseStudies.services[].service` "Pending investigation (see Plan 29-02)" placeholder with the final resolved status (either "RESOLVED — migrated to localized:true, backfilled, applied &lt;date&gt;" or "RESOLVED — documented as intentional non-localized decision, see 29-CASESTUDIES-SERVICES-DECISION.md").
  </action>
  <verify>
    <automated>grep -c "Pending investigation" .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md | grep -q '^0$'</automated>
  </verify>
  <done>Whichever path was taken, the field audit no longer contains a pending-investigation placeholder for CaseStudies.services[].service — it shows the final resolved status. If a migration was approved, it has been applied and payload-types.ts regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Migration file (if generated) -> production Neon Postgres | Same high-risk boundary as Plan 29-03 — applies only if Task 1 concludes a migration is required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-06 | Tampering | CaseStudies.services migration applied without backfill (if one is needed) | mitigate | Same protocol as T-29-04: manual backfill INSERT verification before DROP COLUMN, Juan's named approval required before apply |
| T-29-07 | Tampering | Field audit closed out with an unresolved/ambiguous status | mitigate | Task 3 explicitly requires replacing the "Pending investigation" placeholder with a concrete resolved status before the plan is considered done |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `29-CASESTUDIES-SERVICES-DECISION.md`'s verdict was read and acted on (migration path or no-op path, not both, not neither)
2. If migration path: Juan's named approval was recorded before `payload migrate` ran
3. `29-FIELD-AUDIT.md` no longer contains a "Pending investigation" placeholder for this field
</verification>

<success_criteria>
- VOICE-03: CaseStudies.services[].service is locale-safely resolved — either migrated with correct backfill under Juan's named approval, or documented as an intentional non-localized decision — with the field audit updated to reflect the final state
</success_criteria>

<output>
Create `.planning/phases/29-content-humanization-safety-net/29-04-SUMMARY.md` when done
</output>
