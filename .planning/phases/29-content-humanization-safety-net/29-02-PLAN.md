---
phase: 29-content-humanization-safety-net
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/content-humanization-snapshot.ts
  - .planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md
autonomous: true
requirements: [VOICE-03, VOICE-04]

must_haves:
  truths:
    - "A full-text (not metadata-only) content snapshot script exists and can dump every public text field's real value across the DB, diffable, as prep for Phase 31's post-sweep diff"
    - "The question of whether CaseStudies.services[].service needs the same backfill-migration treatment as TestimonialsCarousel.title is resolved with evidence from real live data, not assumption"
  artifacts:
    - path: "scripts/content-humanization-snapshot.ts"
      provides: "Full-text content snapshot across every collection, locale: 'all', for diffable rollback beyond Neon point-in-time restore"
      contains: "getPayload"
    - path: ".planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md"
      provides: "Evidence-based decision on CaseStudies.services[].service localization, read from real live data"
      contains: "services"
  key_links:
    - from: "scripts/content-humanization-snapshot.ts"
      to: "src/payload.config.ts"
      via: "getPayload({ config })"
      pattern: "getPayload"
---

<objective>
Build the full-text content snapshot tool (VOICE-04) and resolve, with real live data, whether `CaseStudies.services[].service` needs the same locale-migration treatment as `TestimonialsCarousel.title` (VOICE-03's investigation half).

Purpose: Both tasks are read-only against the live DB (a `payload.find()` query and a snapshot dump) — no schema or content changes — so both run fully autonomous. The services decision here feeds Plan 29-04, which applies whatever schema fix (if any) this investigation concludes is needed, under Juan's named approval if a migration is required.
Output: `scripts/content-humanization-snapshot.ts` and `29-CASESTUDIES-SERVICES-DECISION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-content-humanization-safety-net/29-CONTEXT.md
@.planning/phases/29-content-humanization-safety-net/29-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
From scripts/content-freeze-snapshot.ts (full file, 87 lines) — the CLI/output scaffolding to copy and extend:
```typescript
import { getPayload } from 'payload'
import config from '../src/payload.config'

const COLLECTIONS = ['pages', 'posts', 'authors', 'case-studies', 'categories', 'testimonials', 'clientes', 'media', 'redirects'] as const

function getTagArg(): string { /* --tag <name> CLI arg parsing, exits 1 with usage message if missing */ }

async function main() {
  const tag = getTagArg()
  const payload = await getPayload({ config })
  const takenAt = new Date().toISOString()
  for (const collection of COLLECTIONS) {
    const { docs } = await payload.find({ collection, limit: 0, locale: 'all', depth: 0 })
    // existing: normalized = docs.map(d => ({ id, slug, updatedAt })) -- METADATA ONLY, must be replaced with full doc capture
  }
  // writes JSON to .planning/phases/06-deploy-cutover/freeze-snapshots/<tag>-<ISO>.json
}
```

From scripts/verify-content-freeze.ts (full file) — the diff companion, expects `{ tag, takenAt, collections: Record<string, { count, docs: Array<{id, slug, updatedAt}>}> }` JSON shape; the new script's output should remain compatible with this shape (same top-level keys) but with `docs` entries carrying full field values instead of just `{id, slug, updatedAt}`, so this or a future extended diff script can still walk it by `id`.

From src/collections/CaseStudies/index.ts (services field, lines ~36-39):
```typescript
{
  name: 'services',
  type: 'array',
  fields: [{ name: 'service', type: 'text', required: true }],
},
```
Not localized today. Compare against `src/collections/Websites/index.ts`'s `stack[].tag` (same shape, already reviewed/approved non-localized in Phase 38 CONTEXT.md as tech-stack proper nouns).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the content snapshot script to capture full real text</name>
  <files>scripts/content-humanization-snapshot.ts</files>
  <read_first>
    scripts/content-freeze-snapshot.ts (full file — copy CLI/output skeleton)
    scripts/verify-content-freeze.ts (full file — keep the JSON shape close enough that field-level diffing can reuse or lightly extend this)
    .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md (from Plan 29-01, for the full list of public-facing text fields/collections/globals to include — if Plan 29-01 has not yet written this file when this task runs, fall back to the collection/field list embedded in 29-PATTERNS.md's raw findings directly, which is equivalent source data)
  </read_first>
  <action>
    Create `scripts/content-humanization-snapshot.ts` by copying the CLI-arg (`--tag`), `getPayload({ config })`, and file-output skeleton from `content-freeze-snapshot.ts` verbatim, then:

    1. Expand `COLLECTIONS` to include every collection identified in the field audit: `pages`, `posts`, `authors`, `case-studies`, `categories`, `testimonials`, `clientes`, `speaking-events`, `websites`. Drop `redirects` and `media` (not editorial copy — URLs/binary assets, not text content Phase 30/31 will humanize). Also snapshot the 3 globals with editorial text (`footer`, `header`, `llms`) via `payload.findGlobal({ slug, locale: 'all' })` in a second loop, since globals are not queried via `payload.find`.
    2. Replace the `normalized` mapper: instead of `{ id, slug, updatedAt }`, keep the full document object as returned by `payload.find({ collection, locale: 'all', depth: 0 })` (so both locale variants are captured per document), but still retain top-level `id` and `updatedAt` at minimum for the diff tool's existing id-matching logic to keep working.
    3. Keep the same output convention pattern but write to a phase-scoped directory: `.planning/phases/29-content-humanization-safety-net/content-snapshots/<tag>-<ISO>.json`.
    4. Keep the top-level output JSON shape `{ tag, takenAt, collections: Record<string, {count, docs}> }` plus a new `globals: Record<string, object>` key for the 3 globals snapshot.
    5. Update the header doc-comment to describe this as the full-text humanization snapshot tool (VOICE-04), distinct in purpose from `content-freeze-snapshot.ts` (which stays metadata-only for its own deploy-cutover use case — do not modify or delete that file).
  </action>
  <verify>
    <automated>test -f scripts/content-humanization-snapshot.ts && grep -c "getPayload" scripts/content-humanization-snapshot.ts | grep -qv '^0$' && grep -c "findGlobal" scripts/content-humanization-snapshot.ts | grep -qv '^0$' && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>scripts/content-humanization-snapshot.ts exists, type-checks cleanly, captures full document content (not just id/slug/updatedAt) across every editorial collection plus the 3 editorial globals, with locale:'all', writing to the phase-scoped output directory.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate CaseStudies.services[].service live data and document the decision</name>
  <files>.planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md</files>
  <read_first>
    src/collections/CaseStudies/index.ts (services field definition, lines ~36-39)
    src/collections/Websites/index.ts (stack[].tag field, the comparison analog already approved as non-localized in Phase 38 CONTEXT.md)
    src/migrations/20260712_202954_phase19_calltoaction_localized.ts (reference backfill pattern, needed only if this investigation concludes a migration is required)
  </read_first>
  <action>
    Query the real, live `services[].service` values across every existing `CaseStudies` document using Payload's Local API (`getPayload({ config })` + `payload.find({ collection: 'case-studies', locale: 'all', depth: 0 })`, run as a one-off `tsx` script or inline via the existing MCP tooling if available — do not guess or assume values).

    Evaluate the actual strings found:
    - If they are short descriptive service names that plausibly need translation per locale (e.g. "SEO Técnico", "Desarrollo Web", "Auditoría Técnica") — these are editorial content, not proper nouns, and should be treated the same as `TestimonialsCarousel.title`: conclude "MIGRATION REQUIRED" and hand off to Plan 29-04 with the exact field name, current values found, and confirmation this needs the backfill-then-drop pattern.
    - If they are brand/proper-noun-like terms that shouldn't translate (mirroring `Websites.stack[].tag`'s reasoning) — conclude "NO MIGRATION NEEDED" and document why, with the same reasoning precedent cited from Phase 38 CONTEXT.md.

    Write the decision to `29-CASESTUDIES-SERVICES-DECISION.md`: the real values found (verbatim), the reasoning applied, the final verdict (MIGRATION REQUIRED / NO MIGRATION NEEDED), and — if migration required — the exact current field config to change (`localized: true` added to the `service` field) for Plan 29-04 to act on directly without re-investigating.

    Also update `.planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md` (from Plan 29-01) if it already exists at execution time: replace the "Pending investigation (see Plan 29-02)" placeholder in the `CaseStudies.services[].service` row with this task's verdict. If Plan 29-01 has not yet run/written that file (parallel wave), skip this update — Plan 29-04 will read this decision doc directly regardless.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md && grep -c "MIGRATION REQUIRED\|NO MIGRATION NEEDED" .planning/phases/29-content-humanization-safety-net/29-CASESTUDIES-SERVICES-DECISION.md | grep -qv '^0$'</automated>
  </verify>
  <done>29-CASESTUDIES-SERVICES-DECISION.md exists with real live-data evidence (verbatim service values found), explicit reasoning, and a clear verdict (MIGRATION REQUIRED or NO MIGRATION NEEDED) that Plan 29-04 can act on directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Snapshot/investigation scripts -> production Neon Postgres | Both tasks run read-only `payload.find`/`findGlobal` queries against the real production DB (per CLAUDE.md, there is no separate dev/staging DB) — no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-02 | Information Disclosure | Snapshot script output committed to `.planning/` | accept | Content is already public-facing editorial copy (site content), not secrets/PII; output stays in the repo's existing `.planning/` convention, matching `content-freeze-snapshot.ts`'s precedent |
| T-29-03 | Tampering | Investigation task could write to the DB by mistake | mitigate | Task explicitly scoped to `payload.find` (read-only) only — no `payload.update`/`payload.create` calls in this plan |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan — reuses existing `payload`/`tsx` already in the project |
</threat_model>

<verification>
1. `scripts/content-humanization-snapshot.ts` exists, type-checks, and can be run with `--tag <name>` to produce a full-text JSON snapshot
2. `29-CASESTUDIES-SERVICES-DECISION.md` exists with a clear MIGRATION REQUIRED / NO MIGRATION NEEDED verdict backed by real queried values
3. Neither task performed any write operation against the production DB
</verification>

<success_criteria>
- VOICE-04: a full-text, diffable content snapshot script exists as prep for Phase 31's post-sweep diff (beyond Neon point-in-time restore as the only rollback option)
- VOICE-03 (investigation half): `CaseStudies.services[].service` localization need is resolved with real evidence, not assumption, feeding directly into Plan 29-04's resolution task
</success_criteria>

<output>
Create `.planning/phases/29-content-humanization-safety-net/29-02-SUMMARY.md` when done
</output>
