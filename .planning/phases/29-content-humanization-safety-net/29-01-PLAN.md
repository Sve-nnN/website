---
phase: 29-content-humanization-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md
  - .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md
autonomous: true
requirements: [VOICE-01, VOICE-05]

must_haves:
  truths:
    - "A field audit document exists classifying every public text field across every collection/global/block as localized/non-localized, before any real content is rewritten"
    - "The audit explicitly surfaces Llms.llmsTxt/llmsFull as an open decision point for Juan rather than silently leaving them non-localized"
    - "A written voice profile document exists (neutral Spanish, no voceo, professional-direct, first-person with direct credential claims, collaborative CTA) ready as a brief for the humanizer skill"
  artifacts:
    - path: ".planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md"
      provides: "Full field-by-field localization audit across collections/globals/blocks"
      contains: "TestimonialsCarousel"
    - path: ".planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md"
      provides: "Voice profile brief for the humanizer skill (Phases 30/31)"
      contains: "sin voceo"
  key_links:
    - from: ".planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md"
      to: "src/blocks/TestimonialsCarousel/config.ts"
      via: "audit row flagging TestimonialsCarousel.title as non-localized"
      pattern: "TestimonialsCarousel"
---

<objective>
Produce the two pure-documentation deliverables of Phase 29: (1) the pre-flight field-localization audit covering every public text field in every collection, global, and block, and (2) the voice profile brief that Phases 30/31 will hand to the `humanizer` skill.

Purpose: Per 29-CONTEXT.md, no real content gets rewritten in this milestone track until the full safety net (audit + snapshot + schema fixes + voice profile) exists and is verified — these two documents are half of that net, and they are read-only/doc-only, so they run fully autonomous without waiting on any migration approval gate.
Output: `29-FIELD-AUDIT.md` (VOICE-01) and `29-VOICE-PROFILE.md` (VOICE-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-content-humanization-safety-net/29-CONTEXT.md
@.planning/phases/29-content-humanization-safety-net/29-PATTERNS.md
@research/JUAN-PROFILE.md
@.planning/research/SUMMARY-v1.6.md
</context>

<interfaces>
<!-- The raw grep sweep across ~45 files was already performed during phase planning (see 29-PATTERNS.md "Complete raw findings" section).
     Do NOT re-grep the codebase from scratch — reuse those findings verbatim as the input data for Task 1. Only spot-check with a
     targeted grep if a specific field's localized status is ambiguous in the PATTERNS.md findings. -->

From 29-PATTERNS.md, the complete raw findings (collections, globals, blocks) and the recommended table format are already extracted. Task 1 formats this into the final audit doc — it does not need to re-derive it.

From research/JUAN-PROFILE.md (full file) — bio text, expertise list, Arianna Lupi relationship, positioning — the source material for the voice profile.

From .planning/research/SUMMARY-v1.6.md — the one-sentence voice-profile brief already agreed: "Voice profile grounded in named-competitor analysis: first-person like Arianna Lupi (not third-person like Aleyda Solis), direct-quantified credential claims, collaborative-imperative CTAs ('Hablemos', 'Trabajemos juntos'), neutral Spanish (no voceo)".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the pre-flight field-localization audit</name>
  <files>.planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md</files>
  <read_first>
    .planning/phases/29-content-humanization-safety-net/29-PATTERNS.md (section "1. Pre-flight field audit doc" — contains the complete raw findings and recommended table format, use directly)
    .planning/milestones/v1.7-MILESTONE-AUDIT.md (structural analog for table-based audit doc format)
  </read_first>
  <action>
    Create `29-FIELD-AUDIT.md` transcribing the PATTERNS.md raw findings into one complete table with columns `Collection/Global/Block | Field path | Type | localized? | Public-facing? | Notes`. Cover every collection (Authors, CaseStudies, Categories, Clientes, Media, Pages, Posts, SpeakingEvents, Testimonials, Users, Websites), every global (Footer, Header, Llms, FeaturedContent), and every block registered on Pages (AboutSection, ArchiveBlock, CallToAction, ClientLogosBlock, Code, ContactFormBlock, Content, FAQ, FeaturedCaseStudiesBlock, FeaturedPostsBlock, FeaturedWebsitesBlock, Hero, LocalProofSection, MediaBlock, RelatedCaseStudyBlock, RelatedPosts, ResultsSection, Section, ServiceScopeCard, ServicesShowcase, TableOfContentsBlock, TestimonialsCarousel, TestimonialSection).

    Add a dedicated "Action Needed" section below the full table listing exactly the 5 flagged items from PATTERNS.md, each with its own subsection:
    1. `TestimonialsCarousel.title` — non-localized, migration required, cross-reference to VOICE-02 / Plan 29-03.
    2. `CaseStudies.services[].service` — non-localized, needs live-data investigation to decide if a migration is required; cross-reference to VOICE-03 / Plan 29-02 (investigation) and Plan 29-04 (resolution). Note that this document will be updated with the final decision once Plan 29-02's investigation completes — write "Pending investigation (see Plan 29-02)" as a placeholder value in this row, not a guess.
    3. `Llms.llmsTxt` / `Llms.llmsFull` — new finding this session, not previously named in ROADMAP.md's 3-repeat bug list. Present this explicitly as an open decision point for Juan: llms.txt/llms-full.txt are AI-crawler-facing files that conventionally exist in a single canonical form (English is the common web convention), so leaving them non-localized may be correct as-is — but flag it rather than deciding silently. Write the two options (localize like normal copy, or document as an intentional single-locale exception) and mark this row "AWAITING JUAN'S CALL" rather than resolving it unilaterally.
    4. `SpeakingEvents.location` — minor, likely a proper noun (city name), low risk — document as "likely correct as-is, confirm during Phase 30/31 execution" rather than treating as blocking.
    5. `Websites.stack[].tag` — same shape/reasoning as `CaseStudies.services[].service`, but per Phase 38's CONTEXT.md this was already reviewed and approved as intentionally non-localized (tech-stack names are proper nouns). Document as "resolved — see Phase 38 CONTEXT.md" for completeness, no further action needed.

    Add a header block (`# Phase 29: Field-Localization Audit`, `**Audited:** 2026-07-14`, `**Status:**`) matching the living-doc convention used in `research/JUAN-PROFILE.md` and `v1.7-MILESTONE-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md && grep -c "TestimonialsCarousel" .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md | grep -qv '^0$' && grep -c "llmsTxt" .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md | grep -qv '^0$'</automated>
  </verify>
  <done>29-FIELD-AUDIT.md exists, contains a full table covering every collection/global/block field from PATTERNS.md's raw findings, and an "Action Needed" section with all 5 flagged items including the AWAITING JUAN'S CALL marker on Llms.llmsTxt/llmsFull.</done>
</task>

<task type="auto">
  <name>Task 2: Write the voice profile document</name>
  <files>.planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md</files>
  <read_first>
    research/JUAN-PROFILE.md (full file — bio text, expertise, Arianna Lupi relationship, positioning)
    .planning/research/SUMMARY-v1.6.md (voice-profile brief sentence, line ~42)
    ~/.claude/skills/humanizer/SKILL.md (if present — confirm the input format the humanizer skill expects, so this brief is directly consumable)
  </read_first>
  <action>
    Create `29-VOICE-PROFILE.md` as the humanizer-skill input brief for Phases 30/31 — do NOT invent new positioning, reuse and expand the already-agreed research verbatim per 29-CONTEXT.md's "Claude's Discretion" note.

    Structure (mirrors research/JUAN-PROFILE.md's living-doc section style):
    - `## Reglas de tono`: español neutro sin voceo (nunca "vos"/"tenés"), primera persona ("hago", "ayudo", "he trabajado con"), credenciales directas y cuantificadas (ej. "más de cuatro años", "18+ clientes"), CTA colaborativo ("Hablemos", "Trabajemos juntos") en vez de imperativo unilateral ("Contrátame ya").
    - `## Referencia positiva (voz ya correcta)`: cite the Authors bio verbatim from research/JUAN-PROFILE.md as the exemplar, and extract 3-4 concrete stylistic rules from it (sentence structure, credential-framing pattern, verb choice).
    - `## Contraste con competidores`: Arianna Lupi (primera persona — adoptar este ángulo) vs Aleyda Solis (tercera persona / tono más corporativo — NO adoptar), per SUMMARY-v1.6.md's named-competitor analysis.
    - `## Aplicación por tipo de contenido`: brief guidance per content type this brief will apply to in Phases 30/31 (globals/nav copy, core pages, services/geo pages, blog posts, case studies) — cross-reference ROADMAP.md's Phase 30/31 scope split (risk-ascending order: globals+core pages+services first, posts+case studies last).
    - `## No hacer`: explicit anti-patterns — no voceo, no tercera persona, no CTA agresivo/imperativo unilateral, no inventar credenciales no verificadas en research/JUAN-PROFILE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md && grep -c "sin voceo" .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md | grep -qv '^0$' && grep -c "Arianna Lupi" .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md | grep -qv '^0$'</automated>
  </verify>
  <done>29-VOICE-PROFILE.md exists with the tone rules, positive-reference bio citation, competitor contrast, per-content-type application guidance, and an explicit "no hacer" section — ready as a direct input brief for the humanizer skill in Phases 30/31.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Planning docs -> future execution phases | These are read-only documents (no code, no schema, no data writes) that inform Phases 29 (rest of this phase) and 30/31 (content rewrite) |

## STRIDE Threat Register

This plan produces zero code or schema changes — pure documentation derived from already-vetted research and an already-completed grep sweep. No new attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-01 | Tampering | Field audit doc could silently omit a non-localized field, repeating the 3x-bug pattern | mitigate | Task 1 transcribes the full PATTERNS.md sweep (all collections/globals/blocks) rather than a partial list, and explicitly flags the one net-new finding (Llms fields) instead of omitting it |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. `.planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md` exists and covers every collection/global/block named in 29-PATTERNS.md
2. `.planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md` exists with tone rules, positive reference, competitor contrast, and a "no hacer" section
3. Neither file modifies any source code or schema
</verification>

<success_criteria>
- VOICE-01: a complete pre-flight field-localization audit exists, covering every public text field, with the Llms fields explicitly flagged as an open decision rather than silently resolved
- VOICE-05: a voice profile document exists, ready as a direct brief for the humanizer skill, sourced from already-vetted research (no invented positioning)
</success_criteria>

<output>
Create `.planning/phases/29-content-humanization-safety-net/29-01-SUMMARY.md` when done
</output>
