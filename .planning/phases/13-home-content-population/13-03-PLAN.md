---
phase: 13-home-content-population
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - scripts/seed-phase13-home-content.ts
autonomous: true
gap_closure: true
requirements: [ABOUT-02]

must_haves:
  truths:
    - "Home (ES) renders eyebrow 'Estrategia y datos. Más allá del código' and title 'Mi enfoque en Consultoría Técnica' on the AboutSection block (replacing the Phase 10.7 'Sobre mí' / 'Ingeniería de software con mentalidad SEO' placeholder)"
    - "Home (EN) renders the professional English translation of that same eyebrow/title/description on the AboutSection block, not the old 'About Me' / 'Software engineering with an SEO mindset' bio copy"
    - "The AboutSection block's single description paragraph (ES+EN) matches the locked copy from 13-CONTEXT.md's <specifics> section, replacing the old two-paragraph bio text"
    - "The features grid (4 items) and CTA, already correct per 13-02, are unaffected — still render below the new header copy"
    - "Re-running scripts/seed-phase13-home-content.ts a second time is a no-op (idempotent), does not create duplicate blocks or duplicate paragraph/feature rows"
  artifacts:
    - path: "scripts/seed-phase13-home-content.ts"
      provides: "aboutHeaderCopy constant (ES+EN eyebrow/title/description) applied to the existing aboutSection block update, alongside the already-correct features/ctaText/ctaLink"
      contains: "aboutHeaderCopy"
  key_links:
    - from: "scripts/seed-phase13-home-content.ts aboutHeaderCopy[locale]"
      to: "Home Pages doc content.layout[aboutIndex].eyebrow/title/paragraphs"
      via: "payload.update({ collection: 'pages', id: homeDoc.id, locale, data: { content: { layout } } })"
      pattern: "aboutHeaderCopy"
---

<objective>
Close the one gap from 13-VERIFICATION.md (status: gaps_found, truth #2 failed): Home's `aboutSection` block still shows the Phase 10.7 placeholder eyebrow/title/bio ("Sobre mí" / "Ingeniería de software con mentalidad SEO" / the old 2-paragraph bio) instead of the copy locked in 13-CONTEXT.md's `<specifics>` section for this phase's "Mi enfoque en Consultoría Técnica" section: eyebrow "Estrategia y datos. Más allá del código", title "Mi enfoque en Consultoría Técnica", and a single description paragraph ("No veo el SEO y el desarrollo web como disciplinas aisladas..."). `scripts/seed-phase13-home-content.ts` deliberately left eyebrow/title/paragraphs untouched when it added `features[]`/CTA in 13-02 — this plan updates that same script to also set eyebrow/title/paragraphs to the locked copy (ES+EN, EN newly translated here), re-runs it, and verifies the live page.

Purpose: makes ABOUT-02 (ROADMAP Phase 13 Success Criterion #2) fully satisfied — the section is meant to read, end to end, as this phase's "Mi enfoque en Consultoría Técnica" block (eyebrow+title+description+features+CTA together), not a mix of Phase 10.7's unrelated bio intro plus Phase 13's features/CTA.

Output: updated seed script with the locked header copy, re-run against the dev DB, live Home page (ES+EN) reflecting it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-home-content-population/13-CONTEXT.md
@.planning/phases/13-home-content-population/13-VERIFICATION.md

@scripts/seed-phase13-home-content.ts

<interfaces>
<!-- Existing seed-phase13-home-content.ts structure relevant to this edit (already in the file, do not restructure): -->

`featuresCopy: Record<Locale, {icon,title,description}[]>` and `ctaCopy: Record<Locale, string>` are
existing module-level consts — follow the exact same `Record<Locale, ...>` shape for the new
`aboutHeaderCopy` const.

The per-locale loop already does (abbreviated):
- `const aboutIndex = layout.findIndex((b) => b.blockType === 'aboutSection')`
- if found: builds `features` (with `featureIds` reuse), reads/patches `paragraphs` (old
  Phase-10.7-leftover-Spanish-on-EN fix), and assigns
  `layout[aboutIndex] = { ...layout[aboutIndex], paragraphs, features, ctaText: ctaCopy[locale], ctaLink: '#contact' }`.

This plan replaces the `paragraphs`-preservation logic (and removes the now-obsolete
`ABOUT_PARAGRAPH_1_BROKEN_EN_TEXT`/`ABOUT_PARAGRAPH_1_FIXED_EN_TEXT` consts and their `if` branch,
since paragraphs are being fully replaced with fresh locked copy — that old Phase-10.7-bio-leftover
fix no longer applies once the bio paragraphs themselves are replaced) with a brand-new single-item
`paragraphs` array built from `aboutHeaderCopy[locale].description`, and adds `eyebrow`/`title`
overrides to the `layout[aboutIndex] = {...}` assignment.

The existing `featureIds` id-reuse pattern (captured after the first locale's write via refetch,
reused on the second locale's write so Payload doesn't orphan the first locale's array rows — see
the `if (!faqBlockId || !contactBlockId || !featureIds || !faqItemIds) { ... }` refetch block near
the end of the per-locale loop) must be extended with an equivalent `paragraphIds` capture/reuse,
since `paragraphs` is the same kind of nested array field as `features` and is subject to the same
orphaning risk (documented in this file's own "Rule 1 fix" comment for `features[]`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add locked eyebrow/title/description copy to the seed script and apply with id-safe paragraph reuse</name>
  <files>scripts/seed-phase13-home-content.ts</files>
  <action>
Edit `scripts/seed-phase13-home-content.ts`:

1. Remove the `ABOUT_PARAGRAPH_1_BROKEN_EN_TEXT` / `ABOUT_PARAGRAPH_1_FIXED_EN_TEXT` consts and the comment block above them (the "Rule 1 fix" for Phase 10.7's leftover-Spanish-on-EN paragraph) — this becomes dead code once paragraphs are fully replaced by this task's new copy, since the old bio text they patched will no longer be present at all.

2. Add a new module-level const, same pattern/position as `featuresCopy`/`ctaCopy`, typed `Record<Locale, { eyebrow: string; title: string; description: string }>` named `aboutHeaderCopy`, with:
   - `es.eyebrow`: "Estrategia y datos. Más allá del código"
   - `es.title`: "Mi enfoque en Consultoría Técnica"
   - `es.description`: "No veo el SEO y el desarrollo web como disciplinas aisladas. Los motores de búsqueda modernos evalúan la limpieza del código, la velocidad de carga y la arquitectura de la información. Mi metodología se basa en auditar y construir soluciones donde la infraestructura técnica se convierte en el motor principal para el crecimiento orgánico, asegurando que tu web no solo funcione perfectamente, sino que domine en los resultados de búsqueda." (verbatim from 13-CONTEXT.md's `<specifics>` section)
   - `en.eyebrow`: "Data and strategy. Beyond the code"
   - `en.title`: "My Approach to Technical Consulting"
   - `en.description`: a professional English translation of the ES description above, matching the tone already used for `featuresCopy.en` in this same file (natural marketing English, not literal word-for-word) — e.g. "I don't see SEO and web development as separate disciplines. Modern search engines evaluate code quality, load speed, and information architecture. My approach is built on auditing and building solutions where technical infrastructure becomes the primary driver of organic growth — making sure your site not only runs flawlessly, but dominates the search results too."

   Store `eyebrow` in normal case, not literal caps — matches the existing `eyebrow: 'Sobre mí'`/`'About Me'` pattern already established in `scripts/seed-phase10-7-gap-fill.ts`; the block's own CSS applies `uppercase` visually (`src/blocks/AboutSection/Component.tsx`'s `text-label uppercase tracking-wide` class on the eyebrow `<p>`), so do not hardcode all-caps in the seed data.

3. Declare a new `let paragraphIds: (string | undefined)[] | undefined` alongside the existing `let featureIds: (string | undefined)[] | undefined` declaration near the top of `main()`.

4. Inside the per-locale loop's `aboutIndex !== -1` branch, replace the existing `paragraphs`-preservation logic with a fresh single-item array built from `aboutHeaderCopy[locale].description`, reusing `paragraphIds?.[0]` as the row `id` when present (same reuse pattern already used for `features`). Update the `layout[aboutIndex] = { ... }` assignment to also set `eyebrow: aboutHeaderCopy[locale].eyebrow` and `title: aboutHeaderCopy[locale].title` alongside the existing `paragraphs, features, ctaText: ctaCopy[locale], ctaLink: '#contact'`.

5. In the refetch block (the `if (!faqBlockId || !contactBlockId || !featureIds || !faqItemIds) { ... }` section), add `paragraphIds` to the guard condition and capture logic, mirroring exactly how `featureIds` is captured from `refetchedAbout?.features?.map((f) => f.id)` — add `refetchedAbout?.paragraphs?.map((p) => p.id)` assigned to `paragraphIds` when `!paragraphIds`.

6. Run the script against the dev DB: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase13-home-content.ts`.

7. Verify live output against the running dev server: confirm ES page contains "Estrategia y datos" and "Mi enfoque en Consultoría Técnica", EN page contains "Data and strategy" and "My Approach to Technical Consulting", and the old placeholder string "Ingeniería de software con mentalidad SEO" no longer appears on the ES page.

8. Re-run the script a second time and confirm the console output shows no new-block-creation side effects (idempotent) and the live page still shows exactly one occurrence of the new title per locale (no duplication).
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/seed-phase13-home-content.ts; curl -s http://localhost:3000/ | grep -c "Mi enfoque en Consultoria Tecnica\|Mi enfoque en Consultoría Técnica"; curl -s http://localhost:3000/en | grep -c "My Approach to Technical Consulting"; curl -s http://localhost:3000/ | grep -c "Ingenieria de software con mentalidad SEO\|Ingeniería de software con mentalidad SEO"</automated>
  </verify>
  <done>scripts/seed-phase13-home-content.ts sets aboutSection eyebrow/title/paragraphs to the locked 13-CONTEXT.md copy (ES+EN) alongside the already-correct features/CTA; re-running the script is idempotent; live Home (both locales) shows the new eyebrow/title/description and no longer shows the Phase 10.7 placeholder copy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script -> Postgres | Local API write, run manually against dev DB by a trusted operator (same as every prior phase's seed script) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | scripts/seed-phase13-home-content.ts array-field id reuse (paragraphs/features) | mitigate | paragraphIds capture/reuse follows the exact same pattern already validated for featureIds/faqItemIds in this file, preventing accidental data loss (orphaned rows) on the second locale write |
</threat_model>

<verification>
1. Run `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase13-home-content.ts` against the dev DB.
2. `curl -s http://localhost:3000/` — confirm it contains "Estrategia y datos" and "Mi enfoque en Consultoría Técnica", and does NOT contain "Ingeniería de software con mentalidad SEO".
3. `curl -s http://localhost:3000/en` — confirm it contains "Data and strategy" and "My Approach to Technical Consulting", and does NOT contain "Software engineering with an SEO mindset".
4. Confirm the features grid (4 items) and CTA button still render correctly below the new header copy (unaffected by this change).
5. Re-run the script a second time — confirm no duplicate blocks/rows and identical live output.
</verification>

<success_criteria>
- Home's AboutSection (ES+EN) shows the locked "Mi enfoque en Consultoría Técnica" eyebrow/title/description from 13-CONTEXT.md, closing the ABOUT-02 gap from 13-VERIFICATION.md.
- Seed script remains idempotent after the change.
- No regression to the features grid or CTA delivered in 13-02.
</success_criteria>

<output>
Create `.planning/phases/13-home-content-population/13-03-SUMMARY.md` when done
</output>
