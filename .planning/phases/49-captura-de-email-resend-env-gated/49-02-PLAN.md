---
phase: 49-captura-de-email-resend-env-gated
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/blocks/EmailCaptureBlock/config.ts
  - src/blocks/EmailCaptureBlock/Component.tsx
  - src/collections/Posts/index.ts
  - src/components/richTextBlockConverters.tsx
  - src/components/RichTextRenderer.tsx
  - src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx
  - messages/es.json
  - messages/en.json
  - scripts/seed-phase49-email-capture-post.ts
  - scripts/verify-phase49-email-capture.ts
autonomous: true
requirements: [MAIL-01]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un visitante ve un bloque de captura de email inline dentro de un post real, nunca popup ni modal (MAIL-01)"
    - "El formulario funciona con cero JavaScript de cliente nuevo — mismo patrón <form action={fn}> que ContactFormBlockComponent"
    - "Después de enviar el formulario, el visitante vuelve al MISMO post y ve un estado (pending/already/error) en vez del formulario vacío otra vez"
    - "El componente del bloque nunca importa RichTextRenderer/AffiliateDisclosure/FAQComponent (guardia TDZ, mismo ciclo que ya rompió producción una vez)"
  artifacts:
    - src/blocks/EmailCaptureBlock/config.ts
    - src/blocks/EmailCaptureBlock/Component.tsx
    - messages/es.json (namespace emailCapture)
    - messages/en.json (namespace emailCapture)
  key_links:
    - "Posts.content BlocksFeature → richTextBlockConverters['email-capture'] → EmailCaptureCard → subscribe-lead-magnet.ts (Plan 49-01)"
    - "blog/[category]/[slug]/page.tsx searchParams.subscribed → buildRichTextConverters({subscribedState, postPath}) → EmailCaptureCard"
---

<objective>
Hacer descubrible, dentro de un post real y publicado, el mecanismo de doble opt-in + descarga
firmada que 49-01 ya probó de punta a punta pero sin UI. Construir `EmailCaptureBlock` (bloque
Lexical, cero JS de cliente, mismo patrón `<form action={fn}>` que `ContactFormBlockComponent` ya
prueba en producción), registrarlo en `posts.content` junto a `affiliate-inline`, y resolver el
wrinkle arquitectónico real que 49-RESEARCH.md marca explícitamente: `richTextConverters` es hoy un
objeto estático a nivel de módulo, y este bloque necesita leer `searchParams.subscribed` de la
página del post — que requiere convertir esa función en una factory por-request (Pattern 1 de
49-RESEARCH.md), sin romper los otros 8 call sites de `RichTextRenderer`.

Purpose: 49-01 probó que la tubería funciona; este plan la conecta al artículo real donde vive el
tráfico orgánico (Phase 45 baseline) — sin esto, MAIL-01 no está satisfecho, solo el backend lo está.

Output: `EmailCaptureBlock` registrado y estilado exactamente per 49-UI-SPEC.md, `buildRichTextConverters`
factory nueva, `RichTextRenderer` con prop `converters` opcional, la página de post real leyendo
`searchParams` y pasando `postPath`/`subscribedState`, copy bilingüe en `messages/{es,en}.json`, y al
menos un post real publicado con el bloque insertado y verificado en un navegador real vía Playwright.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-captura-de-email-resend-env-gated/49-CONTEXT.md
@.planning/phases/49-captura-de-email-resend-env-gated/49-UI-SPEC.md
@.planning/phases/49-captura-de-email-resend-env-gated/49-RESEARCH.md
@.planning/phases/49-captura-de-email-resend-env-gated/49-01-SUMMARY.md

# Patrones probados a replicar exactamente — leer antes de escribir el componente
@src/blocks/AffiliateInlineBlock/config.ts
@src/components/AffiliateInlineCard.tsx
@src/components/richTextBlockConverters.tsx
@src/components/RichTextRenderer.tsx
@src/blocks/ContactFormBlock/Component.tsx
@src/collections/Posts/index.ts
@src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx
@src/blocks/NewsletterBlock/Component.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EmailCaptureBlock + factory de converters por-request + wiring en la página real del post</name>
  <files>src/blocks/EmailCaptureBlock/config.ts, src/blocks/EmailCaptureBlock/Component.tsx, src/collections/Posts/index.ts, src/components/richTextBlockConverters.tsx, src/components/RichTextRenderer.tsx, src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx, messages/es.json, messages/en.json</files>
  <action>
    Crear `src/blocks/EmailCaptureBlock/config.ts`: bloque Lexical zero-config (`slug: 'email-capture'`,
    `interfaceName: 'EmailCaptureBlock'`, `fields: []`) — per 49-UI-SPEC.md Component Contract Detail,
    el lead magnet es un concepto fijo y singular para esta fase, no hay nada que el editor deba
    elegir por instancia.

    Crear `src/blocks/EmailCaptureBlock/Component.tsx` (export `EmailCaptureCard`, Server Component,
    props `{ postPath: string; subscribedState?: string }`): SOLO importa la Server Action
    (`subscribe-lead-magnet.ts`), `Input`/`Button` de shadcn, `getLocale`/`getTranslations` de
    `next-intl/server` (mismo patrón que `AffiliateInlineCard` para no necesitar prop-drilling de
    locale), y un `<a>` plano — NUNCA `RichTextRenderer`, `AffiliateDisclosure`, ni `FAQComponent`
    (constraint dura T-48-09 ya documentada en el docblock de `richTextBlockConverters.tsx`: cualquiera
    de esos reabre el ciclo de import circular que ya causó un `ReferenceError` de TDZ en producción).
    Anatomía exacta de 49-UI-SPEC.md Component Contract Detail: `div.not-prose.my-8.rounded-lg.
    border.border-border.bg-card.shadow-sm.p-6` con eyebrow/heading/description arriba; si
    `subscribedState` NO coincide con `pending|already|error`, renderiza el formulario (label
    `sr-only`, `Input` `type=email required autoComplete=email h-11 flex-1`, `Button h-11 shrink-0`,
    inputs ocultos `postPath` y `locale`, honeypot `company_website` oculto vía CSS igual que
    `ContactFormBlockComponent`, microcopy de privacidad con link inline a `/privacy` — `/en/privacy`
    si `locale==='en'`); si `subscribedState` coincide, reemplaza el formulario completo por el bloque
    de estado (heading + body, `text-destructive` solo en el caso `error`) — copy EXACTA (ambos
    idiomas) de la tabla Copywriting Contract de 49-UI-SPEC.md.

    Agregar el namespace `emailCapture` a `messages/es.json` y `messages/en.json` (mismo nivel que
    `stackPage`), con las claves: `eyebrow`, `heading`, `description`, `emailLabel`,
    `emailPlaceholder`, `submitLabel`, `privacyPrefix`/`privacyLinkText`/`privacySuffix` (el texto de
    privacidad partido alrededor del link real a `/privacy`, para no romper el string traducido con
    HTML embebido), `pendingHeading`, `pendingBody`, `alreadyHeading`, `alreadyBody`, `errorBody` —
    texto verbatim de la tabla Copywriting Contract de 49-UI-SPEC.md en ambos idiomas.

    Registrar `EmailCaptureBlock` como segunda entrada del MISMO array `BlocksFeature` que
    `src/collections/Posts/index.ts` ya usa para `AffiliateInlineBlock` sobre `content` —
    `BlocksFeature({ blocks: [AffiliateInlineBlock, EmailCaptureBlock] })`, cero migración nueva (jsonb
    ya existente).

    En `src/components/richTextBlockConverters.tsx`: agregar `buildRichTextConverters(ctx: {
    subscribedState?: string; postPath?: string } = {})` — función que envuelve el `richTextConverters`
    estático existente y sobreescribe únicamente `blocks['email-capture']` para renderizar
    `<EmailCaptureCard postPath={ctx.postPath ?? ''} subscribedState={ctx.subscribedState} />`. El
    export estático `richTextConverters` y sus otros converters (`code-block`/`faq`/`affiliate-inline`/
    `table`/`heading`) quedan exactamente iguales — siguen funcionando standalone para los otros 8 call
    sites de `RichTextRenderer`. Documentar en el mismo docblock la limitación ya identificada por
    49-RESEARCH.md: el converter `faq` recursa con el `richTextConverters` estático (no la salida de la
    factory), así que un `email-capture` anidado dentro de una respuesta de FAQ nunca vería
    `subscribedState`/`postPath` — simplificación deliberada, mismo precedente que Phase 48 ya sentó
    para el caso de múltiples instancias por post, no un bug a corregir acá.

    En `src/components/RichTextRenderer.tsx`: agregar una prop opcional `converters?:
    JSXConvertersFunction`, usada en vez de la constante estática cuando está presente (`converters={
    converters ?? richTextConverters}` en el `<RichText>` interno) — los otros 8 call sites que no
    pasan la prop siguen exactamente igual.

    En `src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx`: agregar `searchParams:
    Promise<{ subscribed?: string }>` a la firma de `PostPage`, hacer `await` junto a `params`.
    Calcular `postPath` reusando el MISMO `blogPostPath(primaryCategorySlug, doc.slug ?? slug)` +
    prefijo de locale que la página ya calcula unas líneas arriba para `articleUrl` (no duplicar esa
    lógica, extraerla a una variable local si hace falta reusarla en ambos lugares). Construir `const
    converters = buildRichTextConverters({ subscribedState: searchParamsResolved.subscribed, postPath
    })` UNA sola vez, y pasar `converters={converters}` a AMBAS instancias de `<RichTextRenderer
    data={body.before}.../>` y `<RichTextRenderer data={body.after}.../>` (el bloque puede caer en
    cualquiera de las dos mitades que `splitContentForOffer` produce).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npx payload generate:types; grep -q "EmailCaptureBlock" src/payload-types.ts; grep -q "\"emailCapture\"" messages/es.json; grep -q "\"emailCapture\"" messages/en.json; ! grep -l "RichTextRenderer\|AffiliateDisclosure\|FAQComponent" src/blocks/EmailCaptureBlock/Component.tsx; ! grep -q "'use client'\|\"use client\"" src/blocks/EmailCaptureBlock/Component.tsx; ! grep -qE "onChange=|onInput=|useState\(|useActionState\(|useFormStatus\(" src/blocks/EmailCaptureBlock/Component.tsx; grep -q "<form action=" src/blocks/EmailCaptureBlock/Component.tsx; npx tsc --noEmit; npm run build > /tmp/build-49-02a.log 2>&1; grep -qi "Cannot access .* before initialization" /tmp/build-49-02a.log && (echo "TDZ_REGRESSION_FOUND"; exit 1) || true; echo BUILD_CLEAN; echo BLOCK_WIRED_OK; echo ZERO_CLIENT_JS_OK</automated>
    <fails_when>`EmailCaptureBlock` no aparece en `payload-types.ts`, o falta el namespace `emailCapture` en cualquiera de los 2 archivos de mensajes, o `Component.tsx` importa (directa o indirectamente) `RichTextRenderer`/`AffiliateDisclosure`/`FAQComponent`, o `Component.tsx` contiene la directiva `'use client'`, o contiene `onChange=`/`onInput=`/`useState(`/`useActionState(`/`useFormStatus(` (cualquiera de estos indica JS de cliente, violando MAIL-01), o no contiene `<form action=` (el patrón de Server Action nativo), o `npm run build` falla o loguea un `ReferenceError` de TDZ, o tsc reporta errores.</fails_when>
  </verify>
  <done>`EmailCaptureBlock` existe, tipado, registrado en `Posts.content` junto a `affiliate-inline`; la página del post construye y pasa el factory de converters con `postPath`/`subscribedState`; `npm run build` termina limpio sin regresión de TDZ.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insertar el bloque en un post real publicado + verificación con navegador real (Playwright)</name>
  <files>scripts/seed-phase49-email-capture-post.ts, scripts/verify-phase49-email-capture.ts</files>
  <action>
    Crear `scripts/seed-phase49-email-capture-post.ts`: elige, vía Local API, UN post real ya
    `_status: published` (con tráfico orgánico real razonable — cualquiera de los posts ya usados por
    scripts de verificación de fases anteriores sirve, o el primero que la query devuelva) e inserta un
    nodo de bloque Lexical `email-capture` dentro de su `content` (escritura en AMBOS locales, es y en,
    respetando la disciplina de reusar ids de nodos/bloques existentes en cada escritura por locale —
    documentada en el propio historial de incidentes del repo — para no pisar contenido del otro
    locale). Insertar como un nodo adicional, sin regenerar ids de nodos existentes.

    Crear `scripts/verify-phase49-email-capture.ts` usando Playwright (ya devDependency): abre el post
    real (es y en) contra el dev server real, ubica el formulario del bloque por su heading/eyebrow
    visible y el nombre accesible del campo de email, completa un email de prueba desechable
    (distinto al usado en 49-01, para no chocar con el estado "already" de esos tests), envía el
    formulario, espera la redirección, y confirma que la MISMA página ahora muestra el copy del
    estado `pending` (o `already` si por alguna razón esa dirección ya había pasado por el flujo) en
    vez del formulario vacío — probando que el factory de converters + `searchParams` funciona de
    verdad en un navegador real, no solo en revisión de código.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase49-email-capture-post.ts; npm run dev > /tmp/dev-49-02b.log 2>&1 & echo $! > /tmp/dev-49-02b.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-phase49-email-capture.ts | tee /tmp/verify-49-02b.log; grep -q "^PASS$" /tmp/verify-49-02b.log; kill "$(cat /tmp/dev-49-02b.pid)" 2>/dev/null || true; npx tsc --noEmit; echo DISCOVERABLE_OK</automated>
    <fails_when>El script no encuentra el formulario en el post real, o después de enviarlo la página no muestra un estado swap real (pending/already), o `PASS` no aparece en la salida del verificador, o tsc reporta errores.</fails_when>
  </verify>
  <done>El bloque aparece en un post real publicado (es+en) y, en una sesión de navegador real vía Playwright, enviar el formulario deja al visitante en el estado correcto en la MISMA página — MAIL-01 satisfecho de forma discoverable, no solo mecánicamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `searchParams.subscribed` (query param del visitante) → render del post | Entrada no confiable que decide QUÉ copy fija mostrar, nunca contenido interpolado |
| Contenido del bloque dentro del árbol de Lexical converters | Mismo árbol de conversión que ya renderiza rich text migrado; el bloque nuevo no debe reabrir el ciclo de import TDZ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-08 | Tampering / Reflected content | `searchParams.subscribed` renderizado en la página del post | low | mitigate | El valor crudo del query param NUNCA se interpola en JSX/HTML — se compara con `===` contra un allowlist fijo (`pending`\|`already`\|`error`) para elegir cuál de 3 strings hardcodeados mostrar, mismo criterio que `NewsletterBlockComponent` ya aplica a `newsletterState`. |
| T-49-09 | Denial of Service | Segundo call site público de la Server Action (ahora reachable desde cualquier post con el bloque) | low | mitigate | Hereda el mismo rate limit por IP de `subscribe-lead-magnet.ts` (Plan 49-01) — un segundo punto de entrada visual no agrega un segundo mecanismo de límite. |
| T-49-04 (re-afirmado) | Tampering | Link de descarga en el estado "already"/confirm page renderizado desde este post | medium | mitigate | Sin cambio respecto a Plan 49-01 — el token sigue verificado con HMAC + `timingSafeEqual` antes de resolver cualquier URL firmada. |
</threat_model>

<verification>
`npm run build` limpio (gate de TDZ), y una sesión de Playwright real confirmando que el visitante ve
el bloque, lo completa, y recibe el estado correcto sin recargar manualmente ni JavaScript de cliente
nuevo cargado.
</verification>

<success_criteria>
- `EmailCaptureBlock` visible y funcional en al menos un post real, ambos locales.
- Cero JavaScript de cliente nuevo — el bloque es 100% Server Component + Server Action.
- El estado post-envío se ve en la MISMA página del post, no en una redirección genérica a `/blog`.
- `richTextConverters` estático y sus 8 call sites existentes sin cambio de comportamiento.
</success_criteria>

<output>
Create `.planning/phases/49-captura-de-email-resend-env-gated/49-02-SUMMARY.md` when done
</output>
