---
phase: 49-captura-de-email-resend-env-gated
plan: 03
type: execute
wave: 3
depends_on: ["49-02"]
files_modified:
  - scripts/verify-phase49-lighthouse.ts
  - scripts/verify-phase49-phase-close.ts
autonomous: true
requirements: [MAIL-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Leer searchParams en la página de post más visitada del sitio no degrada Lighthouse/CWV más allá del bar de Phase 50 (GATE-01), o el riesgo queda explícitamente escalado a Juan/checker en vez de ignorado"
    - "MAIL-01 a MAIL-05 quedan verificados con evidencia automatizada contra el sistema completo, tal como quedó al final de 49-02 — no solo contra piezas aisladas"
    - "subscribers y lead-magnets siguen fuera de SITEMAP_COLLECTIONS y del mapa de mcpPlugin después de que 2 plans tocaron archivos adyacentes"
  artifacts:
    - scripts/verify-phase49-lighthouse.ts
    - scripts/verify-phase49-phase-close.ts
  key_links:
    - "Resultado de Lighthouse de este plan → gate de cierre de Phase 50 (GATE-01), comparado contra el baseline de Phase 45"
---

<objective>
Cerrar la fase con la única verificación empírica que 49-RESEARCH.md marca explícitamente como "no
asumir, medir": leer `searchParams` en `blog/[category]/[slug]/page.tsx` (Wave 2) convierte esa ruta
—la de mayor tráfico orgánico real del sitio, per el baseline de Phase 45— en render dinámico
per-request en vez de ISR. `getCachedPost()` sigue sirviendo la lectura de Postgres desde
`unstable_cache` sin importar la clasificación de la ruta, así que el costo real es tiempo de render,
no carga de base de datos — pero eso hay que medirlo, no darlo por sentado, porque Phase 50 (GATE-01)
va a comparar contra el baseline de Phase 45 sin margen para sorpresas.

Purpose: es la última pieza de riesgo real que queda abierta en la fase. Todo lo demás (mecanismo,
UI, contenido) ya está probado en 49-01/49-02.

Output: comparación de Lighthouse antes/después sobre la misma ruta, con una decisión explícita (pasa
tal cual, o se escala a Juan/checker si no pasa) — y una pasada final de verificación de los 5
requirements de la fase contra el sistema ya completo y descubrible.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-captura-de-email-resend-env-gated/49-RESEARCH.md
@.planning/phases/49-captura-de-email-resend-env-gated/49-01-SUMMARY.md
@.planning/phases/49-captura-de-email-resend-env-gated/49-02-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lighthouse antes/después sobre la ruta de post con el bloque, contra el baseline de Phase 45</name>
  <files>scripts/verify-phase49-lighthouse.ts</files>
  <reversibility rating="costly">Leer `searchParams` en la página de post con más tráfico real del sitio saca esa ruta de ISR hacia render dinámico per-request. El código en sí es trivialmente reversible (borrar la lectura del param), pero el impacto de dejarlo sin medir en la ruta de mayor tráfico no lo es — por eso este task es el gate empírico, no una asunción.</reversibility>
  <action>
    Localizar cómo Phase 45 capturó y guardó su baseline de Lighthouse/CWV (buscar en
    `.planning/phases/45-*/` los artefactos/scripts que produjeron esos números — mismos posts o rutas
    comparables) y usar la MISMA metodología (no inventar una nueva herramienta ni un nuevo criterio de
    comparación). Escribir `scripts/verify-phase49-lighthouse.ts`: corre Lighthouse (o el mecanismo que
    Phase 45 haya usado) contra el post real de 49-02 en dos variantes — (a) URL plana, sin
    `?subscribed=`, simulando una visita fresca (el costo de leer `searchParams` aplica igual, por la
    clasificación todo-o-nada de Next.js, exista o no el parámetro); (b) la misma URL con
    `?subscribed=pending` agregado, para confirmar que no hay un costo ADICIONAL más allá de (a).
    Comparar contra los números que Phase 45 registró para una ruta de post comparable, aplicando
    exactamente el bar de GATE-01 (ROADMAP.md): sin caída de más de 5 puntos de performance, sin cruce
    de banda de CWV, delta de CLS 0.00.

    Si PASA: el task queda hecho tal cual, sin cambios de código adicionales.

    Si NO PASA: NO aplicar en silencio el fallback que 49-RESEARCH.md sugiere (redirigir a una
    superficie separada siempre-dinámica en vez de la misma URL del post) — eso es una desviación del
    diseño ya aprobado por el checker en 49-UI-SPEC.md. En vez de eso, detener la ejecución y devolver
    un `checkpoint:decision` presentando a Juan las dos opciones con sus tradeoffs: (1) aceptar la
    regresión medida como una excepción documentada y acotada a esta ruta, o (2) adoptar el fallback
    (romper el intercambio de estado inline y reabrir 49-UI-SPEC.md con el checker). No elegir por
    Juan.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npm run build > /tmp/build-49-03a.log 2>&1; npm run start > /tmp/prod-49-03a.log 2>&1 & echo $! > /tmp/prod-49-03a.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-phase49-lighthouse.ts | tee /tmp/verify-49-03a.log; grep -qE "^(LIGHTHOUSE_PASS|LIGHTHOUSE_ESCALATED)$" /tmp/verify-49-03a.log; kill "$(cat /tmp/prod-49-03a.pid)" 2>/dev/null || true; echo LIGHTHOUSE_CHECKED</automated>
    <fails_when>El script no imprime ni `LIGHTHOUSE_PASS` ni `LIGHTHOUSE_ESCALATED` (es decir, se limita a callar o a aplicar un fallback no aprobado sin avisar), o el build de producción falla.</fails_when>
  </verify>
  <done>Lighthouse antes/después comparado contra el baseline de Phase 45 en la misma ruta con el mismo método; o pasa el bar de GATE-01, o queda un `checkpoint:decision` explícito para Juan con las dos opciones — nunca una sustitución silenciosa.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verificación final de fase — MAIL-01..05 contra el sistema completo + cierre de accesos</name>
  <files>scripts/verify-phase49-phase-close.ts</files>
  <action>
    Escribir `scripts/verify-phase49-phase-close.ts` que re-corre, esta vez contra el sistema YA
    descubrible (bloque real en el post real de 49-02, PDFs reales de 49-01), la secuencia completa
    camino-feliz + camino-degradado ya probada por separado en 49-01/49-02 — la diferencia es que
    ahora todo el recorrido (formulario visible → Server Action → confirm route → confirm page →
    descarga firmada) se ejerce como una sola cadena sobre la superficie final, no en piezas aisladas.
    Reconfirmar además, con grep directo sobre el estado final de los archivos (no asumir que sigue
    igual desde 49-01): `subscribers`/`lead-magnets` ausentes de `SITEMAP_COLLECTIONS` en
    `src/lib/sitemap-data.ts` y del mapa de colecciones de `mcpPlugin` en `src/payload.config.ts`
    (T-49-07) — ambos archivos son candidatos a haber sido tocados por trabajo posterior a 49-01, así
    que esta es la última oportunidad de detectar una regresión antes de que Phase 50 audite el
    milestone completo. Reconfirmar también que la URL pública/sin firma de los 2 `public_id` de
    Cloudinary sigue devolviendo 401/403 (T-49-03).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npm run dev > /tmp/dev-49-03b.log 2>&1 & echo $! > /tmp/dev-49-03b.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-phase49-phase-close.ts | tee /tmp/verify-49-03b.log; grep -q "MAIL01_OK" /tmp/verify-49-03b.log; grep -q "MAIL02_OK" /tmp/verify-49-03b.log; grep -q "MAIL03_OK" /tmp/verify-49-03b.log; grep -q "MAIL04_OK" /tmp/verify-49-03b.log; grep -q "MAIL05_OK" /tmp/verify-49-03b.log; kill "$(cat /tmp/dev-49-03b.pid)" 2>/dev/null || true; ! grep -A3 "SITEMAP_COLLECTIONS" src/lib/sitemap-data.ts | grep -q "lead-magnets\|subscribers"; ! grep -A15 "mcpPlugin(" src/payload.config.ts | grep -q "'lead-magnets'\|'subscribers'"; npx tsc --noEmit; echo PHASE49_CLOSE_OK</automated>
    <fails_when>Falta cualquiera de los 5 sentinels MAIL0X_OK, o `subscribers`/`lead-magnets` aparecen en `SITEMAP_COLLECTIONS` o en el mapa de `mcpPlugin`, o cualquiera de los 2 `public_id` resuelve público sin firma, o tsc reporta errores.</fails_when>
  </verify>
  <done>Los 5 requirements de la fase (MAIL-01..05) verificados con evidencia automatizada contra el sistema completo y descubrible; `subscribers`/`lead-magnets` confirmados fuera de los mapas de plugin/sitemap; ambos PDFs siguen privados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Render dinámico per-request en la ruta de mayor tráfico | Superficie de mayor exposición a un DoS de recursos si el costo de render por request escala mal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-10 | Denial of Service | Render dinámico per-request en `blog/[category]/[slug]` (por leer `searchParams`) | medium | mitigate | `getCachedPost()` sigue sirviendo la lectura de Postgres desde `unstable_cache` sin importar la clasificación de la ruta — el costo agregado es tiempo de render en el servidor de Node persistente, no una query nueva por visita. El gate de Lighthouse de Task 1 es el control concreto que detecta una regresión real antes de que llegue a Phase 50. |
| T-49-03 (re-cierre) | Information Disclosure | PDFs en Cloudinary | high | mitigate | Reconfirmado en Task 2 contra el estado final de archivos — sin cambio de disposición respecto a Plan 49-01. |
| T-49-07 (re-cierre) | Elevation of Privilege | `subscribers`/`lead-magnets` fuera de mapas de plugin | medium | mitigate | Reconfirmado por grep directo en Task 2 contra `sitemap-data.ts`/`payload.config.ts`, no asumido heredado de 49-01. |
</threat_model>

<verification>
Build de producción real + Lighthouse contra el post con el bloque, comparado contra Phase 45; y una
corrida final de extremo a extremo de los 5 requirements sobre el sistema ya completo.
</verification>

<success_criteria>
- Lighthouse/CWV de la ruta de post con el bloque dentro del bar de GATE-01, o escalado explícitamente a Juan con las dos opciones documentadas.
- MAIL-01 a MAIL-05 verificados con evidencia automatizada contra el sistema final, no contra piezas aisladas.
- `subscribers`/`lead-magnets` confirmados, en el estado final de archivos, fuera de `SITEMAP_COLLECTIONS` y del mapa de `mcpPlugin`.
</success_criteria>

<output>
Create `.planning/phases/49-captura-de-email-resend-env-gated/49-03-SUMMARY.md` when done
</output>
