---
phase: 46-disclosure-legal-esquema-de-links-de-afiliado
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/es.json
  - messages/en.json
  - src/components/AffiliateDisclosure.tsx
  - src/app/api/_dev-preview/affiliate-disclosure/route.ts (creado en Task 1, borrado en Task 3)
  - scripts/verify-affiliate-disclosure.ts
  - scripts/update-privacy-resend.ts
autonomous: false
requirements: [LEG-01, LEG-02, LEG-03, LEG-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "AffiliateDisclosure renderiza la frase de Amazon verbatim en inglés (\"As an Amazon Associate I earn from qualifying purchases.\") y la frase en español aprobada por Juan, ambas condicionadas a hasAmazonLinks"
    - "messages/es.json y messages/en.json tienen el namespace affiliateDisclosure con generalDisclosure + amazonDisclosure en ambos idiomas, nunca contenido del CMS"
    - "/privacy y /en/privacy responden 200 y su HTML renderizado menciona Resend, la retención de datos y el proceso de baja, verificado con curl contra el sitio real"
    - "Las 6 secciones originales de /privacy siguen presentes tras la actualización (nada se pisó)"
    - "La restricción de cero tracking con consentimiento (sin GA4/píxeles/document.cookie/localStorage) queda documentada como comentario verificable por grep en el componente nuevo"
  artifacts:
    - messages/es.json (namespace affiliateDisclosure)
    - messages/en.json (namespace affiliateDisclosure)
    - src/components/AffiliateDisclosure.tsx
    - scripts/update-privacy-resend.ts
  key_links:
    - "AffiliateDisclosure -> getTranslations('affiliateDisclosure') -> messages/{es,en}.json, nunca payload.find() de contenido"
    - "scripts/update-privacy-resend.ts -> payload.update(pages, slug=privacy) -> revalidatePagesCache (hook ya existente) -> /privacy y /en/privacy sirven el HTML nuevo sin esperar el TTL de 60s"
---

<objective>
Construir la mitad legal/disclosure de la Fase 46: el componente `AffiliateDisclosure` con copy bilingüe en `messages/{es,en}.json` (nunca en el CMS), incluyendo la frase textual de Amazon verbatim en inglés y su traducción al español aprobada explícitamente por Juan; y la actualización de `/privacy` cubriendo el formulario de email, Resend como encargado del tratamiento, retención y proceso de baja. Cubre LEG-01 a LEG-04.

Purpose: la reactivación de la cuenta de Amazon (DEC-01) y la postulación a Kinsta exigen el disclosure ya publicado — por eso el trabajo legal se completa dentro de esta fase antes de que exista ninguna UI que lo consuma (Phase 48). La frase en español de Amazon no tiene versión oficial para una cuenta de EE.UU. (46-RESEARCH.md, Pitfall 2) — se traduce fielmente y Juan aprueba la redacción exacta antes de que se escriba en `messages/es.json`.

Output:
- `src/components/AffiliateDisclosure.tsx` (componente nuevo, servidor, sin JS de cliente)
- `messages/es.json` / `messages/en.json` extendidos con el namespace `affiliateDisclosure`
- `/privacy` y `/en/privacy` actualizadas con 2 secciones nuevas (email/Resend, retención/baja)
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-disclosure-legal-esquema-de-links-de-afiliado/46-CONTEXT.md
@.planning/phases/46-disclosure-legal-esquema-de-links-de-afiliado/46-RESEARCH.md
@CLAUDE.md
@src/blocks/ServiceScopeCard/Component.tsx
@messages/es.json
@messages/en.json
@scripts/humanize-legal-pages.ts
@src/app/actions/subscribe.tsx
@src/collections/Subscribers/index.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: AffiliateDisclosure de punta a punta — ruta EN probada con la frase verbatim</name>
  <files>
    messages/en.json,
    messages/es.json,
    src/components/AffiliateDisclosure.tsx,
    src/app/api/_dev-preview/affiliate-disclosure/route.ts,
    scripts/verify-affiliate-disclosure.ts
  </files>
  <read_first>
    - src/blocks/ServiceScopeCard/Component.tsx (completo — patrón `getTranslations` a replicar)
    - messages/es.json y messages/en.json (completos — forma actual de los namespaces)
    - 46-RESEARCH.md secciones "Summary" y "Pitfall 2" (frase EN verbatim confirmada contra el US Operating Agreement; por qué no existe frase ES oficial para esta cuenta)
    - 46-CONTEXT.md sección "Disclosure — componente único reusable"
    - src/app/api/redirects-lookup/route.ts (completo — forma de un Route Handler simple bajo src/app/api/)
  </read_first>
  <action>
    1. Agregar el namespace `"affiliateDisclosure"` a `messages/en.json`: `"generalDisclosure": "Some of the links on this page are affiliate links. If you buy through one, I may earn a small commission at no extra cost to you. I only recommend tools I've actually used."` y `"amazonDisclosure": "As an Amazon Associate I earn from qualifying purchases."` — esta última es la frase exacta de la Sección 5 del US Operating Agreement de Amazon (la cuenta `juantech02-20` es un contrato de EE.UU., no de la UE); no cambiar ni un carácter.

    2. Agregar el namespace `"affiliateDisclosure"` a `messages/es.json` con SOLO `"generalDisclosure": "Algunos enlaces de esta página son de afiliado. Si compras a través de alguno, puedo ganar una comisión pequeña sin costo adicional para ti. Solo recomiendo herramientas que uso de verdad."` — dejar la clave `amazonDisclosure` FUERA a propósito en este task; se agrega recién en Task 3, después de que Juan apruebe la redacción exacta en el checkpoint de Task 2.

    3. Crear `src/components/AffiliateDisclosure.tsx`: componente de servidor `async function AffiliateDisclosure({ hasAmazonLinks, locale }: { hasAmazonLinks: boolean; locale?: 'es' | 'en' })`. Llama `getTranslations(locale ? { locale, namespace: 'affiliateDisclosure' } : 'affiliateDisclosure')` (el parámetro `locale` es opcional: las páginas bajo `[locale]` en Phase 48 lo omiten y dejan que next-intl resuelva el segmento; solo hace falta explícito fuera de ese segmento, como en el route handler de este mismo task). Renderiza un único `<p>` con `{t('generalDisclosure')}` siempre, y `{t('amazonDisclosure')}` agregado solo si `hasAmazonLinks` es true. Comentario obligatorio al inicio: "CONSTRAINT (LEG-04): cero tracking que dispare consentimiento en la ruta de afiliado — sin GA4, sin píxeles, sin IDs de clic por usuario, sin document.cookie/localStorage aquí ni en ningún componente que renderice esto."

    4. Crear el Route Handler temporal `src/app/api/_dev-preview/affiliate-disclosure/route.ts` (bajo `/api`, ya excluido del matcher de middleware y de `robots.ts` `disallow: ['/admin', '/api']`) — su único propósito es probar `AffiliateDisclosure` de punta a punta con next-intl real ANTES de que exista una página real que lo monte (Phase 48). `GET` lee `locale` (`'es'|'en'`, default `'es'`) y `hasAmazonLinks` (`'true'|'false'`) de los query params, llama `AffiliateDisclosure({ locale, hasAmazonLinks: hasAmazonLinks === 'true' })` directamente (componente sin hooks, invocable como función) y devuelve `renderToStaticMarkup(...)` como `text/html`. Este archivo es throwaway: Task 3 lo borra una vez aprobada la frase en español.

    5. Escribir `scripts/verify-affiliate-disclosure.ts` como documentación del contrato de verificación (no se ejecuta como script standalone — `getTranslations` de next-intl necesita el runtime de Next para resolver la config, por eso la prueba real corre vía HTTP contra el dev server, no vía `tsx` suelto): dejar un comentario en el archivo explicando exactamente los 2 curls que el `<verify>` de este task ejecuta y por qué (documenta la limitación para quien lea el repo después, per 46-RESEARCH.md Open Question 1).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npm run dev > /tmp/dev-46-02.log 2>&1 & echo $! > /tmp/dev-46-02.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; curl -s "http://localhost:3000/api/_dev-preview/affiliate-disclosure?locale=en&hasAmazonLinks=true" | grep -qF "As an Amazon Associate I earn from qualifying purchases." || { echo "FALTA la frase EN verbatim"; exit 1; }; curl -s "http://localhost:3000/api/_dev-preview/affiliate-disclosure?locale=es&hasAmazonLinks=false" | grep -qF "afiliado" || { echo "FALTA el disclosure general en ES"; exit 1; }; echo TRACER_OK</automated>
    <fails_when>El dev server no responde en 30s; la respuesta EN con hasAmazonLinks=true no contiene la frase verbatim de Amazon; la respuesta ES general no contiene la palabra "afiliado".</fails_when>
  </verify>
  <acceptance_criteria>
    - `messages/en.json` tiene `affiliateDisclosure.amazonDisclosure` con el texto exacto de la Sección 5 del US Operating Agreement
    - `messages/es.json` tiene `affiliateDisclosure.generalDisclosure` pero NO tiene todavía `amazonDisclosure`
    - El route handler temporal responde 200 y renderiza el componente real (no un mock)
  </acceptance_criteria>
  <done>AffiliateDisclosure renderiza correctamente la ruta EN completa (general + Amazon verbatim) y la ruta ES general, probado de punta a punta contra el dev server real. La frase de Amazon en español queda pendiente del checkpoint de Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirmar la redacción en español de la frase de Amazon Associates</name>
  <what-built>
    El servidor de desarrollo sigue corriendo en http://localhost:3000. La ruta temporal
    http://localhost:3000/api/_dev-preview/affiliate-disclosure?locale=en&hasAmazonLinks=true
    ya confirmó la frase obligatoria en inglés: "As an Amazon Associate I earn from
    qualifying purchases." (Sección 5 del US Operating Agreement de Amazon — la cuenta
    juantech02-20 es un contrato de EE.UU., no de la UE, así que esa es la única frase
    exigida contractualmente y no se toca). Falta aprobar la traducción al español antes
    de escribirla en messages/es.json.
  </what-built>
  <how-to-verify>
    No existe una traducción oficial en español para el contrato de EE.UU. — la frase en
    español que usa afiliados.amazon.es pertenece a un programa distinto (el de la UE), que
    esta cuenta no integra, así que usarla presentaría el texto de otro contrato como si
    fuera el propio (46-RESEARCH.md, Pitfall 2). Dos opciones de traducción fiel del texto
    en inglés:

    Opción A (literal): "Como Afiliado de Amazon, obtengo ingresos por las compras que
    califican."

    Opción B (más cercana a la voz del sitio): "Como Afiliado de Amazon, gano una
    comisión por las compras que califiquen."

    Responder con "A", "B", o la redacción exacta que prefieras usar en su lugar.
  </how-to-verify>
  <resume-signal>Responder "A", "B", o tu propia redacción del texto en español</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Escribir la frase ES aprobada + verificación final + limpieza del preview temporal</name>
  <files>messages/es.json, src/app/api/_dev-preview/affiliate-disclosure/route.ts</files>
  <read_first>
    - La resolución de Task 2 (la opción o redacción exacta que Juan eligió)
    - messages/es.json (estado actual tras Task 1)
  </read_first>
  <action>
    1. Agregar la clave `amazonDisclosure` al namespace `affiliateDisclosure` de `messages/es.json` usando EXACTAMENTE la redacción que Juan aprobó/entregó en Task 2 — sin parafrasear.
    2. Con el dev server todavía corriendo (Task 1), volver a curlear la ruta temporal con `locale=es&hasAmazonLinks=true` y confirmar que el HTML contiene la frase aprobada verbatim.
    3. Borrar `src/app/api/_dev-preview/affiliate-disclosure/route.ts` — cumplió su único propósito (probar el tracer de punta a punta antes de que exista una página real en Phase 48) y no debe quedar como superficie expuesta, aunque `/api` ya esté fuera del `robots.ts`.
    4. Detener el dev server (`kill $(cat /tmp/dev-46-02.pid)`).
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node -e "const m=require('./messages/es.json');if(!m.affiliateDisclosure||!m.affiliateDisclosure.amazonDisclosure){console.error('falta amazonDisclosure en es.json');process.exit(1)}"; curl -s "http://localhost:3000/api/_dev-preview/affiliate-disclosure?locale=es&hasAmazonLinks=true" | grep -qF "Amazon" || { echo "la ruta ES no renderiza la frase de Amazon"; exit 1; }; test ! -f src/app/api/_dev-preview/affiliate-disclosure/route.ts; npx tsc --noEmit; kill "$(cat /tmp/dev-46-02.pid)" 2>/dev/null || true; echo DISCLOSURE_OK</automated>
    <fails_when>messages/es.json no tiene la clave amazonDisclosure; la ruta ES no renderiza el texto aprobado; el archivo del preview temporal sigue existiendo; o tsc falla tras borrarlo (import colgante).</fails_when>
  </verify>
  <acceptance_criteria>
    - `messages/es.json` tiene `affiliateDisclosure.amazonDisclosure` con el texto exacto que Juan aprobó
    - El route handler temporal ya no existe en el árbol
    - `npx tsc --noEmit` pasa sin el archivo borrado
  </acceptance_criteria>
  <reversibility rating="reversible">Copy en messages/*.json, editable en cualquier momento sin tocar schema ni código de render.</reversibility>
  <done>Ambos locales de AffiliateDisclosure renderizan su frase de Amazon (EN mandatoria verbatim, ES aprobada por Juan); el preview temporal fue removido; el build sigue limpio.</done>
</task>

<task type="auto">
  <name>Task 4: /privacy — Resend, retención y proceso de baja (LEG-03)</name>
  <files>scripts/update-privacy-resend.ts</files>
  <read_first>
    - scripts/humanize-legal-pages.ts (completo — helpers `heading`/`paragraph`/`richTextDoc`, las 6 `privacySections` actuales verbatim, disciplina de reuso de `blockId`/`columnId` entre locales)
    - src/app/actions/subscribe.tsx (completo — confirma el flujo real de doble opt-in y qué hace Resend en él)
    - src/collections/Subscribers/index.ts (completo — campos `status`/`token`/`confirmedAt`/`unsubscribedAt`, para describir el flujo con precisión)
    - CLAUDE.md sección "Database Safety"
  </read_first>
  <action>
    1. Confirmar la base: `node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts` (Dokploy, la única que existe).

    2. Crear `scripts/update-privacy-resend.ts` modelado directamente en `scripts/humanize-legal-pages.ts`: reusar sus helpers `heading`/`paragraph`/`richTextDoc` y reescribir el array `privacySections` (es/en) incluyendo VERBATIM las 6 secciones existentes (`humanize-legal-pages.ts` líneas 64-117) — Payload reemplaza el árbol completo de `richText` en cada `update()`, así que omitir las 6 secciones actuales las borraría de producción — y AGREGAR 2 secciones nuevas al final de cada locale:
       - ES: "7. Formulario de Alta al Correo" (qué se recopila — email e idioma —, para qué se usa — avisos de contenido nuevo del blog —, y que Resend actúa como encargado del tratamiento para la entrega de esos correos) y "8. Retención y Baja" (retención mientras la suscripción esté activa, coherente con los 12 meses ya declarados en la sección 4 para las demás retenciones, y el proceso de baja vía el enlace de cada correo o escribiendo desde la página de contacto).
       - EN: secciones espejo "7. Newsletter Sign-up Form" y "8. Retention and Opt-Out", mismo contenido en inglés.
       Reusar la misma disciplina de `blockId`/`columnId` entre locales que ya usa `humanize-legal-pages.ts` (líneas 186-223).

    3. Correr el script contra la base real: `node --env-file=.env node_modules/.bin/tsx scripts/update-privacy-resend.ts` — es una escritura de contenido aditiva vía Local API (no una migración de schema), permitida sin pausa de confirmación per CLAUDE.md.
  </action>
  <verify>
    <automated>set -e; for path in "https://juan-tech.com/privacy" "https://juan-tech.com/en/privacy"; do html=$(curl -s "$path"); echo "$html" | grep -qi "resend" || { echo "FALTA mención de Resend en $path"; exit 1; }; echo "$html" | grep -qiE "retenci[oó]n|retention" || { echo "FALTA retención en $path"; exit 1; }; echo "$html" | grep -qiE "baja|opt-out|unsubscribe" || { echo "FALTA proceso de baja en $path"; exit 1; }; done; curl -s "https://juan-tech.com/privacy" | grep -qF "1. Información que Recopilamos" || { echo "Se perdió una sección original de ES"; exit 1; }; curl -s "https://juan-tech.com/en/privacy" | grep -qF "1. Information We Collect" || { echo "Se perdió una sección original de EN"; exit 1; }; echo PRIVACY_OK</automated>
    <fails_when>Cualquiera de los 2 locales no menciona Resend, retención o proceso de baja; o cualquiera de las 6 secciones originales desapareció del HTML renderizado (regresión de contenido).</fails_when>
  </verify>
  <acceptance_criteria>
    - `/privacy` y `/en/privacy` responden 200 y mencionan Resend, retención y proceso de baja
    - Las 6 secciones originales (ES e EN) siguen presentes verbatim
    - El script reusa `blockId`/`columnId` entre locales (sin ids nuevos por escritura)
  </acceptance_criteria>
  <precondition>DATABASE_URI en .env resuelve al Postgres real de Dokploy (scripts/db/04-which-database.ts) — el mismo que sirve juan-tech.com; no hay entorno de staging.</precondition>
  <reversibility rating="reversible">Actualización de contenido vía Local API, re-editable con el mismo patrón de script en cualquier momento; no toca schema.</reversibility>
  <done>/privacy y /en/privacy cubren en vivo el formulario de email, Resend como encargado del tratamiento, la retención y el proceso de baja, con las 6 secciones originales intactas — confirmado por curl contra el sitio real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitante anónimo -> AffiliateDisclosure / /privacy renderizado | Solo lectura pública |
| Executor -> Postgres real de Dokploy (update-privacy-resend.ts) | Escritura de contenido contra la única base que existe, sin sandbox |
| Executor -> route handler temporal de preview | Superficie de desarrollo, viva solo durante este plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-06 | Tampering | Frase de Amazon en español (messages/es.json) | medium | mitigate | `checkpoint:human-verify gate="blocking-human"` antes de escribir la clave (Task 2); nunca se presenta una traducción como "texto oficial" |
| T-46-07 | Information Disclosure | Route handler temporal `/api/_dev-preview/affiliate-disclosure` | low | mitigate | Borrado en Task 3 apenas cumple su propósito; `/api` ya está en `disallow` de robots.ts como defensa adicional |
| T-46-08 | Tampering | `/privacy` vía Local API contra la única base real (sin sandbox) | high | mitigate | El script reusa verbatim las 6 secciones existentes antes de agregar las 2 nuevas (Payload reemplaza el árbol completo en cada update); el `<verify>` confirma por curl que las 6 secciones originales siguen presentes, no solo que las nuevas aparecieron |
| T-46-SC | Tampering | Instalaciones npm/pip/cargo | n/a | accept | Esta fase no instala ningún paquete nuevo — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `messages/en.json`/`messages/es.json` tienen el namespace `affiliateDisclosure` completo en ambos idiomas
- La frase de Amazon en inglés es verbatim; la de español fue aprobada explícitamente por Juan antes de escribirse
- `/privacy` y `/en/privacy` responden 200 y mencionan Resend, retención y proceso de baja, con las 6 secciones originales intactas
- El route handler temporal de preview no existe al cerrar el plan
- `npx tsc --noEmit` pasa en todo el plan
</verification>

<success_criteria>
- LEG-01: AffiliateDisclosure renderiza copy bilingüe desde `messages/{es,en}.json`, nunca del CMS
- LEG-02: la frase de Amazon aparece verbatim en ambos idiomas (EN mandatoria, ES aprobada)
- LEG-03: `/privacy` cubre el formulario de email, Resend, retención y baja, verificado por curl
- LEG-04: la restricción de cero tracking con consentimiento queda documentada como comentario verificable por grep
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 46-02)

| Artefacto | Rol |
|-----------|-----|
| `src/components/AffiliateDisclosure.tsx` | Componente único, disclosure bilingüe, consumido por Phase 48 en `/stack` y el bloque inline |
| `messages/es.json` / `messages/en.json` (`affiliateDisclosure`) | Copy legal fuente, nunca contenido del CMS |
| `scripts/update-privacy-resend.ts` | Actualiza `/privacy` de forma aditiva (6 secciones originales + 2 nuevas) |
</artifacts>

<output>
Crear `.planning/phases/46-disclosure-legal-esquema-de-links-de-afiliado/46-02-SUMMARY.md` al terminar, incluyendo la redacción exacta que Juan aprobó para la frase de Amazon en español (Task 2).
</output>
