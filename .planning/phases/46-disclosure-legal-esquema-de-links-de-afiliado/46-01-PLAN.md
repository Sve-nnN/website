---
phase: 46-disclosure-legal-esquema-de-links-de-afiliado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/access/authenticatedOrActive.ts
  - src/collections/AffiliateLinks/index.ts
  - src/payload.config.ts
  - src/migrations/*_affiliate_links_collection.ts (generado por payload migrate:create)
  - src/payload-types.ts (regenerado)
  - src/lib/affiliate.ts
  - src/lib/cache.ts
  - src/lib/cache-tags.ts
  - src/components/AffiliateLink.tsx
  - scripts/verify-affiliate-links-schema.ts
  - scripts/verify-affiliate-links-cache.ts
  - scripts/verify-affiliate-link-component.ts
autonomous: true
requirements: [AFF-01, AFF-02, AFF-03, AFF-04, AFF-05, AFF-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "La colección affiliate-links existe en el admin de Payload con los 13 campos de la matriz congelada (name, slug, program, destinations, cookieWindowDays, commissionNote, active, placement, order, tagline, whyIUseIt, disclosureOverride, ctaLabel), sin rel ni price, y COUNT(*) = 0 al cerrar el plan"
    - "getCachedAffiliateLinks() es la única función que lee la colección para el frontend, siempre con overrideAccess:false, y redacta cookieWindowDays/commissionNote para lecturas no autenticadas"
    - "pickDestination() resuelve un destino por marketplace sin importar payload/@payload-config, quedando apto para un Client Component futuro (Phase 48)"
    - "AffiliateLink emite rel=\"sponsored nofollow noopener\" como string literal, renderiza la URL de Amazon directa con tag= visible, sin referrerPolicy y sin pasar por /go/"
    - "La migración generada por payload migrate:create fue leída completa antes de aplicarse contra el Postgres real de Dokploy y contiene únicamente CREATE TABLE/CREATE TYPE/CREATE INDEX/ADD COLUMN"
  artifacts:
    - src/collections/AffiliateLinks/index.ts
    - src/access/authenticatedOrActive.ts
    - src/lib/affiliate.ts
    - src/lib/cache.ts (getCachedAffiliateLinks)
    - src/lib/cache-tags.ts (CACHE_TAGS.affiliateLinks + revalidate hooks)
    - src/components/AffiliateLink.tsx
    - src/payload.config.ts (AffiliateLinks registrado)
    - src/migrations/*_affiliate_links_collection.ts
    - scripts/verify-affiliate-links-schema.ts
    - scripts/verify-affiliate-links-cache.ts
    - scripts/verify-affiliate-link-component.ts
  key_links:
    - "AffiliateLinks.hooks.afterChange/afterDelete -> revalidateAffiliateLinksCache/OnDelete (cache-tags.ts) -> invalida el tag affiliate-links:all que consume getCachedAffiliateLinks()"
    - "payload.config.ts registra AffiliateLinks en `collections:` (no en seoPlugin/redirectsPlugin/searchPlugin/mcpPlugin) para que la migración generada corresponda exactamente a este schema"
    - "pickDestination() vive fuera de src/lib/cache.ts para poder importarse sin @payload-config desde un Client Component en Phase 48"
---

<objective>
Construir la mitad de esquema/datos de la Fase 46: la colección `affiliate-links` con la matriz de localización congelada (46-CONTEXT.md), su migración puramente aditiva leída y aplicada contra el Postgres real de Dokploy, la capa de cache pública (`getCachedAffiliateLinks()`), la función pura `pickDestination()`, y el componente `AffiliateLink` que emite `rel="sponsored nofollow noopener"` desde código con Amazon renderizado directo. Cubre AFF-01 a AFF-06.

Purpose: todo lo que un link de afiliado necesita para existir tiene que estar aprobado y funcionando ANTES de que se cargue el primer documento real (Phase 48). Esta mitad es la que toca schema — la única migración de todo el milestone — así que se prueba de punta a punta con un tracer de una sola ronda de datos (crear, leer, borrar) que deja la tabla en COUNT(*) = 0, exactamente como exige SC-3.

Output:
- `src/collections/AffiliateLinks/index.ts` (colección nueva, sin drafts)
- `src/access/authenticatedOrActive.ts` (access function nueva)
- `src/lib/affiliate.ts` (`pickDestination()`, función pura)
- `src/lib/cache.ts` extendido (`getCachedAffiliateLinks()`)
- `src/lib/cache-tags.ts` extendido (tag + hooks de invalidación)
- `src/components/AffiliateLink.tsx` (componente nuevo)
- Migración aditiva aplicada contra Dokploy, leída antes de correr
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-disclosure-legal-esquema-de-links-de-afiliado/46-CONTEXT.md
@.planning/phases/46-disclosure-legal-esquema-de-links-de-afiliado/46-RESEARCH.md
@CLAUDE.md
@src/collections/Websites/index.ts
@src/access/authenticatedOrPublished.ts
@src/fields/targetKeyword.ts
@src/lib/cache.ts
@src/lib/cache-tags.ts
@src/migrations/20260820_155934_subscribers_collection.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Colección affiliate-links + access + migración aditiva contra Dokploy</name>
  <files>
    src/access/authenticatedOrActive.ts,
    src/collections/AffiliateLinks/index.ts,
    src/payload.config.ts,
    src/migrations/*_affiliate_links_collection.ts,
    src/payload-types.ts,
    scripts/verify-affiliate-links-schema.ts
  </files>
  <read_first>
    - src/collections/Websites/index.ts (completo — forma estructural a imitar)
    - src/access/authenticatedOrPublished.ts (completo — access function a espejar)
    - src/fields/targetKeyword.ts (completo — forma del FieldAccess)
    - src/migrations/20260820_155934_subscribers_collection.ts (completo — forma de una migración aditiva limpia: CREATE TYPE + CREATE TABLE + índices, cero ALTER COLUMN/DROP)
    - src/payload.config.ts líneas 97-113 (punto de inserción en `collections:`)
    - 46-CONTEXT.md sección "Matriz de localización de affiliate-links (congelada — SC-3)"
    - CLAUDE.md sección "Database Safety"
  </read_first>
  <action>
    1. Confirmar contra qué base se va a escribir: `node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts` — debe resolver al Postgres real de Dokploy (no hay sandbox).

    2. Crear `src/access/authenticatedOrActive.ts` espejando `authenticatedOrPublished.ts` 1:1, sustituyendo `_status: { equals: 'published' }` por `active: { equals: true }`. Este es el patrón `isPublic` documentado por Payload (no inventado).

    3. Crear `src/collections/AffiliateLinks/index.ts` con slug `affiliate-links`, siguiendo la forma estructural de `Websites/index.ts` pero SIN bloque `versions` (reemplazado por un checkbox `active`, per CONTEXT.md — no hay flujo de revisión editorial). Access: `{ create: authenticated, delete: authenticated, read: authenticatedOrActive, update: authenticated }`. `admin: { useAsTitle: 'name', defaultColumns: ['name', 'program', 'active', 'placement'] }`. NO agregar `hooks` todavía (se agregan en Task 2, una vez existan los exports de cache-tags.ts — evita un import roto entre tasks). Campos EXACTOS, en este orden, per la matriz congelada:
       - `name` (text, required, NO localizado — identificador interno)
       - `slugField('name')` (reusa el helper existente de `src/fields/slug.ts`, usando `name` como fuente en vez del default `title`)
       - `program` (select, required, opciones: amazon, kinsta, dinorank, digitalocean, other — NO localizado)
       - `destinations` (array, NO localizado, subcampos `marketplace` (text, required) y `url` (text, required) — resuelto en render por `pickDestination()`, sin validación de formato adicional, per RESEARCH.md ASVS V5: `required: true` en los subcampos ya cubre la validación esperada)
       - `cookieWindowDays` (number) — campo interno, `access: { read: authenticatedFieldRead }` con `authenticatedFieldRead` definida inline en este archivo como `({ req: { user } }) => Boolean(user)`, aplicada directamente al campo (NO envuelta en un `group`, per el anti-patrón de RESEARCH.md — la matriz que Juan aprobó lista campos planos)
       - `commissionNote` (text) — mismo `access.read` interno que `cookieWindowDays`
       - `active` (checkbox, `defaultValue: false`) — reemplaza `versions.drafts`
       - `placement` (select, opciones: stack-page, inline-post, both — NO localizado)
       - `order` (number, opcional, NO localizado)
       - `tagline` (text, `localized: true`)
       - `whyIUseIt` (textarea, `localized: true`)
       - `disclosureOverride` (textarea, `localized: true`)
       - `ctaLabel` (text, `localized: true`)
       Deliberadamente ausentes: `rel` (se emite desde código en Task 3) y `price` (nunca un campo, per CONTEXT.md).

    4. Registrar `AffiliateLinks` en `src/payload.config.ts`: importar junto a `Websites` y agregar al array `collections:` después de `Websites` (línea ~112). NO agregar a `seoPlugin`/`redirectsPlugin`/`searchPlugin`/`mcpPlugin` — no es contenido público con meta tags propios ni debe exponerse por MCP en esta fase.

    5. Generar la migración: `node --env-file=.env node_modules/.bin/payload migrate:create affiliate_links_collection`. LEER el archivo generado bajo `src/migrations/` completo antes de continuar — debe contener únicamente `CREATE TYPE` (los 2 enums `program`/`placement`), `CREATE TABLE "affiliate_links"`, `CREATE TABLE` para el array `destinations` (Payload modela arrays como tabla hija), índices y `ALTER TABLE ... ADD COLUMN`/`ADD CONSTRAINT` sobre `payload_locked_documents_rels` — exactamente la forma de `20260820_155934_subscribers_collection.ts`. Si aparece cualquier `ALTER COLUMN`, `DROP TABLE` o `DROP COLUMN` fuera del bloque `down()`, PARAR y escalar a Juan por nombre antes de aplicar — no calza con "puramente aditiva".

    6. Aplicar: `node --env-file=.env node_modules/.bin/payload migrate`.

    7. Regenerar tipos: `npm run generate:types` (actualiza `src/payload-types.ts` con el tipo `AffiliateLink`, consumido por Task 2 y Task 3).

    8. Escribir `scripts/verify-affiliate-links-schema.ts`: usando `getPayload({ config })`, (a) `payload.count({ collection: 'affiliate-links' })` debe ser `0`; (b) `payload.create()` un doc de prueba cubriendo los 13 campos (incluyendo 2 entradas en `destinations`), locale `es`; (c) `payload.update()` el mismo doc en locale `en` con valores distintos para los 4 campos localizados; (d) `payload.findByID()` en ambos locales confirmando que los 9 campos no localizados son idénticos y los 4 localizados difieren correctamente por idioma; (e) `payload.delete()` el doc de prueba; (f) `payload.count()` vuelve a ser `0`. Imprimir `PASS` al final si todo se cumple, o `FAIL: <motivo>` y `process.exitCode = 1` si algo falla.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; MIGFILE=$(ls src/migrations/*affiliate_links_collection*.ts 2>/dev/null | head -1); test -n "$MIGFILE"; ! grep -qiE "ALTER COLUMN|DROP TABLE|DROP COLUMN" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); node --env-file=.env node_modules/.bin/tsx scripts/verify-affiliate-links-schema.ts | tee /tmp/verify-schema-46-01.log; grep -q "^PASS$" /tmp/verify-schema-46-01.log; npx tsc --noEmit; echo TRACER_OK</automated>
    <fails_when>El archivo de migración no existe o contiene ALTER COLUMN/DROP TABLE/DROP COLUMN fuera de `down()`; el script de verificación no imprime PASS (schema roto, count distinto de 0 al final, o los campos localizados no difieren por idioma); o `tsc --noEmit` falla.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/access/authenticatedOrActive.ts` existe y espeja `authenticatedOrPublished.ts` con `active` en vez de `_status`
    - `src/collections/AffiliateLinks/index.ts` tiene exactamente los 13 campos de la matriz, sin `rel` ni `price`, sin bloque `versions`
    - `AffiliateLinks` está registrado en `payload.config.ts` `collections:` únicamente (no en plugins)
    - La migración generada es puramente aditiva (leída completa, confirmado por el grep del verify)
    - `payload.count({ collection: 'affiliate-links' })` es 0 tanto antes como después del round-trip de prueba
    - `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <precondition>DATABASE_URI en .env resuelve al Postgres real de Dokploy (confirmado por scripts/db/04-which-database.ts) — no existe entorno de staging/sandbox.</precondition>
  <reversibility rating="costly">La matriz define localized/no-localized por campo; cambiarla después de cargar contenido real exigiría una migración de backfill (mismo patrón del incidente del 2026-07-12 que borró el CTA de Home en producción). Hoy es barata de ajustar porque la tabla queda en COUNT(*) = 0 — la matriz ya fue aprobada por Juan en 46-CONTEXT.md, no se reabre la decisión.</reversibility>
  <done>La colección affiliate-links existe y está migrada contra el Postgres real de Dokploy; el round-trip de prueba (crear en es, actualizar en en, leer ambos locales, borrar) confirma que los 13 campos persisten correctamente y que la tabla queda vacía al cerrar la task.</done>
</task>

<task type="auto">
  <name>Task 2: Capa de cache pública + pickDestination() puro + hooks de invalidación</name>
  <files>
    src/lib/affiliate.ts,
    src/lib/cache.ts,
    src/lib/cache-tags.ts,
    src/collections/AffiliateLinks/index.ts,
    scripts/verify-affiliate-links-cache.ts
  </files>
  <read_first>
    - src/lib/cache.ts función `getCachedCategories` completa (forma exacta a replicar: `unstable_cache`, `overrideAccess: false`, tags, `revalidate`)
    - src/lib/cache-tags.ts completo (comentario de cabecera sobre por qué este archivo NO importa `@payload-config`; forma de `revalidateCategoriesCache`/`revalidateCategoriesCacheOnDelete`)
    - src/collections/CaseStudies/index.ts líneas 1-30 (hooks wireados directamente en la colección, NO vía `overrides` de plugin)
    - 46-RESEARCH.md sección "Pitfall 3" (por qué `overrides.hooks` es solo para colecciones registradas por un plugin)
  </read_first>
  <action>
    1. Crear `src/lib/affiliate.ts` exportando `type Destination = { marketplace: string; url: string }` y `pickDestination(destinations: Destination[], marketplace: string): Destination | undefined` — busca por `marketplace` exacto, si no encuentra hace fallback a `destinations[0]`. Comentario obligatorio al inicio del archivo: "PURE FUNCTION — no `payload`/`@payload-config` import aquí a propósito (AFF-02): debe seguir siendo importable desde un Client Component en Phase 48. No agregar una llamada a DB a este archivo."

    2. En `src/lib/cache-tags.ts`: agregar `affiliateLinks: () => 'affiliate-links:all'` al objeto `CACHE_TAGS`, y `revalidateAffiliateLinksCache`/`revalidateAffiliateLinksCacheOnDelete` (tipados `CollectionAfterChangeHook<AffiliateLink>`/`CollectionAfterDeleteHook<AffiliateLink>` importando `AffiliateLink` desde `@/payload-types`) siguiendo exactamente la forma de `revalidateCategoriesCache`/`revalidateCategoriesCacheOnDelete` (`safeRevalidateTag(CACHE_TAGS.affiliateLinks())` + `safeRevalidateAllPaths()`).

    3. En `src/collections/AffiliateLinks/index.ts`: importar `revalidateAffiliateLinksCache`/`revalidateAffiliateLinksCacheOnDelete` desde `@/lib/cache-tags` y agregar `hooks: { afterChange: [revalidateAffiliateLinksCache], afterDelete: [revalidateAffiliateLinksCacheOnDelete] }` directamente en la config de la colección (NO vía `overrides` de plugin — esta colección no está registrada por ningún plugin).

    4. En `src/lib/cache.ts`: agregar `getCachedAffiliateLinks(locale: Locale)` siguiendo exactamente la forma de `getCachedCategories`: `unstable_cache(async () => { const payload = await getPayload({ config }); const { docs } = await payload.find({ collection: 'affiliate-links', locale, limit: 100, overrideAccess: false }); return docs }, ['affiliate-links', locale], { tags: [CACHE_TAGS.affiliateLinks()], revalidate: CACHE_TTL_SECONDS })()`.

    5. Escribir `scripts/verify-affiliate-links-cache.ts`: crear UN doc de prueba con `active: true`, `cookieWindowDays: 30`, `commissionNote: 'test interno'`, y 2 `destinations` (`{ marketplace: 'us', url: 'https://amazon.com/dp/B0EXAMPLE?tag=juantech02-20' }`, `{ marketplace: 'default', url: 'https://example.com' }`). Llamar `getCachedAffiliateLinks('es')` y verificar: (a) el doc de prueba aparece; (b) `cookieWindowDays` y `commissionNote` vienen `undefined` en el doc devuelto (la lectura no pasa `user`, así que el `FieldAccess` de esos 2 campos los redacta aunque `overrideAccess` sea `false` a nivel de colección); (c) `pickDestination(doc.destinations, 'us')` devuelve la URL de Amazon verbatim con `tag=` intacto; (d) `pickDestination(doc.destinations, 'inexistente')` cae al primer elemento (`us`). Borrar el doc de prueba al final y confirmar `payload.count() === 0`. Imprimir `PASS`/`FAIL: <motivo>` igual que en Task 1.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; ! grep -nE "from 'payload'|@payload-config" src/lib/affiliate.ts; npx tsc --noEmit; node --env-file=.env node_modules/.bin/tsx scripts/verify-affiliate-links-cache.ts | tee /tmp/verify-cache-46-01.log; grep -q "^PASS$" /tmp/verify-cache-46-01.log; echo CACHE_OK</automated>
    <fails_when>src/lib/affiliate.ts importa `payload` o `@payload-config`; `tsc --noEmit` falla; el script no imprime PASS (campos internos visibles sin usuario, pickDestination no resuelve o no cae al fallback, o el doc de prueba no queda borrado al final).</fails_when>
  </verify>
  <acceptance_criteria>
    - `pickDestination()` no tiene ningún import de `payload`/`@payload-config`
    - `getCachedAffiliateLinks()` pasa `overrideAccess: false` explícito, igual que el resto de `cache.ts`
    - Los hooks están wireados directamente en `AffiliateLinks/index.ts`, no vía `overrides` de plugin
    - `cookieWindowDays`/`commissionNote` no aparecen en una lectura sin usuario autenticado
  </acceptance_criteria>
  <reversibility rating="reversible">Funciones de datos detrás de una interfaz estable (mismo shape que getCachedCategories/revalidateCategoriesCache); cambiar la implementación no rompe ningún call site futuro.</reversibility>
  <done>getCachedAffiliateLinks() es la única lectura pública de la colección, redacta los campos internos para lecturas anónimas, pickDestination() es puro y sin acceso a DB, y los hooks de invalidación están wireados directamente en la colección.</done>
</task>

<task type="auto">
  <name>Task 3: AffiliateLink — rel hardcodeado desde código, Amazon directo sin cloaking</name>
  <files>src/components/AffiliateLink.tsx, scripts/verify-affiliate-link-component.ts</files>
  <read_first>
    - 46-RESEARCH.md sección "Code Examples" — `AffiliateLink` (skeleton de referencia)
    - 46-CONTEXT.md sección "Specific Ideas" (reglas de render directo de Amazon)
    - src/lib/cache.ts líneas 1-20 (estilo del comentario SECURITY/CONSTRAINT a replicar)
  </read_first>
  <action>
    Crear `src/components/AffiliateLink.tsx`: componente de servidor (sin `'use client'`, cero JS de cliente) que recibe `{ href, children, className }`. Renderiza `<a href={href} target="_blank" rel="sponsored nofollow noopener" className={className}>{children}</a>`. El valor de `rel` es un string literal hardcodeado — NUNCA un prop, nunca derivado de `destinations[]` ni de ningún campo del CMS (AFF-03). No agregar el atributo `referrerPolicy` en ninguna parte del archivo. No concatenar `/go/` ni ninguna forma de redirect propio — el `href` se renderiza exactamente como llega (en Phase 48, ya resuelto por `pickDestination()`), per AFF-04. Agregar un comentario al inicio del archivo: "CONSTRAINT (AFF-03/AFF-04, LEG-04): rel hardcodeado, nunca un campo del CMS. Sin referrerPolicy. Sin /go/ para ningún href que pase por este componente (Amazon Program Policies 2026-04-14 prohíben Redirecting Links). Cero JS de cliente, cero tracking — sin onClick, sin document.cookie, sin localStorage aquí."

    Escribir `scripts/verify-affiliate-link-component.ts`: importar `AffiliateLink` y `renderToStaticMarkup` de `react-dom/server`; llamar `AffiliateLink({ href: 'https://amazon.com/dp/B0EXAMPLE?tag=juantech02-20', children: 'Ver en Amazon' })` directamente (componente sin hooks, invocable como función plana) y pasar el resultado a `renderToStaticMarkup`. Verificar que el HTML contiene exactamente `rel="sponsored nofollow noopener"`, contiene `tag=juantech02-20` verbatim, y NO contiene (case-insensitive) `referrerpolicy` ni `/go/`. Imprimir `PASS`/`FAIL: <motivo>`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; ! grep -niE "document\.cookie|localStorage|referrerPolicy|/go/" src/components/AffiliateLink.tsx; npx tsc --noEmit; node --env-file=.env node_modules/.bin/tsx scripts/verify-affiliate-link-component.ts | tee /tmp/verify-link-46-01.log; grep -q "^PASS$" /tmp/verify-link-46-01.log; echo LINK_OK</automated>
    <fails_when>El archivo contiene document.cookie/localStorage/referrerPolicy/\/go\/; `tsc --noEmit` falla; el HTML renderizado no tiene el rel exacto o no preserva tag= verbatim.</fails_when>
  </verify>
  <acceptance_criteria>
    - `rel="sponsored nofollow noopener"` es un string literal en el JSX, no una variable derivada de props externos
    - No existe `referrerPolicy` ni `/go/` en el archivo
    - El render de prueba con una URL de Amazon conserva `tag=` verbatim
  </acceptance_criteria>
  <reversibility rating="costly">El render directo sin cloaking de Amazon ya está bloqueado por CONTEXT.md/ROADMAP (Program Policies 2026-04-14 prohíben Redirecting Links; sanción = terminación de cuenta más pérdida de comisiones). No se abre un nuevo checkpoint porque la decisión ya está tomada — pero revertirla implicaría rehacer un componente que Phase 48 va a consumir tal cual.</reversibility>
  <done>AffiliateLink renderiza un anchor directo con rel hardcodeado, sin referrerPolicy, sin /go/, con la URL (incluido tag=) preservada verbatim — confirmado por render estático y por grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitante anónimo -> getCachedAffiliateLinks() | Lectura pública cacheada; no debe filtrar `cookieWindowDays`/`commissionNote` |
| Editor autenticado -> admin de AffiliateLinks | Escritura autenticada, sin flujo de revisión (active en vez de drafts) |
| Executor -> Postgres real de Dokploy | La migración se aplica directo contra la única base que existe, sin sandbox |
| Contenido editable del CMS -> AffiliateLink | El editor controla `destinations[].url`, nunca `rel` ni el mecanismo de render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Information Disclosure | getCachedAffiliateLinks() (unstable_cache) | high | mitigate | `overrideAccess: false` explícito (Task 2) + `access.read` a nivel de campo en `cookieWindowDays`/`commissionNote` (Task 1) — ambos verificados por script antes de cerrar la task |
| T-46-02 | Tampering | AffiliateLinks collection config (`rel`) | medium | mitigate | `rel` nunca es un campo del schema (Task 1); se emite como string literal en `AffiliateLink.tsx` (Task 3), verificado por grep |
| T-46-03 | Tampering | AffiliateLink.tsx (render de Amazon) | high | mitigate | Sin `/go/`, sin `referrerPolicy` override, `href` renderizado verbatim — violarlo arriesga terminación de la cuenta de Amazon per Program Policies 2026-04-14 |
| T-46-04 | Tampering | Migración contra Postgres real de Dokploy (sin sandbox) | high | mitigate | Migración leída completa antes de aplicar (Task 1), confirmado aditiva-only por grep automatizado; `COUNT(*) = 0` verificado antes y después del round-trip de prueba |
| T-46-SC | Tampering | Instalaciones npm/pip/cargo | n/a | accept | Esta fase no instala ningún paquete nuevo (46-RESEARCH.md "Package Legitimacy Audit": no aplica) — sin superficie de cadena de suministro |
</threat_model>

<verification>
- `payload.count({ collection: 'affiliate-links' })` es 0 al cerrar el plan
- La migración generada contiene únicamente CREATE TABLE/CREATE TYPE/CREATE INDEX/ADD COLUMN, leída completa antes de aplicarse
- `getCachedAffiliateLinks()` redacta `cookieWindowDays`/`commissionNote` para lecturas sin usuario
- `pickDestination()` no importa `payload`/`@payload-config`
- `AffiliateLink` emite `rel="sponsored nofollow noopener"` hardcodeado, sin `referrerPolicy`, sin `/go/`
- `npx tsc --noEmit` pasa en todo el plan
</verification>

<success_criteria>
- AFF-01: colección `affiliate-links` existe con la matriz congelada, sin `rel`/`price`, `COUNT(*) = 0`
- AFF-02: `pickDestination()` puro, resuelto por `marketplace`, sin acceso a DB
- AFF-03: `rel="sponsored nofollow noopener"` emitido desde código, nunca un campo
- AFF-04: Amazon renderiza directo, `tag=` visible, sin `referrerPolicy`, sin `/go/`
- AFF-05: `getCachedAffiliateLinks()` es la única lectura pública, `overrideAccess: false`, cache tags + hooks de revalidación
- AFF-06: migración puramente aditiva, leída antes de aplicarse contra la base real
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 46-01)

| Artefacto | Rol |
|-----------|-----|
| `src/collections/AffiliateLinks/index.ts` | Colección nueva — matriz de localización congelada, `active` en vez de drafts |
| `src/access/authenticatedOrActive.ts` | Access function nueva — patrón `isPublic` con checkbox en vez de `_status` |
| `src/lib/affiliate.ts` (`pickDestination`) | Función pura, resuelve destino por `marketplace`, sin acceso a DB |
| `src/lib/cache.ts` (`getCachedAffiliateLinks`) | Única lectura pública de la colección, `overrideAccess: false` |
| `src/lib/cache-tags.ts` (`CACHE_TAGS.affiliateLinks`, `revalidateAffiliateLinksCache*`) | Tag + hooks de invalidación |
| `src/components/AffiliateLink.tsx` | `rel` hardcodeado, Amazon directo sin cloaking |
| `src/migrations/*_affiliate_links_collection.ts` | Migración aditiva aplicada contra Dokploy |
| `scripts/verify-affiliate-links-schema.ts` | Round-trip de schema (create/update/delete), deja COUNT(*) = 0 |
| `scripts/verify-affiliate-links-cache.ts` | Verifica redacción de campos internos + pickDestination |
| `scripts/verify-affiliate-link-component.ts` | Verifica rel/href/ausencia de tracking en el render |
</artifacts>

<output>
Crear `.planning/phases/46-disclosure-legal-esquema-de-links-de-afiliado/46-01-SUMMARY.md` al terminar.
</output>
