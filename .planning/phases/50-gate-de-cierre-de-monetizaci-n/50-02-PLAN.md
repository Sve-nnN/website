---
phase: 50-gate-de-cierre-de-monetizaci-n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-affiliate-crawl.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate02-findings.md
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cada anchor a un dominio de programa de afiliado (Amazon, Hostinger, Kinsta, DinoRANK, DigitalOcean, DataForSEO) en /stack y en al menos una superficie mas lleva rel=\"sponsored nofollow noopener\", verificado sobre HTML real, no sobre lectura de codigo"
    - "El disclosure precede al primer anchor de afiliado en orden del DOM en cada superficie donde ambos coexisten (/stack, posts con affiliate-inline si existen)"
    - "Ningun link de Amazon resuelve via /go/ -- verificado contra los docs reales de affiliate-links con program=amazon si existen, o documentado explicitamente como vacuamente satisfecho si no existe ninguno"
    - "AuditorHighlight/AuditorCallout (producto propio) NUNCA llevan rel=sponsored -- control negativo verificado sobre HTML real"
    - "Un grep confirma overrideAccess:false (o exencion documentada) en cada payload.find(/findByID(/findGlobal( de src/lib/cache.ts, y que subscribers/affiliate-clicks/lead-magnets no aparecen en SITEMAP_COLLECTIONS ni en el mapa de colecciones de mcpPlugin"
  artifacts:
    - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-affiliate-crawl.json
    - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate02-findings.md
  key_links:
    - "El resultado de la consulta a affiliate-links (program=amazon, active=true) y a posts (bloque affiliate-inline presente) decide que superficies son crawleables con contenido real vs. cuales quedan documentadas como no verificables por falta de contenido (Open Questions 1/2 de 50-RESEARCH.md)"
    - "50-affiliate-crawl.json guarda los hallazgos crudos del crawl; 50-gate02-findings.md los resume en un veredicto unico de GATE-02, consumido como input directo por 50-03 Task 1"
---

<objective>
Crawlear el HTML real servido por produccion (nunca leer solo el codigo) en cada superficie de afiliacion construida en Phases 46-49, y confirmar por grep las dos aserciones estaticas de seguridad de acceso (overrideAccess, exclusion de colecciones privadas). Esto cierra GATE-02: cero anchors sin sponsored, disclosure antes en el DOM, cero Amazon por /go/, paridad de locales por dato, overrideAccess correcto.

Purpose: 50-CONTEXT.md exige explicitamente que estas aserciones se verifiquen "sobre HTML renderizado real, nunca por lectura de codigo" -- el research ya confirmo cada superficie por codigo, pero eso no sustituye la verificacion en runtime que exige el gate de cierre del milestone.
Output: `50-affiliate-crawl.json` (hallazgos crudos del crawl) y `50-gate02-findings.md` (veredicto consolidado de GATE-02).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-RESEARCH.md
@.planning/phases/47-ruta-go-fix-de-middleware-registro-de-clics/47-CONTEXT.md
@src/app/go/[slug]/route.ts
@src/lib/affiliate.ts
@src/lib/post-affiliate-scan.ts
@src/lib/cache.ts
@src/lib/sitemap-data.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer -- resolver contenido real (affiliate-links program=amazon, posts con affiliate-inline) + crawl completo de UNA superficie (/stack, es)</name>
  <files>.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-affiliate-crawl.json</files>
  <precondition>Acceso de lectura a la Postgres de produccion via `scripts/db/tunnel.sh` (o Local API contra el `.env` ya apuntado a produccion) esta disponible desde esta maquina, usando la llave SSH ya provisionada en sesiones anteriores (ver `~/.claude/projects/.../memory/reference_prod_db_access.md`). Antes de escribir nada, confirmar contra que base se esta leyendo con `node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts` (Database Safety, CLAUDE.md) -- si el host no es el de Dokploy, halt y documentar como no verificable en vez de leer contra una base equivocada.</precondition>
  <action>
    Resolver las 2 Open Questions del research antes de crawlear nada: (1) consultar `affiliate-links` por documentos con `program: 'amazon'` y `active: true` (Local API de solo lectura, un `payload.find` con `overrideAccess: true` porque es un script admin, no el runtime publico -- ver patron de scripts existentes como `scripts/verify-affiliate-links-schema.ts`); (2) consultar `posts` publicados y correr `postHasAffiliateInline(doc.content)` (de `src/lib/post-affiliate-scan.ts`) contra cada uno para saber si existe al menos un post real con el bloque inline. Documentar el resultado de ambas preguntas en `50-affiliate-crawl.json` bajo una clave `contentAudit` ANTES de continuar -- si no hay ningun doc Amazon activo o ningun post con el bloque, la asercion correspondiente de GATE-02 se satisface vacuamente o se limita a `/stack` (que si tiene contenido real de Phase 48), nunca se fabrica contenido de prueba para forzar cobertura.

    Con eso resuelto, crawlear `/stack` (locale es) va `curl` contra produccion real: extraer todos los `&lt;a&gt;` con href a un dominio de programa de afiliado conocido (amazon, hostinger, kinsta, dinorank, digitalocean, dataforseo) y confirmar que el 100% lleva `rel="sponsored nofollow noopener"` exacto; confirmar que el marcador de disclosure (`role="note"`, emitido por `AffiliateDisclosureFrame`) aparece en el HTML crudo ANTES (offset de string menor) que el primer `rel="sponsored nofollow noopener"`; confirmar que el bloque `AuditorHighlight`/`AuditorCallout` si aparece en esa misma pagina no lleva `sponsored`. Guardar cada hallazgo (URL crawleada, anchors encontrados, rel de cada uno, offset de disclosure vs. primer anchor) en `50-affiliate-crawl.json`.

    Este es el tracer de la fase: prueba la cadena completa "resolver que contenido real existe -> crawlear una superficie -> verificar las 3 aserciones de HTML" de punta a punta antes de escalar a las superficies/locales restantes en Task 2.
  </action>
  <verify>
    <automated>curl -s https://juan-tech.com/stack > /tmp/gate50-stack-es.html && node -e "const fs=require('fs'); const html=fs.readFileSync('/tmp/gate50-stack-es.html','utf8'); const anchors=[...html.matchAll(/&lt;a [^&gt;]*href=\"[^\"]*(amazon|hostinger|kinsta|dinorank|digitalocean|dataforseo)[^\"]*\"[^&gt;]*&gt;/gi)]; const bad=anchors.filter(m=>!m[0].includes('rel=\"sponsored nofollow noopener\"')); if(bad.length){console.error('anchors sin rel correcto:', bad.map(b=>b[0])); process.exit(1);} const discloseIdx=html.search(/role=\"note\"/); const firstAffIdx=html.search(/rel=\"sponsored nofollow noopener\"/); if(discloseIdx===-1||firstAffIdx===-1||discloseIdx&gt;firstAffIdx){console.error('orden de disclosure violado o ausente'); process.exit(1);} console.log('tracer /stack es: PASS,', anchors.length, 'anchors de afiliado, todos con rel correcto, disclosure antes en DOM')"</automated>
    <fails_when>algun anchor a un dominio de programa de afiliado en /stack no lleva `rel="sponsored nofollow noopener"` exacto, o el disclosure (`role="note"`) no aparece antes del primer anchor de afiliado en el HTML crudo.</fails_when>
  </verify>
  <done>`50-affiliate-crawl.json` existe con la clave `contentAudit` respondiendo las 2 Open Questions del research contra la base real, y con el resultado completo del crawl de `/stack` (es): cero anchors de afiliado sin `sponsored`, disclosure confirmado antes en el DOM, control negativo de AuditorHighlight/AuditorCallout sin `sponsored`.</done>
</task>

<task type="auto">
  <name>Task 2: Escalar crawl -- /stack en, /go/&lt;slug&gt; (302/404), paridad de locales, controles negativos</name>
  <files>.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-affiliate-crawl.json</files>
  <read_first>.planning/phases/47-ruta-go-fix-de-middleware-registro-de-clics/47-CONTEXT.md (confirma que `/go/[slug]` vive fuera de `[locale]` y no tiene contraparte `/en/go/` -- no escribir una asercion que espere esa ruta)</read_first>
  <action>
    Repetir el crawl de `/stack` para el locale `en` (mismas 3 aserciones que Task 1: rel exacto, orden del disclosure, control negativo).

    Para `/go/[slug]`: tomar los slugs `active: true` reales devueltos por la consulta a `affiliate-links` de Task 1 (excluyendo cualquiera con `program: 'amazon'`, que por contrato de Phase 46/47 nunca deberia estar activo via `/go/`) y confirmar con `curl -sI` que cada uno devuelve 302 con `Location` a un destino no vacio; para un slug inexistente (o, si existe, el de programa Amazon) confirmar 404 identico (mismo status/body que un slug inexistente, Pitfall de enumeracion ya mitigado en Phase 47). Como `/go/` vive fuera de `[locale]` y no tiene variante `/en/go/`, la "paridad de locales" de GATE-02 para esta superficie se verifica de otra forma: confirmar en el doc real de `affiliate-links` que `destinations[]` tiene entradas por `marketplace` con URLs distintas donde el programa lo amerita (no inspeccion visual, dato crudo del documento), y que el copy del `AffiliateDisclosure` si varia entre `messages/es.json` y `messages/en.json` para la frase de Amazon (LEG-02).

    Confirmar el control negativo de `EmailCaptureCard`: su unico link es a `/privacy`, sin `AffiliateLink` ni `rel=sponsored` -- crawlear el post que lo usa (`scripts/seed-phase49-email-capture-post.ts` indica cual) y confirmar por HTML real.

    Si Task 1 encontro al menos un post real con el bloque `affiliate-inline`, crawlearlo igual que `/stack` (rel + orden del disclosure). Si no existe ninguno, documentar explicitamente en `50-affiliate-crawl.json` que esa superficie queda "sin contenido real que crawlear -- verificado por lectura de codigo unicamente en Phase 48-03" (nunca fabricar un post de prueba para forzar cobertura).
  </action>
  <verify>
    <automated>curl -s https://juan-tech.com/en/stack > /tmp/gate50-stack-en.html && grep -qE 'role="note"' /tmp/gate50-stack-en.html && grep -qE 'rel="sponsored nofollow noopener"' /tmp/gate50-stack-en.html && echo "stack en: disclosure + sponsored presentes"</automated>
    <fails_when>`/en/stack` no contiene el marcador de disclosure, o contiene un anchor de afiliado sin `rel="sponsored nofollow noopener"` exacto, o algun slug `active` de `/go/` devuelve algo distinto de 302, o un slug inexistente/Amazon devuelve algo distinto de 404.</fails_when>
  </verify>
  <done>`50-affiliate-crawl.json` tiene el crawl completo de `/en/stack`, el resultado 302/404 de cada slug de `/go/` probado, la paridad de `destinations[]`/disclosure por locale documentada con datos crudos (no inspeccion visual), el control negativo de `EmailCaptureCard`, y el estado de cobertura de la superficie de posts con `affiliate-inline` (crawleada si existe contenido real, documentada como no verificable si no existe).</done>
</task>

<task type="auto">
  <name>Task 3: Grep de overrideAccess + exclusion de colecciones privadas + veredicto GATE-02</name>
  <files>.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate02-findings.md</files>
  <read_first>src/lib/cache.ts (leer el comentario inmediatamente arriba de `getCachedFeaturedContent` -- es la unica exencion documentada de `overrideAccess:false`, Pitfall 3 de 50-RESEARCH.md; no marcarla como gap sin leer el comentario)</read_first>
  <action>
    Correr el grep de `payload.find(`/`payload.findByID(`/`payload.findGlobal(` contra `src/lib/cache.ts` y contar cuantas llamadas llevan `overrideAccess: false` explicito. Para cada llamada SIN `overrideAccess: false`, leer el comentario inmediatamente arriba: si documenta una exencion real (como `getCachedFeaturedContent`, global sin drafts con `access` por defecto ya restrictivo), no es un gap; si no hay comentario de exencion, es un FAIL real de GATE-02.

    Correr el grep de `SITEMAP_COLLECTIONS` en `src/lib/sitemap-data.ts` y del bloque `mcpPlugin(` en `src/payload.config.ts`, confirmando que `subscribers`, `affiliate-clicks` y `lead-magnets` no aparecen en ninguno de los dos.

    Escribir `50-gate02-findings.md` consolidando: el resultado completo del crawl de Tasks 1-2 (tabla por superficie: rel correcto, orden del disclosure, resultado de `/go/`, paridad de locales, controles negativos), el resultado del grep de `overrideAccess`/exclusion de colecciones, y un veredicto unico GATE-02: PASS o FAIL con el detalle exacto de cualquier asercion que fallo. Si alguna superficie quedo sin contenido real que crawlear (Open Question 2 sin resolver con contenido), documentarlo explicitamente como "cobertura parcial, no como fallo" en una seccion separada.
  </action>
  <verify>
    <automated>grep -c "payload\.find(\|payload\.findByID(\|payload\.findGlobal(" src/lib/cache.ts && grep -c "overrideAccess: false" src/lib/cache.ts; grep -n "SITEMAP_COLLECTIONS" -A 8 src/lib/sitemap-data.ts | grep -iE "subscribers|affiliate-clicks|lead-magnets"; sed -n '/mcpPlugin(/,/^    }),$/p' src/payload.config.ts | grep -iE "subscribers|affiliate-clicks|lead-magnets"; test -f .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate02-findings.md && grep -qE "GATE-02: (PASS|FAIL)" .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate02-findings.md</automated>
    <fails_when>el grep de `SITEMAP_COLLECTIONS` o del bloque `mcpPlugin` encuentra `subscribers`/`affiliate-clicks`/`lead-magnets` (deberia dar 0 coincidencias), o `50-gate02-findings.md` no existe o no contiene un veredicto explicito `GATE-02: PASS` o `GATE-02: FAIL`.</fails_when>
  </verify>
  <done>`50-gate02-findings.md` existe con el crawl completo consolidado, el grep de overrideAccess con cada excepcion justificada por su comentario, la confirmacion de que las 3 colecciones privadas no estan en sitemap ni MCP, y un veredicto unico GATE-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Claude (agente de crawl) -> https://juan-tech.com | Requests HTTP publicas no autenticadas contra rutas y contra `/go/[slug]` |
| Claude -> Postgres de produccion (via tunnel SSH) | Consulta de solo lectura a `affiliate-links`/`posts` para resolver que contenido real existe antes de crawlear |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-03 | Information Disclosure | `/go/[slug]` enumeracion de slugs | low | accept | Ya mitigado en Phase 47 (slug inexistente y slug inactivo devuelven 404 identico); esta fase solo re-confirma en runtime, no introduce superficie nueva |
| T-50-04 | Information Disclosure | Credenciales de la Postgres de produccion via `scripts/db/tunnel.sh` | medium | mitigate | Nunca imprimir `DATABASE_URI` completo ni password en la salida de ningun comando de esta fase -- solo host/puerto/nombre de base, mismo patron que `scripts/db/04-which-database.ts`; confirmar la base ANTES de leer nada (precondition de Task 1) |
| T-50-05 | Information Disclosure | `subscribers`/`affiliate-clicks`/`lead-magnets` expuestos via sitemap o MCP | low | accept | Ya mitigado por diseno en Phases 46/48/49 (excluidas explicitamente); esta fase re-confirma por grep, no introduce cambio |

</threat_model>

<verification>
- `50-affiliate-crawl.json` existe con `contentAudit` resuelto (program=amazon activo si/no, post con affiliate-inline si/no) y el crawl completo de todas las superficies verificables
- `50-gate02-findings.md` existe con veredicto explicito `GATE-02: PASS` o `GATE-02: FAIL`
- Cero anchors de afiliado sin `rel="sponsored nofollow noopener"` en /stack (es+en)
- overrideAccess:false confirmado (o exencion documentada) en cada fetcher de `src/lib/cache.ts`
- subscribers/affiliate-clicks/lead-magnets ausentes de SITEMAP_COLLECTIONS y mcpPlugin
</verification>

<success_criteria>
Existe evidencia real de HTML/HTTP (no lectura de codigo) de que las 5 aserciones de GATE-02 se cumplen, con cualquier gap de cobertura por falta de contenido real documentado explicitamente en vez de omitido. `50-gate02-findings.md` deja un veredicto binario listo para que 50-03 lo consuma.
</success_criteria>

<output>
Create `.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-02-SUMMARY.md` when done
</output>
