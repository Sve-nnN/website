---
phase: 50-gate-de-cierre-de-monetizaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-tracer.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-run1.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-run2.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-run3.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-baseline.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-content.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-headlinks.json
  - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate01-findings.md
autonomous: true
requirements: [GATE-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Las 14 rutas del baseline de Phase 45 fueron re-medidas contra produccion viva (juan-tech.com) con el mismo procedimiento: Lighthouse mobile mediana de 3 (o 5 si el spread supera 15 puntos), H1/JSON-LD, canonical/hreflang"
    - "Ninguna de las 14 rutas cae mas de 5 puntos de performance ni cruza de banda de CWV respecto a lh-phase45-baseline.json, y el delta de CLS es exactamente 0.00 en las 14 -- cualquier excepcion real queda documentada, nunca descartada en silencio"
    - "El JS de cliente agregado por el milestone (Phases 46-49) esta confirmado en 0 KB por ausencia estructural de la directiva 'use client' en los 10 archivos de superficie de afiliacion/email, no por un diff de bundle (el rango de commits del milestone esta contaminado por ~200 commits no relacionados)"
    - "Los 2 hallazgos ya documentados en 45-REGRESSION-BASELINE.md (H1 en espanol de /en, canibalizacion /blog/pilas-y-colas) NO aparecen re-flaggeados como regresiones nuevas en 50-gate01-findings.md"
  artifacts:
    - .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-baseline.json
    - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-content.json
    - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-headlinks.json
    - .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate01-findings.md
  key_links:
    - "lh-phase50-baseline.json (mediana por ruta/metrica) diffea 1:1 contra lh-phase45-baseline.json usando el mismo shape plano keyed-por-path"
    - "50-post-content.json / 50-post-headlinks.json diffean 1:1 contra 45-baseline-content.json / 45-baseline-headlinks.json"
    - "50-gate01-findings.md consolida los 3 diffs + el grep de 'use client' en un veredicto unico de GATE-01, consumido como input directo por 50-03 Task 1"
---

<objective>
Re-medir las 14 rutas criticas del milestone (Home, 4 landings de servicio, 2 geo, ambos locales) contra produccion viva con el mismo procedimiento exacto de la Phase 45 (mismos 3 scripts, mismas rutas, mismo umbral de diff), y confirmar por auditoria estructural que el milestone de monetizacion (Phases 46-49) no agrego JavaScript de cliente. Esto cierra la mitad "performance/SEO no se rompio" de GATE-01.

Purpose: GATE-01 exige probar, con datos reales y no con lectura de codigo, que construir el mecanismo de monetizacion no costo ni un punto de Lighthouse ni un byte de CLS ni JS de cliente medible. Este plan produce esa evidencia.
Output: `lh-phase50-baseline.json`, `50-post-content.json`, `50-post-headlinks.json` (los 3 con el mismo shape que sus contrapartes de Phase 45, diffables archivo-contra-archivo) y `50-gate01-findings.md` (veredicto consolidado de GATE-01, incluyendo el resultado del grep estructural de `'use client'`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md
@.planning/phases/45-baseline-de-regresi-n/lh-phase45-baseline.json
@.planning/phases/45-baseline-de-regresi-n/45-baseline-content.json
@.planning/phases/45-baseline-de-regresi-n/45-baseline-headlinks.json
@.planning/phases/45-baseline-de-regresi-n/45-PATTERNS.md
@.planning/phases/45-baseline-de-regresi-n/45-02-PLAN.md
@.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-RESEARCH.md
@scripts/lighthouse-mobile.mjs
@scripts/capture-service-page-snapshot.mjs
@scripts/capture-head-links-snapshot.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer -- pipeline completo de re-medicion sobre UNA ruta (Home "/")</name>
  <files>.planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-tracer.json</files>
  <precondition>https://juan-tech.com responde 200 (produccion viva en Dokploy) -- confirmar con `curl -sI https://juan-tech.com | head -1` antes de lanzar Lighthouse; si no responde 200, halt (no hay build local que compare, mismo precedente de Phase 45).</precondition>
  <action>
    Correr, contra produccion viva y solo para la ruta "/", los 3 scripts que la fase reusa sin modificar: `scripts/lighthouse-mobile.mjs` (una corrida, `--routes-only /`, salida a `lh-phase50-tracer.json`), `scripts/capture-service-page-snapshot.mjs` (`--routes /`) y `scripts/capture-head-links-snapshot.mjs` (`--routes /`), imprimiendo su salida a stdout sin persistirla todavia (esto es el tracer, no la captura final -- la captura completa de las 14 rutas es Task 2).

    Comparar a mano el resultado de las 3 corridas contra la fila `/` de `lh-phase45-baseline.json`, `45-baseline-content.json` y `45-baseline-headlinks.json`: performance dentro de 5 puntos, LCP/CLS/TBT en la misma banda (o mejor), CLS exactamente 0, mismo H1, mismo tipo de JSON-LD, mismo canonical/hreflang. El objetivo del tracer es probar que la cadena completa fetch -> parse -> comparacion contra baseline funciona de punta a punta para una ruta, con los mismos flags CLI que uso Phase 45 (`--routes-only` para Lighthouse, `--routes` para los otros dos -- no son intercambiables, confirmado en 45-02-PLAN.md), antes de escalar a las 13 rutas restantes en Task 2.

    Si `/` ya muestra una senal de contaminacion por CPU (performance sospechosamente bajo, ver Pitfall 4 de 50-RESEARCH.md: la mediana de "/" cerca de 47 en vez de 85-90 es la senal), repetir la corrida de Lighthouse una vez mas antes de seguir -- no continuar a Task 2 con una medicion contaminada como referencia.
  </action>
  <verify>
    <automated>node scripts/lighthouse-mobile.mjs --base-url https://juan-tech.com --routes-only / --out .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-tracer.json && node -e "const r=require('./.planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-tracer.json'); const row=r['/']||r[Object.keys(r)[0]]; if(!row || typeof row.performance !== 'number') process.exit(1); console.log('tracer performance:', row.performance)"</automated>
    <fails_when>el comando de Lighthouse termina con exit code distinto de 0, o `lh-phase50-tracer.json` no existe, o la fila de "/" no tiene un `performance` numerico.</fails_when>
  </verify>
  <done>`lh-phase50-tracer.json` existe con un performance score real para "/", y la comparacion manual contra las 3 filas "/" del baseline de Phase 45 (Lighthouse + H1/JSON-LD + canonical/hreflang) confirma que el shape de salida de los 3 scripts sigue siendo diffable automaticamente contra los archivos de Phase 45.</done>
</task>

<task type="auto">
  <name>Task 2: Captura completa -- Lighthouse (14 rutas, mediana) + H1/JSON-LD + canonical/hreflang</name>
  <files>.planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-run1.json, .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-run2.json, .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-run3.json, .planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-baseline.json, .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-content.json, .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-headlinks.json</files>
  <read_first>.planning/phases/45-baseline-de-regresi-n/45-02-PLAN.md (Task 1 y 2 completos -- contienen el comando exacto de escalado a 5 corridas y el one-liner `node -e` de mediana que hay que replicar verbatim contra los archivos de esta fase)</read_first>
  <action>
    Correr `scripts/lighthouse-mobile.mjs --base-url https://juan-tech.com --routes-only "&lt;las 14 rutas separadas por coma&gt;" --out .../lh-phase50-run{1,2,3}.json` tres veces (las 14 rutas son: /,/en,/servicios/seo-technical-audit,/servicios/seo-consulting,/servicios/fullstack-development,/servicios/ai-seo-geo,/en/services/seo-technical-audit,/en/services/seo-consulting,/en/services/fullstack-development,/en/services/ai-seo-geo,/seo-tecnico-madrid,/seo-tecnico-lima,/en/seo-tecnico-madrid,/en/seo-tecnico-lima). Para cada ruta calcular `max - min` de performance sobre las 3 lecturas; si supera 15 puntos en alguna, correr esa ruta 2 veces mas a `lh-phase50-run4-escalated.json`/`run5-escalated.json` (mismo patron de escalado que 45-02-PLAN.md Task 1, incluida la nota de contencion de CPU del Pitfall 4 de 50-RESEARCH.md) y usar la mediana de 5 para esa ruta.

    Computar `lh-phase50-baseline.json` con el mismo one-liner `node -e` de mediana que uso 45-02-PLAN.md Task 2 (mediana de 3, o de 5 para rutas escaladas, sobre las 7 metricas: performance, accessibility, best-practices, seo, lcpMs, cls, tbtMs), mismo shape plano keyed-por-path que `lh-phase45-baseline.json`.

    Correr `scripts/capture-service-page-snapshot.mjs --base-url https://juan-tech.com --routes "&lt;las mismas 14 rutas&gt;" --out .../50-post-content.json` y `scripts/capture-head-links-snapshot.mjs --base-url https://juan-tech.com --routes "&lt;las mismas 14 rutas&gt;" --out .../50-post-headlinks.json`, exactamente como los invoco 45-01-PLAN.md.
  </action>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/50-gate-de-cierre-de-monetizaci-n/lh-phase50-baseline.json'); const c=require('./.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-content.json'); const h=require('./.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-post-headlinks.json'); const routes=['/','/en','/servicios/seo-technical-audit','/servicios/seo-consulting','/servicios/fullstack-development','/servicios/ai-seo-geo','/en/services/seo-technical-audit','/en/services/seo-consulting','/en/services/fullstack-development','/en/services/ai-seo-geo','/seo-tecnico-madrid','/seo-tecnico-lima','/en/seo-tecnico-madrid','/en/seo-tecnico-lima']; const missing=routes.filter(r=>!(r in b)); if(missing.length) { console.error('faltan en baseline:', missing); process.exit(1);} console.log('14/14 rutas presentes en los 3 artefactos')"</automated>
    <fails_when>falta alguna de las 14 rutas en cualquiera de los 3 archivos, o algun `--out` de Lighthouse termino con exit code distinto de 0.</fails_when>
  </verify>
  <done>`lh-phase50-run1/2/3.json` (+ escalados si aplico) existen con las 14 rutas cada uno; `lh-phase50-baseline.json` tiene la mediana de las 7 metricas para las 14 rutas, mismo shape que `lh-phase45-baseline.json`; `50-post-content.json` y `50-post-headlinks.json` tienen H1/JSON-LD y canonical/hreflang de las 14 rutas, mismo shape que sus contrapartes de Phase 45.</done>
</task>

<task type="auto">
  <name>Task 3: Diff programatico contra el baseline de Phase 45 + auditoria estructural de JS de cliente + veredicto GATE-01</name>
  <files>.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate01-findings.md</files>
  <read_first>.planning/phases/45-baseline-de-regresi-n/45-REGRESSION-BASELINE.md (seccion de hallazgos ya documentados -- Pitfall 5 de 50-RESEARCH.md: no re-flaggear /en H1 en espanol ni la canibalizacion de /blog/pilas-y-colas como regresiones nuevas)</read_first>
  <action>
    Escribir y correr un diff programatico (`node -e` o script ad-hoc, no persistido como archivo versionado -- mismo precedente de "one-liner, no script" que uso Phase 45 para la mediana) que compare, ruta por ruta: `lh-phase50-baseline.json` contra `lh-phase45-baseline.json` (performance: flag si cae mas de 5 puntos; LCP/CLS/TBT: flag si cruza a una banda peor que las bandas ya fijadas en 50-RESEARCH.md Pattern 1 -- LCP good&lt;=2500/needs-improvement&lt;=4000/poor; CLS good&lt;=0.1/needs-improvement&lt;=0.25/poor; TBT good&lt;=200/needs-improvement&lt;=600/poor; ademas, el criterio propio de GATE-01 es mas estricto que la banda: cualquier CLS post-milestone distinto de 0.00 en cualquiera de las 14 rutas es FAIL, aunque siga en banda "good"); `50-post-content.json` contra `45-baseline-content.json` (mismo H1, mismo tipo de JSON-LD por ruta); `50-post-headlinks.json` contra `45-baseline-headlinks.json` (mismo canonical, mismo set de hreflang por ruta).

    Cruzar cualquier hallazgo de contenido contra la seccion de hallazgos ya documentados de `45-REGRESSION-BASELINE.md` antes de calificarlo de regresion nueva -- los 2 ya conocidos (H1 en espanol de `/en`, canibalizacion `/blog/pilas-y-colas`) son estado pre-existente, no defectos de este milestone.

    Correr el grep estructural de `'use client'` sobre los 10 archivos de superficie de afiliacion/email construidos en Phases 46-49 (ver comando de verify) y documentar el resultado como "0 KB de JS de cliente agregado, por construccion -- verificado por ausencia de 'use client' en los 10 archivos, no por diff de bundle (el rango de commits 60700ac..HEAD esta contaminado por ~200 commits no relacionados con el milestone, ver 50-RESEARCH.md Pitfall 1)".

    Escribir `50-gate01-findings.md` con: tabla de las 14 rutas x 7 metricas con delta vs. baseline y veredicto por fila, el resultado del grep de JS de cliente, y un veredicto unico GATE-01: PASS o FAIL con la lista exacta de rutas/metricas que fallaron si las hay.
  </action>
  <verify>
    <automated>grep -rl "use client" src/components/AffiliateLink.tsx src/components/AffiliateDisclosure.tsx src/components/AffiliateDisclosureFrame.tsx src/components/ToolCard.tsx src/components/GearCard.tsx src/components/AffiliateInlineCard.tsx src/blocks/AuditorHighlight/Component.tsx src/blocks/AuditorCallout/Component.tsx src/blocks/EmailCaptureBlock/Component.tsx "src/app/go/[slug]/route.ts" ; test -f .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate01-findings.md && grep -qE "GATE-01: (PASS|FAIL)" .planning/phases/50-gate-de-cierre-de-monetizaci-n/50-gate01-findings.md</automated>
    <fails_when>el grep de `'use client'` encuentra alguna coincidencia en los 10 archivos (indicaria que un componente dejo de ser un Server Component), o `50-gate01-findings.md` no existe, o no contiene un veredicto explicito `GATE-01: PASS` o `GATE-01: FAIL`.</fails_when>
  </verify>
  <done>`50-gate01-findings.md` existe con la tabla completa de 14 rutas diffeadas contra Phase 45, el resultado del grep de `'use client'` (esperado: sin coincidencias), y un veredicto unico GATE-01 (PASS o FAIL con detalle exacto de que fallo).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Claude (agente de medicion) -> https://juan-tech.com | Requests HTTP no autenticadas a rutas publicas; ninguna credencial cruza este limite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Denial of Service (integridad de la medicion) | Lighthouse corriendo en la laptop local | medium | mitigate | Mediana de 3 (o 5 si spread>15), corridas crudas conservadas, regla de re-chequeo de contencion de CPU antes de leer un salto como regresion real (Pitfall 4) |
| T-50-02 | Tampering (falso negativo de seguridad) | Grep de `'use client'` sobre los 10 archivos | low | accept | El grep es un chequeo estructural exacto (nombre de archivo + directiva literal), no hay superficie de bypass razonable dentro del alcance de esta fase; si un archivo nuevo se agrega fuera de esta lista queda fuera del scope de GATE-01 por diseno del research |

</threat_model>

<verification>
- `lh-phase50-baseline.json`, `50-post-content.json`, `50-post-headlinks.json` existen con las 14 rutas cada uno
- `50-gate01-findings.md` existe con veredicto explicito `GATE-01: PASS` o `GATE-01: FAIL`
- El grep de `'use client'` sobre los 10 archivos de Phases 46-49 no devuelve coincidencias
- Los 2 hallazgos ya documentados en Phase 45 no aparecen re-flaggeados como nuevos
</verification>

<success_criteria>
Existe evidencia real (no lectura de codigo) de que las 14 rutas no regresaron mas de lo permitido por el ROADMAP Phase 50 criterio 1, y de que el JS de cliente agregado por el milestone es 0 KB por construccion. `50-gate01-findings.md` deja un veredicto binario listo para que 50-03 lo consuma.
</success_criteria>

<output>
Create `.planning/phases/50-gate-de-cierre-de-monetizaci-n/50-01-SUMMARY.md` when done
</output>