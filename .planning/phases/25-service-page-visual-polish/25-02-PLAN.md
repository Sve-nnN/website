---
phase: 25-service-page-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/blocks/ServiceScopeCard/config.ts
  - src/blocks/ServiceScopeCard/Component.tsx
  - src/blocks/RelatedCaseStudyBlock/config.ts
  - src/blocks/RelatedCaseStudyBlock/Component.tsx
  - src/collections/Pages/index.ts
  - src/blocks/RenderBlocks.tsx
  - messages/en.json
  - messages/es.json
  - src/payload-types.ts
  - src/migrations/index.ts
autonomous: true
requirements: [SVCPOL-03, SVCPOL-04]

must_haves:
  truths:
    - "A ServiceScopeCard Payload block exists and renders alcance/resultado/tiempo without ever showing a price"
    - "A RelatedCaseStudyBlock Payload block exists, generically modeled (relationship, not hardcoded slug), and falls back to the most recent case study when unconfigured"
    - "Both new blocks are registered purely additively — zero existing lines changed in Pages/index.ts or RenderBlocks.tsx"
  artifacts:
    - path: "src/blocks/ServiceScopeCard/config.ts"
      provides: "title/scope/outcome/timeline fields (localized), slug 'serviceScopeCard'"
    - path: "src/blocks/ServiceScopeCard/Component.tsx"
      provides: "Card-based render, no price/$ ever"
    - path: "src/blocks/RelatedCaseStudyBlock/config.ts"
      provides: "title/framingText/caseStudy(relationship) fields (localized), slug 'relatedCaseStudyBlock'"
    - path: "src/blocks/RelatedCaseStudyBlock/Component.tsx"
      provides: "Reuses CaseStudyCard, overrideAccess:false, null-safe fallback"
  key_links:
    - from: "src/collections/Pages/index.ts"
      to: "src/blocks/ServiceScopeCard/config.ts + src/blocks/RelatedCaseStudyBlock/config.ts"
      via: "blocks array import + entry"
      pattern: "ServiceScopeCard|RelatedCaseStudyBlock"
    - from: "src/blocks/RenderBlocks.tsx"
      to: "ServiceScopeCardComponent + RelatedCaseStudyBlockComponent"
      via: "blockComponents map entries 'serviceScopeCard'/'relatedCaseStudyBlock'"
      pattern: "serviceScopeCard:|relatedCaseStudyBlock:"
---

<objective>
Build the 2 new Payload blocks specified in `25-UI-SPEC.md` (`ServiceScopeCard`, `RelatedCaseStudyBlock`), register them additively (same protocol as Phase 24's `ServicesShowcase`), and apply the resulting schema migration — additive-only, new tables, zero touch on existing columns.

Purpose: These 2 blocks are the only new schema surface in Phase 25. Everything else in the phase reuses existing blocks/config unchanged (per UI-SPEC's Regression Guardrails section).
Output: Two working, typed, additively-registered Payload blocks + an applied additive migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-service-page-visual-polish/25-CONTEXT.md
@.planning/phases/25-service-page-visual-polish/25-UI-SPEC.md
@.planning/phases/24-servicesshowcase-en-home/24-01-SUMMARY.md
@src/blocks/ServicesShowcase/config.ts
@src/blocks/ServicesShowcase/Component.tsx
@src/blocks/ResultsSection/Component.tsx
@src/components/CaseStudyCard.tsx
@src/components/ui/card.tsx
@src/blocks/FeaturedCaseStudiesBlock/Component.tsx
@src/collections/CaseStudies/index.ts
@src/lib/services-data.ts
</context>

<interfaces>
Existing registration precedent (`ServicesShowcase`, Phase 24 — follow verbatim):

`src/collections/Pages/index.ts`: import the block config, append to the `blocks:` array inside `content.layout` (currently ends with `ServicesShowcase` — append after it).

`src/blocks/RenderBlocks.tsx`: import the component, append `<slug>: <Component>` to the `blockComponents` map (currently ends with `servicesShowcase: ServicesShowcaseComponent,`).

`CaseStudyCard` component signature (`src/components/CaseStudyCard.tsx`, reuse as-is, do not modify):
```typescript
export function CaseStudyCard({ caseStudy }: { caseStudy: CaseStudy }): JSX.Element
```
Renders client name, title, sector, `heroMetric` (accent color) inside a `Card`/`CardContent`, wrapped in a `Link` to `/case-studies/[slug]`.

`FeaturedCaseStudiesBlockComponent` precedent for locale-aware server components consuming `CaseStudy`:
```typescript
import { getLocale } from 'next-intl/server'
const locale = (await getLocale()) as 'en' | 'es'
const payload = await getPayload({ config })
```

`next-intl` message-namespace precedent (`ServicesShowcaseComponent`):
```typescript
import { getTranslations } from 'next-intl/server'
const t = await getTranslations('servicesShowcase')
t('cta') // reads messages/{locale}.json -> servicesShowcase.cta
```

`CaseStudies` collection relevant fields (`src/collections/CaseStudies/index.ts`): `title` (localized), `heroMetric` (localized), `sector` (localized), `client` (relationship to `clientes`), `slug`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build both blocks (config + component) and register additively</name>
  <read_first>.planning/phases/25-service-page-visual-polish/25-UI-SPEC.md (sections "New Block 1: Scope Card" and "New Block 2: Related Case Study"), src/blocks/ServicesShowcase/config.ts, src/blocks/ServicesShowcase/Component.tsx</read_first>
  <files>src/blocks/ServiceScopeCard/config.ts, src/blocks/ServiceScopeCard/Component.tsx, src/blocks/RelatedCaseStudyBlock/config.ts, src/blocks/RelatedCaseStudyBlock/Component.tsx, src/collections/Pages/index.ts, src/blocks/RenderBlocks.tsx, messages/en.json, messages/es.json, src/payload-types.ts</files>
  <action>
**`src/blocks/ServiceScopeCard/config.ts`** — `Block` with `slug: 'serviceScopeCard'`, `interfaceName: 'ServiceScopeCardBlock'`, fields exactly per UI-SPEC: `title` (text, localized, optional), `scope` (textarea, localized, required), `outcome` (textarea, localized, required), `timeline` (text, localized, required — short freeform string like "2-3 semanas", NOT a number field).

**`src/blocks/ServiceScopeCard/Component.tsx`** — synchronous server component (no async/no DB call needed, all fields arrive pre-localized via the page's own locale-scoped `payload.find`). Import `getTranslations` from `next-intl/server` (async, so component must be `async function`) for the 3 micro-labels + title fallback (message keys below). Render: `Container className="py-12"` > single `Card` (`max-w-2xl mx-auto` on desktop, full width mobile) > `CardContent className="p-6"` > `title ?? t('title')` as `font-heading text-heading` > 3 stacked rows (NOT columns/grid — stacking avoids any pricing-table resemblance), each `text-label uppercase tracking-wide opacity-70` micro-label (`t('scopeLabel')`/`t('outcomeLabel')`/`t('timelineLabel')`) above a `text-body` value paragraph. The `timeline` value line only gets `text-primary font-semibold` (no `text-display`/`text-heading` sizing — it's a phrase, not a metric number, per UI-SPEC). Hard rule enforced in code and comment: never render `$` or any currency/price string anywhere in this component.

**`src/blocks/RelatedCaseStudyBlock/config.ts`** — `Block` with `slug: 'relatedCaseStudyBlock'`, `interfaceName: 'RelatedCaseStudyBlockBlock'`, fields exactly per UI-SPEC: `title` (text, localized, optional), `framingText` (textarea, localized, optional), `caseStudy` (relationship to `case-studies`, `hasMany: false`, `required: false`).

**`src/blocks/RelatedCaseStudyBlock/Component.tsx`** — `async` server component. Pattern: `getPayload({config})` + `getLocale()` from `next-intl/server` (mirror `FeaturedCaseStudiesBlockComponent`). Resolve the linked case study: if `caseStudy` prop is a populated object use it directly; if it's a bare id (number/string), `payload.findByID({ collection: 'case-studies', id, depth: 1, locale, overrideAccess: false })` (SECURITY: same `overrideAccess: false` discipline as `services-data.ts`'s 24-REVIEW WR-02 fix — Local API bypasses `read: authenticatedOrPublished` by default, an unpublished case study must never leak here). If no `caseStudy` configured (empty/unresolvable), fall back to `payload.find({ collection: 'case-studies', limit: 1, sort: '-createdAt', depth: 1, locale, overrideAccess: false })`, take `docs[0]`. If still nothing resolves, `return null` (same empty-state contract as `ClientLogosBlockComponent`). Render: `Container className="py-12"` > `title ?? t('title')` as `font-heading text-heading` > `framingText` (if present) as `text-body text-muted-foreground italic` > `<div className="max-w-md"><CaseStudyCard caseStudy={resolved} /></div>` — reuse `CaseStudyCard` verbatim, do not build a bespoke card. Add a `relatedCaseStudyBlock` next-intl namespace with `title` fallback key for when Payload's `title` field is empty.

**Additive registration** (append-only, follow the exact `ServicesShowcase` precedent from 24-01-SUMMARY.md):
- `src/collections/Pages/index.ts`: add 2 imports (`ServiceScopeCard` from `@/blocks/ServiceScopeCard/config`, `RelatedCaseStudyBlock` from `@/blocks/RelatedCaseStudyBlock/config`), append both to the `blocks:` array after `ServicesShowcase`.
- `src/blocks/RenderBlocks.tsx`: add 2 imports (`ServiceScopeCardComponent`, `RelatedCaseStudyBlockComponent`), append `serviceScopeCard: ServiceScopeCardComponent` and `relatedCaseStudyBlock: RelatedCaseStudyBlockComponent` to `blockComponents` after `servicesShowcase`.

**Messages** — add to both `messages/en.json` and `messages/es.json`, matching the existing `servicesShowcase` namespace shape:
```
"serviceScopeCard": { "title": "Alcance de este servicio" | "Service scope", "scopeLabel": "Qué incluye" | "What's included", "outcomeLabel": "Qué vas a lograr" | "What you'll get", "timelineLabel": "Tiempo estimado" | "Estimated timeline" }
"relatedCaseStudyBlock": { "title": "Un caso real de cómo trabajo" | "A real example of how I work" }
```

Run `npx payload generate:importmap` (no-op for admin, both are server components with no admin-side custom UI) then `npx payload generate:types` (adds `ServiceScopeCardBlock`/`ServiceScopeCardBlockSelect`/`RelatedCaseStudyBlockBlock`/`RelatedCaseStudyBlockBlockSelect` to `src/payload-types.ts`). Run `npx tsc --noEmit`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `git diff src/collections/Pages/index.ts src/blocks/RenderBlocks.tsx` shows ONLY added lines (no `-` lines besides diff context)
    - `grep -c '\$' src/blocks/ServiceScopeCard/Component.tsx` returns 0 (never renders a price glyph)
    - `grep -n "overrideAccess: false" src/blocks/RelatedCaseStudyBlock/Component.tsx` matches at least once
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && git diff --unified=0 src/collections/Pages/index.ts src/blocks/RenderBlocks.tsx | grep -c '^-[^-]' | grep -qx 0 && echo "PASS: additive-only diff confirmed"</automated>
  </verify>
  <done>Both blocks compile, are registered purely additively, and payload-types.ts includes both new block interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate, read, and apply the additive migration</name>
  <read_first>The generated migration file itself (read in full before running `payload migrate`) — this is the hard gate for this task, not optional</read_first>
  <files>src/migrations/*, src/migrations/index.ts</files>
  <action>
Run `npx payload migrate:create`. This generates a new migration file under `src/migrations/`.

**HARD CONSTRAINT (non-negotiable, per CLAUDE.md Database Safety + this phase's explicit instruction):** Read the generated SQL in full before running anything. It must contain ONLY:
- `CREATE TABLE` statements for new tables (`pages_blocks_service_scope_card`, `pages_blocks_service_scope_card_locales`, `_pages_v_blocks_service_scope_card`, `_pages_v_blocks_service_scope_card_locales`, and the equivalent 4 tables for `relatedCaseStudyBlock`)
- `ALTER TABLE ... ADD CONSTRAINT` statements adding new foreign keys that reference existing `pages`/`_pages_v`/`case_studies` id columns (referencing, not altering, those columns)
- `CREATE INDEX` statements on the new tables

If the SQL contains ANY `DROP COLUMN`, `DROP TABLE`, `ALTER TABLE ... ALTER COLUMN`, or any statement touching a pre-existing table's existing column (not just adding a new FK constraint) — STOP immediately. Do NOT run `payload migrate`. Report the exact offending statement to Juan and wait for his named approval before proceeding (per the project's hard Database Safety rule — the Phase 19 CTA-localization incident is the cautionary precedent, do not repeat it).

If the SQL is confirmed additive-only, run `npx payload migrate` against the real Neon Postgres. No named approval needed for a confirmed additive-only migration, per the project's relaxed rule for additive/non-destructive changes.

After applying, run `npx payload migrate:create` again — it should report "No schema changes detected", confirming the applied migration fully matches the current block configs (no drift).
  </action>
  <acceptance_criteria>
    - Migration file read in full, confirmed additive-only (documented in the task's completion note which statements were present)
    - `npx payload migrate` output shows `Migrated: <name>` with no errors
    - A second `npx payload migrate:create` reports no pending schema changes
  </acceptance_criteria>
  <verify>
    <automated>npx payload migrate:create 2>&1 | grep -qi "no schema changes\|no changes detected" && echo "PASS: schema matches config, migration already applied"</automated>
  </verify>
  <done>Migration generated, read, confirmed additive-only, and applied cleanly against the real production Neon Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload Local API -> Postgres | `RelatedCaseStudyBlockComponent`'s fallback query reads real case-study content; must not leak unpublished drafts |
| Migration script -> production Neon Postgres | Schema-altering SQL runs directly against the only database this project has (no staging) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-25-03 | Information Disclosure | `RelatedCaseStudyBlockComponent` case-study fallback query | mitigate | `overrideAccess: false` on both the `findByID` and `find` calls, matching the 24-REVIEW WR-02 precedent — Local API's default `overrideAccess: true` would otherwise leak unpublished case studies publicly |
| T-25-04 | Tampering | Generated migration SQL applied to production DB with no staging environment | mitigate | Task 2's hard read-before-apply gate: migration SQL is read in full and confirmed additive-only (no DROP/ALTER COLUMN on existing tables) before `payload migrate` runs; any violation stops the task and requires Juan's named approval |
| T-25-05 | Tampering | New relationship field `caseStudy` on `relatedCaseStudyBlock` referencing `case-studies` | accept | Field is editor-only (admin UI, `authenticated` access), not exposed to any public write path — same trust level as every other relationship field already in the schema |
</threat_model>

<verification>
`npx tsc --noEmit` passes; both blocks are additively registered (diff-verified); migration applied and confirmed schema-matching via a second `migrate:create` reporting no changes.
</verification>

<success_criteria>
- `ServiceScopeCard` and `RelatedCaseStudyBlock` exist as fully-typed, registered Payload blocks
- Migration applied against the real Neon Postgres, confirmed additive-only by direct read before applying
- Zero existing lines modified in `Pages/index.ts` or `RenderBlocks.tsx`
</success_criteria>

<output>
Create `.planning/phases/25-service-page-visual-polish/25-02-SUMMARY.md` when done
</output>
