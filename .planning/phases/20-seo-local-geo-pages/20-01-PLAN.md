---
phase: 20-seo-local-geo-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx
  - src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx
  - scripts/seed-phase20-data/types.ts
  - scripts/seed-phase20-data/copy.ts
  - scripts/seed-phase20-geo-pages.ts
autonomous: true
requirements: [SEO-LOCAL-01, SEO-LOCAL-02]

must_haves:
  truths:
    - "Visiting /seo-tecnico-lima and /en/seo-tecnico-lima returns 200 with content specific to Juan's real Lima presence (UPC, DinoRANK/Arianna Lupi workshop) — not generic city-swapped copy"
    - "Visiting /seo-tecnico-madrid and /en/seo-tecnico-madrid returns 200 with content specific to the Spain market angle (remote specialist, real ES keyword data), explicitly not claiming a physical Madrid office"
    - "The two pages' 'qué incluye'/context sections are NOT interchangeable — each has at least one argument/section absent from the other (Lima: physical/community presence; Madrid: remote-market-fluency)"
    - "Both pages have their own H1 (via Hero block) and meta title/description (via plugin-seo, already wired to 'pages' collection since Phase 1) distinct from each other and the rest of the site"
  artifacts:
    - path: "scripts/seed-phase20-data/copy.ts"
      provides: "limaPageCopy, madridPageCopy: BilingualServiceCopy-shaped content (reusing Phase 19's types.ts shape)"
      contains: "limaPageCopy"
    - path: "scripts/seed-phase20-geo-pages.ts"
      provides: "Idempotent seed script assembling Hero/Content/FAQ/CallToAction blocks for both geo pages, reusing Phase 19's id-reuse discipline"
      contains: "upsertPage"
    - path: "src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx"
      provides: "Static single-slug route (like /privacy), no manual H1"
      contains: "notFound"
  key_links:
    - from: "scripts/seed-phase20-geo-pages.ts"
      to: "scripts/seed-phase19-data/types.ts"
      via: "reuses ServiceCopy/FaqItem shape (structurally identical H1->context->includes->process->FAQ->CTA pattern)"
      pattern: "ServiceCopy"
---

<objective>
Create 2 new `pages` docs ("SEO técnico en Lima", "SEO técnico en Madrid") with genuinely
differentiated bilingual content, each reachable at a single fixed URL segment shared across both
locales (`/seo-tecnico-lima`, `/seo-tecnico-madrid` + `/en/` prefix), satisfying SEO-LOCAL-01/02.

Purpose: close the 2 remaining v1.4 geo-positioning gaps identified by the competitive audit,
without falling into the templated find-replace-city pattern Juan explicitly rejected — Lima leans
on Juan's real physical presence and local community involvement (UPC, DinoRANK workshop with
Arianna Lupi), Madrid leans on an honest remote-specialist framing backed by real ES-market keyword
data, never claiming a Madrid office that doesn't exist.

Output: 2 route files (static single-slug pattern, mirroring `/privacy`), a content module with
fully differentiated bilingual copy for both pages, and an idempotent seed script that assembles
Hero/Content/FAQ/CallToAction blocks per locale (same pattern as Phase 19's
`seed-phase19-service-pages.ts`, reusing its id-reuse-across-locales discipline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-seo-local-geo-pages/20-CONTEXT.md
@.planning/REQUIREMENTS.md
@research/keyword-research/KEYWORD-RESEARCH.md
@scripts/seed-author-eeat.ts

@src/app/(frontend)/[locale]/privacy/page.tsx
@scripts/seed-phase19-data/types.ts
@scripts/seed-phase19-service-pages.ts
@src/blocks/CallToAction/config.ts

<interfaces>
<!-- privacy/page.tsx is the exact static-single-slug pattern to mirror (NOT case-studies'
dynamic [slug] pattern, NOT Phase 19's dual-segment services pattern — this phase has 2 known,
fixed slugs and only needs 2 thin route files, no lib registry/allowlist layer):

async function getPage(locale: string) {
  const payload = await getPayload({ config })
  const { docs } = await payload.find({ collection: 'pages', where: { slug: { equals: 'privacy' } }, locale: locale as 'es' | 'en', limit: 1 })
  return docs[0]
}
export async function generateMetadata({ params }) {
  const { locale } = await params
  const doc = await getPage(locale)
  const meta = doc?.meta
  return { title: meta?.title ?? doc?.title ?? fallback, description: meta?.description ?? '' }
}
export default async function PrivacyPage({ params }) {
  const { locale } = await params
  const doc = await getPage(locale)
  if (!doc) notFound()
  return (<main><RenderBlocks blocks={doc.content?.layout ?? []} /></main>)
}

IMPORTANT: no manual <h1> and no <Container> wrapper (same rule as Phase 19 — the seeded Hero block
owns the real H1 and its own Container).

CallToAction.richText is ALREADY localized (Phase 19 fixed this with migration
20260712_202954_phase19_calltoaction_localized) — do NOT propose a new migration for it. Confirm
via `grep -n "localized" src/blocks/CallToAction/config.ts` before writing any block-assembly code,
but no schema change should be needed anywhere in this phase (Hero/Content/FAQ/CallToAction are all
already correctly localized as of Phase 19's fix).

scripts/seed-phase19-data/types.ts's ServiceCopy shape (reused here structurally, not imported
directly, to keep this phase's content module self-contained per project convention of one
seed-phase*-data folder per phase):

interface ServiceCopy {
  slug: string
  hero: { title: string; subtitle: string }
  includes: { title: string; paragraphs: string[] }
  process: { title: string; paragraphs: string[] }
  faqs: { question: string; answer: string }[]
  ctaText: string
  ctaLinkLabel: string
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content module — Lima + Madrid bilingual copy</name>
  <files>scripts/seed-phase20-data/types.ts, scripts/seed-phase20-data/copy.ts</files>
  <read_first>
    scripts/seed-author-eeat.ts,
    research/keyword-research/KEYWORD-RESEARCH.md,
    scripts/seed-phase19-data/types.ts
  </read_first>
  <action>
    Create `scripts/seed-phase20-data/types.ts` — same shape as Phase 19's `ServiceCopy`/
    `FaqItem`/`Locale`/`BilingualServiceCopy` types (self-contained copy, not a cross-phase import,
    per project convention of one seed-data folder per phase).

    Create `scripts/seed-phase20-data/copy.ts` exporting `limaPageCopy: BilingualServiceCopy` and
    `madridPageCopy: BilingualServiceCopy` (slugs `seo-tecnico-lima` / `seo-tecnico-madrid`).

    Lima copy per D-04: hero subtitle frames Juan's real Lima base. `includes`/`process` reference
    concrete local facts — UPC education, the real DinoRANK/Arianna Lupi 4-hour SEO+IA workshop (18
    attendees, co-organized with Lm Marketing) — framed as evidence of local community credibility
    combined with global-caliber technical skill (not a generic "local SEO agency" pitch, and NOT
    the local-SMB/dentist framing the audit flagged as wrong for Juan's ICP). FAQs: 3-5 real
    questions a Lima-based prospect would ask.

    Madrid copy per D-05: hero subtitle honestly frames Juan as a REMOTE specialist serving the
    Spain market — explicitly state he works remotely, do NOT imply a physical Madrid office.
    `includes`/`process` reference the real ES-market keyword data from KEYWORD-RESEARCH.md (e.g.
    "seo técnico" 260 vol/mes, €3.22 CPC) as evidence Spain is a genuine commercial market for
    Spanish-language technical SEO, and frame remote-specialist as an advantage (senior technical
    depth without local-agency overhead) rather than a limitation. FAQs: 3-5 real questions,
    including one addressing "¿trabajás físicamente en Madrid?" answered honestly.

    Both: no pricing (site-wide convention from Phase 19), zero placeholder text, `ctaText`/
    `ctaLinkLabel` pointing toward `/contact`.

    Verify (must_have, success criterion #3): read both files' `includes.paragraphs` +
    `process.paragraphs` back and confirm they share NO paragraph text beyond generic connective
    phrasing — each must contain at least one fact/argument absent from the other (Lima: UPC/
    workshop mentions; Madrid: remote/keyword-data mentions).
  </action>
  <acceptance_criteria>
    - `grep -c "limaPageCopy\|madridPageCopy" scripts/seed-phase20-data/copy.ts` returns >= 2
    - `grep -ic "UPC\|DinoRANK\|Arianna Lupi" scripts/seed-phase20-data/copy.ts` returns >= 2 (Lima-specific facts present)
    - `grep -ic "remoto\|remote" scripts/seed-phase20-data/copy.ts` returns >= 2 (Madrid's honest remote framing present)
    - `grep -iE '(€|\$|USD|EUR|/mes|/month|precio|price|tarifa|pricing)' scripts/seed-phase20-data/copy.ts | grep -viE "sin precio|no price|€3\.22|260 vol"` returns 0 (no pricing for Juan's own services; the one real KEYWORD-RESEARCH.md CPC/volume citation is explicitly allowlisted since it's market evidence, not Juan's own pricing)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Both pages' copy is fully populated, bilingual, structurally H1->context->includes->process->
    FAQ->CTA, and verifiably distinct beyond the city name (Lima's local-presence facts vs. Madrid's
    remote-market-fluency facts do not overlap).
  </done>
</task>

<task type="auto">
  <name>Task 2: Route files — /seo-tecnico-lima and /seo-tecnico-madrid</name>
  <files>
    src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx,
    src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx
  </files>
  <read_first>src/app/(frontend)/[locale]/privacy/page.tsx</read_first>
  <action>
    Create both route files mirroring `privacy/page.tsx`'s static-single-slug pattern exactly
    (`getPage(locale)` querying `pages` collection by fixed slug, `generateMetadata` reading
    `doc.meta`, default export `if (!doc) notFound()` then `<main><RenderBlocks
    blocks={doc.content?.layout ?? []} /></main>` — no manual `<h1>`, no `<Container>` wrapper, same
    rule as Phase 19).
  </action>
  <acceptance_criteria>
    - `grep -c "notFound" "src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx" "src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx"` returns >= 1 for each
    - `grep -c "<h1" "src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx" "src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx"` returns 0 for each
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both route files compile, 404 cleanly until seeded, render via RenderBlocks with no manual H1.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Seed script + run against real DB + end-to-end verification</name>
  <files>scripts/seed-phase20-geo-pages.ts</files>
  <read_first>scripts/seed-phase19-service-pages.ts</read_first>
  <action>
    Create `scripts/seed-phase20-geo-pages.ts`, structurally identical to
    `scripts/seed-phase19-service-pages.ts` (same `lexicalParagraph`/`lexicalWithHeading` helpers,
    same `buildServiceLayout`-equivalent block assembly — Hero/Content/FAQ/CallToAction — same
    `reapplyIds`/`upsertPage` id-reuse discipline), but simpler: only 2 docs, no `SERVICE_SLUGS`
    registry needed (hardcode the 2 slugs directly, since there is no `[slug]` dynamic route in this
    phase to share an allowlist with).

    Confirm via `grep -n "localized" src/blocks/CallToAction/config.ts` that `richText` is already
    `localized: true` (Phase 19 fix) BEFORE writing the block assembly — if this check fails,
    STOP and report a blocker (do not attempt a new migration without following the Database Safety
    approval process in root CLAUDE.md).

    Run the seed script against the real dev DB, twice (idempotency check), then curl-verify:
    - `/seo-tecnico-lima`, `/en/seo-tecnico-lima`, `/seo-tecnico-madrid`, `/en/seo-tecnico-madrid`
      all return 200
    - Lima page contains real facts (grep for "UPC" or "DinoRANK")
    - Madrid page contains the honest remote framing (grep for "remoto"/"remote")
    - Both pages' CTA text is correctly localized per-locale (spot-check ES vs EN CTA button text
      differ, same check that caught Phase 19's CR-01)
    - `npx tsc --noEmit` exits 0; run `npm run build` in the background with a reasonable timeout
      (do not block on it indefinitely — if it does not finish within ~5 minutes, fall back to
      `tsc --noEmit` + a dev-server curl check as sufficient evidence, per the project's current
      "no blocking builds" operating rule)
  </action>
  <acceptance_criteria>
    - Seed script run #1 and #2 both exit 0, no duplicate docs created
    - All 4 URL combinations return 200
    - Lima page content contains "UPC" or "DinoRANK"; Madrid page content contains "remoto"/"remote"
    - ES and EN CTA button text differ between locales on both pages
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Both geo pages exist in the real DB, bilingual, curl-verified live, with distinct per-page
    content and correctly localized CTA text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local seed script -> Postgres | Same trusted, developer-run, idempotent pattern as Phase 19's seed script |
| Public visitor -> rendered geo pages | Unauthenticated readers of admin-authored content, same as every other `pages`-collection route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `<RenderBlocks>` rendering admin-authored blocks | accept | Identical rendering path to every other `pages`-collection route; no new XSS surface |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs |

No new trust boundary. This phase reuses Phase 18/19's already-vetted patterns end to end.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. All 4 URL combinations (2 pages x 2 locales) return 200
3. Lima and Madrid content verifiably distinct beyond city name (grep for phase-specific facts)
4. CTA text correctly localized per locale on both pages (regression check for Phase 19's CR-01 class of bug)
5. No new Postgres migration generated (CallToAction/Hero/Content/FAQ already correctly localized)
</verification>

<success_criteria>
- 2 new `pages` docs exist, bilingual, idempotently seeded
- Both curl-eable at their fixed single-segment URLs in both locales
- Content genuinely differentiated (Lima: physical/community; Madrid: honest remote/market-data)
- Own H1 + meta title/description per page, distinct from each other and the rest of the site
- Zero new schema migrations
</success_criteria>

<output>
Create `.planning/phases/20-seo-local-geo-pages/20-01-SUMMARY.md` when done
</output>
