---
phase: 05-frontend-pages
plan: 08
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - src/app/(frontend)/[locale]/blog/[slug]/page.tsx
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "A blog post detail page shows: hero (title, categories, author byline, date, reading time, hero image with deterministic fallback when heroImage is null), full rich-text content, an expanded E-E-A-T author card, and related posts"
    - "A post with no heroImage (72/72 real migrated posts, per Phase 4) shows the same deterministic per-slug Cloudinary fallback image the old site used, not a broken image or a generic gray box"
    - "Author bio and credentials are visible in the byline, satisfying CONT-02 for every real migrated post"
  artifacts:
    - path: "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
      provides: "Enhanced post detail: hero w/ fallback, reading time, AuthorByline+AuthorCard, RelatedPosts, TableOfContents, Article/BreadcrumbList JSON-LD (existing)"
  key_links:
    - from: "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
      to: "src/lib/heroImageFallback.ts"
      via: "getFallbackHeroImage(doc.slug) when !doc.heroImage"
      pattern: "getFallbackHeroImage"
    - from: "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
      to: "src/components/AuthorCard.tsx"
      via: "renders full author E-E-A-T card below/beside post content"
      pattern: "AuthorCard"
---

<objective>
Upgrade the existing bare-bones `blog/[slug]/page.tsx` (currently just title/excerpt/author-name + Article JSON-LD, from Phase 2's seed) into the full post detail experience per 05-UI-SPEC.md's Page Inventory: hero with deterministic fallback image, author byline, rich-text content, expanded E-E-A-T author card, related posts, table of contents.

Purpose: this is where CONT-02 (author bio+credentials visible in byline) and the hero-image-fallback requirement (STATE.md Phase 4 note — 0/72 real posts have heroImage) actually land for real content, using the shared components built in 05-05.
Output: rewritten post detail page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/STATE.md
@src/app/(frontend)/[locale]/blog/[slug]/page.tsx
@src/lib/heroImageFallback.ts
@src/components/AuthorByline.tsx
@src/components/AuthorCard.tsx
@src/blocks/RelatedPosts/Component.tsx
@src/blocks/TableOfContentsBlock/Component.tsx
@src/components/Prose.tsx
</context>

<interfaces>
Existing `blog/[slug]/page.tsx` (read in full): fetches `posts` by slug via Local API with `depth: 1`, has `generateMetadata` and an `Article` JSON-LD block already correct — preserve both. `Post` doc fields (from `src/collections/Posts/index.ts`): `title`, `excerpt`, `content` (richText), `heroImage` (upload, nullable), `author` (relationship→authors), `categories` (relationship hasMany), `publishedAt`, `slug`.

`getFallbackHeroImage(slug)` (05-05) returns a stable Cloudinary URL string — call it only when `doc.heroImage` is falsy. `AuthorByline`/`AuthorCard` (05-05) both take an `Author` doc prop. `RelatedPosts`/`TableOfContentsBlock` Component.tsx (05-04) are block renderers designed to receive their config fields as props plus (for RelatedPosts) the current post's id/categories — this page is not a Pages-collection doc, so it does not go through `RenderBlocks`; import and call these two components directly with manually-constructed props instead.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hero with fallback, reading time, author byline, rich-text content</name>
  <files>src/app/(frontend)/[locale]/blog/[slug]/page.tsx</files>
  <action>
Rewrite the post detail page body (keep `getPost`, `generateMetadata`, and the existing `Article` JSON-LD untouched): compute `heroImageUrl = doc.heroImage?.url ?? getFallbackHeroImage(doc.slug)` and render it via `next/image` (the Cloudinary loader already configured in `next.config.mjs`'s `remotePatterns`). Compute reading time client-side-free (a simple word-count/200wpm estimate over the plain-text extraction of `doc.content`, done at render time in the server component — no new dependency needed for a rough estimate). Render `doc.categories` as `Badge`s. Render `<AuthorByline author={doc.author} />` in the hero/header area. Serialize `doc.content` (Lexical richText) through `Prose` — if no existing Lexical-to-JSX converter exists yet in this repo, use `@payloadcms/richtext-lexical`'s built-in `RichText` React component (it ships with the package already installed) rather than hand-writing a serializer.
  </action>
  <verify>
    <automated>grep -q "getFallbackHeroImage" src/app/"(frontend)"/"[locale]"/blog/"[slug]"/page.tsx && grep -q "AuthorByline" src/app/"(frontend)"/"[locale]"/blog/"[slug]"/page.tsx && echo OK</automated>
  </verify>
  <done>Post detail page shows a hero image (real or deterministic fallback), reading time, category badges, and the compact author byline.</done>
</task>

<task type="auto">
  <name>Task 2: Expanded AuthorCard, RelatedPosts, TableOfContents</name>
  <files>src/app/(frontend)/[locale]/blog/[slug]/page.tsx</files>
  <action>
Add `<AuthorCard author={doc.author} />` below the post content (the expanded E-E-A-T section CONT-02 requires — full bio, credentials, years experience, social links). Add `<TableOfContentsBlock ... />` (05-04's renderer) fed by headings extracted from `doc.content`, with sensible defaults (sticky, left position, minHeadingLevel 2) since this page isn't composed from a Pages-collection `TableOfContentsBlock` instance — pass static default props here rather than fetching a nonexistent block config. Add `<RelatedPosts posts={undefined} autoSelect currentPostId={doc.id} categories={doc.categories} limit={3} title="Related Posts"/"Artículos relacionados"} />` querying posts sharing at least one category, excluding the current post, via a `payload.find` call in this page (RelatedPosts' Component.tsx expects the current post's id/categories as props per 05-04's interface — this page supplies them).
  </action>
  <verify>
    <automated>grep -q "AuthorCard" src/app/"(frontend)"/"[locale]"/blog/"[slug]"/page.tsx && grep -q "RelatedPosts\|TableOfContents" src/app/"(frontend)"/"[locale]"/blog/"[slug]"/page.tsx && echo OK</automated>
  </verify>
  <done>Post detail page shows the expanded AuthorCard, a working related-posts section using real category overlap, and a table of contents derived from the post's actual headings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Lexical richText → rendered HTML | Editor-authored content only, rendered via the official `RichText` component (no raw `dangerouslySetInnerHTML`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | Rich text rendering | mitigate | Use `@payloadcms/richtext-lexical`'s official `RichText` component rather than `dangerouslySetInnerHTML` with a hand-rolled string converter |
</threat_model>

<verification>
Visit any of the 72 real migrated post slugs in both locales; confirm a hero image renders (fallback, since none have real heroImage), author byline+card show real migrated author data, related posts show actual category-overlapping posts, and reading time/TOC render without errors.
</verification>

<success_criteria>
- [ ] Hero image (real or deterministic fallback) renders for every post
- [ ] AuthorByline + AuthorCard both render real author E-E-A-T data
- [ ] Related posts and table of contents work against real content
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-08-SUMMARY.md` when done
</output>
