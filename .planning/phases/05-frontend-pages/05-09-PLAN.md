---
phase: 05-frontend-pages
plan: 09
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - src/app/(frontend)/[locale]/case-studies/page.tsx
  - src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
  - src/collections/CaseStudies/index.ts
  - migrations/ (new migration file, if author field missing)
  - scripts/backfill-case-study-author.ts
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "A visitor sees a case studies listing (grid: client, sector, hero metric) in both locales, including a correct empty state given 0 real case studies exist yet"
    - "A case study detail page shows hero, 4 KPI cards, El cliente / El reto / La solución (numbered) / Resultados (before-after) / conclusion, and the author byline with E-E-A-T credentials"
  artifacts:
    - path: "src/app/(frontend)/[locale]/case-studies/page.tsx"
      provides: "New listing route querying case-studies collection"
    - path: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      provides: "Enhanced detail page rendering the full structured KPI/challenge/solution/results model"
  key_links:
    - from: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      to: "src/blocks/ResultsSection/Component.tsx"
      via: "reused for the before/after KPI presentation pattern"
      pattern: "results"
---

<objective>
Build the case studies listing page (missing entirely today) and upgrade the existing bare detail page into the full structured model SCHEMA-04 already built into the `CaseStudies` collection (hero metric, client/sector/period, services, 4 KPIs, clientContext, challenge list, solution steps, results before/after, conclusion, author).

Purpose: `CaseStudies` is the richest data model in this project (SCHEMA-04's explicit differentiator) — this plan is where all of that structured data actually becomes a page a visitor can read. Note real Postgres has 0 case studies today (per 04-VERIFICATION.md/04-07's audit closing Works fold-in at 0) — both pages must degrade correctly to an empty/near-empty state without erroring, since the differentiator's content will be authored by Juan after this phase ships, not fabricated here.
Output: new listing route, rewritten detail route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md
@src/collections/CaseStudies/index.ts
@src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
@src/components/AuthorByline.tsx
@src/components/AuthorCard.tsx
@src/blocks/ResultsSection/Component.tsx
</context>

<interfaces>
`CaseStudies` collection fields (read in full above): `title`, `heroMetric`, `heroSubtitle`, `client` (relationship→clientes), `sector`, `period`, `services` (array), `kpis` (array, 1-6, label/value), `clientContext` (richText), `challenge` (array of text), `solution` (array of title/description), `results` (group: periodBefore, periodAfter, metrics array of label/before/after), `conclusion` (richText), `heroImage`, `slug`. Note: `CaseStudies` has no `author` relationship field today — check whether this is an oversight before assuming an author byline is directly renderable; if `author` truly doesn't exist on this collection, either add it here as a `checkpoint:decision` (attribute the case study to the single real Author doc, since Phase 4 confirms exactly 1 author exists) rather than silently omitting the CONT-02 byline requirement for this content type.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Case studies listing page</name>
  <files>src/app/(frontend)/[locale]/case-studies/page.tsx</files>
  <action>
Create `src/app/(frontend)/[locale]/case-studies/page.tsx` following the same Local API pattern as the existing detail page: query `case-studies` collection (published, current locale), render a responsive card grid (client name via the `client` relationship, `sector`, `heroMetric`) using shadcn `Card`. Handle the 0-results case explicitly with a clear "case studies coming soon" state (localized, not a raw empty `<div>` or a console error) rather than assuming content always exists — this is a real, expected state today per 04-VERIFICATION.md, not a bug to hide.
  </action>
  <verify>
    <automated>ls src/app/"(frontend)"/"[locale]"/case-studies/page.tsx && echo OK</automated>
  </verify>
  <done>`/case-studies` and `/en/case-studies` render a working grid or a graceful empty state, never an unhandled error, against the real (currently empty) case-studies table.</done>
</task>

<task type="auto">
  <name>Task 2: Case study detail — full structured model + author byline</name>
  <files>src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx</files>
  <action>
Rewrite the case study detail page body (keep `getCaseStudy`, `generateMetadata`, and the existing `CreativeWork`+`BreadcrumbList` JSON-LD untouched): render hero (`heroImage` if present, else the same `getFallbackHeroImage` fallback pattern from 05-05/05-08 for consistency), `heroMetric`+`heroSubtitle`, `client`/`sector`/`period`/`services` metadata row, the 4-6 `kpis` as cards (reuse `ResultsSection`'s Component.tsx presentation style from 05-04 for KPI cards, don't duplicate the visual pattern), `clientContext` via `Prose`/`RichText` under a "El cliente"/"The Client" heading, `challenge` as a bulleted list under "El reto"/"The Challenge", `solution` as a numbered list (title+description per step) under "La solución"/"The Solution", `results` as a before/after comparison table or paired KPI cards under "Resultados"/"Results" (using the accent color for deltas per UI-SPEC's "before-after deltas in case studies" rule), `conclusion` via `Prose`/`RichText`.

Resolve the missing `author` field noted in Interfaces: if `CaseStudies` has no `author` relationship, add one (`relationship→authors`, required: false) via a small schema addition + migration in this same task (this is a data-model gap discovered during page work, not a new plan-worthy scope item — CONT-02 requires it), default-populate it to the single real Author doc via a short idempotent script, then render `<AuthorByline>`/`<AuthorCard>` the same way 05-08 does for posts.
  </action>
  <verify>
    <automated>grep -q "kpis\|results\|challenge\|solution" src/app/"(frontend)"/"[locale]"/case-studies/"[slug]"/page.tsx && grep -q "AuthorByline\|AuthorCard" src/app/"(frontend)"/"[locale]"/case-studies/"[slug]"/page.tsx && echo OK</automated>
  </verify>
  <done>Case study detail renders all structured sections (KPIs, challenge, solution, results before/after, conclusion) and shows the author E-E-A-T byline; `CaseStudies` collection has a working `author` relationship if it didn't already.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Case study richText fields (`clientContext`, `conclusion`) → rendered HTML | Editor-authored only, rendered via official `RichText` component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | Rich text rendering | mitigate | Official `RichText` component, no `dangerouslySetInnerHTML` |
</threat_model>

<verification>
`/case-studies` renders a correct empty/grid state against the real (currently 0-row) table; once at least one real case study exists, its detail page shows every structured section plus a real author byline.
</verification>

<success_criteria>
- [ ] Listing page exists, handles 0-results gracefully
- [ ] Detail page renders the complete structured model (KPIs/challenge/solution/results/conclusion)
- [ ] CaseStudies has a working author relationship + byline rendering
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-09-SUMMARY.md` when done
</output>
