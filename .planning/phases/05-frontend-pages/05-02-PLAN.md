---
phase: 05-frontend-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/globals/Header/index.ts
  - src/globals/Footer/index.ts
  - src/globals/FeaturedContent/index.ts
  - src/payload.config.ts
  - src/collections/Authors/index.ts
  - migrations/ (new migration file)
  - scripts/cleanup-phase1-fixtures.ts
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "An editor can set nav links, logo, and a CTA button from the Payload admin, and edit footer columns/legal links/social links, without touching code"
    - "An editor can curate exactly which posts and case studies appear in 'featured' sections site-wide from one place (FeaturedContent global), not derived only from recency"
    - "A post/case-study byline can show credentials, years of experience, and social links for its author — fields exist even before any page renders them"
    - "The public listings Phase 5 will build do not show the Phase 1/2 test fixtures (Test Author X, test-post, test-case-study) alongside real migrated content"
  artifacts:
    - path: "src/globals/Header/index.ts"
      provides: "Header global: navItems (array), logo (upload), ctaButton (group: label/href)"
    - path: "src/globals/Footer/index.ts"
      provides: "Footer global: columns (array of link groups), socialLinks, legalLinks, copyrightText"
    - path: "src/globals/FeaturedContent/index.ts"
      provides: "FeaturedContent global: featuredPosts (relationship hasMany posts), featuredCaseStudies (relationship hasMany case-studies)"
    - path: "src/collections/Authors/index.ts"
      provides: "Authors + credentials (array), yearsExperience (number), socialLinks (array: platform select + url)"
    - path: "scripts/cleanup-phase1-fixtures.ts"
      provides: "Guarded one-off deletion of the 4 known Phase 1/2 test fixtures documented in 04-VERIFICATION.md"
  key_links:
    - from: "src/payload.config.ts"
      to: "src/globals/Header, Footer, FeaturedContent"
      via: "globals: [Llms, Header, Footer, FeaturedContent]"
      pattern: "globals:\\s*\\["
---

<objective>
Close the three highest-priority gaps identified by 05-UI-SPEC.md's "HARD RULE — Payload Block-Editability Constraint" table: no site-chrome control (Header/Footer), no featured-content curation mechanism, and Authors' E-E-A-T fields being too thin (CONTEXT.md's explicit competitive-differentiator requirement). Also clears the known Phase 1/2 test-fixture pollution flagged as a non-blocking note in 04-VERIFICATION.md before it becomes visible in Phase 5's public listings.

Purpose: every later Phase 5 plan that renders nav, footer, "featured" sections, or an author byline needs these fields to exist first — this is the interface-first contract wave for the data model (not yet the block-config wave, which is 05-03).
Output: 3 new Payload globals, an expanded Authors collection, one committed migration, and real Postgres cleaned of stray fixture docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-frontend-pages/05-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md
@src/globals/Llms/index.ts
@src/collections/Authors/index.ts
@src/fields/link.ts
@src/fields/linkGroup.ts
@src/payload.config.ts
</context>

<interfaces>
Existing global pattern (`src/globals/Llms/index.ts`) — a `GlobalConfig` with `slug`, `label`, `admin.group`, `fields`. Follow this shape for the 3 new globals.

Existing `link`/`linkGroup` field factories (`src/fields/link.ts`, `src/fields/linkGroup.ts`) already produce a reusable `{type: reference|custom, url, label, newTab, appearance}` group — reuse `link({ appearances: false })` (no button-appearance variants needed for nav/footer text links) for Header nav items and Footer column links instead of hand-rolling a new href/label pair, to stay consistent with the CallToAction block's existing pattern.

`Authors` collection today (read in full): `name` (text), `jobTitle` (localized text), `bio` (localized textarea), `avatar` (upload→media), `slug`. Add new fields; do not remove or rename existing ones (Phase 4 migrated real author data against these field names).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Header, Footer, FeaturedContent globals + config registration</name>
  <files>src/globals/Header/index.ts, src/globals/Footer/index.ts, src/globals/FeaturedContent/index.ts, src/payload.config.ts</files>
  <action>
Create `src/globals/Header/index.ts` exporting `Header: GlobalConfig` with slug `header`, `admin.group: 'Site'`: fields `logo` (upload→media), `navItems` (array, fields: `link({ appearances: false })` reused, or a lean `{label: text localized, href: text}` pair if a full internal/custom link group is overkill for top-nav — prefer the `link` field for consistency with CallToAction), `ctaButton` (group: `label` localized text, `href` text — per UI-SPEC Copywriting Contract, default label "Get in Touch"/"Hablemos" but as an editable field default, not hardcoded in a component).

Create `src/globals/Footer/index.ts` exporting `Footer: GlobalConfig`, slug `footer`, `admin.group: 'Site'`: fields `columns` (array of `{title: text localized, links: array of link({appearances:false})}}`), `socialLinks` (array: `platform` select [linkedin, github, x, website], `url` text), `legalLinks` (array: `label` localized text, `href` text — for Privacy/Terms links), `copyrightText` (localized text).

Create `src/globals/FeaturedContent/index.ts` exporting `FeaturedContent: GlobalConfig`, slug `featured-content`, `admin.group: 'Site'`: fields `featuredPosts` (relationship→posts, hasMany, admin.description explaining this drives the Home + Blog featured sections), `featuredCaseStudies` (relationship→case-studies, hasMany, admin.description explaining this drives Home's featured case studies section). This is the curation mechanism CONTEXT.md requires ("qué posts se destacan, no hardcodeado por fecha únicamente").

Wire all three into `src/payload.config.ts`'s `globals` array (currently `globals: [Llms]` → `globals: [Llms, Header, Footer, FeaturedContent]`), with matching imports alongside the existing `Llms` import.
  </action>
  <verify>
    <automated>grep -c "GlobalConfig" src/globals/Header/index.ts src/globals/Footer/index.ts src/globals/FeaturedContent/index.ts | grep -v ':0' | wc -l | grep -q 3 && echo OK</automated>
  </verify>
  <done>3 global config files exist, each exports a valid `GlobalConfig`, and `payload.config.ts`'s `globals` array includes all three alongside `Llms`.</done>
</task>

<task type="auto">
  <name>Task 2: Authors E-E-A-T expansion + migration</name>
  <files>src/collections/Authors/index.ts, migrations/ (new file via payload migrate:create)</files>
  <action>
Extend `src/collections/Authors/index.ts` fields array (append after `avatar`, before `slugField`) with: `credentials` (array, fields: `{label: text, localized: true}}` — e.g. "10+ años en SEO técnico"), `yearsExperience` (number, optional), `socialLinks` (array, fields: `platform` select [linkedin, github, x, website], `url` text). Do not touch `name`/`jobTitle`/`bio`/`avatar`/`slug` — real Phase 4 author data depends on those field names staying stable.

Run `payload migrate:create` to generate the schema migration covering both this task's Authors changes and Task 1's 3 new globals (globals also create Postgres tables under `db-postgres`), then `payload migrate` to apply it against the real Neon Postgres (per SCHEMA-03/CLAUDE.md: migrations only, never `push:true`). Commit the generated migration file.
  </action>
  <verify>
    <automated>ls migrations/*.ts 2>/dev/null | tail -1 | xargs grep -l "credentials\|header\|footer\|featured" 2>/dev/null && echo OK</automated>
  </verify>
  <done>Authors has `credentials`/`yearsExperience`/`socialLinks`; a new migration file exists, is committed, and was applied cleanly against the real Neon database (no `push: true` used anywhere).</done>
</task>

<task type="auto">
  <name>Task 3: Clean up Phase 1/2 test fixtures from the real database</name>
  <files>scripts/cleanup-phase1-fixtures.ts</files>
  <action>
04-VERIFICATION.md documents 4 stray test fixtures created before the Phase 4 migration ran, which will otherwise appear in Phase 5's public listings: `authors.id=3` ("Test Author X"), `posts.id=1` ("test-post"), `case_studies.id=1` ("test-case-study"), `redirects.id=1` ("/legacy-test-url"). Write `scripts/cleanup-phase1-fixtures.ts` (standalone script, run via `tsx`, using Payload's Local API like the Phase 4 migration scripts — not raw SQL) that: looks up each of the 4 documents by id AND title/slug match (e.g. `authors.findByID(3)` then assert `name === 'Test Author X'` before deleting) as a safety guard against deleting real content if IDs were reused, deletes only documents that pass the guard, and logs what it did/skipped. Run it once against the real database.
  </action>
  <verify>
    <automated>npx tsx scripts/cleanup-phase1-fixtures.ts</automated>
  </verify>
  <done>The script exists, is idempotent (running it twice deletes 0 the second time), and after running once the real Postgres has 0 remaining docs matching the 4 known fixture id+title/slug pairs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Admin editor → globals | Authenticated Payload admin users editing site chrome/featured content — same trust level as existing collections |
| cleanup script → production Neon DB | One-off script with delete capability running against real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Repudiation/Tampering | `scripts/cleanup-phase1-fixtures.ts` | mitigate | Guard each delete on id AND exact title/slug match before deleting; log every action; never a blanket `DELETE WHERE id IN (...)` |
| T-05-02-02 | Elevation of Privilege | Header/Footer/FeaturedContent globals | accept | Globals inherit Payload's default admin-only write access (no custom `access` override needed — same posture as `Llms` global) |
</threat_model>

<verification>
`payload migrate` applies cleanly against the real Neon database with no manual SQL. Admin panel shows Header/Footer/Featured Content under a "Site" group, and Authors edit view shows the 3 new fields. The cleanup script reports 0 remaining fixtures after running.
</verification>

<success_criteria>
- [ ] Header, Footer, FeaturedContent globals exist, registered in payload.config.ts
- [ ] Authors has credentials/yearsExperience/socialLinks fields
- [ ] Migration generated, committed, and applied against real Neon Postgres
- [ ] 4 known Phase 1/2 test fixtures removed from the real database
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-02-SUMMARY.md` when done
</output>
