---
phase: 05-frontend-pages
plan: 07
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - src/app/(frontend)/[locale]/blog/page.tsx
  - scripts/seed-blog-page.ts
autonomous: true
requirements: [CONT-01, CONT-03]

must_haves:
  truths:
    - "A visitor sees a blog listing (both locales) with a featured-posts section above a chronological grid, and can filter the grid by category"
    - "The featured section and category taxonomy are both editor-controlled (FeaturedContent global, categories collection) — not hardcoded by recency alone"
    - "Filtering to a category with 0 posts shows the UI-SPEC empty state copy, not a blank page or a crash"
  artifacts:
    - path: "src/app/(frontend)/[locale]/blog/page.tsx"
      provides: "New route rendering the `blog` Pages doc via RenderBlocks (Hero listing + FeaturedPostsBlock + ArchiveBlock with enableCategoryFilter)"
    - path: "scripts/seed-blog-page.ts"
      provides: "Seeds the real `blog` Pages doc's block composition"
  key_links:
    - from: "src/app/(frontend)/[locale]/blog/page.tsx"
      to: "src/blocks/ArchiveBlock/Component.tsx"
      via: "RenderBlocks renders the archiveBlock with enableCategoryFilter=true"
      pattern: "category"
---

<objective>
Build the blog listing page (currently missing — only `blog/[slug]` exists) replicating the old site's layout per CONTEXT.md: featured section on top (equivalent to `FeaturedBlog`), archive-header chrome, category filter, chronological grid below. Per 05-UI-SPEC.md, this is a `pages` doc (slug `blog`) composed of `Hero`(variant `listing`) + `FeaturedPostsBlock` + `ArchiveBlock`(relationTo=posts, enableCategoryFilter=true).

Purpose: satisfies CONT-03 (blog taxonomy + featured section, not just chronological) end to end using the block contracts built in 05-03/05-04.
Output: new `/blog` route, seeded `blog` Pages doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-frontend-pages/05-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@src/blocks/RenderBlocks.tsx
@src/blocks/ArchiveBlock/config.ts
@src/collections/Categories/index.ts
</context>

<interfaces>
`RenderBlocks` (05-04) renders any Pages block combination. `ArchiveBlock`'s renderer (05-04, Task 2) already implements the category-filter tabs and empty-state handling contract — this plan only needs to seed the block instance correctly and create the route; it must NOT reimplement filtering logic inline in `blog/page.tsx`.

UI-SPEC Copywriting Contract empty states (blog/category, no posts): heading "Nothing here yet"/"Todavía no hay nada aquí", body "This category doesn't have any posts yet. Browse all posts instead."/"Esta categoría todavía no tiene artículos. Explora todos los artículos." — if 05-04's `ArchiveBlock` renderer doesn't already surface these exact strings as editable/default copy, this plan's Task 2 must add them (as block-level default field values, not a hardcoded string in the renderer, to stay consistent with the HARD RULE).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `/blog` listing route</name>
  <files>src/app/(frontend)/[locale]/blog/page.tsx</files>
  <action>
Create `src/app/(frontend)/[locale]/blog/page.tsx` following the same Local API + `generateMetadata` pattern as the existing `blog/[slug]/page.tsx` and the home `page.tsx`: fetch the `pages` doc with `slug: 'blog'` for the current locale, render its `content.layout` via `<RenderBlocks blocks={doc.content.layout} />`, `notFound()` if missing. Accept and forward `searchParams` (`{ category?: string }`) to `RenderBlocks`/the archive block renderer so the category-filter tabs (05-04) can read the active filter from the URL — Next.js Server Components receive `searchParams` as a prop on the page itself, forward it down rather than having `RenderBlocks` read `useSearchParams()` (which would force client-component boundaries unnecessarily).
  </action>
  <verify>
    <automated>ls src/app/"(frontend)"/"[locale]"/blog/page.tsx && echo OK</automated>
  </verify>
  <done>`/blog` and `/en/blog` both render without error once the `blog` Pages doc exists (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Seed the real `blog` Pages doc, verify empty-state copy</name>
  <files>scripts/seed-blog-page.ts</files>
  <action>
Write `scripts/seed-blog-page.ts` (idempotent upsert-by-slug, Local API, mirrors `scripts/seed-home-page.ts`'s pattern from 05-06) creating/updating the `pages` doc with slug `blog`: `content.layout` = [`Hero`(variant `listing`, title "Blog"/"Blog", subtitle summarizing the archive per both locales), `FeaturedPostsBlock`(title "Destacados"/"Featured"), `ArchiveBlock`(relationTo: `posts`, mode: `latest`, limit: 12, enableCategoryFilter: true)].

Verify the `ArchiveBlock` renderer built in 05-04 actually surfaces the UI-SPEC empty-state copy when a category filter yields 0 results (test by filtering to one of the 5 real migrated categories and confirming a post-free category, if one exists, shows the copy — if all 5 categories have posts, verify by temporarily filtering to a nonexistent/garbage category value, which the renderer's allowlist-validation from 05-04's threat mitigation should reject and fall back to the empty state, not a query error).
  </action>
  <verify>
    <automated>npx tsx scripts/seed-blog-page.ts</automated>
  </verify>
  <done>Real `blog` Pages doc exists with the 3-block composition; category filter tabs render from the 5 real migrated categories; empty-state copy confirmed to render correctly for a zero-result filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| `?category=` query param → DB filter | Anonymous visitor-controlled input reaching a Postgres query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | `blog/page.tsx` forwarding `searchParams.category` | mitigate | Relies on the allowlist validation already mitigated in 05-04 (T-05-04-01) inside the ArchiveBlock renderer — this plan does not bypass it by querying categories directly in the page component |
</threat_model>

<verification>
`/blog` and `/en/blog` render featured + filterable archive sections with real migrated posts; selecting a category tab changes the grid; an unknown/garbage category value shows the empty state, not an error page.
</verification>

<success_criteria>
- [ ] `/blog` route exists and renders via RenderBlocks
- [ ] Real `blog` Pages doc seeded with Hero + FeaturedPostsBlock + category-filterable ArchiveBlock
- [ ] Empty-state copy verified to render correctly
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-07-SUMMARY.md` when done
</output>
