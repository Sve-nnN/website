---
phase: 05-frontend-pages
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - src/app/(frontend)/[locale]/page.tsx
  - scripts/seed-home-page.ts
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "A visitor sees the home page in both locales composed entirely of RenderBlocks output (Hero, Featured Case Studies, About, Client Logos, Featured Posts, Testimonials, Contact CTA) driven by the `home` Pages doc — no hardcoded 'Phase 2 test content' placeholder text remains"
    - "The home page's featured sections reflect whatever is curated in the FeaturedContent global, verifiable by changing the global in admin and seeing the home page change"
  artifacts:
    - path: "src/app/(frontend)/[locale]/page.tsx"
      provides: "Fetches the `home` Pages doc and renders its content.layout via RenderBlocks; keeps existing Person JSON-LD"
    - path: "scripts/seed-home-page.ts"
      provides: "One-off script populating the real `home` Pages doc's block array with the composition below, and populating FeaturedContent with real migrated posts/case-studies (if any exist)"
  key_links:
    - from: "src/app/(frontend)/[locale]/page.tsx"
      to: "src/blocks/RenderBlocks.tsx"
      via: "RenderBlocks({ blocks: doc.content.layout })"
      pattern: "RenderBlocks"
---

<objective>
Replace the Phase 2 placeholder home page ("Bienvenido — contenido de prueba Fase 2") with the real, fully block-driven home page per 05-UI-SPEC.md's Page Inventory: Hero → Featured Case Studies → About/credentials → Client logos → Featured posts → Testimonials → Contact CTA.

Purpose: this is the first of Wave 4's parallel page plans — proves the RenderBlocks contract from 05-04 and the SiteHeader/SiteFooter/FeaturedContent wiring from 05-02/05-05 actually compose into a real page.
Output: updated `page.tsx`, and the real `home` Pages doc populated with a working block composition plus curated FeaturedContent entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@src/app/(frontend)/[locale]/page.tsx
@src/blocks/RenderBlocks.tsx
@src/collections/Pages/index.ts
@src/globals/FeaturedContent/index.ts
</context>

<interfaces>
`RenderBlocks` (05-04): `<RenderBlocks blocks={doc.content.layout} />` renders any combination of the 16 registered Pages blocks. The `home` Pages doc (slug `home`, already exists per Phase 2 seed) needs its `content.layout` array populated with: `Hero` (variant `home`), `FeaturedCaseStudiesBlock`, `Content` or `Section` (About/credentials summary — reuse Juan's own `Authors` doc bio/credentials, since Juan is both the site owner and its sole author per Phase 4's "1 author" migrated), `ClientLogosBlock`, `FeaturedPostsBlock`, `TestimonialsCarousel`, `CallToAction` (Contact CTA).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Home page fetches and renders the `home` Pages doc via RenderBlocks</name>
  <files>src/app/(frontend)/[locale]/page.tsx</files>
  <action>
Rewrite `HomePage` in `src/app/(frontend)/[locale]/page.tsx`: keep the existing `getHomePage(locale)` Local API fetch and `generateMetadata` (meta title/description from the doc's SEO tab), keep the existing Person JSON-LD block. Replace the hardcoded `<h1>{doc?.title ?? 'Bienvenido — contenido de prueba Fase 2'}</h1>` fallback with `<RenderBlocks blocks={doc?.content?.layout ?? []} />` from `@/blocks/RenderBlocks`. If `doc` is undefined (Pages doc missing), render a clear `notFound()` rather than the old placeholder string — the placeholder was a Phase 2 stopgap, not a real empty state.
  </action>
  <verify>
    <automated>grep -q "RenderBlocks" src/app/"(frontend)"/"[locale]"/page.tsx && ! grep -q "Phase 2 test content\|contenido de prueba Fase 2" src/app/"(frontend)"/"[locale]"/page.tsx && echo OK</automated>
  </verify>
  <done>Home page renders via RenderBlocks; the Phase 2 placeholder string is gone from the source.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the real `home` Pages doc + FeaturedContent global</name>
  <files>scripts/seed-home-page.ts</files>
  <action>
Write `scripts/seed-home-page.ts` (Local API, `tsx`-run, idempotent — upsert by slug like the Phase 4 migration scripts) that sets the real `home` Pages doc's `content.layout` to: `Hero` (variant `home`, title/subtitle in both locales reflecting Juan's dual positioning per PROJECT.md — "Ingeniero de Software y Experto SEO" / "Software Engineer & SEO Expert"), `FeaturedCaseStudiesBlock` (title "Casos de éxito destacados"/"Featured Case Studies"), `Content` (About section using the real migrated Author's `bio`/`credentials` — query the one real Author doc via Local API and reference it, do not paste static text describing Juan that isn't sourced from the Authors collection), `ClientLogosBlock` (title "Clientes"/"Clients", `clients` left empty to show all 6 real migrated Clientes), `FeaturedPostsBlock` (title "Artículos destacados"/"Featured Posts"), `TestimonialsCarousel` (default config, will render the 1 real migrated testimonial), `CallToAction` (Contact CTA linking to `/contact`, copy per UI-SPEC Copywriting Contract: "Get in Touch"/"Hablemos").

Also populate the `featured-content` global's `featuredPosts` (a handful of the 72 real migrated posts — pick by most recent `publishedAt` as a reasonable initial curation, since there is no existing "featured" signal to migrate) and leave `featuredCaseStudies` empty if the real case-studies table has 0 rows (per 04-VERIFICATION.md: 0 real case studies exist yet) — do not fabricate placeholder case studies to fill the section; the `FeaturedCaseStudiesBlock` renderer (05-04) must already handle an empty list gracefully (verify it does; if not, this is a defect in 05-04 to flag, not something to work around by inventing content here).
  </action>
  <verify>
    <automated>npx tsx scripts/seed-home-page.ts</automated>
  </verify>
  <done>The real `home` Pages doc has a working 7-block composition; FeaturedContent has real curated posts (and case studies, if any exist by execution time); re-running the script is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Seed script → production Neon DB | Idempotent upsert against real content, no destructive writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | `scripts/seed-home-page.ts` | mitigate | Upsert-by-slug only, never delete-then-recreate; safe to re-run |
</threat_model>

<verification>
`curl localhost:3000/` and `curl localhost:3000/en` both render the full 7-section home page with real content (no "Phase 2" placeholder string anywhere in the HTML).
</verification>

<success_criteria>
- [ ] Home page renders via RenderBlocks in both locales
- [ ] Real `home` Pages doc has all 7 sections populated
- [ ] FeaturedContent global has real curated posts (case studies if any exist)
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-06-SUMMARY.md` when done
</output>
