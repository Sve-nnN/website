---
phase: 05-frontend-pages
plan: 13
type: execute
wave: 5
depends_on: ["05-06", "05-07", "05-08", "05-09", "05-10", "05-11", "05-12"]
files_modified: []
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06]

must_haves:
  truths:
    - "Every page named in ROADMAP Phase 5 success criteria works in both locales against real production data, confirmed by a human, not just by automated build success"
    - "All 6 CONT requirements are satisfied end-to-end, not just individually plausible per-plan"
  artifacts:
    - path: ".planning/phases/05-frontend-pages/05-13-SUMMARY.md"
      provides: "Bilingual walkthrough record + final requirements coverage confirmation"
  key_links: []
---

<objective>
Close Phase 5 with a real bilingual walkthrough of every page built across plans 05-06 through 05-12, against real migrated production data, plus a final requirements-coverage pass against all 6 CONT-* requirements and the 5 ROADMAP success criteria. This is the phase's only checkpoint plan — everything upstream was autonomous; this is where a human confirms the whole thing actually holds together end to end.

Purpose: individual plans verify their own slice; nothing so far has walked a real visitor's path across the entire site in both locales at once, or confirmed the featured/curation mechanisms (FeaturedContent global) actually change what a visitor sees.
Output: a completed bilingual QA pass, `05-13-SUMMARY.md` recording results, any gaps flagged for follow-up (not silently fixed here if they require new plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-pages/05-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pass — build, requirement/artifact grep audit</name>
  <files>none (verification only)</files>
  <action>
Run `npm run build` end to end (includes `payload migrate && payload generate:importmap && payload generate:types && next build` per `package.json`) and confirm it completes cleanly with all Phase 5 migrations applied. Grep-audit that every CONT-01..06 requirement has a corresponding real artifact: routes for home/blog/blog-detail/case-studies/case-studies-detail/authors/authors-profile/contact/privacy/terms/search all exist under `src/app/(frontend)/[locale]/`; `@payloadcms/plugin-search` registered; `resendAdapter`+contact action wired; no SEO-tooling collection/global present (re-confirm 05-12's grep). Produce a coverage table (requirement → plan → artifact → status) as the basis for the human-verify checklist in Task 2.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -40</automated>
  </verify>
  <done>Build succeeds; coverage table drafted covering all 6 CONT requirements against real files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 5 pages: home, blog listing + post detail, case studies listing + detail, authors listing + profile, contact, privacy, terms, search — each composed from real Payload content (72 posts, 6 clientes, 5 categories, 1 author, and any case studies authored since Phase 4), in both `es` (default, unprefixed) and `en` (`/en/...`) locales.</what-built>
  <how-to-verify>
    1. Visit `/` and `/en` — confirm the home page shows a real hero, featured case studies section (or its correct empty state if still 0 real case studies), about/credentials, client logos, featured posts, testimonials, and a working Contact CTA — in both languages, with different copy per locale.
    2. Visit `/blog` and `/en/blog` — confirm a featured-posts section above a chronological grid, and that clicking a category tab actually filters the grid.
    3. Open any real post at `/blog/{slug}` — confirm a hero image renders (fallback, since no post has a real heroImage), the author byline shows credentials, and related posts + table of contents work.
    4. Visit `/case-studies` and `/en/case-studies` — confirm either real case study cards or a correct "coming soon" empty state (not an error). If any real case study exists, open its detail page and confirm KPI cards, challenge/solution/results sections, and the author byline all render.
    5. Visit `/authors` and `/en/authors`, then open the one real author's profile — confirm the full E-E-A-T card (credentials, years experience, social links) renders, plus a real list of their posts.
    6. Visit `/search?q=` with a real post title, a real author name, and a nonsense string — confirm relevant results, and confirm the nonsense string shows the exact UI-SPEC empty-state copy.
    7. Submit the real contact form on `/contact` with a test message — confirm Juan actually receives it via Resend (check the inbox at the `CONTACT_TO_EMAIL` address).
    8. Visit `/privacy` and `/terms` (both locales) — confirm the ported legal copy reads correctly and matches the old site's actual sections (not paraphrased or shortened).
    9. In `/admin`, confirm there is no SEO-tooling dashboard/collection anywhere (AdBanners/BrokenLinks/GSCMetrics/KeywordMetrics/PageMetrics/dinorank) — only the Phase 1-5 collections/globals.
    10. In `/admin`, edit the FeaturedContent global (swap a featured post) and reload `/blog` and `/` — confirm the featured section actually changes, proving curation is real and not cached/hardcoded.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found per numbered step above</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| N/A | Verification-only plan, no new code surface |
</threat_model>

<verification>
Human walkthrough of all 10 checklist items above against the real production/dev database completes with no unresolved issues, or any issues found are explicitly logged as follow-up (new gap-closure plan), not silently patched inside this checkpoint.
</verification>

<success_criteria>
- [ ] `npm run build` succeeds with all Phase 5 migrations applied
- [ ] All 5 ROADMAP Phase 5 success criteria confirmed true by a human against real data
- [ ] All 6 CONT-* requirements confirmed satisfied end-to-end
- [ ] FeaturedContent curation confirmed to actually drive what visitors see (not hardcoded/cached)
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-13-SUMMARY.md` when done
</output>
