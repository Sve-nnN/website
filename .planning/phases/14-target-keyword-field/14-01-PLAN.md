---
phase: 14-target-keyword-field
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collections/Pages/index.ts
  - src/collections/Authors/index.ts
  - src/migrations/*.ts (new, generated)
  - src/migrations/*.json (new, generated)
  - src/migrations/index.ts (auto-updated by payload CLI)
  - src/payload-types.ts
  - scripts/seed-phase14-target-keyword.ts
autonomous: true
requirements: [SEO-KW-01, SEO-KW-02]

must_haves:
  truths:
    - "Opening a Pages doc or an Authors doc in Payload admin shows a targetKeyword group field with independent en/es plain-text sub-fields, editable without switching the admin locale selector"
    - "Setting targetKeyword does not trigger any outbound HTTP call — no Ahrefs/DinoRank/SerpAPI request fires when the field is read, written, or the doc is saved"
    - "Home's targetKeyword is populated: es=\"seo técnico\", en=\"technical seo consultant\""
    - "The real Author doc (slug juan-carlos-angulo)'s targetKeyword is populated: es=\"auditoría seo técnico\", en=\"technical seo specialist\""
    - "Re-running the seed script does not duplicate data or error — it updates the same two docs in place"
  artifacts:
    - path: "src/collections/Pages/index.ts"
      provides: "targetKeyword group field (en/es text sub-fields) added to the Pages schema, alongside existing title/content/slug fields"
      contains: "targetKeyword"
    - path: "src/collections/Authors/index.ts"
      provides: "targetKeyword group field (en/es text sub-fields) added to the Authors schema"
      contains: "targetKeyword"
    - path: "src/migrations"
      provides: "Generated up/down migration adding the new targetKeyword columns to pages and authors tables, registered in migrations/index.ts"
    - path: "scripts/seed-phase14-target-keyword.ts"
      provides: "Idempotent Local API seed script populating Home (pages) and the real Author (authors) targetKeyword group with the 4 locked picks"
      contains: "targetKeyword"
  key_links:
    - from: "scripts/seed-phase14-target-keyword.ts"
      to: "src/collections/Pages/index.ts"
      via: "payload.update({ collection: 'pages', ... data: { targetKeyword: { en, es } } })"
      pattern: "targetKeyword"
    - from: "scripts/seed-phase14-target-keyword.ts"
      to: "src/collections/Authors/index.ts"
      via: "payload.update({ collection: 'authors', ... data: { targetKeyword: { en, es } } })"
      pattern: "targetKeyword"
---

<objective>
Add a purely editorial `targetKeyword` group field (plain-text `en`/`es` sub-fields, not localized richText, no external API calls) to both the `pages` and `authors` collections, generate and apply the resulting Postgres migration, and seed it with the four already-researched keyword picks on Home and the real Author doc (SEO-KW-01, SEO-KW-02).

Purpose: gives Juan a visible, admin-only reference for which target keyword each page/author profile is written toward — content-writing guidance, not a rendered or SEO-plugin-integrated field. It stays fully separate from `@payloadcms/plugin-seo`'s existing title/description/OG meta fields on `pages`.

Output: `targetKeyword` group field on both collections' schemas, a committed `push:false` migration, regenerated `payload-types.ts`, and an idempotent seed script populating Home + the real Author with the locked picks from `research/keyword-research/KEYWORD-RESEARCH.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-target-keyword-field/14-CONTEXT.md

@src/collections/Pages/index.ts
@src/collections/Authors/index.ts
@src/migrations/20260711_224308_phase13_about_features_faq.ts
@src/migrations/index.ts
@scripts/seed-phase13-home-content.ts

<interfaces>
<!-- Current Pages collection fields (src/collections/Pages/index.ts): top-level `title`
(localized text), `content.layout` (blocks array), and `slugField()` (from `@/fields/slug`,
appends `slug`/`slugLock` fields). targetKeyword is a NEW top-level sibling field, added
after `slugField()` in the fields array — it is not part of `content` and not a block.

Current Authors collection fields (src/collections/Authors/index.ts): top-level `name`,
`jobTitle`, `bio`, `avatar`, `credentials[]`, `expertise[]`, `education[]`, `experience[]`,
`yearsExperience`, `socialLinks[]`, then `slugField('name')` last. targetKeyword is a NEW
top-level sibling field, added after `slugField('name')`.

Field shape for both collections (identical, per 14-CONTEXT.md decisions — Claude's
discretion resolved as: no SEO tab exists on either collection today, so targetKeyword is
added as a standalone top-level group, not nested under a tab):

  {
    name: 'targetKeyword',
    type: 'group',
    label: { en: 'Target Keyword', es: 'Keyword objetivo' },
    admin: {
      description: {
        en: 'Editorial reference only — the primary keyword this page/profile is written toward. Does not affect SEO meta tags or trigger any external API call.',
        es: 'Solo referencia editorial — la keyword principal para la que está escrito este contenido. No afecta las meta etiquetas de SEO ni dispara ninguna llamada a una API externa.',
      },
    },
    fields: [
      { name: 'en', type: 'text', label: 'English' },
      { name: 'es', type: 'text', label: 'Español' },
    ],
  }

Deliberately NOT using Payload's `localized: true` on a single text field — 14-CONTEXT.md
locks a group with explicit `en`/`es` sub-fields so both language picks are visible
side-by-side in the admin regardless of which admin locale is active (editorial comparison
use case), not toggled by the locale switcher like `title`/`bio` are.

No new block, no new collection, no plugin-seo interaction — plugin-seo's own `meta` tab
(title/description/OG) on `pages` is untouched by this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targetKeyword group field to Pages and Authors collections</name>
  <files>src/collections/Pages/index.ts, src/collections/Authors/index.ts</files>
  <action>
Edit `src/collections/Pages/index.ts`: add the `targetKeyword` group field (exact shape given in `<interfaces>` above) as a new entry in the top-level `fields` array, placed after `slugField()`. Edit `src/collections/Authors/index.ts`: add the identical `targetKeyword` group field as a new entry in the top-level `fields` array, placed after `slugField('name')`. Both fields use the same field name/shape (`targetKeyword: { en, es }`, both plain `type: 'text'`, neither sub-field `localized`, neither `required` — editorial field, optional per doc). Do not touch `@payloadcms/plugin-seo`'s tab config, do not touch any other field on either collection. After editing both files, regenerate the admin import map and generated types from the project root: `node --env-file=.env node_modules/.bin/payload generate:importmap` then `node --env-file=.env node_modules/.bin/payload generate:types` (updates `src/payload-types.ts`'s `Page`/`Author` interfaces to include `targetKeyword`).
  </action>
  <verify>
    <automated>grep -c "targetKeyword" src/collections/Pages/index.ts src/collections/Authors/index.ts src/payload-types.ts</automated>
  </verify>
  <done>Pages and Authors collection configs both expose a top-level targetKeyword group field (en/es text sub-fields, not localized); payload-types.ts's Page and Author interfaces both include targetKeyword</done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply + commit the Postgres migration</name>
  <files>src/migrations/*.ts (new), src/migrations/*.json (new), src/migrations/index.ts</files>
  <action>
Following the established project convention (Phase 10.5/10.7/10.8/12/13 precedent — `payload migrate:create` → `payload migrate`, `push:false` respected throughout, never live-push), run from the project root: `node --env-file=.env node_modules/.bin/payload migrate:create phase14_target_keyword_field`. Confirm Payload introspects the diff between the current Postgres schema and the new `target_keyword_en`/`target_keyword_es` columns on both `pages` and `authors` tables (from Task 1) and writes a new `src/migrations/{timestamp}_phase14_target_keyword_field.ts` (+ matching `.json`), auto-registering it in `src/migrations/index.ts` (do not hand-edit that file — the CLI appends the import + array entry itself). Review the generated `up`/`down` SQL before applying — confirm it only adds columns to `pages`/`_pages_v` and `authors` (no unrelated table changes). Apply it against the dev database with `node --env-file=.env node_modules/.bin/payload migrate`. Do not change `push: false` in `src/payload.config.ts`.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/payload migrate:status 2>&1 | tail -5</automated>
  </verify>
  <done>New migration file(s) exist under src/migrations/, registered in src/migrations/index.ts, applied against the dev Postgres instance (migrate:status shows no pending migrations), and payload.config.ts still has push:false</done>
</task>

<task type="auto">
  <name>Task 3: Idempotent seed script populating Home + the real Author's targetKeyword</name>
  <files>scripts/seed-phase14-target-keyword.ts</files>
  <action>
Create `scripts/seed-phase14-target-keyword.ts` following the Local API idempotent-seed pattern from `scripts/seed-phase13-home-content.ts` / `scripts/seed-author-eeat.ts` (import `getPayload` from `payload`, `config` from `../src/payload.config`, call `getPayload({ config })`). Since `targetKeyword`'s `en`/`es` sub-fields are plain group fields (not `localized: true`), a single `payload.update()` call per doc — with no `locale` option needed, or `locale: 'en'` and Payload's default `fallbackLocale` behavior; use no `locale` param at all since the group itself isn't locale-scoped — writes both sub-values in one shot (unlike the Phase 13 script's per-locale loop, which was required only because those fields ARE `localized: true`).

1. Look up the Home `pages` doc via `payload.find({ collection: 'pages', where: { slug: { equals: 'home' } }, limit: 1 })`. If not found, log and `process.exit(1)` (do not throw silently). Update it: `payload.update({ collection: 'pages', id: homeDoc.id, data: { targetKeyword: { es: 'seo técnico', en: 'technical seo consultant' } } })`.
2. Look up the real Author doc via `payload.find({ collection: 'authors', where: { slug: { equals: 'juan-carlos-angulo' } }, limit: 1 })`. If not found, log and `process.exit(1)`. Update it: `payload.update({ collection: 'authors', id: authorDoc.id, data: { targetKeyword: { es: 'auditoría seo técnico', en: 'technical seo specialist' } } })`.
3. Log a confirmation line per doc updated, then `process.exit(0)`. Wrap `main()` in a `.catch((err) => { console.error(err); process.exit(1) })` per the existing scripts' convention. The script is naturally idempotent (an `update` call always sets the same two values regardless of current state — no duplication risk since `targetKeyword` is a singleton group, not an array).

Add a header comment documenting purpose, the SEO-KW-01/02 requirement IDs, and the run command: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase14-target-keyword.ts`. Run the script against the dev database and confirm both docs report success in the log output.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/seed-phase14-target-keyword.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Script runs successfully against the dev database; Home's targetKeyword.es/en equal "seo técnico"/"technical seo consultant"; the juan-carlos-angulo Author's targetKeyword.es/en equal "auditoría seo técnico"/"technical seo specialist"; re-running the script produces the same result with no errors or duplicates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload admin user -> targetKeyword field -> form state | Admin-authenticated input only (Payload admin requires auth); plain text field, no HTML/richText, no external call triggered by read/write |
| Seed script -> Postgres (dev) -> Local API | Script runs with full Local API access (bypasses collection `access` control by design, same as all prior seed scripts); only writes 2 known docs by slug lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `targetKeyword.en`/`targetKeyword.es` values | accept | Plain editorial text field, admin-auth-gated (same trust level as `title`/`bio`/`jobTitle` already on these collections); no rendering to public frontend, no injection surface since it's never interpolated into HTML/queries outside Payload's own sanitized text-field handling |
| T-14-02 | Information Disclosure | `targetKeyword` group | accept | Field is admin-only surfaced (not exposed via any public page template); Payload's REST/GraphQL API already exposes all `pages`/`authors` fields per existing `read` access rules (`authenticatedOrPublished` on pages, public on authors) — targetKeyword's presence in that same API response is no greater exposure than existing fields like `bio`/`credentials`, and a target-keyword string carries no sensitive data |
| T-14-03 | Tampering | Generated migration SQL | mitigate | `payload migrate:create` output reviewed manually before `payload migrate` is run and before commit — no blind-apply |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — uses only existing `payload`/`@payloadcms/db-postgres`/`tsx` already in `package.json` |
</threat_model>

<verification>
1. `node --env-file=.env node_modules/.bin/payload generate:types && npx tsc --noEmit` — schema compiles cleanly, `Page`/`Author` types include `targetKeyword`.
2. Open `/admin`, navigate to the Home Pages doc — a `targetKeyword` group field with `en`/`es` text inputs is visible and populated with the locked picks, without needing to switch the admin locale.
3. Open `/admin`, navigate to the Author doc with slug `juan-carlos-angulo` — same field, populated with its locked picks.
4. `node --env-file=.env node_modules/.bin/payload migrate:status` shows the new migration applied, none pending.
5. `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase14-target-keyword.ts` run twice in a row produces identical output with no errors (idempotency check).
</verification>

<success_criteria>
- `pages` and `authors` collections both expose `targetKeyword` (group, `en`/`es` plain text, not localized, no external API integration) — SEO-KW-01.
- Home's `targetKeyword` is populated with es="seo técnico"/en="technical seo consultant"; the real Author's `targetKeyword` is populated with es="auditoría seo técnico"/en="technical seo specialist" — SEO-KW-02.
- Migration generated via `payload migrate:create`, applied, and committed — `push:false` untouched.
- `@payloadcms/plugin-seo`'s existing meta fields on `pages` are unmodified.
</success_criteria>

<output>
Create `.planning/phases/14-target-keyword-field/14-01-SUMMARY.md` when done
</output>
