---
phase: 48-p-gina-de-stack-links-inline-en-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/ToolStack/config.ts
  - src/blocks/ToolStack/Component.tsx
  - src/components/ToolCard.tsx
  - src/components/GearCard.tsx
  - src/components/StackHighlightCallout.tsx
  - src/components/AffiliateDisclosureFrame.tsx
  - src/lib/affiliate-cta.ts
  - src/app/(frontend)/[locale]/stack/page.tsx
  - src/collections/Pages/index.ts
  - src/blocks/blockRegistry.tsx
  - src/collections/AffiliateLinks/index.ts
  - messages/es.json
  - messages/en.json
  - src/migrations/*_phase48_tool_stack_block.ts
  - src/migrations/*_phase48_tool_stack_block.json
  - src/migrations/index.ts
  - src/payload-types.ts
  - scripts/seed-phase48-tracer.ts
  - scripts/verify-stack-page.ts
autonomous: true
requirements: [STACK-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "/stack responde 200 en es y en bajo el MISMO segmento compartido (/stack, /en/stack), nunca /servicios ni /es/stack"
    - "canonical + hreflang recíproco (es/en/x-default) de /stack resuelven vía buildAlternates('/stack','/en/stack'), verificado por curl"
    - "git diff --stat de src/lib/sitemap-data.ts, src/lib/canonical.ts y src/lib/breadcrumbs.ts queda vacío"
    - "AffiliateDisclosure renderiza dentro de un frame con borde ANTES del primer AffiliateLink en orden de DOM en /stack"
    - "El tool card de DinoRANK renderiza de punta a punta con un CTA que apunta a /go/dinorank (Phase 47, sin tocar)"
    - "Ningún href renderizado contiene la palabra literal undefined"
  artifacts:
    - src/blocks/ToolStack/config.ts
    - src/blocks/ToolStack/Component.tsx
    - src/components/ToolCard.tsx
    - src/components/GearCard.tsx
    - src/components/StackHighlightCallout.tsx
    - src/components/AffiliateDisclosureFrame.tsx
    - src/lib/affiliate-cta.ts
    - src/app/(frontend)/[locale]/stack/page.tsx
    - "migración additiva generada por payload migrate:create (ToolStack tables + ALTER TYPE ADD VALUE en enum_affiliate_links_program)"
  key_links:
    - "Pages/index.ts blocks[] -> blockRegistry.tsx blockComponents['toolStack'] -> RenderBlocks -> ToolStackComponent"
    - "ToolStack.tools[].affiliateLink (relationship) -> affiliate-links doc -> href /go/{slug} (Phase 47 route, sin cambios)"
    - "getCachedPageBySlug('stack', locale) -> doc.content.layout -> RenderBlocks blockProps={{ toolStack: { locale } }}"
---

<objective>
Construir la infraestructura de código de la página `/stack`: el bloque Payload `ToolStack`
(schema + migración), su registro en `Pages` y en el registry de render, la ruta nueva
`src/app/(frontend)/[locale]/stack/page.tsx`, y los componentes de presentación (`ToolCard`,
`GearCard`, `StackHighlightCallout`, el frame visual de `AffiliateDisclosure`). El Task 1 es un
tracer: prueba TODA la arquitectura de punta a punta (schema -> migración -> ruta -> render ->
`/go/`) con UN solo tool real (DinoRANK) antes de que el Plan 48-02 pueble el resto del contenido.

Purpose: sin esta infraestructura no existe superficie donde renderizar ningún link de afiliado de
`/stack` — STACK-01 es la base de la que dependen STACK-02..06 (Plan 48-02).
Output: bloque `ToolStack` registrado y migrado contra Postgres real, ruta `/stack` sirviendo 200 en
ambos locales con un tool real de punta a punta, componentes de presentación listos para que 48-02
los llene con las 6 herramientas restantes + Gear + los dos callouts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-CONTEXT.md
@.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-UI-SPEC.md
@.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-RESEARCH.md

@src/collections/AffiliateLinks/index.ts
@src/components/AffiliateLink.tsx
@src/components/AffiliateDisclosure.tsx
@src/lib/affiliate.ts
@src/app/go/[slug]/route.ts
@src/collections/Pages/index.ts
@src/blocks/blockRegistry.tsx
@src/blocks/RenderBlocks.tsx
@src/lib/cache.ts
@src/lib/canonical.ts
@src/lib/breadcrumbs.ts
@src/lib/sitemap-data.ts
@src/app/(frontend)/[locale]/page.tsx
@src/components/PageHero.tsx
@src/fields/link.ts
@src/blocks/RelatedCaseStudyBlock/config.ts
@src/blocks/RelatedCaseStudyBlock/Component.tsx
@src/components/Prose.tsx
@src/components/AuthorCard.tsx
@src/components/ui/badge.tsx
@src/components/ui/button-variants.ts
@src/components/Container.tsx
@messages/es.json
@messages/en.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — bloque ToolStack (schema+migración), ruta /stack, y DinoRANK de punta a punta</name>
  <read_first>
    src/collections/AffiliateLinks/index.ts, src/collections/Pages/index.ts, src/blocks/blockRegistry.tsx,
    src/blocks/RenderBlocks.tsx, src/lib/cache.ts (getCachedPageBySlug), src/lib/canonical.ts (buildAlternates),
    src/lib/breadcrumbs.ts (BreadcrumbItem, buildBreadcrumbJsonLd), src/lib/sitemap-data.ts (rama generica
    "else" para pages), src/app/(frontend)/[locale]/page.tsx (patron de route file completo a copiar),
    src/app/go/[slug]/route.ts, src/fields/link.ts (idioma reference/custom), src/components/Prose.tsx,
    src/components/AuthorCard.tsx (patron Prose+bio), scripts/db/04-which-database.ts
  </read_first>
  <precondition>src/app/go/[slug]/route.ts existe y Phase 47 esta cerrada (STATE.md confirma GO-01..04 verificados contra produccion); node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts confirma que DATABASE_URI apunta al Postgres de Dokploy (no a una Neon abandonada, per el incidente 2026-08-17 documentado en CLAUDE.md)</precondition>
  <reversibility rating="costly">El shape de campos de ToolStack.tools[] (narrative/pro/con/referenceLink) queda fijado por una migracion real de Postgres; cambiarlo despues de que 48-02 cargue las 7 herramientas exige otra migracion + re-entrada de contenido bilingue, no solo un refactor de codigo.</reversibility>
  <files>
    src/blocks/ToolStack/config.ts, src/blocks/ToolStack/Component.tsx, src/components/ToolCard.tsx,
    src/components/AffiliateDisclosureFrame.tsx, src/lib/affiliate-cta.ts,
    src/app/(frontend)/[locale]/stack/page.tsx, src/collections/Pages/index.ts, src/blocks/blockRegistry.tsx,
    src/collections/AffiliateLinks/index.ts, messages/es.json, messages/en.json,
    src/migrations/*_phase48_tool_stack_block.ts, scripts/seed-phase48-tracer.ts, scripts/verify-stack-page.ts
  </files>
  <action>
    Paso 1 — Ensanchar el enum de `program`. En `src/collections/AffiliateLinks/index.ts`, agregar a las
    `options` del campo `program` (hoy `['amazon','kinsta','dinorank','digitalocean','other']`) los valores:
    `hostinger`, `dataforseo`, `payload`, `cloudinary`, `resend`, `ahrefs`, `google-search-console`. No tocar
    ningun otro campo de esta coleccion (la matriz de localizacion sigue congelada per 46-CONTEXT.md).

    Paso 2 — Crear `src/blocks/ToolStack/config.ts` como `Block` de Payload (slug `toolStack`, interfaceName
    `ToolStackBlock`) con estos campos exactos: `intro` (textarea, localized) — lede corta antes del
    disclosure; `categoryGroups` (array, `admin.initCollapsed:true`, con un `admin.description` que declare
    literalmente que el ORDEN del array es el unico orden de render y que nunca se reordena en el
    componente por `affiliateLink != null` ni por comision) con subcampos `heading` (text, localized,
    required) y `tools` (array anidado, `admin.initCollapsed:true`, mismo `admin.description` anti-orden)
    cuyos subcampos son: `name` (text, NO localizado, required — nombre propio), `affiliateLink`
    (relationship a `affiliate-links`, NO required — null es la senal de "sin afiliado todavia"),
    `narrative` (textarea, localized, required — >=100 palabras por locale, sin copy de fabricante ni
    specs, per STACK-02), `pro` (text, localized, required), `con` (text, localized, NO required — vacio es
    valido, nunca se fabrica un contra), y `referenceLink` (group con `type` radio defaultValue `caseStudy`
    y opciones `Case study`/`Custom URL`, `caseStudy` relationship a `case-studies` con
    `admin.condition` en `type === 'caseStudy'`, `url` text con `admin.condition` en `type === 'custom'` y
    una descripcion que aclare que es una ruta bare tipo `/servicios/fullstack-development` o
    `/case-studies`, sin prefijo de locale — el mismo idioma que `Footer.legalLinks.href`). Seguir despues
    `gearIntro` (textarea, localized — un solo parrafo compartido, menciona a Amazon Prime en prosa, Prime
    NO es un gearItem con link propio), `gearItems` (array con `name` text required NO localizado y `href`
    text required NO localizado, con una descripcion que prohiba explicitamente cualquier valor `amzn.to`
    en `href`), `elegiriaHoy` (textarea, localized, required), y `noCommissionPick` (relationship a
    `affiliate-links`, NO required, con descripcion "se espera que resuelva al doc de Google Search
    Console").

    Paso 3 — Registrar el bloque. En `src/collections/Pages/index.ts` importar `ToolStack` desde
    `@/blocks/ToolStack/config` y agregarlo al final del array `blocks:` (import + push, mismo patron que
    los 26 bloques ya existentes — no reordenar los existentes). En `src/blocks/blockRegistry.tsx` importar
    `ToolStackComponent` desde `@/blocks/ToolStack/Component` y agregar la entrada `toolStack:
    ToolStackComponent` al objeto `blockComponents` (la key es el `slug` del bloque en camelCase, no el
    `interfaceName`).

    Paso 4 — Migracion. Correr `payload migrate:create` (nombre sugerido: `phase48_tool_stack_block`). Leer
    el archivo `.ts` generado COMPLETO antes de aplicar: debe contener unicamente
    `CREATE TYPE`/`CREATE TABLE`/`ALTER TABLE ... ADD COLUMN`/`ADD CONSTRAINT` para las tablas nuevas de
    `ToolStack` y una o mas sentencias `ALTER TYPE "public"."enum_affiliate_links_program" ADD VALUE` para
    los 7 valores nuevos del Paso 1 — CERO `DROP TABLE`/`DROP COLUMN`/`ALTER COLUMN` sobre columnas
    preexistentes. Es aditiva: aplicarla directamente (`payload migrate`) sin pausar para aprobacion de
    Juan, per la regla de Database Safety de CLAUDE.md. Correr `payload generate:types` despues para que
    `payload-types.ts` incluya `ToolStackBlock` y el `AffiliateLink['program']` ensanchado.

    Paso 5 — Helper de CTA compartido. Crear `src/lib/affiliate-cta.ts` (funcion pura, sin import de
    `payload`/`@payload-config`, igual que `src/lib/affiliate.ts`) que exporte `resolveAffiliateCta(value,
    toolName, ctaDefaultLabel)`. Debe primero comprobar `typeof value === 'object' && value !== null` — si
    falso (cubre tanto `affiliateLink` null/undefined COMO el caso real de Payload donde una relacion con
    acceso denegado se resuelve al ID crudo en vez de null, confirmado contra la documentacion de Payload
    3.85: "ids are visible regardless of access controls") devolver `{ kind: 'pending' }`. Si
    `value.program === 'google-search-console'` devolver `{ kind: 'none' }` (nunca boton, nunca badge —
    GSC no tiene programa por diseno, nunca lo tendra). Si `!value.active` o `!value.slug` devolver
    `{ kind: 'pending' }` (defensivo — nunca construir `/go/undefined`). En cualquier otro caso devolver
    `{ kind: 'active', href: '/go/' + value.slug, label: value.ctaLabel || ctaDefaultLabel }`.

    Paso 6 — Namespace de i18n. Agregar el namespace `stackPage` a `messages/es.json` y `messages/en.json`
    con exactamente estas keys (copy tomado literal de la Copywriting Contract table de 48-UI-SPEC.md):
    `title`, `gearHeading`, `elegiriaHoyHeading`, `noCommissionHeading`, `noCommissionBadge`, `rowUsage`,
    `rowPro`, `rowCon`, `rowConEmpty`, `rowReference`, `ctaDefault` (con placeholder `{tool}`),
    `ctaAmazon`, `badgePending`, `emptyTitle`, `emptyBody`, `inlineEyebrow`, `gscLinkLabel`. Valores ES:
    "Mi stack" / "Mi equipo personal" / "Qué elegiría hoy si empezara de cero" / "Mi recomendación sin
    comisión" / "Recomendación sin comisión" / "Cómo lo uso" / "Lo bueno" / "Lo no tan bueno" / "Sin
    quejas reales hasta ahora" / "Dónde lo usé" / "Probar {tool}" / "Ver en Amazon" / "Sin programa de
    afiliados todavía" / "Próximamente" / "Estoy armando la página de herramientas. Vuelve pronto." /
    "Herramienta que uso" / "search.google.com/search-console". Valores EN: "My stack" / "My personal
    gear" / "What I'd choose today, starting from zero" / "My pick that pays me nothing" /
    "No-commission pick" / "How I use it" / "What's good" / "What's not" / "No real complaints so far" /
    "Where I used it" / "Try {tool}" / "View on Amazon" / "No affiliate program yet" / "Coming soon" /
    "I'm putting together the tools page. Check back soon." / "Tool I use" /
    "search.google.com/search-console".

    Paso 7 — `AffiliateDisclosureFrame`. Crear `src/components/AffiliateDisclosureFrame.tsx`: un `<div
    role="note" className="rounded-lg border border-border bg-muted p-4 [&_p]:text-label
    [&_p]:text-muted-foreground">` que envuelve `<AffiliateDisclosure hasAmazonLinks={hasAmazonLinks}
    locale={locale} />` sin modificar `AffiliateDisclosure.tsx` (su contrato de legal copy + prop queda
    intacto). El selector `[&_p]` es necesario porque `AffiliateDisclosure` devuelve un `<p>` sin
    className propio.

    Paso 8 — `ToolCard`. Crear `src/components/ToolCard.tsx`: `Card`/`CardContent` (`p-8`, mismo override
    que `AuthorCard`/`ServiceScopeCard`) con: `<h3 className="font-heading text-heading">{name}</h3>`;
    fila "Cómo lo uso" (`<p className="text-label uppercase tracking-wide opacity-70">{t('rowUsage')}</p>`
    seguido de `<Prose><p>{narrative}</p></Prose>`, mismo patron que `AuthorCard`'s `bio`); fila "Lo bueno"
    (label + `<p className="text-body">{pro}</p>`); fila "Lo no tan bueno" (label + `<p
    className="text-body">{con ?? t('rowConEmpty')}</p>` — el con vacio SIEMPRE muestra la fila, nunca la
    omite); fila "Dónde lo usé" (label + link locale-aware a `referenceLink.type === 'caseStudy' ?
    '/case-studies/' + caseStudy.slug : referenceLink.url`, estilo `text-primary-text underline
    underline-offset-2`, resuelto via `isPrefixableHref` + `Link` de `@/i18n/navigation` igual que
    `SiteFooter`/`websites/[slug]/page.tsx`; `referenceLink.caseStudy` ya llega poblado como objeto
    `{title, slug}` a traves del `depth` por defecto (2) de `getCachedPageBySlug('stack', locale)` — a
    diferencia de `RelatedCaseStudyBlockComponent`, que necesita `client` un nivel MAS adentro y por eso
    hace un `payload.findByID` propio con `depth:2`, aca alcanza con el chequeo `typeof caseStudy ===
    'object' && caseStudy !== null` sobre el valor ya presente en las props del bloque: NO agregar una
    query nueva para esto); y el slot de CTA que llama `resolveAffiliateCta(affiliateLink,
    name, t('ctaDefault', {tool: name}))`: `kind:'active'` renderiza `<AffiliateLink href={cta.href}
    className={buttonVariants({size:'sm'})}>{cta.label}</AffiliateLink>`; `kind:'pending'` renderiza
    `<Badge variant="outline">{t('badgePending')}</Badge>` — CONSTRAINT (Pitfall 5 de 48-RESEARCH.md):
    nunca `opacity-50` ni ninguna clase de estilo "disabled" en la card cuando el CTA esta en este
    estado, el resto de la card (nombre/narrative/pro/con/referencia) renderiza al mismo peso visual
    que cualquier otro tool, la herramienta es real, solo el link de monetizacion esta pendiente;
    `kind:'none'` no renderiza nada en el slot (nunca boton, nunca badge). Envolver TODA la card en un
    `<div data-tool-narrative={slug ?? name}>` (usar el `name` normalizado como fallback) para que un
    futuro script de verificacion de conteo de
    palabras pueda aislar el texto de cada card sin una libreria de parseo de HTML (este repo no tiene
    ninguna instalada).

    Paso 9 — `ToolStack/Component.tsx`. Server component que recibe las props del bloque completo
    (`intro`, `categoryGroups`, `gearIntro`, `gearItems`, `elegiriaHoy`, `noCommissionPick`) mas `locale`
    (via `blockProps`). Estructura: `<Container className="py-16">` con `intro` en `<Prose>`, luego
    `<AffiliateDisclosureFrame hasAmazonLinks locale={locale} />` (SIEMPRE `hasAmazonLinks=true` en
    `/stack` — el Gear siempre esta presente), luego un `.map` sobre `categoryGroups` (solo renderiza un
    grupo si `tools.length > 0`, per UI-SPEC "zero-one-many") con `<h2 className="font-heading
    text-heading">{heading}</h2>` y un grid `grid-cols-1 md:grid-cols-2 gap-6` de `ToolCard`. En ESTA
    tarea (tracer) el bloque puede tener un solo `categoryGroups[0].tools[0]` real — 48-02 agrega el resto,
    el componente ya debe soportar N grupos/tools sin cambios futuros. Dejar los slots de Gear/callouts
    (`GearCard`/`StackHighlightCallout`) como comentarios TODO explicitos citando "Task 2 de este plan" —
    NO los implementes en este Task 1, eso es expansion.

    Paso 10 — Ruta `/stack`. Crear `src/app/(frontend)/[locale]/stack/page.tsx` copiando el patron EXACTO
    de `src/app/(frontend)/[locale]/page.tsx` (Home): `export const revalidate = 60`,
    `generateStaticParams` devuelve `[]`, `getStackPage(locale)` llama `getCachedPageBySlug('stack',
    locale)`, `generateMetadata` arma `title`/`description`/`openGraph` via `buildOpenGraph` y
    `alternates` via `buildAlternates(locale, '/stack', '/en/stack')` (NO tocar `canonical.ts`, solo
    llamarla), y el componente de pagina arma un `trail` inline (`[{label: t.home, url:...},
    {label: t.stackTitle}]`, sin `url` en el ultimo item) que pasa a `buildBreadcrumbJsonLd(trail)` dentro
    de `<JsonLd>` (NO tocar `breadcrumbs.ts`, solo llamarla) y renderiza `<RenderBlocks blocks={doc.content
    ?.layout ?? []} blockProps={{ toolStack: { locale } }} />` dentro de `<main>`. Si `!doc`, `notFound()`.

    Paso 11 — Seed del tracer. Crear `scripts/seed-phase48-tracer.ts` (Local API, `node --env-file=.env
    node_modules/.bin/tsx scripts/seed-phase48-tracer.ts`) que: (a) crea el doc `affiliate-links` de
    DinoRANK con `name:'DinoRANK'`, `slug:'dinorank'`, `program:'dinorank'`, `active:true`,
    `placement:'both'`, `destinations:[{marketplace:'default',url:'https://dinorank.com/?afid=MTE2NQ=='},
    {marketplace:'registro',url:'https://dinorank.com/registro/?afid=MTE2NQ=='}]`, `tagline`/`ctaLabel`
    localizados cortos en es/en, `commissionNote:'Comisión por referido (afid).'`; (b) crea o actualiza (si
    ya existe por slug) el doc `pages` con `slug:'stack'`, `title` localizado ("Mi stack"/"My stack"), y
    `content.layout` con UN `toolStack` block cuyo `categoryGroups[0]` tiene `heading` "Investigación y
    contenido SEO"/"SEO research & content" y `tools[0]` = DinoRANK con `narrative`/`pro`/`con` reales
    tomados de 48-CONTEXT.md (aunque sean breves en esta tarea — 48-02 los completa/humaniza a >=100
    palabras) y `referenceLink.type:'custom'`, `referenceLink.url:'/case-studies'` (DinoRANK se usa en los
    7 case studies, no hay uno "primario" — enlazar al indice es la representacion honesta, decision de
    Claude's Discretion per 48-CONTEXT.md). Escribir ES primero, capturar el `id` que Payload asigna a
    cada array/block, y REUTILIZAR esos mismos ids al escribir EN (mismo patron `reapplyIds` que
    `scripts/humanize-services-index-and-landings-a.ts`/`scripts/seed-phase20-geo-pages.ts` — nunca dejar
    que Payload regenere ids en el segundo write, es la causa raiz documentada de al menos 5 bugs previos
    de colapso de contenido bilingue en este proyecto). Publicar el doc (`_status:'published'` si la
    coleccion usa drafts).

    Paso 12 — Script de verificacion. Crear `scripts/verify-stack-page.ts` (extendido por el Plan 48-02):
    arranca el dev server, hace `fetch` a `/stack` y `/en/stack`, confirma 200 en ambos, confirma que el
    `<link rel="canonical">`/`hreflang` apuntan a `/stack` (es) y `/en/stack` (en) con `x-default` = es,
    confirma que el marcador `role="note"` del disclosure aparece en el HTML ANTES del primer
    `rel="sponsored nofollow noopener"` (comparar indices de string), confirma que existe un `href`
    conteniendo `/go/dinorank`, y confirma que NINGUN `href` en la pagina contiene la subcadena literal
    `undefined`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-tracer.ts; MIGFILE=$(ls src/migrations/*phase48_tool_stack_block*.ts 2>/dev/null | head -1); test -n "$MIGFILE"; ! grep -qiE "DROP TABLE|DROP COLUMN|ALTER COLUMN.*DROP" <(sed -n '/export async function up/,/^}/p' "$MIGFILE"); grep -q "ToolStackBlock" src/payload-types.ts; npm run dev > /tmp/dev-48-01.log 2>&1 & echo $! > /tmp/dev-48-01.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-stack-page.ts | tee /tmp/verify-stack-48-01.log; grep -q "^PASS$" /tmp/verify-stack-48-01.log; git diff --stat src/lib/sitemap-data.ts src/lib/canonical.ts src/lib/breadcrumbs.ts | tee /tmp/diff-48-01.log; test ! -s /tmp/diff-48-01.log; kill "$(cat /tmp/dev-48-01.pid)" 2>/dev/null || true; npx tsc --noEmit; echo TRACER_OK</automated>
    <fails_when>El script de DB apunta a un host que no es Dokploy; el archivo de migracion falta o contiene DROP TABLE/DROP COLUMN/ALTER COLUMN...DROP; payload-types.ts no incluye ToolStackBlock; /stack o /en/stack no responden 200; verify-stack-page.ts no imprime PASS (canonical/hreflang incorrectos, disclosure ausente o despues del primer link sponsored, /go/dinorank ausente, o algun href contiene la subcadena undefined); git diff de sitemap-data.ts/canonical.ts/breadcrumbs.ts es no vacio; o tsc reporta errores.</fails_when>
  </verify>
  <acceptance_criteria>
    - El enum `enum_affiliate_links_program` en Postgres tiene los 7 valores nuevos (hostinger, dataforseo, payload, cloudinary, resend, ahrefs, google-search-console) ademas de los 5 originales
    - `/stack` y `/en/stack` responden 200 con el card de DinoRANK visible y un CTA que resuelve a `/go/dinorank`
    - El frame de `AffiliateDisclosure` (role="note", borde) aparece antes que cualquier `rel="sponsored"` en el HTML servido, en ambos locales
    - `git diff --stat` de `src/lib/sitemap-data.ts`, `src/lib/canonical.ts` y `src/lib/breadcrumbs.ts` esta vacio
    - La migracion generada es 100% aditiva (confirmado por lectura directa del archivo + el grep negativo del comando de verificacion)
    - `npx tsc --noEmit` no reporta errores
    - Ningun `href` renderizado contiene la subcadena literal `undefined`
  </acceptance_criteria>
  <done>DinoRANK renderiza de punta a punta en /stack (ambos locales) con un CTA funcional a /go/dinorank, la migracion aditiva del bloque ToolStack y del enum de program esta aplicada contra el Postgres real de Dokploy, payload-types.ts esta regenerado, y la arquitectura (config.ts, Component.tsx, page.tsx, blockRegistry.tsx) queda lista para que el Task 2 de este mismo plan agregue Gear/callouts.</done>
</task>

<task type="auto">
  <name>Task 2: Expansion — GearCard, StackHighlightCallout (elegiría hoy + no-commission pick) wireados en ToolStackComponent</name>
  <read_first>
    src/blocks/ToolStack/Component.tsx (creado en Task 1), src/components/ToolCard.tsx (creado en Task 1),
    src/lib/affiliate-cta.ts (creado en Task 1), src/components/ui/badge.tsx, src/components/ui/button-variants.ts,
    src/components/Prose.tsx, 48-UI-SPEC.md seccion "StackHighlightCallout" y "Google Search Console doc note"
  </read_first>
  <files>
    src/components/GearCard.tsx, src/components/StackHighlightCallout.tsx, src/blocks/ToolStack/Component.tsx,
    scripts/verify-stack-page.ts
  </files>
  <action>
    Paso 1 — `GearCard`. Crear `src/components/GearCard.tsx`: `Card`/`CardContent` (`p-4`, compacta, SIN
    filas de label ni pro/con — es una lista de kit personal, no de herramientas revisadas) con `<p
    className="text-label">{name}</p>` y `<AffiliateLink href={href}
    className={buttonVariants({size:'sm'})}>{t('ctaAmazon')}</AffiliateLink>` (href ya viene resuelto y
    completo desde el contenido — nunca construir ni transformar la URL aqui, nunca pasar por `/go/`,
    nunca aceptar un valor que matchee `amzn.to`). Sin `line-clamp` ni altura fija en el nombre — un
    nombre largo empuja la altura de su propia card, nunca se trunca (per UI-SPEC backstop de overflow).

    Paso 2 — `StackHighlightCallout`. Crear `src/components/StackHighlightCallout.tsx`: recibe `heading`,
    `narrative` (string plano, no richText), `badge?` (string opcional — SOLO la instancia no-commission
    lo pasa) y `gscHref?`/`gscLabel?` (opcionales, SOLO la instancia no-commission los pasa cuando el
    `noCommissionPick` resuelto tiene `program === 'google-search-console'`). Estructura: `<div
    className="bg-secondary text-secondary-foreground rounded-lg p-8">` con `<h2
    className="font-heading text-heading">{heading}</h2>`, `{badge && <Badge
    variant="default">{badge}</Badge>}`, `<Prose className="text-secondary-foreground">
    <p>{narrative}</p></Prose>`, y si `gscHref` esta presente un `<a href={gscHref} target="_blank"
    rel="noopener" className="text-secondary-foreground underline underline-offset-2">{gscLabel}</a>`
    DEBAJO del narrative — este link es deliberadamente un `<a>` plano, NUNCA `AffiliateLink` (no lleva
    `rel="sponsored"`: Google Search Console no es una relacion de afiliado, es la recomendacion sin
    comision).

    Paso 3 — Wireado en `ToolStackComponent`. Reemplazar los comentarios TODO del Task 1 con: (a) si
    `gearItems.length > 0`, renderizar `gearIntro` en `<Prose>`, `<h2>{t('gearHeading')}</h2>`, y un grid
    `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 items-start` (NO `items-stretch` — alturas desiguales
    de card no deben estirar a las vecinas, per el backstop de overflow de UI-SPEC) de `GearCard`; (b)
    `<StackHighlightCallout heading={t('elegiriaHoyHeading')} narrative={elegiriaHoy} />` (sin badge, sin
    gscHref); (c) para `noCommissionPick`: resolver el relationship (mismo chequeo `typeof === 'object' &&
    !== null` que `resolveAffiliateCta`, NUNCA asumir poblado), y si resuelve, renderizar
    `<StackHighlightCallout heading={t('noCommissionHeading')} badge={t('noCommissionBadge')}
    narrative={resolved.whyIUseIt ?? ''} gscHref={resolved.program === 'google-search-console' ?
    'https://search.google.com/search-console' : undefined} gscLabel={t('gscLinkLabel')} />` — el
    narrative de este callout se lee del campo `whyIUseIt` del doc de `affiliate-links` resuelto (campo
    de Phase 46, hoy sin consumidor real — este es su primer uso), NO de un campo nuevo en `ToolStack`. Si
    `noCommissionPick` no resuelve (null o bare id), omitir el callout completo en vez de renderizar uno
    vacio.

    Paso 4 — Extender `scripts/verify-stack-page.ts` (creado en Task 1) para tambien confirmar: la
    presencia de al menos 1 `GearCard` renderizado (grep de `t('ctaAmazon')`/"Ver en Amazon" o "View on
    Amazon"), la presencia del heading `elegiriaHoyHeading`, y — solo si el tracer ya sembro un
    `noCommissionPick` (opcional en esta tarea, 48-02 lo completa) — que ningun link dentro del callout de
    no-commission tenga `rel="sponsored"`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; npm run dev > /tmp/dev-48-01b.log 2>&1 & echo $! > /tmp/dev-48-01b.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-stack-page.ts | tee /tmp/verify-stack-48-01b.log; grep -q "^PASS$" /tmp/verify-stack-48-01b.log; curl -s http://localhost:3000/stack | grep -qi "sponsored" || true; kill "$(cat /tmp/dev-48-01b.pid)" 2>/dev/null || true; npx tsc --noEmit; echo EXPANSION_OK</automated>
    <fails_when>verify-stack-page.ts no imprime PASS tras extenderse (Gear/callout ausentes), la card de Gear renderiza con `items-stretch`/altura forzada, el callout de no-commission emite un link con rel="sponsored", o tsc reporta errores.</fails_when>
  </verify>
  <acceptance_criteria>
    - `GearCard` renderiza sin filas de label/pro/con, con CTA directo (nunca `/go/`) y sin truncar nombres largos
    - `StackHighlightCallout` se usa para las dos instancias (elegiría hoy / no-commission) sin duplicar markup
    - El link de Google Search Console dentro del callout de no-commission nunca lleva `rel="sponsored"`
    - `ToolStackComponent` ya no tiene comentarios TODO pendientes de Task 1
  </acceptance_criteria>
  <done>ToolStackComponent renderiza las 3 secciones completas (tool cards por categoría, Gear grid, los dos StackHighlightCallout) sin contenido placeholder de codigo — listo para que 48-02 solo agregue contenido, sin tocar componentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Visitante anonimo -> `/stack` | Request publico llega a un Server Component que lee contenido y relaciones de Payload sin autenticacion |
| Ejecutor de la migracion -> Postgres real (Dokploy) | Una migracion aditiva de DDL corre contra la base de produccion, sin ambiente de staging |
| Editor autenticado -> contenido de `ToolStack` | Un admin escribe relaciones `affiliateLink`/`referenceLink.caseStudy` que luego determinan hrefs publicos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | migracion `up()` de las tablas de ToolStack + ensanche del enum de `program` | high | mitigate | Leer el SQL generado completo antes de aplicar (Paso 4 de Task 1); confirmar solo CREATE TABLE/CREATE TYPE/ALTER TYPE ADD VALUE, cero DROP/ALTER COLUMN sobre columnas existentes; el comando de verificacion re-confirma esto con un grep negativo |
| T-48-02 | Information Disclosure | Resolucion de `referenceLink.caseStudy`/`affiliateLink` dentro de `getCachedPageBySlug('stack', locale)` | medium | mitigate | `getCachedPageBySlug` ya pasa `overrideAccess:false`; Payload deniega la poblacion de un doc sin acceso y deja el ID crudo (no null) — `resolveAffiliateCta` trata explicitamente ese caso como "pending", nunca expone que el acceso fue denegado ni construye un href con el ID crudo |
| T-48-03 | Tampering | Un anchor construido a mano que evite `AffiliateLink` y su `rel="sponsored nofollow noopener"` | medium | mitigate | `ToolCard`/`GearCard`/`StackHighlightCallout` enrutan cada CTA de afiliado exclusivamente por `AffiliateLink`; `verify-stack-page.ts` confirma el disclosure antes del primer `rel="sponsored"` y que el link de GSC nunca lo lleva |
| T-48-04 | Denial of Service | `/stack` lanza excepcion o 500 si un admin deja `categoryGroups`/`tools`/`gearItems` vacios | low | accept | El componente ya contempla arrays vacios (contrato zero-one-many de UI-SPEC); el tracer prueba el camino no-vacio, Task 2 no agrega superficie de fallo nueva |

</threat_model>

<verification>
`npx tsc --noEmit` limpio. `/stack` y `/en/stack` responden 200 con DinoRANK + Gear grid + ambos StackHighlightCallout renderizados de punta a punta. `git diff --stat` de `sitemap-data.ts`/`canonical.ts`/`breadcrumbs.ts` vacio. Migracion confirmada aditiva por lectura directa. Disclosure precede al primer link `rel="sponsored"` en el HTML de ambos locales.
</verification>

<success_criteria>
El bloque `ToolStack` esta registrado, migrado contra el Postgres real de Dokploy, y la ruta `/stack` sirve contenido real de punta a punta (al menos DinoRANK + Gear + los dos callouts) en ambos locales bajo el segmento compartido. La arquitectura queda lista para que el Plan 48-02 solo agregue contenido (las 6 herramientas restantes, los 13 items de Gear, footer/author links) sin tocar schema, ruta ni componentes.
</success_criteria>

<output>
Create `.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-01-SUMMARY.md` when done
</output>