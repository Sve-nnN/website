---
phase: 48-p-gina-de-stack-links-inline-en-contenido
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - scripts/seed-phase48-affiliate-links.ts
  - scripts/seed-phase48-stack-content.ts
  - scripts/seed-phase48-footer-link.ts
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
  - scripts/verify-stack-page.ts
  - scripts/verify-stack-word-count.ts
autonomous: true
requirements: [STACK-02, STACK-03, STACK-04, STACK-05, STACK-06]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cada herramienta listada en /stack tiene >=100 palabras de experiencia propia por locale, medidas sobre el HTML renderizado real (no sobre el contenido crudo del CMS)"
    - "La página incluye el bloque 'qué elegiría hoy si empezara de cero' con contenido real de Juan"
    - "La página incluye al menos una recomendación destacada sin comisión (Google Search Console) y negativos honestos por herramienta (o el copy honesto-vacío cuando Juan no reportó ninguno)"
    - "/stack está enlazada desde el footer y desde la página de autor en ambos locales, y el HTML de Home confirma que NO está en el nav principal"
    - "Cada herramienta (incluyendo las 2 excepciones documentadas DigitalOcean/Kinsta) enlaza al case study o página de servicio donde Juan la usó realmente"
    - "Los 13 hrefs de Gear son URLs completas de Amazon con tag=juantech02-20 visible, ninguno es un shortlink amzn.to"
    - "DigitalOcean/Kinsta/Hostinger renderizan con el chip 'Sin programa de afiliados todavía' (nunca disabled/greyed), con el resto del card a peso visual completo"
  artifacts:
    - scripts/seed-phase48-affiliate-links.ts
    - scripts/seed-phase48-stack-content.ts
    - scripts/seed-phase48-footer-link.ts
  key_links:
    - "7 docs de affiliate-links (dinorank ya creado en 48-01, +6 en este plan) -> ToolStack.tools[].affiliateLink / noCommissionPick"
    - "Footer.legalLinks[] (global) -> /stack, /en/stack"
    - "authors/[slug]/page.tsx -> link a /stack (código, no CMS)"
---

<objective>
Poblar `/stack` con el contenido real que Plan 48-01 dejó listo para recibir: las 6 herramientas
restantes como docs `affiliate-links` reales (DataForSEO, Payload, Cloudinary, Resend, Ahrefs,
Google Search Console), el `ToolStack` block completo del doc `pages` slug `stack` (categoryGroups
con narrativas humanizadas >=100 palabras/locale, pro/con, referenceLink; los 13 gearItems de
Amazon ya resueltos a URL completa; elegiríaHoy; noCommissionPick), y los dos links de STACK-05
(footer + autor). Cierra STACK-02..06.

Purpose: STACK-01 (Plan 48-01) construyó la arquitectura vacía; sin este plan `/stack` existe pero
no cumple ninguno de los criterios de contenido del ROADMAP.
Output: `/stack` completo y verificable en producción — 9 tools (6 con affiliate-links activo +
DinoRANK ya contado entre esos 6, + Hostinger/DigitalOcean/Kinsta sin afiliado aún) + 13 gear items +
2 callouts + footer/author links.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-CONTEXT.md
@.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-UI-SPEC.md
@.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-01-SUMMARY.md

@src/blocks/ToolStack/config.ts
@src/blocks/ToolStack/Component.tsx
@src/collections/AffiliateLinks/index.ts
@scripts/seed-phase48-tracer.ts
@scripts/seed-header-footer-content.ts
@scripts/humanize-services-index-and-landings-a.ts
@src/globals/Footer/index.ts
@src/app/(frontend)/[locale]/authors/[slug]/page.tsx
@src/app/(frontend)/[locale]/websites/[slug]/page.tsx
@src/i18n/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear los 6 docs de affiliate-links restantes (DataForSEO, Payload, Cloudinary, Resend, Ahrefs, Google Search Console)</name>
  <read_first>
    src/collections/AffiliateLinks/index.ts, scripts/seed-phase48-tracer.ts (patrón ya usado para DinoRANK
    en Plan 48-01), 48-CONTEXT.md sección "Contenido real por herramienta"
  </read_first>
  <precondition>node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts confirma que DATABASE_URI sigue apuntando al Postgres de Dokploy (re-confirmar por si esta tarea corre en una sesión nueva, per el incidente 2026-08-17)</precondition>
  <files>scripts/seed-phase48-affiliate-links.ts</files>
  <action>
    Crear `scripts/seed-phase48-affiliate-links.ts` (Local API, idempotente — si un doc con ese `slug` ya
    existe, actualizarlo en vez de duplicar, mismo patrón defensivo que los scripts `seed-phase*` ya
    existentes) que crea/actualiza estos 6 docs en la colección `affiliate-links` (todos con
    `active:true`; ES y EN escritos con locale explícito, `commissionNote`/`cookieWindowDays` son campos
    internos NO localizados así que se escriben una sola vez):

    1. **DataForSEO** — `slug:'dataforseo'`, `program:'dataforseo'`, `placement:'stack-page'`,
       `destinations:[{marketplace:'default',url:'https://dataforseo.com/?aff=1b6bcf57-3342-484a-b84b-44885374c08c'},{marketplace:'platform',url:'https://app.dataforseo.com/?aff=1b6bcf57-3342-484a-b84b-44885374c08c'},{marketplace:'sheets',url:'https://dataforseo.com/google-sheets-connector?connector_aff=1b6bcf57-3342-484a-b84b-44885374c08c'}]`,
       `commissionNote:'Comisión por referido (aff). Recarga mínima $50.'`, `tagline`/`ctaLabel`
       localizados cortos (p.ej. ctaLabel es/en: "Probar DataForSEO"/"Try DataForSEO").
    2. **Payload** — `slug:'payload'`, `program:'payload'`, `placement:'stack-page'`,
       `destinations:[{marketplace:'default',url:'https://payloadcms.com'}]`, `commissionNote:'Sin
       programa de afiliados — link directo.'`, `ctaLabel` es/en: "Ver Payload CMS"/"See Payload CMS".
    3. **Cloudinary** — `slug:'cloudinary'`, `program:'cloudinary'`, `placement:'stack-page'`,
       `destinations:[{marketplace:'default',url:'https://cloudinary.com/invites/lpov9zyyucivvxsnalc5/odxm3irnpy6wjrctdf8y?t=default'}]`,
       `commissionNote:'Créditos por referido (hasta 60), NO comisión en dinero — no confundir con
       comisión monetaria en ningún copy visible.'`, `ctaLabel` es/en: "Probar Cloudinary"/"Try
       Cloudinary".
    4. **Resend** — `slug:'resend'`, `program:'resend'`, `placement:'stack-page'`,
       `destinations:[{marketplace:'default',url:'https://resend.com'}]`, `commissionNote:'Sin programa
       de afiliados — link directo.'`, `ctaLabel` es/en: "Probar Resend"/"Try Resend".
    5. **Ahrefs** — `slug:'ahrefs'`, `program:'ahrefs'`, `placement:'stack-page'`,
       `destinations:[{marketplace:'default',url:'https://ahrefs.com'}]`, `commissionNote:'Sin programa
       de afiliados (cerrado) — link directo.'`, `ctaLabel` es/en: "Ver Ahrefs"/"See Ahrefs".
    6. **Google Search Console** — `slug:'google-search-console'`, `program:'google-search-console'`,
       `placement:'stack-page'`, `destinations:[]` (vacío, a propósito — GSC no tiene programa),
       `commissionNote:'Sin programa de afiliados — recomendación sin comisión (STACK-04).'`,
       `whyIUseIt` localizado (es/en) con un párrafo real (~40-60 palabras, no necesita >=100 porque no
       es un "tool card" regular — alimenta el callout de no-commission, no un ToolCard) basado
       literalmente en 48-CONTEXT.md: "gratis, sin programa de afiliados de ningún tipo, y ya es parte
       real de su stack de día a día" — expandir a prosa completa en primera persona, sin fabricar
       datos que Juan no dio. `ctaLabel`/`tagline` pueden quedar vacíos (esta card nunca muestra un CTA
       de ningún tipo, per el diseño de Plan 48-01 Task 2).

    NO crear docs para DigitalOcean, Kinsta ni Hostinger en este plan — permanecen sin
    `affiliate-links` doc (su `ToolStack.tools[].affiliateLink` queda `null`, lo que dispara el chip
    "Sin programa de afiliados todavía" per el diseño de Plan 48-01). Cuando Juan confirme sus links
    reales, un follow-up crea esos 3 docs y los enlaza — no anticiparlo aquí con datos inventados.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-affiliate-links.ts; node --env-file=.env node_modules/.bin/tsx -e "import{getPayload}from'payload';import config from './src/payload.config';(async()=>{const p=await getPayload({config});const r=await p.find({collection:'affiliate-links',limit:100,overrideAccess:false});const slugs=r.docs.map(d=>d.slug).sort();const need=['ahrefs','cloudinary','dataforseo','dinorank','google-search-console','payload','resend'];const missing=need.filter(s=>!slugs.includes(s));if(missing.length){console.log('FAIL missing:',missing);process.exit(1)}console.log('PASS');process.exit(0)})()"; npx tsc --noEmit; echo DOCS_OK</automated>
    <fails_when>El script de DB no confirma Dokploy; el seed falla o no es idempotente en una segunda corrida; falta cualquiera de los 7 slugs esperados (dinorank + los 6 nuevos) en la colección; o tsc reporta errores.</fails_when>
  </verify>
  <acceptance_criteria>
    - 7 docs `affiliate-links` existen con `active:true` y los `program`/`destinations`/`commissionNote` exactos descritos arriba
    - Ningún doc de DigitalOcean/Kinsta/Hostinger fue creado en este plan
    - `google-search-console` tiene `destinations:[]` y `whyIUseIt` poblado en ambos locales
    - Re-correr el script no duplica docs (idempotente)
  </acceptance_criteria>
  <done>Los 7 docs reales de affiliate-links necesarios para poblar /stack existen en la base de datos real, verificados por una consulta directa vía Local API.</done>
</task>

<task type="auto">
  <name>Task 2: Poblar el ToolStack block completo — categoryGroups (9 tools), 13 gearItems, elegiríaHoy, noCommissionPick</name>
  <read_first>
    src/blocks/ToolStack/config.ts, scripts/seed-phase48-tracer.ts, scripts/humanize-services-index-and-landings-a.ts
    (patrón reapplyIds completo), 48-CONTEXT.md completo, 48-UI-SPEC.md sección "Copywriting Contract"
    y "Component Contract Detail"
  </read_first>
  <precondition>Task 1 de este plan completó y los 7 docs de affiliate-links existen (verificado por su propio comando de verify)</precondition>
  <files>scripts/seed-phase48-stack-content.ts</files>
  <action>
    Crear `scripts/seed-phase48-stack-content.ts` que reescribe COMPLETO el `content.layout[toolStack]`
    del doc `pages` slug `stack` (el mismo doc que Plan 48-01 creó con un solo tool) con la forma final.
    Esto SUPERSEDE el contenido parcial del tracer — no hace falta preservar los ids que dejó 48-01,
    porque este task reemplaza el array entero de una sola vez por locale. Escribir primero el update en
    `locale:'es'`, leer la respuesta (`findByID` o el resultado del propio `update`) para capturar los
    `id` que Payload asignó a cada `categoryGroups[]`/`tools[]`/`gearItems[]`, y REUTILIZAR esos mismos
    ids explícitamente al construir el payload de `locale:'en'` antes de escribirlo (mismo patrón
    `reapplyIds` de `scripts/humanize-services-index-and-landings-a.ts` — sin esto, Payload genera ids
    distintos por locale y las dos versiones dejan de compartir fila, el bug que ya rompió el Home CTA en
    2026-07-12).

    Estructura de `categoryGroups` (orden = orden de renderizado, nunca se reordena):

    1. **"Investigación y contenido SEO" / "SEO research & content"**: DinoRANK (ya sembrado por 48-01 —
       este task REESCRIBE su `narrative`/`pro`/`con` con la versión final humanizada >=100 palabras por
       locale, partiendo del contenido crudo de 48-CONTEXT.md: pro precio/funcionalidad + redactor de
       contenido, contra IA de keywords floja + mapa local lento, `referenceLink.type:'custom'`
       `url:'/case-studies'`), DataForSEO (afiliado a `slug:'dataforseo'`, narrative sobre research en
       bulto y las apps locales que Juan construyó con su API, pro tokens que no vencen, contra recarga
       mínima $50, `referenceLink.type:'custom'` `url:'/case-studies'` — los 7 case studies usan
       DataForSEO igual que DinoRANK).
    2. **"Hosting e infraestructura" / "Hosting & infrastructure"**: Hostinger (`affiliateLink: null` —
       sin doc, dispara el chip pendiente; narrative sobre ser el hosting predilecto para todos sus
       clientes, pro velocidad/precio/configuración libre, `con` VACÍO — nunca fabricar uno, el
       componente ya muestra el copy honesto-vacío; `referenceLink.type:'caseStudy'` apuntando al case
       study `migracion-ecommerce-nextjs-seo-tecnico`), DigitalOcean (`affiliateLink: null`, narrative
       sobre VPS para clientes que necesitan algo específico, pro facilidad/rapidez, contra centros de
       datos limitados, `referenceLink.type:'custom'` `url:'/servicios/fullstack-development'` — excepción
       documentada, proyectos personales sin case study público), Kinsta (`affiliateLink: null`, narrative
       sobre hosting de BD/WordPress/proyectos universitarios, pro rapidez sobre GCP + staging incluido,
       contra caro + limitado en WordPress, `referenceLink` igual que DigitalOcean).
    3. **"Desarrollo, CMS y medios" / "Development, CMS & media"**: Payload (afiliado a `slug:'payload'`
       aunque sin comisión — sigue rindiendo por `/go/`, narrative sobre por qué es su alternativa a
       WordPress y cómo le permite optimizar juan-tech.com al máximo, pro optimización total, contra curva
       de aprendizaje + mantenimiento a código, `referenceLink.type:'caseStudy'` ->
       `migracion-ecommerce-nextjs-seo-tecnico`), Cloudinary (afiliado a `slug:'cloudinary'`, narrative
       sobre edición on-the-fly y cómo genera las OG images del propio sitio, pro edición on-the-fly,
       `con` VACÍO, mismo `referenceLink` que Payload), Resend (afiliado a `slug:'resend'`, narrative
       sobre el tier gratuito y la entrega confiable, pro fácil implementación + buena entrega, contra
       automatizaciones de email marketing sin explorar todavía, mismo `referenceLink` que Payload).
    4. **"Medición" / "Measurement"**: Ahrefs (afiliado a `slug:'ahrefs'`, narrative sobre el onboarding
       de proyectos de cliente y la visión de progreso que da, pro visión clara de escalamiento, contra
       muy caro, `referenceLink.type:'custom'` `url:'/case-studies'`).

    Cada `narrative` debe ser real (partir del contenido crudo de 48-CONTEXT.md, escribir en primera
    persona, pasar por el humanizer antes de escribir — sin em/en dash, sin voceo, español neutro) y
    llegar a >=100 palabras EN CADA locale — no basta con que el promedio de los 9 tools llegue a 100, la
    verificación de la Task 3 cuenta por tool. Sin copy de fabricante, sin tabla de specs.

    `gearIntro` (localized): un párrafo que mencione que compra ahí todos sus componentes de PC y que le
    gusta Amazon Prime (entrega rápida, ofertas — el contra honesto de que no aprovecha Music/Video), SIN
    convertir la mención de Prime en un `gearItem` con link propio.

    `gearItems` (13, NO localizado, mismo id-reuse discipline entre locale writes si el campo tuviera
    contenido localizado — en este caso `name`/`href` no son localizados así que basta un solo write, pero
    seguir escribiendo el array completo en la MISMA operación que escribe `categoryGroups`/etc. de cada
    locale para no generar un segundo `update` con ids potencialmente distintos):

    | name | href |
    |------|------|
    | AMD RYZEN 7 9800X3D | https://www.amazon.com/dp/B0DKFMSMYK?tag=juantech02-20 |
    | Samsung SSD 990 PRO 2TB | https://www.amazon.com/dp/B0BHJJ9Y77?tag=juantech02-20 |
    | CORSAIR Vengeance DDR5 32GB (2x16GB) | https://www.amazon.com/dp/B0CJ8ZHMVF?tag=juantech02-20 |
    | MSI MAG B850 Tomahawk MAX | https://www.amazon.com/dp/B0DT58JK2W?tag=juantech02-20 |
    | Fractal Design North Chalk White | https://www.amazon.com/dp/B09Y9FSZFX?tag=juantech02-20 |
    | ARCTIC MX-7 | https://www.amazon.com/dp/B0FT2TC2NW?tag=juantech02-20 |
    | Apple 2022 MacBook Air M2, 13", 8GB RAM, 256GB SSD | https://www.amazon.com/dp/B0BLYG2JBR?tag=juantech02-20 |
    | ASUS Prime GeForce RTX 5070 12GB GDDR7 OC | https://www.amazon.com/dp/B0DS6WPTLL?tag=juantech02-20 |
    | MSI PRO A1000PL PCIE5 | https://www.amazon.com/dp/B0GY1RBQRH?tag=juantech02-20 |
    | Noctua NH-D15 G2 chromax.Black | https://www.amazon.com/dp/B0FXGWKHND?tag=juantech02-20 |
    | Logitech G305 | https://www.amazon.com/dp/B07CMS5Q6P?tag=juantech02-20 |
    | Logitech G435 | https://www.amazon.com/dp/B0FXYGKNZ4?tag=juantech02-20 |
    | LG UltraWide Monitor 29U511A 29" | https://www.amazon.com/dp/B0FHLN6M8Y?tag=juantech02-20 |

    Estas 13 URLs ya fueron resueltas y verificadas en vivo durante el planning de esta fase (cada
    `amzn.to` original de 48-CONTEXT.md fue seguido con `curl -sIL` el 2026-09-04, confirmando que el
    redirect de Amazon YA incluye `tag=juantech02-20` — el `dp/{ASIN}` es la forma canónica del mismo
    producto, sin los parámetros de sesión de Amazon que no aportan nada y podrían expirar). Antes de
    escribirlas en el doc, correr un spot-check propio: `curl -s -o /dev/null -w "%{http_code}"` sobre
    al menos 3 de las 13 (elegidas al azar) y confirmar 200 — si alguna ya no resuelve, marcarlo en el
    SUMMARY en vez de escribir una URL rota, NO sustituir por el shortlink original.

    `elegiriaHoy` (localized, >=1 párrafo real): DataForSEO+DinoRANK para datos de cliente, Hostinger
    para levantar proyectos, GSC + tier free de Ahrefs + GA4 para medición, y la regla de stack de código
    (CMS -> Payload, estático -> Astro, WordPress si el cliente lo pide -> directo en Hostinger),
    aprovechando los free tiers de Resend y Cloudinary — contenido literal de 48-CONTEXT.md sección
    "Bloques obligatorios", expandido a prosa fluida.

    `noCommissionPick`: relationship al doc `google-search-console` creado en Task 1.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; for asin in B0DKFMSMYK B0GY1RBQRH B0FHLN6M8Y; do code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 -A "Mozilla/5.0" "https://www.amazon.com/dp/$asin?tag=juantech02-20"); echo "$asin -> $code"; done; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-stack-content.ts; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-stack-content.ts; npx tsc --noEmit; echo CONTENT_OK</automated>
    <fails_when>El script de DB no confirma Dokploy; el spot-check de 3 ASINs no devuelve 200; el seed falla, no es idempotente en una segunda corrida (crea contenido duplicado o pierde ids entre locale writes), o deja algún `gearItems[].href` conteniendo `amzn.to`; o tsc reporta errores.</fails_when>
  </verify>
  <acceptance_criteria>
    - `content.layout` del doc `stack` tiene 4 categoryGroups con 9 tools totales (2 en investigación, 3 en hosting, 3 en dev/cms/medios, 1 en medición — de los cuales 3, Hostinger/DigitalOcean/Kinsta, quedan con `affiliateLink: null` a propósito) y 13 gearItems
    - Cero `href` en `gearItems` contiene la subcadena `amzn.to`
    - `noCommissionPick` resuelve al doc `google-search-console`
    - Correr el script dos veces seguidas no duplica bloques ni pierde contenido en ningún locale
  </acceptance_criteria>
  <done>El doc `pages` slug `stack` tiene el ToolStack block completo (9 tools reales + 13 gear items + elegiríaHoy + noCommissionPick) en ambos locales, escrito de forma idempotente contra la base real.</done>
</task>

<task type="auto">
  <name>Task 3: Footer + página de autor (STACK-05), conteo de palabras real, y verificación final de fase</name>
  <read_first>
    src/globals/Footer/index.ts, scripts/seed-header-footer-content.ts, src/app/(frontend)/[locale]/authors/[slug]/page.tsx,
    src/app/(frontend)/[locale]/websites/[slug]/page.tsx (patrón exacto del link con text-primary-text),
    src/i18n/navigation.ts, src/components/PageHero.tsx (para confirmar que no hay nav global en Home)
  </read_first>
  <files>
    scripts/seed-phase48-footer-link.ts, src/app/(frontend)/[locale]/authors/[slug]/page.tsx,
    scripts/verify-stack-word-count.ts, scripts/verify-stack-page.ts
  </files>
  <action>
    Paso 1 — Footer. Crear `scripts/seed-phase48-footer-link.ts` que PRIMERO lee el global `Footer` vigente
    con `payload.findGlobal({slug:'footer', locale:'es'})` y `locale:'en'` por separado, toma el array
    `legalLinks` de cada uno TAL CUAL viene (con sus `id` existentes intactos — hoy son Privacidad/Términos
    per `seed-header-footer-content.ts`), le hace `push` de UNA entrada nueva
    `{label:'Mi stack', href:'/stack'}` (es) / `{label:'My stack', href:'/stack'}` (en — la ruta es la
    misma en ambos locales, el prefijo `/en` lo agrega el componente de render vía `isPrefixableHref`,
    igual que el resto de `legalLinks`), y escribe de vuelta CON `updateGlobal` el array COMPLETO
    (existentes + nueva) preservando los `id` de los existentes — NUNCA reconstruir el array desde cero
    con solo Privacidad/Términos/Stack hardcodeados, porque `updateGlobal` reemplaza el array entero y
    regenerar ids ya causó pérdida de contenido bilingüe en este proyecto (Fases 5/13/14/19/21/40, ver
    STATE.md). Idempotente: si ya existe una entrada con `href:'/stack'`, no duplicar.

    Paso 2 — Página de autor. En `src/app/(frontend)/[locale]/authors/[slug]/page.tsx`, agregar el import
    `import { Link as LocaleLink } from '@/i18n/navigation'` (no está importado hoy — el único `Link` de
    este archivo es el plano de `next/link` para eventos externos) y, dentro del `<Container>` principal,
    después de la última sección existente (`caseStudies`) y antes del cierre de `</Container>`, agregar un
    link simple: `<LocaleLink href="/stack" className="text-primary-text underline underline-offset-2">
    {locale === 'es' ? 'Ver mi stack de herramientas' : 'See my tool stack'}</LocaleLink>` — mismo
    tratamiento visual que `/websites/[slug]/page.tsx` usa para "View related case study". NO agregar este
    link a `AuthorCard.tsx` (ese componente también renderiza como byline en 100+ posts/case-studies —
    agregarlo ahí diluiría exactamente el link equity de servicios que STACK-05 protege).

    Paso 3 — Conteo de palabras real. Crear `scripts/verify-stack-word-count.ts`: hace fetch de `/stack` y
    `/en/stack` renderizados, y para cada `data-tool-narrative="{slug-o-name}"` (marcador agregado por
    `ToolCard` en Plan 48-01 Task 1) extrae el contenido HTML entre esa apertura y su cierre balanceado,
    le quita las etiquetas HTML con una regex simple (`replace(/<[^>]+>/g, ' ')`), cuenta palabras
    (`.trim().split(/\s+/).filter(Boolean).length`) y falla si alguna narrativa tiene menos de 100 —
    reportando el nombre de la herramienta y el conteo real, no solo pass/fail. No requiere ninguna
    librería nueva (sin cheerio/jsdom, per el Package Legitimacy Audit de 48-RESEARCH.md: cero paquetes
    nuevos esta fase).

    Paso 4 — Extender `scripts/verify-stack-page.ts` (de Plan 48-01) para confirmar también: (a) el
    footer de ambas rutas (`/` y `/en`) contiene un `href` a `/stack` con el label correcto por locale;
    (b) `/authors/juan-carlos-angulo` (o el slug real del único autor) contiene el link a `/stack`; (c) el
    HTML de Home (`/` y `/en`) NO contiene ningún link a `/stack` dentro del `<nav>` del header (grep
    acotado a la región del `<header>`/`<nav>`, no un grep global de la página — un grep global fallaría
    falsamente en cuanto el footer, ya presente en la misma página, tenga el link legítimo); (d) para cada
    uno de los 9 tools, que el link de "Dónde lo usé" resuelva a una URL real (`/case-studies` o un slug
    real de case study, o `/servicios/fullstack-development`), nunca vacío ni `#`.
  </action>
  <verify>
    <automated>set -e; cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload; node --env-file=.env node_modules/.bin/tsx scripts/db/04-which-database.ts; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-footer-link.ts; node --env-file=.env node_modules/.bin/tsx scripts/seed-phase48-footer-link.ts; npm run dev > /tmp/dev-48-02.log 2>&1 & echo $! > /tmp/dev-48-02.pid; for i in $(seq 1 30); do curl -sf http://localhost:3000/ > /dev/null 2>&1 && break; sleep 1; done; node --env-file=.env node_modules/.bin/tsx scripts/verify-stack-word-count.ts | tee /tmp/verify-wc-48-02.log; grep -q "^PASS$" /tmp/verify-wc-48-02.log; node --env-file=.env node_modules/.bin/tsx scripts/verify-stack-page.ts | tee /tmp/verify-stack-48-02.log; grep -q "^PASS$" /tmp/verify-stack-48-02.log; kill "$(cat /tmp/dev-48-02.pid)" 2>/dev/null || true; npx tsc --noEmit; echo PHASE_CONTENT_OK</automated>
    <fails_when>El script de footer no es idempotente (duplica la entrada en la segunda corrida); verify-stack-word-count.ts reporta cualquier tool con menos de 100 palabras en cualquier locale; verify-stack-page.ts no confirma footer+autor+ausencia-en-nav+referenceLinks; o tsc reporta errores.</fails_when>
  </verify>
  <acceptance_criteria>
    - Footer (es + en) tiene la entrada "Mi stack"/"My stack" -> `/stack` sin perder Privacidad/Términos
    - `/authors/{slug-real}` (ambos locales) tiene el link a `/stack`
    - Home (`/` y `/en`) no tiene `/stack` dentro de su `<nav>` de header
    - Las 9 herramientas listadas tienen >=100 palabras por locale, medidas sobre HTML real
    - Los 9 links de "Dónde lo usé" resuelven a una URL no vacía
  </acceptance_criteria>
  <done>STACK-02 a STACK-06 están cumplidos y verificados de punta a punta contra el dev server real: contenido humano >=100 palabras por herramienta y locale, callouts obligatorios presentes, footer/autor enlazando a /stack, nav principal sin el link, y cada herramienta con su referencia real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Editor autenticado -> 7 docs `affiliate-links` + contenido de `ToolStack` | Escrituras vía Local API con `overrideAccess` implícito en `true` (scripts corren sin `user`) — el riesgo no es acceso público sino escribir datos incorrectos en producción sin sandbox |
| Visitante anónimo -> Footer/página de autor | Los nuevos links son de solo lectura, sin superficie de input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-05 | Tampering | 13 hrefs de Amazon escritos directo en el CMS sin pasar por `/go/` | high | mitigate | Cada href se resolvió por redirect real (`curl -sIL`) durante el planning y se re-verifica por spot-check en el `<verify>` de Task 2; el formato canónico `amazon.com/dp/{ASIN}?tag=juantech02-20` nunca es un shortlink `amzn.to`, cumpliendo el ban de Amazon Program Policies a "Redirecting Links" que motivó toda la arquitectura `/go/` de Phase 46/47 |
| T-48-06 | Tampering | Escritura del Footer global reemplaza el array `legalLinks` completo | high | mitigate | El script de Task 3 SIEMPRE lee el array vigente antes de escribir y hace `push` conservando los `id` existentes — nunca reconstruye Privacidad/Términos desde cero, evitando repetir el incidente de pérdida de contenido bilingüe de Fases 5/13/14/19/21/40 |
| T-48-07 | Repudiation / Data Integrity | `google-search-console` doc con `destinations:[]` podría interpretarse erróneamente como un doc "roto" por un editor futuro que le agregue un destino real | low | accept | Documentado explícitamente en el `admin.description` del campo `noCommissionPick` (Plan 48-01) y en `48-CONTEXT.md`; no hay enforcement de código porque es una decisión editorial, no técnica |
| T-48-08 | Information Disclosure | El script de word-count hace fetch público de `/stack` sin autenticación | low | accept | Es exactamente el mismo acceso que tiene cualquier visitante anónimo; no hay dato sensible en la respuesta |

</threat_model>

<verification>
`npx tsc --noEmit` limpio. Los 7 docs `affiliate-links` de este plan existen y son correctos. El doc `stack` tiene el `ToolStack` block completo (9 tools + 13 gear + 2 callouts) en ambos locales, escrito de forma idempotente. Cada tool tiene >=100 palabras por locale medidas sobre HTML real. Footer y página de autor enlazan a `/stack`; Home no la enlaza en su nav. Cada "Dónde lo usé" resuelve a una URL real.
</verification>

<success_criteria>
`/stack` cumple los 5 success criteria del ROADMAP para STACK-01..06 con contenido real, verificado contra el dev server conectado al Postgres real de Dokploy — no queda ningún placeholder, dato fabricado, ni herramienta con menos de 100 palabras.
</success_criteria>

<output>
Create `.planning/phases/48-p-gina-de-stack-links-inline-en-contenido/48-02-SUMMARY.md` when done
</output>
