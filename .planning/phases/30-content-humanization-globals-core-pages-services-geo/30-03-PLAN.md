---
phase: 30-content-humanization-globals-core-pages-services-geo
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - scripts/humanize-services-index-and-landings-a.ts
  - scripts/humanize-services-landings-b.ts
  - scripts/humanize-geo-pages.ts
autonomous: true
requirements: [VOICE-06]

must_haves:
  truths:
    - "The services index page (slug: services) hero/content/callToAction copy is rewritten in Juan's voice, both locales, and its per-service link labels/URLs remain correct (es -> /servicios/..., en -> /en/services/...)"
    - "All 4 service landing pages (seo-technical-audit, seo-consulting, fullstack-development, ai-seo-geo) have their hero/content/faq/callToAction copy rewritten in Juan's voice, both locales"
    - "Both geo-pages (seo-tecnico-madrid, seo-tecnico-lima) have their hero/content/faq/callToAction copy rewritten in Juan's voice, both locales, and any LocalProofSection stats/testimonial marked [PLACEHOLDER] are left untouched rather than rewritten as if real"
    - "Every rewrite uses the update path only (docs already exist, docs.length===0 branch never triggers) — no new pages doc is created"
    - "No block/sub-array id is orphaned across any locale write on any of the 7 pages touched"
    - "No meta.title/meta.description/targetKeyword field is touched on any of these 7 pages"
  artifacts:
    - path: "scripts/humanize-services-index-and-landings-a.ts"
      provides: "Rewrites services index + seo-technical-audit + seo-consulting landing pages, both locales"
      contains: "reapplyIds"
    - path: "scripts/humanize-services-landings-b.ts"
      provides: "Rewrites fullstack-development + ai-seo-geo landing pages, both locales"
      contains: "reapplyIds"
    - path: "scripts/humanize-geo-pages.ts"
      provides: "Rewrites seo-tecnico-madrid + seo-tecnico-lima geo-pages, both locales"
      contains: "reapplyIds"
  key_links:
    - from: "scripts/humanize-services-index-and-landings-a.ts"
      to: "pages docs slug=services, seo-technical-audit, seo-consulting"
      via: "upsertPage() helper using reapplyIds() + payload.update, update-only path"
      pattern: "upsertPage"
    - from: "scripts/humanize-geo-pages.ts"
      to: "pages docs slug=seo-tecnico-madrid, seo-tecnico-lima"
      via: "upsertPage() helper using reapplyIds() + payload.update, update-only path"
      pattern: "reapplyIds"
---

<objective>
Rewrite the real editorial copy of the Services index, 4 service landing pages, and 2 geo-pages in Juan's calibrated voice, in both locales, via Payload Local API against the real Neon Postgres — reusing the `reapplyIds`/`upsertPage` helper pair from `seed-phase19-service-pages.ts`, the most mature id-reuse implementation in the repo (blockType-mismatch detection instead of silent misattachment).

Purpose: Closes success criterion 3 of Phase 30 — the last content group before this tramo's mandatory verification (Plan 30-04). All 7 pages already exist and are populated (Phase 19/20 already ran), so every script here only needs the update path, never create.
Output: Three new one-off scripts under `scripts/`, applied once against production, split by page count for context budget (index+2 landings / 2 landings / 2 geo-pages).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-content-humanization-globals-core-pages-services-geo/30-CONTEXT.md
@.planning/phases/30-content-humanization-globals-core-pages-services-geo/30-PATTERNS.md
@.planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md
@.planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md
@research/voice-sample-juan.md
@CLAUDE.md
</context>

<interfaces>
<!-- reapplyIds/upsertPage — the pattern to standardize on for every task in this plan. -->

From scripts/seed-phase19-service-pages.ts (full file, 332 lines — copy `reapplyIds`/`upsertPage` near-verbatim):
```typescript
function reapplyIds(
  freshLayout: Record<string, unknown>[],
  referenceLayout: Record<string, unknown>[] | undefined,
): Record<string, unknown>[] {
  if (!referenceLayout) return freshLayout
  return freshLayout.map((block, i) => {
    const refBlock = referenceLayout[i] as Record<string, unknown> | undefined
    if (!refBlock || refBlock.blockType !== block.blockType) {
      if (refBlock) console.warn(`reapplyIds: blockType mismatch at index ${i} ...`)
      return block
    }
    const withId: Record<string, unknown> = { ...block, id: refBlock.id }
    if (block.blockType === 'content' && Array.isArray(block.columns)) { /* reuse columns[].id by index */ }
    if (block.blockType === 'faq' && Array.isArray(block.faqs)) { /* reuse faqs[].id by index */ }
    if (block.blockType === 'callToAction' && Array.isArray(block.links)) { /* reuse links[].id by index */ }
    return withId
  })
}

async function upsertPage(payload, { slug, titleByLocale, buildLayout }) {
  const { docs } = await payload.find({ collection: 'pages', where: { slug: { equals: slug } }, limit: 1 })
  const docId = docs[0].id // Phase 30: docs.length===0 branch never triggers, all 7 pages already exist
  const refetched = await payload.findByID({ collection: 'pages', id: docId, depth: 0 })
  const referenceLayout = refetched.content?.layout as Record<string, unknown>[] | undefined
  for (const locale of LOCALES) {
    const layoutWithIds = reapplyIds(buildLayout(locale), referenceLayout)
    await payload.update({ collection: 'pages', id: docId, locale, data: { title: titleByLocale[locale], content: { layout: layoutWithIds } } })
  }
}
```

Lexical helpers (same file, lines 34-85), reuse verbatim: `lexicalParagraph(text)`, `lexicalWithHeading(heading, paragraphs)`.

Confirmed live `content.layout` blockType orders:
- `services` index: `['hero', 'content', 'callToAction']`
- 4 service landings + 2 geo-pages: `['hero', 'content', 'faq', 'callToAction']`

Confirmed live page slugs (all already exist, populated): `services, seo-technical-audit, seo-consulting, fullstack-development, ai-seo-geo, seo-tecnico-madrid, seo-tecnico-lima`.

Fields to rewrite per page: `hero.title`/`hero.subtitle` (variant `listing` — no `cityName`/`inlineStat`, those are `local-landing` variant fields not used here), `content.columns[].richText` (Lexical, built via `lexicalWithHeading`/`lexicalParagraph`), `faq.title`/`faqs[].question`/`faqs[].answer` (landings/geo only, index has no faq block), `callToAction.richText`/`links[].link.label`.

Services index has a `content` block whose `columns[]` are per-service link cards (`enableLink: true`, `link.url` locale-branched `/servicios/{slug}` vs `/en/services/{slug}`, `link.label` "Ver más"/"Learn more") — rewrite `richText` (service name + description) but do NOT alter the locale-branched URL logic or the link.label pattern beyond humanizing "Ver más"/"Learn more" itself if desired, keeping it short/functional.

Geo-page caution (per 30-PATTERNS.md, VOICE-PROFILE services/geo section): confirm during execution whether `seo-tecnico-madrid`/`seo-tecnico-lima` now carry a `LocalProofSection` block (added by Phase 33/34, after this phase's PATTERNS.md was written) with stats/testimonial content. If so, and if any stat/testimonial is marked `[PLACEHOLDER]`, leave it untouched — do not humanize a placeholder marker into prose that reads as a real stat/quote.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Humanize services index + seo-technical-audit + seo-consulting</name>
  <files>scripts/humanize-services-index-and-landings-a.ts</files>
  <read_first>
    scripts/seed-phase19-service-pages.ts (full file — reapplyIds/upsertPage/Lexical helpers to reuse)
    scripts/seed-phase19-data/group-a.ts (existing copy for indexPageCopy/auditServiceCopy/consultingServiceCopy — read for structure and current field values, do not just re-run these literals)
    ~/.claude/skills/humanizer/SKILL.md
    research/voice-sample-juan.md
  </read_first>
  <action>
    Create `scripts/humanize-services-index-and-landings-a.ts` (same import convention as `seed-phase19-service-pages.ts`; run with `node --env-file=.env node_modules/.bin/tsx scripts/humanize-services-index-and-landings-a.ts`). Copy `reapplyIds`, `upsertPage`, `lexicalParagraph`, `lexicalWithHeading` near-verbatim from the analog.

    Read the LIVE current copy of the `services` index, `seo-technical-audit`, and `seo-consulting` pages via `payload.findByID` per locale before writing (do not assume `seed-phase19-data/group-a.ts`'s literals still match production — they may have drifted since Phase 19 shipped). Rewrite hero title/subtitle, content columns' richText (service names/descriptions on the index; includes/process sections on the 2 landings), faq title/questions/answers (landings only), callToAction richText/link label — in Juan's calibrated voice, both locales, preserving factual claims (what each service includes, process steps) exactly.

    Call `upsertPage` for `SERVICES_INDEX_SLUG` and `SERVICE_SLUGS[0]`/`SERVICE_SLUGS[1]` (import from `@/lib/services-data` or `@/lib/service-slugs`, same as the analog) — the update-only path (docs already exist), never the create branch.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/humanize-services-index-and-landings-a.ts</automated>
  </verify>
  <done>Script exists and, when run, rewrites the services index and seo-technical-audit/seo-consulting landing pages' hero/content/faq/callToAction copy in both locales via the update-only upsertPage path, with block/sub-array ids reused across locale writes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Humanize fullstack-development + ai-seo-geo landings</name>
  <files>scripts/humanize-services-landings-b.ts</files>
  <read_first>
    scripts/seed-phase19-service-pages.ts (reapplyIds/upsertPage/Lexical helpers, same pattern as Task 1)
    scripts/seed-phase19-data/group-b.ts (existing copy for fullstackServiceCopy/geoServiceCopy — read for structure/current values)
    ~/.claude/skills/humanizer/SKILL.md
    research/voice-sample-juan.md
  </read_first>
  <action>
    Create `scripts/humanize-services-landings-b.ts`, identical pattern to Task 1's script but for `SERVICE_SLUGS[2]` (fullstack-development) and `SERVICE_SLUGS[3]` (ai-seo-geo). Read live current copy first, rewrite hero/content/faq/callToAction in Juan's calibrated voice, both locales, preserving factual claims (tech stack mentioned, AI-SEO/GEO specifics) exactly. Split into its own script/task rather than combining with Task 1 to keep this plan's per-task context cost manageable — do not merge back into the same file.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/humanize-services-landings-b.ts</automated>
  </verify>
  <done>Script exists and, when run, rewrites fullstack-development and ai-seo-geo landing pages' hero/content/faq/callToAction copy in both locales via the update-only upsertPage path, with ids reused across locale writes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Humanize both geo-pages</name>
  <files>scripts/humanize-geo-pages.ts</files>
  <read_first>
    scripts/seed-phase20-geo-pages.ts (full file — "structurally identical to seed-phase19-service-pages.ts" per its own header, confirm any geo-specific field differences e.g. LocalProofSection before writing)
    ~/.claude/skills/humanizer/SKILL.md
    research/voice-sample-juan.md
    .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md (geo-page placeholder-data caution)
  </read_first>
  <action>
    Create `scripts/humanize-geo-pages.ts`, same `reapplyIds`/`upsertPage`/Lexical-helper pattern, for `seo-tecnico-madrid` and `seo-tecnico-lima`. Read live current copy first via `payload.findByID` per locale.

    Before rewriting, check whether either page's live `content.layout` now includes a `LocalProofSection` block (possibly added by a later phase, per PATTERNS.md's flag). If present, inspect its `stats[].value`/`stats[].label`/`testimonial.quote`/`testimonial.authorBusiness` — if any value is marked or clearly a `[PLACEHOLDER]` stand-in rather than real data, leave that field completely untouched (do not humanize placeholder markers into prose that would misrepresent them as real). Only rewrite hero/content/faq/callToAction, plus any confirmed-real (non-placeholder) LocalProofSection text, in Juan's calibrated voice, both locales.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/humanize-geo-pages.ts</automated>
  </verify>
  <done>Script exists and, when run, rewrites both geo-pages' hero/content/faq/callToAction copy in both locales via the update-only upsertPage path; any placeholder-marked LocalProofSection data is confirmed left untouched; ids reused across locale writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| script -> Local API -> Neon Postgres | Direct production writes via Local API, same trust boundary as Plan 30-01/30-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-30-07 | Tampering | content.layout full-replace orphaning sibling blocks/other locale on 7 pages | mitigate | reapplyIds/upsertPage pattern reused near-verbatim from seed-phase19/20 — the most defensive id-reuse implementation in the repo, includes blockType-mismatch warning instead of silent misattachment |
| T-30-08 | Tampering (accidental create) | upsertPage's create branch firing unexpectedly (duplicate page created instead of update) | mitigate | Live slug inventory confirmed in PATTERNS.md before planning (all 7 slugs exist); each task's action explicitly states the update-only path is expected, create branch should never trigger — script should log/warn if it does |
| T-30-09 | Information Disclosure | Humanizing a [PLACEHOLDER]-marked local stat into prose that reads as real data | mitigate | Task 3 explicitly requires checking for and skipping placeholder-marked LocalProofSection fields |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new package installed this plan |
</threat_model>

<verification>
1. `node --env-file=.env node_modules/.bin/tsx scripts/humanize-services-index-and-landings-a.ts` exits 0, no "create" log line appears (confirms update-only path)
2. `node --env-file=.env node_modules/.bin/tsx scripts/humanize-services-landings-b.ts` exits 0, same confirmation
3. `node --env-file=.env node_modules/.bin/tsx scripts/humanize-geo-pages.ts` exits 0, same confirmation, plus manual confirmation of placeholder handling if LocalProofSection is present
4. Re-read all 7 pages in both locales post-write, confirm block ids match pre-write ids and no sibling block/locale lost data
5. No task touches `meta.title`/`meta.description`/`targetKeyword` on any of the 7 pages
</verification>

<success_criteria>
- Services index + 4 service landings + 2 geo-pages are rewritten in Juan's calibrated voice, both locales
- All 7 pages used the update-only path, no duplicate page created
- No placeholder-marked local data was humanized into fake-real prose
- No block/array id orphaned across any locale write
- No SEO/meta field touched
</success_criteria>

<output>
Create `.planning/phases/30-content-humanization-globals-core-pages-services-geo/30-03-SUMMARY.md` when done
</output>
