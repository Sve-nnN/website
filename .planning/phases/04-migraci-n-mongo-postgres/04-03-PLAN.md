---
phase: 04-migraci-n-mongo-postgres
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [scripts/migrate/steps/02-authors-categories.ts, scripts/migrate/data/remap-table.json]
autonomous: true
requirements: [MIGR-02, MIGR-03, MIGR-04]

must_haves:
  truths:
    - "Cada autor y categoría real de JuanPortfolio existe en la colección authors/categories del nuevo backend, con slug idéntico al original"
    - "authors.avatar apunta al Media ya migrado en wave 2 (vía remap-table), no a la URL vieja de Vercel Blob"
    - "La remap-table queda con las claves authors y categories completas, consumidas por posts/case-studies en waves siguientes"
  artifacts:
    - path: "scripts/migrate/steps/02-authors-categories.ts"
      provides: "Migra authors.json y categories.json -> colecciones authors/categories del nuevo Postgres"
  key_links:
    - from: "scripts/migrate/steps/02-authors-categories.ts"
      to: "scripts/migrate/lib/remap-table.ts"
      via: "getMapping(table, 'media', oldAvatarId) para resolver avatar remapeado"
      pattern: "getMapping.*media"
---

<objective>
Migrar Authors y Categories: dos colecciones simples de taxonomía/perfil, sin richText, que son prerequisito de Posts (author, categories) y CaseStudies (mismo orden del roadmap).

Purpose: Completar el segundo escalón del orden mandatado por el roadmap (Media → Authors/Categories → resto), habilitando las relaciones `posts.author`/`posts.categories` en wave 5.
Output: `scripts/migrate/steps/02-authors-categories.ts`, remap-table con `authors` y `categories` completas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/migrate/lib/remap-table.ts
@scripts/migrate/data/export/authors.json
@scripts/migrate/data/export/categories.json
@src/collections/Authors/index.ts
@src/collections/Categories/index.ts

<field_mapping>
**Authors** (JuanPortfolio `Authors/index.ts` -> juan-payload `Authors/index.ts`, campo a campo):
- `name` -> `name` (verbatim, no localizado en ninguno de los dos esquemas)
- `jobTitle` -> `jobTitle` (localizado en ambos — copiar por locale: crear en `locale:'es'` con el valor español, luego `payload.update` en `locale:'en'` con el valor inglés; el dump con `locale:'all'` trae objetos `{es, en}` por campo localizado)
- `bio` -> `bio` (localizado igual que jobTitle)
- `avatar` -> `avatar` (relación a media; resolver `getMapping(table,'media', String(doc.avatar))` — `doc.avatar` puede venir como ID plano o como objeto poblado con `.id` según depth del dump; normalizar a string ID antes de mapear)
- `slug` -> `slug` **verbatim** (MIGR-03: asignar el string tal cual, sin pasar por el título) — nota: el `slugField` del proyecto nuevo tiene un hook `beforeValidate` que lowercasea y reemplaza espacios por guiones; como el slug viejo ya está en ese formato, el hook es un no-op, pero verificar tras la migración que el slug resultante sea idéntico al original
- Campos del esquema viejo que NO existen en el nuevo (`role`, `expertise`, `socialMedia`, `education`, `experience`, tab SEO) están intencionalmente fuera de alcance — decisión ya tomada en Phase 1 (`Authors/index.ts` nuevo documenta "Trimmed... per CONTEXT.md's discretion"), no es un recorte de esta fase

**Categories** (JuanPortfolio `Categories.ts` -> juan-payload `Categories/index.ts`):
- `title` -> `title` (localizado en ambos)
- `description` -> `description` (localizado en ambos)
- `slug` -> `slug` **verbatim**
- Campos viejos sin equivalente nuevo (`primaryKeyword`, `faqs`, `indexingControl`, tab SEO) fuera de alcance — tooling interno / el plugin SEO del nuevo esquema ya cubre metas
</field_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar Authors y Categories con slugs verbatim y avatar remapeado</name>
  <files>scripts/migrate/steps/02-authors-categories.ts</files>
  <action>
Crear `scripts/migrate/steps/02-authors-categories.ts`: importa `config` desde `../../../src/payload.config`, `getPayload({config})`, carga la remap-table con `loadRemapTable()`.

Para `authors.json`: por cada doc, si `getMapping(table,'authors',doc.id)` existe, saltar. Si no, crear en locale `es` con los campos de `<field_mapping>` (slug verbatim, avatar remapeado si existe — si `getMapping` devuelve `undefined` para el avatar, crear sin avatar y loguear un warning, no abortar), luego `payload.update` en locale `en` con los campos localizados en inglés. Guardar `setMapping(table,'authors',doc.id,created.id)`.

Para `categories.json`: mismo patrón (slug verbatim, título/descripción por locale), `setMapping(table,'categories',doc.id,created.id)`.

`saveRemapTable(table)` al final. Ejecutar de verdad: `npx tsx scripts/migrate/steps/02-authors-categories.ts`.

Después de correr, verificar con un query rápido (`payload.find({collection:'authors', where:{slug:{equals: <un slug real conocido>}}})`) que al menos un autor conocido (ej. el propio Juan Carlos Angulo, autor principal del blog) existe con el slug idéntico al de `authors.json`.
  </action>
  <verify>
    <automated>node -e "const t=require('./scripts/migrate/data/remap-table.json'); const a=require('./scripts/migrate/data/export/authors.json'); const c=require('./scripts/migrate/data/export/categories.json'); if(Object.keys(t.authors||{}).length < a.length) process.exit(1); if(Object.keys(t.categories||{}).length < c.length) process.exit(1); console.log('ok')"</automated>
  </verify>
  <done>La remap-table tiene las claves `authors` y `categories` completas (una entrada por cada doc de los dumps respectivos), y los slugs de los documentos creados coinciden verbatim con los originales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Script → Postgres nuevo (Local API) | Crea contenido real de producción en el backend nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | Re-ejecución duplica authors/categories | mitigate | Idempotencia vía `getMapping` antes de cada `create`, igual que wave 2 |
| T-04-07 | Repudiation | Slug regenerado por error en vez de verbatim | mitigate | Asignación directa del string `doc.slug`, nunca derivado de `title`; verificación post-run compara slugs 1:1 contra el dump |
</threat_model>

<verification>
`npx tsx scripts/migrate/steps/02-authors-categories.ts` corre sin excepción no controlada; comando `<automated>` de la task confirma cobertura completa de remap.
</verification>

<success_criteria>
Authors y Categories completos en el backend nuevo, slugs verbatim, remap-table lista para que Posts (wave 5) resuelva `author`/`categories`.
</success_criteria>

<output>
Create `.planning/phases/04-migraci-n-mongo-postgres/04-03-SUMMARY.md` when done
</output>
