---
phase: 04-migraci-n-mongo-postgres
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified: [scripts/migrate/steps/05-case-studies.ts, scripts/migrate/data/remap-table.json]
autonomous: true
requirements: [MIGR-02, MIGR-03, MIGR-04, MIGR-05]

must_haves:
  truths:
    - "Cada case study real de JuanPortfolio existe en case-studies del nuevo backend, con slug identico al original"
    - "El richText viejo (unico campo de contenido) se preserva integro en clientContext, con referencias a media reescritas a Cloudinary"
    - "El nuevo esquema estructurado (kpis, challenge, solution, results, client/sector/period/services) no tiene datos de origen en el esquema viejo -- el script NO inventa esos datos; los deja vacios/con un placeholder minimo que satisfaga la validacion de Payload (kpis exige minRows:1) y produce una lista explicita de case studies que requieren autoria editorial manual de esos campos"
  artifacts:
    - path: "scripts/migrate/steps/05-case-studies.ts"
      provides: "Migra case-studies.json -> coleccion case-studies del nuevo Postgres"
  key_links:
    - from: "scripts/migrate/steps/05-case-studies.ts"
      to: "scripts/migrate/lib/richtext-remap.ts"
      via: "remapRichTextMediaRefs(doc.content, remapTable.media)"
      pattern: "remapRichTextMediaRefs"
---

<objective>
Migrar CaseStudies. Este es el unico tipo de contenido de la fase con un mismatch estructural real entre origen y destino: el esquema viejo de JuanPortfolio es una sola pagina de richText libre; el esquema nuevo (Phase 1, SCHEMA-04) es un modelo estructurado con KPIs en tarjetas, reto/solucion por pasos, y comparativa antes-despues, calcado del patron ariannalupi.com/casos/. Esos campos estructurados NO existen en ningun documento del origen -- no hay forma de derivarlos automaticamente sin inventar datos.

Purpose: Cumplir MIGR-02/03/05 para el contenido migrable de verdad (title, slug, heroImage, richText -> clientContext), y documentar honestamente -- no ocultar -- el vacio de datos estructurados que requiere una pasada editorial manual posterior (excepcion legitima de "informacion faltante en la fuente", no una reduccion de alcance: el roadmap pide "contenido migrado", no "KPIs historicos reconstruidos de la nada").
Output: scripts/migrate/steps/05-case-studies.ts, ejecutado contra el Postgres real; remap-table con la clave case-studies completa; lista de case studies pendientes de autoria estructurada en el log final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/migrate/lib/remap-table.ts
@scripts/migrate/lib/richtext-remap.ts
@scripts/migrate/data/export/case-studies.json
@src/collections/CaseStudies/index.ts

<field_mapping>
CaseStudies (JuanPortfolio CaseStudies/index.ts, solo title+heroImage+content+publishedAt+slug -> juan-payload CaseStudies/index.ts, modelo estructurado completo):
- title -> title (localizado en ambos)
- heroImage -> heroImage (remapeado via remapTable.media)
- content (richText libre) -> clientContext (richText, localizado en ambos; aplicar remapRichTextMediaRefs por locale antes de escribir -- es el mapeo semantico mas cercano: "contenido narrativo del caso" -> "contexto del cliente")
- publishedAt -> publishedAt no existe en el esquema nuevo (no hay campo publishedAt en juan-payload CaseStudies) -- usar el valor viejo solo para decidir _status:'published' si existia, si no dejar draft
- slug -> slug **verbatim**
- kpis (nuevo, minRows:1, requerido) -> SIN datos de origen. Crear con un unico item placeholder: {label: 'Resumen', value: 'Ver caso completo'} (satisface minRows:1 sin inventar una metrica falsa) y agregar el slug a needsStructuredContent
- heroMetric, heroSubtitle, client, sector, period, services, challenge, solution, results, conclusion -> SIN datos de origen, todos opcionales en el esquema nuevo (ninguno tiene minRows/required salvo kpis) -- dejar sin asignar (undefined), no enviar el campo en el payload de create
</field_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar CaseStudies preservando el richText viejo y documentando el vacio estructurado</name>
  <files>scripts/migrate/steps/05-case-studies.ts</files>
  <action>
Crear scripts/migrate/steps/05-case-studies.ts: importa config desde ../../../src/payload.config, getPayload({config}), carga la remap-table.

Por cada doc en case-studies.json: si ya migrado (getMapping(table,'case-studies',doc.id)), saltar. Mapear campos segun field_mapping exactamente: slug verbatim, heroImage remapeado, clientContext = remapRichTextMediaRefs(doc.content[locale], remapTable.media) por locale, kpis = [{label:'Resumen', value:'Ver caso completo'}], _status derivado de publishedAt viejo. Crear en locale es, update en locale en para clientContext/title. setMapping(table,'case-studies',doc.id,created.id). Acumular cada slug migrado en un array needsStructuredContent (TODOS los case studies migrados entran aqui, ya que ninguno tiene KPIs/challenge/solution reales de origen).

Al final del script, imprimir needsStructuredContent completo con un mensaje claro: "Los siguientes case studies fueron migrados con contenido narrativo intacto (clientContext) pero requieren autoria manual de kpis/challenge/solution/results/client/sector/period/services antes de publicarse con el modelo estructurado completo: [slugs]". saveRemapTable(table). Ejecutar de verdad: npx tsx scripts/migrate/steps/05-case-studies.ts.
  </action>
  <verify>
    <automated>node -e "const t=require('./scripts/migrate/data/remap-table.json'); const cs=require('./scripts/migrate/data/export/case-studies.json'); if(Object.keys(t['case-studies']||{}).length < cs.length) process.exit(1)"</automated>
  </verify>
  <done>Todos los case studies existen en el backend nuevo con slug verbatim, clientContext preservando el contenido narrativo original con media remapeada, y el script deja constancia explicita (en log, no oculta) de cuales requieren autoria estructurada manual posterior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Script -> Postgres nuevo (Local API) | Crea contenido real de produccion con un mismatch de esquema conocido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | Re-ejecucion duplica case studies | mitigate | Idempotencia via getMapping antes de cada create |
| T-04-13 | Repudiation | KPIs inventados presentados como datos reales | mitigate | Placeholder explicito y generico ("Ver caso completo"), nunca una metrica numerica inventada; lista needsStructuredContent impresa y documentada en el SUMMARY |
</threat_model>

<verification>
npx tsx scripts/migrate/steps/05-case-studies.ts corre sin excepcion; comando automated confirma cobertura de remap.
</verification>

<success_criteria>
Todos los case studies del origen existen en el backend nuevo con slug verbatim y contenido narrativo preservado; el gap de datos estructurados (kpis/challenge/solution/results) queda documentado explicitamente, no oculto, en el SUMMARY de este plan.
</success_criteria>

<output>
Create .planning/phases/04-migraci-n-mongo-postgres/04-06-SUMMARY.md when done
</output>
