---
phase: 04-migraci-n-mongo-postgres
plan: 07
type: execute
wave: 7
depends_on: ["04-06"]
files_modified: [scripts/migrate/steps/06-works-audit.ts, scripts/migrate/data/works-audit-report.json, scripts/migrate/data/remap-table.json]
autonomous: false
requirements: [MIGR-02]

must_haves:
  truths:
    - "La coleccion Works del sitio viejo (retirada, sin equivalente 1:1 en el nuevo esquema) fue auditada documento por documento, no ignorada"
    - "Cada Work con contenido genuino de proyecto/caso se convierte en un CaseStudy nuevo (con clientContext derivado de su excerpt, heroImage desde su cover) solo tras confirmacion explicita de Juan -- no por decision unilateral del script"
    - "Ningun Work se migra como coleccion propia -- la coleccion works no existe en el esquema nuevo (Phase 1 ya la elimino) y este plan no la reintroduce"
  artifacts:
    - path: "scripts/migrate/data/works-audit-report.json"
      provides: "Tabla de auditoria: cada Work viejo, su excerpt/tags/caseStudyUrl, y si ya existe un CaseStudy con contenido solapado (matched via caseStudyUrl -> slug)"
    - path: "scripts/migrate/steps/06-works-audit.ts"
      provides: "Genera el audit report y, tras aprobacion, crea los CaseStudies confirmados"
---

<objective>
Auditar la coleccion Works del sitio viejo (retirada por decision ya tomada en Phase 1 -- "Works se retira, absorbido conceptualmente en CaseStudies") y, para cada documento con contenido de proyecto genuino, decidir junto con Juan si se convierte en un CaseStudy nuevo o se descarta por ser redundante con un case study que ya existe.

Purpose: Cumplir el mandato explicito del roadmap/CONTEXT.md ("audita su contenido manualmente y fold-in en case studies donde corresponda, no se migra 1:1 como coleccion") -- esta es una decision de contenido/curatorial, no una decision tecnica, por eso requiere el checkpoint humano en vez de automatizarse por completo.
Output: scripts/migrate/steps/06-works-audit.ts, works-audit-report.json, CaseStudies nuevos creados para los Works aprobados por Juan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/migrate/lib/remap-table.ts
@scripts/migrate/data/export/works.json
@src/collections/CaseStudies/index.ts

<works_shape>
Cada doc en works.json (JuanPortfolio Works/index.ts) tiene: title (localizado), excerpt (localizado), cover (upload -> media), tags (array de {label localizado}), caseStudyUrl (texto libre, a veces apunta a la URL de un case study relacionado del mismo sitio -- usar para detectar duplicados).
</works_shape>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generar el reporte de auditoria de Works</name>
  <files>scripts/migrate/steps/06-works-audit.ts, scripts/migrate/data/works-audit-report.json</files>
  <action>
Crear scripts/migrate/steps/06-works-audit.ts con una funcion generateAuditReport() que: lee works.json y case-studies.json (dump viejo) y la remap-table (case-studies ya migrados en wave 6), y para cada Work determina si caseStudyUrl contiene un slug que coincide con algun case-study ya migrado (comparar el ultimo segmento de la URL contra los slugs de case-studies.json). Escribe scripts/migrate/data/works-audit-report.json como un array de {oldId, title, excerpt, tags, caseStudyUrl, likelyDuplicateOfSlug: string|null, recommendation: 'fold-in-as-new-case-study' | 'skip-likely-duplicate'}.

Ejecutar generateAuditReport() de verdad (npx tsx con un flag --report-only o una funcion separada invocada primero) y mostrar el contenido completo del reporte en la consola de forma legible para que Juan lo revise en el siguiente task.
  </action>
  <verify>
    <automated>test -s scripts/migrate/data/works-audit-report.json && node -e "const r=require('./scripts/migrate/data/works-audit-report.json'); if(!Array.isArray(r)) process.exit(1)"</automated>
  </verify>
  <done>works-audit-report.json existe con una entrada por cada Work del sitio viejo, cada una con una recomendacion explicita y su justificacion (duplicado detectado o no).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: aprobar que Works se convierten en CaseStudies nuevos</name>
  <files>scripts/migrate/data/works-audit-report.json</files>
  <action>
Presentar a Juan el contenido completo de scripts/migrate/data/works-audit-report.json (generado en Task 1) y esperar su decision explicita antes de crear ningun CaseStudy nuevo a partir de un Work. No proceder a Task 2 sin una respuesta clara.
  </action>
  <decision>Que Works viejos se convierten en CaseStudies nuevos vs se descartan</decision>
  <context>
La coleccion Works fue retirada en Phase 1 por decision de Juan (no hay pagina publica /works en el sitio viejo, asi que no hay URLs que preservar). Cada documento fue auditado en Task 1 y clasificado con una recomendacion automatica basada en si su `caseStudyUrl` ya coincide con un case study migrado. Juan debe confirmar, para cada Work con recommendation:'fold-in-as-new-case-study', si realmente quiere que se cree un CaseStudy nuevo a partir de el (con clientContext derivado del excerpt y heroImage desde su cover), o si prefiere descartarlo.
  </context>
  <options>
    <option id="approve-all-recommended">
      <name>Aprobar todas las recomendaciones fold-in del reporte</name>
      <pros>Rapido, no requiere revisar item por item</pros>
      <cons>Puede crear CaseStudies de baja calidad para Works que en realidad no merecen ser un case study propio</cons>
    </option>
    <option id="selective-list">
      <name>Aprobar solo una lista especifica de oldIds (Juan indica cuales)</name>
      <pros>Control total sobre que se convierte en CaseStudy</pros>
      <cons>Requiere que Juan revise works-audit-report.json y responda con la lista</cons>
    </option>
    <option id="skip-all">
      <name>Descartar todos los Works, no crear ningun CaseStudy nuevo</name>
      <pros>Mas simple, asume que los case studies ya existentes cubren todo lo relevante</pros>
      <cons>Se pierde contenido de proyectos que solo existian como Work</cons>
    </option>
  </options>
  <resume-signal>Responder con: approve-all-recommended, skip-all, o una lista de oldIds separados por coma para selective-list</resume-signal>
  <verify>
    <manual>Respuesta explicita de Juan recibida y registrada antes de avanzar a Task 2</manual>
  </verify>
  <done>Juan respondio con una de las 3 opciones (o la lista de oldIds), y esa decision queda registrada para que Task 2 la ejecute exactamente.</done>
</task>

<task type="auto">
  <name>Task 2: Crear los CaseStudies aprobados y cerrar la auditoria</name>
  <files>scripts/migrate/steps/06-works-audit.ts</files>
  <action>
Agregar a scripts/migrate/steps/06-works-audit.ts una funcion foldApprovedWorks(approvedOldIds: string[]) que, segun la decision del checkpoint anterior, para cada Work aprobado: crea un CaseStudy nuevo con title (del Work), clientContext = parrafo de richText construido desde excerpt (usar el mismo helper lexicalParagraph del patron de scripts/seed-phase2.ts), heroImage remapeado desde cover via remapTable.media, kpis con el mismo placeholder de wave 6 ({label:'Resumen', value:'Ver caso completo'}), slug derivado del title del Work vía el mismo slugField hook del nuevo esquema (NO existe un slug viejo verbatim para Works porque nunca tuvieron URL propia -- este es el unico caso de la fase donde generar el slug desde el titulo es correcto, ya que no hay URL original que preservar). Agregar el nuevo case-study creado a needsStructuredContent igual que wave 6. Actualizar works-audit-report.json marcando cada entrada procesada con status:'created'|'skipped'.

Ejecutar la funcion con los oldIds aprobados por Juan en el checkpoint.
  </action>
  <verify>
    <automated>node -e "const r=require('./scripts/migrate/data/works-audit-report.json'); const processed=r.filter(x=>x.status==='created'||x.status==='skipped'); if(processed.length!==r.length) process.exit(1)"</automated>
  </verify>
  <done>Todos los Works del reporte tienen un status final (created o skipped); los aprobados existen como CaseStudy nuevo con contenido derivado de su excerpt/cover; la coleccion works en si misma no fue reintroducida en ningun esquema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Checkpoint humano -> creacion de contenido nuevo | Decision curatorial que crea documentos reales en produccion basada en la respuesta de Juan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | Fold-in automatico sin aprobacion crea contenido no deseado | mitigate | Checkpoint:decision bloqueante obligatorio antes de cualquier create; Task 2 no corre sin la lista aprobada |
| T-04-15 | Repudiation | Slug de CaseStudy derivado del titulo (unico caso de la fase) sin dejar constancia | mitigate | Documentado explicitamente en la accion de Task 2 y en el SUMMARY: es el unico caso donde no hay slug verbatim que preservar porque el Work nunca tuvo URL propia |
</threat_model>

<verification>
Revisar works-audit-report.json final: todas las entradas tienen status. Confirmar en el admin de Payload que los CaseStudies fold-in aprobados existen con heroImage/clientContext poblados.
</verification>

<success_criteria>
Works auditado documento por documento, con decision humana explicita registrada; ningun Work se migro como coleccion propia; los folds aprobados enriquecen CaseStudies de forma trazable.
</success_criteria>

<output>
Create .planning/phases/04-migraci-n-mongo-postgres/04-07-SUMMARY.md when done
</output>
