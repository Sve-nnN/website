---
phase: 04-migraci-n-mongo-postgres
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [scripts/migrate/steps/03-testimonials-clientes.ts, scripts/migrate/data/remap-table.json]
autonomous: true
requirements: [MIGR-02, MIGR-03, MIGR-04, MIGR-05]

must_haves:
  truths:
    - "Cada testimonio y cliente real de JuanPortfolio existe en testimonials/clientes del nuevo backend, con avatar/logo remapeados a Cloudinary (wave 2), no a URLs viejas"
    - "El nuevo esquema de Testimonials exige name/role/company obligatorios (SCHEMA-05) -- el script rechaza o repara explicitamente cualquier testimonio viejo sin esos 3 campos, sin crear un doc invalido silenciosamente"
    - "Clientes.logo es obligatorio en el esquema nuevo -- un cliente viejo sin logo no puede migrarse tal cual; el script lo reporta como pendiente de decision, no lo omite en silencio"
  artifacts:
    - path: "scripts/migrate/steps/03-testimonials-clientes.ts"
      provides: "Migra testimonials.json y clientes.json -> colecciones testimonials/clientes del nuevo Postgres"
  key_links:
    - from: "scripts/migrate/steps/03-testimonials-clientes.ts"
      to: "scripts/migrate/lib/remap-table.ts"
      via: "getMapping(table, 'media', oldAvatarOrLogoId)"
      pattern: "getMapping.*media"
---

<objective>
Migrar Testimonials y Clientes. Ambas colecciones son hojas de la relacion (nada depende de ellas para crearse), pero el nuevo esquema es mas estricto que el viejo en dos puntos: Testimonials exige name/role/company (el viejo solo exigia author+testimonial), y Clientes exige logo (el viejo tambien lo exigia, asi que ese caso ya esta cubierto).

Purpose: Cerrar el grupo "Posts/CaseStudies/Testimonials/Clientes" del orden roadmap, manejando explicitamente el mismatch de reglas de validacion entre el esquema viejo (mas laxo) y el nuevo (mas estricto, SCHEMA-05/07 de Phase 1) sin inventar datos ni fallar en silencio.
Output: scripts/migrate/steps/03-testimonials-clientes.ts, remap-table con testimonials y clientes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/migrate/lib/remap-table.ts
@scripts/migrate/data/export/testimonials.json
@scripts/migrate/data/export/clientes.json
@src/collections/Testimonials/index.ts
@src/collections/Clientes/index.ts

<field_mapping>
Testimonials (JuanPortfolio Testimonials.ts -> juan-payload Testimonials/index.ts):
- author (viejo, texto libre, unico campo de nombre) -> name (nuevo, requerido)
- role (viejo, localizado, opcional) -> role (nuevo, localizado, requerido)
- company (viejo, localizado, opcional) -> company (nuevo, requerido, NO localizado en el nuevo esquema -- usar el valor es como fuente de verdad si el viejo tiene valores distintos por locale)
- testimonial -> testimonial (localizado en ambos, requerido en ambos)
- avatar -> avatar (remapeado via media remap-table, opcional en ambos)
- rating (viejo) -> sin equivalente en el nuevo esquema -- fuera de alcance, no se migra (dato no solicitado por SCHEMA-05)
- Regla de validacion: si un doc viejo no tiene role o company (ambos opcionales en el viejo esquema), el nuevo esquema los requiere. NO inventar un valor -- usar el placeholder explicito "(sin especificar)" para el campo faltante Y loguearlo en una lista de "testimonios que requieren revision editorial" impresa al final del script, para que Juan los complete manualmente despues. Esto no es una migracion fiel al 100% de esos casos puntuales porque el dato simplemente no existia en el origen (excepcion legitima de informacion faltante, no una reduccion de alcance).

Clientes (JuanPortfolio Clientes/index.ts -> juan-payload Clientes/index.ts):
- name -> name
- logo -> logo (remapeado via media remap-table; ambos esquemas lo requieren, asi que si el remap de media fallo para ese logo especifico, saltar ese cliente y agregarlo a la misma lista de revision editorial en vez de crear un doc con logo faltante que violaria el schema)
- url -> websiteUrl
- invertInDark/forceWhiteBackground (viejo) -> sin equivalente en el nuevo esquema (deliberadamente minimalista, SCHEMA-07 "puramente credibilidad visual") -- fuera de alcance
</field_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar Testimonials y Clientes con manejo explicito de campos ahora-requeridos</name>
  <files>scripts/migrate/steps/03-testimonials-clientes.ts</files>
  <action>
Crear scripts/migrate/steps/03-testimonials-clientes.ts: importa config desde ../../../src/payload.config, getPayload({config}), carga la remap-table.

Para testimonials.json: por cada doc, si ya migrado (getMapping(table,'testimonials',doc.id)), saltar. Mapear campos segun field_mapping, aplicando el placeholder "(sin especificar)" para role/company faltantes y acumulando esos casos en un array needsReview. Crear en locale es, luego update en locale en para role/testimonial. setMapping(table,'testimonials',doc.id,created.id).

Para clientes.json: por cada doc, si ya migrado, saltar. Resolver logo via getMapping(table,'media',String(doc.logo)); si no hay mapeo, agregar a needsReview con motivo "logo no migrado" y NO crear el doc (Clientes.logo es required:true en el nuevo esquema -- un create sin logo fallaria de todos modos). Si hay logo, crear con name/logo/websiteUrl, setMapping(table,'clientes',doc.id,created.id).

Al final, imprimir needsReview completo (coleccion, id viejo, motivo) y saveRemapTable(table). Ejecutar de verdad: npx tsx scripts/migrate/steps/03-testimonials-clientes.ts.
  </action>
  <verify>
    <automated>node -e "const t=require('./scripts/migrate/data/remap-table.json'); const te=require('./scripts/migrate/data/export/testimonials.json'); if((Object.keys(t.testimonials||{}).length===0) && te.length>0) process.exit(1); console.log('ok')"</automated>
  </verify>
  <done>Testimonials y Clientes migrados con avatar/logo remapeados a Cloudinary; cualquier doc con datos faltantes esta explicitamente listado en needsReview (no silenciosamente omitido ni inventado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Script -> Postgres nuevo (Local API) | Crea contenido real de produccion en el backend nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Re-ejecucion duplica testimonials/clientes | mitigate | Idempotencia via getMapping antes de cada create |
| T-04-09 | Repudiation | Doc creado con datos faltantes disfrazados de completos | mitigate | Placeholder explicito "(sin especificar)" + lista needsReview impresa, nunca un valor inventado sin marcar |
</threat_model>

<verification>
npx tsx scripts/migrate/steps/03-testimonials-clientes.ts corre sin excepcion no controlada; comando automated de la task confirma cobertura de remap.
</verification>

<success_criteria>
Testimonials y Clientes completos en el backend nuevo (o explicitamente listados para revision editorial si faltaba data obligatoria en el origen).
</success_criteria>

<output>
Create .planning/phases/04-migraci-n-mongo-postgres/04-04-SUMMARY.md when done
</output>
