---
phase: 09-hero-resultados-kpi-tipografia
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Prose.tsx
  - "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
autonomous: true
requirements: [UI-08]

must_haves:
  truths:
    - "Los posts y case studies aplican la jerarquía tipográfica Inter (body/UI) + Fraunces (display/headings) de forma consistente en contenido largo: encabezados, cuerpo, y citas/blockquotes"
    - "Las citas (blockquotes) del rich text tienen un tratamiento editorial distintivo (Fraunces, indentación, acento) que hoy no existe en Prose.tsx"
    - "La jerarquía de encabezados (h1 del post, h2/h3 dentro del rich text) permanece exactamente igual en semántica/orden — solo cambian clases CSS, nunca el tag ni el nivel"
  artifacts:
    - path: "src/components/Prose.tsx"
      provides: "Estilo blockquote/cita editorial + ritmo de encabezados reforzado (tracking, diferenciación h2/h3)"
    - path: "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
      provides: "Ritmo tipográfico del header del artículo (título/meta) alineado con Prose, sin cambios de semántica de encabezados"
  key_links:
    - from: "src/app/(frontend)/[locale]/blog/[slug]/page.tsx"
      to: "src/components/RichTextRenderer.tsx"
      via: "<RichTextRenderer data={doc.content} />"
      pattern: "RichTextRenderer"
    - from: "src/components/RichTextRenderer.tsx"
      to: "src/components/Prose.tsx"
      via: "<Prose className={className}>...<RichText .../></Prose>"
      pattern: "Prose"
---

<objective>
Extender `Prose.tsx` con un tratamiento editorial para blockquotes/citas (que hoy no existe) y reforzar el ritmo/jerarquía de sus reglas de encabezado existentes, luego alinear el header del artículo en `blog/[slug]/page.tsx` con ese mismo ritmo tipográfico — todo usando exclusivamente los tokens Inter/Fraunces y el type scale de 4 tamaños ya bloqueados, sin degradar la semántica de encabezados existente (h1/h2/h3).

Purpose: Implementa UI-08. `Prose.tsx` es el punto único de estilo para todo rich text del sitio (`content`/`clientContext`/`conclusion` de Posts y CaseStudies) — reforzarlo aquí propaga automáticamente a ambos tipos de contenido largo sin tocar cada página. `blog/[slug]/page.tsx` es el otro lugar con tipografía "larga" fuera de Prose (el header del artículo) y necesita el mismo ritmo para sentirse parte de la misma familia visual.

Output: `Prose.tsx` con reglas de blockquote añadidas y jerarquía de encabezados reforzada; `blog/[slug]/page.tsx` con el header del artículo alineado al mismo ritmo, heading semantics verificadas sin cambios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-hero-resultados-kpi-tipografia/09-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/research/FEATURES.md

<interfaces>
<!-- Current Prose.tsx (read in full) -->
```tsx
export function Prose({ children, className, ...props }: ProseProps) {
  return (
    <div
      className={cn(
        'text-body font-sans text-foreground',
        '[&_h1]:font-display [&_h1]:text-display [&_h1]:mb-6',
        '[&_h2]:font-display [&_h2]:text-heading [&_h2]:mt-10 [&_h2]:mb-4',
        '[&_h3]:font-display [&_h3]:text-heading [&_h3]:mt-8 [&_h3]:mb-3',
        '[&_p]:text-body [&_p]:mb-4',
        '[&_ul]:list-disc [&_ul]:pl-6 [&_ul]:mb-4',
        '[&_ol]:list-decimal [&_ol]:pl-6 [&_ol]:mb-4',
        '[&_li]:mb-2',
        '[&_a]:text-primary [&_a]:underline [&_a]:underline-offset-2 hover:[&_a]:opacity-80',
        '[&_strong]:font-semibold',
        className,
      )}
      {...props}
    >
      {children}
    </div>
  )
}
```

`RichTextRenderer.tsx` wraps Payload's `defaultJSXConverters` from `@payloadcms/richtext-lexical/react` — its default converters already emit a plain `<blockquote>` tag for Lexical quote blocks with no class, which is why `Prose.tsx` currently has no `[&_blockquote]` rule (it inherits browser default styling only). This plan only adds a Tailwind arbitrary-descendant selector for it — no converter/serializer changes needed, no new Payload field.

Current `blog/[slug]/page.tsx` article header (relevant excerpt, read in full):
```tsx
<h1 className="font-display text-display">{doc.title}</h1>
<div className="mt-4 flex flex-wrap items-center gap-6">
  {author && <AuthorByline author={author} />}
  <div className="text-label text-muted-foreground">
    {doc.publishedAt && <time dateTime={doc.publishedAt}>...</time>}
    {' · '}
    {readingTimeMinutes} {locale === 'es' ? 'min de lectura' : 'min read'}
  </div>
</div>
```

Locked type scale (only 4 sizes, `tailwind.config.ts`): `text-body`, `text-label`, `text-heading`, `text-display`. Research guidance (`FEATURES.md`): "Editorial 'chapter' rhythm in long-form content ... using Fraunces for section breaks/pull-quotes ... keep Inter for body — no new component needed."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add editorial blockquote treatment and strengthen heading rhythm in Prose.tsx</name>
  <files>src/components/Prose.tsx</files>
  <action>
Add a new `[&_blockquote]` rule to the `cn()` call in `Prose.tsx`, using the existing arbitrary-descendant-selector pattern already established for `h1`/`h2`/`h3`/`p`/`ul`/etc. Style: `[&_blockquote]:font-display [&_blockquote]:text-heading [&_blockquote]:border-l-4 [&_blockquote]:border-primary [&_blockquote]:pl-6 [&_blockquote]:my-8 [&_blockquote]:text-foreground/80` — Fraunces display face at heading size, ember-accent left rule, generous indent/margin, slightly muted ink color. This is the "editorial chapter rhythm... Fraunces for pull-quotes" pattern from `FEATURES.md`, expressed with existing tokens only (no new color, no new size).

Strengthen the existing `h1`/`h2`/`h3` rules for stronger hierarchy per 09-CONTEXT.md's "stronger contrast between eyebrow/title/subtitle roles" and "editorial-tech direction" intent: add `tracking-tight` to all three (`[&_h1]:tracking-tight`, `[&_h2]:tracking-tight`, `[&_h3]:tracking-tight`). Since `h2` and `h3` currently share the identical `text-heading` size (the 4-size budget has no room for a 5th size), differentiate them by weight/opacity instead: add `[&_h3]:opacity-90` (h3 recedes slightly relative to h2, preserving visual hierarchy despite the shared size token) — do NOT invent a new font-size utility for either.

Do not change any `h1`/`h2`/`h3` tag names, do not restructure the converter — this task only ever touches the `cn()` className string.
  </action>
  <verify>
    <automated>grep -c "blockquote" src/components/Prose.tsx ; npx tsc --noEmit</automated>
  </verify>
  <done>Prose.tsx has a `[&_blockquote]` rule using Fraunces/existing accent/existing spacing tokens, h1/h2/h3 rules gain `tracking-tight`, h3 gains `opacity-90` for recession relative to h2, no tag/converter changes, `tsc --noEmit` passes clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align blog post article header rhythm with Prose and verify heading semantics unchanged</name>
  <files>"src/app/(frontend)/[locale]/blog/[slug]/page.tsx"</files>
  <action>
Apply the same `tracking-tight` treatment to the article `<h1>` (`font-display text-display tracking-tight`) so the post title matches the strengthened hierarchy from Task 1's `Prose.tsx` rules, keeping the site's h1 treatment visually consistent whether it renders inside or outside `Prose`.

Strengthen the vertical rhythm of the byline/meta row: change `mt-4` to `mt-6` on the wrapping `<div>` (a standard Tailwind spacing step, consistent with the locked 4px-multiple scale) for clearer separation between title and metadata, matching the increased breathing room applied to the Hero subtitle in `09-01-PLAN.md`'s Task 1 for sitewide consistency.

Do not add, remove, or rename the `<h1>` tag, and do not touch the `<article>` wrapper around `<RichTextRenderer data={doc.content} />` — Task 1's `Prose.tsx` changes already propagate into it automatically.

Run a before/after heading-tag count check (documented in the SUMMARY) to confirm this task's diff introduces zero structural heading changes — only className edits on the existing `<h1>`.
  </action>
  <verify>
    <automated>grep -oE '<h[1-3]' "src/app/(frontend)/[locale]/blog/[slug]/page.tsx" | sort | uniq -c ; npx tsc --noEmit</automated>
  </verify>
  <done>Article h1 gains `tracking-tight`, byline/meta spacing increased to `mt-6`, heading tag count identical before/after this task's diff (exactly one `<h1`, zero `<h2`/`<h3` outside rich text content), `tsc --noEmit` passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Lexical rich text → rendered HTML | `RichTextRenderer`/`Prose` render editor-authored Payload rich text via Payload's own default converters; this plan only adds CSS selectors, it does not touch the converter/serialization boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-09-04 | Tampering | Heading semantics in `Prose.tsx` consumers (Posts, CaseStudies rich text) | mitigate | Task 1 only edits the `cn()` className string, never the converter or tag structure; Task 2's grep-based before/after heading count check is the explicit regression gate required by RESEARCH.md pitfall #6 (markup/SEO regression from "just visual" edits) |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no new type errors.
2. `grep -oE '<h[1-3]' "src/app/(frontend)/[locale]/blog/[slug]/page.tsx" | sort | uniq -c` matches the pre-change count exactly (documented in SUMMARY).
3. `git diff --stat` shows no changes to any `src/blocks/*/config.ts` or `src/payload-types.ts`.
4. Manual visual spot-check on a post with at least one blockquote in its rich text (or a quick local Lexical test doc) confirming the new blockquote treatment renders as intended in both `/en` and `/es`.
</verification>

<success_criteria>
- Prose.tsx has an editorial blockquote treatment using existing Fraunces/accent/spacing tokens.
- Heading hierarchy (h1/h2/h3) is more strongly differentiated (tracking, h3 recession) without a new size token.
- Blog post article header rhythm matches the strengthened Prose hierarchy.
- Heading semantics (tags/order) unchanged in `blog/[slug]/page.tsx`.
- Zero diffs in `src/blocks/*/config.ts` / `src/payload-types.ts`.
</success_criteria>

<output>
Create `.planning/phases/09-hero-resultados-kpi-tipografia/09-03-SUMMARY.md` when done
</output>
