# Phase 35 Plan 01: Component Polish Pass

**Goal:** Review the 28 .pen-modeled components (grouped as POLISH-01..05) against their code implementations, and implement every genuine micro-improvement found directly in code (POLISH-06), without touching `HeroGrainGradient` or adding any new design token.

## Tasks

1. **Read design docs + .pen schema.** `designs/DESIGN-SYSTEM.md`, `designs/DESIGN-SYSTEM-PEN.md` in full; `mcp__pencil__get_editor_state(include_schema: true)` to enumerate all 30 reusable component IDs in `designs/current-site-real.pen`.
2. **Read all 28 target component source files** directly (not just config/schema) across the 5 groups.
3. **Pull structured .pen data** via two `mcp__pencil__batch_get` calls (`resolveVariables: true`) covering all 30 reusable nodes, to get exact fill/padding/gap/cornerRadius/font values per component.
4. **Cross-check every candidate diff against `tailwind.config.ts`** to determine whether a fix would reuse an existing CSS-variable-backed utility or require inventing a new token.
5. **Take confirmatory screenshots** (`SiteHeader`, `Card`) via `mcp__pencil__get_screenshot` to sanity-check the JSON-derived findings.
6. **Apply in-scope fixes directly** — one edit per genuine finding, each reusing an already-existing token/utility with precedent elsewhere in the codebase.
7. **Verify:** `npx tsc --noEmit`; re-run `scripts/smoke-check-phase8.mjs` against a freshly started, isolated `next dev` server; curl sanity check (200 + exactly 1 `<h1>`) on `/`, `/en`, `/en/blog`, `/servicios`; `git diff --stat` review + explicit `git diff -- src/components/HeroGrainGradient.tsx` empty-output check.
8. **Document:** this CONTEXT/PLAN, a SUMMARY with the full findings table per group, VERIFICATION against ROADMAP's Phase 35 success criteria, and update STATE.md/ROADMAP.md/REQUIREMENTS.md.

## Approach

Single wave, no separate discuss-phase round — the scope was already fully bounded by REQUIREMENTS.md POLISH-01..06 and the task brief explicitly authorized using judgment on which micro-improvements to make without per-finding approval. Comparison method favored structured `.pen` data (resolved hex/px values) over screenshot-only review, since the .pen schema's `resolveVariables` output gives exact values directly comparable against Tailwind class outputs — screenshots were used only to confirm the two most visually load-bearing candidates (header background, Card radius) before committing to any fix.

## Files Touched

- `src/components/ui/badge-variants.ts`
- `src/components/ui/button-variants.ts`
- `src/components/ui/card.tsx`
- `src/blocks/AboutSection/Component.tsx`
- `src/blocks/ServiceScopeCard/Component.tsx`
- `src/blocks/ContactFormBlock/Component.tsx`

No schema/config/migration files touched — this phase is styling-only, same class of change as Phase 8.
</content>
