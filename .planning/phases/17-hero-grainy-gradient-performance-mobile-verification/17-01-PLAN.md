---
phase: 17-hero-grainy-gradient-performance-mobile-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lighthouse-mobile.mjs
  - .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/lh-phase17-current.json
  - .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-lighthouse-cwv-report.md
autonomous: true
requirements: [HERO-ANIM-05, HERO-ANIM-06]

must_haves:
  truths:
    - "A mobile Lighthouse run (Performance/Accessibility/Best-Practices/SEO + LCP/CLS/TBT) against a LOCAL PRODUCTION BUILD (npm run build && npm run start, never next dev) of current HEAD (post-Phase-16, shader live) exists for /en and /es (the Hero home routes) and is documented."
    - "The current run is compared against Phase 11's own most-recent pre-shader local-production-build measurement for /en and /es (.planning/phases/11-verificacion-cruzada-final/lh-current.json) — the most recent available baseline per 17-CONTEXT.md's discretion note, not re-derived from scratch."
    - "No Performance category score for /en or /es regresses by more than ~5 points versus that baseline without an explicit documented investigation; LCP/CLS/TBT stay within Lighthouse's 'good'/'needs improvement' lab bands (not 'poor') given the WebGL shader now running in the Hero."
    - "A mobile-first spot-check (375/768/1280px) against the SAME local production build (not the dev server) confirms zero horizontal overflow and no layout break on the Hero home page, reusing scripts/verify-hero-grain-gradient.mjs's existing overflow assertions via its BASE_URL override rather than rebuilding a new check from scratch."
    - "A written verification report with the real captured numbers exists, following the 11-03-lighthouse-report.md precedent (methodology, baseline-comparison table, disposition)."
  artifacts:
    - path: "scripts/lighthouse-mobile.mjs"
      provides: "Extended to also extract LCP (largest-contentful-paint numericValue ms), CLS (cumulative-layout-shift numericValue), and TBT (total-blocking-time numericValue ms, the Lighthouse lab-metric proxy for INP since real INP requires field data) per route, alongside the existing 4 category scores; comma-separated --routes-only support added"
      exports: ["main"]
    - path: ".planning/phases/17-hero-grainy-gradient-performance-mobile-verification/lh-phase17-current.json"
      provides: "Raw Lighthouse scores + CWV numbers for /en and /es against the post-Phase-16 production build"
    - path: ".planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-lighthouse-cwv-report.md"
      provides: "Baseline-vs-current comparison table (scores + LCP/CLS/TBT), mobile spot-check result, and explicit pass/flag disposition"
  key_links:
    - from: "scripts/lighthouse-mobile.mjs"
      to: "local Chrome binary (via @puppeteer/browsers, already cached in .lighthouse-chrome/ from Phase 11)"
      via: "chrome-launcher with an explicit chromePath"
      pattern: "chromePath|executablePath"
    - from: "17-01-lighthouse-cwv-report.md"
      to: ".planning/phases/11-verificacion-cruzada-final/lh-current.json"
      via: "baseline comparison table sourced from Phase 11's own post-11-02 /en /es scores"
      pattern: "lh-current\\.json|11-03"
    - from: "scripts/verify-hero-grain-gradient.mjs"
      to: "local production build (BASE_URL override)"
      via: "BASE_URL env var pointed at npm run start's port instead of npm run dev's default"
      pattern: "BASE_URL"
---

<objective>
Confirm with real evidence that the Hero's WebGL `GrainGradient` shader (Phase 16) does not degrade Lighthouse Performance/Core Web Vitals on the home page, and does not break layout at 375/768/1280px, when measured against a LOCAL PRODUCTION BUILD (never `next dev`).

Purpose: ROADMAP Phase 17 success criteria require Lighthouse Performance with no significant regression, LCP/INP/CLS in acceptable range, and a mobile-first overflow/layout spot-check — all against production-build numbers, not dev-server numbers (dev overhead distorts metrics). This is the verification gate for HERO-ANIM-05/06, closing milestone v1.3.
Output: `scripts/lighthouse-mobile.mjs` extended with CWV audit extraction (reused from Phase 11, not rebuilt), a captured JSON of current /en+/es scores, and `17-01-lighthouse-cwv-report.md` documenting the baseline comparison, CWV numbers, and mobile spot-check result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-CONTEXT.md
@.planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md
@.planning/phases/11-verificacion-cruzada-final/lh-current.json

<interfaces>
<!-- Confirmed during planning, do not re-discover: -->

- `lighthouse` (^13.4.0), `chrome-launcher` (^1.2.1), `@puppeteer/browsers` (^3.0.6), and `playwright` (^1.61.1) are ALREADY installed as devDependencies (from Phase 11 and Phase 16) — no new package installs, no legitimacy checkpoint needed for this plan.
- `.lighthouse-chrome/` already has a cached Chrome-for-Testing binary from Phase 11's run (gitignored) — `scripts/lighthouse-mobile.mjs`'s `getChromePath()` will reuse it without re-downloading.
- `scripts/lighthouse-mobile.mjs` (Phase 11-03) currently only extracts the 4 category scores (`performance`/`accessibility`/`best-practices`/`seo`, each 0-100) per route — it does NOT extract LCP/CLS/TBT audit values yet. This plan's Task 1 adds that.
- `scripts/lighthouse-mobile.mjs` CLI: `node scripts/lighthouse-mobile.mjs --base-url <url> --out <path> [--routes-only <route>]`. `--routes-only` currently accepts exactly one route string; Task 1 extends it to accept a comma-separated list (e.g. `--routes-only /en,/es`) without breaking the single-route case.
- `scripts/lighthouse-mobile.mjs` does NOT start/stop the Next.js server itself — the caller must `npm run build && npm run start` (or `PORT=<n> npm run start`) first, poll for HTTP 200, then invoke the script, then stop the server. Phase 11 used `PORT=3010`/`3011` to avoid colliding with a dev server already running on 3000 — follow the same pattern (pick an unused port, e.g. `PORT=3020`).
- `scripts/verify-hero-grain-gradient.mjs` (Phase 16-03) does NOT spawn its own server — it reads `BASE_URL` (defaults to `http://localhost:3000`) and runs overflow/canvas/reduced-motion checks via Playwright against whatever is already listening there. Point `BASE_URL` at the same production server from Task 2 to get the "lighter re-confirmation against the production build" this phase's scope calls for — do not rebuild a new mobile-check script.
- Phase 11's `lh-current.json` (post-11-02, pre-Phase-16, i.e. the last measurement before the shader existed) is the most-recent available baseline for `/en` and `/es`: `/en` = 92/96/96/100 (P/A/BP/SEO), `/es` = 90/92/96/100. Per 17-CONTEXT.md's discretion note, this is the comparison baseline — do not re-checkout historical commits or rebuild a separate pre-milestone baseline from scratch.
- Regression threshold per 17-CONTEXT.md: a Performance drop of more than ~5 points, or visible LCP/CLS/TBT degradation into a "poor" Lighthouse band, warrants a flag; +/-2-3 points of normal run-to-run variance is not a regression.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend lighthouse-mobile.mjs with LCP/CLS/TBT extraction</name>
  <files>scripts/lighthouse-mobile.mjs</files>
  <action>
Edit the existing runLighthouse() function in scripts/lighthouse-mobile.mjs to also pull result.lhr.audits['largest-contentful-paint'].numericValue (ms), result.lhr.audits['cumulative-layout-shift'].numericValue (unitless CLS score), and result.lhr.audits['total-blocking-time'].numericValue (ms) -- add these as lcpMs, cls, and tbtMs fields alongside the existing performance/accessibility/best-practices/seo fields in the object returned per route. Round lcpMs/tbtMs to whole ms and cls to 3 decimal places. Add a short comment above the tbtMs field noting it is Lighthouse's lab-metric proxy for INP, since true INP requires real-user field data (Chrome UX Report) that a pre-production local build cannot produce.

Extend parseArgs()'s handling of --routes-only so a comma-separated value (e.g. /en,/es) splits into an array of routes instead of being treated as one literal route string; preserve backward compatibility so a single route with no comma still works exactly as before. Update main()'s use of args.routesOnly accordingly (it currently does args.routesOnly ? [args.routesOnly] : ROUTES -- change this to use the parsed array directly).

Do not change the ROUTES constant, the Chrome-download/launch logic, the CLI flag names, or any other existing behavior.
  </action>
  <verify>
    <automated>node --check scripts/lighthouse-mobile.mjs</automated>
  </verify>
  <done>Script parses (node --check passes), contains references to all three new Lighthouse audit keys (largest-contentful-paint, cumulative-layout-shift, total-blocking-time), and --routes-only accepts a comma-separated list.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run production-build Lighthouse against /en and /es, capture CWV</name>
  <files>.planning/phases/17-hero-grainy-gradient-performance-mobile-verification/lh-phase17-current.json</files>
  <action>
Confirm no dev server is already bound to the port you pick (use PORT=3020 to avoid any collision with a running npm run dev session on 3000, following the Phase 11 pattern).

1. Start the production server in the background: PORT=3020 npm run build, then PORT=3020 npm run start in the background. Poll http://localhost:3020/en until it returns HTTP 200 before proceeding (do not measure against a cold/incomplete build).
2. Run: node scripts/lighthouse-mobile.mjs --base-url http://localhost:3020 --out .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/lh-phase17-current.json --routes-only /en,/es
3. Inspect the output JSON -- confirm both /en and /es have populated performance/accessibility/best-practices/seo/lcpMs/cls/tbtMs fields with no error key.
4. Stop the background production server before moving to Task 3.

Do not modify application source code in this task -- this is a pure measurement step. If a genuine regression appears (Performance drop of more than ~5 points, or CWV entering a "poor" lab band), do NOT attempt a code fix here; document it clearly for Task 3's report and flag it as requiring a follow-up decision from Juan (this phase is verification-only per 17-CONTEXT.md's scope, which lists no design/implementation decisions).
  </action>
  <verify>
    <automated>grep -c '"performance"' .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/lh-phase17-current.json</automated>
  </verify>
  <done>lh-phase17-current.json exists with both /en and /es entries populated (performance, accessibility, best-practices, seo, lcpMs, cls, tbtMs), no error keys, and the local production server has been stopped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Production-build mobile spot-check + write verification report</name>
  <files>.planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-lighthouse-cwv-report.md</files>
  <action>
Restart the same local production server from Task 2 (PORT=3020 npm run start in the background; the build artifact from Task 2 is still valid, no rebuild needed unless files changed). Poll until http://localhost:3020/en returns HTTP 200.

Run the existing Phase 16 mobile/overflow verification script against this production server instead of the dev server: BASE_URL=http://localhost:3020 node scripts/verify-hero-grain-gradient.mjs. This reuses its existing 375/768/1280px overflow checks, canvas-paint check, and content-unchanged checks -- do not write a new script. Capture its console output (pass/fail counts, any FAIL lines) for the report.

Stop the background production server once the script finishes.

Write .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-lighthouse-cwv-report.md, following the 11-03-lighthouse-report.md precedent structure:
1. Methodology section: local production build (PORT=3020, npm run build && npm run start, never next dev), routes tested (/en, /es), reused scripts (lighthouse-mobile.mjs extended in Task 1, verify-hero-grain-gradient.mjs reused unmodified in this task via BASE_URL override).
2. Baseline note: explicitly state the comparison baseline is Phase 11's post-11-02 lh-current.json /en and /es entries (the most recent pre-shader local-production-build measurement available), per 17-CONTEXT.md's discretion note -- not a fresh pre-milestone checkout.
3. Scores table: baseline P/A/BP/SEO for /en and /es (from Phase 11) vs current P/A/BP/SEO + LCP/CLS/TBT for /en and /es (from Task 2's lh-phase17-current.json).
4. Disposition: apply the ~5-point Performance threshold and CWV lab-band judgment from 17-CONTEXT.md; state clearly whether this is a PASS or a flagged regression requiring Juan's decision.
5. Mobile spot-check section: verify-hero-grain-gradient.mjs's pass/fail summary against the production build, explicitly noting this is the "lighter re-confirmation against the production build" scoped by 17-CONTEXT.md (full dev-server verification already done in Phase 16-03).
6. Summary table mirroring 11-03's closing format (checks x results).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-lighthouse-cwv-report.md && grep -c "Baseline" .planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-lighthouse-cwv-report.md</automated>
  </verify>
  <done>Report exists documenting methodology, baseline comparison (scores + CWV), disposition (PASS or flagged regression), and the production-build mobile spot-check result, with no outstanding overflow/layout failures left undocumented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local production server (PORT=3020) -> Lighthouse/Playwright | Both measurement tools connect to a locally-built, locally-served instance of the app; no external network exposure. |
| lh-phase17-current.json / report -> git commit | Measurement artifacts are committed as evidence, same pattern as Phase 11's lh-baseline.json/lh-current.json. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-17-01 | Tampering | scripts/lighthouse-mobile.mjs edit (Task 1) | accept | No new package installs in this plan; edit is to an already-legitimacy-checked, already-committed script (Phase 11 checkpoint), scoped to audit-field extraction only. |
| T-17-02 | Denial of Service | Background npm run start left running after measurement | mitigate | Task 2 and Task 3 both explicitly require stopping the background production server before moving on, preventing an orphaned process left bound to port 3020. |
</threat_model>

<verification>
- scripts/lighthouse-mobile.mjs parses cleanly and extracts LCP/CLS/TBT alongside existing category scores.
- lh-phase17-current.json contains valid /en and /es entries with no error keys, captured against a local production build (never next dev).
- 17-01-lighthouse-cwv-report.md documents the baseline comparison (against Phase 11's lh-current.json), the CWV numbers, the disposition (PASS or flagged), and the production-build mobile spot-check result.
- No orphaned production server process left running on port 3020.
</verification>

<success_criteria>
Lighthouse Performance for /en and /es on the local production build shows no regression beyond ~5 points versus Phase 11's post-11-02 baseline; LCP/CLS/TBT stay within Lighthouse's good/needs-improvement lab bands; the production-build mobile spot-check (375/768/1280px) via verify-hero-grain-gradient.mjs shows zero overflow/layout failures; all findings are documented in 17-01-lighthouse-cwv-report.md.
</success_criteria>

<output>
Create `.planning/phases/17-hero-grainy-gradient-performance-mobile-verification/17-01-SUMMARY.md` when done
</output>
</content>
