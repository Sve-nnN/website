---
phase: 28-component-motion-rollout-hero-variants-blog-grids
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture-service-page-snapshot.mjs
  - .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json
  - .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json
autonomous: true
requirements: [MOTION-04]

must_haves:
  truths:
    - "A pre-change H1/JSON-LD snapshot exists for every Phase 28 representative route, captured before any Hero/blog-grid code changes land"
    - "A pre-change Lighthouse mobile (production build) score exists for the same routes, captured before any Hero/blog-grid code changes land"
    - "capture-service-page-snapshot.mjs remains fully backward-compatible with Phase 25's own re-run (its default URL list and CLI contract are unchanged when --routes is omitted)"
  artifacts:
    - path: ".planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json"
      provides: "Pre-change H1 + JSON-LD snapshot for the 6 Phase 28 representative routes"
    - path: ".planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json"
      provides: "Pre-change Lighthouse mobile scores (production build) for the same 6 routes"
    - path: "scripts/capture-service-page-snapshot.mjs"
      provides: "Now accepts an optional --routes CLI flag (comma-separated paths), defaulting to the original Phase 25 URLS array when omitted"
      contains: "--routes"
  key_links:
    - from: "28-baseline-content.json"
      to: "28-04-PLAN.md (post-change diff)"
      via: "same 6 URL keys, re-diffed programmatically after Hero/blog-grid changes land"
      pattern: "28-baseline-content\\.json"
    - from: "lh-phase28-baseline.json"
      to: "28-04-PLAN.md (post-change Lighthouse diff)"
      via: "same 6 route keys, re-diffed against the >5pt Performance / CWV-band-crossing thresholds"
      pattern: "lh-phase28-baseline\\.json"
---

<objective>
Capture the pre-change regression baseline (H1/JSON-LD snapshot + Lighthouse mobile scores) for the 6 routes Phase 28 will touch, BEFORE any Hero variant or blog-grid code changes land — this is the reference point 28-04's closing gate diffs against, same pattern as v1.5 Phase 25's 25-01.

Purpose: MOTION-04 requires "zero regression... compared to a pre-phase baseline, same gate pattern as v1.5 Phase 25" — that baseline does not exist yet for Phase 28's routes and must be captured before Plans 28-02/28-03 touch any code.
Output: `28-baseline-content.json`, `lh-phase28-baseline.json`, and a backward-compatible `--routes` flag added to `capture-service-page-snapshot.mjs` so it stops being hardcoded to Phase 25's service-page URLs only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-CONTEXT.md
@.planning/milestones/v1.5-phases/25-service-page-visual-polish/25-REGRESSION-DIFF.md

# Reused verbatim (pattern) from Phase 25 — read before editing
@scripts/capture-service-page-snapshot.mjs
@scripts/lighthouse-mobile.mjs

<interfaces>
`scripts/lighthouse-mobile.mjs` already supports `--routes-only <comma-separated-paths>` (falls back to its own hardcoded `ROUTES` array when omitted or empty) — use this flag as-is, no changes needed to this script.

`scripts/capture-service-page-snapshot.mjs` currently hardcodes a `URLS` array (the 8 Phase 25 `/servicios/*` + `/en/services/*` paths) with no CLI override. Add a `--routes` flag mirroring `lighthouse-mobile.mjs`'s `--routes-only` parsing exactly (comma-separated, trimmed, filtered for empties, falls back to the existing `URLS` constant when omitted) — this keeps Phase 25's own script re-runnable byte-for-byte (no `--routes` flag = old behavior) while making the script reusable for Phase 28's different route set.

Phase 28 representative routes (per `28-CONTEXT.md`'s "Representative routes" note — `/en`, `/es`, `/en/blog`, plus routes rendering the Hero block in `listing` variant: services index, GEO pages):
```
/en
/es
/en/blog
/servicios
/en/services
/en/seo-tecnico-lima
```
`/servicios` and `/en/services` are the ES/EN services index pages (Hero `listing` variant, confirmed via `src/app/(frontend)/[locale]/servicios/page.tsx` and `.../services/page.tsx`). `/en/seo-tecnico-lima` is a GEO landing page (`src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx`), also Hero `listing` variant per `28-CONTEXT.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --routes flag to capture-service-page-snapshot.mjs, capture pre-change H1/JSON-LD snapshot</name>
  <read_first>scripts/capture-service-page-snapshot.mjs</read_first>
  <files>scripts/capture-service-page-snapshot.mjs, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json</files>
  <action>
    In `scripts/capture-service-page-snapshot.mjs`'s `parseArgs()`, add a `--routes` flag using the exact same comma-separated/trim/filter-empty parsing logic already used by `lighthouse-mobile.mjs`'s `--routes-only` (i.e. `args.routes = raw ? raw.split(',').map(r => r.trim()).filter(Boolean) : null`, defaulting `args.routes` to `null`). In `main()`, replace the hardcoded `for (const path of URLS)` loop's source with `const targets = args.routes ?? URLS` and iterate `targets` instead — this is purely additive: running the script with no `--routes` flag produces byte-identical behavior to today (same `URLS` constant, same output shape), so Phase 25's own script re-run stays reproducible byte-for-byte.

    Start the dev server if not already running (`npm run dev`), then run: `node scripts/capture-service-page-snapshot.mjs --base-url http://localhost:3000 --routes /en,/es,/en/blog,/servicios,/en/services,/en/seo-tecnico-lima --out .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json`.
  </action>
  <verify>
    <automated>node -e "const s=require('./.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json'); const need=['/en','/es','/en/blog','/servicios','/en/services','/en/seo-tecnico-lima']; const missing=need.filter(k=>!s[k]||s[k].error||s[k].h1.count===0); if(missing.length>0) throw new Error('missing/zero-H1 routes: '+missing.join(', ')); console.log('PASS: 6/6 routes captured with >=1 H1')"</automated>
  </verify>
  <done>`capture-service-page-snapshot.mjs` accepts `--routes` (comma-separated), old no-flag behavior unchanged (grep confirms `URLS` constant untouched); `28-baseline-content.json` exists with all 6 keys, each with `h1.count >= 1` and a non-empty `h1.texts[0]`.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-change Lighthouse mobile baseline (production build)</name>
  <read_first>scripts/lighthouse-mobile.mjs</read_first>
  <files>.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json</files>
  <action>
    Same production-build/kill-PID pattern as Phase 25's 25-01/25-04 (Phase 17 precedent): run `PORT=3033 npm run build` (this DOES run `payload migrate` — safe here since Phase 28 makes zero schema changes, per CLAUDE.md's confirmation-only-for-destructive rule and this phase's CSS/JSX-only scope) then `PORT=3033 npm run start &` (capture the PID), then `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3033 --routes-only /en,/es,/en/blog,/servicios,/en/services,/en/seo-tecnico-lima --out .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json`, then `kill <PID>` and confirm via `lsof -i :3033` that no orphan process remains.
  </action>
  <verify>
    <automated>node -e "const s=require('./.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json'); const need=['/en','/es','/en/blog','/servicios','/en/services','/en/seo-tecnico-lima']; const missing=need.filter(k=>!s[k]||s[k].error||typeof s[k].performance!=='number'); if(missing.length>0) throw new Error('missing/errored routes: '+missing.join(', ')); console.log('PASS: 6/6 routes captured with numeric performance scores')"</automated>
  </verify>
  <done>`lh-phase28-baseline.json` exists with all 6 route keys, each carrying numeric `performance`/`accessibility`/`best-practices`/`seo`/`lcpMs`/`cls`/`tbtMs` fields and zero `error` entries; the production server on port 3033 is confirmed killed (no orphan process).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local script -> local HTTP server | Same as Phase 25's 25-01/25-04 — measurement tooling fetches a locally-running dev/prod server, no untrusted network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-28-01 | Repudiation | Baseline could be captured after code changes land, invalidating the whole gate | mitigate | This plan is Wave 1 with zero dependencies, and 28-02/28-03 (the code-changing plans) are Wave 2, `depends_on: ["28-01"]` — the orchestrator's wave ordering enforces baseline-before-change |
| T-28-02 | Denial of Service | Orphaned `next start` background process on the Lighthouse build port | mitigate | Task 2 explicitly requires capturing the PID and confirming it is killed post-run, per the Phase 17/25-01 precedent |

</threat_model>

<verification>
`28-baseline-content.json` and `lh-phase28-baseline.json` both exist with all 6 representative routes captured cleanly (non-zero H1, numeric Lighthouse scores), and `capture-service-page-snapshot.mjs`'s new `--routes` flag is backward-compatible with Phase 25's original no-flag invocation.
</verification>

<success_criteria>
- Pre-change baseline exists for both H1/JSON-LD content and Lighthouse mobile CWV, covering all 6 Phase 28 representative routes
- `capture-service-page-snapshot.mjs` gains reusable `--routes` support without breaking Phase 25's own byte-for-byte re-run
- Zero orphaned Node processes left running after Task 2
</success_criteria>

<output>
Create `.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-01-SUMMARY.md` when done
</output>
