---
phase: 28-component-motion-rollout-hero-variants-blog-grids
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - scripts/verify-reduced-motion-phase28.mjs
  - .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-post-content.json
  - .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-reduced-motion-check.json
  - .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-post.json
  - .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-REGRESSION-DIFF.md
autonomous: true
requirements: [MOTION-03, MOTION-04]

must_haves:
  truths:
    - "Every touched page (Hero variants, blog grid, FeaturedPostsBlock) renders with zero hydration-mismatch console errors under prefers-reduced-motion: reduce emulation"
    - "Every ScrollReveal-wrapped element is fully visible (opacity 1) after page load under reduced-motion emulation, not stuck at its initial opacity:0 state"
    - "H1 count/text and BreadcrumbList JSON-LD are byte-identical/deep-equal to the 28-01 baseline on all 6 representative routes"
    - "No route's Lighthouse mobile Performance score drops more than 5 points, and no CWV metric (LCP/CLS/TBT) crosses from a passing lab band into a worse one, relative to the 28-01 baseline"
    - "28-REGRESSION-DIFF.md records an explicit top-line PASS/FAIL verdict, not silently glossed over on a failure"
  artifacts:
    - path: "scripts/verify-reduced-motion-phase28.mjs"
      provides: "Playwright headless check: reducedMotion:'reduce' context, per-route console-error capture, ScrollReveal opacity assertion"
      contains: "reducedMotion: 'reduce'"
    - path: ".planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-REGRESSION-DIFF.md"
      provides: "Human-readable pass/fail record — reduced-motion consistency, H1/JSON-LD diff, and Lighthouse CWV diff — the phase's closing gate evidence"
  key_links:
    - from: "28-post-content.json"
      to: "28-baseline-content.json (28-01)"
      via: "programmatic per-URL diff (H1 text equality, BreadcrumbList itemListElement equality)"
      pattern: "28-baseline-content\\.json"
    - from: "lh-phase28-post.json"
      to: "lh-phase28-baseline.json (28-01)"
      via: "programmatic per-route score comparison, same >5pt / band-crossing thresholds as Phase 25"
      pattern: "lh-phase28-baseline\\.json"
---

<objective>
Prove zero regression on Phase 28's highest-risk axes — reduced-motion consistency, H1/JSON-LD integrity, and Core Web Vitals — by re-running the same measurement tooling from 28-01 against the now-changed Hero/blog-grid code, plus a new headless `prefers-reduced-motion: reduce` pass, and diffing programmatically against the 28-01 baseline. This is the phase's closing gate: MOTION-03 and MOTION-04 are not satisfied by "it looks right," they are satisfied by this diff passing.

Purpose: Phase 28 touches the site's highest-traffic surfaces (home Hero, blog grid, GEO/services listing heroes). This plan is the evidence that the Hero variant differentiation (28-02) and blog-grid motion polish (28-03) did not cost the site its existing SEO/CWV foundation, and that every animated component consistently respects the user's reduced-motion preference.
Output: Post-change H1/JSON-LD snapshot, post-change Lighthouse scores, a new reduced-motion headless verification script + its results, and a written diff record confirming pass/fail per must-have in `28-REGRESSION-DIFF.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-CONTEXT.md
@.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json
@.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json
@scripts/capture-service-page-snapshot.mjs
@scripts/lighthouse-mobile.mjs

# Reused Playwright pattern, read-only reference
@scripts/verify-hero-grain-gradient.mjs

<interfaces>
Both measurement scripts already exist (from Plan 28-01) and must be re-run with the same `--routes`/`--routes-only` arguments — this plan's entire premise is comparing like-for-like tooling output against the 28-01 baseline:
```
node scripts/capture-service-page-snapshot.mjs --base-url <url> --routes /en,/es,/en/blog,/servicios,/en/services,/en/seo-tecnico-lima --out <path>
node scripts/lighthouse-mobile.mjs --base-url <url> --routes-only /en,/es,/en/blog,/servicios,/en/services,/en/seo-tecnico-lima --out <path>
```

Baseline files already on disk (Plan 28-01, do not regenerate, read-only reference):
- `.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json` — shape `{ "<path>": { h1: { count, texts }, jsonLd: [{ type, raw }] } }`
- `.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json` — shape `{ "<path>": { performance, accessibility, 'best-practices', seo, lcpMs, cls, tbtMs } }`

Lighthouse regression threshold (same as v1.5 Phase 25 precedent, `25-04-PLAN.md`): flag if Performance drops more than 5 points, or any CWV metric (LCP/CLS/TBT) crosses from its baseline lab band (good/needs-improvement/poor — LCP good <=2500ms / needs-improvement <=4000ms / poor above; CLS good <=0.1 / needs-improvement <=0.25 / poor above; TBT good <=200ms / needs-improvement <=600ms / poor above) into a worse one.

`scripts/verify-hero-grain-gradient.mjs`'s `checkReducedMotion()` function (lines 209-233) is the direct precedent for the new script: `browser.newPage({ reducedMotion: 'reduce' })`, navigate, assert a `data-testid` attribute/state, screenshot. This plan's new script follows the same shape but targets `[data-testid="scroll-reveal"]` (from 28-03) instead of `[data-testid="hero-grain-gradient"]`, and additionally captures `page.on('console', ...)` / `page.on('pageerror', ...)` events to catch hydration-mismatch warnings, which `verify-hero-grain-gradient.mjs` does not currently check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headless reduced-motion consistency pass + H1/JSON-LD diff against 28-01 baseline</name>
  <read_first>scripts/verify-hero-grain-gradient.mjs, scripts/capture-service-page-snapshot.mjs, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json</read_first>
  <files>scripts/verify-reduced-motion-phase28.mjs, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-post-content.json, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-reduced-motion-check.json, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-REGRESSION-DIFF.md</files>
  <action>
1. Create `scripts/verify-reduced-motion-phase28.mjs` (ESM, `import { chromium } from 'playwright'`, following `scripts/verify-hero-grain-gradient.mjs`'s structure: `parseArgs` for `--base-url`, a `results.failures`/`warnings`/`notes` accumulator, a `fail()`/`note()` helper, `main()` launches one `chromium` instance and closes it in a `finally`). For each of the 6 representative routes (`/en`, `/es`, `/en/blog`, `/servicios`, `/en/services`, `/en/seo-tecnico-lima`): open a page with `reducedMotion: 'reduce'`, attach `page.on('console', msg => ...)` and `page.on('pageerror', err => ...)` listeners BEFORE navigating, `goto` with `waitUntil: 'networkidle'`, then assert (a) zero console messages whose text contains `hydrat` (case-insensitive) or `Warning: Text content does not match`, and (b) if the page has at least one `[data-testid="scroll-reveal"]` element (blog/home routes will, Hero-only routes may not), the first such element's computed `opacity` (via `page.evaluate` + `getComputedStyle`) equals `'1'`. Write the per-route pass/fail results (not just console output) to `.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-reduced-motion-check.json`. Exit non-zero if any route fails either assertion.
2. Run `node scripts/verify-reduced-motion-phase28.mjs --base-url http://localhost:3000` against a running dev server.
3. Run `node scripts/capture-service-page-snapshot.mjs --base-url http://localhost:3000 --routes /en,/es,/en/blog,/servicios,/en/services,/en/seo-tecnico-lima --out .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-post-content.json` (unmodified script logic from 28-01, only the `--routes` values are identical to that plan's baseline capture).
4. Write a one-off Node diff (inline `node -e`, no new script file, same pattern as Phase 25's 25-04 Task 1) asserting per route: `h1.count` unchanged (or at minimum still >=1 — note that Hero-variant padding/border changes from 28-02 are CSS-only and must not have altered H1 text), `h1.texts[0]` byte-identical to `28-baseline-content.json`, and every `BreadcrumbList` JSON-LD block's `itemListElement` deep-equal to baseline where present.
5. Create `.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-REGRESSION-DIFF.md` with the reduced-motion and H1/JSON-LD findings (one line per route per check, explicit PASS/FAIL) — Task 2 appends the Lighthouse findings and the final top-line verdict below this.
  </action>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-baseline-content.json'); const p=require('./.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-post-content.json'); let fail=0; for(const k of Object.keys(b)){ if(!p[k]){ console.error(k+': missing from post-content'); fail++; continue; } if(p[k].h1.texts[0]!==b[k].h1.texts[0]){ console.error(k+': h1 text changed'); fail++; } } if(fail>0) throw new Error(fail+' H1 regressions found'); console.log('PASS: H1 text unchanged on all routes present in baseline')"</automated>
  </verify>
  <done>`scripts/verify-reduced-motion-phase28.mjs` exists and exits 0 against a live dev server (zero hydration-console-errors, all present `scroll-reveal` elements at opacity 1 under reduced-motion emulation); `28-reduced-motion-check.json` and `28-post-content.json` both exist; the H1/JSON-LD diff shows zero regressions against the 28-01 baseline; `28-REGRESSION-DIFF.md` exists with explicit per-route PASS/FAIL lines for both checks.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run Lighthouse mobile baseline (production build), diff CWV, finalize regression gate verdict</name>
  <read_first>scripts/lighthouse-mobile.mjs, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json</read_first>
  <files>.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-post.json, .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-REGRESSION-DIFF.md</files>
  <action>
1. Same production-build pattern as 28-01 Task 2: `PORT=3034 npm run build` -> `PORT=3034 npm run start &` (capture PID) -> `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3034 --routes-only /en,/es,/en/blog,/servicios,/en/services,/en/seo-tecnico-lima --out .planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-post.json` -> kill the PID, confirm via `lsof -i :3034` that no orphan process remains.
2. Diff each route's `lh-phase28-post.json` entry against `lh-phase28-baseline.json`: flag any route where `performance` drops more than 5 points from baseline, or where `lcpMs`/`cls`/`tbtMs` crosses out of its current Lighthouse lab band into a worse one than the baseline was in (thresholds per this plan's `<interfaces>` block).
3. Append the Lighthouse findings to `28-REGRESSION-DIFF.md` (one line per route: baseline vs post score/CWV, PASS/FAIL) below the Task 1 findings, and write an explicit final verdict line at the very top of the file: `## Phase 28 Regression Gate: PASS` or `## Phase 28 Regression Gate: FAIL (see below)`.
4. If any route or check fails (reduced-motion, H1/JSON-LD, or Lighthouse), do NOT silently mark the phase done — the FAIL verdict in `28-REGRESSION-DIFF.md` is the signal MOTION-03/MOTION-04 were not met; surface it plainly in this plan's own completion summary, following the same discipline as Phase 25's `25-REGRESSION-DIFF.md` (which recorded and investigated a real FAIL rather than glossing over it). If a Lighthouse Performance-score drop is observed, follow Phase 25's precedent of a reproducibility check (2-3 isolated re-runs on a clean, zero-competing-process environment) before concluding it's a real regression versus lab-benchmark noise.
  </action>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-baseline.json'); const p=require('./.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/lh-phase28-post.json'); let fail=0; for(const k of Object.keys(b)){ if(!p[k]||p[k].error){ console.error(k+': missing or errored'); fail++; continue; } const drop=b[k].performance-p[k].performance; if(drop>5){ console.error(k+': performance dropped '+drop+' points'); fail++; } } if(fail>0) throw new Error(fail+' Lighthouse regressions found'); console.log('PASS: 6/6 routes, no performance regression >5 points')"</automated>
  </verify>
  <done>Lighthouse mobile scores re-captured for all 6 routes, diffed against baseline with zero regressions beyond threshold (or a Phase-25-style reproducibility investigation completed if a drop was observed), explicit PASS/FAIL verdict recorded at the top of `28-REGRESSION-DIFF.md`, background server confirmed stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local script -> local HTTP server | Same as 28-01/Phase 25 — measurement tooling fetches a locally-running dev/prod server, no untrusted network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-28-07 | Repudiation | Regression verdict could be silently glossed over if a check fails | mitigate | Task 2's action explicitly forbids marking the phase done on a FAIL verdict — `28-REGRESSION-DIFF.md`'s top-line verdict must be surfaced plainly in this plan's completion summary, same discipline as Phase 25's `T-25-10` |
| T-28-08 | Denial of Service | Orphaned `next start` background process on the Lighthouse build port | mitigate | Task 2 explicitly requires capturing the PID and confirming it is killed post-run |

</threat_model>

<verification>
`28-post-content.json`, `28-reduced-motion-check.json`, and `lh-phase28-post.json` all exist, diffed programmatically against the 28-01 baseline with zero H1/JSON-LD/CWV/reduced-motion regressions (or an investigated, explained FAIL), and `28-REGRESSION-DIFF.md` records an explicit top-line PASS/FAIL verdict.
</verification>

<success_criteria>
- MOTION-03 (consistent `useReducedMotion()` usage) backed by a real headless Playwright pass under `prefers-reduced-motion: reduce` emulation, not visual impression
- MOTION-04 (zero CWV regression) backed by a programmatic Lighthouse diff against the 28-01 pre-change baseline
- `28-REGRESSION-DIFF.md` is the closing artifact for the entire phase — if it says PASS, Phase 28 is verifiably done; if FAIL, the specific failing route/metric/check is named for follow-up
</success_criteria>

<output>
Create `.planning/phases/28-component-motion-rollout-hero-variants-blog-grids/28-04-SUMMARY.md` when done
</output>
