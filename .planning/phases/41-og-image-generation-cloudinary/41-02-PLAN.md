---
phase: 41-og-image-generation-cloudinary
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/app/(frontend)/[locale]/contact/page.tsx
  - src/app/(frontend)/[locale]/privacy/page.tsx
  - src/app/(frontend)/[locale]/terms/page.tsx
  - src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx
  - src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx
  - src/app/(frontend)/[locale]/blog/page.tsx
autonomous: true
requirements: [OG-01, OG-02, OG-03]

must_haves:
  truths:
    - "GET /contact, /privacy, /terms, /seo-tecnico-lima, /seo-tecnico-madrid, /blog (and their /en/... counterparts) all return a <meta property=\"og:image\" content=\"...\"> tag on res.cloudinary.com"
    - "Each of these 6 routes falls back to its own fixed-slug deterministic image (contact/privacy/terms/seo-tecnico-lima/seo-tecnico-madrid/blog) when the doc has no meta.image set — 6 different fallback images, not 1 repeated one"
    - "og:url resolves correctly per locale for all 6 routes (unprefixed es, /en-prefixed en, per next-intl's as-needed routing)"
  artifacts:
    - path: "src/app/(frontend)/[locale]/contact/page.tsx"
      provides: "openGraph wired via buildOpenGraph"
      contains: "buildOpenGraph({"
    - path: "src/app/(frontend)/[locale]/blog/page.tsx"
      provides: "openGraph wired via buildOpenGraph (blog listing, doc-backed via 'pages' collection slug 'blog')"
      contains: "buildOpenGraph({"
  key_links:
    - from: "src/app/(frontend)/[locale]/contact/page.tsx generateMetadata"
      to: "src/lib/og-image.ts buildOpenGraph (created in Plan 41-01)"
      via: "import { buildOpenGraph } from '@/lib/og-image'"
      pattern: "buildOpenGraph\\("
---

<objective>
Expand the Cloudinary `og:image` mechanism built in Plan 41-01 to the 6 remaining `pages`-collection routes that have no `heroImage` field (only `meta.image` + a deterministic slug fallback applies): `/contact`, `/privacy`, `/terms`, `/seo-tecnico-lima`, `/seo-tecnico-madrid`, and the `/blog` listing page.

Purpose: These 6 routes share the exact same wiring shape already proven end-to-end in Plan 41-01 (Home) — this plan is pure horizontal expansion of the tracer slice, mechanical repetition of the same `buildOpenGraph` call with each route's own fixed slug and relative URL.
Output: 6 `generateMetadata` functions updated with `openGraph`, each curl-verified to return a distinct, resolvable Cloudinary og:image URL.
</objective>

## Artifacts this phase produces

No new files or exports — this plan only calls `buildOpenGraph` (created in Plan 41-01's `src/lib/og-image.ts`) from 6 existing `generateMetadata` functions:

- `src/app/(frontend)/[locale]/contact/page.tsx` (MODIFIED)
- `src/app/(frontend)/[locale]/privacy/page.tsx` (MODIFIED)
- `src/app/(frontend)/[locale]/terms/page.tsx` (MODIFIED)
- `src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx` (MODIFIED)
- `src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx` (MODIFIED)
- `src/app/(frontend)/[locale]/blog/page.tsx` (MODIFIED)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-og-image-generation-cloudinary/41-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- All 6 files share this exact current shape (Pages collection, no heroImage). Already read in full by the planner. -->

`src/app/(frontend)/[locale]/contact/page.tsx` current `generateMetadata`:
```typescript
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  const doc = await getContactPage(locale)
  const meta = doc?.meta

  return {
    title: meta?.title ?? doc?.title ?? contactFallbackTitle(locale),
    description: meta?.description ?? '',
  }
}
```

`src/app/(frontend)/[locale]/privacy/page.tsx` / `terms/page.tsx` / `seo-tecnico-lima/page.tsx` / `seo-tecnico-madrid/page.tsx` all share this identical shape (only the fallback-title ternary text differs):
```typescript
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  const doc = await getPage(locale)
  const meta = doc?.meta

  return {
    title: meta?.title ?? doc?.title ?? (locale === 'es' ? '<ES fallback title>' : '<EN fallback title>'),
    description: meta?.description ?? '',
  }
}
```

`src/app/(frontend)/[locale]/blog/page.tsx` current `generateMetadata`:
```typescript
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  const doc = await getBlogPage(locale)
  const meta = doc?.meta

  return {
    title: meta?.title ?? doc?.title ?? 'Blog',
    description: meta?.description ?? '',
  }
}
```

`src/lib/og-image.ts` (created in Plan 41-01) exports `buildOpenGraph(params: { title, description?, url, locale: 'es' | 'en', slug, metaImage?, heroImage? })` — see 41-01-PLAN.md for the full signature. None of these 6 routes has a `heroImage` field (Pages collection has none) — never pass `heroImage` in this plan.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire contact, privacy, terms</name>
  <files>src/app/(frontend)/[locale]/contact/page.tsx, src/app/(frontend)/[locale]/privacy/page.tsx, src/app/(frontend)/[locale]/terms/page.tsx</files>
  <read_first>
    src/lib/og-image.ts (buildOpenGraph, from Plan 41-01)
    src/app/(frontend)/[locale]/contact/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/privacy/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/terms/page.tsx (current full generateMetadata — see Interfaces block)
  </read_first>
  <action>
    In each of the 3 files, add `import { buildOpenGraph } from '@/lib/og-image'`, then in `generateMetadata` extract the existing inline title/description expressions into named `const title = ...` / `const description = meta?.description ?? ''` consts (keep each file's own fallback-title ternary unchanged), and add `openGraph: buildOpenGraph({ title, description, url, locale: locale as 'es' | 'en', slug, metaImage: meta?.image })` to the returned object, where per file:

    - `contact/page.tsx`: `url: locale === 'en' ? '/en/contact' : '/contact'`, `slug: 'contact'`
    - `privacy/page.tsx`: `url: locale === 'en' ? '/en/privacy' : '/privacy'`, `slug: 'privacy'`
    - `terms/page.tsx`: `url: locale === 'en' ? '/en/terms' : '/terms'`, `slug: 'terms'`

    Do not pass `heroImage` — Pages collection docs have no such field. Do not touch any other part of these files (page bodies, JSON-LD, contact form action are unmodified, out of scope).
  </action>
  <acceptance_criteria>
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/contact/page.tsx"` returns 1, and the call includes `slug: 'contact'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/privacy/page.tsx"` returns 1, and the call includes `slug: 'privacy'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/terms/page.tsx"` returns 1, and the call includes `slug: 'terms'`
    - `curl -s http://localhost:3000/contact`, `/privacy`, `/terms` each return an `og:image` tag on `res.cloudinary.com`
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && for p in contact privacy terms; do curl -s "http://localhost:3000/$p" | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com' || exit 1; done && echo OK</automated>
  </verify>
  <done>contact/privacy/terms all emit a working og:image tag with a distinct fixed slug each; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire seo-tecnico-lima, seo-tecnico-madrid, blog listing</name>
  <files>src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx, src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx, src/app/(frontend)/[locale]/blog/page.tsx</files>
  <read_first>
    src/lib/og-image.ts (buildOpenGraph, from Plan 41-01)
    src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/blog/page.tsx (current full generateMetadata — see Interfaces block)
  </read_first>
  <action>
    Same pattern as Task 1 — add `import { buildOpenGraph } from '@/lib/og-image'`, extract `title`/`description` consts, add `openGraph: buildOpenGraph({ title, description, url, locale: locale as 'es' | 'en', slug, metaImage: meta?.image })`:

    - `seo-tecnico-lima/page.tsx`: `url: locale === 'en' ? '/en/seo-tecnico-lima' : '/seo-tecnico-lima'`, `slug: 'seo-tecnico-lima'` — this route uses the SAME URL segment in both locales (Phase 20 decision), only the `/en` prefix differs.
    - `seo-tecnico-madrid/page.tsx`: `url: locale === 'en' ? '/en/seo-tecnico-madrid' : '/seo-tecnico-madrid'`, `slug: 'seo-tecnico-madrid'`
    - `blog/page.tsx`: `url: locale === 'en' ? '/en/blog' : '/blog'`, `slug: 'blog'`

    Do not pass `heroImage` in any of the 3. Do not touch page bodies / JSON-LD / breadcrumbs.
  </action>
  <acceptance_criteria>
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/seo-tecnico-lima/page.tsx"` returns 1, includes `slug: 'seo-tecnico-lima'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/seo-tecnico-madrid/page.tsx"` returns 1, includes `slug: 'seo-tecnico-madrid'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/blog/page.tsx"` returns 1, includes `slug: 'blog'`
    - `curl -s http://localhost:3000/seo-tecnico-lima`, `/seo-tecnico-madrid`, `/blog` each return an `og:image` tag on `res.cloudinary.com`
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && for p in seo-tecnico-lima seo-tecnico-madrid blog; do curl -s "http://localhost:3000/$p" | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com' || exit 1; done && echo OK</automated>
  </verify>
  <done>seo-tecnico-lima/seo-tecnico-madrid/blog listing all emit a working og:image tag with a distinct fixed slug each; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload editor content (page titles) -> Cloudinary transform URL | Same boundary as Plan 41-01, no new mechanism introduced — this plan only adds call sites of the already-mitigated `buildOpenGraph`/`getCloudinaryOgWithTitle` |

## STRIDE Threat Register

This plan introduces zero new code paths — it is pure call-site expansion of Plan 41-01's already-mitigated `buildOpenGraph`. No new trust boundary, no new input handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-04 | Tampering | 6 additional `generateMetadata` call sites of `buildOpenGraph` | low | accept | Mitigation (double-encoding of `,`/`/` in titles) already lives in `src/lib/og-image.ts` (Plan 41-01, T-41-01) — these call sites carry no new logic, just pass through `title`/`slug`/`metaImage`. |
| T-41-SC | Tampering | npm/CLI installs | n/a | accept | Zero new packages installed this plan. |
</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` exits 0
2. `curl -s http://localhost:3000/{contact,privacy,terms,seo-tecnico-lima,seo-tecnico-madrid,blog}` each return a `res.cloudinary.com` og:image tag
3. `curl -s http://localhost:3000/en/{contact,privacy,terms,seo-tecnico-lima,seo-tecnico-madrid,blog}` each also return one, with a `/en/...` og:url
</verification>

<success_criteria>
- All 6 routes in this plan emit a valid, resolvable Cloudinary `og:image` tag in both locales
- Each route's fallback (when no `meta.image` is set) uses its own distinct fixed slug — verifiable by the 6 different `l_text:`-embedded image public_ids across routes
</success_criteria>

<output>
Create `.planning/phases/41-og-image-generation-cloudinary/41-02-SUMMARY.md` when done
</output>
