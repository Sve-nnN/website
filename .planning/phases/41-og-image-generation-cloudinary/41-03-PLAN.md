---
phase: 41-og-image-generation-cloudinary
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/app/(frontend)/[locale]/services/page.tsx
  - src/app/(frontend)/[locale]/servicios/page.tsx
  - src/app/(frontend)/[locale]/services/[slug]/page.tsx
  - src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
  - src/app/(frontend)/[locale]/websites/[slug]/page.tsx
  - src/app/(frontend)/[locale]/case-studies/page.tsx
  - src/app/(frontend)/[locale]/authors/page.tsx
  - src/app/(frontend)/[locale]/websites/page.tsx
  - src/app/(frontend)/[locale]/search/page.tsx
autonomous: true
requirements: [OG-01, OG-02, OG-03]

must_haves:
  truths:
    - "GET /servicios, /en/services, /servicios/{slug}, /en/services/{slug} all return og:image + og:url using the SAME locale-canonical URL regardless of which of the 4 physical route files served the request (matches the existing canonical-URL collapsing behavior from Phase 23)"
    - "GET /authors/{slug} and /websites/{slug} return og:image from meta.image when set, else a deterministic per-doc-slug fallback (no heroImage field on either collection)"
    - "GET /case-studies, /authors, /websites (listings) and /search — none of which are backed by a Payload doc — still each return a distinct, resolvable og:image tag via a fixed per-route slug"
  artifacts:
    - path: "src/app/(frontend)/[locale]/services/[slug]/page.tsx"
      provides: "openGraph wired via buildOpenGraph, url shares the servicios/services canonical-collapsing pattern"
      contains: "buildOpenGraph({"
    - path: "src/app/(frontend)/[locale]/websites/page.tsx"
      provides: "openGraph wired via buildOpenGraph on a route with no backing Payload doc (fixed slug 'websites')"
      contains: "buildOpenGraph({"
  key_links:
    - from: "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx generateMetadata"
      to: "src/lib/og-image.ts buildOpenGraph (created in Plan 41-01)"
      via: "import { buildOpenGraph } from '@/lib/og-image'"
      pattern: "buildOpenGraph\\("
    - from: "src/app/(frontend)/[locale]/authors/[slug]/page.tsx generateMetadata"
      to: "src/lib/og-image.ts buildOpenGraph (created in Plan 41-01)"
      via: "import { buildOpenGraph } from '@/lib/og-image'"
      pattern: "buildOpenGraph\\("
---

<objective>
Finish the OG-image rollout: the 4 Servicios/Services physical routes (index + detail, es + en segment pairs), the 2 remaining doc-backed detail routes with no `heroImage` field (Author, Website), and the 4 routes with no backing Payload doc at all (Case Studies listing, Authors listing, Websites listing, Search). This closes out every `generateMetadata` in the codebase — after this plan, all ~19 public routes emit a real `og:image`.

Purpose: These are the remaining route shapes not covered by Plans 41-01/41-02 — the Servicios group needs its own canonical-URL logic (mirroring Phase 23's `buildServiceAlternates`), and the no-doc listings need a fixed fallback slug since there is no `meta.image` to check at all.
Output: 10 files across 3 tasks, each with a working `openGraph` field, completing ROADMAP Phase 41 Success Criteria #3 (every public route wired) in full.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-og-image-generation-cloudinary/41-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- All already read in full by the planner. -->

`src/app/(frontend)/[locale]/services/page.tsx` and `servicios/page.tsx` (identical current shape, both import `buildServiceAlternates` from `@/lib/canonical`):
```typescript
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  const doc = await getPage(locale)

  return {
    title: doc?.meta?.title ?? doc?.title ?? (locale === 'es' ? 'Servicios' : 'Services'),
    description: doc?.meta?.description ?? '',
    alternates: buildServiceAlternates(locale as 'es' | 'en'),
  }
}
```

`src/app/(frontend)/[locale]/services/[slug]/page.tsx` and `servicios/[slug]/page.tsx` (identical current shape):
```typescript
export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }) {
  const { locale, slug } = await params
  const doc = await getPage(locale, slug)
  if (!doc) return {}

  return {
    title: doc.meta?.title ?? doc.title,
    description: doc.meta?.description ?? '',
    alternates: buildServiceAlternates(locale as 'es' | 'en', { slug: doc.slug ?? slug }),
  }
}
```
`src/lib/canonical.ts`'s `esPathFor`/`enPathFor` (the source of truth these 4 files' `alternates` already reuse) compute: index `/servicios` (es) / `/en/services` (en); detail `/servicios/{slug}` (es) / `/en/services/{slug}` (en) — purely from `locale`, identical regardless of which physical route file served the request. This plan's `og:url` mirrors that exact same pair of path formulas inline (no new import needed).

`src/app/(frontend)/[locale]/authors/[slug]/page.tsx` current `generateMetadata` (no `depth` override — Payload's default depth already resolves `meta.image`; Author has no `heroImage` field, only an unrelated `avatar` field that is NOT part of the OG fallback chain per 41-CONTEXT.md):
```typescript
async function getAuthor(locale: string, slug: string) {
  const payload = await getPayload({ config })
  const { docs } = await payload.find({
    collection: 'authors',
    where: { slug: { equals: slug } },
    locale: locale as 'es' | 'en',
    limit: 1,
  })
  return docs[0]
}

export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }) {
  const { locale, slug } = await params
  const doc = await getAuthor(locale, slug)
  if (!doc) return {}

  return {
    title: doc.meta?.title ?? doc.name,
    description: doc.meta?.description ?? doc.jobTitle ?? '',
  }
}
```

`src/app/(frontend)/[locale]/websites/[slug]/page.tsx` current `generateMetadata` (`depth: 1`, no `heroImage` field on Website):
```typescript
async function getWebsite(locale: string, slug: string) {
  const payload = await getPayload({ config })
  const { docs } = await payload.find({
    collection: 'websites',
    where: { slug: { equals: slug } },
    locale: locale as 'es' | 'en',
    depth: 1,
    limit: 1,
  })
  return docs[0]
}

export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }) {
  const { locale, slug } = await params
  const doc = await getWebsite(locale, slug)
  if (!doc) return {}

  return {
    title: doc.meta?.title ?? doc.title,
    description: doc.meta?.description ?? doc.role ?? doc.industry ?? '',
  }
}
```

The 4 no-doc routes — `case-studies/page.tsx`, `authors/page.tsx`, `websites/page.tsx`, `search/page.tsx` — each currently return only a static `{ title }` with no Payload query in `generateMetadata` at all. Example (`case-studies/page.tsx`):
```typescript
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params
  return {
    title: locale === 'es' ? 'Casos de éxito' : 'Case Studies',
  }
}
```
`search/page.tsx` differs only in reading its title from a local `copy` dict: `const t = copy[locale as 'es' | 'en'] ?? copy.es; return { title: t.title }`.

`src/lib/og-image.ts` (created in Plan 41-01) exports `buildOpenGraph(params: { title, description?, url, locale: 'es' | 'en', slug, metaImage?, heroImage? })`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire the 4 Servicios/Services physical routes (index + detail, es + en)</name>
  <files>src/app/(frontend)/[locale]/services/page.tsx, src/app/(frontend)/[locale]/servicios/page.tsx, src/app/(frontend)/[locale]/services/[slug]/page.tsx, src/app/(frontend)/[locale]/servicios/[slug]/page.tsx</files>
  <read_first>
    src/lib/og-image.ts (buildOpenGraph, from Plan 41-01)
    src/lib/canonical.ts (full file — buildServiceAlternates/esPathFor/enPathFor, already imported by these 4 files)
    src/app/(frontend)/[locale]/services/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/servicios/page.tsx (current full generateMetadata — identical to services/page.tsx)
    src/app/(frontend)/[locale]/services/[slug]/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/servicios/[slug]/page.tsx (current full generateMetadata — identical to services/[slug]/page.tsx)
  </read_first>
  <action>
    In `services/page.tsx` and `servicios/page.tsx` (both index files, identical edit): add `import { buildOpenGraph } from '@/lib/og-image'`. Extract `const title = doc?.meta?.title ?? doc?.title ?? (locale === 'es' ? 'Servicios' : 'Services')` and `const description = doc?.meta?.description ?? ''` as named consts, keep the existing `alternates: buildServiceAlternates(locale as 'es' | 'en')` line unchanged, and add `openGraph: buildOpenGraph({ title, description, url: locale === 'es' ? '/servicios' : '/en/services', locale: locale as 'es' | 'en', slug: 'servicios', metaImage: doc?.meta?.image })`. Use the literal fixed `slug: 'servicios'` in BOTH files (not `'services'`) so both physical routes hash to the identical fallback image when no `meta.image` is set — they represent the same logical page per the canonical-collapsing design.

    In `services/[slug]/page.tsx` and `servicios/[slug]/page.tsx` (both detail files, identical edit): add the same import. Extract `const title = doc.meta?.title ?? doc.title` and `const description = doc.meta?.description ?? ''`, keep `alternates: buildServiceAlternates(locale as 'es' | 'en', { slug: doc.slug ?? slug })` unchanged, and add `openGraph: buildOpenGraph({ title, description, url: locale === 'es' ? \`/servicios/${doc.slug ?? slug}\` : \`/en/services/${doc.slug ?? slug}\`, locale: locale as 'es' | 'en', slug: doc.slug ?? slug, metaImage: doc.meta?.image })`.

    None of the 4 files pass `heroImage` (Pages collection has no such field).
  </action>
  <acceptance_criteria>
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/services/page.tsx"` returns 1, includes `slug: 'servicios'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/servicios/page.tsx"` returns 1, includes `slug: 'servicios'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/services/[slug]/page.tsx"` returns 1
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"` returns 1
    - `curl -s http://localhost:3000/servicios` and `curl -s http://localhost:3000/en/services` both return an `og:image` tag on `res.cloudinary.com`, and both index routes' fallback (if no `meta.image` set) resolve to the identical `l_text:`-embedded public_id
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && curl -s http://localhost:3000/servicios | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com' && curl -s http://localhost:3000/en/services | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com' && echo OK</automated>
  </verify>
  <done>All 4 Servicios/Services route files emit og:image via buildOpenGraph, with og:url matching the same canonical-collapsing formula already used by buildServiceAlternates; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Author detail + Website detail (doc-backed, no heroImage)</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/(frontend)/[locale]/websites/[slug]/page.tsx</files>
  <read_first>
    src/lib/og-image.ts (buildOpenGraph, from Plan 41-01)
    src/app/(frontend)/[locale]/authors/[slug]/page.tsx (current full generateMetadata — see Interfaces block)
    src/app/(frontend)/[locale]/websites/[slug]/page.tsx (current full generateMetadata — see Interfaces block)
  </read_first>
  <action>
    In `authors/[slug]/page.tsx`: add `import { buildOpenGraph } from '@/lib/og-image'`. Extract `const title = doc.meta?.title ?? doc.name` and `const description = doc.meta?.description ?? doc.jobTitle ?? ''`, add `openGraph: buildOpenGraph({ title, description, url: locale === 'en' ? \`/en/authors/${slug}\` : \`/authors/${slug}\`, locale: locale as 'es' | 'en', slug: doc.slug ?? slug, metaImage: doc.meta?.image })`. Do not pass `heroImage` and do not introduce `doc.avatar` into the OG chain — Author's `avatar` field is a distinct field not part of the 3-tier priority decided in 41-CONTEXT.md.

    In `websites/[slug]/page.tsx`: add the same import. Extract `const title = doc.meta?.title ?? doc.title` and `const description = doc.meta?.description ?? doc.role ?? doc.industry ?? ''`, add `openGraph: buildOpenGraph({ title, description, url: locale === 'en' ? \`/en/websites/${slug}\` : \`/websites/${slug}\`, locale: locale as 'es' | 'en', slug: doc.slug ?? slug, metaImage: doc.meta?.image })`. Website has no `heroImage` field either — do not pass it, and do not use `screenshots[]` as a background source (out of scope, not part of the decided priority chain).
  </action>
  <acceptance_criteria>
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"` returns 1, no `doc.avatar` reference added
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/websites/[slug]/page.tsx"` returns 1, no `screenshots` reference added
    - A real author slug (`curl -s http://localhost:3000/sitemap.xml | grep -oE '<loc>[^<]*/authors/[^<]*</loc>'` — excluding the bare `/authors` listing URL) curls an `og:image` tag on `res.cloudinary.com`
    - A real website slug (same approach, `/websites/`) curls an `og:image` tag on `res.cloudinary.com`
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && AUTHOR_URL=$(curl -s http://localhost:3000/sitemap.xml | grep -oE '<loc>[^<]*/authors/[^<]+</loc>' | head -1 | sed -E 's#<loc>(.*)</loc>#\1#') && curl -s "$AUTHOR_URL" | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com' && WEBSITE_URL=$(curl -s http://localhost:3000/sitemap.xml | grep -oE '<loc>[^<]*/websites/[^<]+</loc>' | head -1 | sed -E 's#<loc>(.*)</loc>#\1#') && curl -s "$WEBSITE_URL" | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com'</automated>
  </verify>
  <done>Author detail and Website detail both emit a working og:image tag from meta.image or the slug fallback; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire the 4 no-doc routes (Case Studies listing, Authors listing, Websites listing, Search)</name>
  <files>src/app/(frontend)/[locale]/case-studies/page.tsx, src/app/(frontend)/[locale]/authors/page.tsx, src/app/(frontend)/[locale]/websites/page.tsx, src/app/(frontend)/[locale]/search/page.tsx</files>
  <read_first>
    src/lib/og-image.ts (buildOpenGraph, from Plan 41-01)
    src/app/(frontend)/[locale]/case-studies/page.tsx (current full generateMetadata — see Interfaces block, static title only)
    src/app/(frontend)/[locale]/authors/page.tsx (current full generateMetadata — static title only)
    src/app/(frontend)/[locale]/websites/page.tsx (current full generateMetadata — static title only)
    src/app/(frontend)/[locale]/search/page.tsx (current full generateMetadata — reads title from local `copy` dict)
  </read_first>
  <action>
    None of these 4 routes queries a Payload doc in `generateMetadata` (no `meta.image` exists to check at all) — `buildOpenGraph` is called with `metaImage`/`heroImage` both omitted, so `resolveOgBackgroundUrl` falls straight to `getFallbackHeroImage(slug)`. Add `import { buildOpenGraph } from '@/lib/og-image'` to each file, keep the existing title expression as a named `const title = ...`, and return `{ title, openGraph: buildOpenGraph({ title, url, locale: locale as 'es' | 'en', slug }) }`:

    - `case-studies/page.tsx`: `const title = locale === 'es' ? 'Casos de éxito' : 'Case Studies'`, `url: locale === 'en' ? '/en/case-studies' : '/case-studies'`, `slug: 'case-studies'`
    - `authors/page.tsx`: `const title = locale === 'es' ? 'Autores' : 'Authors'`, `url: locale === 'en' ? '/en/authors' : '/authors'`, `slug: 'authors'`
    - `websites/page.tsx`: `const title = locale === 'es' ? 'Sitios web' : 'Websites'`, `url: locale === 'en' ? '/en/websites' : '/websites'`, `slug: 'websites'`
    - `search/page.tsx`: keep `const t = copy[locale as 'es' | 'en'] ?? copy.es` as-is, use `const title = t.title`, `url: locale === 'en' ? '/en/search' : '/search'`, `slug: 'search'`
  </action>
  <acceptance_criteria>
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/case-studies/page.tsx"` returns 1, includes `slug: 'case-studies'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/authors/page.tsx"` returns 1, includes `slug: 'authors'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/websites/page.tsx"` returns 1, includes `slug: 'websites'`
    - `grep -c "buildOpenGraph({" "src/app/(frontend)/[locale]/search/page.tsx"` returns 1, includes `slug: 'search'`
    - `curl -s http://localhost:3000/case-studies`, `/authors`, `/websites`, `/search` each return a distinct `og:image` tag on `res.cloudinary.com` (4 different fallback images, verifiable via 4 different `l_text:`-embedded public_ids since each uses a different slug hash)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && for p in case-studies authors websites search; do curl -s "http://localhost:3000/$p" | grep -o 'property="og:image" content="[^"]*"' | grep -q 'res.cloudinary.com' || exit 1; done && echo OK</automated>
  </verify>
  <done>All 4 no-doc routes emit a working, distinct og:image tag via their own fixed slug; tsc passes. This is the last of the ~19 public generateMetadata functions — Phase 41 Success Criteria #3 is fully closed after this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload editor content (page/author/website titles) -> Cloudinary transform URL | Same boundary as Plan 41-01, no new mechanism — this plan only adds call sites of the already-mitigated `buildOpenGraph`/`getCloudinaryOgWithTitle` |
| Static/no-doc route titles (Task 3) -> Cloudinary transform URL | Titles here are hardcoded string literals in the codebase (not user/editor input at all), strictly lower risk than the doc-backed routes |

## STRIDE Threat Register

This plan introduces zero new code paths — pure call-site expansion of Plan 41-01's already-mitigated `buildOpenGraph`, including 4 call sites (Task 3) where the title isn't even dynamic content.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Tampering | 10 additional `generateMetadata` call sites of `buildOpenGraph` (Servicios group, Author/Website detail, 4 no-doc listings) | low | accept | Mitigation (double-encoding of `,`/`/` in titles) already lives in `src/lib/og-image.ts` (Plan 41-01, T-41-01); Task 3's 4 titles are hardcoded literals, not even a theoretical injection surface. |
| T-41-06 | Information Disclosure | Servicios canonical-URL collapsing (`og:url` computed purely from `locale`, matching `buildServiceAlternates`) | low | accept | Mirrors the exact, already-shipped Phase 23 canonical logic — no new URL-disclosure surface, same 2 canonical targets already public. |
| T-41-SC | Tampering | npm/CLI installs | n/a | accept | Zero new packages installed this plan. |
</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` exits 0
2. `curl -s http://localhost:3000/servicios` and `curl -s http://localhost:3000/en/services` both return a `res.cloudinary.com` og:image tag
3. A real `/authors/{slug}` and a real `/websites/{slug}` (discovered via `/sitemap.xml`) each return one
4. `curl -s http://localhost:3000/{case-studies,authors,websites,search}` each return one, with 4 distinct fallback public_ids
</verification>

<success_criteria>
- All 10 remaining route files in this plan emit a valid, resolvable Cloudinary `og:image` tag in both locales
- The 4 Servicios/Services physical routes collapse to the same 2 canonical og:url targets per locale, matching Phase 23's established behavior
- Combined with Plans 41-01 and 41-02, every public `generateMetadata` in the codebase (~19 total) now emits `og:image` + `og:url` — ROADMAP Phase 41 Success Criteria #2/#3/#5 fully closed
</success_criteria>

<output>
Create `.planning/phases/41-og-image-generation-cloudinary/41-03-SUMMARY.md` when done
</output>
