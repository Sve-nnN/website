---
phase: 02-biling-e-seo
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.mjs
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - messages/es.json
  - messages/en.json
autonomous: false
requirements: [I18N-01]

must_haves:
  truths:
    - "next-intl routing config exists with defaultLocale 'es' and localePrefix 'as-needed', matching the current hand-rolled JuanPortfolio URL behavior"
    - "localeDetection is explicitly disabled so Accept-Language never overrides the unprefixed Spanish root (RESEARCH.md Pitfall 1)"
    - "next.config.mjs wires next-intl's plugin around the existing withPayload wrapper without touching output:standalone or images.remotePatterns"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "defineRouting() config consumed by middleware.ts and request.ts"
      contains: "localeDetection: false"
    - path: "src/i18n/request.ts"
      provides: "getRequestConfig() loading messages/{locale}.json"
      exports: ["default"]
    - path: "messages/es.json"
      provides: "Spanish UI strings"
    - path: "messages/en.json"
      provides: "English UI strings"
  key_links:
    - from: "next.config.mjs"
      to: "src/i18n/request.ts"
      via: "createNextIntlPlugin('./src/i18n/request.ts')"
      pattern: "createNextIntlPlugin"
    - from: "src/i18n/request.ts"
      to: "src/i18n/routing.ts"
      via: "import { routing } from './routing'"
      pattern: "from '\\./routing'"
---

<objective>
Install `next-intl` and scaffold the routing/messages configuration this phase's middleware and pages will consume.

Purpose: Establish the next-intl foundation (routing rules, request config, UI message catalogs, Next.js plugin wiring) that Plan 02-03 (middleware + `[locale]` pages) depends on. This plan does NOT touch `payload.config.ts`, `middleware.ts`, or any route files — pure config scaffold, isolated so it can run in parallel with 02-02.
Output: `src/i18n/routing.ts`, `src/i18n/request.ts`, `messages/es.json`, `messages/en.json`, modified `next.config.mjs`, `next-intl` added to `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-biling-e-seo/02-CONTEXT.md
@.planning/phases/02-biling-e-seo/02-RESEARCH.md
@.planning/phases/02-biling-e-seo/02-PATTERNS.md

<interfaces>
Reference shape (values, NOT to be copied verbatim — apturio uses different locale/prefix decisions):

From /Users/juan/Documents/Codigo/Arianna/apturio/website/src/i18n/routing.ts:
```typescript
import { defineRouting } from 'next-intl/routing'
export const routing = defineRouting({
  locales: ['en', 'es'],
  defaultLocale: 'en',
  localePrefix: 'always',
})
```

From /Users/juan/Documents/Codigo/Arianna/apturio/website/src/i18n/request.ts (copy this shape verbatim, it is locale-agnostic):
```typescript
import { getRequestConfig } from 'next-intl/server'
import { hasLocale } from 'next-intl'
import { routing } from './routing'

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale
  const locale = hasLocale(routing.locales, requested) ? requested : routing.defaultLocale
  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  }
})
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify next-intl package legitimacy before install</name>
  <read_first>.planning/phases/02-biling-e-seo/02-RESEARCH.md (Package Legitimacy Audit table — next-intl tagged [ASSUMED], slopcheck unavailable this session)</read_first>
  <what-built>Nothing yet — this gate runs BEFORE `npm install next-intl` per the Package Legitimacy Gate protocol, since RESEARCH.md could not run slopcheck and tagged the package [ASSUMED].</what-built>
  <action>Do not run `npm install` yet. Present the verification steps below to Juan and wait for explicit approval before Task 2 proceeds with the install.</action>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/next-intl — confirm publisher is `amannn` (Jan Amann), weekly downloads are in the millions, latest version matches `4.13.x`.
    2. Visit https://github.com/amannn/next-intl — confirm it is the source repo linked from the npm page, actively maintained (recent commits/releases).
  </how-to-verify>
  <acceptance_criteria>Juan confirms the package identity matches (correct publisher, correct repo, high download count) before install proceeds.</acceptance_criteria>
  <resume-signal>Type "approved" to proceed with `npm install next-intl@^4.13`, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install next-intl and scaffold routing/request/messages/next.config.mjs</name>
  <files>package.json, package-lock.json, src/i18n/routing.ts, src/i18n/request.ts, messages/es.json, messages/en.json, next.config.mjs</files>
  <read_first>
    /Users/juan/Documents/Codigo/Personal/juantech/juan-payload/next.config.mjs
    /Users/juan/Documents/Codigo/Personal/juantech/juan-payload/package.json
    /Users/juan/Documents/Codigo/Arianna/apturio/website/next.config.mjs
    /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/i18n/translations.ts
  </read_first>
  <action>
    Run `npm install next-intl@^4.13` (resolves to 4.13.1 per RESEARCH.md registry check) — adds it to `dependencies` in package.json.

    Create `src/i18n/routing.ts` exporting `routing` via `defineRouting()` from `next-intl/routing` with exactly: `locales: ['es', 'en']`, `defaultLocale: 'es'`, `localePrefix: 'as-needed'`, `localeDetection: false`. Add an inline comment above `localeDetection: false` noting it is CRITICAL for SEO URL parity (RESEARCH.md Pitfall 1) — without it, Accept-Language sniffing could redirect the unprefixed Spanish root for English-preferring browsers/bots, which the current site never does.

    Create `src/i18n/request.ts` copying the apturio shape verbatim from the `<interfaces>` block above — `getRequestConfig` from `next-intl/server`, `hasLocale` from `next-intl`, importing `routing` from `./routing`, loading `../../messages/${locale}.json`.

    Create `messages/es.json` and `messages/en.json` at the project root (sibling to `src/`, matching the `../../messages/${locale}.json` import path from `src/i18n/request.ts`). Both files share identical top-level keys, only values translated. Minimum key set (Claude's discretion on exact structure per CONTEXT.md, but MUST include these three top-level groups so `[locale]` pages built in 02-03/02-05 have real strings to consume instead of hardcoded literals):
    - `nav`: `home`, `blog`, `caseStudies`, `contact` (es: "Inicio"/"Blog"/"Casos de éxito"/"Contacto"; en: "Home"/"Blog"/"Case Studies"/"Contact")
    - `home`: `heroTitle`, `heroSubtitle` (placeholder copy is fine — real content arrives Phase 4/5)
    - `common`: `readMore`, `viewAll` (es: "Leer más"/"Ver todos"; en: "Read more"/"View all")

    Modify `next.config.mjs`: add `import createNextIntlPlugin from 'next-intl/plugin'` below the existing `withPayload` import. Add `const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')` above `const nextConfig = {...}`. Change the final export line from `export default withPayload(nextConfig)` to `export default withPayload(withNextIntl(nextConfig))`. Leave `output: 'standalone'` and `images.remotePatterns: []` untouched — do not add Cloudinary patterns (Phase 3 scope) and do not copy apturio's `async redirects()` block (this project's redirects are handled by `@payloadcms/plugin-redirects` + middleware, not `next.config.mjs`).
  </action>
  <acceptance_criteria>
    - `package.json` `dependencies` contains `"next-intl"` matching `^4.13`
    - `src/i18n/routing.ts` contains the literal strings `defaultLocale: 'es'`, `localePrefix: 'as-needed'`, and `localeDetection: false`
    - `src/i18n/request.ts` imports `hasLocale` from `'next-intl'` and `routing` from `'./routing'`
    - `messages/es.json` and `messages/en.json` are valid JSON with identical top-level key sets (`nav`, `home`, `common`)
    - `next.config.mjs` exports `withPayload(withNextIntl(nextConfig))` and still contains `output: 'standalone'`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "localeDetection: false" src/i18n/routing.ts && grep -q "defaultLocale: 'es'" src/i18n/routing.ts && node -e "const es=require('./messages/es.json'); const en=require('./messages/en.json'); const k1=Object.keys(es).sort().join(','); const k2=Object.keys(en).sort().join(','); if(k1!==k2){process.exit(1)}; console.log('keys match:', k1)" && grep -q "withPayload(withNextIntl(nextConfig))" next.config.mjs && grep -q '"next-intl"' package.json</automated>
  </verify>
  <done>next-intl installed, src/i18n/routing.ts and request.ts scaffolded per locked CONTEXT.md decisions, messages/es.json and en.json exist with matching keys, next.config.mjs wraps nextConfig with both withPayload and withNextIntl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> local install | `next-intl` package legitimacy — mitigated via human-verify checkpoint above (Package Legitimacy Gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-02-SC | Tampering | `npm install next-intl` | mitigate | Blocking `checkpoint:human-verify` (Task 1) confirms publisher/repo/downloads on npmjs.com before install, per Package Legitimacy Gate — package tagged `[ASSUMED]` in RESEARCH.md since slopcheck was unavailable |

</threat_model>

<verification>
Run `npm run dev` and confirm the dev server boots without errors related to `next-intl/plugin` resolution (this plan does not yet wire `middleware.ts`, so no locale-prefixed routes exist yet — 02-03 handles that). Confirm `next.config.mjs` loads without throwing (a broken `createNextIntlPlugin` config path fails the Next.js build immediately).
</verification>

<success_criteria>
- `next-intl@^4.13` installed and present in `package.json`/`package-lock.json`
- `src/i18n/routing.ts` and `src/i18n/request.ts` exist matching CONTEXT.md's locked `defaultLocale: 'es'` / `localePrefix: 'as-needed'` / `localeDetection: false` decisions
- `messages/es.json` and `messages/en.json` exist with matching key structure
- `next.config.mjs` wires `createNextIntlPlugin` around the existing `withPayload` wrapper without regressing `output: 'standalone'`
</success_criteria>

<output>
Create `.planning/phases/02-biling-e-seo/02-01-SUMMARY.md` when done
</output>
