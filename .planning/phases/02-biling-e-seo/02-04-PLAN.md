---
phase: 02-biling-e-seo
plan: 4
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/llms.txt/route.ts
  - src/app/llms-full.txt/route.ts
autonomous: true
requirements: [I18N-03, I18N-04]

must_haves:
  truths:
    - "/sitemap.xml reflects real pages/posts/case-studies/authors/categories via the Local API, not a static file or plugin"
    - "/robots.txt is generated via the native Next.js convention, disallows /admin and /api, and references /sitemap.xml"
    - "/llms.txt and /llms-full.txt are publicly reachable and read from the Llms global, reflecting admin edits without redeploy"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "MetadataRoute.Sitemap merging pages/posts/case-studies/authors/categories with hreflang alternates"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "MetadataRoute.Robots"
      exports: ["default"]
    - path: "src/app/llms.txt/route.ts"
      provides: "GET handler reading Llms.llmsTxt"
      exports: ["GET", "dynamic"]
    - path: "src/app/llms-full.txt/route.ts"
      provides: "GET handler reading Llms.llmsFull"
      exports: ["GET", "dynamic"]
  key_links:
    - from: "src/app/sitemap.ts"
      to: "pages/posts/case-studies/authors/categories collections"
      via: "payload.find({ collection, locale: 'all' })"
      pattern: "locale: 'all'"
    - from: "src/app/llms.txt/route.ts"
      to: "Llms global"
      via: "payload.findGlobal({ slug: 'llms' })"
      pattern: "findGlobal\\(\\{\\s*slug:\\s*'llms'"
---

<objective>
Build the four hand-written SEO discoverability routes — `sitemap.ts`, `robots.ts`, `llms.txt/route.ts`, `llms-full.txt/route.ts` — all native Next.js/Payload Local API patterns, no plugins.

Purpose: I18N-03 and I18N-04 explicitly forbid using an official sitemap plugin (none exists) — the confirmed pattern is Local API queries inside native Next.js `MetadataRoute` file conventions. This plan is independent of the `[locale]`/middleware work in 02-03 (different files, no shared dependency beyond the schema from 02-02), so it runs in parallel within Wave 2.
Output: `src/app/sitemap.ts`, `src/app/robots.ts`, `src/app/llms.txt/route.ts`, `src/app/llms-full.txt/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-biling-e-seo/02-CONTEXT.md
@.planning/phases/02-biling-e-seo/02-RESEARCH.md
@.planning/phases/02-biling-e-seo/02-PATTERNS.md

<interfaces>
Collection slug -> public path prefix map (same map used in 02-03's middleware redirects resolver — keep in sync):
```
{ pages: '', posts: 'blog', 'case-studies': 'case-studies', authors: 'authors', categories: 'categories' }
```

Collections with `versions.drafts` (need `where: { _status: { equals: 'published' } }` filter): `pages`, `posts`, `case-studies` (all confirmed via src/collections/*/index.ts — `versions: { drafts: {...} }` present). `authors` and `categories` have NO `versions` block (confirmed by reading src/collections/Authors/index.ts and Categories/index.ts) — do not apply a `_status` filter to those two, it would error since the field doesn't exist.

Llms global (from 02-02, src/globals/Llms/index.ts): slug `'llms'`, fields `llmsTxt` (textarea), `llmsFull` (textarea).

Reference route handler shape, from /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub/app/llms.txt/route.ts (adapt import — this project has no `getPayloadClient()` helper, use the direct pattern already established in src/app/(payload)/api/[...slug]/route.ts):
```
import { getPayload } from 'payload'
import config from '@payload-config'
export const dynamic = 'force-dynamic'
export async function GET() {
  const payload = await getPayload({ config })
  const llms = await payload.findGlobal({ slug: 'llms' })
  return new Response(llms.llmsTxt ?? '', {
    headers: {
      'content-type': 'text/plain; charset=utf-8',
      'cache-control': 'public, max-age=0, s-maxage=3600, stale-while-revalidate=86400',
    },
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sitemap.ts and robots.ts</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <read_first>
    src/collections/Pages/index.ts
    src/collections/Posts/index.ts
    src/collections/Authors/index.ts
    src/collections/Categories/index.ts
    src/collections/CaseStudies/index.ts
  </read_first>
  <action>
    Create `src/app/sitemap.ts` with `const SITE_URL = process.env.NEXT_PUBLIC_SERVER_URL || 'https://juancarlosangulo.com'`. Export `export default async function sitemap(): Promise<MetadataRoute.Sitemap>` (import `type { MetadataRoute } from 'next'`, `{ getPayload } from 'payload'`, `config from '@payload-config'`).

    Instantiate `const payload = await getPayload({ config })`. For each of the 5 collections, run `payload.find({ collection, limit: 0, locale: 'all', where })` — `where: { _status: { equals: 'published' } }` for `pages`/`posts`/`case-studies` ONLY; no `where` clause for `authors`/`categories` (they have no draft/publish status field).

    For each returned doc across all 5 collections, build one `MetadataRoute.Sitemap` entry using the collection-to-prefix map from `<interfaces>`: `const path = prefix === '' && doc.slug !== 'home' ? doc.slug : prefix === '' ? '' : \`${prefix}/${doc.slug}\`` (home page maps to empty path = site root). Each entry: `{ url: \`${SITE_URL}/${path}\`, lastModified: doc.updatedAt, alternates: { languages: { es: \`${SITE_URL}/${path}\`, en: \`${SITE_URL}/en/${path}\` } } }`. Flatten and return the merged array from all 5 collections.

    Create `src/app/robots.ts`: `import type { MetadataRoute } from 'next'`, export `export default function robots(): MetadataRoute.Robots { return { rules: { userAgent: '*', allow: '/', disallow: ['/admin', '/api'] }, sitemap: \`${SITE_URL}/sitemap.xml\` } }` (define `SITE_URL` the same way as in `sitemap.ts`, this file is standalone — no shared import needed for two lines).
  </action>
  <acceptance_criteria>
    - `src/app/sitemap.ts` default export queries all 5 collections: `pages`, `posts`, `case-studies`, `authors`, `categories`
    - `_status: { equals: 'published' }` filter applied only to `pages`/`posts`/`case-studies` queries, not `authors`/`categories`
    - Each sitemap entry includes `alternates.languages` with both `es` and `en` keys
    - `src/app/robots.ts` disallows `/admin` and `/api`, references `${SITE_URL}/sitemap.xml`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "collection: '" src/app/sitemap.ts | grep -qE "^[5-9]|^[1-9][0-9]" && grep -q "alternates" src/app/sitemap.ts && grep -q "disallow: \['/admin', '/api'\]" src/app/robots.ts</automated>
  </verify>
  <done>sitemap.ts merges all 5 content collections with hreflang alternates; robots.ts disallows admin/api and points to sitemap.xml.</done>
</task>

<task type="auto">
  <name>Task 2: llms.txt and llms-full.txt route handlers</name>
  <files>src/app/llms.txt/route.ts, src/app/llms-full.txt/route.ts</files>
  <read_first>
    src/globals/Llms/index.ts
    src/app/(payload)/api/[...slug]/route.ts
  </read_first>
  <action>
    Create `src/app/llms.txt/route.ts` copying the reference shape from `<interfaces>` verbatim: `export const dynamic = 'force-dynamic'`, `export async function GET()` using `getPayload({ config })` + `payload.findGlobal({ slug: 'llms' })`, returning `new Response(llms.llmsTxt ?? '', { headers: { 'content-type': 'text/plain; charset=utf-8', 'cache-control': 'public, max-age=0, s-maxage=3600, stale-while-revalidate=86400' } })`.

    Create `src/app/llms-full.txt/route.ts` with the identical shape, reading `llms.llmsFull` instead of `llms.llmsTxt`.
  </action>
  <acceptance_criteria>
    - Both route files export `dynamic = 'force-dynamic'` and an async `GET` function
    - Both call `payload.findGlobal({ slug: 'llms' })`
    - `llms.txt/route.ts` returns `llms.llmsTxt`; `llms-full.txt/route.ts` returns `llms.llmsFull`
    - Both set `content-type: text/plain; charset=utf-8`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "dynamic = 'force-dynamic'" src/app/llms.txt/route.ts && grep -q "llms.llmsTxt" src/app/llms.txt/route.ts && grep -q "dynamic = 'force-dynamic'" src/app/llms-full.txt/route.ts && grep -q "llms.llmsFull" src/app/llms-full.txt/route.ts</automated>
  </verify>
  <done>Both routes are publicly reachable, force-dynamic (reflect admin edits without redeploy), reading the correct Llms global field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public internet -> `/sitemap.xml`, `/robots.txt`, `/llms.txt`, `/llms-full.txt` | All four routes are intentionally public, read-only, no auth required (matches `read: () => true` on the underlying collections/global) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-02-06 | Information Disclosure | `sitemap.ts` querying unpublished docs | mitigate | `_status: { equals: 'published' }` filter applied to `pages`/`posts`/`case-studies` — draft/unpublished content is never listed in the public sitemap |
| T-02-07 | Denial of Service | `llms.txt`/`llms-full.txt` `force-dynamic` (no caching) | accept | Low-traffic personal portfolio site; `cache-control: s-maxage=3600, stale-while-revalidate=86400` headers still allow CDN/browser caching even though the route itself is dynamic — acceptable risk for this scale |

</threat_model>

<verification>
After `npm run dev`: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/sitemap.xml` -> `200`; `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/robots.txt` -> `200`; `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/llms.txt` -> `200`; `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/llms-full.txt` -> `200`. Full content-reflects-real-data verification (with seeded docs) happens in 02-05.
</verification>

<success_criteria>
- `/sitemap.xml` queries all 5 collections via Local API with hreflang alternates, no plugin
- `/robots.txt` uses the native Next.js convention, disallows admin/api
- `/llms.txt` and `/llms-full.txt` are publicly reachable and read from the `Llms` global dynamically
</success_criteria>

<output>
Create `.planning/phases/02-biling-e-seo/02-04-SUMMARY.md` when done
</output>
</content>
