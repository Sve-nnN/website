---
phase: 07-design-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
  - scripts/check-dark-contrast.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "A component can apply shadow-sm/md/lg/focus Tailwind classes and get a real box-shadow backed by CSS custom properties"
    - "A component can apply duration-fast/base/slow and ease-out/ease-standard Tailwind classes and get real transition timing backed by CSS custom properties"
    - "A user with prefers-reduced-motion: reduce sees near-zero transition/animation duration on any element, existing or future, without per-component opt-in"
    - "The .dark class renders an ember/navy branded palette (not generic shadcn gray oklch values) when applied to the document"
    - "No dark-mode toggle UI exists anywhere in the codebase (grep-verifiable absence)"
    - "Every rebranded .dark text/background pair meets WCAG AA contrast (4.5:1 for text, 3:1 for large text/non-text UI), verified by an automated script before the phase closes"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Shadow/motion primitive + semantic tokens, prefers-reduced-motion global rule, rebranded .dark block"
      contains: "--shadow-md"
    - path: "tailwind.config.ts"
      provides: "boxShadow/transitionDuration/transitionTimingFunction theme.extend mappings to the new CSS vars"
      contains: "boxShadow"
    - path: "scripts/check-dark-contrast.ts"
      provides: "Automated WCAG AA contrast verification for every rebranded .dark token pair"
      exports: ["main"]
  key_links:
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "CSS var reference"
      pattern: "var\\(--shadow-(sm|md|lg|focus)\\)"
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "theme.extend.boxShadow/transitionDuration/transitionTimingFunction"
      pattern: "boxShadow"
    - from: "scripts/check-dark-contrast.ts"
      to: "src/app/globals.css"
      via: "hardcoded hex/rgba token values mirroring the .dark block"
      pattern: "contrastRatio"
---

<objective>
Add the CSS-only elevation (shadow) and motion (timing) token layer that does not exist yet in this codebase, wire both into `tailwind.config.ts` as reusable Tailwind utilities, add a global `prefers-reduced-motion` safety net, and rebrand the `.dark` block from generic shadcn gray to an ember/navy palette systematically derived from the already-locked light-mode values in `05-UI-SPEC.md` — with WCAG AA contrast verified by an automated script before this phase is considered done.

Purpose: every subsequent v1.1 phase (Phase 8 shadcn primitives, Phase 9 hero/KPI, Phase 10 cards) composes visual polish on top of this token layer. This phase is the only one in the milestone that introduces genuinely new token categories; everything else refines existing usage.
Output: extended `globals.css` (elevation primitives, motion primitives, reduced-motion rule, rebranded `.dark` block), extended `tailwind.config.ts` (`boxShadow`, `transitionDuration`, `transitionTimingFunction`), and a committed contrast-verification script with a passing run recorded in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-token-foundation/07-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@src/app/globals.css
@tailwind.config.ts

<interfaces>
Current globals.css :root / .dark structure — exact anchor points for edits.

`src/app/globals.css` today has two `@layer base` blocks: the first contains `:root { ... }` and `.dark { ... }` (shadcn default color variables, `.dark` never rebranded); the second applies `border-border` / `bg-background text-foreground` to `*`/`body`. Add new primitives/semantics inside the existing `:root { ... }` block (do not create a third `@layer base` block). Add the `prefers-reduced-motion` rule as its own top-level `@media` block, outside `@layer base`, placed after the two existing `@layer base` blocks. Replace the color-bearing lines inside the existing `.dark { ... }` block in place — keep the block, keep every existing variable name, only change values (and add `--shadow-color` only if you choose to override it there; not required).

`tailwind.config.ts` today has a single `theme.extend` object with `fontFamily`, `fontSize`, `borderRadius`, `colors` keys. Add `boxShadow`, `transitionDuration`, `transitionTimingFunction` as sibling keys inside the same `theme.extend` object, following the existing `colors` pattern of `key: 'var(--css-var-name)'`.

Locked light-mode values this dark palette must derive from (from `globals.css` `:root`, confirmed against `05-UI-SPEC.md`): `--background: #FAFAF7` (warm off-white); `--foreground: #12141C` (near-black navy ink); `--primary: #FF5B1F` (ember); `--primary-foreground: #FAFAF7`; `--secondary: #12141C` (navy, same hue as foreground, used for dark surfaces/header/footer in light mode); `--secondary-foreground: #FAFAF7`; `--destructive: #DC2626`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elevation + motion token layer, mapped to Tailwind, with reduced-motion safety net</name>
  <files>src/app/globals.css, tailwind.config.ts</files>
  <action>
Per D-01/UI-01: in src/app/globals.css, inside the existing :root block, append these new CSS custom properties after the existing --sidebar-ring line, before the closing brace.

Elevation primitives and semantics: --shadow-color set to the HSL channel triplet 220 20% 10% (navy-hued to match --foreground's hue family, consumed via hsl(var(--shadow-color) / alpha)); --shadow-sm as 0 1px 2px 0 hsl(var(--shadow-color) / 0.05); --shadow-md as 0 4px 6px -1px hsl(var(--shadow-color) / 0.1), 0 2px 4px -2px hsl(var(--shadow-color) / 0.1); --shadow-lg as 0 10px 15px -3px hsl(var(--shadow-color) / 0.1), 0 4px 6px -4px hsl(var(--shadow-color) / 0.1); --shadow-focus as 0 0 0 3px rgba(255, 91, 31, 0.45) (ember-tinted focus ring, using the literal --primary hex directly since --primary is not stored as an HSL triplet).

Motion primitives: --motion-fast: 150ms; --motion-base: 250ms; --motion-slow: 400ms; --ease-out: cubic-bezier(0.22, 1, 0.36, 1); --ease-standard: cubic-bezier(0.4, 0, 0.2, 1).

Per D-02/UI-02: after both existing @layer base blocks (i.e. at the end of the file, outside any @layer), add a new top-level prefers-reduced-motion media rule targeting the universal selector group (the element itself plus ::before and ::after) and setting animation-duration to 0.01ms important, animation-iteration-count to 1 important, transition-duration to 0.01ms important, and scroll-behavior to auto important inside it. This must stay a global, unscoped selector (not gated behind a class) so it neutralizes any existing or future CSS transition/animation, per the CONTEXT decision.

In tailwind.config.ts, inside the existing theme.extend object, add three new sibling keys after colors and before the closing brace of extend: a boxShadow object mapping sm/md/lg/focus to 'var(--shadow-sm)'/'var(--shadow-md)'/'var(--shadow-lg)'/'var(--shadow-focus)'; a transitionDuration object mapping fast/base/slow to 'var(--motion-fast)'/'var(--motion-base)'/'var(--motion-slow)'; a transitionTimingFunction object mapping out/standard to 'var(--ease-out)'/'var(--ease-standard)'. Follow the existing colors key's formatting style (single-quoted string values referencing CSS vars).

Do not touch darkMode, content, plugins, or any existing theme.extend key (fontFamily, fontSize, borderRadius, colors). No new npm dependency is introduced by this task — tailwindcss-animate stays as-is.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && npx tsc --noEmit -p tsconfig.json && grep -q shadow-md src/app/globals.css && grep -q motion-base src/app/globals.css && grep -q prefers-reduced-motion src/app/globals.css && grep -q boxShadow tailwind.config.ts && grep -q transitionDuration tailwind.config.ts && grep -q transitionTimingFunction tailwind.config.ts && echo TASK1_PASS</automated>
  </verify>
  <done>globals.css exposes --shadow-sm/md/lg/focus and --motion-fast/base/slow/--ease-out/--ease-standard inside :root; a global unscoped prefers-reduced-motion media rule exists outside @layer base; tailwind.config.ts theme.extend maps all of them to boxShadow/transitionDuration/transitionTimingFunction; tsc --noEmit reports no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand .dark block to ember/navy palette</name>
  <files>src/app/globals.css</files>
  <action>
Per D-03/UI-03: replace every color-bearing CSS custom property value inside the existing .dark block in src/app/globals.css (keep every existing variable name and the block's position — this is a value swap, not a restructure). Derive every value from the light-mode palette's hue family (navy #12141C, off-white #FAFAF7, ember #FF5B1F) — do not invent an unrelated hue.

Use exactly these values: --background: #12141C (promote the light-mode "dark surface" navy to the dark-mode base surface); --foreground: #FAFAF7 (reuse the light-mode off-white); --card: #1B1E29 (navy lightened one step for surface elevation above background); --card-foreground: #FAFAF7; --popover: #1B1E29; --popover-foreground: #FAFAF7; --primary: #FF7A45 (ember lightened for AA legibility on a dark navy background); --primary-foreground: #12141C; --secondary: #23283A (a second, lighter navy surface tier, distinct from --card); --secondary-foreground: #FAFAF7; --muted: #1F2230; --muted-foreground: #A8ACBB (desaturated warm gray-navy, tuned for 4.5:1 against --muted/--background); --accent: #23283A; --accent-foreground: #FAFAF7; --destructive: #F87171 (softened red for dark-surface legibility, same hue family as the light-mode #DC2626); --border: rgba(250, 250, 247, 0.12); --input: rgba(250, 250, 247, 0.16); --ring: rgba(255, 122, 69, 0.6) (ember-tinted, ties focus ring to brand); --chart-1: #FF7A45; --chart-2: #23283A; --chart-3: #3A4159; --chart-4: #5A6488; --chart-5: #8890AC; --sidebar: #1B1E29; --sidebar-foreground: #FAFAF7; --sidebar-primary: #FF7A45; --sidebar-primary-foreground: #12141C; --sidebar-accent: #23283A; --sidebar-accent-foreground: #FAFAF7; --sidebar-border: rgba(250, 250, 247, 0.12); --sidebar-ring: rgba(255, 122, 69, 0.6).

Leave --radius alone (not defined in .dark today, no change needed). Do not add a toggle component, next-themes provider, or any UI control — this task only changes the CSS variable values consumed when the .dark class is present on the document (mechanism already exists via darkMode: ['class'] in tailwind.config.ts, untouched).

After editing, confirm no dark-mode toggle exists anywhere in src/ (none should pre-exist and none should be added by this task) by searching for next-themes, useTheme, and ThemeToggle — all three must return zero matches.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && grep -q "background: #12141C" src/app/globals.css && grep -q "primary: #FF7A45" src/app/globals.css && ! grep -rq "next-themes\|useTheme\|ThemeToggle" src/ && OKLCH_COUNT=$(awk "/\.dark {/,/^  }/" src/app/globals.css | grep -c "oklch(0") && test "$OKLCH_COUNT" -eq 0 && echo TASK2_PASS</automated>
  </verify>
  <done>.dark block in globals.css contains zero generic shadcn oklch(0...) gray values; every variable now resolves to an ember/navy hex or rgba value derived from the light-mode hue family; no dark-mode toggle component or next-themes/useTheme usage exists anywhere in src/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Automated WCAG AA contrast verification for the new .dark tokens</name>
  <files>scripts/check-dark-contrast.ts</files>
  <behavior>
    - Given the .dark token pairs foreground/background, muted-foreground/background, card-foreground/card, primary-foreground/primary, secondary-foreground/secondary, accent-foreground/accent, and destructive/background, computing WCAG relative luminance and contrast ratio for each pair returns a ratio >= 4.5 (text pairs).
    - Given the .dark token pair primary/background (used for links/icons/UI elements, not full-size text blocks), the computed contrast ratio returns >= 3.0 (WCAG 1.4.11 non-text / large-text threshold).
    - Given a hex color like "#12141C" and an rgba color like "rgba(250, 250, 247, 0.12)", the color parser returns correct sRGB channel values for both formats (rgba composited against the paired background per WCAG's relative-luminance guidance for translucent foreground layers).
    - If any pair fails its threshold, the script prints which pair failed, the computed ratio, and the required threshold, then exits with a non-zero status code.
    - If every pair passes, the script prints a pass table and exits 0.
  </behavior>
  <action>
Create scripts/check-dark-contrast.ts as a standalone Node/TypeScript script (no new npm dependency — use only built-in logic), runnable via `node_modules/.bin/tsx scripts/check-dark-contrast.ts`, following the existing script style in this repo (see scripts/verify-db-pool.ts for the header-comment convention).

Implement: a hex-to-RGB parser and an rgba(...)-string parser that composites the translucent color over its paired background color to get an effective opaque RGB (needed for --border/--input/--ring, which are rgba); a WCAG relative-luminance function (sRGB channels normalized to 0-1, gamma-corrected per the standard piecewise formula, weighted 0.2126/0.7152/0.0722); a contrast-ratio function ((L1 + 0.05) / (L2 + 0.05) with L1 the lighter luminance); a hardcoded array of token pairs mirroring exactly the .dark values written in Task 2 (do not read globals.css at runtime — hardcode the same literal values so the script is self-contained and fast, but add a code comment noting these must be kept in sync with globals.css's .dark block).

Token pairs and thresholds to check: background/foreground pair (both directions is the same ratio, check once) at 4.5; muted-foreground on background at 4.5; muted-foreground on muted at 4.5; card-foreground on card at 4.5; primary-foreground on primary at 4.5; secondary-foreground on secondary at 4.5; accent-foreground on accent at 4.5; destructive on background at 4.5 (destructive text/error-message use case); primary on background at 3.0 (link/icon/UI-element use case); border on background at 3.0 (WCAG 1.4.11 non-text UI-component contrast, composite the rgba border over background first).

Print a formatted table (pair name, computed ratio to 2 decimals, required threshold, PASS/FAIL) to stdout. If any row is FAIL, process.exit(1) after printing the table; otherwise process.exit(0).

Run the script and read its output. If any pair fails, adjust only the specific failing CSS variable's lightness in src/app/globals.css's .dark block (task 2's file) — keep the same hue family, do not redesign passing tokens — then re-run the script until all pairs pass. Record the final passing table output in the plan's SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && node_modules/.bin/tsx scripts/check-dark-contrast.ts</automated>
  </verify>
  <done>scripts/check-dark-contrast.ts exists, computes WCAG contrast ratios for every rebranded .dark token pair listed above using only built-in logic, and running it exits 0 with every pair at or above its required threshold (4.5 for text pairs, 3.0 for primary/background and border/background).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only static CSS custom properties, a Tailwind config object, and a standalone build-time verification script. No user input, no network call, no new runtime dependency, no trust boundary is created or crossed.

| Boundary | Description |
|----------|--------------|
| None applicable | Pure CSS token authoring + a Node script that runs at dev/CI time only, never in a request path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-07-01 | Tampering | scripts/check-dark-contrast.ts | accept | Script has zero external dependencies (no npm install), runs only locally/in CI, never ships to the production bundle or runtime — no supply-chain surface introduced. |
| T-07-02 | Information Disclosure | src/app/globals.css / tailwind.config.ts | accept | Both files contain only public, non-secret design values (colors, durations); no PII, no credentials, no environment-specific data. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no new type errors after all three tasks.
2. `grep` confirms --shadow-sm/md/lg/focus, --motion-fast/base/slow, --ease-out/--ease-standard exist in globals.css :root, and boxShadow/transitionDuration/transitionTimingFunction exist in tailwind.config.ts theme.extend.
3. `grep` confirms a global, unscoped `@media (prefers-reduced-motion: reduce)` rule exists in globals.css outside any @layer block.
4. `grep` confirms zero `oklch(0` occurrences remain inside the .dark block, and confirms zero next-themes/useTheme/ThemeToggle references exist anywhere in src/.
5. `node_modules/.bin/tsx scripts/check-dark-contrast.ts` exits 0, with every checked .dark token pair at or above its WCAG AA threshold.
</verification>

<success_criteria>
- ROADMAP Phase 7 success criterion 1: globals.css/tailwind.config.ts expose --shadow-sm/md/lg/focus and --motion-fast/base/slow/--ease-* mapped to boxShadow/transitionDuration/transitionTimingFunction Tailwind utilities, usable from any component — DONE via Task 1.
- ROADMAP Phase 7 success criterion 2: a global prefers-reduced-motion media rule neutralizes any existing or future CSS transition — DONE via Task 1.
- ROADMAP Phase 7 success criterion 3: the .dark block uses an ember/navy palette derived from 05-UI-SPEC.md, not generic shadcn gray, with no toggle UI exposed — DONE via Task 2.
- ROADMAP Phase 7 success criterion 4: WCAG contrast on the new dark token set is verified before the phase closes, not deferred — DONE via Task 3, with a passing automated script run recorded in SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/07-design-token-foundation/07-01-SUMMARY.md` when done, including the final passing contrast table from Task 3.
</output>
