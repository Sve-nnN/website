---
phase: 26-ui-ux-polish-pass-low-risk-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SiteHeader.tsx
  - src/components/SiteHeaderChrome.tsx
autonomous: true
requirements: [UIPOL-02]

must_haves:
  truths:
    - "SiteHeader shows a visually distinguishable state when the user scrolls past the top of the page (background opacity/blur/shadow change)"
    - "SiteHeader highlights the nav item matching the current route with a persistent underline + color, on both desktop nav and mobile Sheet nav"
    - "SiteHeader's Local API data-fetching (findGlobal header, normalizeServiceHref locale fix) is unchanged -- only rendering/interactivity is touched"
  artifacts:
    - path: "src/components/SiteHeaderChrome.tsx"
      provides: "Client component owning scroll-state + usePathname active-route logic (UIPOL-02)"
      contains: "usePathname"
    - path: "src/components/SiteHeader.tsx"
      provides: "Server component fetching header global, delegating chrome rendering to SiteHeaderChrome"
      contains: "SiteHeaderChrome"
  key_links:
    - from: "src/components/SiteHeaderChrome.tsx"
      to: "next/navigation usePathname"
      via: "client-side active-route matching against normalized navItems"
      pattern: "usePathname"
    - from: "src/components/SiteHeader.tsx"
      to: "src/components/SiteHeaderChrome.tsx"
      via: "server component passes resolved navItems/ctaButton/logo/locale as props"
      pattern: "SiteHeaderChrome"
---

<objective>
Give SiteHeader a scroll-state treatment (background opacity/blur/shadow shift past 8px scroll) and a persistent active-route indicator (underline + color on the nav item matching the current URL, in both desktop and mobile nav), both driven by client-side state extracted into a new SiteHeaderChrome client subcomponent -- since SiteHeader itself is an async Server Component that fetches the header global via the Local API.

Purpose: Close UIPOL-02 per 26-UI-SPEC.md Component 2 ("SiteHeader -- Scroll State + Active Route"), reusing only existing Phase 7 motion tokens (--duration-base, --ease-standard) and the existing hover-underline pattern, promoted to a persistent active state.
Output: New src/components/SiteHeaderChrome.tsx (client component), src/components/SiteHeader.tsx updated to delegate interactive chrome to it. Zero new npm dependency, zero global state, zero schema change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md
@.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-CONTEXT.md

CRITICAL CONSTRAINT (per CLAUDE.md + orchestrator instructions): this plan touches ZERO database schema, ZERO Payload collection/global config.ts files, ZERO migrations. SiteHeader's existing findGlobal('header', ...) Local API call and the existing normalizeServiceHref locale-fix logic must remain byte-identical -- only the rendering/interactivity layer is touched. If executing this plan reveals a genuine need for a schema/config change, STOP and flag it instead of proceeding.
</context>

<interfaces>
Current state of src/components/SiteHeader.tsx (full file, read directly from repo):

  import Image from 'next/image'
  import Link from 'next/link'
  import { getPayload } from 'payload'
  import config from '@/payload.config'
  import { Container } from '@/components/Container'
  import { CMSLink } from '@/components/CMSLink'
  import { Button } from '@/components/ui/button'
  import { NavigationMenu, NavigationMenuItem, NavigationMenuLink, NavigationMenuList } from '@/components/ui/navigation-menu'
  import { Sheet, SheetContent, SheetTitle, SheetTrigger } from '@/components/ui/sheet'
  import { Separator } from '@/components/ui/separator'
  import { Menu } from 'lucide-react'
  import { LocaleSwitcher } from '@/components/LocaleSwitcher'
  import { normalizeServiceHref } from '@/lib/service-slugs'

  export async function SiteHeader({ locale }: { locale: string }) {
    const payload = await getPayload({ config })
    const header = await payload.findGlobal({ slug: 'header', depth: 1, locale: locale as 'en' | 'es' })
    const logo = typeof header.logo === 'object' ? header.logo : null

    // navItems locale-fix (Phase 25 live bug fix) -- KEEP UNCHANGED
    const navItems = (header.navItems ?? []).map((item) => ({
      ...item,
      link: item.link?.url
        ? { ...item.link, url: normalizeServiceHref(item.link.url, locale as 'es' | 'en') }
        : item.link,
    }))

    return (
      <header className="sticky top-0 z-50 bg-secondary text-secondary-foreground shadow-md border-b border-border/20 transition-shadow duration-base ease-standard">
        <Container className="flex items-center justify-between py-4">
          <Link href="/" className="flex items-center gap-2">
            {logo?.url ? (
              <Image src={logo.url} alt={logo.alt ?? 'Logo'} width={40} height={40} />
            ) : (
              <span className="font-heading text-heading">Juan Carlos Angulo</span>
            )}
          </Link>
          <nav className="hidden md:flex items-center gap-8">
            <NavigationMenu>
              <NavigationMenuList>
                {navItems.map((item, i) => (
                  <NavigationMenuItem key={item.id ?? i}>
                    <NavigationMenuLink asChild>
                      <CMSLink {...item.link} className="relative pb-1 border-b-2 border-transparent hover:border-primary hover:text-primary focus-visible:border-primary focus-visible:text-primary transition-colors duration-fast ease-out text-body" />
                    </NavigationMenuLink>
                  </NavigationMenuItem>
                ))}
              </NavigationMenuList>
            </NavigationMenu>
            <LocaleSwitcher currentLocale={locale} />
            {header.ctaButton?.label && (
              <Button asChild><Link href={header.ctaButton.href ?? '/contact'}>{header.ctaButton.label}</Link></Button>
            )}
          </nav>
          <Sheet>
            <SheetTrigger asChild className="md:hidden">
              <Button variant="outline" size="icon" aria-label="Menu" className="size-10"><Menu className="size-5" /></Button>
            </SheetTrigger>
            <SheetContent>
              <SheetTitle className="sr-only">Navigation menu</SheetTitle>
              <nav className="flex flex-col mt-8">
                <div className="flex flex-col gap-1">
                  {navItems.map((item, i) => (
                    <CMSLink key={item.id ?? i} {...item.link} className="font-heading rounded-md px-3 py-3 min-h-11 flex items-center border-b-2 border-transparent hover:border-primary hover:bg-muted focus-visible:border-primary focus-visible:bg-muted transition-colors duration-fast ease-out text-body" />
                  ))}
                </div>
                <Separator className="opacity-30 my-6" />
                <div className="flex flex-col gap-4 px-3">
                  <LocaleSwitcher currentLocale={locale} />
                  {header.ctaButton?.label && (
                    <Button asChild><Link href={header.ctaButton.href ?? '/contact'}>{header.ctaButton.label}</Link></Button>
                  )}
                </div>
              </nav>
            </SheetContent>
          </Sheet>
        </Container>
      </header>
    )
  }

Precedent for a client subcomponent reading usePathname() next to a server-fetched parent -- src/components/LocaleSwitcher.tsx (full file, 'use client', imports usePathname from 'next/navigation', not next-intl):

  'use client'
  import Link from 'next/link'
  import { usePathname } from 'next/navigation'
  import { routing } from '@/i18n/routing'
  import { normalizeServiceHref } from '@/lib/service-slugs'

  export function LocaleSwitcher({ currentLocale }: { currentLocale: string }) {
    const pathname = usePathname()
    // ...locale-swap logic, unrelated to this plan...
  }

CMSLink type shape (from src/components/CMSLink.tsx) accepts a link object with url/label/newTab-style fields spread as props -- item.link is spread directly as {...item.link} in both nav renders above; SiteHeaderChrome must accept the same already-normalized navItems array (post normalizeServiceHref) as a prop, not re-derive it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SiteHeaderChrome client component with scroll-state + active-route logic</name>
  <files>src/components/SiteHeaderChrome.tsx</files>
  <read_first>
    @src/components/SiteHeader.tsx
    @src/components/LocaleSwitcher.tsx
    @.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md (Component 2 "SiteHeader -- Scroll State + Active Route", lines 156-186 -- exact class contracts, threshold, match rule)
  </read_first>
  <action>
    Create src/components/SiteHeaderChrome.tsx as a new 'use client' component. It receives as props everything SiteHeader.tsx currently renders inline inside the header/nav/Sheet markup: navItems (already-normalized array, same shape as SiteHeader's local navItems const), ctaButton (header.ctaButton), logo (resolved logo object or null), and locale (string). It owns two pieces of client state: (1) a scrolled boolean from a window scroll listener (useEffect + addEventListener('scroll', ...) with passive: true, cleanup on unmount) toggling at threshold scrollY > 8, and (2) the active route derived from usePathname() (import from 'next/navigation', same as LocaleSwitcher.tsx) matched via exact-pathname comparison against each navItem's link.url after stripping locale prefix and trailing slash (per UI-SPEC's match rule -- exact match only, no prefix match).

    Move the entire header JSX (the header element, its Container, desktop nav, LocaleSwitcher usage, CTA button, Sheet mobile nav) from SiteHeader.tsx into this new component, applying the UI-SPEC's exact class changes:
    - header className: idle state keeps "bg-secondary shadow-md", scrolled state (scrolled === true) becomes "bg-secondary/95 backdrop-blur-sm shadow-lg" -- conditionally applied via template string or clsx-style ternary, both states always keep "sticky top-0 z-50 text-secondary-foreground border-b border-border/20 transition-[background-color,box-shadow] duration-base ease-standard" (transition property list widened from the current transition-shadow to also animate background-color, per UI-SPEC 2a).
    - desktop CMSLink className: default "relative pb-1 border-b-2 border-transparent hover:border-primary hover:text-primary focus-visible:border-primary focus-visible:text-primary transition-colors duration-fast ease-out text-body", when the item is the active route append "border-primary text-primary" (same classes hover already applies, now persistent) and add aria-current="page" -- inactive items get no aria-current attribute at all (not aria-current="false").
    - mobile Sheet CMSLink className: same active-state augmentation applied to the existing mobile link className string ("font-heading rounded-md px-3 py-3 min-h-11 flex items-center border-b-2 border-transparent hover:border-primary hover:bg-muted focus-visible:border-primary focus-visible:bg-muted transition-colors duration-fast ease-out text-body"), active route gets "border-primary text-primary" appended plus aria-current="page".

    Keep LocaleSwitcher, Button/CTA, Sheet/SheetContent/SheetTrigger/SheetTitle, Separator, Menu icon, and the logo Image/fallback span exactly as they render today -- only the header/nav wrapper classes and the two CMSLink usages change.

    This is a UI-only extraction -- SiteHeaderChrome must not call getPayload/findGlobal or import '@/payload.config'; it is a pure presentational client component receiving already-resolved data as props.
  </action>
  <verify>
    <automated>grep -c "'use client'" src/components/SiteHeaderChrome.tsx && grep -c "usePathname" src/components/SiteHeaderChrome.tsx && grep -c "aria-current" src/components/SiteHeaderChrome.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SiteHeaderChrome.tsx starts with 'use client' and imports usePathname from next/navigation
    - Contains both a scroll-state boolean (window scroll listener with passive:true, threshold 8) and pathname-based active-route matching against navItems
    - Both desktop NavigationMenuLink/CMSLink and mobile Sheet CMSLink render aria-current="page" conditionally on the active item
    - File does not import getPayload or @/payload.config (pure presentational component, no server-only Local API calls)
  </acceptance_criteria>
  <done>SiteHeaderChrome.tsx exists as a client component implementing scroll-state background/shadow transition and persistent active-route underline+color+aria-current on both desktop and mobile nav.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SiteHeader.tsx to delegate to SiteHeaderChrome + verify build</name>
  <files>src/components/SiteHeader.tsx</files>
  <read_first>
    @src/components/SiteHeader.tsx
  </read_first>
  <action>
    Update SiteHeader.tsx (stays an async Server Component) to keep its existing getPayload/findGlobal('header', ...) call and the existing navItems normalizeServiceHref-fix mapping completely unchanged. Replace the JSX it currently returns (the full header/nav/Sheet markup) with a single render of the new SiteHeaderChrome, passing navItems, ctaButton={header.ctaButton}, logo={logo}, and locale={locale} as props. Remove now-unused imports from SiteHeader.tsx (Image/Link/Container/CMSLink/Button/NavigationMenu*/Sheet*/Separator/Menu/LocaleSwitcher) that moved into SiteHeaderChrome -- keep only getPayload, config, normalizeServiceHref, and the new SiteHeaderChrome import in SiteHeader.tsx.

    After wiring, run a full TypeScript typecheck to confirm no prop-shape mismatch between SiteHeader's resolved data and SiteHeaderChrome's prop types, and confirm the app still builds (this phase's constraint is zero schema/DB change, but a full Next.js production build is the correct verification that no import cycle or client/server boundary violation was introduced by the extraction).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c "SiteHeaderChrome" src/components/SiteHeader.tsx && grep -c "getPayload" src/components/SiteHeader.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SiteHeader.tsx still calls payload.findGlobal({ slug: 'header', ... }) and applies normalizeServiceHref to navItems, unchanged
    - src/components/SiteHeader.tsx renders SiteHeaderChrome with navItems/ctaButton/logo/locale props
    - npx tsc --noEmit reports zero errors
    - npm run build completes successfully (or npx next build, confirming no client/server boundary violation)
  </acceptance_criteria>
  <done>SiteHeader.tsx is a thin server-side data-fetching wrapper delegating all interactive chrome rendering to SiteHeaderChrome; project builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server (SiteHeader, Local API) -> Client (SiteHeaderChrome, browser) | header global data (navItems URLs/labels, ctaButton, logo) crosses from a trusted server fetch into a client component -- already Payload-editorial content, not user input |
| Browser window.scrollY / usePathname | Read-only browser APIs, no user-controlled input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Denial of Service | SiteHeaderChrome scroll listener | mitigate | addEventListener('scroll', handler, { passive: true }) plus cleanup in useEffect return -- prevents listener leaks across client-side navigations and avoids blocking the scroll thread |
| T-26-04 | Information Disclosure | Active-route matching via usePathname | accept | Pathname is already public browser-visible routing state, no new data exposed by comparing it to editorial navItems URLs |
</threat_model>

<verification>
Run `npx tsc --noEmit -p tsconfig.json` -- zero errors.
Run `npm run build` (or `npx next build`) -- completes without client/server boundary errors.
Run `git diff --stat` -- confirm only SiteHeader.tsx and the new SiteHeaderChrome.tsx changed, zero config.ts/payload-types.ts/migration files touched.
Start `npm run dev`, visit `/` and scroll past 8px on both `/` (es) and `/en` -- header background should shift to the 95%-opacity/blur/shadow-lg state. Navigate to a top-level nav route (e.g. Servicios/Services) and confirm that nav item shows the persistent underline+ember-color+aria-current, on both desktop width and mobile Sheet (open the hamburger menu at ~375px).
</verification>

<success_criteria>
- SiteHeader shows a scroll-triggered background/blur/shadow state change past an 8px threshold (UIPOL-02)
- The nav item matching the current route shows a persistent underline + ember color + aria-current="page", on both desktop and mobile Sheet nav (UIPOL-02)
- SiteHeader's existing Local API fetch and locale-fix logic are unchanged
- Zero config.ts/schema/migration changes
</success_criteria>

<output>
Create `.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-02-SUMMARY.md` when done
</output>
