---
phase: 26-ui-ux-polish-pass-low-risk-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/CallToAction/Component.tsx
  - src/blocks/FAQ/Component.tsx
  - src/blocks/ClientLogosBlock/Component.tsx
  - src/blocks/TestimonialsCarousel/Component.tsx
autonomous: true
requirements: [UIPOL-01, UIPOL-04, UIPOL-05, UIPOL-06]

must_haves:
  truths:
    - "CallToAction block renders inside the same Container wrapper as every other block, with zero vw-wide edge bleed, in both locales"
    - "FAQ items render as individually-bordered, elevated cards (not a flat divide-y list), keeping native details/summary keyboard/screen-reader behavior"
    - "Client logos render at a normalized visual height regardless of native aspect ratio (no more fixed 120x48 distortion-box)"
    - "TestimonialsCarousel shows a right-edge fade affordance hinting more content is scrollable"
  artifacts:
    - path: "src/blocks/CallToAction/Component.tsx"
      provides: "Container-wrapped CTA card (UIPOL-01)"
      contains: "Container"
    - path: "src/blocks/FAQ/Component.tsx"
      provides: "Bordered/elevated FAQ item cards (UIPOL-04)"
      contains: "rounded-lg border border-border bg-card"
    - path: "src/blocks/ClientLogosBlock/Component.tsx"
      provides: "Height-normalized logo cells (UIPOL-05)"
      contains: "h-10 md:h-12"
    - path: "src/blocks/TestimonialsCarousel/Component.tsx"
      provides: "Edge-fade scroll affordance (UIPOL-06)"
      contains: "bg-gradient-to-l"
  key_links:
    - from: "src/blocks/CallToAction/Component.tsx"
      to: "src/components/Container.tsx"
      via: "single outer Container import, no nested double-Container"
      pattern: "Container"
---

<objective>
Fix the CTA strip's missing outer Container (the one concrete visual complaint Juan named directly), and give FAQ, ClientLogosBlock, and TestimonialsCarousel a polish pass using only existing design tokens -- no new npm dependency, no schema change, no data-fetching change.

Purpose: Close UIPOL-01/04/05/06 -- the four lowest-risk, purely-visual component fixes in the v1.6 milestone, per 26-UI-SPEC.md Components 1, 3, 4, 5 (exact code contracts already written there).
Output: 4 updated block Component.tsx files, zero config.ts/schema changes, zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md
@.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-CONTEXT.md

CRITICAL CONSTRAINT (per CLAUDE.md + orchestrator instructions): this plan touches ZERO database schema, ZERO Payload collection/block config.ts files, ZERO migrations. Every task below is a pure JSX/Tailwind edit to an existing Component.tsx render function. If executing either task reveals a genuine need for a schema/config change, STOP and flag it instead of proceeding -- that would mean the UI-SPEC's "zero schema" assumption was wrong.
</context>

<interfaces>
Current state of all 4 files, read directly from the repo -- executor should edit these in place, not rewrite from scratch.

From src/blocks/CallToAction/Component.tsx (current, full file):

  import type { CallToActionBlock as CallToActionBlockProps } from '@/payload-types'
  import { Container } from '@/components/Container'
  import { RichTextRenderer } from '@/components/RichTextRenderer'
  import { CMSLink } from '@/components/CMSLink'
  import { HeroGrainGradient } from '@/components/HeroGrainGradient'

  export function CallToActionComponent(props: CallToActionBlockProps) {
    const { richText, links } = props
    return (
      <section className="relative overflow-hidden rounded-2xl shadow-xl ring-1 ring-white/10">
        <HeroGrainGradient variant="cta" />
        <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-black/20 to-black/10" aria-hidden="true" />
        <Container className="relative z-10 py-16 md:py-20 flex flex-col md:flex-row items-center md:items-start justify-between gap-8 md:gap-12 text-center md:text-left">
          <div className="flex-1 max-w-xl">
            <RichTextRenderer data={richText} className="text-secondary-foreground" />
          </div>
          {links && links.length > 0 && (
            <div className="flex flex-col sm:flex-row gap-3 sm:gap-4 w-full sm:w-auto">
              {links.map(({ link }, i) => (
                <div key={i} className="w-full sm:w-auto">
                  <CMSLink {...link} className="w-full sm:w-auto" />
                </div>
              ))}
            </div>
          )}
        </Container>
      </section>
    )
  }

From src/blocks/FAQ/Component.tsx (current, full file):

  import type { FAQBlock as FAQBlockProps } from '@/payload-types'
  import { Container } from '@/components/Container'
  import { RichTextRenderer } from '@/components/RichTextRenderer'

  export function FAQComponent(props: FAQBlockProps) {
    const { title, faqs } = props
    return (
      <Container className="py-12">
        {title && <h2 className="font-heading text-heading mb-6">{title}</h2>}
        <div className="divide-y divide-border">
          {faqs?.map((item, i) => (
            <details key={i} className="py-4 group">
              <summary className="cursor-pointer font-sans font-semibold text-body list-none flex items-center justify-between">
                {item.question}
                <span className="ml-4 text-primary group-open:rotate-45 transition-transform">+</span>
              </summary>
              <div className="mt-3">
                <RichTextRenderer data={item.answer} />
              </div>
            </details>
          ))}
        </div>
      </Container>
    )
  }

From src/blocks/ClientLogosBlock/Component.tsx (current, relevant excerpt -- logo mapping):

  {logos.map((client) => {
    const logo = typeof client.logo === 'object' ? client.logo : null
    if (!logo?.url) return null
    const image = (
      <Image
        src={logo.url}
        alt={logo.alt ?? client.name}
        width={120}
        height={48}
        className="object-contain grayscale opacity-70 hover:opacity-100 hover:grayscale-0 transition-all"
      />
    )
    return client.websiteUrl ? (
      <a key={client.id} href={client.websiteUrl} target="_blank" rel="noopener noreferrer">{image}</a>
    ) : (
      <div key={client.id}>{image}</div>
    )
  })}

From src/blocks/TestimonialsCarousel/Component.tsx (current, relevant excerpt -- scroll row):

  <div className="flex gap-4 overflow-x-auto pb-4 snap-x">
    {testimonials.map((testimonial) => { /* Card.map ... */ })}
  </div>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CTA Container fix + FAQ visual grouping</name>
  <files>src/blocks/CallToAction/Component.tsx, src/blocks/FAQ/Component.tsx</files>
  <read_first>
    @src/blocks/CallToAction/Component.tsx
    @src/blocks/FAQ/Component.tsx
    @.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md (Component 1 "CTA Strip Container Fix" and Component 3 "FAQ Visual Grouping" sections, lines 110-217 -- exact target JSX)
  </read_first>
  <action>
    In CallToAction/Component.tsx: wrap the entire existing outer section (card styling rounded-2xl shadow-xl ring-1 ring-white/10, HeroGrainGradient, gradient overlay div, all unchanged) inside a new outer Container with className "py-12 md:py-16". Change the existing inner Container (currently holding "relative z-10 py-16 md:py-20 flex ...") to a plain div with the exact same className string unchanged -- only the tag changes from Container to div, so only one Container exists in the tree (per UI-SPEC Component 1's binding structural points: nesting two Containers double-applies max-w-6xl px-4 md:px-6). Keep RichTextRenderer/CMSLink/HeroGrainGradient imports and usage byte-identical otherwise. Per UIPOL-01 (26-CONTEXT.md CTA strip decision).

    In FAQ/Component.tsx: replace the flat "divide-y divide-border" wrapper div and its details children with the bordered-card treatment from UI-SPEC Component 3: outer div className "space-y-3", each details item gets className "group rounded-lg border border-border bg-card px-6 shadow-sm hover:shadow-md transition-shadow duration-base ease-standard", summary gets className "cursor-pointer font-sans font-semibold text-body list-none flex items-center justify-between py-4" containing the question text plus a Plus icon from lucide-react (className "ml-4 size-5 text-primary transition-transform duration-fast ease-out group-open:rotate-45", aria-hidden true), replacing the current bare "+" text glyph, and the answer wraps in a div className "pb-4" containing the existing RichTextRenderer. Add the lucide-react Plus import. Keep the outer Container className "py-12" and the title heading (font-heading text-heading mb-6) unchanged. Per UIPOL-04 (26-CONTEXT.md FAQ decision).

    Do NOT touch either file's config.ts -- this is a render-only fix in both cases, per UI-SPEC's Regression Guardrails section.
  </action>
  <verify>
    <automated>grep -c "Container" src/blocks/CallToAction/Component.tsx && grep -c "rounded-lg border border-border bg-card" src/blocks/FAQ/Component.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - src/blocks/CallToAction/Component.tsx contains exactly one Container-wrapping element around the whole card section (no nested second Container)
    - src/blocks/FAQ/Component.tsx contains "rounded-lg border border-border bg-card" and imports Plus from lucide-react
    - npx tsc --noEmit reports zero errors referencing either file
    - Neither file's corresponding config.ts is modified (git diff shows 0 changes to src/blocks/CallToAction/config.ts and src/blocks/FAQ/config.ts)
  </acceptance_criteria>
  <done>Both components render via a single Container each, FAQ items are visually bordered/elevated cards using the Plus icon, zero schema/config diff.</done>
</task>

<task type="auto">
  <name>Task 2: ClientLogos scale normalization + Testimonials scroll affordance</name>
  <files>src/blocks/ClientLogosBlock/Component.tsx, src/blocks/TestimonialsCarousel/Component.tsx</files>
  <read_first>
    @src/blocks/ClientLogosBlock/Component.tsx
    @src/blocks/TestimonialsCarousel/Component.tsx
    @.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md (Component 4 "ClientLogosBlock -- Logo Scale Normalization" and Component 5 "TestimonialsCarousel -- Scroll Affordance" sections, lines 220-278 -- exact target JSX)
  </read_first>
  <action>
    In ClientLogosBlock/Component.tsx: wrap each logo's Image in a fixed-height flex cell (div className "flex h-10 md:h-12 items-center"), keep the Image component's width={160}/height={48} props (Next.js layout-shift requirement, values updated from the current 120/48 per UI-SPEC), and change its className to "h-full w-auto max-w-[140px] object-contain grayscale opacity-70 hover:opacity-100 hover:grayscale-0 transition-all duration-base ease-standard". Preserve the existing conditional anchor-vs-div wrapping logic around the new inner cell exactly as-is -- only the image JSX constant changes shape (now includes the height-cell wrapper), the surrounding map/conditional structure is untouched. Per UIPOL-05 (26-CONTEXT.md clientes decision).

    In TestimonialsCarousel/Component.tsx: wrap the existing scroll row div ("flex gap-4 overflow-x-auto pb-4 snap-x" containing testimonials.map) in a new outer div className "relative", and add a sibling absolutely-positioned fade div immediately after the scroll row: className "pointer-events-none absolute inset-y-0 right-0 w-8 bg-gradient-to-l from-background to-transparent", aria-hidden true. Do not touch the testimonials.map internals, the Card/CardContent markup, or the resolvedTitle/getTranslations locale-fallback logic (25-REVIEW fix) above it -- those are unrelated to this fix and must stay byte-identical. Per UIPOL-06 (26-CONTEXT.md testimonios decision).

    Do NOT touch either file's data-fetching logic (payload.find collection clientes, payload.find collection testimonials) or config.ts -- render-only fixes, per UI-SPEC's Regression Guardrails section.
  </action>
  <verify>
    <automated>grep -c "h-10 md:h-12" src/blocks/ClientLogosBlock/Component.tsx && grep -c "bg-gradient-to-l" src/blocks/TestimonialsCarousel/Component.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - src/blocks/ClientLogosBlock/Component.tsx contains "h-10 md:h-12" and "max-w-[140px]"
    - src/blocks/TestimonialsCarousel/Component.tsx contains "bg-gradient-to-l" inside a "relative" wrapper div
    - npx tsc --noEmit reports zero errors referencing either file
    - Neither file's data-fetching calls (payload.find) or config.ts are modified
  </acceptance_criteria>
  <done>Client logos render at normalized visual height with no aspect-ratio distortion, TestimonialsCarousel shows a right-edge scroll-affordance fade, zero data-fetching/schema diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload CMS -> rendered JSX | Editorial content (richText, FAQ questions/answers, client names, testimonial text) flows from Payload into these components -- already rendered through RichTextRenderer/plain text interpolation, unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | FAQ/Component.tsx RichTextRenderer output | accept | No change to RichTextRenderer's existing sanitization/escaping in this plan -- pure wrapper/class changes around already-trusted render path |
| T-26-02 | Information Disclosure | ClientLogosBlock external website links | accept | rel="noopener noreferrer" already present and untouched -- this plan does not modify the anchor's security attributes |
</threat_model>

<verification>
Run `npx tsc --noEmit -p tsconfig.json` from repo root after both tasks -- zero errors referencing any of the 4 touched files.
Run `git diff --stat` -- confirm only the 4 Component.tsx files changed, zero config.ts/payload-types.ts/migration files touched.
Start `npm run dev`, visit `/` (home, has CallToAction/ClientLogos/Testimonials) and any page with FAQ (Home, per Phase 13) in both `/` (es) and `/en` -- confirm no visual overflow, CTA card corners no longer touch viewport edge, FAQ items show bordered cards, logos appear height-aligned, testimonials row shows a right-edge fade.
</verification>

<success_criteria>
- CallToAction renders inside Container with no vw-wide bleed in both locales (UIPOL-01)
- FAQ items are bordered/elevated cards, native details/summary semantics unchanged (UIPOL-04)
- ClientLogosBlock logos are height-normalized, no distortion (UIPOL-05)
- TestimonialsCarousel shows a right-edge fade affordance (UIPOL-06)
- Zero config.ts/schema/migration changes across all 4 files
</success_criteria>

<output>
Create `.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-01-SUMMARY.md` when done
</output>
