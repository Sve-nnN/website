---
phase: 26-ui-ux-polish-pass-low-risk-components
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/breadcrumbs.ts
  - "src/app/(frontend)/[locale]/case-studies/page.tsx"
  - "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
autonomous: true
requirements: [UIPOL-09]

must_haves:
  truths:
    - "Case Studies listing page shows a visible breadcrumb trail (Inicio > Casos de exito / Home > Case Studies) and emits matching BreadcrumbList JSON-LD, which it previously had neither"
    - "Case Studies detail page shows a visible breadcrumb trail (Inicio > Casos de exito > title) sourced from the same shared helper Services already uses, replacing its previous hand-rolled, unaligned JSON-LD-only breadcrumbData object"
    - "buildTrail (Services existing export) remains byte-for-byte backward compatible -- all 4 existing Services call sites require zero changes"
  artifacts:
    - path: "src/lib/breadcrumbs.ts"
      provides: "buildSectionTrail internal generalization + buildCaseStudiesTrail sibling export (UIPOL-09)"
      contains: "buildCaseStudiesTrail"
    - path: "src/app/(frontend)/[locale]/case-studies/page.tsx"
      provides: "Visual breadcrumb nav + BreadcrumbList JSON-LD on the listing page"
      contains: "buildCaseStudiesTrail"
    - path: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      provides: "Visual breadcrumb nav + BreadcrumbList JSON-LD sourced from the shared helper, replacing the hand-rolled breadcrumbData object"
      contains: "buildCaseStudiesTrail"
  key_links:
    - from: "src/app/(frontend)/[locale]/case-studies/page.tsx"
      to: "src/lib/breadcrumbs.ts"
      via: "buildCaseStudiesTrail(locale) plus buildBreadcrumbJsonLd(trail) feeding both the visual nav and JsonLd component"
      pattern: "buildCaseStudiesTrail"
    - from: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      to: "src/lib/breadcrumbs.ts"
      via: "buildCaseStudiesTrail(locale, current) replacing the hand-rolled breadcrumbData object"
      pattern: "buildCaseStudiesTrail"
---

<objective>
Extend the existing Services breadcrumb helper (src/lib/breadcrumbs.ts, Phase 22) with a generalized internal buildSectionTrail() and a new buildCaseStudiesTrail() sibling export, then wire both Case Studies pages (listing + detail) to it -- adding a visual breadcrumb trail the listing page never had, and replacing the detail page's hand-rolled, desynced breadcrumbData JSON-LD object with the shared helper.

Purpose: Close UIPOL-09 per 26-UI-SPEC.md Component 6, unifying Case Studies onto the exact same single-source-of-truth breadcrumb pattern Services already uses, per 26-CONTEXT.md's explicit instruction not to duplicate URL/locale logic.
Output: src/lib/breadcrumbs.ts extended (buildTrail stays byte-for-byte backward compatible via a thin wrapper), both Case Studies pages updated with a visual nav breadcrumb and shared JSON-LD. Zero schema/migration change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md
@.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-CONTEXT.md

CRITICAL CONSTRAINT (per CLAUDE.md and orchestrator instructions): this plan touches ZERO database schema, ZERO Payload collection config.ts files, ZERO migrations, ZERO payload-types.ts changes. It is a pure module extension (breadcrumbs.ts) plus two page.tsx JSX and data-wiring edits. If executing this plan reveals a genuine need for a schema/config change, STOP and flag it instead of proceeding -- the UI-SPEC's decision is explicitly to compute the trail purely from the existing slug/title/locale, not add a new Payload field.

Regression-critical: Services' 4 existing pages (servicios/page.tsx, servicios/[slug]/page.tsx, services/page.tsx, services/[slug]/page.tsx) call buildTrail() today and MUST require zero code changes after this plan.
</context>

<interfaces>
Current full content of src/lib/breadcrumbs.ts (the file this plan extends):

  import { SITE_URL } from '@/lib/sitemap-data'
  export type Locale = 'es' | 'en'
  export type BreadcrumbItem = { label: string; url: string }
  const LABELS: Record<Locale, { home: string; services: string }> = {
    es: { home: 'Inicio', services: 'Servicios' },
    en: { home: 'Home', services: 'Services' },
  }
  function homeHref(locale: Locale): string {
    return locale === 'es' ? '/' : '/en'
  }
  function servicesSegment(locale: Locale): string {
    return locale === 'es' ? 'servicios' : 'services'
  }
  function servicesIndexHref(locale: Locale): string {
    const home = homeHref(locale)
    return home === '/' ? '/' + servicesSegment(locale) : home + '/' + servicesSegment(locale)
  }
  export function buildTrail(locale: Locale, current?: { slug: string; title: string }): BreadcrumbItem[] {
    const trail: BreadcrumbItem[] = [
      { label: LABELS[locale].home, url: homeHref(locale) },
      { label: LABELS[locale].services, url: servicesIndexHref(locale) },
    ]
    if (current) {
      trail.push({ label: current.title, url: servicesIndexHref(locale) + '/' + current.slug })
    }
    return trail
  }
  export function buildBreadcrumbJsonLd(trail: BreadcrumbItem[]): Record<string, unknown> {
    return {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: trail.map((crumb, i) => ({
        '@type': 'ListItem',
        position: i + 1,
        name: crumb.label,
        item: crumb.url === '/' ? SITE_URL : SITE_URL + crumb.url,
      })),
    }
  }

Case Studies detail page (case-studies/[slug]/page.tsx) -- relevant excerpt to replace, current copy object and hand-rolled breadcrumbData:

  const copy = {
    es: { client: 'El cliente', challenge: 'El reto', solution: 'La solución', results: 'Resultados', home: 'Inicio', caseStudies: 'Casos de éxito' },
    en: { client: 'The Client', challenge: 'The Challenge', solution: 'The Solution', results: 'Results', home: 'Home', caseStudies: 'Case Studies' },
  }
  const t = copy[locale as 'es' | 'en'] ?? copy.es
  const breadcrumbData = {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: t.home, item: SITE_URL },
      { '@type': 'ListItem', position: 2, name: t.caseStudies, item: SITE_URL + '/case-studies' },
      { '@type': 'ListItem', position: 3, name: doc.title, item: SITE_URL + '/case-studies/' + doc.slug },
    ],
  }
  // rendered later as: <JsonLd data={breadcrumbData} />
  // module-level const SITE_URL = process.env.NEXT_PUBLIC_SERVER_URL || 'https://juancarlosangulo.com'
  // hero section JSX: <section className="relative bg-secondary text-secondary-foreground"> ... <Container className="py-8"> (client/sector/period row, h1, subtitle, heroMetric)

Case Studies listing page (case-studies/page.tsx) -- full file today has no breadcrumb of any kind and no JsonLd import:

  export default async function CaseStudiesListPage({ params }) {
    const { locale } = await params
    const caseStudies = await getCaseStudies(locale)
    return (
      <main>
        <Container className="py-16">
          <h1 className="font-display text-display">{locale === 'es' ? 'Casos de éxito' : 'Case Studies'}</h1>
          {/* empty-state / grid, unchanged -- do not touch this copy per UI-SPEC Copywriting Contract */}
        </Container>
      </main>
    )
  }

Hero block's existing breadcrumb nav JSX pattern to replicate visually (src/blocks/Hero/Component.tsx, navy-background variant, already the exact pattern Services reuses):

  <nav aria-label="Breadcrumb" className="mb-4">
    <ol className="flex flex-wrap items-center gap-x-2 gap-y-1 text-sm text-secondary-foreground/70">
      {trail.map((crumb, i) => {
        const isLast = i === trail.length - 1
        return (
          <li key={i} className="flex items-center gap-x-2">
            {i > 0 && <span aria-hidden="true">/</span>}
            {isLast ? (
              <span aria-current="page">{crumb.label}</span>
            ) : (
              <Link href={crumb.url} className="hover:text-secondary-foreground underline-offset-2 hover:underline">{crumb.label}</Link>
            )}
          </li>
        )
      })}
    </ol>
  </nav>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend breadcrumbs.ts with buildSectionTrail and buildCaseStudiesTrail</name>
  <files>src/lib/breadcrumbs.ts</files>
  <read_first>
    @src/lib/breadcrumbs.ts
    @.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md (Component 6 section, "Decision: generalize buildTrail internally" through "Exact trail structure" -- exact target module code)
  </read_first>
  <action>
    Refactor src/lib/breadcrumbs.ts per UI-SPEC Component 6's code contract: introduce a Section type of 'services' or 'case-studies', a SECTION_LABELS record (services: es Servicios / en Services; case-studies: es "Casos de éxito" / en "Case Studies"), and a SECTION_SEGMENTS record mapping each section plus locale to its URL segment (services: es servicios / en services; case-studies: es case-studies / en case-studies -- Case Studies routes are NOT locale-prefixed in their segment, the same folder serves both /case-studies and /en/case-studies, unlike Services which has genuinely different es/en segments). Add a sectionIndexHref(locale, section) helper generalizing the existing servicesIndexHref. Extract a generic internal buildSectionTrail(locale, section, current) function implementing the same trail-building logic buildTrail() has today, parameterized by section. Redefine buildTrail(locale, current) as a thin wrapper that returns buildSectionTrail(locale, 'services', current) -- its exported signature and behavior stay byte-for-byte identical to today for all 4 existing Services call sites. Add a new exported function buildCaseStudiesTrail(locale, current) calling buildSectionTrail(locale, 'case-studies', current). Leave buildBreadcrumbJsonLd() completely unchanged. Keep the existing LABELS constant as-is; SECTION_LABELS is additive. Per UIPOL-09 (26-CONTEXT.md Case Studies breadcrumbs decision).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c buildCaseStudiesTrail src/lib/breadcrumbs.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/breadcrumbs.ts exports both buildTrail (unchanged signature) and buildCaseStudiesTrail (new, same parameter shape)
    - buildBreadcrumbJsonLd is untouched (same generic function body)
    - npx tsc --noEmit reports zero errors
    - The 4 existing Services page.tsx files still compile against buildTrail with zero code changes needed
  </acceptance_criteria>
  <done>breadcrumbs.ts exposes buildCaseStudiesTrail as a sibling to the unchanged buildTrail, both backed by a shared internal buildSectionTrail, zero duplication of URL/locale logic, zero regression to Services' 4 call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Case Studies listing and detail pages to the shared breadcrumb helper</name>
  <files>src/app/(frontend)/[locale]/case-studies/page.tsx, src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx</files>
  <read_first>
    @src/app/(frontend)/[locale]/case-studies/page.tsx
    @src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
    @src/blocks/Hero/Component.tsx
    @.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-UI-SPEC.md (Component 6, "Exact trail structure", "Wiring: JSON-LD", "Wiring: visual nav" subsections)
  </read_first>
  <action>
    In case-studies/[slug]/page.tsx: import buildCaseStudiesTrail and buildBreadcrumbJsonLd from '@/lib/breadcrumbs'. Compute the trail inside the page component after doc is resolved, calling buildCaseStudiesTrail with the locale and a current object containing the doc's slug and title. Delete the hand-rolled breadcrumbData object. Replace its JsonLd usage with buildBreadcrumbJsonLd(trail). Insert a visual breadcrumb nav inside the existing hero section (bg-secondary text-secondary-foreground), inside its Container py-8, positioned above the existing client/sector/period metadata row -- reuse the exact JSX pattern from Hero/Component.tsx's own breadcrumb nav (aria-label Breadcrumb, ol with text-sm text-secondary-foreground/70, slash separator between items, last item as a plain aria-current page span, earlier items as Link with hover:text-secondary-foreground underline-offset-2 hover:underline), mapping over the computed trail. Remove the now-redundant home/caseStudies keys from the copy object only if nothing else in the file references them (verify via grep first) -- keep the client/challenge/solution/results keys untouched.

    In case-studies/page.tsx: import buildCaseStudiesTrail and buildBreadcrumbJsonLd from '@/lib/breadcrumbs' and JsonLd from '@/components/JsonLd' (this page currently has no JsonLd import). Compute the trail by calling buildCaseStudiesTrail with just the locale (no current, 2-level trail). Insert a visual breadcrumb nav inside the existing Container py-16, immediately above the existing h1, using the light-surface variant per UI-SPEC: text-sm text-muted-foreground for the ol, hover:text-foreground on links instead of the navy secondary-foreground variant, since this page's background is the off-white background token with no hero band. Add a JsonLd component rendering buildBreadcrumbJsonLd(trail) inside the returned main element. Do NOT touch the existing empty-state copy or the caseStudies.map grid -- those are out of scope per UI-SPEC's Copywriting Contract.

    Both insertions must reuse the same trail variable for both the JSON-LD call and the visual nav -- never re-derive labels or URLs inline in either page, per breadcrumbs.ts's own header-comment contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - case-studies/page.tsx imports and calls buildCaseStudiesTrail and buildBreadcrumbJsonLd, renders a JsonLd component and a visual breadcrumb nav above its h1
    - case-studies/[slug]/page.tsx imports and calls buildCaseStudiesTrail with slug/title, renders buildBreadcrumbJsonLd(trail) via JsonLd, and no longer contains a breadcrumbData object
    - case-studies/[slug]/page.tsx contains a visual breadcrumb nav inside its hero Container, above the client/sector/period row
    - npx tsc --noEmit reports zero errors
    - The empty-state copy strings and caseStudies.map grid in case-studies/page.tsx are unchanged (git diff shows no modification to those lines)
  </acceptance_criteria>
  <done>Both Case Studies pages render a visual breadcrumb trail and matching BreadcrumbList JSON-LD sourced from the single shared helper, replacing the previous hand-rolled/absent implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload CaseStudies doc (slug, title) -> breadcrumb trail -> JSON-LD | doc.slug/doc.title flow from a trusted Local API fetch into both the visual nav and the JSON-LD payload -- already-trusted editorial content, same trust level as Services' existing usage of the same pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering | JsonLd component rendering buildBreadcrumbJsonLd output | accept | JsonLd.tsx already carries the Phase 22 code-review escape fix for injected script content -- unchanged by this plan, reused as-is |
| T-26-06 | Repudiation/Integrity | buildTrail backward compatibility | mitigate | Task 1's explicit acceptance criterion requiring the 4 existing Services call sites to compile unchanged, verified via tsc before considering the task complete |
</threat_model>

<verification>
Run npx tsc --noEmit -p tsconfig.json -- zero errors.
Run git diff --stat -- confirm only breadcrumbs.ts and the two case-studies page.tsx files changed, zero config.ts/payload-types.ts/migration files touched, zero changes to the 4 Services page.tsx files.
Start npm run dev, visit /case-studies and /en/case-studies (listing) and one real case study detail page in both locales -- confirm a visible breadcrumb trail renders (Inicio / Casos de éxito on the listing, Inicio / Casos de éxito / title on the detail), and view page source or curl to confirm a BreadcrumbList JSON-LD script tag is present on both pages with matching entries.
Spot-check one Services page (e.g. /servicios) still renders its existing breadcrumb correctly, confirming zero regression from the buildTrail refactor.
</verification>

<success_criteria>
- Case Studies listing page shows a visible 2-level breadcrumb trail and emits BreadcrumbList JSON-LD, which it previously had neither (UIPOL-09)
- Case Studies detail page shows a visible 3-level breadcrumb trail sourced from the shared helper, replacing the previous hand-rolled JSON-LD-only implementation (UIPOL-09)
- buildTrail remains byte-for-byte backward compatible, zero code changes required in Services' 4 existing pages
- Zero config.ts/schema/migration changes
</success_criteria>

<output>
Create `.planning/phases/26-ui-ux-polish-pass-low-risk-components/26-03-SUMMARY.md` when done
</output>
