---
phase: 10.5-typography-header-footer-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["10.5-03", "10.5-04"]
files_modified:
  - package.json
  - scripts/verify-mobile-viewport.mjs
autonomous: false
requirements: [10.5-SC4]

must_haves:
  truths:
    - "El layout mobile (~375px) de header y footer se verifica de forma real y automatizada (viewport real via Chromium headless), no solo 'riesgo bajo asumido' como en Phase 10/11"
    - "Se verifican tambien tablet (~768px) y desktop (~1280px) para confirmar progresion mobile-first, no solo el punto minimo"
    - "Juan confirma visualmente que header y footer ya no se sienten planos/vacios"
  artifacts:
    - path: "scripts/verify-mobile-viewport.mjs"
      provides: "Script Playwright que carga la home en 375/768/1280px, revisa overflow horizontal y visibilidad correcta de nav mobile vs desktop, y falla el proceso si algo esta roto"
      contains: "375"
  key_links:
    - from: "scripts/verify-mobile-viewport.mjs"
      to: "src/components/SiteHeader.tsx"
      via: "assertion sobre visibilidad del trigger del Sheet a 375px y del NavigationMenu a 1280px"
      pattern: "375"
---

<objective>
Verificar de forma real y automatizada (no solo revision de codigo) que el header y el footer restyleados en los planes 10.5-03 y 10.5-04 funcionan correctamente en mobile (~375px) antes que en tablet/desktop, cerrando el gap admitido explicitamente en Phase 10/11 ("no viewport-resize tool available"). Instala Playwright (headless Chromium), escribe un script de verificacion de viewport, y cierra con un checkpoint de confirmacion visual de Juan.

Purpose: 10.5-CONTEXT.md pide explicitamente no repetir el patron de "riesgo bajo asumido" de fases anteriores — encontrar una forma real de verificar mobile, no solo documentar la limitacion.
Output: scripts/verify-mobile-viewport.mjs (reutilizable para futuras fases de este milestone), Playwright instalado, verificacion pasada y confirmacion visual de Juan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10.5-typography-header-footer-overhaul/10.5-CONTEXT.md
@src/components/SiteHeader.tsx
@src/components/SiteFooter.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirmar legitimidad del paquete npm playwright antes de instalar</name>
  <action>No hay acción autónoma en este task — es un gate bloqueante de confirmación humana antes de instalar playwright.</action>
  <what-built>Nada todavia. No existe una tabla de Package Legitimacy Audit en RESEARCH.md/STACK.md para este proyecto, asi que por protocolo playwright se trata como [ASSUMED] hasta confirmacion manual.</what-built>
  <how-to-verify>
    Visita https://www.npmjs.com/package/playwright y confirma que el paquete es el oficial de Microsoft (autor microsoft/playwright, millones de descargas semanales, no un typosquat). Es una devDependency (herramienta de verificacion, no shipea a produccion), pero igual requiere confirmacion antes de instalar segun el protocolo de este proyecto.
  </how-to-verify>
  <resume-signal>Escribe "confirmado" para desbloquear la instalacion.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Instalar Playwright y escribir el script de verificacion de viewport</name>
  <files>package.json, scripts/verify-mobile-viewport.mjs</files>
  <action>
    Instala playwright como devDependency y corre la instalacion del binario de Chromium (npx playwright install chromium --with-deps, o el equivalente sin --with-deps si el entorno no tiene permisos de sistema para paquetes nativos adicionales — documenta cual se uso).

    Crea scripts/verify-mobile-viewport.mjs: un script Node ejecutable con node (no requiere test runner) que:
    1. Levanta o asume un servidor de desarrollo corriendo en http://localhost:3000 (usa una variable de entorno BASE_URL con fallback a ese valor; documenta en un comentario que el ejecutor debe correr npm run dev en paralelo antes de este script, o adapta el script para lanzar el servidor el mismo con child_process si prefieres autocontencion total).
    2. Para cada viewport [{width: 375, height: 812, name: 'mobile'}, {width: 768, height: 1024, name: 'tablet'}, {width: 1280, height: 800, name: 'desktop'}]: abre una pagina de Chromium headless con ese viewport, navega a /en (o /es), y corre las siguientes aserciones, coleccionando fallos en un array en vez de abortar en el primero:
       - document.documentElement.scrollWidth no excede el viewport width en mas de un margen de 1px (sin overflow horizontal).
       - A 375px: el SheetTrigger del header (boton hamburguesa) es visible, y el NavigationMenu horizontal de desktop NO es visible.
       - A 1280px: el NavigationMenu horizontal de desktop es visible, y el SheetTrigger NO es visible.
       - El footer es visible y su scrollWidth tampoco excede el viewport en ningun breakpoint.
    3. Al final, imprime un resumen por viewport (OK/FAIL con el detalle de cada assertion) y termina con process.exit(1) si hubo algun fallo, process.exit(0) si todo paso.

    Corre el script contra el dev server real de este proyecto y adjunta el resultado (pasa/falla, con detalle) en el summary de este plan.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && test -f scripts/verify-mobile-viewport.mjs && grep -q "375" scripts/verify-mobile-viewport.mjs && grep -q "1280" scripts/verify-mobile-viewport.mjs && node scripts/verify-mobile-viewport.mjs</automated>
  </verify>
  <done>El script existe, corre contra Chromium headless real (no una simulacion de CSS), verifica los 3 breakpoints, y termina en 0 (sin overflow horizontal, nav mobile/desktop correctamente exclusivos por breakpoint).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmación visual final de Juan sobre header y footer</name>
  <action>No hay acción autónoma en este task — es un gate bloqueante de confirmación visual humana sobre el resultado final.</action>
  <what-built>Header restyleado (10.5-03) y footer con contenido dinamico + restyle (10.5-04), verificados automaticamente a 375/768/1280px sin overflow horizontal ni bugs de visibilidad de nav (Task 2). Falta la confirmacion subjetiva: que el header/footer ya NO se sientan "planos/vacios" segun el criterio original de Juan.</what-built>
  <how-to-verify>
    1. Corre npm run dev y abre http://localhost:3000/en (o /es) en un navegador real.
    2. Reduce la ventana a ~375px de ancho (o usa las DevTools del navegador en modo dispositivo movil) y revisa: el header se ve completo con el boton hamburguesa, al abrirlo el menu muestra navegacion clara con buen espaciado; el footer se ve como un footer real (columnas apiladas, incluyendo la seccion de ultimos posts/case studies si ya hay contenido publicado con dynamicColumns configurado en /admin).
    3. Expande a desktop (~1280px) y revisa el nav horizontal completo y el footer en grid multi-columna.
    4. Confirma si esto ya resuelve tu feedback original ("las fuentes no me gustan, el footer no tiene nada, el header muy simple") o si falta algo puntual.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los ajustes puntuales que faltan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> devDependencies | playwright y su binario de Chromium se descargan de npm/CDN de Microsoft en install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10.5-09 | Tampering | Instalacion de playwright | mitigate | Task 1 checkpoint bloqueante: confirmar en npmjs.com/package/playwright que el autor es microsoft antes de instalar |
| T-10.5-10 | Denial of Service (leve) | Descarga de binario Chromium puede ser pesada/lenta en el entorno de ejecucion | accept | Es una devDependency de verificacion, no afecta el build de produccion; si falla la descarga, el checkpoint humano del Task 3 sigue siendo la verificacion de respaldo |

</threat_model>

<verification>
- node scripts/verify-mobile-viewport.mjs termina en 0 contra el dev server real.
- Juan confirma visualmente (checkpoint Task 3) que el resultado resuelve su feedback original.
</verification>

<success_criteria>
El layout mobile (~375px) de header y footer se verifica de forma real (Chromium headless real, no CSS review ni "riesgo bajo asumido"), antes que desktop, y Juan confirma visualmente el resultado. ROADMAP Phase 10.5 Success Criterion 4.
</success_criteria>

<output>
Create `.planning/phases/10.5-typography-header-footer-overhaul/10.5-05-SUMMARY.md` when done
</output>
