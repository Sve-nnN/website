---
phase: 10.5-typography-header-footer-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["10.5-01"]
files_modified:
  - src/components/SiteHeader.tsx
autonomous: true
requirements: [10.5-SC2]

must_haves:
  truths:
    - "SiteHeader muestra navegacion visible (no solo logo+CTA), verificada mobile-first: el menu hamburguesa/Sheet se disena y revisa primero, luego el nav horizontal de desktop"
    - "navItems del global Header se renderiza tanto en el Sheet movil como en el nav horizontal de desktop, con jerarquia visual clara (no plano/vacio)"
  artifacts:
    - path: "src/components/SiteHeader.tsx"
      provides: "Header restyleado: Sheet movil con jerarquia clara y tap targets grandes, nav desktop con mayor presencia visual"
      contains: "SheetContent"
  key_links:
    - from: "src/components/SiteHeader.tsx"
      to: "header.navItems"
      via: "header.navItems?.map(...) ya existente, ahora con estilos mobile-first reforzados"
      pattern: "navItems"
---

<objective>
Restylear SiteHeader.tsx para que la navegacion se sienta completa y visible, no "simple/vacia" (queja directa de Juan). navItems del global Header YA se renderiza hoy en ambos contextos (Sheet movil y nav horizontal desktop) — confirmado por lectura directa del componente actual, asi que este plan es puramente de estilo/jerarquia visual, no de wiring faltante. Diseña y verifica el menu movil PRIMERO (mobile-first), luego el nav de desktop.

Purpose: Juan revizo el sitio acumulado y dijo "el header tampoco me gusta, esta muy simple". El campo navItems existe en Payload y se renderiza, pero sin suficiente peso visual.
Output: src/components/SiteHeader.tsx restyleado, mobile-first, usando los nuevos tokens de fuente del plan 10.5-01 (font-heading para wordmark/labels de nav).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10.5-typography-header-footer-overhaul/10.5-CONTEXT.md
@src/components/SiteHeader.tsx
@src/globals/Header/index.ts

<!-- Referencia estructural (no literal — distinto dominio/color) de como auditor construye su header -->
@/Users/juan/Documents/Codigo/Personal/juantech/auditor/apps/web/app/components/AppHeader.tsx
</context>

<interfaces>
Estado actual confirmado de src/components/SiteHeader.tsx (leido directamente, no asumido):
- header.navItems ya se mapea en un NavigationMenu horizontal (desktop, oculto en mobile via "hidden md:flex") y tambien dentro del Sheet (mobile, trigger visible solo en "md:hidden").
- El Sheet ya usa componentes shadcn existentes: Sheet, SheetContent, SheetTrigger, con icono Menu de lucide-react.
- El wordmark de fallback (cuando no hay logo subido) usa className "font-display text-heading" — tras el plan 10.5-01 esto ya habra migrado a "font-heading text-heading" via el sed masivo; no hace falta que este plan lo toque de nuevo.
- CTA button usa el componente Button de shadcn (@/components/ui/button), ya estilado con variant default.
- LocaleSwitcher ya existe como componente separado, se renderiza en ambos contextos (desktop nav y Sheet).

No hay wiring faltante que arreglar: el gap es puramente de densidad/jerarquia visual, confirmado por lectura directa del archivo antes de planear.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rediseñar el Sheet movil primero (mobile-first, ~375px)</name>
  <files>src/components/SiteHeader.tsx</files>
  <action>
    Mejora el SheetContent movil para que se sienta completo, no una lista plana de links: aumenta el tap-target de cada link de navegacion a un minimo de 44px de alto (padding vertical generoso, ej. py-3), aplica font-heading a los labels de navegacion para que hereden Khand (mas peso visual que el body), añade un separador visual (componente Separator ya usado en SiteFooter) entre el bloque de nav links y el bloque de LocaleSwitcher+CTA para crear jerarquia de secciones dentro del Sheet, y añade un estado activo/hover mas visible (fondo sutil bg-muted en hover, no solo el borde inferior que ya existe). El boton trigger del hamburguesa (SheetTrigger) debe tener suficiente tamaño de toque (ya usa size="icon" de Button, confirma que resuelve a al menos 40x40px) y buen contraste contra el fondo bg-secondary del header.

    Verifica visualmente (o via revision cuidadosa de las utilities de Tailwind aplicadas, dado que no hay herramienta de redimension de viewport en esta tarea — la verificacion real a 375px ocurre en el plan 10.5-05) que ninguna clase usa un breakpoint md:/lg: que oculte contenido necesario en mobile antes de que el usuario pueda abrir el Sheet.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && grep -q "SheetContent" src/components/SiteHeader.tsx && grep -q "py-3\|py-4" src/components/SiteHeader.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>El Sheet movil tiene tap targets grandes, separador visual entre secciones, hover state con fondo, y font-heading aplicado a los labels de nav.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reforzar el nav horizontal de desktop</name>
  <files>src/components/SiteHeader.tsx</files>
  <action>
    Sobre el nav horizontal existente (NavigationMenu, visible solo en md:flex), aumenta el espaciado entre items (gap-6 actual puede subir a gap-8 si el numero de nav items lo permite sin overflow), refuerza el estado activo/hover de cada link (el border-b-2 actual se mantiene pero considera añadir un leve cambio de color de texto a text-primary en hover ademas del borde, para mayor feedback visual), y evalua si el header sticky necesita mas presencia (ej. un borde inferior sutil border-b border-border/20 ademas del shadow-md ya existente, para que se perciba como una barra solida y no flotante). El boton CTA debe destacar claramente del resto del nav (ya usa Button default variant, confirma que el contraste es suficiente contra bg-secondary).

    No cambies la estructura de datos (header.navItems, header.ctaButton) ni el query a Payload — es un cambio puramente de clases Tailwind sobre el JSX existente.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && grep -q "NavigationMenu" src/components/SiteHeader.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>El nav horizontal de desktop tiene mayor espaciado, feedback de hover mas claro, y el header sticky se percibe visualmente solido (no plano/vacio).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS (Header global) -> render publico | navItems/ctaButton vienen de contenido editable por un editor autenticado; se renderizan a usuarios anonimos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10.5-06 | Tampering | Ninguna dependencia nueva instalada en este plan | accept | Solo cambios de clases Tailwind sobre componentes shadcn ya auditados en fases previas |

</threat_model>

<verification>
- npx tsc --noEmit pasa sin errores.
- npm run dev levanta sin errores de render en SiteHeader.
- Revision manual del JSX confirma que ningun breakpoint md:/lg: oculta el trigger del Sheet en mobile ni el nav horizontal en desktop.
</verification>

<success_criteria>
SiteHeader muestra navegacion visible (no solo logo+CTA), diseñada mobile-first (Sheet primero, nav horizontal despues). ROADMAP Phase 10.5 Success Criterion 2.
</success_criteria>

<output>
Create `.planning/phases/10.5-typography-header-footer-overhaul/10.5-03-SUMMARY.md` when done
</output>
