---
phase: 10.6-header-footer-completion-mobile-verification
plan: 03
type: execute
wave: 2
depends_on: ["10.6-01", "10.6-02"]
files_modified:
  - package.json
  - scripts/verify-mobile-viewport.mjs
autonomous: false
requirements: [UI-19]

must_haves:
  truths:
    - "El layout mobile (~375px) de header y footer se verifica de forma real y automatizada (viewport real via Chromium headless), no solo 'riesgo bajo asumido' como en fases previas"
    - "Se verifican tambien tablet (~768px) y desktop (~1280px) para confirmar progresion mobile-first, no solo el punto minimo"
    - "Header y footer no rompen layout ni ocultan contenido critico en ningun breakpoint, con evidencia (screenshots o assertions) adjunta al cierre de la fase"
  artifacts:
    - path: "scripts/verify-mobile-viewport.mjs"
      provides: "Script Playwright que carga la home en 375/768/1280px, revisa overflow horizontal y visibilidad correcta de nav mobile vs desktop, y falla el proceso si algo esta roto"
      contains: "375"
  key_links:
    - from: "scripts/verify-mobile-viewport.mjs"
      to: "src/components/SiteHeader.tsx"
      via: "assertion sobre visibilidad del trigger del Sheet a 375px y del NavigationMenu a 1280px"
      pattern: "375"
---

<objective>
Cerrar UI-19: verificar de forma real y automatizada (no solo revision de codigo) que el header (10.6-01) y el footer (10.6-02) funcionan correctamente en mobile (~375px), tablet (~768px) y desktop (~1280px). Instala Playwright (headless Chromium), escribe un script de verificacion de viewport que produce evidencia real (screenshots + assertions), y cierra con un checkpoint de confirmacion visual de Juan sobre el resultado acumulado de la fase.

Purpose: Juan pidio explicitamente no repetir el patron de "riesgo bajo asumido" de fases anteriores (Phase 10/11 admitieron no tener herramienta de redimension de viewport) — esta es la primera verificacion mobile real del proyecto con evidencia adjunta.
Output: scripts/verify-mobile-viewport.mjs (reutilizable para Phases 10.7/10.8/11), Playwright instalado como devDependency, screenshots de los 3 breakpoints, verificacion pasada, y confirmacion visual de Juan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/SiteHeader.tsx
@src/components/SiteFooter.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirmar legitimidad del paquete npm playwright antes de instalar</name>
  <action>No hay accion autonoma en este task — es un gate bloqueante de confirmacion humana antes de instalar playwright.</action>
  <what-built>Nada todavia. No existe una tabla de Package Legitimacy Audit en RESEARCH.md/STACK.md para este proyecto, asi que por protocolo playwright se trata como [ASSUMED] hasta confirmacion manual.</what-built>
  <how-to-verify>
    Visita https://www.npmjs.com/package/playwright y confirma que el paquete es el oficial de Microsoft (autor microsoft/playwright, millones de descargas semanales, no un typosquat). Es una devDependency (herramienta de verificacion, no shipea a produccion), pero igual requiere confirmacion antes de instalar segun el protocolo de este proyecto.
  </how-to-verify>
  <resume-signal>Escribe "confirmado" para desbloquear la instalacion.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Instalar Playwright y escribir el script de verificacion de viewport</name>
  <files>package.json, scripts/verify-mobile-viewport.mjs</files>
  <action>
    Instala playwright como devDependency y corre la instalacion del binario de Chromium (npx playwright install chromium --with-deps, o el equivalente sin --with-deps si el entorno no tiene permisos de sistema para paquetes nativos adicionales — documenta cual se uso en el summary).

    Crea scripts/verify-mobile-viewport.mjs: un script Node ejecutable con node (no requiere test runner) que:
    1. Levanta o asume un servidor de desarrollo corriendo en http://localhost:3000 (usa una variable de entorno BASE_URL con fallback a ese valor; documenta en un comentario que el ejecutor debe correr npm run dev en paralelo antes de este script).
    2. Para cada viewport [{width: 375, height: 812, name: 'mobile'}, {width: 768, height: 1024, name: 'tablet'}, {width: 1280, height: 800, name: 'desktop'}]: abre una pagina de Chromium headless con ese viewport, navega a /en (o /es), y corre las siguientes aserciones, coleccionando fallos en un array en vez de abortar en el primero:
       - document.documentElement.scrollWidth no excede el viewport width en mas de un margen de 1px (sin overflow horizontal).
       - A 375px: el SheetTrigger del header (boton hamburguesa) es visible, y el NavigationMenu horizontal de desktop NO es visible.
       - A 1280px: el NavigationMenu horizontal de desktop es visible, y el SheetTrigger NO es visible.
       - El footer es visible (scrollIntoView + boundingBox no nulo), su scrollWidth tampoco excede el viewport en ningun breakpoint, y al menos un elemento de la seccion dynamicColumns (si footer.dynamicColumns tiene entradas configuradas con contenido publicado) es visible.
       - Toma un screenshot de pagina completa (page.screenshot({ fullPage: true })) por cada viewport, guardado en scripts/.mobile-verify-screenshots/{name}.png (crea el directorio si no existe; agregalo a .gitignore si no aplica versionar screenshots binarios).
    3. Al final, imprime un resumen por viewport (OK/FAIL con el detalle de cada assertion) y termina con process.exit(1) si hubo algun fallo, process.exit(0) si todo paso.

    Corre el script contra el dev server real de este proyecto y adjunta el resultado (pasa/falla, con detalle, y referencia a los 3 screenshots generados) en el summary de este plan.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && test -f scripts/verify-mobile-viewport.mjs && grep -q "375" scripts/verify-mobile-viewport.mjs && grep -q "1280" scripts/verify-mobile-viewport.mjs && node scripts/verify-mobile-viewport.mjs</automated>
  </verify>
  <done>El script existe, corre contra Chromium headless real (no una simulacion de CSS), verifica los 3 breakpoints con screenshots como evidencia, y termina en 0 (sin overflow horizontal, nav mobile/desktop correctamente exclusivos por breakpoint, footer visible con dynamicColumns si hay contenido publicado).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmacion visual final de Juan sobre header y footer</name>
  <action>No hay accion autonoma en este task — es un gate bloqueante de confirmacion visual humana sobre el resultado final de la fase.</action>
  <what-built>Header completado (10.6-01) y footer con contenido dinamico + restyle mobile-first (10.6-02), verificados automaticamente a 375/768/1280px sin overflow horizontal ni bugs de visibilidad de nav (Task 2), con screenshots adjuntos. Falta la confirmacion subjetiva: que el header/footer ya NO se sientan "planos/vacios" segun el criterio original de Juan.</what-built>
  <how-to-verify>
    1. Corre npm run dev y abre http://localhost:3000/en (o /es) en un navegador real.
    2. Reduce la ventana a ~375px de ancho (o usa las DevTools del navegador en modo dispositivo movil) y revisa: el header se ve completo con el boton hamburguesa, al abrirlo el menu muestra navegacion clara con buen espaciado; el footer se ve como un footer real (columnas apiladas, incluyendo la seccion de ultimos posts/case studies si ya hay contenido publicado con dynamicColumns configurado en /admin).
    3. Expande a desktop (~1280px) y revisa el nav horizontal completo y el footer en grid multi-columna con al menos 2 columnas manuales mas la seccion dinamica visibles simultaneamente.
    4. Revisa los 3 screenshots generados en scripts/.mobile-verify-screenshots/ como evidencia adicional si prefieres no levantar el dev server localmente.
    5. Confirma si esto ya resuelve tu feedback original ("las fuentes no me gustan, el footer no tiene nada, el header muy simple") o si falta algo puntual.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los ajustes puntuales que faltan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> devDependencies | playwright y su binario de Chromium se descargan de npm/CDN de Microsoft en install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10.6-04 | Tampering | Instalacion de playwright | mitigate | Task 1 checkpoint bloqueante: confirmar en npmjs.com/package/playwright que el autor es microsoft antes de instalar |
| T-10.6-05 | Denial of Service (leve) | Descarga de binario Chromium puede ser pesada/lenta en el entorno de ejecucion | accept | Es una devDependency de verificacion, no afecta el build de produccion; si falla la descarga, el checkpoint humano del Task 3 sigue siendo la verificacion de respaldo |

</threat_model>

<verification>
- node scripts/verify-mobile-viewport.mjs termina en 0 contra el dev server real, con screenshots generados para los 3 breakpoints.
- Juan confirma visualmente (checkpoint Task 3) que el resultado resuelve su feedback original.
</verification>

<success_criteria>
El layout mobile (~375px), tablet (~768px) y desktop (~1280px) de header y footer se verifica de forma real (Chromium headless real, con evidencia de screenshots), y Juan confirma visualmente el resultado. ROADMAP Phase 10.6 Success Criterion 3 (UI-19).
</success_criteria>

<output>
Create `.planning/phases/10.6-header-footer-completion-mobile-verification/10.6-03-SUMMARY.md` when done
</output>
</content>
