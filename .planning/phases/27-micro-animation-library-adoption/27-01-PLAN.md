---
phase: 27-micro-animation-library-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/hooks/useReducedMotion.ts
  - src/components/MotionProvider.tsx
  - src/app/(frontend)/[locale]/layout.tsx
  - src/components/ScrollReveal.tsx
  - src/blocks/FAQ/Component.tsx
  - src/components/TestimonialCardMotion.tsx
  - src/blocks/TestimonialsCarousel/Component.tsx
autonomous: false
requirements: [MOTION-01, MOTION-02]

must_haves:
  truths:
    - "motion (npm package `motion`) is installed and consumed via LazyMotion + m + domAnimation, never the full `motion` component import"
    - "A single MotionProvider wraps the app root exactly once (in the [locale] layout), so the ~20KB animation runtime is paid once, not per component"
    - "useReducedMotion() is a custom SSR-safe hook (state initializes to false, real matchMedia read happens inside useEffect, listener cleaned up on unmount) matching the HeroGrainGradient.tsx pattern exactly"
    - "FAQ items reveal (fade+rise) the first time they scroll into view, using whileInView (IntersectionObserver-backed), not a hand-rolled scroll listener"
    - "Testimonial cards show a hover micro-interaction (lift) using whileHover"
    - "The real First Load JS bundle delta from adding motion is measured via a raw `next build` (not `npm run build`, which would run `payload migrate` against the live production DB) before and after, and the actual gzipped/parsed KB numbers are written into the plan SUMMARY — not just cited from research"
  artifacts:
    - path: "src/hooks/useReducedMotion.ts"
      provides: "SSR-safe reduced-motion hook, reusable by every future animated component"
      exports: ["useReducedMotion"]
    - path: "src/components/MotionProvider.tsx"
      provides: "Root LazyMotion + MotionConfig wrapper, 'use client'"
      exports: ["MotionProvider"]
    - path: "src/components/ScrollReveal.tsx"
      provides: "Generic scroll-reveal client leaf (motion/react-m, whileInView)"
      exports: ["ScrollReveal"]
    - path: "src/components/TestimonialCardMotion.tsx"
      provides: "Generic hover-lift client leaf (motion/react-m, whileHover)"
      exports: ["TestimonialCardMotion"]
    - path: "package.json"
      provides: "motion dependency pinned to ^12.42.2"
      contains: "\"motion\":"
  key_links:
    - from: "src/app/(frontend)/[locale]/layout.tsx"
      to: "src/components/MotionProvider.tsx"
      via: "wraps {children} exactly once at the root of the public frontend tree"
      pattern: "MotionProvider"
    - from: "src/blocks/FAQ/Component.tsx"
      to: "src/components/ScrollReveal.tsx"
      via: "each FAQ item's <details> wrapped in <ScrollReveal>"
      pattern: "ScrollReveal"
    - from: "src/blocks/TestimonialsCarousel/Component.tsx"
      to: "src/components/TestimonialCardMotion.tsx"
      via: "each testimonial Card wrapped in <TestimonialCardMotion>"
      pattern: "TestimonialCardMotion"
    - from: "src/components/ScrollReveal.tsx"
      to: "src/hooks/useReducedMotion.ts"
      via: "reads reducedMotion to zero out the initial/animate delta when the user prefers reduced motion"
      pattern: "useReducedMotion"
    - from: "src/components/TestimonialCardMotion.tsx"
      to: "src/hooks/useReducedMotion.ts"
      via: "reads reducedMotion to disable the hover lift transform when the user prefers reduced motion"
      pattern: "useReducedMotion"
---

<objective>
Adopt `motion` (npm package `motion`, current `12.42.2`) as the site's single micro-animation library via the `LazyMotion` + `m` + `domAnimation` pattern, wire a root provider that pays the ~20KB runtime cost exactly once, ship a custom SSR-safe `useReducedMotion()` hook, and prove both the provider and the pattern work end-to-end on 2 real pilot components (FAQ = scroll-reveal, Testimonials = hover) already polished in Phase 26 — with the actual bundle-size delta measured against a real `next build`, not estimated from research.

Purpose: MOTION-01/MOTION-02 close the infrastructure decision so Phase 28 can roll `motion` out to the rest of the site (Hero variants, blog grids) without re-litigating the library choice or re-deriving the SSR-safe reduced-motion pattern.
Output: `motion` installed, `MotionProvider` wired into the root layout, `useReducedMotion()` hook, `ScrollReveal` + `TestimonialCardMotion` pilot leaf components, FAQ + TestimonialsCarousel updated to use them, and a documented real before/after bundle-size diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-micro-animation-library-adoption/27-CONTEXT.md
@.planning/research/STACK-v1.6.md

# Reusable SSR-safe pattern to replicate exactly for useReducedMotion()
@src/components/HeroGrainGradient.tsx

# Files this plan modifies directly
@src/app/(frontend)/[locale]/layout.tsx
@src/blocks/FAQ/Component.tsx
@src/blocks/TestimonialsCarousel/Component.tsx
@src/components/ui/card.tsx
@tailwind.config.ts

<interfaces>
<!-- Motion tokens already established in Phase 7 -- reuse verbatim, do not invent new timing values -->
From src/app/globals.css (Phase 7 tokens):
```css
--motion-fast: 150ms;
--motion-base: 250ms;
--motion-slow: 400ms;
--ease-out: cubic-bezier(0.22, 1, 0.36, 1);
--ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
```
Motion's `transition` prop takes numeric `duration` (seconds) and an `ease` array/string — convert: `duration: 0.25` (base), `ease: [0.22, 1, 0.36, 1]` (out).

From src/components/HeroGrainGradient.tsx (the pattern to replicate in the new hook):
```tsx
const [reducedMotion, setReducedMotion] = useState(false) // matches SSR (no window)
useEffect(() => {
  const mediaQuery = window.matchMedia('(prefers-reduced-motion: reduce)')
  setReducedMotion(mediaQuery.matches)
  const handleChange = (event: MediaQueryListEvent) => setReducedMotion(event.matches)
  mediaQuery.addEventListener('change', handleChange)
  return () => mediaQuery.removeEventListener('change', handleChange)
}, [])
```

From src/blocks/FAQ/Component.tsx (current, server component, no 'use client'):
```tsx
export function FAQComponent(props: FAQBlockProps) {
  const { title, faqs } = props
  return (
    <Container className="py-12">
      {title && <h2 className="font-heading text-heading mb-6">{title}</h2>}
      <div className="space-y-3">
        {faqs?.map((item, i) => (
          <details key={i} className="group rounded-lg border border-border bg-card px-6 shadow-sm hover:shadow-md transition-shadow duration-base ease-standard">
            ...
          </details>
        ))}
      </div>
    </Container>
  )
}
```
`ScrollReveal` wraps each `<details>` — the server component stays a Server Component; only `ScrollReveal` itself is `'use client'`.

From src/blocks/TestimonialsCarousel/Component.tsx (current, async server component, fetches via Local API):
```tsx
export async function TestimonialsCarouselComponent(props) {
  ...
  return (
    <Container className="py-12">
      ...
      <div className="flex gap-4 overflow-x-auto pb-4 snap-x">
        {testimonials.map((testimonial) => (
          <Card key={testimonial.id} className="min-w-[300px] snap-start">
            <CardContent className="pt-6">...</CardContent>
          </Card>
        ))}
      </div>
      ...
    </Container>
  )
}
```
`TestimonialCardMotion` wraps each `Card` — the async server component stays async/server; only the new leaf is `'use client'`.

From src/components/ui/card.tsx:
```tsx
const Card = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(...)
```
`Card` is a plain forwardRef div — wrap it in `<m.div whileHover={...}>` rather than trying to pass motion props through `Card` itself.

From src/app/(frontend)/[locale]/layout.tsx (current):
```tsx
<body className="font-sans">
  <NextIntlClientProvider>
    <SiteHeader locale={locale} />
    {children}
    <SiteFooter locale={locale} />
  </NextIntlClientProvider>
</body>
```
`MotionProvider` wraps inside `NextIntlClientProvider` (or around the whole body — either is a single root wrap; pick whichever keeps the smallest diff, both `SiteHeader`/`SiteFooter`/`children` must be inside it for Phase 28 rollout to reach them).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check — `motion`</name>
  <files>None (verification-only task, no files touched)</files>
  <action>
    Nothing built yet. This is the mandatory package-legitimacy gate before installing the phase's one new npm dependency (root CLAUDE.md flags this explicitly as "the single external dependency addition"). STACK-v1.6.md has no `## Package Legitimacy Audit` table, so per the fallback policy `motion` is treated as [ASSUMED] until manually confirmed before Task 2 installs it.
  </action>
  <what-built>Nothing yet — pre-install verification gate only.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/motion and confirm: publisher is the Framer/Motion org (motiondivision), current version resolves to `12.42.2` (or a newer 12.x), weekly downloads are in the millions, repository link points to `github.com/motiondivision/motion`.
    2. Confirm `framer-motion` on npm is listed as a legacy alias of the same package (same maintainers, same version number) — this cross-check rules out a typosquat on either name.
    3. Confirm no supply-chain advisory exists: `npm view motion@12.42.2` shows no deprecation warning.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-only npmjs.com verification, no automated check applicable to a pre-install legitimacy gate</automated>
  </verify>
  <done>Juan confirms "approved" in the thread, naming the package (`motion`) explicitly, before Task 2 runs `npm install`.</done>
  <resume-signal>Type "approved" to proceed with `npm install motion@^12.42.2`, or report if anything looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install motion, create MotionProvider + useReducedMotion, wire root layout, capture BEFORE/mid bundle baseline</name>
  <files>package.json, src/hooks/useReducedMotion.ts, src/components/MotionProvider.tsx, src/app/(frontend)/[locale]/layout.tsx</files>
  <action>
    First, capture the BEFORE baseline: run `npx next build` (NOT `npm run build` — that script runs `payload migrate` first, which would touch the live production Neon DB for a pure frontend/bundle change; use the raw Next CLI to skip migrate/generate:importmap/generate:types entirely) and save the printed route-size table (First Load JS shared by all, and the `/[locale]` route's own size) to a scratch note for later diffing — do not commit this output.

    Run `npm install motion@^12.42.2` (per D-01/research STACK-v1.6.md, resolves to `12.42.2` at time of writing; the package legitimacy checkpoint in Task 1 must be approved first).

    Create `src/hooks/useReducedMotion.ts`: a `'use client'` hook exporting `useReducedMotion(): boolean`, replicating HeroGrainGradient.tsx's exact SSR-safe pattern verbatim (state initializes to `false` to match SSR's lack of `window`, the real `matchMedia('(prefers-reduced-motion: reduce)')` read and `change` listener live inside a `useEffect`, listener removed on cleanup). This is a standalone hook, not tied to Motion's API, so it is usable by any future animated component (Motion-based or not) per MOTION-02.

    Create `src/components/MotionProvider.tsx`: `'use client'`, imports `LazyMotion`, `domAnimation`, `MotionConfig` from `motion/react`, exports `MotionProvider({ children })` rendering `<LazyMotion features={domAnimation} strict><MotionConfig reducedMotion="user">{children}</MotionConfig></LazyMotion>`. This is the sitewide belt for every `m.*` component going forward (Phase 28 rollout too) — the custom hook above is the suspenders for any animated logic that doesn't go through `m.*`.

    Wire `MotionProvider` into `src/app/(frontend)/[locale]/layout.tsx`, wrapping `<SiteHeader />{children}<SiteFooter />` (inside or around `NextIntlClientProvider`, whichever produces the smaller diff) so every route in the public frontend tree is covered exactly once.

    Capture the mid-point baseline: run `npx next build` again (same command, same DB-safety rule) and note the new First Load JS shared-by-all size — this isolates the provider's own cost, independent of the pilot components added in Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>`motion` present in package.json dependencies at `^12.42.2`; `useReducedMotion.ts` exports a hook whose implementation matches HeroGrainGradient's useState(false)+useEffect+matchMedia+cleanup shape (grep-verifiable: contains `useState(false)`, `useEffect`, `matchMedia('(prefers-reduced-motion: reduce)')`, `removeEventListener`); `MotionProvider.tsx` exports `MotionProvider` using `LazyMotion`+`domAnimation`+`MotionConfig reducedMotion="user"`; layout.tsx renders `<MotionProvider>` wrapping SiteHeader/children/SiteFooter; `npx tsc --noEmit` passes with zero errors; two `next build` route-size captures recorded (before motion, after provider wiring) for the final diff in Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Pilot components (FAQ scroll-reveal, Testimonials hover) + final bundle-size diff</name>
  <files>src/components/ScrollReveal.tsx, src/components/TestimonialCardMotion.tsx, src/blocks/FAQ/Component.tsx, src/blocks/TestimonialsCarousel/Component.tsx</files>
  <action>
    Create `src/components/ScrollReveal.tsx`: `'use client'`, imports `* as m from 'motion/react-m'` and `useReducedMotion` from `@/hooks/useReducedMotion`. Exports `ScrollReveal({ children }: { children: React.ReactNode })` rendering `<m.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.3 }} transition={reducedMotion ? { duration: 0 } : { duration: 0.5, ease: [0.22, 1, 0.36, 1] }}>{children}</m.div>` — the reduced-motion branch collapses the transition to instant rather than skipping the wrapper, so layout/DOM structure stays identical either way (same discipline as HeroGrainGradient's `motionProps` branch). Wrap each `<details>` item in `FAQ/Component.tsx`'s map with `<ScrollReveal key={i}>...</ScrollReveal>` (move the existing `key={i}` from `<details>` to `<ScrollReveal>`, `<details>` keeps its existing className/attributes unchanged).

    Create `src/components/TestimonialCardMotion.tsx`: `'use client'`, same imports. Exports `TestimonialCardMotion({ children }: { children: React.ReactNode })` rendering `<m.div whileHover={reducedMotion ? {} : { y: -4 }} transition={{ duration: 0.15, ease: [0.4, 0, 0.2, 1] }}>{children}</m.div>` (duration/ease reuse `--motion-fast`/`--ease-standard` token values). Wrap each `Card` in `TestimonialsCarousel/Component.tsx`'s map with `<TestimonialCardMotion key={testimonial.id}>...</TestimonialCardMotion>` (move the existing `key={testimonial.id}` from `Card` to `TestimonialCardMotion`, `Card`'s own className/props unchanged).

    Capture the final AFTER bundle baseline: run `npx next build` one more time (same DB-safety rule as Task 2 — raw Next CLI, not `npm run build`) and record the final First Load JS shared-by-all size plus the `/[locale]` (or home) route's own size.

    Compute and write into this plan's SUMMARY.md a table with 3 rows (before motion / after provider only / after provider+pilots) showing the real KB deltas at each step, compared explicitly against STACK-v1.6.md's ~19-20KB estimate.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>`ScrollReveal` and `TestimonialCardMotion` exist, both import from `motion/react-m` (not the full `motion` import) and both call `useReducedMotion()`; FAQ items are wrapped in `ScrollReveal`, testimonial cards are wrapped in `TestimonialCardMotion`; `npx tsc --noEmit` passes; a live `npm run dev` + curl of `/` and `/en` shows both pages render with zero hydration-mismatch warnings in the dev server log and the FAQ/testimonials markup unchanged apart from the new wrapper divs; final `npx next build` succeeds and its route-size table is captured; the real before/after/after-pilots KB numbers are written into the plan SUMMARY.md, not just the research estimate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> node_modules | `motion` is the one new external package this phase pulls in; a compromised or typosquatted package would execute arbitrary code at build/runtime |
| User's `prefers-reduced-motion` OS setting -> rendered animation | Client-side JS reads a browser API; failure mode is a UX/accessibility miss (motion not respected), not a security issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-27-SC | Tampering | `npm install motion` | mitigate | Blocking `checkpoint:human-verify` (Task 1) before install — confirms publisher, version, download volume, and the `framer-motion` alias cross-check on npmjs.com before the package ever enters `node_modules` |
| T-27-01 | Information Disclosure | `MotionProvider.tsx` / `ScrollReveal.tsx` / `TestimonialCardMotion.tsx` | accept | Pure presentational client components, no user input, no data fetching, no secrets in scope — zero attack surface beyond the supply-chain concern already covered by T-27-SC |
| T-27-02 | Denial of Service (perf) | Root `MotionProvider` wrapping every route | mitigate | `LazyMotion`+`domAnimation` (not `domMax`/full `motion` import) keeps the shared runtime to the smallest sufficient feature set (~19-20KB per research); Task 2/3's real `next build` measurement is the actual gate — if the measured delta materially exceeds the research estimate, that is a finding to surface in the SUMMARY, not silently accept |

</threat_model>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` — zero errors across all 8 touched/created files.
2. `git diff --stat` — confirms zero changes to `payload-types.ts`, any `config.ts`, or any migration file (this phase has zero schema/DB impact, per CLAUDE.md's explicit confirmation requirement).
3. Live `npm run dev` + curl of `/` and `/en` — FAQ and Testimonials sections render, no hydration-mismatch warnings in the dev server log.
4. Three `next build` captures (before motion / after provider / after pilots) exist and their real KB deltas are documented in the plan SUMMARY, compared against STACK-v1.6.md's ~19-20KB estimate.
</verification>

<success_criteria>
- `motion` installed and consumed exclusively via `LazyMotion`+`m`+`domAnimation` (grep confirms no `import { motion } from 'motion/react'` full-component import anywhere in `src/`)
- `MotionProvider` wraps the app root exactly once in `src/app/(frontend)/[locale]/layout.tsx`
- `useReducedMotion()` hook exists, matches HeroGrainGradient's SSR-safe shape, and is consumed by both pilot components
- FAQ items scroll-reveal, testimonial cards show a hover lift, both verified live with zero hydration warnings
- Real bundle-size delta measured via raw `next build` (not `npm run build`) and documented in the SUMMARY with actual KB numbers
</success_criteria>

<output>
Create `.planning/phases/27-micro-animation-library-adoption/27-01-SUMMARY.md` when done
</output>
