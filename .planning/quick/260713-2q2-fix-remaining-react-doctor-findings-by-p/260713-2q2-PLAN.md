---
phase: quick-260713-2q2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/JsonLd.tsx
  - src/app/actions/contact.ts
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
  - src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
  - src/app/(frontend)/[locale]/case-studies/page.tsx
  - src/blocks/AboutSection/Component.tsx
  - src/blocks/CallToAction/Component.tsx
  - src/blocks/ContactFormBlock/Component.tsx
  - src/blocks/Content/Component.tsx
  - src/blocks/FAQ/Component.tsx
  - src/blocks/Hero/Component.tsx
  - src/blocks/ResultsSection/Component.tsx
  - src/components/AuthorCard.tsx
  - src/app/(frontend)/[locale]/blog/[slug]/page.tsx
  - src/blocks/MediaBlock/Component.tsx
  - src/blocks/Section/Component.tsx
  - src/components/PostCard.tsx
  - src/app/(frontend)/[locale]/blog/page.tsx
  - src/app/(frontend)/[locale]/contact/page.tsx
  - src/search/beforeSync.ts
  - src/app/sitemap.html/route.ts
  - src/blocks/ArchiveBlock/Component.tsx
  - src/components/HeroGrainGradient.tsx
  - src/fields/IconPicker/Component.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/button.tsx
  - src/components/ui/navigation-menu.tsx
  - src/blocks/RenderBlocks.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "JsonLd.tsx's script-tag XSS vector is confirmed closed (already escaped) and documented, not silently re-broken"
    - "sendContactMessage stays callable without login (it is an intentionally public contact form) but gains a real abuse-protection gap-fix: per-IP rate limiting"
    - "None of the 19 no-array-index-as-key sites use raw array index as key where a stable Payload-assigned id or other unique value is available"
    - "All 7 next/image fill instances ship a sizes attribute matched to that image's real rendered width in its layout"
    - "blog/page.tsx and contact/page.tsx no longer await two independent async calls sequentially"
    - "beforeSync.ts's category-lookup loop no longer awaits payload.findByID one at a time"
    - "IconPicker's autofocus is removed"
    - "npx react-doctor@latest --verbose no longer reports any of the targeted rules except a documented false positive"
    - "npx tsc --noEmit passes with zero errors after all changes"
  artifacts:
    - path: "src/components/JsonLd.tsx"
      provides: "Confirmed/hardened HTML-safe JSON-LD injection (escapeForScriptTag already escapes <, >, &)"
    - path: "src/app/actions/contact.ts"
      provides: "Public contact server action with honeypot + validation + new per-IP rate limit"
  key_links:
    - from: "src/app/actions/contact.ts"
      to: "next/headers"
      via: "headers() read of x-forwarded-for/x-real-ip for rate-limit keying"
      pattern: "headers\\(\\)"
---

<objective>
Close the remaining triaged react-doctor findings in juan-payload (44 of the 68 raw diagnostics — the other 24 are already-documented false positives / a deferred Next-16 blocker / an already-removed dependency from prior quick tasks 260713-1zw and 260713-2ce), in the priority order Juan specified: security first, then real bugs (auth-action investigation, array-index keys, image sizes, sequential awaits), then performance, then accessibility, then maintainability.

Purpose: this project's CLAUDE.md states performance and SEO are the core value proposition of the site ("si el rendimiento... falla, el sitio no cumple su proposito") — the image-sizes and performance findings are not style nitpicks here, they're load-bearing for the site's stated purpose. The array-index-key and unsafe-json-in-html findings are real bug/vulnerability classes on a CMS-driven site where lists reorder/filter based on admin content.

Output: 27 files modified across security, bug, performance, accessibility, and maintainability fixes; final task re-runs `npx react-doctor@latest --verbose` and `npx tsc --noEmit` to confirm closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/quick/260713-2q2-fix-remaining-react-doctor-findings-by-p/diagnostics.json

Pre-planning investigation already completed (do NOT re-run this analysis, execute against these findings):

1. `src/components/JsonLd.tsx` (unsafe-json-in-html) is ALREADY FIXED. The file currently defines
   `escapeForScriptTag(json)` which replaces `<`, `>`, and `&` with unicode escapes, and the `<script>`
   tag's `dangerouslySetInnerHTML` calls `escapeForScriptTag(JSON.stringify(data))`, not raw
   `JSON.stringify`. This was fixed in a prior phase's code review (comment references "22-REVIEW
   WR-01"). react-doctor's static rule pattern-matches on `JSON.stringify` textually and does not trace
   through the wrapper function's escaping — this is a stale/false-positive finding against current
   code. No functional change needed; verify, harden the doc comment, and add a cheap standalone
   verification script.

2. `src/app/actions/contact.ts` (server-auth-actions) already has a honeypot field
   (`company_website`) and input validation (required fields + email regex) — this is by design an
   unauthenticated public contact-form action, not a bug requiring an auth check. What is genuinely
   missing: rate limiting. This is the real gap to close (per Juan's own priority instructions: "rate
   limiting is the closest true fix for an intentionally-public write action").

3. Payload array-type fields in this codebase always carry an auto-generated `id?: string | null` on
   each row (confirmed via `src/payload-types.ts` for `CaseStudies.kpis/challenge/solution/
   results.metrics`, `Authors.expertise/education/experience/credentials/socialLinks`,
   `HeroBlock.breadcrumbs`, `ContactFormBlock.contactInfo`, and every other CMS-driven array field
   checked during planning). The fix pattern for the vast majority of the 19 no-array-index-as-key
   sites is `key={item.id ?? i}` (id as primary key, index only as a defensive fallback for the rare
   case Payload hasn't assigned one yet) — not a blanket removal of index entirely. Two sites (the
   `trail.map` breadcrumb loops in `case-studies/[slug]/page.tsx:105` and `case-studies/page.tsx:44`)
   render a locally computed, non-Payload `BreadcrumbItem[]` (`src/lib/breadcrumbs.ts`) with no `id`
   field, only `{ label, url }` — for these, use `key={crumb.url}` (guaranteed unique per row, stable
   across re-renders) instead of index.

4. No test framework (vitest/jest) is installed in this project (confirmed: no vitest/jest
   dependency, no `*.test.*` files, no test script in `package.json`). Verification for this plan uses
   grep, `npx tsc --noEmit`, and `npx react-doctor@latest --verbose` — consistent with the two prior
   react-doctor quick-task passes (260713-1zw, 260713-2ce). Do not introduce a new test framework as
   part of this plan — that is out of scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden JsonLd.tsx XSS fix (unsafe-json-in-html)</name>
  <files>src/components/JsonLd.tsx</files>
  <action>
    Read src/components/JsonLd.tsx and confirm escapeForScriptTag already replaces &lt;, &gt;, and &amp;
    before the JSON is injected via dangerouslySetInnerHTML. This closes the actual XSS vector
    react-doctor's unsafe-json-in-html rule (diagnostics.json, src/components/JsonLd.tsx line 13) warns
    about — the finding is a false positive against current code, fixed in a prior phase's code review.
    Do not change the escaping logic. Strengthen the existing top-of-file security comment to explicitly
    name the react-doctor rule (unsafe-json-in-html) this satisfies, so a future scan or reviewer does
    not re-flag or "fix" this into a regression. Then add one throwaway verification script at
    scripts/verify-jsonld-escape.mjs: a standalone plain-JS file (no TS/app imports — copy the same
    3-step replace chain inline) that runs the escape logic against a payload containing a literal
    `</script>` tag, an `<img onerror=x>` fragment, and a bare `&`, then asserts the output contains no
    unescaped `<`, `>`, or `&` characters, exiting non-zero on failure. Run it once with node to confirm
    it passes, then leave it in place as a regression check for future runs.
  </action>
  <verify>
    <automated>grep -q "escapeForScriptTag" src/components/JsonLd.tsx && node scripts/verify-jsonld-escape.mjs && echo OK</automated>
  </verify>
  <done>JsonLd.tsx's escaping function is confirmed intact and unchanged, the security comment explicitly references the unsafe-json-in-html react-doctor rule it satisfies, and scripts/verify-jsonld-escape.mjs passes confirming the escape chain neutralizes script-breakout payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate server-auth-actions finding and add per-IP rate limiting to the contact action</name>
  <files>src/app/actions/contact.ts</files>
  <action>
    Read src/app/actions/contact.ts in full first. Confirm it already has: a honeypot field
    (company_website) that silently drops bot submissions, required-field plus email-regex validation,
    and HTML-escaping of user input before it reaches the outbound email body. Document in the plan
    SUMMARY that react-doctor's server-auth-actions finding (diagnostics.json, src/app/actions/
    contact.ts:26, sendContactMessage) is a false positive on the "needs auth" premise — a public
    contact form is intentionally callable without login, that is the entire point of the feature, and
    adding a fake auth check would break the form for every real visitor. Do NOT add any auth/session
    check.
    What IS a real, currently-missing gap per Juan's priority list: no rate limiting, so a script could
    hammer the Resend-backed email send in a tight loop. Add a minimal in-memory per-IP rate limiter —
    this runs as a single persistent Node process on Hostinger, not serverless, so a module-level Map
    survives between requests within the process, which is sufficient for a low-traffic portfolio
    contact form. Do not add Redis or any external dependency. Implementation: a module-level
    `const submissionLog = new Map<string, number[]>()` storing recent submission timestamps per client
    IP; read the IP via `next/headers`'s `headers()` (`x-forwarded-for`'s first entry, falling back to
    `x-real-ip`, falling back to the literal string `unknown`); before the existing honeypot/validation
    checks, prune timestamps older than a 10-minute window for that IP's entry and redirect to
    `${redirectBase}?sent=false` if 5 or more submissions remain in that window, otherwise push the
    current timestamp and continue. Keep it simple: no sliding-window precision beyond a basic prune,
    no exported config, no new npm dependency.
  </action>
  <verify>
    <automated>grep -q "x-forwarded-for" src/app/actions/contact.ts && grep -q "submissionLog" src/app/actions/contact.ts && npx tsc --noEmit 2>&1 | grep -c "actions/contact.ts" | grep -qx 0 && echo OK</automated>
  </verify>
  <done>sendContactMessage remains callable without authentication (documented as a false positive on the auth premise) and now rejects the 6th-plus submission from the same IP within a 10-minute window; npx tsc --noEmit reports zero errors in this file.</done>
</task>

<task type="auto">
  <name>Task 3: Fix no-array-index-as-key — page-level dynamic content lists (9 sites, 3 files)</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx, src/app/(frontend)/[locale]/case-studies/page.tsx</files>
  <action>
    Fix each site individually — do not blanket-apply one strategy. Exact sites from diagnostics.json:

    authors/[slug]/page.tsx: line 183 `Badge key={i}` over `doc.expertise.map((item, i) => ...)`,
    change to `key={item.id ?? i}` (Authors.expertise[] has id). Line 200 `Card key={i}` over
    `doc.education.map((item, i) => ...)`, change to `key={item.id ?? i}` (Authors.education[] has id).
    Line 238 `li key={i}` over `doc.experience.map((item, i) => ...)`, change to `key={item.id ?? i}`
    (Authors.experience[] has id).

    case-studies/[slug]/page.tsx: line 105 `li key={i}` over `trail.map((crumb, i) => ...)` — trail is
    a locally computed BreadcrumbItem[] ({label, url}, no id, from buildCaseStudiesTrail()), change to
    `key={crumb.url}` (never index here, url is always unique and present). Line 141 `div key={i}` over
    `doc.kpis.map((kpi, i) => ...)`, change to `key={kpi.id ?? i}` (CaseStudies.kpis[] has id). Line 168
    `li key={i}` over `doc.challenge.map((item, i) => ...)`, change to `key={item.id ?? i}`
    (CaseStudies.challenge[] has id). Line 179 `li key={i}` over `doc.solution.map((step, i) => ...)`,
    change to `key={step.id ?? i}` (CaseStudies.solution[] has id). Line 205 `div key={i}` over
    `doc.results.metrics.map((metric, i) => ...)`, change to `key={metric.id ?? i}`
    (CaseStudies.results.metrics[] has id).

    case-studies/page.tsx: line 44 `li key={i}` over `trail.map((crumb, i) => ...)` — same locally
    computed BreadcrumbItem[] as above, change to `key={crumb.url}`.

    After editing, re-read each changed block to confirm only the `key` prop changed, no other
    prop/logic in the map callback was touched.
  </action>
  <verify>
    <automated>grep -rn "key={i}" "src/app/(frontend)/[locale]/authors/[slug]/page.tsx" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" "src/app/(frontend)/[locale]/case-studies/page.tsx" | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <done>All 9 listed sites use a stable key (Payload id with index fallback, or crumb.url for computed breadcrumbs); grep for the literal pattern key={i} in these 3 files returns zero matches.</done>
</task>

<task type="auto">
  <name>Task 4: Fix no-array-index-as-key — content block components, batch A (5 sites, 4 files)</name>
  <files>src/blocks/AboutSection/Component.tsx, src/blocks/CallToAction/Component.tsx, src/blocks/ContactFormBlock/Component.tsx, src/blocks/Content/Component.tsx</files>
  <action>
    Fix each site individually. Exact sites from diagnostics.json:

    AboutSection/Component.tsx: line 31 `p key={i}` over `paragraphs?.map((paragraph, i) => ...)` — read
    the surrounding code first to confirm the actual Payload field name/shape backing `paragraphs`
    (AboutSection's bio-paragraphs array) and whether each row has an id; if it is a genuine Payload
    array field, change to `key={paragraph.id ?? i}`, otherwise use the most stable available value
    (e.g. paragraph.text) documented inline. Line 55 `div key={i}` over
    `features.map((item, i) => ...)` — AboutSection.features[] is a Payload array field, change to
    `key={item.id ?? i}`.

    CallToAction/Component.tsx: line 26 `div key={i}` over `links.map(({ link }, i) => ...)` —
    CallToAction.links[] is a Payload array field wrapping a link group; change to
    `key={link.id ?? i}` if the destructured row itself carries an id, otherwise destructure the row
    without dropping its id (`links.map((row, i) => { const { link } = row; return <div
    key={row.id ?? i}> ... } )`) — read the actual payload-types.ts shape for CallToAction.links before
    picking the exact destructure to avoid breaking the existing `{ link }` destructuring pattern.

    ContactFormBlock/Component.tsx: line 105 `li key={i}` over `contactInfo?.map((info, i) => ...)` —
    ContactFormBlock.contactInfo[] is a Payload array field with id; change to `key={info.id ?? i}`.

    Content/Component.tsx: line 36 `div key={i}` over the columns map (read the file to confirm the
    exact source array name, likely `columns.map((col, i) => ...)` per the file's existing `col`
    variable naming) — Content's column array is a Payload array field with id; change to
    `key={col.id ?? i}`.

    After editing, re-read each changed block to confirm only the key prop changed.
  </action>
  <verify>
    <automated>grep -rn "key={i}" src/blocks/AboutSection/Component.tsx src/blocks/CallToAction/Component.tsx src/blocks/ContactFormBlock/Component.tsx src/blocks/Content/Component.tsx | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <done>All 6 listed sites use a stable key derived from the underlying Payload array row's id (with index fallback); grep for key={i} in these 4 files returns zero matches.</done>
</task>

<task type="auto">
  <name>Task 5: Fix no-array-index-as-key — remaining block/card components (5 sites, 4 files)</name>
  <files>src/blocks/FAQ/Component.tsx, src/blocks/Hero/Component.tsx, src/blocks/ResultsSection/Component.tsx, src/components/AuthorCard.tsx</files>
  <action>
    Fix each site individually. Exact sites from diagnostics.json:

    FAQ/Component.tsx: line 22 `ScrollReveal key={i}` over `faqs?.map((item, i) => ...)` — FAQ.faqs[]
    is a Payload array field with id; change to `key={item.id ?? i}`.

    Hero/Component.tsx: line 84, currently `li key={crumb.id ?? i}` over the breadcrumbs map (this site
    already has a partial fix using HeroBlock.breadcrumbs[].id with an index fallback, which is why
    react-doctor still flags the literal index-fallback pattern). Tighten this: HeroBlock.breadcrumbs[]
    rows always carry both an id and a url; change the fallback from index to
    `key={crumb.id ?? crumb.url}` so index is never used as a key anywhere in this component.

    ResultsSection/Component.tsx: line 22 `div key={i}` over `stats?.map((stat, i) => ...)` —
    ResultsSection.stats[] is a Payload array field with id; change to `key={stat.id ?? i}`.

    AuthorCard.tsx: line 77 `Badge key={i}` over `author.credentials.map((c, i) => ...)` —
    Authors.credentials[] has id, change to `key={c.id ?? i}`. Line 94 `a key={i}` over
    `author.socialLinks.map((social, i) => ...)` — Authors.socialLinks[] has id, change to
    `key={social.id ?? i}`.

    After editing, re-read each changed block to confirm only the key prop changed.
  </action>
  <verify>
    <automated>grep -rn "key={i}" src/blocks/FAQ/Component.tsx src/blocks/Hero/Component.tsx src/blocks/ResultsSection/Component.tsx src/components/AuthorCard.tsx | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <done>All 4 listed sites use a stable key (Payload id with index fallback, or id-with-url fallback for Hero's breadcrumbs); grep for key={i} in these 4 files returns zero matches.</done>
</task>

<task type="auto">
  <name>Task 6: Fix nextjs-image-missing-sizes — add sizes to all 7 next/image fill instances</name>
  <files>src/app/(frontend)/[locale]/blog/[slug]/page.tsx, src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx, src/blocks/AboutSection/Component.tsx, src/blocks/Hero/Component.tsx, src/blocks/MediaBlock/Component.tsx, src/blocks/Section/Component.tsx, src/components/PostCard.tsx</files>
  <action>
    Add a sizes attribute to each of the 7 fill-mode next/image instances, matched to that image's real
    rendered width in its layout context. Read each surrounding container's className before picking a
    value — do not copy-paste one sizes value everywhere.

    blog/[slug]/page.tsx line 104: Image with fill className="object-cover" priority inside
    div className="relative aspect-[21/9] w-full" under a full-width section (post hero banner,
    edge-to-edge). Add sizes="100vw" — this image spans the full viewport width at every breakpoint.

    case-studies/[slug]/page.tsx line 97: same full-bleed hero pattern
    (aspect-[21/9] w-full inside a full-width section). Add sizes="100vw".

    AboutSection/Component.tsx line 40-45: Image inside div className="relative aspect-square
    w-full ..." nested under a md:col-span-5 column of a md:grid-cols-12 grid (photo occupies
    roughly 5/12 of the container width on desktop, full width below the md breakpoint, inside a
    Container with standard max-width). Add sizes="(min-width: 768px) 41vw, 100vw" (5/12 rounds to
    about 41 percent).

    Hero/Component.tsx line 42: Image with fill className="object-cover" priority inside
    div className="absolute inset-0 opacity-30" — a full-bleed background image behind the entire
    Hero section. Add sizes="100vw".

    MediaBlock/Component.tsx line 16: read the containing element's className directly above this
    Image (the block's own wrapper) to confirm whether MediaBlock renders full-width or constrained;
    if the wrapper has no explicit width constraint beyond the page Container, add sizes="100vw"; if
    it's rendered inside a narrower column context (check callers of MediaBlock in RenderBlocks.tsx
    and any grid/column wrapper), use that column's actual fraction instead — verify before choosing.

    Section/Component.tsx line 47: Image with fill className="object-cover" inside
    div className="absolute inset-0 -z-10" when backgroundStyle === 'image' — this is a full-bleed
    section background image. Add sizes="100vw".

    PostCard.tsx line 17: Image inside a card thumbnail (grid card in blog/archive listings — check
    the card's wrapper className for its grid column span, e.g. a 3-column card grid puts each card at
    roughly 33vw on desktop, narrower below breakpoints). Read the actual grid className the card sits
    in (ArchiveBlock/Component.tsx and any other caller) and set a sizes value reflecting the card's
    real column fraction at each breakpoint, e.g.
    sizes="(min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw" if it is a responsive 1/2/3
    column card grid — confirm the actual column counts used before finalizing this value.
  </action>
  <verify>
    <automated>for f in "src/app/(frontend)/[locale]/blog/[slug]/page.tsx" "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" src/blocks/AboutSection/Component.tsx src/blocks/Hero/Component.tsx src/blocks/MediaBlock/Component.tsx src/blocks/Section/Component.tsx src/components/PostCard.tsx; do grep -q "sizes=" "$f" || echo "MISSING:$f"; done | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <done>All 7 files have a sizes attribute on their fill-mode next/image usage, each value reflecting that image's actual rendered width in its layout (full-bleed images get 100vw, column/card images get breakpoint-aware fractional values), not a single copy-pasted value.</done>
</task>

<task type="auto">
  <name>Task 7: Fix server-sequential-independent-await — parallelize params/searchParams reads</name>
  <files>src/app/(frontend)/[locale]/blog/page.tsx, src/app/(frontend)/[locale]/contact/page.tsx</files>
  <action>
    Both files currently do const { locale } = await params immediately followed by
    const { category } = await searchParams (blog/page.tsx line 38) or
    const { sent } = await searchParams (contact/page.tsx line 43) — two independent promises awaited
    one after another with no data dependency between them. In each file, replace the two sequential
    awaits with a single Promise.all, e.g.
    const [{ locale }, { category }] = await Promise.all([params, searchParams]) for blog/page.tsx,
    and the equivalent destructuring for contact/page.tsx's locale/sent pair. Keep every
    other line in each function unchanged — this is a pure await-restructuring, no logic changes.
  </action>
  <verify>
    <automated>grep -q "Promise.all(\[params, searchParams\])" "src/app/(frontend)/[locale]/blog/page.tsx" && grep -q "Promise.all(\[params, searchParams\])" "src/app/(frontend)/[locale]/contact/page.tsx" && echo OK</automated>
  </verify>
  <done>Both files resolve params and searchParams concurrently via Promise.all instead of two sequential awaits; npx tsc --noEmit shows no new errors in either file.</done>
</task>

<task type="auto">
  <name>Task 8: Performance batch — hoist Intl formatters, flatMap, combine iterations, hydration flicker, async-await-in-loop</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/sitemap.html/route.ts, src/blocks/ArchiveBlock/Component.tsx, src/components/HeroGrainGradient.tsx, src/search/beforeSync.ts</files>
  <action>
    Before implementing, curl each rule's canonical recipe from the react-doctor help URL/message in
    diagnostics.json for exact wording, then apply:

    js-hoist-intl (authors/[slug]/page.tsx lines 31 and 41): formatDateRange and formatEventDate
    both call new Intl.DateTimeFormat(locale, {...}) inline on every invocation, and locale is a
    runtime parameter (not a module constant), so the formatter cannot be hoisted to a single top-level
    instance. Instead, memoize per-locale: add a module-level
    const dateRangeFormatters = new Map<string, Intl.DateTimeFormat>() (and a second Map for the
    event-date formatter's distinct options), and inside each function, look up or lazily create-and-
    cache the formatter for the given locale key instead of constructing a new one every call. Since
    this codebase only ever uses 'es'/'en', the cache stays at most 2 entries.

    js-flatmap-filter (sitemap.html/route.ts line 54): groups.map((group) => {...; if
    (groupEntries.length === 0) return null; ...}).filter(Boolean) makes two passes. Replace with a
    single .flatMap((group) => {...; return groupEntries.length === 0 ? [] : [sectionHtml]})
    (returning an empty array to drop, a one-element array to keep), removing the trailing
    .filter(Boolean).

    js-combine-iterations (ArchiveBlock/Component.tsx line 40): selectedDocs.map((d) => d.value as
    Post | CaseStudy).filter((d) => typeof d === 'object') makes two passes. Replace with
    selectedDocs.flatMap((d) => (typeof d.value === 'object' ? [d.value as Post | CaseStudy] : []))
    (single pass, same resulting array).

    rendering-hydration-no-flicker (HeroGrainGradient.tsx line 185): the useEffect(() => {
    setIsDark(document.documentElement.classList.contains('dark')) }, []) flashes light-then-dark on
    first paint. Fix per the rule's recipe — prefer useSyncExternalStore if the codebase already has
    a comparable subscribe/getSnapshot pattern nearby (check the existing reducedMotion matchMedia
    effect just above for a reusable pattern), otherwise add suppressHydrationWarning to the element
    whose class depends on isDark as the simpler fix, since the color values only affect a decorative
    shader background, not layout-shifting content.

    async-await-in-loop (search/beforeSync.ts line 49): the for (const category of categories) { ...
    await req.payload.findByID(...) } loop awaits one category lookup at a time. Refactor to collect
    the categories needing a lookup (skip ones already populated as objects, same as today), run
    await Promise.all(idsToLookup.map((id) => req.payload.findByID({...}))), then merge the results
    back into populatedCategories preserving the original input order (do not just concat — objects
    already present and freshly-looked-up ones must end up in the same relative order as the source
    categories array, since this feeds search-index category display order).
  </action>
  <verify>
    <automated>grep -q "dateRangeFormatters" "src/app/(frontend)/[locale]/authors/[slug]/page.tsx" && grep -q "flatMap" src/app/sitemap.html/route.ts && grep -q "flatMap" src/blocks/ArchiveBlock/Component.tsx && grep -q "Promise.all" src/search/beforeSync.ts && npx tsc --noEmit 2>&1 | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <done>All 5 files apply their rule's canonical fix (memoized Intl formatters, single-pass flatMap replacing map+filter in both files, hydration-safe isDark handling, and Promise.all-based category lookup preserving original order); npx tsc --noEmit passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 9: Fix no-autofocus in IconPicker admin field</name>
  <files>src/fields/IconPicker/Component.tsx</files>
  <action>
    Line 101 has an autoFocus prop (diagnostics.json: "autoFocus moves focus on load, which can
    disrupt screen reader and keyboard users"). Read the surrounding element (likely the icon-search
    input inside the picker's popup/modal) and remove the autoFocus prop entirely — let the admin
    user tab or click into the field themselves, per the rule's canonical recipe. Do not replace it
    with a manual ref.current?.focus() in a useEffect — that reintroduces the same disorientation
    problem the rule flags; simply drop autofocus behavior.
  </action>
  <verify>
    <automated>grep -c "autoFocus" src/fields/IconPicker/Component.tsx | grep -qx 0 && echo OK</automated>
  </verify>
  <done>autoFocus no longer appears anywhere in IconPicker/Component.tsx; the icon-search input behaves like a normal input on modal open (no forced focus jump).</done>
</task>

<task type="auto">
  <name>Task 10: Maintainability batch — only-export-components, no-inline-exhaustive-style, circular-dependency</name>
  <files>src/components/ui/badge.tsx, src/components/ui/button.tsx, src/components/ui/navigation-menu.tsx, src/fields/IconPicker/Component.tsx, src/blocks/RenderBlocks.tsx, src/blocks/Section/Component.tsx</files>
  <action>
    only-export-components (badge.tsx line 36, button.tsx line 57, navigation-menu.tsx line 119): each
    file exports a non-component value (a cva variants function, e.g. badgeVariants/
    buttonVariants/similar in navigation-menu.tsx) alongside its component(s), which breaks Fast
    Refresh state preservation. Move each non-component export into a sibling file in the same
    directory (e.g. src/components/ui/badge-variants.ts, button-variants.ts,
    navigation-menu-variants.ts), re-export nothing extra from the original component file, and
    update every importer of the moved export (grep each variants export name across src/ to find
    all call sites) to import from the new file instead. Do not change any variant/className logic —
    this is a pure file-split refactor.

    no-inline-exhaustive-style (IconPicker/Component.tsx lines 40, 65, 131): three large inline style
    objects (9-10 properties each) rebuild on every render. Move each to a Tailwind utility class
    string where a direct utility exists, or to a small named constant object declared once at module
    scope (outside the component function) if the values are static and don't depend on props/state —
    read each one first to check whether it depends on component state/props (if so, keep it as a
    computed style but hoist any static sub-parts out); do not silently drop any visual property.

    circular-dependency (RenderBlocks.tsx and Section/Component.tsx): break the import cycle by
    extracting the shared code both files depend on into a third module. Read both files' imports of
    each other first to identify exactly what's shared (likely a shared type or a block-rendering
    helper) and extract only that into a new file (e.g. src/blocks/shared-types.ts or similar,
    named after what's actually extracted), then update both files' imports to pull from the new
    module instead of from each other.
  </action>
  <verify>
    <automated>! grep -q "^export.*badgeVariants" src/components/ui/badge.tsx && ! grep -q "^export.*buttonVariants" src/components/ui/button.tsx && ! grep -q "^export.*NavigationMenu.*Variants\|^export.*navigationMenuTriggerStyle" src/components/ui/navigation-menu.tsx && npx tsc --noEmit 2>&1 | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <done>The 3 UI primitive files no longer export non-component values directly (variants moved to sibling files, all importers updated), IconPicker's 3 large inline style objects are reduced to Tailwind classes or hoisted static constants, and RenderBlocks.tsx no longer imports from Section/Component.tsx in a cycle (shared code extracted to a third module); npx tsc --noEmit passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 11: Final verification — re-run react-doctor and confirm build</name>
  <files>none (verification only)</files>
  <action>
    Run npx tsc --noEmit and confirm zero errors across the whole project (not just the touched
    files). Run npx react-doctor@latest --verbose and inspect the full output. Confirm each of the
    following no longer appears, except where explicitly documented as a false positive in this plan's
    SUMMARY: unsafe-json-in-html (documented false positive — verify it's either absent or, if still
    reported due to react-doctor's static-only analysis, explicitly noted as such), server-auth-actions
    (documented false positive on the auth premise, real gap closed via rate limiting), no-array-index-
    as-key (all 19 sites), nextjs-image-missing-sizes (all 7 sites), server-sequential-independent-await
    (both sites), js-hoist-intl, js-flatmap-filter, js-combine-iterations, rendering-hydration-no-
    flicker, async-await-in-loop, no-autofocus, only-export-components (all 3 sites), no-inline-
    exhaustive-style (all 3 sites), circular-dependency. Also confirm the previously-documented 23
    unused-file findings and the deferred no-vulnerable-react-server-components finding are unchanged
    (not newly regressed by this plan's edits) — these remain explicitly out of scope per the task
    description. Run npm run build if time/environment allows for a full production-build sanity
    check; at minimum npx tsc --noEmit must pass.
  </action>
  <verify>
    <automated>npx react-doctor@latest --verbose 2>&1 | grep -v '^#' | grep -Ec 'no-array-index-as-key|nextjs-image-missing-sizes|server-sequential-independent-await|js-hoist-intl|js-flatmap-filter|js-combine-iterations|rendering-hydration-no-flicker|async-await-in-loop|no-autofocus|only-export-components|no-inline-exhaustive-style|circular-dependency' | grep -qx 0 && echo CLEAN</automated>
  </verify>
  <done>npx tsc --noEmit passes with zero errors project-wide; npx react-doctor@latest --verbose no longer reports any of the 12 mechanically-verifiable target rules; unsafe-json-in-html and server-auth-actions dispositions are documented in the SUMMARY with reasoning; the 23 pre-existing unused-file findings and the deferred RSC-CVE finding are confirmed unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public visitor -> sendContactMessage server action | Unauthenticated form submission crosses into a server action that sends email and echoes input into an HTML email body |
| Any CMS-authored field -> JsonLd/RichText/HTML sinks | Admin-entered content (Payload) crosses into HTML/script injection sinks on public pages |
| Admin (IconPicker) -> browser focus/DOM | Payload admin UI field crosses into direct DOM/style manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-260713-2q2-01 | Tampering | src/components/JsonLd.tsx | mitigate | escapeForScriptTag already escapes &lt;, &gt;, &amp; before dangerouslySetInnerHTML injection — verified intact in Task 1, doc comment hardened to prevent future regression |
| T-260713-2q2-02 | Denial of Service | src/app/actions/contact.ts (sendContactMessage) | mitigate | Per-IP in-memory rate limit (5 submissions / 10 min) added in Task 2, on top of existing honeypot + validation |
| T-260713-2q2-03 | Information Disclosure | src/app/actions/contact.ts email body | accept | Existing escapeHtml() already neutralizes markup injection into the outbound email; unchanged by this plan |
| T-260713-2q2-04 | Tampering | src/fields/IconPicker/Component.tsx (admin-only) | accept | Admin-only surface (Payload /admin, authenticated by Payload's own auth), autofocus removal is an accessibility fix not a security boundary |
| T-260713-2q2-SC | Tampering | npm installs | not applicable | No new package-manager installs in this plan — all fixes use existing dependencies (next/headers, existing Payload types) |
</threat_model>

<verification>
Run npx react-doctor@latest --verbose after all tasks complete and confirm:
1. No unsafe-json-in-html finding (or, if react-doctor's static pattern still flags it, confirm it is documented as a verified false positive in the SUMMARY)
2. No server-auth-actions finding on sendContactMessage, OR if still flagged, confirmed documented as a false positive (public form) with the real gap (rate limiting) closed
3. Zero no-array-index-as-key findings across the 11 files touched in Tasks 3-5
4. Zero nextjs-image-missing-sizes findings across the 7 files touched in Task 6
5. Zero server-sequential-independent-await findings in blog/page.tsx and contact/page.tsx
6. Zero js-hoist-intl, js-flatmap-filter, js-combine-iterations, rendering-hydration-no-flicker, async-await-in-loop findings
7. Zero no-autofocus finding
8. Zero only-export-components and no-inline-exhaustive-style findings
9. Zero circular-dependency finding on RenderBlocks.tsx and Section/Component.tsx
10. The 23 pre-existing unused-file findings and the deferred no-vulnerable-react-server-components (CVE-2026-23870) finding are unchanged (not a regression introduced by this plan)

Run npx tsc --noEmit and confirm zero errors project-wide.
</verification>

<success_criteria>
- JsonLd.tsx's XSS escaping confirmed intact, doc comment hardened, standalone regression script passes
- sendContactMessage keeps working unauthenticated (documented as intentional) and gains a per-IP rate limit
- All 19 no-array-index-as-key sites use a stable key (Payload id, id-with-fallback, or crumb.url)
- All 7 next/image fill instances have a layout-appropriate sizes attribute
- blog/page.tsx and contact/page.tsx parallelize their params/searchParams awaits via Promise.all
- All 6 remaining performance findings (js-hoist-intl x2, js-flatmap-filter, js-combine-iterations, rendering-hydration-no-flicker, async-await-in-loop) fixed per their canonical recipes
- IconPicker's autoFocus removed
- The 7 maintainability findings (only-export-components x3, no-inline-exhaustive-style x3, circular-dependency) resolved without behavior changes
- npx tsc --noEmit passes with zero errors
- npx react-doctor@latest --verbose shows none of the 12 mechanically-verifiable target rules firing; unsafe-json-in-html and server-auth-actions dispositions documented with reasoning in the SUMMARY
- Zero regression in the 23 already-documented unused-file findings or the deferred RSC-CVE finding
</success_criteria>

<output>
Create `.planning/quick/260713-2q2-fix-remaining-react-doctor-findings-by-p/260713-2q2-SUMMARY.md` when done
</output>
