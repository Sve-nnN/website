---
quick_id: 260712-1f1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HeroGrainGradient.tsx
  - src/blocks/CallToAction/Component.tsx
autonomous: false

must_haves:
  truths:
    - "The 'Ready to work together?' CTA strip renders a shader background that reads as a deliberate visual variation from the home Hero's shader (different shape/motion feel), not a copy-paste of the same look"
    - "The CTA strip's shader background uses the exact same LIGHT_COLORS/DARK_COLORS/NEAR_BLACK values as the Hero — same brand palette, only shape/motion differs"
    - "The home Hero's shader (variant: 'home' in Hero/Component.tsx) and the ContactFormBlock sidebar's shader are visually unchanged from their current look — calling HeroGrainGradient with no variant prop still produces the exact prior blob configuration"
    - "The CallToAction section shows a meaningfully elevated UI/UX treatment (spacing/rhythm, typography hierarchy, button treatment, rounded corners/elevation, responsive layout) compared to its prior plain flex-row strip"
    - "All text and links in the CallToAction section stay readable over the new shader variant in light theme, dark theme, and both locales (EN/ES)"
  artifacts:
    - path: "src/components/HeroGrainGradient.tsx"
      provides: "Optional `variant` prop ('default' | 'cta') selecting between the existing Hero/blob shader config (default, unchanged) and a new distinct CTA/ripple-based config, both built from the same color constants"
      contains: "variant"
    - path: "src/blocks/CallToAction/Component.tsx"
      provides: "Elevated CTA section markup consuming `<HeroGrainGradient variant=\"cta\" />` with improved spacing, typography, and button layout"
      contains: "variant=\"cta\""
  key_links:
    - from: "src/blocks/CallToAction/Component.tsx"
      to: "src/components/HeroGrainGradient.tsx"
      via: "<HeroGrainGradient variant=\"cta\" />"
      pattern: "HeroGrainGradient variant=.cta."
    - from: "src/blocks/Hero/Component.tsx"
      to: "src/components/HeroGrainGradient.tsx"
      via: "<HeroGrainGradient /> called with no variant prop — must keep resolving to the original blob config"
      pattern: "<HeroGrainGradient />"
---

<objective>
Follow-up to quick task 260712-159 (commits 455885a, f76678f), which reused `HeroGrainGradient` verbatim (no props) as the background for both the ContactFormBlock sidebar and the CallToAction "Ready to work together?" strip. Juan reviewed and asked for two changes scoped ONLY to the CallToAction strip:

1. A visually distinct shader treatment for the CTA strip — different shape/motion feel from the Hero's `blob` look — while keeping the exact same brand color tokens (`LIGHT_COLORS`/`DARK_COLORS`/`NEAR_BLACK`).
2. A "pro max" UI/UX elevation pass on the CallToAction component itself: spacing, typography hierarchy, button/link treatment, rounded corners/elevation, responsive behavior — not just the shader background.

The Hero (`src/blocks/Hero/Component.tsx`) and the ContactFormBlock sidebar (`src/blocks/ContactFormBlock/Component.tsx`) must keep their exact current look — both call `<HeroGrainGradient />` with no arguments today and must continue to resolve to the identical `blob` configuration after this change.

Purpose: make the CTA strip read as a deliberate, premium variation of the brand's shader language instead of a duplicate of the Hero, and give the section itself a visual polish pass worthy of being the site's final conversion moment.

Output: `HeroGrainGradient` gains an optional `variant` prop (`'default' | 'cta'`, defaulting to the existing `blob` look when unset or `'default'`); `CallToAction/Component.tsx` opts into `variant="cta"` and receives an elevated layout/typography/spacing treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Current HeroGrainGradient contract (src/components/HeroGrainGradient.tsx),
before this plan's change. It is a self-contained client component with NO
PROPS today. It renders its own absolutely-positioned, full-bleed root div
(`absolute inset-0 overflow-hidden`, `aria-hidden="true"`), so callers must
place it inside a `relative` positioned ancestor. -->

Current constants (must NOT change in value — only be reused/duplicated for the new variant):
```typescript
const LIGHT_COLORS = ['#23283A', '#3A4159', '#F7581E']
const DARK_COLORS = ['#3A4159', '#4B5470', '#FF7A45']
const NEAR_BLACK = '#0A0A0F'
```

Current (default/Hero) shape config — this exact set of values must remain the behavior when `variant` is unset or `'default'`:
```typescript
const SHADER_SHAPE = 'blob' as const
const SHADER_SOFTNESS = 0.15
const SHADER_INTENSITY = 0.2
const SHADER_NOISE = 0.35
const SHADER_SCALE = 1.4
// motion: speed 0.3 (0 when prefers-reduced-motion)
```

`GrainGradient` (from `@paper-design/shaders-react`) accepts (relevant subset, from `node_modules/@paper-design/shaders/dist/shaders/grain-gradient.d.ts`): `colors`, `colorBack`, `shape` (`'wave'|'dots'|'truchet'|'corners'|'ripple'|'blob'|'sphere'`), `softness`, `intensity`, `noise`, `scale`, `speed`, `frame`, and sizing params `offsetX`/`offsetY`/`rotation` (each -1..1 / 0..360, inherited from `ShaderSizingParams`). These are the only knobs available — do not invent props the library doesn't have.

The component's own header comment already documents that `ripple` was built and compared live against `blob` during Phase 16 and produces "a bolder, more graphic single curved arc (its concentric-ring center can be parked outside the frame via static offsetX/offsetY)" — this is the recommended starting point for the CTA variant's distinct shape, still using the same color constants.
</interfaces>

<current_cta_markup>
`src/blocks/CallToAction/Component.tsx` (post-260712-159) currently renders:
```tsx
<section className="relative overflow-hidden rounded-lg">
  <HeroGrainGradient />
  <div className="absolute inset-0 bg-black/25" aria-hidden="true" />
  <Container className="relative z-10 py-12 md:py-16 flex flex-col md:flex-row items-center justify-between gap-8">
    <div className="flex-1">
      <RichTextRenderer data={richText} className="text-secondary-foreground" />
    </div>
    {links && links.length > 0 && (
      <div className="flex flex-wrap gap-4">
        {links.map(({ link }, i) => (
          <CMSLink key={i} {...link} />
        ))}
      </div>
    )}
  </Container>
</section>
```
The Payload schema (`src/blocks/CallToAction/config.ts`) only exposes two fields — `richText` and a `linkGroup` (max 2 links, appearances `'default'|'outline'`) — so no new content fields are available; the elevation pass must work within `richText` + up to 2 `CMSLink`s. `CMSLink` (src/components/CMSLink.tsx) renders `appearance: 'default'|'outline'` as a shadcn `Button` (`asChild`), or — with no `appearance` — a plain `text-primary underline` text link. Do not modify `CMSLink` or `src/components/ui/button.tsx`; reuse their existing variants/tokens only. `RichTextRenderer` wraps content in `Prose` (src/components/Prose.tsx), which already styles `h1`-`h3`/`p`/`ul`/`ol`/`a`/`blockquote` — do not fork `Prose`, only pass `className` overrides.
</current_cta_markup>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an optional `variant` prop to HeroGrainGradient with a distinct CTA configuration</name>
  <files>src/components/HeroGrainGradient.tsx</files>
  <action>
Add an exported type `HeroGrainGradientVariant = 'default' | 'cta'` and an optional prop `variant?: HeroGrainGradientVariant` on `HeroGrainGradient` (default value `'default'` when omitted, matching current call sites `<HeroGrainGradient />` in `Hero/Component.tsx` and `ContactFormBlock/Component.tsx` exactly — those files are NOT touched in this task and must continue to compile and render identically).

Restructure the existing standalone `SHADER_SHAPE`/`SHADER_SOFTNESS`/`SHADER_INTENSITY`/`SHADER_NOISE`/`SHADER_SCALE` constants into a small per-variant config lookup (e.g. a `const SHADER_VARIANTS: Record<HeroGrainGradientVariant, {...}>` object or two named config consts selected by a ternary) — keep the `'default'` entry's values byte-identical to today's constants (shape `'blob'`, softness `0.15`, intensity `0.2`, noise `0.35`, scale `1.4`, no offset/rotation). Preserve the existing header comment explaining the wave->ripple->blob history for the default/Hero variant.

Add a `'cta'` entry using shape `'ripple'` (per the existing code comment's own recommendation — "bolder, more graphic single curved arc") with values that read as a clear-but-harmonious departure from the Hero's calm `blob`: e.g. softness `0.35`, intensity `0.45`, noise `0.3`, scale `1.1`, plus a static `offsetX`/`offsetY` (e.g. `0.2` / `-0.15`) to park the ripple's concentric-ring center off-center per the code's own guidance, and a slightly livelier base motion speed (e.g. `0.45` instead of `0.3`) — still fully frozen to `speed: 0, frame: 0` when `prefers-reduced-motion` is active, same as today. Do NOT change `LIGHT_COLORS`, `DARK_COLORS`, or `NEAR_BLACK` — both variants must read from the exact same color/`colorBack` values. Add a short comment documenting this as the CTA variant's rationale (distinct shape/motion, identical palette, ripple chosen as the phase-16-vetted bolder alternative).

Update the `GrainGradient` JSX call to read `shape`/`softness`/`intensity`/`noise`/`scale`/`offsetX`/`offsetY` from the resolved per-variant config object, and update the `motionProps` base speed to come from that same config (still overridden to 0 on reduced motion). Keep every existing piece of logic completely unchanged: SSR-safe `reducedMotion`/`isDark` state, `supportsWebGL2` feature-detect, `ShaderErrorBoundary`, `data-testid`/`data-motion` attributes, colors-by-theme resolution.
  </action>
  <verify>
    <automated>grep -c "variant" src/components/HeroGrainGradient.tsx</automated>
  </verify>
  <done>`HeroGrainGradient` accepts an optional `variant` prop; calling it with no prop (or `variant="default"`) produces the exact same shape/softness/intensity/noise/scale/colors as before this change; `variant="cta"` produces a `ripple`-shaped config using the identical color constants; `npx tsc --noEmit` passes with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the CTA variant and elevate CallToAction's UI/UX</name>
  <files>src/blocks/CallToAction/Component.tsx</files>
  <action>
Change `<HeroGrainGradient />` to `<HeroGrainGradient variant="cta" />`.

Elevate the section's visual design within the existing `richText` + up to 2 `CMSLink`s content contract, reusing only existing design tokens/components (no new colors, no new shadow/radius scale beyond what Tailwind config + `src/components/ui/button.tsx` already expose):

- Section container: increase from `rounded-lg` to `rounded-2xl`, add a visible elevation (`shadow-xl`) and a subtle edge definition (e.g. `ring-1 ring-white/10`) so the block reads as a distinct, elevated card rather than a flat strip.
- Overlay: replace the flat `bg-black/25` with a directional gradient overlay (e.g. `bg-gradient-to-t from-black/50 via-black/20 to-black/10` or similar using only black/opacity, no new color tokens) for more depth and to keep bottom-aligned content/buttons legible.
- Spacing/rhythm: increase vertical padding (e.g. `py-16 md:py-20`) and the gap between text and links (e.g. `gap-8 md:gap-12`).
- Layout/alignment: center content on mobile (`text-center md:text-left`, `items-center md:items-start` on the flex children) for a more deliberate stacked mobile presentation, keeping the existing `flex-col md:flex-row` two-zone layout for larger breakpoints.
- Typography: constrain the `RichTextRenderer` wrapper to a readable measure on wider screens (e.g. `max-w-xl`) so the copy doesn't stretch full-width next to the buttons.
- Buttons/links: change the links wrapper from `flex flex-wrap gap-4` to `flex flex-col sm:flex-row gap-3 sm:gap-4 w-full sm:w-auto` and give each `CMSLink` a full-width feel on mobile via a wrapper className (e.g. wrap each in a `<div className="w-full sm:w-auto">` around `<CMSLink key={i} {...link} className="w-full sm:w-auto" />`) — do not modify `CMSLink` or `Button` source, only pass `className` through the existing `className` prop `CMSLink` already accepts.

Do not touch `src/components/HeroGrainGradient.tsx` beyond Task 1's change, `src/blocks/Hero/Component.tsx`, or `src/blocks/ContactFormBlock/Component.tsx`.
  </action>
  <verify>
    <automated>grep -c 'variant="cta"' src/blocks/CallToAction/Component.tsx</automated>
  </verify>
  <done>`CallToAction/Component.tsx` renders `<HeroGrainGradient variant="cta" />`; section has `rounded-2xl`/`shadow-xl`/ring elevation, gradient overlay, increased padding/gaps, mobile-centered stacked layout, constrained text measure, and stacked full-width-on-mobile buttons; `npx tsc --noEmit` passes with no new errors in this file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The "Ready to work together?" CallToAction strip now uses a visually distinct `ripple`-based shader variant (same brand colors as the Hero's `blob`, different shape/motion) plus an elevated layout: rounded-2xl card with shadow/ring, gradient overlay, more breathing room, centered-on-mobile stacked content, and full-width-on-mobile stacked buttons.

Follow-up fix (post-checkpoint feedback): the ContactFormBlock sidebar ("Charlemos sobre tu próximo proyecto" / "Let's talk about your next project") was still rendering the same `blob` look as the Hero, matching it visually instead of reading as distinct. `src/blocks/ContactFormBlock/Component.tsx` now calls `<HeroGrainGradient variant="cta" />` (same ripple variant as CallToAction), so the sidebar's shader now matches the CTA strip's look, not the Hero's. The Hero itself (`src/blocks/Hero/Component.tsx`) remains untouched on `blob`. Commit `fb5d3d5`.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and find a page rendering the CallToAction block (the "Ready to work together?" strip) in both `/en` and `/es` (or the project's actual locale routes).
2. Confirm the CTA strip's shader visibly reads as a different shape/motion than the home Hero's shader (bolder/more graphic arc vs. the Hero's calm blob) while clearly using the same ember-on-navy/near-black color palette — not a random new brand color.
3. Confirm the CallToAction section looks meaningfully more polished than a plain flat strip: rounded card with visible shadow/edge, comfortable spacing, readable text against the gradient overlay, and clear button hierarchy (default vs outline where both links are present).
4. Resize to mobile width (or use devtools responsive mode) and confirm the section stacks and centers cleanly, with buttons full-width and stacked vertically.
5. Toggle light/dark theme (if a switcher exists) and re-check readability and color correctness in both.
6. Revisit the home page Hero — confirm it looks completely unchanged (still the calm `blob` shader), confirming the `variant` prop default did not regress it.
7. Visit the contact page and check the sidebar ("Charlemos sobre tu próximo proyecto" / "Let's talk about your next project") — confirm it now shows the same ripple/CTA look as the CallToAction strip (bolder arc, off-center ring), NOT the Hero's blob look. This is the fix for the checkpoint feedback on task 260712-1f1.
8. Optionally: enable OS-level "reduce motion" and confirm both the CTA strip's and sidebar's shaders freeze (no animation), same as before.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS content -> RichTextRenderer | `richText` field from Payload is trusted editorial content (not third-party user input), rendered via Payload's own Lexical JSX converters — unchanged trust boundary from 260712-159, no new input surface introduced by this styling/variant change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1F1-01 | Tampering | HeroGrainGradient `variant` prop (new, internal-only API surface) | accept | `variant` is a TypeScript-narrowed union (`'default' \| 'cta'`) consumed only by same-repo components (`Hero`, `ContactFormBlock`, `CallToAction`), never derived from CMS/user input — no injection surface. |
| T-Q1F1-02 | Denial of Service | Additional shader config branch in an already-mounted-3x-per-page WebGL component | accept | Same accepted risk as T-Q159-01 (260712-159) — purely client-side GPU cost, still gated by the existing `supportsWebGL2` feature-detect and `ShaderErrorBoundary` fallback to `bg-secondary`. No new dependency or attack surface; only new constant values. |
</threat_model>

<verification>
1. `grep -c "variant" src/components/HeroGrainGradient.tsx` returns a non-zero count confirming the prop and per-variant config exist.
2. `grep -c 'variant="cta"' src/blocks/CallToAction/Component.tsx` returns `1`, confirming the CTA strip opts into the new variant.
3. `grep -c "HeroGrainGradient" src/blocks/Hero/Component.tsx src/blocks/ContactFormBlock/Component.tsx` — both files unchanged, still call `<HeroGrainGradient />` with no variant argument.
4. Project typecheck (`npx tsc --noEmit` or the project's configured typecheck script) passes with no new errors.
5. Human verification checkpoint confirms the CTA shader reads as a deliberate variation (not identical to Hero), the elevated UI/UX pass is visible, both themes/locales stay legible, and Hero/ContactFormBlock sidebar are visually unaffected.
</verification>

<success_criteria>
- `HeroGrainGradient` exposes an optional `variant` prop; omitting it (Hero, ContactFormBlock) reproduces the exact pre-existing `blob` configuration.
- `CallToAction/Component.tsx` uses `variant="cta"` with a `ripple`-based config sharing the identical `LIGHT_COLORS`/`DARK_COLORS`/`NEAR_BLACK` values.
- CallToAction's layout/typography/spacing/buttons are visibly elevated compared to the pre-260712-1f1 plain strip, using only existing design tokens and `Button`/`CMSLink` variants.
- `src/blocks/Hero/Component.tsx` and `src/blocks/ContactFormBlock/Component.tsx` are untouched.
- Human checkpoint approved.
</success_criteria>

<output>
Create `.planning/quick/260712-1f1-cta-ready-to-work-together-variante-de-g/260712-1f1-SUMMARY.md` when done
</output>
