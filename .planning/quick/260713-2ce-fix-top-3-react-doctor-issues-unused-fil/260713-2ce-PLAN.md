---
phase: quick-260713-2ce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run doctor no longer reports deslop/unused-dependency for @radix-ui/react-select"
    - "npm run doctor no longer reports react-doctor/no-vulnerable-react-server-components"
    - "The 23 deslop/unused-file findings are each individually triaged with a documented disposition (deleted or kept-with-reason), not blanket-deleted"
    - "payload migrate still runs cleanly after the pass (migration history intact)"
  artifacts:
    - path: "package.json"
      provides: "next bumped to patched 15.5.x line, @radix-ui/react-select removed"
    - path: "package-lock.json"
      provides: "lockfile regenerated to match package.json changes"
  key_links:
    - from: "src/migrations/index.ts"
      to: "payload migrate CLI"
      via: "CLI-time dynamic import, not app entry-point static graph"
      pattern: "migrations"
---

<objective>
Close the top 3 react-doctor findings from `.planning/quick/260713-2ce-fix-top-3-react-doctor-issues-unused-fil/`: unused-file warnings (deslop, x23), the unused `@radix-ui/react-select` dependency, and the vulnerable-RSC security warning on `next@15.4.11` (CVE-2026-23870).

Purpose: reduce maintenance surface and close a known high-severity Next.js RSC DoS vulnerability, without breaking the Payload migration chain or deviating from the project's pinned Next 15.5.x line (per project CLAUDE.md — Payload 3.85 compatibility).

Output: updated `package.json` / `package-lock.json` (next bumped, react-select dependency removed), a documented triage of all 23 unused-file findings (this investigation already determined all 23 are false positives — see `<context>` below), and a clean re-run of `npx react-doctor@latest` confirming these 3 issue types are gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

Pre-planning investigation already completed (do NOT re-run this analysis, just execute against these findings):

1. diagnostics.json has EXACTLY 23 `deslop/unused-file` entries. All 23 are:
   - `ecosystem.config.cjs` (1 file) — PM2 process config, read by the `pm2` CLI at deploy time on the
     Hostinger server, never imported by the Next.js app itself. Genuinely unreachable from any Next
     entry point, by design -- that's how PM2 configs work. NOT dead code.
   - `src/migrations/*.ts` (21 files) + `src/migrations/index.ts` (1 file) = 22 files -- these are
     Payload's migration history. `src/migrations/index.ts` re-exports every migration and is loaded
     dynamically by the `payload migrate` CLI command at build/deploy time (see `package.json` build
     script: `payload migrate && payload generate:importmap && ...`). None of these files are imported
     by any Next.js route, component, or page -- that's expected, migrations are CLI-invoked, not
     app-code. Confirmed via `grep -rn "migrations" src/payload.config.ts` (no explicit migrationDir
     override -- Payload's default convention `src/migrations` + `index.ts` is what the CLI reads).

   Conclusion: all 23 flagged files are false positives from react-doctor's static module-graph
   analysis, which only traces reachability from Next.js app entry points and does not know about
   CLI-invoked scripts (PM2, `payload migrate`). Per project CLAUDE.md's hard DB-safety rule, migration
   files must stay in sequence and must never be deleted once applied -- deleting any of the 21 migration
   files (or the index.ts that registers them) would break `payload migrate`'s ability to compute/replay
   migration history against the real production Neon Postgres DB. Zero files are deleted in this plan.

2. `grep -rln "@radix-ui/react-select" src/` returns no matches -- the only consumer
   (`src/components/ui/select.tsx`) was already removed in commit `6972ae8` (2026-07-13, prior quick
   task). Dependency removal is safe.

3. Patched Next 15.x line versions available on npm (`npm view next versions`): 15.5.18, 15.5.19,
   15.5.20 all satisfy `>=15.5.18`. Use 15.5.20 (latest in the pinned next-15 line, matches the version
   already documented as the recommended stack in project CLAUDE.md). Installed dependents
   (`@payloadcms/next`, `@payloadcms/ui`, `mcp-handler`, `geist`, `next-intl`) all currently dedupe to
   the root `next` version, so bumping root `next` is sufficient -- no separate bumps needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage and document the 23 unused-file findings (no deletions)</name>
  <files>none (investigation output goes into the plan SUMMARY, no source files change)</files>
  <action>
    Re-verify the pre-planning investigation findings from the context block above before treating them
    as final: run `grep -rn "migrations" src/payload.config.ts` to confirm no explicit migrationDir
    override exists, and confirm `ecosystem.config.cjs` is the file PM2 loads directly by filename at
    deploy time (`pm2 start ecosystem.config.cjs`), not app code. Do not delete `ecosystem.config.cjs`,
    any file under `src/migrations/`, or `src/migrations/index.ts` -- all 23 are CLI/ops-invoked and
    load-bearing, per the project's hard migration-safety rule in CLAUDE.md (migrations must stay in
    sequence; deleting any breaks `payload migrate`'s history against the real production Neon Postgres
    DB). This task produces no file changes -- its output is the documented disposition recorded in the
    plan SUMMARY: "23/23 flagged unused-file findings verified as false positives (1 PM2 deploy config +
    22 Payload migration registry files, both CLI-invoked, not reachable from any Next.js app entry
    point); zero files deleted."
  </action>
  <verify>
    <automated>test -f ecosystem.config.cjs && test -f src/migrations/index.ts && test "$(ls src/migrations/*.ts | grep -v index.ts | wc -l | tr -d ' ')" = "21" && echo OK</automated>
  </verify>
  <done>All 23 files confirmed present and untouched; disposition documented for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused @radix-ui/react-select dependency</name>
  <files>package.json, package-lock.json</files>
  <action>
    Confirm zero remaining imports first: `grep -rln "@radix-ui/react-select" src/` must return no
    matches (already verified in investigation, re-confirm before removing). Run
    `npm uninstall @radix-ui/react-select` to remove the dependency from `package.json` and update
    `package-lock.json` in one step (do not hand-edit `package.json` -- let npm rewrite both files
    consistently).
  </action>
  <verify>
    <automated>grep -q react-select package.json && echo STILL_PRESENT || echo REMOVED</automated>
  </verify>
  <done>`@radix-ui/react-select` no longer appears in `package.json` dependencies or `package-lock.json`.</done>
</task>

<task type="auto">
  <name>Task 3: Bump next to patched 15.5.x and reinstall</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm install next@15.5.20` (patched next-15 line per project CLAUDE.md pin -- do NOT install
    `next@latest`/16.x). This updates the `next` entry in `package.json` `dependencies` and regenerates
    `package-lock.json`. Do not touch `react`/`react-dom` versions -- Next 15.5.20 still targets React
    19.2.x, no peer bump required. After install, run `npm ls next` to confirm the resolved version is
    `>=15.5.18` and that all dependents (`@payloadcms/next`, `@payloadcms/ui`, `mcp-handler`, `geist`,
    `next-intl`) dedupe to the same root version (no duplicate `next` copies in the tree).
  </action>
  <verify>
    <automated>grep '"next"' package.json | grep -oE '[0-9]+\.[0-9]+\.[0-9]+'</automated>
  </verify>
  <done>`package.json` pins `next` to `15.5.18` or higher on the 15.x line, `package-lock.json` regenerated, `npm ls next` shows a single deduped resolved version across all dependents.</done>
</task>

<task type="auto">
  <name>Task 4: Reinstall, verify build, and re-run react-doctor</name>
  <files>none (verification only)</files>
  <action>
    Run `npm install` to ensure the lockfile is fully consistent after both dependency changes. Run
    `npm run build` (or at minimum `npx tsc --noEmit` if a full build is too slow for this pass) to
    confirm the `next` bump and dependency removal did not break compilation. Then run
    `npx react-doctor@latest --verbose` and inspect the output: confirm zero
    `deslop/unused-dependency` findings for `@radix-ui/react-select`, zero
    `react-doctor/no-vulnerable-react-server-components` findings, and that any remaining
    `deslop/unused-file` findings are limited to the 23 already-triaged false positives from Task 1
    (no new unused-file regressions introduced by this plan).
  </action>
  <verify>
    <automated>npx react-doctor@latest --verbose 2>&1 | grep -v '^#' | grep -Ec 'unused-dependency|no-vulnerable-react-server-components' | grep -qx 0 && echo CLEAN</automated>
  </verify>
  <done>`npm run build` (or `tsc --noEmit`) passes, and react-doctor output no longer contains the `@radix-ui/react-select` unused-dependency finding or the vulnerable-RSC security finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> local install | `next@15.5.20` and lockfile regeneration pull from the public npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-260713-2ce-01 | Information Disclosure | next RSC runtime (CVE-2026-23870) | mitigate | Bump `next` to `15.5.20` (patched), the direct fix this plan delivers for Task 3 |
| T-260713-2ce-SC | Tampering | npm install of `next@15.5.20` | accept | `next` is a well-known, already-installed first-party dependency being version-bumped within its existing major/minor pin, not a new/unverified package; no legitimacy checkpoint required |
</threat_model>

<verification>
Run `npx react-doctor@latest --verbose` after all tasks complete and confirm:
1. No `deslop/unused-dependency` finding for `@radix-ui/react-select`
2. No `react-doctor/no-vulnerable-react-server-components` finding
3. The `deslop/unused-file` findings, if still reported, are exactly the same 23 files documented as false positives in Task 1 (no regressions)

Run `npm run build` (or `payload migrate` in a safe dry-run sense: at minimum confirm `src/migrations/index.ts` still imports and exports all 21 migrations without error via `npx tsc --noEmit`) to confirm the migration chain is intact.
</verification>

<success_criteria>
- `package.json` dependencies no longer include `@radix-ui/react-select`
- `package.json` `next` version is `>=15.5.18` on the 15.x line (not 16.x)
- `package-lock.json` regenerated and consistent with `package.json`
- Zero files deleted from `src/migrations/` or `ecosystem.config.cjs` (documented false positives)
- `npm run build` or `tsc --noEmit` passes with no new errors
- Re-run of `npx react-doctor@latest --verbose` shows none of the 3 target issue types
</success_criteria>

<output>
Create `.planning/quick/260713-2ce-fix-top-3-react-doctor-issues-unused-fil/260713-2ce-SUMMARY.md` when done
</output>
