---
quick_id: 260713-1zw
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/select.tsx
  - src/components/ui/skeleton.tsx
  - scripts/migrate/export/dump-source.ts
  - scripts/migrate/lib/richtext-remap.ts
  - scripts/migrate/steps/04-posts.ts
autonomous: true

must_haves:
  truths:
    - "src/components/ui/select.tsx and src/components/ui/skeleton.tsx no longer exist in the repo (confirmed genuinely unreachable — zero imports anywhere in src/)"
    - "ecosystem.config.cjs and every file under src/migrations/ are explicitly left untouched — they are false positives (deslop's static import graph can't see PM2's CLI load of ecosystem.config.cjs or Payload's migration-runner load of src/migrations/index.ts, both confirmed as real consumers)"
    - "None of the 3 JSON.parse(JSON.stringify(x)) deep-clone sites remain in scripts/migrate/export/dump-source.ts, scripts/migrate/lib/richtext-remap.ts, or scripts/migrate/steps/04-posts.ts — all replaced with structuredClone(x)"
    - "TypeScript typecheck (npx tsc --noEmit) passes with no new errors after both fixes"
    - "Task 2 (next CVE bump) is SKIPPED per explicit user decision — Juan chose to defer the Next 16 major bump to a separate decision, do not touch package.json/package-lock.json/next version in this run"
    - "No other react-doctor finding (the other ~70) is touched in this plan"
  artifacts:
    - path: "scripts/migrate/export/dump-source.ts"
      provides: "structuredClone(result.docs) instead of JSON.parse(JSON.stringify(...))"
      contains: "structuredClone"
    - path: "scripts/migrate/lib/richtext-remap.ts"
      provides: "structuredClone(richText) instead of JSON.parse(JSON.stringify(...))"
      contains: "structuredClone"
    - path: "scripts/migrate/steps/04-posts.ts"
      provides: "structuredClone(original) instead of JSON.parse(JSON.stringify(...))"
      contains: "structuredClone"
  key_links: []
---

<objective>
Fix exactly the top 3 react-doctor findings in this repo, and no others:

1. **deslop/unused-file (25 findings)** — most of these are false positives (migration files + ecosystem.config.cjs are loaded dynamically, invisible to static analysis). Only 2 files are genuinely dead: src/components/ui/select.tsx, src/components/ui/skeleton.tsx. Delete those two; explicitly leave the rest.
2. **react-doctor/no-vulnerable-react-server-components (package.json)** — next@15.4.11 carries CVE-2026-23870 (high-severity RSC DoS), patched in 15.5.18+. @payloadcms/next@3.85.2's peer range is `>=15.2.9 <15.3.0 || >=15.3.9 <15.4.0 || >=15.4.11 <15.5.0 || >=16.2.6 <17.0.0` — it has no compatible 15.5.x version at all. The only way to land the patch without breaking Payload's peer contract is to move to the 16.2.6+ line (which the peer range explicitly allows, and which trivially post-dates the 15.5.18 fix).
3. **react-doctor/no-json-parse-stringify-clone (3 findings)** — replace JSON.parse(JSON.stringify(x)) with structuredClone(x) at 3 confirmed genuine full-object clone sites in the migration scripts.

Purpose: close the 3 highest-severity/highest-count react-doctor findings without touching the other ~70 (deferred, out of scope for this task).

Output: 2 deleted dead files, next bumped to a CVE-patched + peer-compatible version, 3 deep-clone call sites modernized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<pre_verified_findings>
Planning already did the verification legwork the diagnostics.json / rule docs ask for. Do not re-derive from scratch — use these findings, but do a quick grep-based sanity check per task before acting, per each task's action.

Issue 1 — unused-file, 25 findings, verified individually:
- src/components/ui/select.tsx — zero references anywhere in src/ outside itself (checked with `grep -rn "components/ui/select" src`, returns nothing). Genuinely dead. DELETE.
- src/components/ui/skeleton.tsx — same, zero references. Genuinely dead. DELETE.
- ecosystem.config.cjs — referenced by scripts/deploy.sh line 23 (`pm2 startOrReload ecosystem.config.cjs --env production`). PM2 loads this via its own CLI, not a JS import, so deslop's static graph can't see it. FALSE POSITIVE. DO NOT DELETE.
- All 22 files under src/migrations/ (the 21 individual migration files plus src/migrations/index.ts) — src/migrations/index.ts statically imports all 21 migration files (confirmed by reading the file). index.ts itself is loaded by the Payload CLI's migration runner (`payload migrate`, invoked from `npm run build`) via its conventional migrationDir discovery, not a static import from any file in src/, so deslop's graph can't see that edge either. FALSE POSITIVE, all 22. DO NOT DELETE ANY OF THEM.

Issue 2 — next CVE, verified peer range:
`npm view @payloadcms/next@3.85.2 peerDependencies` returns:
next: '>=15.2.9 <15.3.0 || >=15.3.9 <15.4.0 || >=15.4.11 <15.5.0 || >=16.2.6 <17.0.0'
There is no 15.5.x anywhere in that range — the CVE fix (15.5.18) is structurally unreachable while pinned to Payload 3.85.2. 16.2.6 is the lowest version in the range that both satisfies the peer contract and post-dates the 15.5.18 patch (16.x forked after 15.5.18 shipped, so it carries the fix). This deviates from the existing CLAUDE.md guidance to hold at the 15.5.x line — that guidance predates this specific finding, and Payload 3.85.2's own peer metadata already explicitly allows 16.2.6+ today (the stated condition for upgrading, "after Payload publishes explicit 16 support," is already met by the installed Payload version).

Issue 3 — JSON.parse/JSON.stringify sites, verified genuine:
All 3 sites clone a full plain-JSON-safe object (a Payload docs array, or a Lexical rich-text tree) with no functions, no Date/Map/Set, no intentional partial/lossy serialization — genuine full clones, safe to swap to structuredClone. Node's global structuredClone and the project's tsconfig.json (lib: dom, dom.iterable, esnext) both support it with no config changes needed.
</pre_verified_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 2 genuinely-dead UI files (leave the 23 false positives untouched)</name>
  <files>src/components/ui/select.tsx, src/components/ui/skeleton.tsx</files>
  <action>
Before deleting, re-run the exact verification grep for each file to confirm nothing changed since planning: grep for "components/ui/select" and "components/ui/skeleton" across src/ with --include="*.tsx" --include="*.ts" — both must return zero matches outside the files themselves. If either returns a real import, stop and do not delete that file (report it instead — the finding would be a false positive too).

If both are confirmed empty, delete src/components/ui/select.tsx and src/components/ui/skeleton.tsx. Do NOT touch ecosystem.config.cjs or anything under src/migrations/ — both are confirmed false positives (see pre_verified_findings above), deleting either would break PM2 process startup or the Payload migration runner.
  </action>
  <verify>
    <automated>test ! -f src/components/ui/select.tsx && test ! -f src/components/ui/skeleton.tsx && test -f ecosystem.config.cjs && test -f src/migrations/index.ts && npx tsc --noEmit</automated>
  </verify>
  <done>select.tsx and skeleton.tsx are deleted from the repo; ecosystem.config.cjs and src/migrations/* still exist untouched; npx tsc --noEmit passes with no new errors (proves nothing was actually importing the 2 deleted files).</done>
</task>

<task type="skipped">
  <name>Task 2: Bump next off the CVE-2026-23870-vulnerable version — SKIPPED</name>
  <action>
Deferred per explicit user decision (2026-07-13): the only compatible patched version is next@16.2.x (no 15.5.x exists in @payloadcms/next@3.85.2's peer range), which is a major-version bump contradicting CLAUDE.md's "hold at 15.5, don't lead the Next 16 upgrade" guidance. Juan chose to skip this fix in this pass and decide separately. Do NOT modify package.json, package-lock.json, or the next version in this run.
  </action>
</task>

<task type="auto">
  <name>Task 3: Replace JSON.parse(JSON.stringify(x)) with structuredClone(x) at the 3 confirmed sites</name>
  <files>scripts/migrate/export/dump-source.ts, scripts/migrate/lib/richtext-remap.ts, scripts/migrate/steps/04-posts.ts</files>
  <action>
First, fetch the canonical fix recipe and false-positive checklist with a no-cache curl: `curl -s -H "Cache-Control: no-cache" https://react.doctor/docs/rules/react-doctor/no-json-parse-stringify-clone`. Read it and confirm each of the 3 sites below is a genuine full-object clone (not an intentional partial/lossy JSON round-trip) before changing anything — planning already did this check (see pre_verified_findings above: all 3 clone plain-JSON-safe Payload docs / Lexical rich-text trees, no functions/Date/Map/Set/cycles), but re-confirm against the fetched recipe's guidance before editing.

Then make these 3 exact edits:
- scripts/migrate/export/dump-source.ts line 93: replace `const docs = JSON.parse(JSON.stringify(result.docs))` with `const docs = structuredClone(result.docs)`.
- scripts/migrate/lib/richtext-remap.ts line 54: replace `const cloned = JSON.parse(JSON.stringify(richText))` with `const cloned = structuredClone(richText)`.
- scripts/migrate/steps/04-posts.ts line 185: replace `const cloned = JSON.parse(JSON.stringify(original))` with `const cloned = structuredClone(original)`.

Do not change any other logic in these 3 files — only the clone call itself. Do not touch any of the other react-doctor findings in these same files if present (e.g. async-await-in-loop in src/search/beforeSync.ts is a separate, out-of-scope finding).
  </action>
  <verify>
    <automated>! grep -n "JSON.parse(JSON.stringify" scripts/migrate/export/dump-source.ts scripts/migrate/lib/richtext-remap.ts scripts/migrate/steps/04-posts.ts && grep -c "structuredClone" scripts/migrate/export/dump-source.ts scripts/migrate/lib/richtext-remap.ts scripts/migrate/steps/04-posts.ts && npx tsc --noEmit</automated>
  </verify>
  <done>All 3 files use structuredClone instead of JSON.parse(JSON.stringify(...)); npx tsc --noEmit passes with no new errors; no other code in these 3 files was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> package.json/package-lock.json | Task 2 installs a new next version from the public npm registry. Trusted publisher (Vercel), version chosen from the official peer-compatible range published by Payload itself — no new/unknown package introduced. |
| Migration scripts -> production data shape | Tasks 3's structuredClone sites operate on data already pulled from the real Payload/Postgres source during a one-time migration ETL — clone semantics change (structuredClone preserves Date/Map/Set/undefined that JSON.stringify silently drops) but these 3 sites were confirmed to only ever hold plain-JSON-safe data, so no behavior change is expected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1ZW-01 | Denial of Service | Next.js RSC runtime (package.json next dependency) | mitigate | This is the fix itself — CVE-2026-23870 (high-severity RSC DoS) is patched by moving off 15.4.11 to the 16.2.x line, since no compatible 15.5.x exists in Payload's peer range. |
| T-Q1ZW-02 | Tampering | npm install of next@16.2.x | mitigate | Version pulled from the official npm registry (Vercel-published `next` package, not a third-party/community package), chosen from Payload's own documented peer-compatible range — no legitimacy checkpoint needed (this is a well-known, non-squattable first-party package, not an ASSUMED/SUS community package). |
| T-Q1ZW-03 | Tampering | Deleting src/components/ui/select.tsx / skeleton.tsx | accept | Verified zero references repo-wide before deletion (Task 1); npx tsc --noEmit re-verifies post-deletion that nothing broke. Low risk, reversible via git if wrong. |
</threat_model>

<verification>
1. `test ! -f src/components/ui/select.tsx && test ! -f src/components/ui/skeleton.tsx` — both dead files gone.
2. `test -f ecosystem.config.cjs && test -f src/migrations/index.ts` — false positives untouched.
3. `node -e "console.log(require('./package.json').dependencies.next)"` prints a 16.2.x version.
4. `npm ls next @payloadcms/next` shows no UNMET/invalid peer markers.
5. `grep -rn "JSON.parse(JSON.stringify" scripts/migrate/` returns zero matches across the 3 target files.
6. `npx tsc --noEmit` passes with no new errors.
7. Final full re-scan: `npx react-doctor@latest --verbose` — confirm the specific `deslop/unused-file` findings for select.tsx/skeleton.tsx are gone, the `no-vulnerable-react-server-components` finding is gone, and all 3 `no-json-parse-stringify-clone` findings are gone. Confirm the other ~70 findings are unchanged in count (no new findings introduced by these edits).
</verification>

<success_criteria>
- select.tsx and skeleton.tsx deleted; all other unused-file findings left as documented false positives.
- next bumped to a 16.2.x version that is both CVE-2026-23870-patched and inside @payloadcms/next@3.85.2's peer range; npm install clean, tsc/build clean.
- All 3 JSON.parse(JSON.stringify(x)) sites replaced with structuredClone(x); tsc clean.
- Zero other react-doctor findings touched.
</success_criteria>

<output>
Create `.planning/quick/260713-1zw-fix-top-3-react-doctor-issues-unused-fil/260713-1zw-SUMMARY.md` when done
</output>
