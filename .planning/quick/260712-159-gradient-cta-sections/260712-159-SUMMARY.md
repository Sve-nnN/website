---
phase: quick-260712-159
plan: 01
subsystem: ui
tags: [shader, hero-grain-gradient, cta, contact-form, tailwind]

# Dependency graph
requires:
  - phase: 16-hero-grainy-gradient-implementation
    provides: HeroGrainGradient component (WebGL2 feature-detect, prefers-reduced-motion, ShaderErrorBoundary fallback)
provides:
  - Shader background reused on the ContactFormBlock sidebar
  - Shader background reused on the CallToAction strip
affects: [contact-page, home-page, any-page-with-calltoaction-block]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shader-background CTA wrapper: relative overflow-hidden host > HeroGrainGradient (absolute inset-0) > bg-black/25 contrast overlay > relative z-10 content wrapper with text-secondary-foreground"

key-files:
  created: []
  modified:
    - src/blocks/ContactFormBlock/Component.tsx
    - src/blocks/CallToAction/Component.tsx

key-decisions:
  - "Reused HeroGrainGradient as-is (no props, no fork) per plan's interfaces contract -- both call sites mount it exactly as Hero does, no new WebGL/reduced-motion logic written"

patterns-established:
  - "Shader-background CTA wrapper pattern (see tech-stack.patterns) -- future CTA sections needing this treatment should copy this three-layer structure"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-07-12
---

# Quick Task 260712-159: Gradient CTA Sections Summary

**Reused the Hero's validated grain-gradient WebGL shader as an animated background for the contact-sidebar and CallToAction strip CTAs, each behind a dark contrast overlay for text legibility.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-12T05:42:00Z
- **Completed:** 2026-07-12T05:54:40Z
- **Tasks:** 2 of 3 (Tasks 1-2 complete; Task 3 is a blocking human-verify checkpoint, not yet resolved)
- **Files modified:** 2

## Accomplishments
- ContactFormBlock sidebar ("Charlemos sobre tu próximo proyecto" / "Let's talk about your next project") now renders `<HeroGrainGradient />` as its background instead of a flat `bg-secondary` fill
- CallToAction strip ("Ready to work together?") now renders the same shader background instead of no background at all
- Both sections keep a `bg-black/25` contrast overlay and `text-secondary-foreground` copy for legibility over the shader
- Zero new WebGL/reduced-motion logic written — both call sites mount `HeroGrainGradient` exactly as `Hero/Component.tsx` does
- `HeroGrainGradient.tsx` and `Hero/Component.tsx` left completely untouched — Hero's own shader is unaffected

## Task Commits

Each task was committed atomically:

1. **Task 1: Add shader background to the ContactFormBlock sidebar** - `455885a` (feat)
2. **Task 2: Add shader background to the CallToAction strip** - `f76678f` (feat)

**Plan metadata:** committed separately as part of this SUMMARY/STATE update

## Files Created/Modified
- `src/blocks/ContactFormBlock/Component.tsx` - Sidebar wrapped in `relative overflow-hidden` host rendering `HeroGrainGradient` + `bg-black/25` overlay; existing sidebar content (title, description, contact info list, social proof) moved into an inner `relative z-10 text-secondary-foreground p-8` wrapper, unchanged in substance
- `src/blocks/CallToAction/Component.tsx` - `Container` wrapped in a `relative overflow-hidden` `<section>` rendering `HeroGrainGradient` + `bg-black/25` overlay; `Container` gets `relative z-10`; `RichTextRenderer` receives `className="text-secondary-foreground"` for contrast; `links`/`CMSLink` block left as-is

## Decisions Made
- Followed the plan's `<interfaces>` contract exactly: `HeroGrainGradient` takes no props and is not forked — both call sites use it verbatim, matching Hero's own usage pattern
- No changes to non-button `CMSLink` text color (`text-primary`) in CallToAction, per plan rationale (already contrasts against the shader's near-black backdrop)

## Deviations from Plan

None - plan executed exactly as written for Tasks 1 and 2.

## Issues Encountered

None. `npx tsc --noEmit` passed cleanly after both tasks with no new errors.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Task 3 (checkpoint:human-verify, gate="blocking") is NOT resolved.** This is a code-only close-out; the plan's blocking human-verify checkpoint still requires Juan to:

1. Run `npm run dev` and open the contact page (`/contact` and `/es/contact`, or whatever the actual contact route is)
2. Confirm the sidebar shows the animated shader (not a flat navy fill), and that title, description, contact info list, and social-proof text are all clearly readable
3. Toggle light/dark theme (if available) and re-check readability in both
4. Find a page rendering the CallToAction block and confirm the shader renders there too, with readable text and working links/buttons, in both EN and ES
5. Confirm the home Hero's shader still looks unchanged (colors/shape untouched)
6. Optionally: enable OS-level "reduce motion" and confirm both new sections freeze (no animation), same as Hero

Once Juan approves (or reports issues to fix), this quick task can be marked fully complete. No code work is blocked in the meantime — the two source-code tasks are done, committed, and typecheck-clean.

---
*Quick task: 260712-159*
*Completed: 2026-07-12 (Tasks 1-2; checkpoint pending)*
