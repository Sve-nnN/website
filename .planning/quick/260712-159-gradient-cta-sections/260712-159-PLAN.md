---
quick_id: 260712-159
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/ContactFormBlock/Component.tsx
  - src/blocks/CallToAction/Component.tsx
autonomous: false

must_haves:
  truths:
    - "The 'Charlemos sobre tu próximo proyecto' / 'Let's talk about your next project' sidebar shows the animated grain-gradient shader as its background instead of a flat bg-secondary fill"
    - "The 'Ready to work together?' CTA strip shows the same animated grain-gradient shader as its background instead of no background"
    - "All text and links in both sections stay readable over the shader in light theme, dark theme, and both locales (EN/ES)"
    - "Both sections inherit HeroGrainGradient's existing WebGL2 feature-detect, prefers-reduced-motion handling, and error-boundary fallback (bg-secondary) without any new detection logic"
    - "Hero's own shader (variant: 'home') is visually unchanged — its colors/shape/config are untouched"
  artifacts:
    - path: "src/blocks/ContactFormBlock/Component.tsx"
      provides: "Sidebar wrapped in a relative/overflow-hidden container rendering HeroGrainGradient + contrast overlay behind the existing sidebar content"
      contains: "HeroGrainGradient"
    - path: "src/blocks/CallToAction/Component.tsx"
      provides: "CTA strip wrapped in a relative/overflow-hidden container rendering HeroGrainGradient + contrast overlay behind the existing richText/links content"
      contains: "HeroGrainGradient"
  key_links:
    - from: "src/blocks/ContactFormBlock/Component.tsx"
      to: "src/components/HeroGrainGradient.tsx"
      via: "import { HeroGrainGradient } from '@/components/HeroGrainGradient'"
      pattern: "import.*HeroGrainGradient.*@/components/HeroGrainGradient"
    - from: "src/blocks/CallToAction/Component.tsx"
      to: "src/components/HeroGrainGradient.tsx"
      via: "import { HeroGrainGradient } from '@/components/HeroGrainGradient'"
      pattern: "import.*HeroGrainGradient.*@/components/HeroGrainGradient"
---

<objective>
Reuse the already-validated `HeroGrainGradient` shader background (WebGL2 feature-detect, `ShaderErrorBoundary`, `prefers-reduced-motion` handling — all already implemented, none of it duplicated here) as the animated background of two CTA sections that currently render flat/no backgrounds:

1. The contact sidebar ("Charlemos sobre tu próximo proyecto" / "Let's talk about your next project") in `src/blocks/ContactFormBlock/Component.tsx`.
2. The "Ready to work together?" strip in `src/blocks/CallToAction/Component.tsx`.

Purpose: draw visual attention to both conversion CTAs using the same shader look established in the Hero, without reimplementing any of its feature-detection or accessibility logic, and without touching the Hero's own shader configuration.

Output: Both blocks render `<HeroGrainGradient />` as an absolutely-positioned background layer inside a `relative overflow-hidden` wrapper, with a semi-opaque contrast overlay between the shader and the content, and content colors adjusted to `text-secondary-foreground` (matching the Hero's own pattern for text-over-shader) so copy stays legible in both themes/locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- HeroGrainGradient is a self-contained client component. It renders its own
absolutely-positioned, full-bleed root div (`absolute inset-0 overflow-hidden`,
`aria-hidden="true"`), so it must be dropped inside a `relative` positioned
ancestor with defined dimensions — it does not manage its own layout. It takes
NO PROPS. Do not add props to it or fork it; both call sites use it exactly as
Hero does. -->

From src/components/HeroGrainGradient.tsx:
```typescript
export function HeroGrainGradient(): JSX.Element
// - No props.
// - Renders `<div className="absolute inset-0 overflow-hidden" aria-hidden="true" ...>`
//   containing either the GrainGradient shader (WebGL2-capable) or a
//   `bg-secondary` fallback div (no WebGL2 / render error).
// - Already handles: WebGL2 feature-detect, prefers-reduced-motion,
//   light/dark color swap (reads `document.documentElement.classList.contains('dark')`),
//   and a ShaderErrorBoundary fallback to `bg-secondary`.
```

Reference usage pattern from `src/blocks/Hero/Component.tsx` (the pattern both
call sites in this plan must follow):
```tsx
<section className="relative ... overflow-hidden">
  <HeroGrainGradient />
  <Container className="relative z-10">
    {/* content, using text-secondary-foreground / text-secondary-foreground/80 for contrast */}
  </Container>
</section>
```
</interfaces>

<current_sidebar_markup>
`src/blocks/ContactFormBlock/Component.tsx` line 83 currently reads:
```tsx
<div className="bg-secondary text-secondary-foreground rounded-lg p-8">
```
This becomes the `relative overflow-hidden` shader host; its existing children
(sidebarTitle, sidebarDescription, contactInfo list, socialProofText) move into
an inner `relative z-10` wrapper that keeps the current `text-secondary-foreground`
class and `p-8` padding.
</current_sidebar_markup>

<current_cta_markup>
`src/blocks/CallToAction/Component.tsx` currently renders a plain `<Container>`
with no background — `RichTextRenderer` inherits `text-foreground` (dark-on-light
via `Prose`) and `CMSLink` non-button links use `text-primary`. Once a shader
background is added, `RichTextRenderer` needs `className="text-secondary-foreground"`
(the `Prose` component merges classNames via `cn()`/`twMerge`, so this overrides
the default `text-foreground` cleanly) to stay legible. `CMSLink` button-appearance
links (`appearance="default"`/`"outline"`) use solid `Button` variants and need no
change; non-button `CMSLink` instances keep `text-primary`, which already reads
fine against the shader's near-black backdrop (same ember-on-near-black contrast
Hero relies on).
</current_cta_markup>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shader background to the ContactFormBlock sidebar</name>
  <files>src/blocks/ContactFormBlock/Component.tsx</files>
  <action>
Import `HeroGrainGradient` from `@/components/HeroGrainGradient`. Replace the sidebar's outer `<div className="bg-secondary text-secondary-foreground rounded-lg p-8">` (line 83) with a two-layer structure: an outer `<div className="relative overflow-hidden rounded-lg">` that renders `<HeroGrainGradient />` first, then a contrast overlay `<div className="absolute inset-0 bg-black/25" aria-hidden="true" />`, then an inner `<div className="relative z-10 text-secondary-foreground p-8">` that wraps the existing sidebar children (sidebarTitle, sidebarDescription, contactInfo `<ul>`, socialProofText) completely unchanged. Do not alter any other part of the file — the form column on the left, the success/error messaging, and the honeypot field stay exactly as they are. Do not touch `src/components/HeroGrainGradient.tsx` itself.
  </action>
  <verify>
    <automated>grep -c "HeroGrainGradient" src/blocks/ContactFormBlock/Component.tsx</automated>
  </verify>
  <done>File imports and renders `<HeroGrainGradient />` once inside a `relative overflow-hidden` wrapper around the sidebar; sidebar content is unchanged in substance, now inside a `relative z-10` inner wrapper; `npx tsc --noEmit` (or equivalent project typecheck) passes with no new errors in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Add shader background to the CallToAction strip</name>
  <files>src/blocks/CallToAction/Component.tsx</files>
  <action>
Import `HeroGrainGradient` from `@/components/HeroGrainGradient`. Wrap the existing `<Container>` in an outer `<section className="relative overflow-hidden rounded-lg">` that renders `<HeroGrainGradient />` first, then a contrast overlay `<div className="absolute inset-0 bg-black/25" aria-hidden="true" />`. Add `relative z-10` to the existing `<Container>`'s className (keep its current `py-12 md:py-16 flex flex-col md:flex-row items-center justify-between gap-8` classes). Pass `className="text-secondary-foreground"` to the existing `<RichTextRenderer data={richText} ... />` call so paragraph/heading/link colors from `Prose` resolve against the shader background instead of the default `text-foreground`. Leave the `links.map` / `CMSLink` block as-is — button-appearance CMSLinks already render solid buttons unaffected by the new background, and non-button CMSLinks' `text-primary` already contrasts against the shader's near-black backdrop. Do not touch `src/components/HeroGrainGradient.tsx` or `src/blocks/Hero/Component.tsx`.
  </action>
  <verify>
    <automated>grep -c "HeroGrainGradient" src/blocks/CallToAction/Component.tsx</automated>
  </verify>
  <done>File imports and renders `<HeroGrainGradient />` once inside a `relative overflow-hidden` section wrapping the existing `Container`; `RichTextRenderer` receives `className="text-secondary-foreground"`; `npx tsc --noEmit` (or equivalent project typecheck) passes with no new errors in this file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Both CTA sections (contact-page sidebar and any page using the CallToAction block) now render the same animated grain-gradient shader background as the home Hero, with a dark contrast overlay behind their text/links.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open the contact page (e.g. `/contact` and `/es/contact`, whatever the actual route is in this project).
2. Confirm the "Let's talk about your next project" / "Charlemos sobre tu próximo proyecto" sidebar shows the animated shader (not a flat navy fill), and that title, description, contact info list, and social-proof text are all clearly readable.
3. Toggle light/dark theme (if a theme switcher exists in the header) and re-check readability in both.
4. Find a page rendering the CallToAction block ("Ready to work together?" strip) in both EN and ES, and confirm the shader renders there too with readable text and working CTA button(s)/link(s).
5. Confirm the home Hero's shader still looks unchanged (colors/shape untouched).
6. Optionally: enable OS-level "reduce motion" and confirm both new sections freeze (no animation) same as Hero does.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS content -> RichTextRenderer | `richText` field from Payload is trusted editorial content (not third-party user input), rendered via Payload's own Lexical JSX converters — no new trust boundary introduced by this change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q159-01 | Denial of Service | HeroGrainGradient (WebGL shader) now mounted 3x per page instead of 1x (Hero + sidebar + CTA strip) | accept | Purely client-side rendering cost on the visitor's own GPU; component already feature-detects WebGL2 and falls back to a static `bg-secondary` div, so worst case on low-end devices is a flat background, not a crash. No new dependency or attack surface introduced — same component, more mount points. |
| T-Q159-02 | Information Disclosure | ShaderErrorBoundary console.error on render failure | accept | Already-existing behavior in HeroGrainGradient (unmodified here); logs only the caught error object, no user data, per existing T-16-02 comment in the source file. |
</threat_model>

<verification>
1. `grep -c "HeroGrainGradient" src/blocks/ContactFormBlock/Component.tsx` returns `1` (import) or `2` (import + usage) — confirms wiring.
2. `grep -c "HeroGrainGradient" src/blocks/CallToAction/Component.tsx` returns `1` or `2` — confirms wiring.
3. Project typecheck (`npx tsc --noEmit` or the project's configured `npm run` typecheck script) passes with no new errors.
4. Human verification checkpoint confirms visual readability in both themes and both locales, and confirms Hero itself is unaffected.
</verification>

<success_criteria>
- Both `src/blocks/ContactFormBlock/Component.tsx` and `src/blocks/CallToAction/Component.tsx` render `HeroGrainGradient` as a background layer, reusing the component as-is (no new WebGL/reduced-motion logic written).
- Text and links in both sections remain legible over the shader in light theme, dark theme, and both locales.
- `src/components/HeroGrainGradient.tsx` and `src/blocks/Hero/Component.tsx` are untouched — Hero's shader configuration is unaffected.
- Human checkpoint approved.
</success_criteria>

<output>
Create `.planning/quick/260712-159-gradient-cta-sections/260712-159-SUMMARY.md` when done
</output>
