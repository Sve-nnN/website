---
phase: quick-260813-o0n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/navigation.ts
  - src/components/CaseStudyCard.tsx
  - src/components/WebsiteCard.tsx
  - src/components/AuthorByline.tsx
  - src/components/AuthorCard.tsx
  - src/components/PostCard.tsx
  - src/components/FeaturedEntry.tsx
  - src/components/CMSLink.tsx
  - src/components/SiteHeaderChrome.tsx
  - src/app/(frontend)/[locale]/authors/page.tsx
  - src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx
  - src/app/(frontend)/[locale]/search/page.tsx
  - src/app/(frontend)/[locale]/websites/[slug]/page.tsx
  - src/components/Breadcrumbs.tsx
  - src/components/LocaleSwitcher.tsx
  - src/blocks/Hero/Component.tsx
  - src/app/(frontend)/[locale]/websites/page.tsx
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
  - src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
  - src/lib/blog-paths.ts
  - scripts/verify-locale-links.mjs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "On /en/case-studies, every case-study card href starts with /en/case-studies/"
    - "On /en/blog, every post and category href starts with /en/blog/"
    - "On /en/websites and /en/authors, every card href starts with /en/"
    - "The header nav items and the header CTA button on any /en page point at /en/... paths"
    - "No rendered page anywhere contains a doubly-prefixed href (a second locale segment stacked on the first)"
    - "Spanish pages keep their unprefixed internal hrefs — no /en leaks into the default-locale tree except the locale switcher itself"
    - "Breadcrumb trails still render exactly one locale segment, and BreadcrumbList JSON-LD URLs are byte-identical to before this change"
    - "External/admin-authored absolute URLs (speaking-event links, social links, custom CMS urls) are never given a locale prefix"
  artifacts:
    - "src/i18n/navigation.ts exporting Link, redirect, usePathname, useRouter, getPathname from createNavigation(routing), plus the isPrefixableHref guard"
    - "scripts/verify-locale-links.mjs — runnable HTTP assertion script against a live server"
    - "Corrected localizeBlogPath docblock in src/lib/blog-paths.ts"
    - "An explicit in-file comment on each of the 6 files deliberately left on next/link"
  key_links:
    - "src/i18n/navigation.ts consumes the SAME routing object (src/i18n/routing.ts) the middleware consumes — one source of truth for localePrefix/defaultLocale"
    - "NextIntlClientProvider in src/app/(frontend)/[locale]/layout.tsx (line 69) supplies the locale that the client-side Link reads via useLocale — every file touched here renders inside that provider"
    - "CMSLink routes admin-authored hrefs through isPrefixableHref: internal unprefixed paths get the locale-aware Link, everything else (absolute, mailto/tel, #anchor, already-prefixed) falls through to plain next/link"
    - "src/lib/breadcrumbs.ts already emits locale-prefixed urls — Breadcrumbs.tsx and its 4 inline consumers MUST stay on next/link or they double-prefix"
---

<objective>
Create the locale-aware `Link` that `src/i18n/` never had, and route every internal
`<Link href>` that is currently unprefixed through it, so a reader on `/en` stops
being dropped into Spanish on the next click.

Purpose: `routing.ts` uses `localePrefix: 'as-needed'` with `defaultLocale: 'es'`,
so an unprefixed path IS the Spanish URL. The middleware only rewrites INCOMING
requests; nothing rewrites an OUTGOING href. Confirmed against production: with
`Cookie: NEXT_LOCALE=en`, `/blog/development/react-19` returns 200 and
`<html lang="es">` — there is no fallback path. The fix must happen at render time.

Output: `src/i18n/navigation.ts`, 12 files swapped to the locale-aware `Link`,
6 files deliberately left on `next/link` with the reason written in the file, one
factually wrong docblock corrected, and a runnable HTTP verification script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@src/i18n/routing.ts
@src/middleware.ts
@src/lib/blog-paths.ts
@src/lib/breadcrumbs.ts
@src/components/CMSLink.tsx
@src/components/LocaleSwitcher.tsx
@src/components/Breadcrumbs.tsx
</context>

<investigation_findings>

This section is the result of reading every file in scope. It is NOT a mechanical
find-replace: 6 of the 18 listed files must NOT be swapped, and the reason differs
per file. Do not deviate from these per-file calls without re-deriving them.

**Facts established before planning (do not re-investigate):**

1. `next-intl@4.13.4` is installed. `createNavigation` is the v4 API.
2. `NextIntlClientProvider` already wraps the entire frontend tree
   (`src/app/(frontend)/[locale]/layout.tsx:69`), so `useLocale()` resolves inside
   every client component touched here. No new provider is needed.
3. `next/link` is already a client component, so swapping it for next-intl's `Link`
   introduces NO new client boundary and needs NO new `'use client'` directive
   anywhere. Server Components may render it directly (children pass through as a
   slot).
4. Per next-intl source (`createSharedNavigationFns.tsx`), `Link` calls
   `isLocalizableHref(href)` and skips prefixing entirely for non-localizable hrefs
   (absolute `https://…`, and anything not starting with `/`). We do NOT rely on
   that for admin-authored data — see the `isPrefixableHref` guard below — but it
   means an accidental external href degrades safely rather than corrupting.
5. Per next-intl docs, passing an explicit `locale` prop ALWAYS emits a locale
   prefix, even under `as-needed`, on purpose (the prefixed URL updates the cookie,
   then redirects). That is why `LocaleSwitcher` is left alone — see below.
6. `npx tsc --noEmit` is clean on the current tree (exit 0, verified). It is
   therefore a meaningful gate: any new error is caused by this work.
7. There is no `typecheck` npm script. Use `npx tsc --noEmit`.

**SWAP — 12 files whose hrefs are internal and unprefixed (currently broken on /en):**

| File | Link site(s) | href today |
|---|---|---|
| `src/components/CaseStudyCard.tsx` | L16 | `` `/case-studies/${slug}` `` |
| `src/components/WebsiteCard.tsx` | L11 | `` `/websites/${slug}` `` |
| `src/components/AuthorByline.tsx` | L24 | `` `/authors/${slug}` `` |
| `src/components/AuthorCard.tsx` | L44, L49 | `` `/authors/${slug}` `` (L90 is a plain `<a>` to `social.url` — leave it) |
| `src/components/PostCard.tsx` | L38 | `blogPostPath(...)` or a caller-passed href, both unprefixed |
| `src/components/FeaturedEntry.tsx` | L88 | `` `/case-studies/${slug}` `` or `props.href` = `blogPostPath(...)`, both unprefixed |
| `src/components/CMSLink.tsx` | L47, L60 | admin-authored — needs the guard, see below |
| `src/components/SiteHeaderChrome.tsx` | L150 logo, L201/L252 CTA | logo hand-prefixes; CTA is `ctaButton.href ?? '/contact'`, unprefixed |
| `src/app/(frontend)/[locale]/authors/page.tsx` | L57 | `` `/authors/${slug}` `` |
| `src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx` | L156 | `blogCategoryPath(...)` |
| `src/app/(frontend)/[locale]/search/page.tsx` | L129 | `hrefFor(...)` → `/blog/...`, `/case-studies/...`, `/authors/...`, or `'#'` |
| `src/app/(frontend)/[locale]/websites/[slug]/page.tsx` | L241 ONLY | `` `/case-studies/${slug}` `` — **L132 is `crumb.url`, leave it on `next/link`** |

**DO NOT SWAP — 6 files, each for a different concrete reason:**

| File | Reason |
|---|---|
| `src/components/Breadcrumbs.tsx` | `src/lib/breadcrumbs.ts` ALREADY emits locale-prefixed urls (`homeHref()` returns `/en`; `sectionIndexHref()` builds on it). Swapping would produce a stacked second locale segment. It is also the same array `buildBreadcrumbJsonLd()` turns into absolute JSON-LD URLs — changing the lib would corrupt structured data. Trail links are already correct on /en today. |
| `src/app/(frontend)/[locale]/websites/page.tsx` | Its only `<Link>` (L65) is `crumb.url` — already prefixed. Nothing to fix. |
| `src/blocks/Hero/Component.tsx` | Its only `<Link>` (L132) is `crumb.url` — already prefixed. Nothing to fix. (Hero's CTA row goes through `CMSLink`, which IS fixed.) |
| `src/app/(frontend)/[locale]/authors/[slug]/page.tsx` | Its only `<Link>` (L359) is `event.link` — an admin-authored EXTERNAL speaking-event URL rendered with `target="_blank"` + an ExternalLink icon. Must never be locale-prefixed. |
| `src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx` | Both link sites are already locale-correct: L173 is `crumb.url` (prefixed), and L337/L340 hand-build `` `${localePrefix}/contact` `` from `const localePrefix = locale === 'es' ? '' : '/en'` (L112). Already right on /en. |
| `src/components/LocaleSwitcher.tsx` | Cross-locale by design. Converting to `<Link locale={other}>` would be *worse*: next-intl always emits a prefix when a `locale` prop is present, so switching to Spanish would render `/es/servicios` — a non-canonical URL in the HTML plus a redirect hop, on a site whose whole value proposition is SEO. Its existing manual strip-and-rebuild + `normalizeServiceHref` + explicit `hrefLang` already produces the correct canonical target. |

**DEFERRED — hard exclusion, another session holds uncommitted edits in these files:**

- `src/components/SiteFooter.tsx` (still imports `next/link`)
- `src/blocks/ServicesShowcase/Component.tsx` (still imports `next/link`)
- `src/lib/service-slugs.ts`

Do NOT read-modify-write, `git add`, or commit any of the three. Their footer/showcase
link swaps are a deliberate follow-up, not an oversight. After this plan lands, footer
links and the home ServicesShowcase cards on `/en` will STILL drop to Spanish — that is
the known, accepted residual, and the verification script below explicitly excludes them
so it does not report a false PASS or a false FAIL.

</investigation_findings>

<tasks>

<task type="tracer">
  <name>Task 1: Create the locale-aware Link and prove one card end-to-end on /en</name>
  <precondition>A dev server is reachable at http://localhost:3000 (`npm run dev`). It reads the real Neon DB read-only; no writes, no migrations.</precondition>
  <files>src/i18n/navigation.ts, src/components/CaseStudyCard.tsx</files>
  <read_first>src/i18n/routing.ts, src/components/CaseStudyCard.tsx</read_first>
  <action>
Create `src/i18n/navigation.ts`. Import `createNavigation` from `next-intl/navigation`
and the existing `routing` object from `./routing` — reuse it, never re-declare locales
or `localePrefix`, because the middleware consumes that same object and the two must not
drift. Destructure and re-export `Link`, `redirect`, `usePathname`, `useRouter`, and
`getPathname`.

In the same file, export a pure guard `isPrefixableHref(href: string): boolean`. It
returns true only when the href is an app-internal path that next-intl should prefix:
starts with a single forward slash, is not protocol-relative (a leading double slash),
and does not already begin with a locale segment from `routing.locales` (match the whole
segment — the bare locale path and the locale followed by a slash both count, so a slug
that merely starts with those two letters is not mistaken for a prefix). Everything else
returns false: absolute URLs, `mailto:`/`tel:`, bare fragments, and hrefs an editor
already prefixed by hand. This exists because next-intl's own internal check is an
implementation detail, and the hrefs flowing through `CMSLink` come from the admin panel
where any of those shapes is possible.

Write a docblock at the top of the file stating the actual mechanism: the middleware
rewrites incoming requests only, so an unprefixed href rendered on an /en page navigates
the reader to Spanish content — this module is what prefixes outgoing hrefs, and it is
the only correct import for an internal `<Link>`.

Then convert `src/components/CaseStudyCard.tsx`: replace the default `next/link` import
with the named `Link` from `@/i18n/navigation`. Change nothing else — the href expression,
className, and children stay byte-identical. This one card is the vertical slice: i18n
module to component to rendered HTML on a real server.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/i18n/navigation.ts','utf8'); for (const t of ['createNavigation','isPrefixableHref','getPathname','usePathname','useRouter','redirect']) if(!s.includes(t)) { console.error('MISSING export/usage: '+t); process.exit(1);} console.log('navigation.ts OK')"</automated>
    <automated>curl -s http://localhost:3000/en/case-studies | grep -o 'href="/en/case-studies/[^"]*"' | head -3 && test "$(curl -s http://localhost:3000/en/case-studies | grep -c 'href="/case-studies/')" -eq 0 && test "$(curl -s http://localhost:3000/case-studies | grep -c 'href="/case-studies/')" -gt 0 && echo TRACER_PASS</automated>
  </verify>
  <done>`src/i18n/navigation.ts` exists and typechecks. On a live server, `/en/case-studies` renders at least one card href carrying the `/en` prefix and zero unprefixed `/case-studies/` card hrefs, while the Spanish `/case-studies` still renders unprefixed card hrefs. The path from routing config to rendered anchor is proven before any other file is touched.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the swap to the remaining 11 files, and write down the 6 refusals</name>
  <files>src/components/WebsiteCard.tsx, src/components/AuthorByline.tsx, src/components/AuthorCard.tsx, src/components/PostCard.tsx, src/components/FeaturedEntry.tsx, src/components/CMSLink.tsx, src/components/SiteHeaderChrome.tsx, src/app/(frontend)/[locale]/authors/page.tsx, src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx, src/app/(frontend)/[locale]/search/page.tsx, src/app/(frontend)/[locale]/websites/[slug]/page.tsx, src/components/Breadcrumbs.tsx, src/components/LocaleSwitcher.tsx, src/blocks/Hero/Component.tsx, src/app/(frontend)/[locale]/websites/page.tsx, src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx, src/lib/blog-paths.ts</files>
  <read_first>src/components/CMSLink.tsx, src/components/SiteHeaderChrome.tsx, src/lib/blog-paths.ts</read_first>
  <action>
**A. Straight import swap (7 files, no other change).** In `WebsiteCard.tsx`,
`AuthorByline.tsx`, `AuthorCard.tsx`, `PostCard.tsx`, `FeaturedEntry.tsx`,
`authors/page.tsx`, and `blog/[category]/[slug]/page.tsx`: replace the default
`next/link` import with the named `Link` from `@/i18n/navigation`. Leave every href
expression exactly as it is — they are already the correct unprefixed input, and the
`Link` supplies the prefix. In `AuthorCard.tsx` do not touch the `<a href={social.url}>`
social row at L90; it is an external link and is already a plain anchor.

**B. `search/page.tsx`.** Same import swap. `hrefFor()` can return a bare `'#'` for an
unknown `relationTo`; that has no leading slash so next-intl passes it through untouched.
No guard needed, no change to `hrefFor()` — it must keep returning unprefixed paths.

**C. `websites/[slug]/page.tsx` — mixed file.** Keep the existing default `next/link`
import for the breadcrumb `<Link href={crumb.url}>` at L132 (already prefixed by
`buildTrail`). ADD a second import, `Link as LocaleLink` from `@/i18n/navigation`, and
use `LocaleLink` for the related-case-study link at L241 only. Put a one-line comment at
the breadcrumb link saying its href arrives already locale-prefixed, which is why it
keeps the plain component.

**D. `CMSLink.tsx` — admin-authored hrefs.** Import both the plain component (keep the
existing default import, renaming the local identifier if needed for clarity) and the
locale-aware `Link` plus `isPrefixableHref` from `@/i18n/navigation`. After the existing
href resolution block, choose the component: when `isPrefixableHref(href)` is true use
the locale-aware one, otherwise use the plain one. Apply that choice in BOTH render
branches (the `appearance` Button-asChild branch and the default anchor branch) — pick
the component into a single local variable before the return so the two branches cannot
drift. Everything else — `target`, `rel`, `className`, `aria-current`, `Button asChild`
composition — stays identical. Add a short comment naming the three shapes the guard
protects against: an absolute external URL, a bare fragment, and an href an editor
already prefixed by hand. Note that `type === 'reference'` always produces an internal
unprefixed path, so references always take the locale-aware branch.

**E. `SiteHeaderChrome.tsx`.** Import the locale-aware `Link` and `isPrefixableHref`,
and keep the plain import for the fallback branch. Two changes:
  1. The logo link at L150 currently hand-picks its target from the `locale` prop.
     Replace that expression with the site root path and let the locale-aware `Link`
     derive the prefix. Keep the `locale` prop itself — it is used elsewhere in the file.
  2. The two CTA buttons (L201 and L252) render `ctaButton.href ?? '/contact'`, which is
     admin-authored from the Header global. Run it through the same `isPrefixableHref`
     component choice used in `CMSLink`, so an editor who types an absolute URL or an
     already-prefixed path is not corrupted. Both call sites must use the same resolved
     component — compute it once above the return.
  The nav items themselves need no work here: they already render through `CMSLink`,
  which step D fixes.

**F. The six refusals — each gets a comment in its own file** so a future reader does not
"finish the job" and reintroduce the bug. Add a concise comment at the relevant import or
link site stating the reason, using the wording of the reason column in the plan's
investigation table above:
  - `Breadcrumbs.tsx`: trail urls arrive already locale-prefixed from `src/lib/breadcrumbs.ts`,
    and the same array feeds `buildBreadcrumbJsonLd()`.
  - `websites/page.tsx` and `Hero/Component.tsx`: their only link renders a breadcrumb url,
    already prefixed.
  - `authors/[slug]/page.tsx`: its only link is an external speaking-event URL.
  - `case-studies/[slug]/page.tsx`: one breadcrumb url plus two CTAs that already build the
    prefix from the `localePrefix` constant at L112.
  - `LocaleSwitcher.tsx`: cross-locale by design; an explicit `locale` prop would force a
    prefix even for the default locale, emitting a non-canonical URL plus a redirect hop.
Do not otherwise modify these six files.

**G. `src/lib/blog-paths.ts` — correct the false docblock.** Rewrite only the second
sentence of the `localizeBlogPath` docblock. Delete its assertion that the middleware
handles in-page hrefs — the middleware only handles incoming requests, and that false
premise is what spread this bug across 20 files. State instead that this helper is
for absolute metadata/canonical/sitemap URLs, and that in-page hrefs stay unprefixed
because the locale-aware `Link` from `@/i18n/navigation` adds the prefix at render time.
Change no code in this file: `blogPostPath()`, `blogCategoryPath()`, and
`blogIndexPath()` must keep returning unprefixed paths, and `localizeBlogPath()` and all
its metadata callers are correct as-is.

**H. Untouchable.** Do not open, edit, stage, or commit `src/components/SiteFooter.tsx`,
`src/blocks/ServicesShowcase/Component.tsx`, or `src/lib/service-slugs.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const swapped=['src/components/CaseStudyCard.tsx','src/components/WebsiteCard.tsx','src/components/AuthorByline.tsx','src/components/AuthorCard.tsx','src/components/PostCard.tsx','src/components/FeaturedEntry.tsx','src/app/(frontend)/[locale]/authors/page.tsx','src/app/(frontend)/[locale]/blog/[category]/[slug]/page.tsx','src/app/(frontend)/[locale]/search/page.tsx'];let bad=[];for(const f of swapped){const L=fs.readFileSync(f,'utf8').split('\n');if(L.some(l=>/^import\s+Link\s+from\s+.next\/link./.test(l)))bad.push('still default-imports: '+f);if(!L.some(l=>/^import\s*\{[^}]*\bLink\b[^}]*\}\s*from\s*.@\/i18n\/navigation./.test(l)))bad.push('missing i18n import: '+f);}const untouched=['src/components/SiteFooter.tsx','src/blocks/ServicesShowcase/Component.tsx'];for(const f of untouched){const L=fs.readFileSync(f,'utf8').split('\n');if(!L.some(l=>/^import\s+Link\s+from\s+.next\/link./.test(l)))bad.push('deferred file was modified: '+f);}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('import gates OK')"</automated>
    <automated>git status --porcelain src/components/SiteFooter.tsx src/blocks/ServicesShowcase/Component.tsx src/lib/service-slugs.ts | grep -qE '^[MARD]' && { echo 'FAIL: a deferred file is staged'; exit 1; } || echo 'deferred files unstaged OK'</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/blog-paths.ts','utf8');if(/resolved by the next-intl\s*\n?\s*\*?\s*middleware/.test(s.replace(/\s+/g,' '))||/resolved by the next-intl middleware/.test(s.replace(/\s+/g,' '))){console.error('false middleware claim still present');process.exit(1)}if(!/export function blogPostPath/.test(s)||/localizeBlogPath\(locale: Locale, path: string\): string \{\s*return locale === 'en'/.test(s)===false){console.error('blog-paths behavior changed');process.exit(1)}console.log('blog-paths OK')"</automated>
  </verify>
  <done>`npx tsc --noEmit` still exits 0. The 9 straight-swap files import the named `Link` from `@/i18n/navigation` and no longer default-import from the plain router package. `CMSLink`, `SiteHeaderChrome`, and `websites/[slug]/page.tsx` compile with both components and route each href through the guard. The 6 refusal files carry an in-file reason. `blog-paths.ts` behavior is unchanged and its docblock no longer asserts middleware href resolution. The 3 deferred files are byte-unchanged and unstaged.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable proof that /en links carry the prefix and Spanish is untouched</name>
  <precondition>A dev server is reachable at http://localhost:3000 (`npm run dev`), serving the code from Tasks 1 and 2.</precondition>
  <files>scripts/verify-locale-links.mjs</files>
  <action>
Write `scripts/verify-locale-links.mjs`, a plain Node ESM script (no new dependencies)
that fetches real pages from `process.env.VERIFY_BASE_URL ?? 'http://localhost:3000'`
and asserts the fix rather than describing it. Exit 1 on any failure, 0 on all-pass,
printing one line per assertion with the URL and the offending match when it fails.

Pages to fetch: `/en`, `/en/blog`, `/en/case-studies`, `/en/websites`, `/en/authors`,
and the Spanish controls `/blog`, `/case-studies`, `/websites`. Fail loudly if any
returns a non-200 — a silent 404 must not read as a pass.

Scoping helper, applied before the negative assertions: slice each document at its LAST
closing `main` tag and keep only the part before it. The layout renders the header above
the page's own main element and the footer after it, so this keeps the header (in scope,
fixed by `CMSLink`) and drops the footer (deferred, still unprefixed). Then remove any
anchor whose `hreflang` attribute is `es` — that is the locale switcher, whose Spanish
target is correctly unprefixed and would otherwise trip the checks.

Assertions:
  1. Double-prefix guard, on the FULL document of every page including the footer: the
     count of hrefs containing a locale segment immediately followed by a second locale
     segment must be zero. Build the pattern from the locale list rather than hardcoding
     one pair. This is the single most important regression check — it catches any file
     where a locale-aware Link was pointed at an already-prefixed href.
  2. Unprefixed-internal guard, on the scoped slice of each `/en/...` page: zero hrefs
     matching the section roots `/blog/`, `/case-studies/`, `/authors/`, `/websites/`
     without the locale segment. Exclude `/en` (the home page) from this one and say why
     in a comment: home renders the deferred ServicesShowcase block.
  3. Positive proof, so absence alone cannot pass: `/en/blog` must contain at least one
     href beginning with the locale segment plus `/blog/`, and `/en/case-studies` at
     least one beginning with the locale segment plus `/case-studies/`.
  4. Header proof on `/en`: within the slice taken before the page's main element, at
     least one nav or CTA href begins with the locale segment, and none of the header's
     internal hrefs is an unprefixed section root.
  5. Spanish no-regression, on the scoped slice of `/blog`, `/case-studies`, `/websites`:
     zero hrefs beginning with the locale segment (the switcher is already stripped), and
     at least one unprefixed section-root href still present.

Print a final summary line with the pass/fail count, then run it.
  </action>
  <verify>
    <automated>node scripts/verify-locale-links.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <human-check>Load http://localhost:3000/en in a browser, click through: a case study card, a blog post card, a header nav item, and the header CTA. Every destination stays in English with the /en prefix in the address bar. Then click the locale switcher and confirm it lands on the unprefixed Spanish equivalent, not /es/... . Known and accepted for now: footer links and the home "services showcase" cards still drop to Spanish — those two files are held by another session and are a deliberate follow-up.</human-check>
  </verify>
  <done>`node scripts/verify-locale-links.mjs` exits 0 with every assertion passing: /en section pages emit prefixed internal hrefs, at least one prefixed href is positively present on the blog and case-study listings, the header on /en is prefixed, no page anywhere emits a stacked double locale segment, and the three Spanish control pages are byte-equivalent in linking behavior to before the change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload admin → rendered anchor | `CMSLink.url`, `Header.ctaButton.href`, and `SpeakingEvents.link` are editor-authored strings that reach `href` attributes. |
| Reader browser → Next middleware | Locale resolution happens on the request; this change alters what the server *emits*, not what it accepts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o0n-01 | Tampering | `CMSLink` href handling | medium | mitigate | `isPrefixableHref` only rewrites hrefs starting with a single slash. An absolute or scheme-bearing URL is never rewritten, so the guard cannot be used to turn an external URL into a same-origin one or vice versa. |
| T-o0n-02 | Elevation of Privilege | `CMSLink` / `ctaButton.href` | low | accept | A `javascript:` URI in an admin-authored url is a pre-existing risk not introduced here; `isPrefixableHref` returns false for it, so behavior is unchanged. Out of scope for this fix; note it in the SUMMARY as a standing item rather than expanding scope. |
| T-o0n-03 | Information Disclosure | Breadcrumb JSON-LD | low | mitigate | `src/lib/breadcrumbs.ts` is not modified, so `buildBreadcrumbJsonLd()` output stays byte-identical; the plan explicitly forbids changing the trail url shape. |
| T-o0n-04 | Denial of Service | Locale switcher redirect hop | low | mitigate | Leaving `LocaleSwitcher` on the plain component avoids emitting `/es/...` hrefs that would force an extra 307 per switch and publish non-canonical URLs to crawlers. |
| T-o0n-SC | Tampering | package installs | low | accept | Zero new dependencies. `next-intl@4.13.4` is already installed and `createNavigation` is part of it — no install step, so no package-legitimacy gate applies. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 (it did before this work, so any error is ours).
2. `node scripts/verify-locale-links.mjs` exits 0 against a live server.
3. `git status --porcelain` shows no modification to `src/components/SiteFooter.tsx`,
   `src/blocks/ServicesShowcase/Component.tsx`, or `src/lib/service-slugs.ts`.
4. `git diff -- src/lib/breadcrumbs.ts` is empty — breadcrumb urls and their JSON-LD
   must be untouched.
5. `git diff -- src/lib/blog-paths.ts` contains comment lines only, no changed
   expression.
6. No migration was generated and no script wrote to the database. This is frontend
   routing only, per CLAUDE.md's Database Safety section.
</verification>

<success_criteria>
- A reader on any `/en` section page can click a card, a breadcrumb, a nav item, or the
  header CTA and stay in English.
- No page renders a stacked double locale segment in an href.
- Spanish pages render exactly the hrefs they rendered before.
- Breadcrumb `BreadcrumbList` JSON-LD is unchanged.
- The 6 files deliberately left on the plain link component each say why, in the file.
- The 3 deferred files are untouched and the residual gap (footer + home services
  showcase still unprefixed on /en) is stated in the SUMMARY as a named follow-up.
</success_criteria>

<output>
Create `.planning/quick/260813-o0n-add-locale-aware-link-via-next-intl-navi/260813-o0n-SUMMARY.md` when done.
Include: the per-file decision table as executed, any file where the decision changed
during execution and why, and an explicit "Follow-up required" section naming
`src/components/SiteFooter.tsx` and `src/blocks/ServicesShowcase/Component.tsx`.
</output>
