---
phase: 22-breadcrumbs-visual-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/breadcrumbs.ts
  - src/app/(frontend)/[locale]/servicios/page.tsx
  - src/app/(frontend)/[locale]/services/page.tsx
  - src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
  - src/app/(frontend)/[locale]/services/[slug]/page.tsx
autonomous: true
requirements: [BREAD-01, BREAD-02, BREAD-03]

must_haves:
  truths:
    - "Visitor sees a visible breadcrumb trail (Inicio/Home > Servicios/Services [> nombre de la landing]) at the top of /servicios, /en/services, and each of the 4 individual service landings, in both locales"
    - "Each of those 5 pages (index + 4 landings) emits a BreadcrumbList JSON-LD <script> tag whose entries match the visible trail exactly, in both locales"
    - "The generated BreadcrumbList JSON-LD passes a seo-schema agent validation pass with no missing required properties, checked for both locales before the phase is considered closed"
    - "No new Payload field, collection change, or migration was introduced — breadcrumbs are 100% computed at render time from SERVICE_SLUGS + the already-fetched page title"
  artifacts:
    - path: "src/lib/breadcrumbs.ts"
      provides: "Single source of truth buildTrail()/buildBreadcrumbJsonLd() consumed by all 4 page.tsx files"
      exports: ["buildTrail", "buildBreadcrumbJsonLd"]
    - path: "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"
      provides: "3-level breadcrumb trail + BreadcrumbList JSON-LD wired for the ES service landing route"
    - path: "src/app/(frontend)/[locale]/services/[slug]/page.tsx"
      provides: "3-level breadcrumb trail + BreadcrumbList JSON-LD wired for the EN service landing route"
  key_links:
    - from: "src/app/(frontend)/[locale]/servicios/page.tsx"
      to: "src/lib/breadcrumbs.ts"
      via: "import { buildTrail, buildBreadcrumbJsonLd } from '@/lib/breadcrumbs'"
      pattern: "buildTrail\\("
    - from: "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"
      to: "src/blocks/RenderBlocks.tsx"
      via: "blockProps override forces the computed trail into the seeded Hero block's editorial breadcrumbs field"
      pattern: "blockProps=\\{\\{\\s*hero:\\s*\\{\\s*breadcrumbs"
    - from: "src/app/(frontend)/[locale]/servicios/page.tsx"
      to: "src/components/JsonLd.tsx"
      via: "<JsonLd data={buildBreadcrumbJsonLd(trail)} />"
      pattern: "buildBreadcrumbJsonLd\\("
---

<objective>
Give the Servicios index page and its 4 individual landings (both `/servicios(...)` ES and `/en/services(...)` EN routes) a visible breadcrumb trail plus a matching `BreadcrumbList` JSON-LD block, both derived from one shared pure function (`buildTrail()`), with zero Payload schema changes and zero migrations.

Purpose: Servicios today has no wayfinding trail and no BreadcrumbList structured data — a real gap versus the flat single-level nav most competitors also lack, but one the site's existing individual-landing IA (an established structural advantage per RESEARCH.md) should close rather than leave unpolished (BREAD-01/02/03).
Output: `src/lib/breadcrumbs.ts` (new pure module) + all 4 Servicios `page.tsx` route files wired to consume it, both visually (via the Hero block's existing `breadcrumbs` prop) and structurally (via a new inline `<JsonLd>` block per page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-breadcrumbs-visual-schema/22-CONTEXT.md

<interfaces>
<!-- Existing shapes the executor must match exactly — no exploration needed. -->

From src/blocks/Hero/config.ts (breadcrumbs field on the `hero` block, only rendered when `variant === 'listing'`):
```typescript
breadcrumbs?: { label: string; url: string; id?: string | null }[] | null
```

From src/blocks/Hero/Component.tsx (already renders this exact shape as a <nav aria-label="Breadcrumb"> — do not touch this file, only feed it data):
```typescript
{isListing && breadcrumbs && breadcrumbs.length > 0 && ( /* <nav> already implemented */ )}
```

From src/blocks/RenderBlocks.tsx (merge order — `blockProps` wins over the block's own Payload-sourced data because it spreads last):
```typescript
<Comp key={block.id ?? i} {...(block as unknown as Record<string, unknown>)} {...sharedProps} {...blockProps?.[block.blockType]} />
```
`blockProps={{ hero: { breadcrumbs: trail } }}` on `<RenderBlocks>` overrides ANY editorial `breadcrumbs` value on every `hero`-type block in that page's layout with the computed trail. Every Servicios page has exactly one `hero` block (`variant: 'listing'`, confirmed in `scripts/seed-phase19-service-pages.ts`), so this is safe.

From src/lib/services-data.ts (allowlist + fetch helpers, already imported by all 4 page.tsx files):
```typescript
export const SERVICES_INDEX_SLUG = 'services' as const
export const SERVICE_SLUGS = ['seo-technical-audit', 'seo-consulting', 'fullstack-development', 'ai-seo-geo'] as const
export type ServiceSlug = (typeof SERVICE_SLUGS)[number]
export function isServiceSlug(slug: string): slug is ServiceSlug
export async function getServicesIndexPage(locale: 'es' | 'en')
export async function getServicePage(locale: 'es' | 'en', slug: string)
```

From src/lib/sitemap-data.ts (SITE_URL + the exact ES/EN URL-segment convention already used for Servicios in the sitemap — reuse it verbatim, do not invent a different one):
```typescript
export const SITE_URL: string // e.g. "https://juancarlosangulo.com", no trailing slash
// es index: `${SITE_URL}/servicios`   en index: `${SITE_URL}/en/services`
// es landing: `${SITE_URL}/servicios/${slug}`   en landing: `${SITE_URL}/en/services/${slug}`
```

From src/components/JsonLd.tsx (already used by authors/[slug] and case-studies/[slug] pages for their own BreadcrumbList — reuse verbatim):
```typescript
export function JsonLd({ data }: { data: Record<string, unknown> }): JSX.Element
```

From src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx (existing hand-written BreadcrumbList convention to match, lines 93-101):
```typescript
const breadcrumbData = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: t.home, item: SITE_URL },
    { '@type': 'ListItem', position: 2, name: t.caseStudies, item: `${SITE_URL}/case-studies` },
    { '@type': 'ListItem', position: 3, name: doc.title, item: `${SITE_URL}/case-studies/${doc.slug}` },
  ],
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/breadcrumbs.ts — buildTrail() + buildBreadcrumbJsonLd()</name>
  <files>src/lib/breadcrumbs.ts</files>
  <read_first>
    src/blocks/Hero/config.ts (breadcrumbs field shape, lines 34-62)
    src/lib/services-data.ts (SERVICE_SLUGS/SERVICES_INDEX_SLUG, full file already in context above)
    src/lib/sitemap-data.ts (SITE_URL export + ES/EN URL-segment convention, full file already in context above)
    src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx lines 49-101 (existing BreadcrumbList JSON-LD hand-written convention to match)
  </read_first>
  <action>
    Create a new pure module `src/lib/breadcrumbs.ts` with zero DB/Payload imports (import only `SITE_URL` from `@/lib/sitemap-data`). Export:

    `export type Locale = 'es' | 'en'`
    `export type BreadcrumbItem = { label: string; url: string }`

    A local, non-exported `LABELS` dictionary: `{ es: { home: 'Inicio', services: 'Servicios' }, en: { home: 'Home', services: 'Services' } }`.

    A non-exported `homeHref(locale)`: returns `'/'` for `'es'`, `'/en'` for `'en'`.
    A non-exported `servicesSegment(locale)`: returns `'servicios'` for `'es'`, `'services'` for `'en'` — matching the exact ES/EN segment split already used in `src/lib/sitemap-data.ts`.
    A non-exported `servicesIndexHref(locale)`: `` `${homeHref(locale) === '/' ? '' : homeHref(locale)}/${servicesSegment(locale)}` `` — produces `/servicios` for es and `/en/services` for en.

    `export function buildTrail(locale: Locale, current?: { slug: string; title: string }): BreadcrumbItem[]`:
    - Always starts with `{ label: LABELS[locale].home, url: homeHref(locale) }`, then `{ label: LABELS[locale].services, url: servicesIndexHref(locale) }`.
    - When `current` is provided, appends a 3rd entry `{ label: current.title, url: `${servicesIndexHref(locale)}/${current.slug}` }`. `current.title` must be the already-locale-fetched Payload `title` field the caller passes in — this function performs no Payload query of its own, per CONTEXT.md's explicit decision.
    - When `current` is omitted, returns the 2-level index trail only.

    `export function buildBreadcrumbJsonLd(trail: BreadcrumbItem[]): Record<string, unknown>`:
    - Returns `{ '@context': 'https://schema.org', '@type': 'BreadcrumbList', itemListElement: [...] }`.
    - Maps every `trail` entry (including the last) to `{ '@type': 'ListItem', position: i + 1, name: crumb.label, item: absoluteUrl }` — 1-indexed `position`, per CONTEXT.md's explicit decision to NOT omit `item` on the last entry.
    - `absoluteUrl` computation: if `crumb.url === '/'`, use `SITE_URL` bare (no trailing slash, matching the sitemap convention where the ES root has no trailing `/`); otherwise use `` `${SITE_URL}${crumb.url}` ``.

    Add a short top-of-file comment stating this module is the single source of truth for BREAD-01/02 — both the visual trail (fed into the Hero block's `breadcrumbs` prop) and the JSON-LD (fed into `<JsonLd>`) must always call through here, never re-derive labels/URLs inline in a page.

    Deviation note (per context_fidelity): `research/SUMMARY.md` (Phase 1 recommendation) suggested a standalone `src/components/Breadcrumbs.tsx` + a new `shadcn add breadcrumb` source file as the rendering layer. CONTEXT.md's locked "Implementación" decision instead reuses the Hero block's existing `breadcrumbs` prop/render path (built in Phase 10.8) fed via `RenderBlocks`' `blockProps` override — zero new component, zero new shadcn source file. Follow CONTEXT.md (the later, phase-specific locked decision) over RESEARCH.md's earlier suggestion. This does not strand the Hero `breadcrumbs` field for other listing-variant pages (blog listing, geo-pages) — those pages' own `page.tsx` files are untouched by this plan and keep using their existing editorial Payload `breadcrumbs` values unaffected, since the override in Task 2 is scoped only to the 4 Servicios route files via their own local `blockProps` prop, not global.
  </action>
  <verify>
    <automated>npx tsx -e "import('./src/lib/breadcrumbs.ts').then(m => { const idx = m.buildTrail('es'); const landing = m.buildTrail('en', { slug: 'seo-consulting', title: 'SEO Consulting' }); console.log(JSON.stringify(idx)); console.log(JSON.stringify(landing)); console.log(JSON.stringify(m.buildBreadcrumbJsonLd(landing))); })"</automated>
  </verify>
  <acceptance_criteria>
    <source>`grep -c "export function buildTrail" src/lib/breadcrumbs.ts` returns 1; `grep -c "export function buildBreadcrumbJsonLd" src/lib/breadcrumbs.ts` returns 1; `grep -c "getPayload\|@payload-config" src/lib/breadcrumbs.ts` returns 0 (confirms no DB access, pure module)</source>
    <automated>npx tsx -e "import('./src/lib/breadcrumbs.ts').then(m => { const idx = m.buildTrail('es'); const landing = m.buildTrail('en', { slug: 'seo-consulting', title: 'SEO Consulting' }); console.log(JSON.stringify(idx)); console.log(JSON.stringify(landing)); console.log(JSON.stringify(m.buildBreadcrumbJsonLd(landing))); })"</automated>
  </acceptance_criteria>
  <done>File exists, exports both functions with the exact signatures above, `buildTrail('es')` returns a 2-item array (Inicio, Servicios), `buildTrail('en', {slug,title})` returns a 3-item array ending in the landing title, and `buildBreadcrumbJsonLd()` output has `itemListElement.length === trail.length` with every entry carrying an absolute `item` URL starting with `https://`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire buildTrail() + JsonLd into all 4 Servicios page.tsx files</name>
  <files>
    src/app/(frontend)/[locale]/servicios/page.tsx
    src/app/(frontend)/[locale]/services/page.tsx
    src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
    src/app/(frontend)/[locale]/services/[slug]/page.tsx
  </files>
  <read_first>
    src/lib/breadcrumbs.ts (Task 1 output — exact export signatures)
    src/blocks/RenderBlocks.tsx (blockProps merge-order contract, already in context above)
    src/components/JsonLd.tsx (already in context above)
  </read_first>
  <action>
    All 4 files are near-identical (the `servicios`/`services` pair and the `[slug]`/`[slug]` pair are byte-for-byte duplicates of each other today — dual-route pattern from Phase 19, no `pathnames` config in next-intl). Apply the same edit shape to each pair.

    In `servicios/page.tsx` and `services/page.tsx` (index, both currently identical — see full content already in context above):
    - Add imports: `import { buildTrail, buildBreadcrumbJsonLd } from '@/lib/breadcrumbs'` and `import { JsonLd } from '@/components/JsonLd'`.
    - Inside `ServicesIndexPage`, after the `if (!doc) notFound()` guard, add `const trail = buildTrail(locale as 'es' | 'en')`.
    - Change the return to render `<JsonLd data={buildBreadcrumbJsonLd(trail)} />` as the first child inside `<main>` (or immediately before it), followed by `<RenderBlocks blocks={doc.content?.layout ?? []} blockProps={{ hero: { breadcrumbs: trail } }} />` — replacing the current bare `<RenderBlocks blocks={doc.content?.layout ?? []} />` call.

    In `servicios/[slug]/page.tsx` and `services/[slug]/page.tsx` (landing, both currently identical — see full content already in context above):
    - Same two imports.
    - Inside `ServicePage`, after the `if (!doc) notFound()` guard, add `const trail = buildTrail(locale as 'es' | 'en', { slug: doc.slug ?? slug, title: doc.title })` — reuses the `doc` already fetched by `getServicePage(locale, slug)` at the correct locale, per CONTEXT.md's explicit decision to not re-query Payload for the title.
    - Same `<JsonLd data={buildBreadcrumbJsonLd(trail)} />` + `<RenderBlocks blocks={doc.content?.layout ?? []} blockProps={{ hero: { breadcrumbs: trail } }} />` replacement.

    Do not touch `generateMetadata` in any of the 4 files (out of scope for this phase — canonical/hreflang is Phase 23). Do not touch `src/blocks/Hero/Component.tsx` or `src/blocks/Hero/config.ts` — the `breadcrumbs` prop/render path already exists from Phase 10.8 and needs no schema change.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    <source>`grep -c "buildTrail(" src/app/\(frontend\)/\[locale\]/servicios/page.tsx src/app/\(frontend\)/\[locale\]/services/page.tsx src/app/\(frontend\)/\[locale\]/servicios/\[slug\]/page.tsx src/app/\(frontend\)/\[locale\]/services/\[slug\]/page.tsx` returns 1 in each of the 4 files</source>
    <automated>npx tsc --noEmit</automated>
    <automated>curl -s http://localhost:3000/servicios | grep -o '<nav aria-label="Breadcrumb">.*</nav>' | grep -c 'Servicios' </automated>
    <automated>curl -s http://localhost:3000/servicios | grep -c 'application/ld+json'</automated>
  </acceptance_criteria>
  <done>All 4 files import and call `buildTrail`/`buildBreadcrumbJsonLd`, `tsc --noEmit` passes with zero new errors, and a live curl against the dev server shows both a visible `<nav aria-label="Breadcrumb">` and a `application/ld+json` script tag on `/servicios` (index) — confirming the wiring end-to-end before the broader sweep in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Full 10-URL sweep + seo-schema agent validation (BREAD-01/02/03 close-out)</name>
  <files>none (verification-only task, no source files modified)</files>
  <read_first>
    src/lib/breadcrumbs.ts (Task 1)
    All 4 page.tsx files as modified in Task 2
  </read_first>
  <action>
    Confirm the Next.js dev server is running (start it in the background with `npm run dev` if not already up, wait for it to bind before curling). Then, for all 5 Servicios pages in both locales (10 URLs total: `/servicios`, `/en/services`, and the 4 landings `/servicios/{slug}` + `/en/services/{slug}` for each of `seo-technical-audit`, `seo-consulting`, `fullstack-development`, `ai-seo-geo`), curl each URL and extract the `<nav aria-label="Breadcrumb">...</nav>` block and the `<script type="application/ld+json">...</script>` block containing `"@type":"BreadcrumbList"`.

    Confirm visually (via the extracted HTML) that: the index pages show a 2-level trail (Inicio/Home > Servicios/Services, last item not a link), and all 4 landing pages show a 3-level trail (Inicio/Home > Servicios/Services > landing title, last item not a link) — matching CONTEXT.md's locked trail structure decision.

    Then invoke the `seo-schema` agent (Task tool, `subagent_type: "seo-schema"`) with a representative sample covering both locales and both page types — the extracted JSON-LD from `/servicios` (ES index), `/en/services` (EN index), `/servicios/seo-consulting` (ES landing), and `/en/services/seo-consulting` (EN landing) — asking it to validate each `BreadcrumbList` block against Google's supported rich-result requirements (per `~/.claude/agents/seo-schema.md`'s validation checklist: `@context` is `https://schema.org`, `@type` valid, all required properties present — `position`/`name`/`item` on every `ListItem`, absolute URLs, no placeholder text). Record the agent's PASS/FAIL verdict per sample in the plan's SUMMARY.md.

    This closes BREAD-03 ("se valida sin errores en ambos locales usando el agente... antes de dar la fase por cerrada") with real evidence, not an assumption.
  </action>
  <verify>
    <automated>for u in /servicios /en/services /servicios/seo-technical-audit /en/services/seo-technical-audit /servicios/seo-consulting /en/services/seo-consulting /servicios/fullstack-development /en/services/fullstack-development /servicios/ai-seo-geo /en/services/ai-seo-geo; do curl -s "http://localhost:3000$u" | grep -c 'application/ld+json'; done</automated>
  </verify>
  <acceptance_criteria>
    <behavior>All 10 URLs return a `<nav aria-label="Breadcrumb">` with the correct 2-level (index) or 3-level (landing) trail, and a `BreadcrumbList` JSON-LD script whose `itemListElement.length` matches the visible trail length</behavior>
    <behavior>The seo-schema agent reports no missing required properties and no invalid `@type` for all 4 sampled URLs (2 index + 2 landing, covering both locales)</behavior>
  </acceptance_criteria>
  <done>10/10 URLs verified with matching visual trail + JSON-LD via curl; seo-schema agent PASS recorded for the 4-sample cross-locale/cross-page-type check; SUMMARY.md documents the full sweep result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload-editor content -> rendered breadcrumb label / JSON-LD `name` | The `current.title` fed into `buildTrail()` comes from an already-fetched `pages` document — editor-controlled (admin-only), not end-user input |
| `buildTrail()` output -> `<JsonLd>` script injection | JSON-LD is serialized via the existing `JsonLd` component, not raw string concatenation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-22-01 | Tampering | `src/components/JsonLd.tsx` (reused, unmodified) | accept | Already uses `JSON.stringify` for serialization (T-02-02 precedent) — a title containing `</script>` cannot break out of the script block; no new code path introduced by this phase |
| T-22-02 | Tampering | `src/lib/breadcrumbs.ts` URL construction | mitigate | `buildTrail()` only accepts a `locale: 'es'|'en'` union and a `slug`/`title` pair already resolved by `getServicePage()`, which allowlist-checks the slug via `isServiceSlug()` before ever querying Payload (T-19-01 precedent) — no raw request input reaches URL construction in this new module |
| T-22-03 | Information Disclosure | Absolute URLs in `BreadcrumbList` JSON-LD | accept | URLs are public page paths already indexed via `sitemap.xml` (Phase 15) — no new information exposed |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero new errors after all 4 page.tsx edits.
2. `grep -c "getPayload\|@payload-config" src/lib/breadcrumbs.ts` returns 0 — confirms the shared helper is a pure, DB-free module (no schema/migration risk, matching the phase's zero-migration premise).
3. Live curl sweep of all 10 Servicios URLs (5 pages x 2 locales) shows a visible breadcrumb `<nav>` and a matching `BreadcrumbList` JSON-LD script on every one.
4. `seo-schema` agent validation pass recorded for at least 4 representative URLs (2 index, 2 landing, both locales) with no missing required properties.
5. No new Payload migration file exists in `src/migrations/` as a result of this phase (`git status src/migrations/` clean) — hard requirement per CLAUDE.md's flag on this phase's zero-schema-change premise.
</verification>

<success_criteria>
- BREAD-01: Visible breadcrumb trail renders on `/servicios`, `/en/services`, and all 4 individual service landings, in both locales.
- BREAD-02: Every one of those pages emits `BreadcrumbList` JSON-LD derived from the same `buildTrail()` call that produced the visible trail — zero duplicated URL/locale logic.
- BREAD-03: `seo-schema` agent validation confirms no schema errors across both locales before the phase closes.
- Zero new Payload fields, zero new collections, zero migration files — 100% render-time computation, confirmed by `git status` on `src/migrations/`.
</success_criteria>

<output>
Create `.planning/phases/22-breadcrumbs-visual-schema/22-01-SUMMARY.md` when done
</output>
