---
phase: 25-service-page-visual-polish
plan: 04
type: execute
wave: 4
depends_on: ["25-01", "25-03"]
files_modified:
  - .planning/phases/25-service-page-visual-polish/25-post-content.json
  - .planning/phases/25-service-page-visual-polish/lh-phase25-post.json
  - .planning/phases/25-service-page-visual-polish/25-REGRESSION-DIFF.md
autonomous: true
requirements: [SVCPOL-07, SVCPOL-08, SVCPOL-09]

must_haves:
  truths:
    - "Every one of the 8 service URLs still has exactly 1 H1, with text identical to the 25-01 baseline"
    - "Every one of the 8 service URLs still emits a BreadcrumbList JSON-LD block, identical itemListElement to the 25-01 baseline"
    - "No route's Lighthouse mobile Performance score drops more than 5 points, and no CWV metric (LCP/CLS/TBT) crosses from a passing lab band into a poor one, relative to the 25-01 baseline"
    - "All new component strings (scope-card labels, related-case-study framing, pain-section heading) render with real, non-empty, locale-distinct text on all 8 URLs — zero empty labels, zero shared/untranslated strings between ES and EN"
  artifacts:
    - path: ".planning/phases/25-service-page-visual-polish/25-post-content.json"
      provides: "Post-change H1 + JSON-LD snapshot for all 8 URLs, same shape as 25-01's baseline, for direct diffing"
    - path: ".planning/phases/25-service-page-visual-polish/lh-phase25-post.json"
      provides: "Post-change Lighthouse mobile scores for all 8 URLs, production build"
    - path: ".planning/phases/25-service-page-visual-polish/25-REGRESSION-DIFF.md"
      provides: "Human-readable pass/fail record of the H1/JSON-LD/CWV/ES-EN-parity diff against the 25-01 baseline — the phase's final regression gate evidence"
  key_links:
    - from: "25-post-content.json"
      to: "25-baseline-content.json (25-01)"
      via: "programmatic per-URL diff (H1 text equality, BreadcrumbList itemListElement equality)"
      pattern: "25-baseline-content\\.json"
    - from: "lh-phase25-post.json"
      to: "lh-phase25-baseline.json (25-01)"
      via: "programmatic per-route score comparison"
      pattern: "lh-phase25-baseline\\.json"
---

<objective>
Prove zero regression on the phase's highest-risk axes — H1, JSON-LD, Core Web Vitals, and ES/EN parity — by re-running the exact same measurement tooling from Plan 25-01 against the now-restructured 8 service URLs and diffing programmatically against the 25-01 baseline. This is the phase's closing gate: SVCPOL-07, SVCPOL-08, and SVCPOL-09 are not satisfied by "it looks right," they are satisfied by this diff passing.

Purpose: Phase 25 is explicitly the highest-surface, highest-regression-risk phase of the v1.5 milestone. This plan is the evidence that the visual polish (25-03) did not cost the site its existing SEO/CWV foundation.
Output: Post-change snapshot + Lighthouse JSON, and a written diff record confirming pass/fail per must-have.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-service-page-visual-polish/25-CONTEXT.md
@.planning/phases/25-service-page-visual-polish/25-UI-SPEC.md
@.planning/phases/25-service-page-visual-polish/25-baseline-content.json
@.planning/phases/25-service-page-visual-polish/lh-phase25-baseline.json
@scripts/capture-service-page-snapshot.mjs
@scripts/lighthouse-mobile.mjs
</context>

<interfaces>
Both measurement scripts already exist (from Plan 25-01) and must be re-run **verbatim, unmodified** — this plan's entire premise is comparing like-for-like tooling output:

```
node scripts/capture-service-page-snapshot.mjs --base-url <url> --out <path>
node scripts/lighthouse-mobile.mjs --base-url <url> --routes-only <paths> --out <path>
```

Baseline files already on disk (Plan 25-01, do not regenerate, read-only reference):
- `.planning/phases/25-service-page-visual-polish/25-baseline-content.json` — shape `{ "<path>": { h1: { count, texts }, jsonLd: [{ type, raw }] } }`
- `.planning/phases/25-service-page-visual-polish/lh-phase25-baseline.json` — shape `{ "<path>": { performance, accessibility, bestPractices, seo, lcpMs, cls, tbtMs } }`

Same 8 target URLs as 25-01 (canonical per `buildServiceAlternates`):
```
/servicios/seo-technical-audit
/servicios/seo-consulting
/servicios/fullstack-development
/servicios/ai-seo-geo
/en/services/seo-technical-audit
/en/services/seo-consulting
/en/services/fullstack-development
/en/services/ai-seo-geo
```

Phase 17 production-build pattern (verbatim reuse, same as 25-01 Task 2): `PORT=<n> npm run build` -> `PORT=<n> npm run start &` (capture PID) -> run lighthouse script -> `kill <PID>`, confirm no orphan process.

Lighthouse regression threshold (Phase 17 precedent, `17-CONTEXT.md`): flag if Performance drops more than ~5 points, or any CWV metric (LCP/CLS/TBT) crosses from its current lab band (good/needs-improvement/poor) into a worse one.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-capture H1/JSON-LD snapshot, diff against baseline, and verify ES/EN parity on all 8 URLs</name>
  <read_first>scripts/capture-service-page-snapshot.mjs, .planning/phases/25-service-page-visual-polish/25-baseline-content.json</read_first>
  <files>.planning/phases/25-service-page-visual-polish/25-post-content.json</files>
  <action>
1. Start the dev server (or reuse a running one) and run `node scripts/capture-service-page-snapshot.mjs --base-url http://localhost:3000 --out .planning/phases/25-service-page-visual-polish/25-post-content.json` unmodified — same script Plan 25-01 wrote, no edits.
2. Write and run a one-off Node diff (inline `node -e`, no new script file needed) that loads both `25-baseline-content.json` and `25-post-content.json` and asserts, per URL: `h1.count === 1` (unchanged from baseline expectation), `h1.texts[0]` is byte-identical to the baseline's `h1.texts[0]` (Hero was never touched, so the H1 text itself must not have shifted at all, not just the count), and every `BreadcrumbList` entry's `itemListElement` array is deep-equal to the baseline's. Any mismatch is a hard failure — log the exact URL and field that diverged.
3. Separately verify ES/EN parity (SVCPOL-09) by fetching all 8 URLs live and confirming: the rendered HTML contains non-empty text for the `serviceScopeCard` labels (localized via next-intl — "Qué incluye"/"What's included" etc, confirm the ES page shows Spanish labels and the EN page shows English labels, not the same string on both), the `relatedCaseStudyBlock` framing text is present and non-empty on every URL, and the top/bottom CTA button text differs correctly by locale ("Agenda una llamada" on `/servicios/*`, "Book a call" on `/en/services/*`).
4. Write findings to `.planning/phases/25-service-page-visual-polish/25-REGRESSION-DIFF.md` (create in this task, append Lighthouse findings in Task 2) with one line per URL per check (H1 match, JSON-LD match, ES/EN parity), explicit PASS/FAIL.
  </action>
  <acceptance_criteria>
    - `25-post-content.json` contains all 8 URLs
    - The diff finds `h1.count===1` and `h1.texts[0]` byte-identical to baseline for all 8 URLs
    - The diff finds every baseline `BreadcrumbList` `itemListElement` array deep-equal in the post-change snapshot for all 8 URLs
    - `25-REGRESSION-DIFF.md` exists with an explicit PASS/FAIL line for H1, JSON-LD, and ES/EN parity, for each of the 8 URLs (24 lines minimum)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/25-service-page-visual-polish/25-baseline-content.json'); const p=require('./.planning/phases/25-service-page-visual-polish/25-post-content.json'); let fail=0; for(const k of Object.keys(b)){ if(!p[k]) { console.error(k+': missing from post-content'); fail++; continue; } if(p[k].h1.count!==1) { console.error(k+': h1.count='+p[k].h1.count); fail++; } if(p[k].h1.texts[0]!==b[k].h1.texts[0]) { console.error(k+': h1 text changed'); fail++; } const bBc=b[k].jsonLd.find(j=>j.type==='BreadcrumbList'); const pBc=p[k].jsonLd.find(j=>j.type==='BreadcrumbList'); if(JSON.stringify(bBc?.raw?.itemListElement)!==JSON.stringify(pBc?.raw?.itemListElement)) { console.error(k+': BreadcrumbList itemListElement changed'); fail++; } } if(fail>0) throw new Error(fail+' regressions found'); console.log('PASS: 8/8 URLs, H1 + BreadcrumbList unchanged from baseline')"</automated>
  </verify>
  <done>Post-change snapshot captured, diffed programmatically against baseline with zero H1/JSON-LD regressions, ES/EN parity confirmed live, findings recorded in 25-REGRESSION-DIFF.md.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run Lighthouse mobile baseline and diff Core Web Vitals against 25-01</name>
  <read_first>scripts/lighthouse-mobile.mjs, .planning/phases/25-service-page-visual-polish/lh-phase25-baseline.json</read_first>
  <files>.planning/phases/25-service-page-visual-polish/lh-phase25-post.json, .planning/phases/25-service-page-visual-polish/25-REGRESSION-DIFF.md</files>
  <action>
1. Run the same production-build pattern as 25-01 Task 2 against the same 8 routes: `PORT=3027 npm run build` -> `PORT=3027 npm run start &` (capture PID) -> `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3027 --routes-only /servicios/seo-technical-audit,/servicios/seo-consulting,/servicios/fullstack-development,/servicios/ai-seo-geo,/en/services/seo-technical-audit,/en/services/seo-consulting,/en/services/fullstack-development,/en/services/ai-seo-geo --out .planning/phases/25-service-page-visual-polish/lh-phase25-post.json` -> kill the PID, confirm no orphan process via `lsof`.
2. Diff each route's `lh-phase25-post.json` entry against `lh-phase25-baseline.json`: flag any route where `performance` drops more than 5 points from baseline, or where `lcpMs`/`cls`/`tbtMs` crosses out of its current Lighthouse lab band (good/needs-improvement/poor — use standard thresholds: LCP good <=2500ms, needs-improvement <=4000ms, poor above; CLS good <=0.1, needs-improvement <=0.25, poor above; TBT good <=200ms, needs-improvement <=600ms, poor above) into a worse band than the baseline was in.
3. Append the Lighthouse findings to `25-REGRESSION-DIFF.md` (one line per route: baseline vs post score/CWV, PASS/FAIL) below the Task 1 findings, and write an explicit final verdict line at the top of the file: `## Phase 25 Regression Gate: PASS` or `## Phase 25 Regression Gate: FAIL (see below)`.
4. If any route fails, do NOT silently mark the phase done — the FAIL verdict in `25-REGRESSION-DIFF.md` is the signal this phase's must-haves were not met; surface it plainly in the plan's own completion summary rather than glossing over it.
  </action>
  <acceptance_criteria>
    - `lh-phase25-post.json` contains all 8 routes, zero `error` entries
    - No route shows a Performance drop greater than 5 points versus baseline
    - No route's LCP/CLS/TBT crosses into a worse Lighthouse lab band than its baseline value
    - `25-REGRESSION-DIFF.md` has a top-line explicit PASS/FAIL verdict and background server confirmed killed (no orphan process on the build port)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/25-service-page-visual-polish/lh-phase25-baseline.json'); const p=require('./.planning/phases/25-service-page-visual-polish/lh-phase25-post.json'); let fail=0; for(const k of Object.keys(b)){ if(!p[k]||p[k].error){ console.error(k+': missing or errored'); fail++; continue; } const drop=b[k].performance-p[k].performance; if(drop>5){ console.error(k+': performance dropped '+drop+' points'); fail++; } } if(fail>0) throw new Error(fail+' Lighthouse regressions found'); console.log('PASS: 8/8 routes, no performance regression >5 points')"</automated>
  </verify>
  <done>Lighthouse mobile scores re-captured for all 8 routes, diffed against baseline with zero regressions beyond threshold, explicit PASS/FAIL verdict recorded in 25-REGRESSION-DIFF.md, background server confirmed stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local script -> local HTTP server | Same as 25-01 — measurement tooling fetches a locally-running dev/prod server, no untrusted network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-25-10 | Repudiation | Regression verdict could be silently glossed over if a check fails | mitigate | Task 2's action explicitly forbids marking the phase done on a FAIL verdict — `25-REGRESSION-DIFF.md`'s top-line verdict must be surfaced plainly in the plan's completion summary |
| T-25-11 | Denial of Service | Orphaned `next start` background process on the Lighthouse build port | mitigate | Task 2 explicitly requires capturing the PID and confirming it is killed post-run, per the Phase 17/25-01 precedent |
</threat_model>

<verification>
`25-post-content.json` and `lh-phase25-post.json` both exist, diffed programmatically against the 25-01 baseline with zero H1/JSON-LD/CWV regressions, ES/EN parity confirmed live on all 8 URLs, and `25-REGRESSION-DIFF.md` records an explicit top-line PASS/FAIL verdict.
</verification>

<success_criteria>
- SVCPOL-07 (H1/JSON-LD integrity), SVCPOL-08 (no CWV regression), and SVCPOL-09 (ES/EN parity) all backed by programmatic diff evidence, not visual impression
- `25-REGRESSION-DIFF.md` is the closing artifact for the entire phase — if it says PASS, Phase 25 is verifiably done; if FAIL, the specific failing URL/metric is named for follow-up
</success_criteria>

<output>
Create `.planning/phases/25-service-page-visual-polish/25-04-SUMMARY.md` when done
</output>
