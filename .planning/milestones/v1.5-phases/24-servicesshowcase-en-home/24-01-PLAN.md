---
phase: 24-servicesshowcase-en-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/ServicesShowcase/config.ts
  - src/blocks/ServicesShowcase/Component.tsx
  - src/collections/Pages/index.ts
  - src/blocks/RenderBlocks.tsx
  - messages/en.json
  - messages/es.json
  - scripts/seed-phase24-services-showcase.ts
  - src/migrations/*.ts (generated)
  - src/migrations/index.ts (generated, appended by payload migrate:create)
autonomous: true
requirements: [SVCHOME-01, SVCHOME-02, SVCHOME-03]

must_haves:
  truths:
    - "A visitor on Home (/, /en) sees a 'Services' section with exactly 4 cards, one per SERVICE_SLUGS entry, in both locales"
    - "Each card links to the correct locale-appropriate service landing URL (/servicios/{slug} on es, /en/services/{slug} on en) and that URL returns 200"
    - "The card grid is driven by SERVICE_SLUGS + getServicePage — no card title/copy is hardcoded per-instance in the block component"
    - "The new block is registered purely additively: new config.ts/Component.tsx, appended to Pages' existing blocks array, appended to RenderBlocks' blockComponents map — zero existing fields/columns modified"
  artifacts:
    - path: "src/blocks/ServicesShowcase/config.ts"
      provides: "Block slug 'servicesShowcase', interfaceName ServicesShowcaseBlock, single localized 'title' text field"
      exports: ["ServicesShowcase"]
    - path: "src/blocks/ServicesShowcase/Component.tsx"
      provides: "Server component fetching the 4 SERVICE_SLUGS pages via services-data.ts helpers, rendering 2x2 card grid"
      exports: ["ServicesShowcaseComponent"]
    - path: "scripts/seed-phase24-services-showcase.ts"
      provides: "Idempotent seed appending one servicesShowcase block instance to Home's content.layout in both locales"
  key_links:
    - from: "src/collections/Pages/index.ts"
      to: "src/blocks/ServicesShowcase/config.ts"
      via: "import + blocks array entry"
      pattern: "ServicesShowcase"
    - from: "src/blocks/RenderBlocks.tsx"
      to: "src/blocks/ServicesShowcase/Component.tsx"
      via: "blockComponents map entry keyed 'servicesShowcase'"
      pattern: "servicesShowcase: ServicesShowcaseComponent"
    - from: "src/blocks/ServicesShowcase/Component.tsx"
      to: "src/lib/services-data.ts"
      via: "SERVICE_SLUGS + getServicePage(locale, slug) calls"
      pattern: "SERVICE_SLUGS|getServicePage"
---

<objective>
Add a new, purely-additive `ServicesShowcase` Payload block that renders the 4 fixed service pages (read live from `SERVICE_SLUGS`) as a 2x2 card grid on Home, in both locales, per `24-UI-SPEC.md`.

Purpose: closes SVCHOME-01/02/03 — Home currently has no on-page showcase linking to the 4 service landings built in Phase 19; visitors must already know the URLs or find them via nav.
Output: new block (config + component), additive registration in `Pages` collection + `RenderBlocks`, a committed additive migration, regenerated `payload-types.ts`, and Home's layout seeded with one populated instance in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-servicesshowcase-en-home/24-CONTEXT.md
@.planning/phases/24-servicesshowcase-en-home/24-UI-SPEC.md
@src/lib/services-data.ts
@src/blocks/FeaturedCaseStudiesBlock/config.ts
@src/blocks/FeaturedCaseStudiesBlock/Component.tsx
@src/blocks/AboutSection/Component.tsx
@src/collections/Pages/index.ts
@src/blocks/RenderBlocks.tsx
@scripts/seed-phase13-home-content.ts

**Hard project rule (CLAUDE.md — Database Safety):** This phase is additive-only (new block registration + new column addition for the new block's own field, new seed writes). Additive migrations and normal `update()` writes do NOT need Juan's confirmation — generate the migration, read the generated SQL yourself, and if it is purely additive (`CREATE TABLE`/`ADD COLUMN`, no `DROP`/no touching an existing column's data), just run it and report. If at any point the migration touches an EXISTING column (not just adding new ones for this new block), STOP and ask Juan by name before applying — do not proceed on your own judgment.
</context>

<interfaces>
<!-- Precedent block Claude should copy the shape of, not the content -->

From src/blocks/FeaturedCaseStudiesBlock/config.ts (pattern to mirror for ServicesShowcase's config — single localized title field, `interfaceName`, `labels`):
```typescript
export const FeaturedCaseStudiesBlock: Block = {
  slug: 'featuredCaseStudiesBlock',
  interfaceName: 'FeaturedCaseStudiesBlock',
  labels: { singular: 'Featured Case Studies', plural: 'Featured Case Studies Blocks' },
  fields: [
    { name: 'title', type: 'text', localized: true, admin: { description: '...' } },
  ],
}
```

From src/lib/services-data.ts (exact helpers the new Component must call — do not re-derive service metadata):
```typescript
export const SERVICE_SLUGS = ['seo-technical-audit', 'seo-consulting', 'fullstack-development', 'ai-seo-geo'] as const
export type ServiceSlug = (typeof SERVICE_SLUGS)[number]
export async function getServicePage(locale: 'es' | 'en', slug: string) // returns docs[0] | undefined, allowlist-checked
```

From src/blocks/RenderBlocks.tsx (exact map shape to append to — do not restructure):
```typescript
const blockComponents: Record<string, (props: any) => ReactNode> = {
  // ...existing entries...
  aboutSection: AboutSectionComponent,
  testimonialSection: TestimonialSectionComponent,
}
```

From src/collections/Pages/index.ts (exact blocks array to append to, line 66-84):
```typescript
blocks: [
  Hero, Content, ArchiveBlock, CallToAction, FAQ, MediaBlock, TestimonialsCarousel,
  ContactFormBlock, Code, RelatedPosts, TableOfContentsBlock, ResultsSection, Section,
  FeaturedPostsBlock, FeaturedCaseStudiesBlock, ClientLogosBlock, AboutSection,
],
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ServicesShowcase block (config + Component) + additive Payload registration</name>
  <files>
    src/blocks/ServicesShowcase/config.ts
    src/blocks/ServicesShowcase/Component.tsx
    src/collections/Pages/index.ts
    src/blocks/RenderBlocks.tsx
  </files>
  <read_first>
    @src/blocks/FeaturedCaseStudiesBlock/config.ts
    @src/blocks/FeaturedCaseStudiesBlock/Component.tsx
    @src/blocks/AboutSection/Component.tsx (icon badge markup, ICON_OPTIONS usage)
    @src/lib/services-data.ts
    @src/collections/Pages/index.ts
    @src/blocks/RenderBlocks.tsx
    @.planning/phases/24-servicesshowcase-en-home/24-UI-SPEC.md
  </read_first>
  <action>
Create `src/blocks/ServicesShowcase/config.ts` exporting `ServicesShowcase: Block` — `slug: 'servicesShowcase'`, `interfaceName: 'ServicesShowcaseBlock'`, `labels: { singular: 'Services Showcase', plural: 'Services Showcase Blocks' }`, single field `{ name: 'title', type: 'text', localized: true, admin: { description: 'Section heading — editable per page instance.' } }` (no default, no other fields — per CONTEXT, section title is the only editorial field; card content is entirely derived from the 4 service pages, not editable per-block).

Create `src/blocks/ServicesShowcase/Component.tsx` exporting `ServicesShowcaseComponent(props: ServicesShowcaseBlockProps)`:
- Import `type { ServicesShowcaseBlock as ServicesShowcaseBlockProps } from '@/payload-types'` (will exist after Task 1's `generate:types` run at the end of this task).
- Import `getLocale` from `next-intl/server` (same pattern as `FeaturedCaseStudiesBlockComponent`), `SERVICE_SLUGS`, `getServicePage` from `@/lib/services-data`, `Container` from `@/components/Container`, `Card`/`CardContent` from `@/components/ui/card`, `ArrowRight` plus `Search`, `TrendingUp`, `Code`, `Sparkles` from `lucide-react`, `Link` from `next/link`, `getTranslations` from `next-intl/server` (for the "Explorar servicio"/"Explore service" CTA label — add message keys, see below).
- Build a local `const iconBySlug: Record<string, LucideIcon> = { 'seo-technical-audit': Search, 'seo-consulting': TrendingUp, 'fullstack-development': Code, 'ai-seo-geo': Sparkles }` colocated in this file (not a new Payload field, per UI-SPEC's icon decision) with `Code` as the fallback for any unmapped slug.
- Resolve `locale` via `getLocale()`, cast to `'es' | 'en'`.
- Fetch all 4 pages in parallel: `const pages = await Promise.all(SERVICE_SLUGS.map((slug) => getServicePage(locale, slug)))`, then `.filter((p): p is NonNullable<typeof p> => Boolean(p))` (silent per-card omission on resolution failure, per UI-SPEC Empty state contract).
- If the resulting array has length 0, `return null` (whole-block guard, matching `FeaturedCaseStudiesBlockComponent`'s `if (caseStudies.length === 0) return null`).
- Fetch the CTA label via `const t = await getTranslations('servicesShowcase')` then `t('cta')`.
- Render exactly the shell + card markup specified in `24-UI-SPEC.md`'s "Component Inventory" section verbatim: `<Container className="py-12">`, conditional `{title && <h2 className="font-heading text-heading mb-6">{title}</h2>}`, grid `<div className="grid grid-cols-1 sm:grid-cols-2 gap-6">` — 2x2, no `lg:grid-cols-4` override (locked UI-SPEC decision, do not deviate). Each card: full-bleed `<Link href={buildServiceHref(locale, page.slug)} className="group block">` wrapping `<Card><CardContent className="p-6">` containing the icon badge (`flex size-10 shrink-0 items-center justify-center rounded-md bg-primary/10` > `Icon className="size-5 text-primary"`), `<h3 className="font-heading text-heading mt-4">{page.title}</h3>`, conditional excerpt `{page.meta?.description && <p className="mt-2 text-body text-muted-foreground line-clamp-2">{page.meta.description}</p>}`, and the CTA row `<div className="mt-4 flex items-center gap-1 text-label text-primary"><span>{t('cta')}</span><ArrowRight className="size-4 transition-transform duration-fast ease-standard group-hover:translate-x-1" /></div>`.
- Write a small local helper `buildServiceHref(locale: 'es' | 'en', slug: string)` returning `` `/servicios/${slug}` `` for `es` and `` `/en/services/${slug}` `` for `en` — same dual-segment convention `src/lib/canonical.ts`/`src/lib/breadcrumbs.ts` already use (do not import from those modules; they pull in DB-touching dependencies not needed here — a 2-line local helper is correct, matching CONTEXT's "reuse the pattern" not "reuse the module" framing).

Add the CTA message keys to both `messages/en.json` and `messages/es.json` under a new top-level `"servicesShowcase"` key: `{ "cta": "Explore service" }` (en) / `{ "cta": "Explorar servicio" }` (es) — insert alongside the existing `"home"`/`"common"` top-level keys, do not nest inside `"home"`.

Register the block additively:
- In `src/collections/Pages/index.ts`: add `import { ServicesShowcase } from '@/blocks/ServicesShowcase/config'` alongside the other block imports, and append `ServicesShowcase` as the last entry in the `blocks: [...]` array (after `AboutSection`). Do not reorder or touch any existing array entry.
- In `src/blocks/RenderBlocks.tsx`: add `import { ServicesShowcaseComponent } from '@/blocks/ServicesShowcase/Component'` and append `servicesShowcase: ServicesShowcaseComponent,` as the last entry in the `blockComponents` map. Do not touch any existing map entry.

After all files are written, run `npx payload generate:importmap` then `npx payload generate:types` (Local API / CLI, no live DB write) so `ServicesShowcaseBlock` exists in `payload-types.ts` and the Component's import resolves. Then run `npx tsc --noEmit` and fix any type errors before proceeding.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c "ServicesShowcase" src/collections/Pages/index.ts` returns 2 (import + array entry)
    - `grep -c "servicesShowcase: ServicesShowcaseComponent" src/blocks/RenderBlocks.tsx` returns 1
    - `grep -c "ServicesShowcaseBlock" src/payload-types.ts` is greater than 0 (type generated)
    - `git diff src/collections/Pages/index.ts` shows only additive lines (no existing line removed or reordered)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ServicesShowcase block exists, is registered in Pages' blocks array and RenderBlocks' component map, types are generated, and the project compiles clean with zero changes to any pre-existing block/collection field.</done>
</task>

<task type="auto">
  <name>Task 2: Schema migration for the new block's own field (additive only)</name>
  <files>
    src/migrations/*.ts (generated)
    src/migrations/index.ts (generated, auto-appended)
  </files>
  <read_first>
    @CLAUDE.md (Database Safety section — read again before running anything against the real DB)
  </read_first>
  <action>
Run `npx payload migrate:create` to generate the migration for the new `servicesShowcase` block's table/column (Payload's `blocks` field type creates one new table per block slug — this is a brand new table for a brand new block, not a change to any existing table). Read the generated migration file yourself before applying: confirm it contains only `CREATE TABLE`/`ADD COLUMN`/index-creation statements referencing `pages_blocks_services_showcase` (or equivalent generated table name) and nothing that touches an existing table's existing columns (e.g. no `ALTER TABLE ... DROP`, no `ALTER TABLE ... ALTER COLUMN` on any pre-existing table).

If the migration is confirmed purely additive (expected outcome, per CONTEXT's "100% additive" scope), run `npx payload migrate` directly against the real Neon Postgres — this does NOT require Juan's named approval per the relaxed Database Safety rule (additive writes proceed automatically). Report what ran.

If the generated migration touches ANY existing column (unexpected — would indicate a modeling mistake in Task 1), STOP immediately, do not run `payload migrate`, and flag this to Juan by name for explicit approval before proceeding — this is the one condition in this plan that requires a pause.
  </action>
  <acceptance_criteria>
    - Migration file exists under `src/migrations/` referencing only the new block's table
    - `npx payload migrate` completed with no errors (or execution stopped with an explicit flag to Juan if the migration was non-additive)
    - `git diff src/migrations/` shows only new files added, no existing migration file modified
  </acceptance_criteria>
  <verify>
    <automated>ls src/migrations/*.ts | tail -1</automated>
  </verify>
  <done>A new, additive-only migration is committed and has been applied to the real production Neon Postgres database; Payload's internal migration ledger reflects it as run.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Home's layout with one populated ServicesShowcase instance (both locales) + live verification</name>
  <files>scripts/seed-phase24-services-showcase.ts</files>
  <read_first>
    @scripts/seed-phase13-home-content.ts (id-reuse-across-locale-writes pattern — content.layout is full-replaced by Payload on update)
    @.planning/phases/24-servicesshowcase-en-home/24-UI-SPEC.md (Copywriting Contract — section title copy requirement)
  </read_first>
  <action>
Create `scripts/seed-phase24-services-showcase.ts`, idempotent, following the exact `content.layout` id-reuse-across-locale-writes discipline established in `seed-phase13-home-content.ts` (fetch each locale's full layout fresh via `findByID({ collection: 'pages', id: homeDoc.id, locale, depth: 0 })`, splice/replace by `blockType === 'servicesShowcase'` lookup so a re-run updates in place instead of duplicating, preserve existing `id` on re-run).

Locate the real Home page doc (`payload.find({ collection: 'pages', where: { slug: { equals: 'home' } } })` — confirm the actual Home slug by checking `scripts/seed-home-page.ts` if uncertain, do not assume). For each locale (`es`, `en`):
- Build a `servicesShowcase` block object: `{ blockType: 'servicesShowcase', title: <real bilingual copy> }`.
- Section title copy (grounded, non-generic, per UI-SPEC's Copywriting Contract — write real sentences, not placeholders): es `"Cómo puedo ayudarte"`, en `"How I can help"` (or an equally concrete alternative in the same voice as the existing `AboutSection`/`Hero` copy already in Home — do not use a bare "Servicios"/"Services" label, UI-SPEC requires a real sentence).
- Insert the block at the end of the existing layout array (`layout.push(...)`) if not already present; if a `servicesShowcase` block already exists (re-run), update its `title` in place reusing its existing `id`.
- Write via `payload.update({ collection: 'pages', id: homeDoc.id, locale, data: { content: { layout } } })`.

This is a normal, additive `update()` write against the real production Neon Postgres — proceed without pausing for confirmation, per the relaxed Database Safety rule in CLAUDE.md.

After seeding both locales, verify live: start (or reuse) the dev server, then `curl` `/` (es) and `/en` (en). Confirm both responses contain exactly 4 `<a` elements whose `href` matches `/servicios/{slug}` (es) or `/en/services/{slug}` (en) for each of the 4 real `SERVICE_SLUGS`, and that each of those 4 target URLs independently returns `200` (curl -o /dev/null -w '%{http_code}'). Also confirm the section title text renders (grep the rendered HTML for the exact seeded title string on both locale routes).
  </action>
  <acceptance_criteria>
    - Re-running `scripts/seed-phase24-services-showcase.ts` a second time produces no duplicate `servicesShowcase` block in Home's layout (idempotency confirmed by comparing layout array length before/after second run)
    - `curl -s http://localhost:3000/ | grep -oc 'href="/servicios/[a-z-]*"'` returns `4`
    - `curl -s http://localhost:3000/en | grep -oc 'href="/en/services/[a-z-]*"'` returns `4`
    - All 4 `/servicios/{slug}` URLs and all 4 `/en/services/{slug}` URLs return HTTP 200
  </acceptance_criteria>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/seed-phase24-services-showcase.ts</automated>
  </verify>
  <done>Home's real production layout (both locales) includes one populated `servicesShowcase` block instance with 4 correctly-linked, correctly-localized cards, verified live against the running dev server with real HTTP responses — SVCHOME-01/02/03 fully closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Editor (Payload admin) → `title` field | Editor-controlled localized text field, rendered directly as heading text |
| Public visitor → card links | Visitor clicks a card; href is server-derived from an allowlisted, fixed `SERVICE_SLUGS` set, never user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `Component.tsx` slug→icon map | accept | `SERVICE_SLUGS` is a fixed, compile-time-known allowlist (`src/lib/services-data.ts`), not user input — no injection surface |
| T-24-02 | Information Disclosure | `getServicePage` resolution failure | mitigate | Failed/undefined resolution silently omits that card (`.filter(Boolean)`), never surfaces a raw error or stack trace to the client, matching `FeaturedCaseStudiesBlockComponent` precedent |
| T-24-03 | Tampering | npm/pip/cargo installs | accept | No new package installed this phase — reuses `lucide-react` (already a direct dependency, used in 3+ files) and existing shadcn `Card`/`CardContent` |
</threat_model>

<verification>
1. `npx tsc --noEmit` clean after Task 1.
2. Migration applied to real Neon Postgres, confirmed additive-only by reading the generated SQL before running (Task 2).
3. Live curl verification of both locale Home routes confirms exactly 4 correctly-linked cards each, all 8 target service URLs (4 slugs x 2 locales) return 200 (Task 3).
4. `grep -rn "Servicios\|Services" src/blocks/ServicesShowcase/Component.tsx` shows no hardcoded per-card title/copy — only the icon-map keys (slugs) and the CTA label pulled from `next-intl` messages.
</verification>

<success_criteria>
- SVCHOME-01: Home shows a `ServicesShowcase` block with 4 cards in both `/` (es) and `/en`.
- SVCHOME-02: Cards are driven by `SERVICE_SLUGS`/`getServicePage`, not hardcoded; each links to the correct locale-appropriate landing.
- SVCHOME-03: Registration is 100% additive (new block, new table) — confirmed via `git diff` showing zero removed/modified lines in any pre-existing collection field, plus the migration's SQL confirmed additive-only.
</success_criteria>

<output>
Create `.planning/phases/24-servicesshowcase-en-home/24-01-SUMMARY.md` when done
</output>
