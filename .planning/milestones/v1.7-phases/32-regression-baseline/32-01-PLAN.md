# Phase 32 Plan 01: Capture Regression Baseline

**Requirements:** REG-01
**Type:** Measurement only — no code/content changes

## Task 1: Lighthouse mobile baseline (production build)

1. `npx next build`
2. `PORT=3040 npx next start &` (background), wait for ready, `caffeinate -u &` to prevent display sleep during capture
3. `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3040 --out .planning/phases/32-regression-baseline/lh-phase32-baseline.json --routes-only /,/en,/seo-tecnico-madrid,/en/seo-tecnico-madrid,/seo-tecnico-lima,/en/seo-tecnico-lima`
4. Kill the `next start` PID and `caffeinate`, verify via `lsof -i :3040` and `ps aux | grep next` clear.

**Verify:** `lh-phase32-baseline.json` exists with 6 route entries, no `error` keys.

## Task 2: H1 + JSON-LD snapshot

Against the same production server (or a fresh `next start` on the same port), for each of the 6 routes: fetch HTML, extract `<h1>` text and any `<script type="application/ld+json">` blocks. Write to `.planning/phases/32-regression-baseline/32-baseline-content.json`.

**Verify:** JSON file has one entry per route with `h1` and `jsonLd` fields populated (or explicitly `null` where a route has no JSON-LD).

## Task 3: Write baseline doc

Write `.planning/phases/32-regression-baseline/32-REGRESSION-BASELINE.md` summarizing both captures in tables (route / performance / LCP / CLS / TBT / H1 / JSON-LD present), same shape as `28-REGRESSION-DIFF.md`'s baseline section. Commit everything.

**Verify:** File committed, referenced from STATE.md/ROADMAP.md progress update.
