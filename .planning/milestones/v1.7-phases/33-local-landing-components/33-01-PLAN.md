# Phase 33 Plan 01: Build Local Landing Components

**Status:** Complete

## Goal

Ship the 2 new reusable components the .pen introduces — Hero `local-landing` variant and `LocalProofSection` block — registered in Payload, type-safe, and verified against real test content on a running dev server. Do not touch `/seo-tecnico-madrid`/`/seo-tecnico-lima` content (Phase 34 scope).

## Tasks

1. **Extend `src/blocks/Hero/config.ts`**: add `local-landing` to the `variant` select; add 5 new conditional fields (`cityName`, `inlineStat`, `ringSide`, `ringOpacity`, `ringFlipX`) gated on `variant === 'local-landing'` via `admin.condition`, following the existing `breadcrumbs` field's condition pattern exactly.
2. **Extend `src/blocks/Hero/Component.tsx`**: add a `local-landing` entry to `variantStyles`; render city badge (MapPin icon + cityName) above the title, decorative ring SVG (ellipse, stroke only, `fill="none"`, positioned left/right via `ringSide`, opacity via `ringOpacity`, `scaleX(-1)` transform when `ringFlipX`), inline stat (CheckCircle2 icon + text) below the subtitle. Reuse the existing generic `links` CTA-row rendering as-is (already a flex-wrap row, no variant-specific duplication needed).
3. **Create `src/blocks/LocalProofSection/config.ts` + `Component.tsx`**: new block, `stats` array (exactly 3 rows: localized `value` + localized `label`, modeled on `ResultsSection`) + `testimonial` group (localized `quote` textarea, `authorName` text, localized `authorBusiness` text, modeled on `TestimonialSection` extended with the business field).
4. **Register the new block**: import + add to `blockComponents` in `src/blocks/blockRegistry.tsx`, and import + add to the `layout` blocks array in `src/collections/Pages/index.ts`.
5. **Schema migration**: run `payload generate:types`, then `payload migrate:create`, read the generated SQL to confirm the UP path is purely additive (new ENUM values, new tables, new nullable/defaulted columns — no DROP/TRUNCATE), then `payload migrate` against the real Neon DB.
6. **TypeScript check**: `npx tsc --noEmit` clean.
7. **Functional verification**: create a throwaway Payload page (`phase33-local-landing-test`) via a guarded Local API script with 2 Hero blocks (Madrid-style: ring right/opacity 0.25/no flip; Lima-style: ring left/opacity 0.35/flipX) + 1 LocalProofSection block, plus a throwaway Next.js route to render it. Start `next dev`, curl both locale variants, confirm 200 + expected DOM (city badges, 2 ellipses with correct opacity/transform, CTA hrefs, 3 stats, testimonial), confirm zero server errors in the dev log. Kill the dev server, delete the throwaway route file, delete the throwaway page via a guarded cleanup script (exact slug match).

## Interfaces

- `HeroBlock` (generated type): `variant: 'home' | 'listing' | 'post-header' | 'case-study-header' | 'local-landing'`, plus optional `cityName`, `inlineStat`, `ringSide`, `ringOpacity`, `ringFlipX`.
- `LocalProofSectionBlock` (generated type): `stats: { value, label }[]` (3 items), `testimonial: { quote, authorName, authorBusiness }`.
- `blockComponents.localProofSection` → `LocalProofSectionComponent`.

## Verification

- `npx tsc --noEmit` — clean.
- Dev server functional test — 200 on both locale routes, expected DOM confirmed via curl+grep, zero errors in dev log.
- Test page + test route deleted, confirmed via re-query (0 docs found).
