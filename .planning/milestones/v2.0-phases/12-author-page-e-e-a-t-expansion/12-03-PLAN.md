---
phase: 12-author-page-e-e-a-t-expansion
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - scripts/seed-author-eeat.ts
autonomous: true
requirements: [AUTHOR-06]

must_haves:
  truths:
    - "Las 3 secciones nuevas están pobladas con contenido real de Juan (4 expertise, 2 education, 2 experience, en ES y EN)"
    - "El avatar existente (portfolio/juan-angulo-portrait, migrado en Phase 4) no se re-sube ni se sobrescribe — se verifica que ya existe antes de tocar el Author"
  artifacts:
    - path: "scripts/seed-author-eeat.ts"
      provides: "Script idempotente (Payload Local API, upsert por slug juan-carlos-angulo) que puebla expertise/education/experience/socialLinks en ES y EN con las traducciones profesionales del content real"
      contains: "juan-carlos-angulo"
  key_links:
    - from: "scripts/seed-author-eeat.ts"
      to: "authors collection (slug juan-carlos-angulo)"
      via: "payload.find + payload.update por locale, mismo patrón que scripts/seed-phase10-7-gap-fill.ts"
      pattern: "juan-carlos-angulo"
---

<objective>
Crear `scripts/seed-author-eeat.ts`, un script idempotente que puebla `expertise[]`, `education[]`, `experience[]` y `socialLinks[]` del Author real (`slug: juan-carlos-angulo`) con el contenido de `12-CONTEXT.md` `<specifics>`, en ES (fuente literal) y EN (traducción profesional escrita en este plan). Verifica — sin re-subir — que el avatar Cloudinary ya migrado en Phase 4 sigue asignado.

Purpose: AUTHOR-06 exige que las 3 secciones nuevas tengan contenido real, no placeholder. Este script es la única vía de escritura (CONTEXT.md: "Traducciones EN se escriben directamente en el seed script, no vía admin").
Output: `scripts/seed-author-eeat.ts`, ejecutado contra la base de datos de desarrollo, con el Author real actualizado en ambos locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-author-page-e-e-a-t-expansion/12-CONTEXT.md
@scripts/seed-phase10-7-gap-fill.ts
</context>

<interfaces>
Patrón de upsert-por-locale ya usado en este proyecto (de `scripts/seed-phase10-7-gap-fill.ts`, función `seedAboutSection`): buscar el doc por `slug`, luego un `payload.update` por cada locale de `LOCALES = ['es', 'en'] as const`, reutilizando el `id` de sub-array generado en la primera pasada (`es`) para las siguientes pasadas (`en`) — evita duplicar filas del array en Postgres cuando se actualiza un array field localizado por múltiples locales.

Contenido real fuente (ES, de `12-CONTEXT.md` `<specifics>`, slug `juan-carlos-angulo`) y sus traducciones EN (a escribir literalmente en el script, tono coherente con `jobTitle`/`bio` EN ya existentes):

expertise (4 items, topic):
- ES: "SEO Técnico Avanzado (Rastreo e indexación)" / EN: "Advanced Technical SEO (Crawling & Indexing)"
- ES: "Rendimiento Web (WPO & Core Web Vitals)" / EN: "Web Performance (WPO & Core Web Vitals)"
- ES: "Algoritmia y Estructuras de Datos" / EN: "Algorithms & Data Structures"
- ES: "Optimización de Tasa de Conversión (CRO)" / EN: "Conversion Rate Optimization (CRO)"

education (2 items, degree/institution/startDate/endDate — institution es nombre propio, mismo valor en ambos locales):
- ES degree: "Ingeniero de software" / EN degree: "Software Engineering" — institution: "Universidad Peruana de Ciencias Aplicadas (UPC)" — startDate: 2022-05-01 — endDate: 2028-08-01
- ES degree: "Técnico en informática" / EN degree: "IT Technician" — institution: "Universidad Politécnica Territorial del Estado Bolívar" — startDate: 2018-02-01 — endDate: 2020-08-01

experience (2 items, role/company/startDate/endDate — company es nombre propio, mismo valor en ambos locales):
- ES role: "Especialista en Tech SEO" / EN role: "Technical SEO Specialist" — company: "AprendoSEO" — startDate: 2022-11-01 — endDate: 2026-02-01
- ES role: "Desarrollador Web" / EN role: "Web Developer" — company: "Cripto Avances & Nakama Digital" — startDate: 2022-01-01 — endDate: 2022-09-01

No hay descripción real disponible para ningún item de education/experience en la fuente — deja `description` sin asignar (undefined) en los 6 items; el campo queda disponible para edición futura desde /admin, per AUTHOR-06 ("o quedan como placeholder claramente editable desde /admin si el dato real no está disponible" — aplica aquí solo al sub-campo description, no a la sección completa, que sí tiene datos reales en sus campos principales).

socialLinks (3 items, platform/url — campo NO localizado, escribir una sola vez sin loop de locale):
- platform: "linkedin", url: "https://linkedin.com/in/juancangulo"
- platform: "github", url: "https://github.com/sve-nnn"
- platform: "website", url: "https://juan-tech.com"
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Escribir scripts/seed-author-eeat.ts</name>
  <files>scripts/seed-author-eeat.ts</files>
  <action>
    Crea `scripts/seed-author-eeat.ts` siguiendo el patrón de `scripts/seed-phase10-7-gap-fill.ts` (import `getPayload` de `payload`, import `config` de `../src/payload.config`, `LOCALES = ['es', 'en'] as const`).

    Estructura:

    1. `async function verifyAvatar(payload)`: busca el Author por `slug: 'juan-carlos-angulo'` (`payload.find`, `collection: 'authors'`, `where: { slug: { equals: 'juan-carlos-angulo' } }`, `limit: 1`, `depth: 1` para poblar `avatar`). Si no existe el doc, lanza error (el script asume que el Author real de Phase 4 ya existe — no lo crea desde cero). Si existe pero `avatar` es null/vacío, loguea una advertencia clara ("Avatar no encontrado — el Author existe pero sin avatar asignado; no se re-sube, per CONTEXT.md, revisar manualmente") pero continúa (no aborta el resto del seed). Si `avatar` existe, loguea confirmación con la URL de Cloudinary encontrada. Retorna el doc.

    2. `async function seedExpertise(payload, authorId)`: para cada locale en `LOCALES`, arma el array `expertise` (4 items, `{ topic }`) con las traducciones de `<interfaces>`, reutilizando los `id` generados en la pasada `es` para la pasada `en` (mismo patrón de reutilización de sub-array id que `seedAboutSection`). `payload.update({ collection: 'authors', id: authorId, locale, data: { expertise } })`.

    3. `async function seedEducation(payload, authorId)`: mismo patrón, array `education` (2 items, `{ degree, institution, startDate, endDate }`, sin `logo` ni `description`) con los valores exactos de `<interfaces>` (fechas como ISO string `'2022-05-01'` etc.).

    4. `async function seedExperience(payload, authorId)`: mismo patrón, array `experience` (2 items, `{ role, company, startDate, endDate }`, sin `description`).

    5. `async function seedSocialLinks(payload, authorId)`: `socialLinks` NO es localizado — un solo `payload.update` sin loop de locale, con los 3 items de `<interfaces>`. Solo escribe si `doc.socialLinks` está vacío o ausente (idempotencia: no sobrescribas si ya hay datos reales distintos, per "upsert" — si ya existen exactamente estos 3 valores, el update es un no-op seguro; si existen valores DIFERENTES, loguea una advertencia y no los sobrescribas, para no perder datos reales ya cargados por otra vía).

    6. `async function main()`: `getPayload({ config })`, llama `verifyAvatar`, luego `seedExpertise`/`seedEducation`/`seedExperience`/`seedSocialLinks` en secuencia (reutilizando el mismo `authorId`), loguea un resumen final, `process.exit(0)`. Envuelto en `.catch` que loguea el error y `process.exit(1)` (mismo patrón que `seed-phase10-7-gap-fill.ts`).

    Agrega el mismo comment de cabecera de uso que los scripts existentes: `Run with: npx tsx scripts/seed-author-eeat.ts` (o `node --env-file=.env node_modules/.bin/tsx scripts/seed-author-eeat.ts` si el proyecto requiere `--env-file` explícito, confirmar contra el comment real de `seed-phase10-7-gap-fill.ts`).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && test -f scripts/seed-author-eeat.ts && grep -q "juan-carlos-angulo" scripts/seed-author-eeat.ts && grep -q "verifyAvatar" scripts/seed-author-eeat.ts && grep -q "Advanced Technical SEO" scripts/seed-author-eeat.ts && echo OK</automated>
  </verify>
  <done>scripts/seed-author-eeat.ts existe, compila (tsx no lanza syntax errors), y contiene las 4 funciones de seed más verifyAvatar, con los datos reales ES/EN literales de las specifics de 12-CONTEXT.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ejecutar el seed contra la base de datos de desarrollo y verificar el resultado</name>
  <files>scripts/seed-author-eeat.ts</files>
  <action>
    Corre `npx tsx scripts/seed-author-eeat.ts` contra la base de datos de desarrollo (misma que usan los demás scripts de seed de este proyecto). Confirma en la salida de consola: (a) el avatar fue encontrado y NO re-subido, (b) los 4 items de expertise, 2 de education, 2 de experience y 3 de socialLinks se escribieron sin error para ambos locales.

    Verifica el resultado con una query directa de Local API (puede ser un script ad-hoc de una línea con `tsx -e` o agregando un log final a `main()`): `payload.findByID({ collection: 'authors', id: authorId, locale: 'es' })` y lo mismo con `locale: 'en'`, confirmando que `expertise[0].topic`, `education[0].degree`, `experience[0].role` difieren correctamente entre ES y EN (prueba de que la localización escribió valores distintos por locale, no el mismo valor duplicado).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && npx tsx scripts/seed-author-eeat.ts 2>&1 | tail -20</automated>
  </verify>
  <done>El script corrió sin errores contra Postgres de desarrollo; el Author real tiene expertise/education/experience/socialLinks poblados en ES y EN con valores distintos por locale donde corresponde (topic/degree/role/institution-label), y el avatar preexistente no fue tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script de seed (ejecución local, Local API con overrideAccess) -> Postgres de producción | El script escribe directo contra la base de datos real (no hay DB de test separada en este proyecto, mismo patrón que scripts previos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-05 | Tampering | seed-author-eeat.ts escribiendo sobre el único Author real | mitigate | Upsert por slug (nunca delete-then-recreate), verifyAvatar solo lee/loguea (nunca escribe sobre avatar), socialLinks no sobrescribe valores reales existentes distintos a los esperados |
| T-12-06 | Repudiation | Ejecución del script no queda registrada más allá del log de consola | accept | Mismo nivel de auditoría que todos los scripts de seed previos de este proyecto (Phase 4/10.x) — no se introduce un patrón nuevo de riesgo |

</threat_model>

<verification>
- `npx tsx scripts/seed-author-eeat.ts` corre limpio (exit code 0) contra la base de datos de desarrollo.
- `payload.findByID` en ambos locales muestra expertise/education/experience con 4/2/2 items respectivamente, con textos distintos entre `es` y `en` en los campos localizados.
- El avatar del Author (`portfolio/juan-angulo-portrait`) sigue siendo el mismo Media doc antes y después de correr el script (id sin cambios).
</verification>

<success_criteria>
El Author real tiene contenido E-E-A-T completo y bilingüe, listo para que 12-02 lo renderice. Cumple ROADMAP Phase 12 Success Criteria #6 (AUTHOR-06).
</success_criteria>

<output>
Create `.planning/phases/12-author-page-e-e-a-t-expansion/12-03-SUMMARY.md` when done
</output>
</content>
