---
phase: 13-home-content-population
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/blocks/AboutSection/Component.tsx
  - src/blocks/ContactFormBlock/Component.tsx
  - "src/app/(frontend)/[locale]/page.tsx"
  - scripts/seed-phase13-home-content.ts
autonomous: true
requirements: [ABOUT-02, FAQ-01]

must_haves:
  truths:
    - "Home (ES and EN) renders the AboutSection block's 4 features (icon + title + description) below the existing eyebrow/title/paragraphs, in a 1-col (<640px) / 2-col (>=640px) borderless grid"
    - "Home (ES and EN) renders a CTA button with the real copy, linking to #contact, below the features grid"
    - "Home (ES and EN) renders the FAQ block with the 5 real Q&A pairs, positioned after AboutSection and before ContactFormBlock"
    - "The #contact CTA target actually resolves to a rendered, functional ContactFormBlock on Home (id=\"contact\"), wired to the same Resend-backed server action already used on /contact — not a dead anchor"
    - "AboutSection's features/CTA content is an in-place update of the existing Home aboutSection block instance (populated in Phase 10.7) — no duplicate AboutSection block is created"
  artifacts:
    - path: "src/blocks/AboutSection/Component.tsx"
      provides: "Features grid (bg-primary/10 icon box, size-10/size-5, borderless, gap-4) + CTA button rendering, conditional on non-empty features / both ctaText+ctaLink present"
      contains: "features"
    - path: "scripts/seed-phase13-home-content.ts"
      provides: "Idempotent Local API seed: updates the existing Home aboutSection block's features/ctaText/ctaLink (ES+EN), adds a faq block (5 Q&A, ES+EN) and a contactFormBlock (reusing seed-contact-page.ts's real contact copy) to the Home layout, in order aboutSection -> faq -> contactFormBlock"
  key_links:
    - from: "src/blocks/AboutSection/Component.tsx"
      to: "AboutSectionBlock.features[].icon (payload-types.ts)"
      via: "iconMap lookup (trendingUp/zap/code/monitor) with a Code fallback, per 13-UI-SPEC.md's locked Section 1 snippet"
      pattern: "iconMap"
    - from: "AboutSection CTA <a href={ctaLink}>"
      to: "ContactFormBlockComponent's Container"
      via: "anchor id=\"contact\" added to the Container wrapping ContactFormBlockComponent, matched against ctaLink=\"#contact\" seeded in the AboutSection block"
      pattern: "id=\"contact\""
    - from: "src/app/(frontend)/[locale]/page.tsx"
      to: "src/app/actions/contact.ts#sendContactMessage"
      via: "RenderBlocks sharedProps={{ onSubmit: sendContactMessage, locale, contactEmail }}, mirroring the existing contact/page.tsx wiring"
      pattern: "sendContactMessage"
---

<objective>
Render the `AboutSection` features grid + CTA on the frontend (13-UI-SPEC.md Section 1, locked contract), populate Home with the real "Mi enfoque en Consultoría Técnica" content and the FAQ block's 5 real Q&A pairs (ABOUT-02, FAQ-01), and close a gap discovered while planning: CONTEXT.md's CTA decision (`ctaLink` → `#contact`, "mismo comportamiento que el sitio de referencia") assumes a `ContactFormBlock` is already present on Home — it is not (confirmed live against the dev Postgres instance: Home's current layout is `hero, featuredCaseStudiesBlock, aboutSection, clientLogosBlock, featuredPostsBlock, testimonialsCarousel, callToAction`, no `contactFormBlock`). This plan adds a minimal, fully-functional `contactFormBlock` to Home (reusing the exact existing content/action/pattern already live on `/contact` — no new subsystem) so the CTA is a real, working link instead of a dead anchor.

Purpose: this is the content-delivery half of Phase 13 — it makes the schema/admin work from 13-01 visible and real on the public site.

Output: updated `AboutSection` rendering, a working `#contact` anchor, and Home populated with real features/CTA/FAQ/contact content in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-home-content-population/13-CONTEXT.md
@.planning/phases/13-home-content-population/13-UI-SPEC.md

@src/blocks/AboutSection/Component.tsx
@src/blocks/FAQ/Component.tsx
@src/blocks/ContactFormBlock/Component.tsx
@src/blocks/RenderBlocks.tsx
@src/app/(frontend)/[locale]/page.tsx
@src/app/(frontend)/[locale]/contact/page.tsx
@src/app/actions/contact.ts
@scripts/seed-contact-page.ts
@scripts/seed-phase10-7-gap-fill.ts

<interfaces>
<!-- AboutSectionBlock (payload-types.ts, regenerated by 13-01) now includes:
     features?: { icon: string; title: string; description: string; id?: string | null }[] | null
     ctaText?: string | null
     ctaLink?: string | null
   FAQBlock (payload-types.ts, unchanged) — faqs[].answer is Lexical richText, use the
   existing lexicalParagraph()-style helper (see seed-phase10-7-gap-fill.ts) to build it,
   not a plain string.
   ContactFormBlock (payload-types.ts, unchanged) — fields used below: eyebrow, title,
   description, submitLabel, sidebarTitle, sidebarDescription, socialProofText, contactInfo[]
   ({ icon, title, value, href }). -->

From src/blocks/RenderBlocks.tsx: `blockComponents` maps `blockType` string -> renderer
component; `faq` -> FAQComponent and `contactFormBlock` -> ContactFormBlockComponent are
already registered — no RenderBlocks change needed, only layout data + sharedProps.

From src/app/(frontend)/[locale]/contact/page.tsx (the pattern to mirror on Home):
`<RenderBlocks blocks={doc.content?.layout ?? []} sharedProps={{ onSubmit: sendContactMessage, locale, sent, contactEmail: process.env.CONTACT_TO_EMAIL }} />`
— Home does not need `sent` (no `?sent=` search param handling exists there and none is
being added in this plan; ContactFormBlockComponent's `sent` prop is optional/undefined-safe).

From scripts/seed-contact-page.ts: the real, already-live contact copy to reuse verbatim
for the Home instance (ES: eyebrow "Contacto", title "Hablemos", description "¿Tienes un
proyecto en mente? Cuéntame de qué se trata.", submitLabel "Enviar mensaje", sidebarTitle
"Charlemos sobre tu próximo proyecto", sidebarDescription "Disponible para consultoría en
ingeniería de software y SEO técnico.", socialProofText "Respondo en menos de 48 horas.",
contactInfo: [{ icon: 'mail', title: 'Email', value: 'hello@juan-tech.com', href:
'mailto:hello@juan-tech.com' }]; EN mirrors 1:1 per that file).

From scripts/seed-phase10-7-gap-fill.ts: the id-reuse pattern for locale-by-locale
`payload.update()` writes on an array-of-blocks field (`content.layout`) — capture the
block's server-assigned `id` after the first locale write and reuse it on subsequent
locale writes, or each locale's write orphans the previous locale's localized sibling
fields (this bug was already hit and fixed once in this codebase — do not reintroduce it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render features grid + CTA, and make the #contact anchor real</name>
  <files>src/blocks/AboutSection/Component.tsx, src/blocks/ContactFormBlock/Component.tsx, src/app/(frontend)/[locale]/page.tsx</files>
  <action>
Edit `src/blocks/AboutSection/Component.tsx`: destructure `features` and `ctaText`/`ctaLink` from `props` alongside the existing `eyebrow, title, paragraphs, photo`. Implement exactly the rendering contract locked in 13-UI-SPEC.md's "Section 1" (do not deviate from the values given there): import `TrendingUp, Zap, Code, Monitor` from `lucide-react` and `Button` from `@/components/ui/button`; define `const iconMap = { trendingUp: TrendingUp, zap: Zap, code: Code, monitor: Monitor } as const` at module scope. After the existing `eyebrow`/`title`/`paragraphs`/`photo` grid closes (still inside the same `<Container className="py-12">`), add: (1) a features grid rendered only when `features && features.length > 0`, wrapped in `<div className="mt-12 grid grid-cols-1 sm:grid-cols-2 gap-6">`, mapping each `item` to a `<div className="flex gap-4">` containing an icon box `<div className="flex size-10 shrink-0 items-center justify-center rounded-md bg-primary/10">` with `<Icon className="size-5 text-primary" />` (`Icon = iconMap[item.icon as keyof typeof iconMap] ?? Code`) and a text column with `<p className="font-heading text-body font-semibold">{item.title}</p>` + `<p className="mt-1 text-body text-muted-foreground">{item.description}</p>`; (2) a CTA rendered only when both `ctaText && ctaLink` are truthy, wrapped in `<div className="mt-8">` containing `<Button asChild><a href={ctaLink}>{ctaText}</a></Button>`. Do not wrap feature items in `Card`, do not add `border`/`shadow-*`/`bg-card` to them (borderless by design per the UI-SPEC pattern decision), and do not render the CTA if only one of `ctaText`/`ctaLink` is set.

Edit `src/blocks/ContactFormBlock/Component.tsx`: add `id="contact"` to the outer `<Container className="py-12 md:py-16">` element (Container forwards extra props/className but confirm it also forwards `id` — if `Container`'s prop types don't include `id`, wrap the existing `<Container>` in a plain `<div id="contact">` instead, whichever compiles cleanly without changing Container's public API). This is the anchor target the new AboutSection CTA (`ctaLink="#contact"`, seeded in Task 2) scrolls to — closes the gap noted in this plan's `<objective>` (CONTEXT.md assumed this block was already on Home; it wasn't).

Edit `src/app/(frontend)/[locale]/page.tsx`: import `sendContactMessage` from `@/app/actions/contact` (same import already used in `contact/page.tsx`), and pass `sharedProps={{ onSubmit: sendContactMessage, locale, contactEmail: process.env.CONTACT_TO_EMAIL }}` to the existing `<RenderBlocks blocks={doc.content?.layout ?? []} />` call — this is what makes the `contactFormBlock` instance seeded onto Home in Task 2 actually send email via Resend instead of silently no-op'ing (`RenderBlocks`/`ContactFormBlockComponent` already support this generically; `contact/page.tsx` is the existing precedent for this exact wiring).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "AboutSection|ContactFormBlock|locale\]/page" ; echo done</automated>
  </verify>
  <done>AboutSectionComponent renders the features grid + CTA per the UI-SPEC snippet; ContactFormBlockComponent's root element carries id="contact"; Home's page.tsx forwards onSubmit/locale/contactEmail to RenderBlocks; project type-checks clean</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed script — populate AboutSection features/CTA, add FAQ + ContactFormBlock to Home</name>
  <files>scripts/seed-phase13-home-content.ts</files>
  <action>
Create `scripts/seed-phase13-home-content.ts` (idempotent, Payload Local API, `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase13-home-content.ts` — mirror the header-comment/import/`getPayload({ config })`/`LOCALES = ['es','en'] as const` structure of `scripts/seed-phase10-7-gap-fill.ts`). It must:

1. Fetch the `home` Pages doc by slug (abort with a clear console message if missing, same guard pattern as the reference scripts).
2. Find the existing `aboutSection` block in `content.layout` by `blockType === 'aboutSection'` (do NOT create a second AboutSection — this must be an in-place update of the block populated in Phase 10.7). For each locale in `['es','en']`, set that block's `features` to the 4 real items translated per locale and `ctaText`/`ctaLink`:
   - ES: eyebrow/title/paragraphs stay whatever Phase 10.7 already seeded (do not touch them) — only add `features` and `ctaText`/`ctaLink`. `ctaText: 'Hablemos de tu proyecto'`, `ctaLink: '#contact'`. `features`: `[{ icon: 'trendingUp', title: 'SEO Técnico', description: 'Optimización profunda de infraestructura, Rendering y esquema (Schema) para maximizar la visibilidad en motores de búsqueda.' }, { icon: 'zap', title: 'Rendimiento web', description: 'Obsesión por el 100/100. Optimización de la ruta crítica de renderizado para tiempos de carga inmediatos.' }, { icon: 'code', title: 'Arquitectura escalable', description: 'Diseño de sistemas modulares y limpios. Código mantenible que facilita el crecimiento del proyecto sin deuda técnica.' }, { icon: 'monitor', title: 'Ingeniería de UX', description: 'Interfaces adaptables y accesibles (A11Y). Desarrollo Mobile-First real, no solo visual, sino funcional.' }]`.
   - EN: write accurate professional translations of the same 4 features (same `icon` values, translated `title`/`description`) and `ctaText: 'Let's talk about your project'`, `ctaLink: '#contact'`. Author the EN description translations to read as natural professional marketing copy, not literal word-for-word — same bar already applied by `seed-phase10-7-gap-fill.ts`'s EN copy.
   Reuse the block's existing `id` across both locale writes (same id-reuse pattern already implemented in `seed-phase10-7-gap-fill.ts`'s `seedAboutSection` function) so the second locale's write doesn't orphan the first locale's localized fields.
3. Insert a new `faq` block into `content.layout` immediately after the `aboutSection` block (if a `faq` block already exists in the layout, update it in place instead of duplicating — same idempotency discipline as the rest of this script) with `title` ("Preguntas frecuentes" / "Frequently asked questions") and 5 `faqs[]` entries — `question` (localized text) + `answer` (localized richText, built with a `lexicalParagraph(text)` helper identical to the one in `seed-phase10-7-gap-fill.ts`) — using the exact 5 ES questions/answers from `13-CONTEXT.md`'s `<specifics>` section verbatim, each with an accurate professional EN translation (do not leave Spanish in the `en` locale, same criterion Phase 12 already established).
4. Insert a `contactFormBlock` at the end of `content.layout` (after `faq`, i.e. after `callToAction` too — matching CONTEXT.md's locked order "FAQ se agrega al final del layout, antes del ContactFormBlock"; since `callToAction` already sits at the end of the current layout, `contactFormBlock` goes after it), reusing the exact content from `scripts/seed-contact-page.ts` (ES: eyebrow "Contacto", title "Hablemos", description "¿Tienes un proyecto en mente? Cuéntame de qué se trata.", submitLabel "Enviar mensaje", sidebarTitle "Charlemos sobre tu próximo proyecto", sidebarDescription "Disponible para consultoría en ingeniería de software y SEO técnico.", socialProofText "Respondo en menos de 48 horas.", contactInfo: `[{ icon: 'mail', title: 'Email', value: 'hello@juan-tech.com', href: 'mailto:hello@juan-tech.com' }]`; EN mirrors that file's `en` entry 1:1) — again skip creation if a `contactFormBlock` already exists in Home's layout (idempotent).
5. Write the full updated `layout` array per locale via `payload.update({ collection: 'pages', id: homeDoc.id, locale, data: { content: { layout: newLayout } } })`, refetching after the first locale write to capture server-assigned ids for the new `faq`/`contactFormBlock` blocks before writing the second locale (same pattern as step 2 and as `seed-phase10-7-gap-fill.ts`).

Run the script against the dev database: `node --env-file=.env node_modules/.bin/tsx scripts/seed-phase13-home-content.ts`.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/seed-phase13-home-content.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Running the script twice in a row is a no-op the second time (idempotent); Home's `content.layout` (both locales) contains exactly one `aboutSection` (with features[4]/ctaText/ctaLink populated), exactly one `faq` (5 Q&A), and exactly one `contactFormBlock`, in that relative order after the pre-existing blocks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor -> AboutSection CTA -> ContactFormBlock on Home | Same trust boundary already accepted on `/contact` (public form submission -> server action -> Resend); no new boundary introduced, just a second entry point into the same, already-hardened `sendContactMessage` action (honeypot + email regex + escapeHtml already in place there) |
| Seed script -> Postgres | Local API write, run manually against dev DB by a trusted operator (same as every prior phase's seed script) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | `contactEmail` prop on Home | accept | Same `process.env.CONTACT_TO_EMAIL` value already publicly implied by the `/contact` page's error copy — no new disclosure surface |
| T-13-04 | Spoofing | Contact form submitted from Home | accept | Reuses the exact same `sendContactMessage` action already live on `/contact`, with its existing honeypot + email-format validation — no new attack surface introduced by mounting it on a second page |
| T-13-05 | Denial of Service (broken UX, not availability) | CTA button rendered with only one of ctaText/ctaLink set | mitigate | AboutSectionComponent's rendering contract explicitly requires both truthy before rendering the CTA (Task 1) |
</threat_model>

<verification>
1. `npm run dev` (or existing dev server), visit `/` and `/es` — confirm the features grid (4 items, correct icons/titles/descriptions) and CTA button render below the existing About paragraphs, and the FAQ section renders with 5 real Q&A above the contact form.
2. Click the CTA button — confirm the page scrolls to the contact form section (anchor resolves, no dead link).
3. Resize to 375px — confirm 1-column feature grid, no cramped/overflowing text; resize to 768/1280px — confirm 2-column grid.
4. Re-run `scripts/seed-phase13-home-content.ts` a second time — confirm no duplicate blocks are created in `content.layout` (idempotency).
</verification>

<success_criteria>
- Home (ES+EN) shows the AboutSection features grid + working CTA and the FAQ block with 5 real Q&A, positioned after AboutSection and before the (newly present) ContactFormBlock (ABOUT-02, FAQ-01).
- The `#contact` CTA target is a real, functional contact form, not a dead anchor.
- Seed script is idempotent and does not duplicate the existing Phase 10.7 AboutSection block.
</success_criteria>

<output>
Create `.planning/phases/13-home-content-population/13-02-SUMMARY.md` when done
</output>
