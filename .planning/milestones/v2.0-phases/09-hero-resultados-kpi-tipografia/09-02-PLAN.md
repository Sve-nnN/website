---
phase: 09-hero-resultados-kpi-tipografia
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/ResultsSection/Component.tsx
  - "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
autonomous: true
requirements: [UI-07]

must_haves:
  truths:
    - "ResultsSection y las tarjetas KPI de una case study muestran la métrica principal como el elemento visualmente dominante de la sección, reforzado solo con type scale/weight/color existentes — sin agregar chrome de UI nueva (sin nuevos wrappers de card, bordes o fondos que no existieran ya)"
    - "El tratamiento visual de dominancia de métrica es consistente entre ResultsSection (genérico, reutilizable en cualquier página) y las KPI cards reales del detalle de case study"
    - "Los encabezados de sección del detalle de case study (El cliente/El reto/La solución/Resultados) mantienen exactamente los mismos tags h1/h2/h3 que antes — solo cambian clases CSS"
  artifacts:
    - path: "src/blocks/ResultsSection/Component.tsx"
      provides: "Stat cards con jerarquía tipográfica reforzada (valor dominante, label recesivo) usando solo tokens existentes"
    - path: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      provides: "heroMetric, KPI cards y comparativa de resultados con el mismo patrón de dominancia visual que ResultsSection, ritmo de encabezados alineado con Prose"
  key_links:
    - from: "src/blocks/ResultsSection/Component.tsx"
      to: "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx"
      via: "mismo patrón de clases para valor dominante (text-display font-display font-semibold text-primary tracking-tight tabular-nums) y label recesivo (text-label opacity-70 uppercase tracking-wide)"
      pattern: "tabular-nums"
---

<objective>
Reforzar visualmente la métrica principal en `ResultsSection` (genérico) y en las KPI cards + comparativa de resultados del detalle de case study, para que el número sea inequívocamente el elemento dominante de la sección — usando solo type scale/weight/color de los tokens ya existentes, sin introducir chrome de UI nueva. Alinear también el ritmo tipográfico de los encabezados de sección (El cliente/El reto/La solución/Resultados) del detalle de case study con el patrón ya establecido en `Prose.tsx`, sin degradar la semántica de encabezados.

Purpose: Implementa UI-07 y el patrón "métrica en el titular" ya decidido en PROJECT.md (modelo `ariannalupi.com/casos/`) — el principal diferenciador competitivo del sitio. `ResultsSection` es un bloque genérico Payload-editable; el detalle de case study renderiza los KPIs/resultados reales estructurados — ambos deben leer como la misma familia visual.

Output: `ResultsSection/Component.tsx` con stats reforzados; `case-studies/[slug]/page.tsx` con heroMetric/KPIs/resultados reforzados y encabezados de sección re-alineados a Prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-hero-resultados-kpi-tipografia/09-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md

<interfaces>
<!-- Current ResultsSection/Component.tsx (read in full) -->
```tsx
import type { ResultsSectionBlock as ResultsSectionBlockProps } from '@/payload-types'
import { Container } from '@/components/Container'
import { cn } from '@/lib/utils'

const backgroundMap: Record<string, string> = {
  gray: 'bg-muted',
  white: 'bg-background',
  primary: 'bg-primary text-primary-foreground',
}

export function ResultsSectionComponent(props: ResultsSectionBlockProps) {
  const { title, description, stats, backgroundColor } = props
  return (
    <section className={cn('py-16', backgroundMap[backgroundColor ?? 'gray'])}>
      <Container>
        <h2 className="font-display text-heading">{title}</h2>
        {description && <p className="mt-2 text-body max-w-2xl">{description}</p>}
        <div className="mt-8 grid grid-cols-2 md:grid-cols-3 gap-8">
          {stats?.map((stat, i) => (
            <div key={i}>
              <p className="text-display font-display font-semibold text-primary">{stat.value}</p>
              <p className="mt-1 text-label">{stat.label}</p>
            </div>
          ))}
        </div>
      </Container>
    </section>
  )
}
```

<!-- Current case-studies/[slug]/page.tsx KPI/results sections (relevant excerpts,
read in full — no need to re-read the file) -->
```tsx
{/* Hero metric */}
{doc.heroMetric && (
  <p className="mt-4 text-display font-display font-semibold text-primary">{doc.heroMetric}</p>
)}

{/* KPI cards */}
{doc.kpis.map((kpi, i) => (
  <div key={i} className="rounded-lg bg-secondary text-secondary-foreground p-6 text-center">
    <p className="text-display font-display font-semibold text-primary">{kpi.value}</p>
    <p className="mt-1 text-label">{kpi.label}</p>
  </div>
))}

{/* Section headings, e.g. */}
<h2 className="font-display text-heading mb-4">{t.client}</h2>

{/* Results comparison metrics */}
{doc.results.metrics.map((metric, i) => (
  <div key={i} className="rounded-lg border border-border p-4">
    <p className="text-label text-muted-foreground">{metric.label}</p>
    <div className="mt-2 flex items-baseline gap-2">
      <span className="text-body line-through opacity-60">{metric.before}</span>
      <span className="font-display text-heading font-semibold text-primary">{metric.after}</span>
    </div>
  </div>
))}
```

From `src/components/Prose.tsx` (already-established heading rhythm, read in full):
```
h2: font-display text-heading mt-10 mb-4
h3: font-display text-heading mt-8 mb-3
```

Locked type scale (only 4 sizes, `tailwind.config.ts`): `text-body`, `text-label`, `text-heading`, `text-display`. Locked accent color: `--primary: #FF5B1F` reserved for "KPI metric numbers/before-after deltas in case studies" per 05-UI-SPEC.md Color table — already correctly applied, do not change the color itself.

Known content gap (documented in STATE.md, Phase 8): no real CaseStudies exist in the DB with populated stats yet — this is an accepted content gap, not a code defect. Do not block this plan on it; verify against the block's render logic and TypeScript correctness, not live production data.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reinforce metric dominance in ResultsSection stat cards</name>
  <files>src/blocks/ResultsSection/Component.tsx</files>
  <action>
Per the "Results/KPI treatment" decision in 09-CONTEXT.md: reinforce the main metric via type scale/weight/color using existing tokens only — do NOT add new UI chrome (no new wrapper `<div>` background/border/rounded treatment around each stat; the stats stay bare `<div>`s as today).

On the stat value `<p>`: add `tracking-tight` (tighter number display reads more authoritative) and `tabular-nums` (aligns digits when multiple stats sit in a grid row — a typography-only Tailwind utility, not new chrome).

On the stat label `<p>`: recede it relative to the value by adding `uppercase tracking-wide opacity-70` — this increases the *relative* dominance of the value through label recession, the same hierarchy technique used by the reference model (`ariannalupi.com/casos/`), without inventing a new size token.

Increase the gap between stat groups from `gap-8` to `gap-12` (a standard Tailwind spacing step, consistent with the locked 4px-multiple spacing scale) so each metric has more breathing room and reads as more dominant.

Leave `backgroundMap`, the section wrapper, and the grid column counts unchanged.
  </action>
  <verify>
    <automated>grep -c "tabular-nums" src/blocks/ResultsSection/Component.tsx ; npx tsc --noEmit</automated>
  </verify>
  <done>Stat value uses `tracking-tight tabular-nums`, stat label uses `uppercase tracking-wide opacity-70`, gap increased to `gap-12`, no new wrapper elements added, `tsc --noEmit` passes clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align case-study KPI/results dominance treatment and section heading rhythm</name>
  <files>src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx</files>
  <action>
Apply the exact same value/label pattern from Task 1 to keep both surfaces visually consistent (per this plan's `key_links`):
- `heroMetric` paragraph: add `tracking-tight tabular-nums`.
- KPI card value `<p>`: add `tracking-tight tabular-nums`; KPI card label `<p>`: add `uppercase tracking-wide opacity-70` (existing `mt-1 text-label` stays).
- Results-comparison "after" `<span>`: add `tracking-tight tabular-nums`; the "before" `<span>` (already `line-through opacity-60`) stays as-is — it is intentionally recessive already. The metric label `<p>` above it: add `uppercase tracking-wide` (it already has `text-label text-muted-foreground`, which already recedes it — just add the uppercase/tracking treatment for consistency with the other two surfaces).

Do NOT add new wrapper elements or change the existing `rounded-lg bg-secondary p-6` / `rounded-lg border border-border p-4` chrome — those containers already existed pre-phase; only the internal type treatment changes.

Align the four section headings (`t.client`, `t.challenge`, `t.solution`, `t.results`, each currently `<h2 className="font-display text-heading mb-4">`) with the vertical rhythm already established in `Prose.tsx`'s `h2` rule (`mt-10 mb-4`) for consistency across all long-form content in the site — change `mb-4` to `mt-10 mb-4` on all four (the first section's extra `mt-10` is harmless since it already sits inside a `space-y-12` container, Tailwind's `mt-*` on a flex/grid-item sibling stacks additively with `space-y` — verify visually there is no doubled gap; if there is, use `first:mt-0` as an explicit override on the h2 className to zero it out only for the first section heading in the page).

Do NOT rename, add, or remove any `<h1>`/`<h2>`/`<h3>` tag in this file — only className edits.
  </action>
  <verify>
    <automated>grep -oE '<h[1-3]' "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | sort | uniq -c ; npx tsc --noEmit</automated>
  </verify>
  <done>heroMetric/KPI/results-comparison values use `tracking-tight tabular-nums`, their labels use `uppercase tracking-wide`, the four section h2 headings use `mt-10 mb-4` rhythm (with `first:mt-0` if the visual check shows doubled spacing), heading tag count identical before/after this task's diff, `tsc --noEmit` passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload CMS → rendered page | `stats`/`kpis`/`results.metrics` values are editor-authored Payload field content rendered as plain text (React auto-escapes); no new boundary introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-09-03 | Tampering | Heading semantics in `case-studies/[slug]/page.tsx` | mitigate | Task 2's verify step greps `<h[1-3]` tag counts before/after to confirm no structural markup change, only className edits — protects the SEO-relevant heading hierarchy this exact class of change is known to risk per RESEARCH.md pitfall #6 |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no new type errors.
2. `grep -oE '<h[1-3]' "src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx" | sort | uniq -c` matches the pre-change count exactly (documented in SUMMARY).
3. `git diff --stat` shows no changes to any `src/blocks/*/config.ts` or `src/payload-types.ts`.
4. Manual visual spot-check against a seeded/dev case-study doc (or the ArchiveBlock preview) confirming the metric values read as visually dominant over their labels.
</verification>

<success_criteria>
- ResultsSection and case-study KPI/results metric values are visually dominant via type/color/spacing only, no new UI chrome.
- Visual treatment is consistent between ResultsSection and the case-study detail page's KPI/results surfaces.
- Heading semantics (h1/h2/h3 tags/order) unchanged in `case-studies/[slug]/page.tsx`.
- Zero diffs in `src/blocks/*/config.ts` / `src/payload-types.ts`.
</success_criteria>

<output>
Create `.planning/phases/09-hero-resultados-kpi-tipografia/09-02-SUMMARY.md` when done
</output>
