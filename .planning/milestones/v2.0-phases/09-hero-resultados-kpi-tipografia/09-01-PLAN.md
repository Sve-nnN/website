---
phase: 09-hero-resultados-kpi-tipografia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocks/Hero/Component.tsx
  - scripts/check-hero-overlay-contrast.ts
autonomous: true
requirements: [UI-06]

must_haves:
  truths:
    - "El Hero (todas las variantes: home/listing/post-header/case-study-header) transmite mayor impacto visual mediante tipografía y espaciado, sin agregar campos nuevos a HeroBlock ni tocar src/blocks/Hero/config.ts"
    - "El contraste WCAG del texto sobre el fondo compuesto (imagen + overlay navy) del Hero se re-verifica automáticamente contra las 53 imágenes reales del pool de fallback de Cloudinary, no solo contra una imagen idealizada"
    - "Si alguna imagen real del pool produce un fondo compuesto que falla el contraste WCAG, el overlay se ajusta con el cambio mínimo necesario para pasar, documentado en el SUMMARY"
  artifacts:
    - path: "src/blocks/Hero/Component.tsx"
      provides: "Tratamiento tipográfico de mayor impacto (jerarquía título/subtítulo, ritmo vertical) y, si el script de contraste lo exige, ajuste mínimo del overlay — sin cambios de config.ts"
    - path: "scripts/check-hero-overlay-contrast.ts"
      provides: "Verificación WCAG automatizada del contraste texto-sobre-fondo-compuesto contra las 53 imágenes reales de fallback de Cloudinary más el caso teórico peor-caso (píxel blanco puro)"
  key_links:
    - from: "scripts/check-hero-overlay-contrast.ts"
      to: "src/lib/heroImageFallback.ts"
      via: "mismas constantes FALLBACK_BASE_URL / FALLBACK_POOL_SIZE, mantenidas en sync"
      pattern: "FALLBACK_BASE_URL|FALLBACK_POOL_SIZE"
    - from: "src/blocks/Hero/Component.tsx"
      to: "scripts/check-hero-overlay-contrast.ts"
      via: "los valores de opacidad/overlay codificados en el componente deben coincidir exactamente con los que el script usa para calcular el contraste compuesto"
      pattern: "opacity-|bg-secondary"
---

<objective>
Aumentar el impacto visual del Hero (jerarquía tipográfica título/subtítulo, ritmo vertical) usando únicamente tokens ya existentes, y re-verificar de forma automatizada el contraste WCAG del texto sobre el fondo compuesto (imagen + overlay) contra las 53 imágenes reales del pool de fallback de Cloudinary — no asumir que el check de contraste plano de Fase 7 cubre este caso compuesto.

Purpose: Implementa UI-06 y el criterio de éxito #4 del ROADMAP de la Fase 9 (re-verificación de contraste sobre fondos compuestos, no heredado de Fase 7). El Hero es la primera impresión de cada página; su overlay actual (`opacity-30` sobre `bg-secondary`) nunca fue verificado matemáticamente contra la variación real de brillo de las 53 imágenes de fallback que la Fase 4 confirmó como el patrón real de producción.

Output: `src/blocks/Hero/Component.tsx` con jerarquía tipográfica reforzada; `scripts/check-hero-overlay-contrast.ts` como verificación WCAG repetible, siguiendo el mismo patrón que `scripts/check-dark-contrast.ts` de Fase 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-hero-resultados-kpi-tipografia/09-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/phases/07-design-token-foundation/07-01-SUMMARY.md

<interfaces>
<!-- Current Hero/Component.tsx (read in full — reproduced here so the executor
does not need to re-read it to understand the starting point). -->

```tsx
import Image from 'next/image'
import type { HeroBlock as HeroBlockProps } from '@/payload-types'
import { Container } from '@/components/Container'

export function HeroComponent(props: HeroBlockProps) {
  const { variant, title, subtitle, media } = props
  const image = typeof media === 'object' ? media : null
  const isHome = variant === 'home'

  return (
    <section
      className={
        isHome
          ? 'relative bg-secondary text-secondary-foreground py-16 md:py-24'
          : 'relative bg-secondary text-secondary-foreground py-12 md:py-16'
      }
    >
      {image?.url && (
        <div className="absolute inset-0 opacity-30">
          <Image src={image.url} alt={image.alt ?? ''} fill className="object-cover" priority />
        </div>
      )}
      <Container className="relative z-10">
        {title && (
          <h1 className={isHome ? 'font-display text-display' : 'font-display text-heading'}>
            {title}
          </h1>
        )}
        {subtitle && <p className="mt-4 text-body max-w-2xl">{subtitle}</p>}
      </Container>
    </section>
  )
}
```

HeroBlock config fields (unchanged, zero-diff constraint): `variant` (select: home/listing/post-header/case-study-header), `title` (localized text), `subtitle` (localized text), `media` (upload). No `eyebrow` field exists — do not invent one; there is no schema slot for a third text role beyond title/subtitle.

From `src/lib/heroImageFallback.ts` (read in full):
```ts
const FALLBACK_POOL_SIZE = 53
const FALLBACK_BASE_URL = 'https://res.cloudinary.com/dmufha3qv/image/upload/f_auto,q_auto/portfolio'
// URLs: `${FALLBACK_BASE_URL}/fallback-image-${n}.avif` for n = 1..53
```

Locked color tokens (`src/app/globals.css` `:root`): `--secondary: #12141C` (navy), `--secondary-foreground: #FAFAF7` (off-white). Locked type scale (`tailwind.config.ts`): only `text-body`(16px/400), `text-label`(14px/600), `text-heading`(clamp 22-28px/600), `text-display`(clamp 36-56px/600) exist — do not invent new sizes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strengthen Hero typographic hierarchy (title/subtitle roles, vertical rhythm)</name>
  <files>src/blocks/Hero/Component.tsx</files>
  <action>
Per the "Hero treatment" decision in 09-CONTEXT.md: increase visual impact purely via typography/spacing/hierarchy on the text layer — do NOT alter the existing image overlay opacity/mechanics as an "impact" lever in this task (that is reserved for Task 2, only if the contrast script demands it), do NOT add fields, do NOT touch `src/blocks/Hero/config.ts`.

Apply to the `<h1>` title: add `tracking-tight` for a more editorial, authoritative feel at both `text-display` (home) and `text-heading` (other variants) sizes — this reinforces weight without a new size token.

Apply to the `<p>` subtitle: strengthen its role separation from the title by muting its color relative to the title (`text-secondary-foreground/80` — an opacity modifier on the existing token, not a new color) and increasing its top margin for clearer vertical rhythm (`mt-6` instead of `mt-4`, still a standard Tailwind spacing step, not an arbitrary value).

Do not change `py-16 md:py-24` / `py-12 md:py-16` section padding — those already sit at or above the locked 3xl=64px spacing-scale ceiling from 05-UI-SPEC.md; touching them is out of scope for this plan.

Keep the `isHome` branch as the sole size differentiator (unchanged): home = `text-display`, other variants = `text-heading`.
  </action>
  <verify>
    <automated>grep -n "tracking-tight" src/blocks/Hero/Component.tsx | grep -c "h1" ; npx tsc --noEmit</automated>
  </verify>
  <done>Hero title uses `tracking-tight`, subtitle uses a muted color token and increased top margin, no new Payload field or config.ts diff, `tsc --noEmit` passes clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Automated WCAG contrast verification against the real 53-image Cloudinary fallback pool</name>
  <files>scripts/check-hero-overlay-contrast.ts, src/blocks/Hero/Component.tsx</files>
  <action>
Write `scripts/check-hero-overlay-contrast.ts`, following the same standalone/self-contained pattern as `scripts/check-dark-contrast.ts` (hex/rgba parsing, alpha compositing, WCAG relative-luminance, contrast-ratio formula — reuse the same math, do not add a new contrast-calculation dependency).

The script must:
1. Import `sharp` (already a project dependency — do not add a new one) to decode downloaded image bytes.
2. For `n` = 1..53, `fetch()` `https://res.cloudinary.com/dmufha3qv/image/upload/f_auto,q_auto/portfolio/fallback-image-${n}.avif`, decode with `sharp(buffer).resize(8, 8).raw().toBuffer()`, and compute that image's average RGB across the 64 sampled pixels.
3. Also include one synthetic worst-case entry: pure white `{ r: 255, g: 255, b: 255 }`, to guarantee a safety margin beyond the sampled real pool (matches 09-CONTEXT.md's instruction to verify against the real fallback scenario, not just an idealized image — the synthetic case is an explicit additional safety margin, not a substitute for the real sample).
4. For every one of the 54 candidate colors, alpha-composite it over the navy `--secondary` (`#12141C`) background at the exact opacity value currently used in `Hero/Component.tsx`'s overlay div (read the value from the component's own className string via a small regex extraction, or hardcode it with an inline comment cross-referencing the exact line — keep the two files in sync, same pattern `check-dark-contrast.ts` uses for `.dark` token literals).
5. Contrast-check the resulting composited background against `--secondary-foreground` (`#FAFAF7`) at two thresholds: 3.0:1 (large text — the `text-display`/`text-heading` title) and 4.5:1 (normal text — the `text-body` subtitle).
6. Print a pass/fail table (worst 5 images by contrast ratio, plus the synthetic white case) and exit 1 if any candidate fails either applicable threshold; exit 0 if all 54 pass.

Run it: `node_modules/.bin/tsx scripts/check-hero-overlay-contrast.ts`.

If it fails: apply the minimal necessary fix directly to the overlay in `src/blocks/Hero/Component.tsx` — e.g., adding a `bg-secondary/NN` scrim div between the image and the text, or adjusting the existing `opacity-30` value — narrowly scoped to restoring the mandated contrast, not a broader visual redesign of the overlay. Update the script's hardcoded opacity value to match if changed, then re-run until it exits 0.
  </action>
  <verify>
    <automated>node_modules/.bin/tsx scripts/check-hero-overlay-contrast.ts</automated>
  </verify>
  <done>Script exits 0: all 53 real fallback images plus the synthetic white worst-case pass WCAG contrast (3:1 large text, 4.5:1 normal text) against the Hero's composited overlay background, using the exact opacity value shipped in `Hero/Component.tsx`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Verification script → Cloudinary CDN | The contrast script fetches 53 remote images over HTTPS at plan-execution/CI time; not part of the runtime app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-09-01 | Tampering | `scripts/check-hero-overlay-contrast.ts` fetch of remote Cloudinary URLs | accept | Read-only GET against a known, already-production `res.cloudinary.com/dmufha3qv` bucket referenced by `heroImageFallback.ts`; no write, no credential exposure, script is a dev/CI-time verification tool, not shipped to the runtime bundle |
| T-09-02 | Information Disclosure | Hero overlay contrast on composited background | mitigate | Task 2's script enforces WCAG AA thresholds against the real image pool before the plan is considered done — prevents shipping a Hero where text becomes unreadable against a bright fallback image |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no new type errors.
2. `node_modules/.bin/tsx scripts/check-hero-overlay-contrast.ts` exits 0.
3. `git diff --stat` shows no changes to `src/blocks/Hero/config.ts` or `src/payload-types.ts`.
4. Manual visual spot-check (dev server, `/es` and `/en` home page) that the Hero title/subtitle read clearly and the overlay change (if any) does not look like a visual regression from the current washed-out treatment.
</verification>

<success_criteria>
- Hero title/subtitle typography strengthened using only existing tokens (`tracking-tight`, opacity modifier on `--secondary-foreground`, standard spacing step).
- `scripts/check-hero-overlay-contrast.ts` exists, runs standalone, and passes for all 53 real Cloudinary fallback images plus the synthetic white worst-case.
- Zero diffs in `src/blocks/Hero/config.ts` / `src/payload-types.ts`.
</success_criteria>

<output>
Create `.planning/phases/09-hero-resultados-kpi-tipografia/09-01-SUMMARY.md` when done
</output>
