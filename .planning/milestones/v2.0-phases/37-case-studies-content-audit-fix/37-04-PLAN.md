---
phase: 37-case-studies-content-audit-fix
plan: 04
type: execute
wave: 3
depends_on: ["37-03"]
files_modified: []
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, CASE-04, CASE-05, CASE-06]

must_haves:
  truths:
    - "Docs 18, 19 have non-empty bilingual challenge/solution, labeled KPIs/metrics, and multi-row real-GSC-backed results.metrics"
    - "Doc 20 contains no real client name, domain, county, or review count in ANY field (title, heroSubtitle, clientContext, meta, client relationship) — replaced with anonymized data consistent with docs 15-19"
    - "The phase's final output includes the full raw JSON of all 6 corrected docs (not a summary) for Juan's manual review"
  artifacts:
    - path: "case-studies collection docs 18, 19, 20 (Postgres content, not files)"
      provides: "Complete bilingual content, labeled KPIs/metrics, GSC-backed results, doc 20 anonymized"
  key_links:
    - from: "doc 20 anonymization"
      to: "title/heroSubtitle/clientContext/meta/client fields"
      via: "mcp__juan-payload__updateCaseStudies + mcp__juan-payload__updateClientes writes stripping real firm identity"
      pattern: "Pittsburgh"
    - from: "final verification task"
      to: "all 6 docs' raw JSON"
      via: "mcp__juan-payload__findCaseStudies({ where: { id: { in: [15,16,17,18,19,20] } }, locale: 'all', depth: 1 })"
      pattern: "findCaseStudies"
---

<objective>
Finish the 6-doc content fix (docs 18, 19), then anonymize doc 20 — the Pittsburgh criminal defense case study currently exposing a real client's identity across multiple fields — and close the phase by re-fetching and returning the full raw JSON of all 6 corrected docs for Juan's manual review.

Purpose: CASE-03 (anonymization) is the most content-sensitive requirement in this phase; it runs last, after the property-mapping/audit groundwork from Plan 37-03, and after the "how we fix a doc" pattern has already been proven on 3 other docs. CASE-05 (return full raw JSON, not a summary) is this phase's explicit closing gate — a prior attempt in this project declared work "verified" without actually being verified, so this plan's last task is a hard requirement, not optional polish.
Output: Docs 18/19 fully corrected; doc 20 fully anonymized with real (but anonymized) GSC-backed metrics; the complete raw JSON of docs 15-20 returned in this plan's execution output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-case-studies-content-audit-fix/37-CONTEXT.md
@.planning/phases/37-case-studies-content-audit-fix/37-PATTERNS.md
@.planning/phases/37-case-studies-content-audit-fix/37-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Same MCP call shapes as Plan 37-03 — repeated here so this plan is self-contained. -->

Read: `mcp__juan-payload__findCaseStudies({ where: { id: { in: [15,16,17,18,19,20] } }, locale: 'all', depth: 1 })` — `locale: 'all'` returns every localized field as `{ en, es }`.

Write (one locale per call for localized fields): `mcp__juan-payload__updateCaseStudies({ id: <doc-id>, data: {...}, locale: 'en' | 'es' })`.

`mcp__juan-payload__findClientes` / `mcp__juan-payload__updateClientes` — both `find`/`update` are live (confirmed against `payload.config.ts`'s `mcpPlugin` config during planning) for the `client` relationship, if doc 20 has one set.

Field shapes (from `src/collections/CaseStudies/index.ts`, already read in full during planning): `challenge[]: { text }[]` (localized), `solution[]: { title, description }[]` (localized), `kpis[]: { label, value }[]` (label localized, value not), `results.metrics[]: { label, before, after }[]` (label localized, before/after not). `title`/`heroSubtitle`/`sector` are `localized: true` text fields directly on the doc. `clientContext` is a `localized: true` richText field. `meta` (title/description/ogImage) comes from `@payloadcms/plugin-seo`'s tabbed UI, registered for `case-studies` — check its contents too for real client leakage (per Success Criterion 3's explicit "incluidos metadata/clientContext").

This plan depends on Plan 37-03's SUMMARY for: (a) the GSC property mapped to docs 18/19, and (b) the full gap audit of all 6 docs from 37-03's Task 1 — read `.planning/phases/37-case-studies-content-audit-fix/37-03-SUMMARY.md` before starting Task 1 below.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix docs 18 and 19 (challenge/solution/kpis/results.metrics + real GSC data)</name>
  <files>none (MCP writes only)</files>
  <action>
    Read `37-03-SUMMARY.md` first to get the GSC property mapping and gap audit for docs 18/19 (already decided in Plan 37-03 — do not re-discover).

    For doc 18, then doc 19 (continue to the next doc if one fails):
    1. Fill any empty `challenge[]`/`solution[]` entries in BOTH `en` and `es` locales, grounded in the doc's existing real fields.
    2. Add a `label` to every `kpis[]`/`results.metrics[]` entry currently missing one, in both locales.
    3. Query the GSC property mapped to this doc (from 37-03's SUMMARY) for real clicks/impressions/position/CTR data, and populate `results.metrics[]` with multiple real rows (minimum: clicks, impressions, position), each with `before`/`after` and a visible `label`. No invented numbers, no real property domain/branding written into any field.
  </action>
  <verify>
    <automated>echo "manual: re-fetch docs 18,19 via findCaseStudies locale:'all' and confirm challenge/solution non-empty in both locales, all kpis/metrics have labels, metrics.length > 2"</automated>
  </verify>
  <done>Docs 18 and 19 have non-empty bilingual challenge/solution, every KPI/metric row has a label, and results.metrics has 3+ real GSC-sourced rows each with before/after values.</done>
</task>

<task type="auto">
  <name>Task 2: Anonymize doc 20 (Pittsburgh criminal defense) + real GSC data</name>
  <files>none (MCP writes only)</files>
  <read_first>
    37-03-SUMMARY.md's audit of doc 20 (which fields currently contain real client identity, and whether a `client` relationship doc is linked)
  </read_first>
  <action>
    Fetch doc 20 fresh (`locale: 'all'`, `depth: 1`) and check every field for real firm name, real domain, real county name, or real review count — not just the fields already flagged in 37-03's audit, re-verify directly:
    - `title`, `heroSubtitle` (both locales)
    - `clientContext` (richText, both locales) — read the actual rendered/raw text, not just presence
    - `meta.title`/`meta.description` (both locales, via the seo plugin's tabbed fields on this doc)
    - `sector`, any free-text field mentioning "Pittsburgh"/county name/firm name/review count
    - the `client` relationship, if set: fetch the linked `clientes` doc via `findClientes`. Before editing it, check whether any OTHER case-studies doc references the same `clientes` id (query `findCaseStudies` for `client: { equals: <that-id> }`) — if doc 20 is the ONLY doc using that Clientes record, anonymize its `name`/`websiteUrl` fields in place via `updateClientes`; if it's shared with another doc, instead clear/unset the `client` relationship on doc 20 (do not risk deanonymizing a different doc by editing a shared Clientes record).

    Replace every real-identity value found with an anonymized-but-consistent value matching the pattern already established across docs 15-19 (generic sector-appropriate framing, e.g. "a regional criminal defense firm" rather than any real name/domain/county). Write via `updateCaseStudies` (per-locale for localized fields) and `updateClientes` if applicable.

    Then, exactly as in Task 1/Plan 37-03: fill any empty `challenge`/`solution` in both locales, add missing `label`s to `kpis[]`/`results.metrics[]`, and populate `results.metrics[]` with multiple real rows sourced from the already-confirmed real property `www.pittsburghcriminalattorney.com` via the `gsc-javier-*` MCP tools (real clicks/impressions/position data) — same constraint as every other doc: real numbers only, and critically for THIS doc, never write the actual property domain string into any doc field (only the aggregate numbers), since that would reintroduce the exact identity leak this task exists to fix.

    After writing, re-fetch doc 20 one more time and grep/scan the full JSON response yourself for "pittsburgh" (case-insensitive), the real firm name (from the pre-fix audit), and any county name — confirm zero matches before marking this task done.
  </action>
  <verify>
    <automated>echo "manual: re-fetch doc 20 via findCaseStudies locale:'all', grep the full JSON response for the real firm name / 'pittsburgh' / county name / real review count, confirm zero matches; confirm challenge/solution/kpis/metrics are complete with labels"</automated>
  </verify>
  <done>Doc 20 contains zero occurrences of the real firm name, domain, county, or review count in any field (verified by re-fetching and scanning the raw JSON), has complete bilingual challenge/solution, labeled KPIs/metrics, and 3+ real GSC-sourced results.metrics rows.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification — return full raw JSON of all 6 corrected docs (CASE-05)</name>
  <files>none (read-only)</files>
  <action>
    Re-fetch all 6 docs in a single call: `mcp__juan-payload__findCaseStudies({ where: { id: { in: [15,16,17,18,19,20] } }, locale: 'all', depth: 1 })`.

    Spot-check the code fixes from Plan 37-01/37-02 against this now-fully-populated content (this is the point where the deferred CASE-10 mobile check from Plan 37-02 Task 2 becomes meaningful): with the dev server running, visit at least 2 of the 6 case-study detail pages at a 375px viewport and confirm the results chart renders correctly with the now-multi-row, potentially mixed-magnitude data (dual-axis if magnitudes diverge) — no overflow, no clipped bars/labels. Also confirm the closing CTA section and single AuthorCard render correctly on these fully-populated docs, and that the JSON-LD `additionalProperty` reflects each doc's real (now-complete) kpis.

    Output the COMPLETE raw JSON response for all 6 docs (not a summary, not an excerpt, not "looks good") directly in this task's final message/output — this is the literal deliverable Success Criterion 5 requires, since a prior attempt in this project declared work "verified" without actually returning verifiable evidence. Do not compress, truncate, or paraphrase the JSON.
  </action>
  <verify>
    <automated>echo "manual: the findCaseStudies response for all 6 docs is reproduced in full in this task's output, unaltered and untruncated"</automated>
  </verify>
  <done>The full raw JSON of docs 15-20 (post-fix) is returned in this task's output for Juan's manual review; a live 375px check on at least 2 fully-populated docs confirms the chart/CTA/author/JSON-LD fixes from Plan 37-01/37-02 hold up against real multi-row content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Executor session -> `juan-payload` MCP server -> production Neon Postgres | Content writes to real production data, including a content-sensitive anonymization edit (removing real client PII from doc 20) |
| Executor session -> `gsc-javier-*` MCP server -> Google Search Console API | Read-only real data pull for the already-confirmed `www.pittsburghcriminalattorney.com` property |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-37-07 | Information Disclosure | Doc 20's real client PII (name/domain/county/review count) currently exposed | mitigate | Task 2 is this phase's direct fix — full-field re-audit (not just the fields flagged in Plan 37-03), post-write grep-for-zero-matches verification before marking done |
| T-37-08 | Information Disclosure | Doc 20's `clientes` relationship record, if shared with another case study | mitigate | Task 2 explicitly checks for shared usage before editing a linked `clientes` doc — clears the relationship instead of editing in place if shared, to avoid deanonymizing a different doc |
| T-37-09 | Information Disclosure | Real GSC property domain (`pittsburghcriminalattorney.com`) leaking into doc 20's own anonymized copy | mitigate | Task 2 explicitly forbids writing the real property domain string into any doc field — only aggregate numeric metrics are written |
| T-37-10 | Repudiation | Phase closes without verifiable evidence (repeat of the prior "declared verified without being verified" incident referenced in CONTEXT.md/ROADMAP.md) | mitigate | Task 3 requires the full untruncated raw JSON of all 6 docs in this plan's own output — not a summary, not deferred to a separate verification step that might be skipped |
</threat_model>

<verification>
1. `findCaseStudies` re-fetch of docs 18, 19 shows non-empty bilingual challenge/solution, labeled kpis/metrics, 3+ metrics rows each
2. `findCaseStudies` re-fetch of doc 20, scanned for the real firm name/"pittsburgh"/county name/real review count, returns zero matches across all fields
3. `findCaseStudies` re-fetch of all 6 docs (15-20) is reproduced in full, untruncated, in Task 3's output
4. A live 375px check on 2+ fully-populated docs confirms chart/CTA/author/JSON-LD fixes hold against real content
</verification>

<success_criteria>
- All 9 ROADMAP Phase 37 success criteria are satisfiable from this phase's combined output (37-01 through 37-04)
- Doc 20 has zero real client-identifying data in any field, confirmed by direct re-fetch and scan
- The phase closes with the full raw JSON of all 6 corrected docs visible in this plan's execution output, per CASE-05
</success_criteria>

<output>
Create `.planning/phases/37-case-studies-content-audit-fix/37-04-SUMMARY.md` when done — MUST include (or directly reference/reproduce) the full raw JSON of all 6 docs from Task 3.
</output>
