---
phase: 11-verificacion-cruzada-final
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - package.json
  - scripts/lighthouse-mobile.mjs
  - .planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md
autonomous: false
requirements: [UI-14]

must_haves:
  truths:
    - "A mobile Lighthouse run (Performance/Accessibility/Best-Practices/SEO) against a LOCAL PRODUCTION BUILD (npm run build && npm run start, never next dev) of current HEAD exists and is documented."
    - "A comparable mobile Lighthouse run against a LOCAL PRODUCTION BUILD of the pre-milestone commit (immediately before Phase 7 started) exists, serving as the best-available proxy for the 'production baseline' ROADMAP criterion #4 references — since no actual production deployment exists yet (Phase 6 is paused)."
    - "The baseline-substitution adjustment (local pre-milestone build standing in for a real production baseline) is documented explicitly in the report, not silently redefined."
    - "No category score in the current-HEAD run regresses meaningfully versus the pre-milestone baseline run; any genuine regression found is fixed before this plan closes."
  artifacts:
    - path: "scripts/lighthouse-mobile.mjs"
      provides: "Reusable script: builds+starts a local production server, runs mobile-preset Lighthouse via the Node API against a fixed route list, writes category scores to JSON"
    - path: ".planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md"
      provides: "Baseline vs current score comparison table + explicit documentation of the production-baseline substitution"
  key_links:
    - from: "scripts/lighthouse-mobile.mjs"
      to: "local Chrome binary (via @puppeteer/browsers)"
      via: "chrome-launcher with an explicit chromePath"
      pattern: "chromePath|executablePath"
    - from: "scripts/lighthouse-mobile.mjs"
      to: "npm run build && npm run start"
      via: "child_process spawn of the production server before each Lighthouse run"
      pattern: "spawn.*(build|start)"
---

<objective>
Run mobile Lighthouse (Performance/Accessibility/Best-Practices/SEO) against a LOCAL PRODUCTION BUILD of current HEAD, compare it against an equivalent local production build of the commit immediately before Phase 7 started, and document the result. This is the final, heaviest check of the phase — it mutates the working tree temporarily (git checkout to capture the baseline) so it runs in its own wave, after Plans 11-01/11-02 have finished reading/grepping the repo, to avoid a concurrent-checkout conflict on the shared git working tree.

Purpose: ROADMAP success criterion #4 says "sin regresión respecto al baseline de producción" — but Phase 6 (deploy) is paused and no production URL exists yet. Per 11-CONTEXT.md's explicit, pre-approved adjustment: use a LOCAL production build (`npm run build && npm run start`, never `next dev` — dev-mode Lighthouse scores are not representative) of the commit before Phase 7 started as the best-available baseline proxy, and document this substitution explicitly rather than silently redefining the criterion.
Output: `scripts/lighthouse-mobile.mjs` (reusable local-prod-build Lighthouse runner) + `.planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md` documenting both runs, the score comparison, and the baseline-substitution note. Any genuine CWV/score regression found gets fixed in this plan per 11-CONTEXT.md's scope rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-verificacion-cruzada-final/11-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Environment facts confirmed during planning, do not re-discover: -->

- No system Chrome/Chromium is installed in this environment (`/Applications/Google Chrome.app` absent, no `chromium`/`google-chrome` on PATH). `npx --yes @puppeteer/browsers install chrome@stable --path <dir>` successfully downloads a working "Google Chrome for Testing" binary (confirmed working during planning — do this once, cache the path, do not re-download per run).
- `lighthouse` and `chrome-launcher` are NOT currently in package.json — no Package Legitimacy Audit table exists for Phase 11 in RESEARCH.md (this need was identified during planning, after research concluded). Per the Package Legitimacy Gate's fallback policy, treat `lighthouse`, `chrome-launcher`, and `@puppeteer/browsers` as `[ASSUMED]` and gate the install behind a blocking human-verify checkpoint (Task 1 below), consistent with how Phase 3 gated the `cloudinary` package.
- Pre-milestone baseline commit: `0812dc4^` (i.e. commit `4be20f5`, "docs: create milestone v1.1 roadmap") — the last commit before Phase 7's context/plan commits, itself a docs-only commit sitting on the same code as Phase 5's close. Checking this out gives the exact pre-Phase-7 application code state.
- `package.json` scripts already present: `"build": "payload migrate && payload generate:importmap && payload generate:types && next build"`, `"start": "next start"`. These work as-is for both the baseline commit and HEAD (schema is confirmed zero-diff across the whole range by Plan 11-01, so `payload migrate` is a no-op either way).
- Current branch is `master`, working tree is clean at plan-time (HEAD `2784cf4`) — but re-verify with `git status --short` immediately before checkout, since Plans 11-01/11-02 may have left commits or working-tree changes.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm lighthouse/chrome-launcher/@puppeteer/browsers package legitimacy before install</name>
  <files></files>
  <what-built>Nothing yet — this gate runs BEFORE installing `lighthouse`, `chrome-launcher`, and `@puppeteer/browsers`, since no Package Legitimacy Audit table exists for these packages in this milestone's RESEARCH.md (fallback: treat as `[ASSUMED]`).</what-built>
  <how-to-verify>
Confirm on the npm registry (npmjs.com) for each of `lighthouse`, `chrome-launcher`, `@puppeteer/browsers`:
1. Publisher is the expected official org (`lighthouse`/`chrome-launcher` -> GoogleChrome org; `@puppeteer/browsers` -> puppeteer/Google org)
2. Weekly download count is high (millions/week for `lighthouse`, hundreds of thousands/week for the others) — these are all extremely well-known Google-maintained tooling packages, not obscure/typosquat risks
3. No recent suspicious version-history anomalies (e.g. a brand-new major version published hours ago by a different maintainer)
  </how-to-verify>
  <action>
Present the npm registry stats (publisher org, weekly downloads, latest version/publish date) for `lighthouse`, `chrome-launcher`, and `@puppeteer/browsers` to Juan and wait for explicit confirmation before installing them as devDependencies.
  </action>
  <verify>
    <automated>npm view lighthouse chrome-launcher @puppeteer/browsers version 2>&1 | grep -c '.'</automated>
  </verify>
  <done>Juan has typed "approved" confirming the three packages' legitimacy per the checkpoint verification steps above.</done>
  <resume-signal>Type "approved" to proceed with installing these three devDependencies, or state a concern</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the local-prod-build mobile Lighthouse runner</name>
  <files>package.json, scripts/lighthouse-mobile.mjs</files>
  <action>
Install `lighthouse`, `chrome-launcher`, and `@puppeteer/browsers` as devDependencies (`npm install --save-dev lighthouse chrome-launcher @puppeteer/browsers`).

Write `scripts/lighthouse-mobile.mjs` (ESM, Node script) that:
1. Locates or downloads a local Chrome binary via `@puppeteer/browsers`' `install()`/`computeExecutablePath()` API into a fixed cache dir (e.g. `.lighthouse-chrome/` — add this dir to `.gitignore`), so repeat runs reuse the download instead of re-fetching.
2. Launches that Chrome binary via `chrome-launcher`'s `launch({ chromePath, chromeFlags: ['--headless=new', '--no-sandbox'] })`.
3. Runs `lighthouse` (Node API, not CLI) with the mobile form-factor preset (`formFactor: 'mobile'`, standard throttling — the default `lighthouse` config already applies mobile throttling/emulation when `formFactor: 'mobile'` is set; do not disable throttling) against a fixed list of routes read from a `ROUTES` constant at the top of the file: `/en`, `/es`, `/en/blog`, `/en/blog/{longest-real-slug-from-Phase-10}` (reuse post id 53 or 66's slug, confirmed 75-char ES title precedent), `/en/case-studies`.
4. For each route, extracts `categories.performance.score`, `categories.accessibility.score`, `categories['best-practices'].score`, `categories.seo.score` (0-1, multiply by 100 for the familiar 0-100 scale) and writes them to a JSON file passed via a CLI arg (e.g. `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3000 --out ./lh-current.json`).
5. Does NOT itself start/stop the Next.js server — that's the caller's job (Task 3), so this script stays a pure "given a running base URL, run Lighthouse and write scores" utility, reusable for both the baseline and current runs.

Smoke-test the script once against a server you start manually (`npm run build && npm run start` in one terminal, then run the script against `http://localhost:3000` in another) to confirm it produces a valid JSON output with all 4 categories populated for at least the `/en` route before moving to Task 3. Stop the manual server afterward.
  </action>
  <verify>
    <automated>node scripts/lighthouse-mobile.mjs --base-url http://localhost:3000 --out /tmp/lh-smoke-test.json --routes-only /en</automated>
  </verify>
  <done>Script exists, runs against a live local server, and produces valid JSON with performance/accessibility/best-practices/seo scores for at least one route.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Capture pre-milestone baseline, run current-HEAD comparison, document and fix regressions</name>
  <files>.planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md</files>
  <action>
Sequence carefully, checking `git status --short` before any checkout and stashing (`git stash -u`) if anything is uncommitted:

1. Record current ref: `git rev-parse HEAD` (should be the tip of Plan 11-01/11-02's work).
2. Checkout the pre-milestone baseline: `git checkout 0812dc4^` (detached HEAD is fine — this is a read-only build-and-measure step, no commits made here).
3. `npm install` if needed (dependency set should be identical minus the 3 new lighthouse-related devDeps — those aren't needed for the app build itself, only for running the script from Task 2, which stays on disk since it's an untracked-by-that-old-commit file you keep from the current working directory... actually since checkout will remove `scripts/lighthouse-mobile.mjs` if it wasn't committed yet at that point in history: copy the script to a temp location OUTSIDE the repo (e.g. the scratchpad or `/tmp`) BEFORE checking out, then invoke it from that temp path against whichever code is currently checked out).
4. `npm run build && npm run start &` (background), wait for the server to be ready (poll `http://localhost:3000` until 200), run the Task-2 script against it with `--out` pointing to a baseline JSON file (e.g. `.planning/phases/11-verificacion-cruzada-final/lh-baseline.json`), then stop the server.
5. Checkout back: `git checkout master` (or the original ref recorded in step 1), restore the stash if one was created.
6. `npm install` again if the lockfile differs, then `npm run build && npm run start &`, wait for ready, run the Task-2 script against current HEAD with `--out` pointing to a current-state JSON file (e.g. `lh-current.json`), stop the server.
7. Compare baseline vs current per route per category. Write the full comparison table into `.planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md`, explicitly stating: "Production baseline substitution: no live production deployment exists (Phase 6 paused per 11-CONTEXT.md) — this report uses a local production build (`npm run build && npm run start`) of commit `4be20f5` (immediately pre-Phase-7) as the best-available baseline proxy, compared against an equivalent local production build of the current milestone-close state."
8. If any category score for any route regresses by a meaningful margin (more than a few points, or any category dropping out of the "good" band it was previously in — e.g. Performance dropping below 90 when baseline was 90+), investigate the cause (check for newly-added CSS transitions/JS, image handling changes, font additions per Pitfall 4) and fix it before closing this plan. Re-run the current-HEAD Lighthouse pass after any fix to confirm the regression is resolved.
9. Confirm you end on the original branch/ref (not left in detached HEAD) before finishing.
  </action>
  <verify>
    <automated>git branch --show-current | grep -q master</automated>
  </verify>
  <done>Baseline and current-HEAD Lighthouse scores are both captured and documented, the production-baseline substitution is explicitly noted, no unresolved regression remains, and the repo is back on the original branch with a clean working tree (aside from the new report/script files).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> local devDependency install | New packages (`lighthouse`, `chrome-launcher`, `@puppeteer/browsers`) installed without a RESEARCH.md legitimacy audit table for this phase. |
| Local headless Chrome download (`@puppeteer/browsers`) -> local disk | Binary downloaded from Google's official Chrome-for-Testing distribution channel, not an arbitrary third-party mirror. |
| git working tree checkout (baseline capture) -> local repo state | Temporary detached-HEAD checkout to measure a historical commit; must not leave the repo in a dirty/detached state on completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-11-SC | Tampering | `npm install --save-dev lighthouse chrome-launcher @puppeteer/browsers` | mitigate | Blocking `checkpoint:human-verify` (Task 1) confirms publisher/downloads on npmjs.com before install, per Package Legitimacy Gate fallback policy (no RESEARCH.md audit table exists for this phase's newly-identified packages). |
| T-11-04 | Tampering | `@puppeteer/browsers install chrome@stable` binary download | accept | Downloads from Google's official Chrome-for-Testing distribution (the same channel Puppeteer itself uses); confirmed working during planning; not an arbitrary third-party binary source. |
| T-11-05 | Repudiation | git detached-HEAD checkout during baseline capture | mitigate | Task 3 explicitly records the starting ref before checkout and verifies return to `master` with a clean tree as its `<done>` criterion, preventing an accidental permanent detached-HEAD state. |
</threat_model>

<verification>
- `scripts/lighthouse-mobile.mjs` produces valid Lighthouse category scores against a live local production server.
- Both baseline (pre-Phase-7) and current-HEAD local production builds have been measured.
- `.planning/phases/11-verificacion-cruzada-final/11-03-lighthouse-report.md` documents the comparison and explicitly states the production-baseline substitution.
- Repo ends on `master` (or original branch) with a clean working tree.
- No unresolved score regression remains.
</verification>

<success_criteria>
Mobile Lighthouse scores for current HEAD (local production build) show no meaningful regression versus the pre-milestone local production baseline, across Performance/Accessibility/Best-Practices/SEO, with the production-baseline substitution explicitly documented per 11-CONTEXT.md.
</success_criteria>

<output>
Create `.planning/phases/11-verificacion-cruzada-final/11-03-SUMMARY.md` when done
</output>
