---
phase: 01-schema-foundation
plan: 10
type: execute
wave: 6
depends_on: ["01-09"]
files_modified:
  - .env
  - src/migrations/*.ts
  - src/migrations/*.json
  - src/migrations/index.ts
autonomous: false
requirements: [SCHEMA-01, SCHEMA-03]
user_setup:
  - service: neon
    why: "PostgreSQL dev database for payload migrate:create/migrate — the connection string this project runs all schema migrations against"
    env_vars:
      - name: DATABASE_URI
        source: "Neon Dashboard -> Project -> Connection Details -> select the UNPOOLED/direct connection string (no '-pooler' in the hostname). The pooled string breaks payload migrate:create/migrate prepared statements."
      - name: PAYLOAD_SECRET
        source: "Generate locally via `openssl rand -base64 32` — no external dashboard needed"
    dashboard_config:
      - task: "Create a Neon project/branch if one doesn't exist yet, and copy the UNPOOLED connection string specifically (Neon's dashboard shows both pooled and unpooled per branch — do not copy the one with '-pooler' in the hostname)"
        location: "Neon Dashboard -> Connection Details"

must_haves:
  truths:
    - "A schema change is applied to Postgres EXCLUSIVELY via payload migrate:create + payload migrate — no push:true, no manual SQL, no admin auto-sync"
    - "The generated migration file (SQL DDL) is committed to git, reviewable as a diff"
    - "Running `payload migrate` against a fresh Postgres database produces tables for exactly the 9 KEEP-list collections, matching payload.config.ts"
  artifacts:
    - path: "src/migrations/<timestamp>_initial.ts"
      provides: "the initial schema migration, generated by payload migrate:create, committed to git"
    - path: "src/migrations/index.ts"
      provides: "migration index auto-generated by the CLI, imported by payload.config.ts's migration runner"
  key_links:
    - from: ".env"
      to: "DATABASE_URI (Neon UNPOOLED connection string)"
      via: "postgresAdapter pool.connectionString"
      pattern: "DATABASE_URI="
---

<objective>
[BLOCKING] Apply the full 9-collection schema to a real Neon PostgreSQL database by running `payload migrate:create` (generates the initial SQL migration from `payload.config.ts`) followed by `payload migrate` (applies it) — using the Neon UNPOOLED/direct connection string, never the pooled `-pooler` string, which breaks migration prepared statements per RESEARCH.md Pitfall 1.

Purpose: This is the mandatory schema-push task per the phase's blocking requirement. Build and type checks (Waves 1-5) pass without ever touching a real database — types come from `payload.config.ts`, not the live schema. Without this task, the phase would show a false-positive "complete" state while zero tables exist in Postgres. SCHEMA-01 and SCHEMA-03 are unverifiable without this task actually running against Neon.
Output: A committed migration file under `src/migrations/`, and a live Neon Postgres database with all 9 collections' tables created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-schema-foundation/01-RESEARCH.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm Neon UNPOOLED DATABASE_URI is available</name>
  <files>.env</files>
  <what-built>Nothing yet — this checkpoint exists because Claude cannot create a Neon account or provision a database on Juan's behalf, and using the wrong (pooled) connection string silently corrupts the migration workflow per RESEARCH.md Pitfall 1.</what-built>
  <action>Check `.env` first — it may already have been provisioned with a real `DATABASE_URI` before planning started. Run `test -f .env && grep -c "^DATABASE_URI=" .env` to check for presence, and `grep "^DATABASE_URI=" .env | grep -c pooler` to confirm it is the UNPOOLED variant (must be 0). Do NOT print, log, or echo the actual value — only check for presence/pattern via grep exit codes and counts. If `DATABASE_URI` is already present and non-pooler, leave `.env` untouched and proceed directly to Task 2. Only if `DATABASE_URI` is missing or does contain `-pooler`, ask Juan to provide the Neon UNPOOLED/direct connection string (no `-pooler` in the hostname) — do not fabricate or guess one — and once provided, write it into `.env`.</action>
  <how-to-verify>
    1. Open the Neon Dashboard for this project's Postgres branch (create one if it doesn't exist yet).
    2. Go to Connection Details.
    3. Copy the connection string variant WITHOUT `-pooler` in the hostname (the "unpooled" or "direct" connection string — Neon shows both).
    4. Confirm the string looks like `postgresql://<user>:<password>@<endpoint>.<region>.aws.neon.tech/<db>?sslmode=require` — NOT `...-pooler.<region>.aws.neon.tech...`.
  </how-to-verify>
  <verify>
    <human-check>Juan confirms the DATABASE_URI in .env does not contain "-pooler" in the hostname (if `.env` was already provisioned pre-planning, this is a quick confirmation, not a request for the value)</human-check>
  </verify>
  <done>.env contains a DATABASE_URI value without "-pooler" in the hostname (either pre-existing and left untouched, or newly provided by Juan), confirmed before Task 2 proceeds.</done>
  <resume-signal>If `.env` already has a valid UNPOOLED DATABASE_URI, reply "confirmed" to proceed without any value being pasted. Otherwise, paste the UNPOOLED DATABASE_URI to continue — Claude will write it to .env (gitignored) and proceed with the migration.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Generate and apply the initial migration against Neon Postgres</name>
  <files>.env, src/migrations/*.ts, src/migrations/*.json, src/migrations/index.ts</files>
  <read_first>
    - .planning/phases/01-schema-foundation/01-RESEARCH.md (section "Migration workflow (dev -> commit -> deploy)", "Pitfall 1: Using Neon's pooled connection string for migrations", "Pitfall 3: Forgetting push: false")
    - src/payload.config.ts
    - .env.example
  </read_first>
  <action>
If Task 1 determined `.env` already had a valid UNPOOLED `DATABASE_URI`, leave `.env` untouched here — do not rewrite it. Only write to `.env` if Task 1 collected a new value from Juan. Confirm `.env` is NOT tracked by git: `git check-ignore .env` must succeed.

Run `CI=true PAYLOAD_MIGRATING=true npx payload migrate:create initial` — this introspects `payload.config.ts`'s 9 collections and generates `src/migrations/<timestamp>_initial.ts` (+ matching `.json`) containing the DDL for: `users`, `media`, `pages`, `posts`, `authors`, `categories`, `case-studies`, `testimonials`, `clientes` tables (plus Payload's internal tables: `payload_locked_documents`, `payload_preferences`, `payload_migrations`). If the CLI prompts interactively and hangs in this non-TTY environment, the `CI=true PAYLOAD_MIGRATING=true` env prefix is the documented workaround (per the phase's schema-push requirement) — do not pipe `yes`/force through any other mechanism.

Open the generated `src/migrations/<timestamp>_initial.ts` file and read the SQL DDL inside — confirm it creates tables matching all 9 collections and contains NO reference to any DROP-listed collection (`works`, `ad_banners`, `keyword_metrics`, `page_metrics`, `gsc_metrics`, `broken_links`).

Run `npx payload migrate` to apply the generated migration against the live Neon database.

Commit the migration files (`src/migrations/<timestamp>_initial.ts`, matching `.json`, and `src/migrations/index.ts`) to git — do NOT commit `.env`.
  </action>
  <verify>
    <automated>Run the shell checks listed in the acceptance_criteria block below; all must pass</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .env` exits 0 (confirms .env is gitignored, never staged)
    - `ls src/migrations/*_initial.ts` returns exactly one file
    - `grep -c "works\|ad_banners\|keyword_metrics\|page_metrics\|gsc_metrics\|broken_links" src/migrations/*_initial.ts` == 0
    - `grep -cE "case_studies|case-studies" src/migrations/*_initial.ts` >= 1
    - `grep -cE "clientes" src/migrations/*_initial.ts` >= 1
    - `npx payload migrate:status 2>&1 | grep -c "initial"` >= 1 (confirms the migration is recorded as applied against the live DB)
    - `git status --porcelain src/migrations/` shows the new migration files as staged/tracked (not gitignored, not untracked-and-forgotten)
  </acceptance_criteria>
  <done>The initial migration is generated from the real 9-collection payload.config.ts, applied successfully against the Neon UNPOOLED database (payload migrate:status confirms it's recorded as run), the migration file is committed to git, and .env remains untracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local `.env` → Neon Postgres | `DATABASE_URI` carries live database credentials over the network (TLS via `sslmode=require`, Neon default) |
| `payload migrate` execution → production schema discipline | This is the ONLY sanctioned path for schema changes to ever reach Postgres in this project, at any point in its lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-15 | Information Disclosure | `DATABASE_URI` with live Neon credentials | mitigate | Written only to local `.env` (gitignored, verified via `git check-ignore` in acceptance criteria), never passed as an inline shell argument in any committed script, never logged |
| T-01-16 | Tampering | Using Neon's pooled (`-pooler`) connection string for migrations, corrupting prepared-statement state | mitigate | Task 1 checkpoint explicitly requires the UNPOOLED string, verified by Juan before Task 2 proceeds; RESEARCH.md Pitfall 1 documents this as a hard requirement with no fallback |
| T-01-SC | Tampering | npm/pip/cargo installs (package legitimacy gate) | mitigate | No new packages installed in this task — `payload` CLI was already installed and audited in 01-01 (Package Legitimacy Audit: all packages Approved, no `[ASSUMED]`/`[SUS]` entries); no blocking checkpoint needed here since no new install occurs |
</threat_model>

<verification>
`payload migrate:status` shows the initial migration as applied. The migration file's DDL matches exactly the 9 KEEP-list collections with zero DROP-listed table references. `.env` is confirmed gitignored via `git check-ignore`.
</verification>

<success_criteria>
The full Phase 1 schema exists in a live Neon PostgreSQL database, reached exclusively via `payload migrate:create`/`payload migrate` (SCHEMA-01, SCHEMA-03), with the migration file committed to git as the sole source of schema truth — no `push:true` was ever used.
</success_criteria>

<output>
Create `.planning/phases/01-schema-foundation/01-10-SUMMARY.md` when done
</output>
