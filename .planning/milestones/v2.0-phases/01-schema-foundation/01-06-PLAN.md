---
phase: 01-schema-foundation
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/blocks/TestimonialsCarousel/config.ts
  - src/blocks/ContactFormBlock/config.ts
  - src/blocks/Code/config.ts
  - src/blocks/RelatedPosts/config.ts
  - src/blocks/TableOfContentsBlock/config.ts
  - src/blocks/ResultsSection/config.ts
  - src/blocks/Section/config.ts
autonomous: true
requirements: [SCHEMA-06]

must_haves:
  truths:
    - "The ContactFormBlock is a simple hardcoded-field block (eyebrow/title/description/submitLabel/contactInfo), not built on plugin-form-builder"
    - "The Code block supports language selection for syntax-highlighted technical posts (Juan-confirmed requirement)"
    - "The Section wrapper's nested blocks array references only the new consolidated block set, not the old ~35-block list"
  artifacts:
    - path: "src/blocks/TestimonialsCarousel/config.ts"
      provides: "testimonials carousel block"
      exports: ["TestimonialsCarousel"]
    - path: "src/blocks/ContactFormBlock/config.ts"
      provides: "hardcoded contact form block (no form-builder plugin)"
      exports: ["ContactFormBlock"]
    - path: "src/blocks/Code/config.ts"
      provides: "syntax-highlighted code block"
      exports: ["Code"]
    - path: "src/blocks/RelatedPosts/config.ts"
      provides: "related-posts block"
      exports: ["RelatedPosts"]
    - path: "src/blocks/TableOfContentsBlock/config.ts"
      provides: "table-of-contents block for long posts"
      exports: ["TableOfContentsBlock"]
    - path: "src/blocks/ResultsSection/config.ts"
      provides: "stats/results display block"
      exports: ["ResultsSection"]
    - path: "src/blocks/Section/config.ts"
      provides: "generic layout wrapper block with nested consolidated blocks"
      exports: ["Section"]
  key_links:
    - from: "src/blocks/Section/config.ts"
      to: "consolidated block set (CallToAction/Content/MediaBlock/ArchiveBlock)"
      via: "nested blocks field"
      pattern: "blocks:\\s*\\["
---

<objective>
Create the remaining 7 of the ~12-14 consolidated block configs: `TestimonialsCarousel`, `ContactFormBlock`, `Code`, `RelatedPosts`, `TableOfContentsBlock`, `ResultsSection`, `Section`.

Purpose: Completes SCHEMA-06's consolidated block library. `ContactFormBlock` and `Code`/`TableOfContentsBlock` are explicitly Juan-confirmed inclusions per CONTEXT.md. `Section` is the generic layout wrapper whose nested `blocks` array must be updated to reference only the new consolidated set.
Output: 7 block config files, ready to be registered on `Pages.layout` in Wave 3, completing the full block library alongside the 6 created in 01-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-schema-foundation/01-RESEARCH.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
@.planning/phases/01-schema-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create TestimonialsCarousel, ContactFormBlock, Code, RelatedPosts configs</name>
  <files>src/blocks/TestimonialsCarousel/config.ts, src/blocks/ContactFormBlock/config.ts, src/blocks/Code/config.ts, src/blocks/RelatedPosts/config.ts</files>
  <read_first>
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/TestimonialsCarousel/config.ts
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/ContactFormBlock/config.ts
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/Code/config.ts
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/RelatedPostsBlock/config.ts
  </read_first>
  <action>
Create `src/blocks/TestimonialsCarousel/config.ts` exporting `TestimonialsCarousel: Block` with `slug: 'testimonialsCarousel'` — port verbatim from the JuanPortfolio analog (35 lines): `title` field, `showRating` checkbox, `limit` number. Pulls from the `testimonials` collection at render time (Phase 5) — no config-side relationship field needed here.

Create `src/blocks/ContactFormBlock/config.ts` exporting `ContactFormBlock: Block` with `slug: 'contactFormBlock'` — port verbatim from the JuanPortfolio analog (116 lines): `eyebrow`/`title`/`description`/`submitLabel` text fields + `contactInfo` array with an `icon` select. This already matches CONTEXT.md's "ContactFormBlock simple, sin plugin-form-builder" decision precisely — do not add any `@payloadcms/plugin-form-builder` dependency or generic `Form` block wiring.

Create `src/blocks/Code/config.ts` exporting `Code: Block` with `slug: 'code'` — port verbatim from the JuanPortfolio analog (33 lines): `language` select field + `code` field type. This block is CONFIRMED by Juan for technical posts with syntax highlighting per CONTEXT.md.

Create `src/blocks/RelatedPosts/config.ts` exporting `RelatedPosts: Block` with `slug: 'relatedPosts'` — port verbatim from the JuanPortfolio `RelatedPostsBlock/config.ts` analog (53 lines): `title` field, `posts` relationship (relationTo: 'posts', hasMany: true), `autoSelect` checkbox, `limit` number.
  </action>
  <verify>
    <automated>Run the shell checks listed in the acceptance_criteria block below; all must pass</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const TestimonialsCarousel" src/blocks/TestimonialsCarousel/config.ts` == 1
    - `grep -c "export const ContactFormBlock" src/blocks/ContactFormBlock/config.ts` == 1 and `grep -c "contactInfo" src/blocks/ContactFormBlock/config.ts` == 1
    - `grep -c "form-builder\|formBuilder" src/blocks/ContactFormBlock/config.ts` == 0
    - `grep -c "export const Code" src/blocks/Code/config.ts` == 1 and `grep -c "language" src/blocks/Code/config.ts` == 1
    - `grep -c "export const RelatedPosts" src/blocks/RelatedPosts/config.ts` == 1
  </acceptance_criteria>
  <done>TestimonialsCarousel, ContactFormBlock, Code, and RelatedPosts config files exist and export valid `Block` configs with zero form-builder plugin dependency.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create TableOfContentsBlock, ResultsSection, Section configs</name>
  <files>src/blocks/TableOfContentsBlock/config.ts, src/blocks/ResultsSection/config.ts, src/blocks/Section/config.ts</files>
  <read_first>
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/TableOfContentsBlock/config.ts
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/ResultsSection/config.ts
    - /Users/juan/Documents/Codigo/Personal/juantech/JuanPortfolio/src/blocks/Section/config.ts
  </read_first>
  <action>
Create `src/blocks/TableOfContentsBlock/config.ts` exporting `TableOfContentsBlock: Block` with `slug: 'tableOfContentsBlock'` — port verbatim from the JuanPortfolio analog (60 lines): position/sticky/minHeadingLevel controls. CONFIRMED by Juan for long posts per CONTEXT.md.

Create `src/blocks/ResultsSection/config.ts` exporting `ResultsSection: Block` with `slug: 'resultsSection'` — port verbatim from the JuanPortfolio analog (65 lines): `title`/`description` fields, `stats` array (minRows: 1, maxRows: 6), `backgroundColor` select.

Create `src/blocks/Section/config.ts` exporting `Section: Block` with `slug: 'section'` — port the wrapper mechanics verbatim from the JuanPortfolio analog (101 lines): `container`/`paddingY`/`backgroundStyle` row, conditional `backgroundColor`/`backgroundMedia`, `anchorId`/`className`, nested `blocks` field. CRITICAL DEVIATION from the analog: update the nested `blocks` array (the analog's line 94) to reference only `CallToAction`, `Content`, `MediaBlock`, `ArchiveBlock` — do NOT reference the old site's `Form`/`Intro`/`WorkCards`/`FeaturedClients` blocks (DROP-listed or superseded per CONTEXT.md). Import these four blocks from their sibling `../CallToAction/config`, `../Content/config`, `../MediaBlock/config`, `../ArchiveBlock/config` paths (created in Plan 01-05).
  </action>
  <verify>
    <automated>Run the shell checks listed in the acceptance_criteria block below; all must pass</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const TableOfContentsBlock" src/blocks/TableOfContentsBlock/config.ts` == 1
    - `grep -c "export const ResultsSection" src/blocks/ResultsSection/config.ts` == 1 and `grep -c "name: 'stats'" src/blocks/ResultsSection/config.ts` == 1
    - `grep -c "export const Section" src/blocks/Section/config.ts` == 1
    - `grep -c "WorkCards\|FeaturedClients\|Intro'" src/blocks/Section/config.ts` == 0 (old DROP-listed blocks not referenced)
    - `grep -c "CallToAction\|ArchiveBlock" src/blocks/Section/config.ts` >= 1
  </acceptance_criteria>
  <done>TableOfContentsBlock, ResultsSection, and Section config files exist; Section's nested blocks array references only the new consolidated set, zero references to DROP-listed old blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin editor → ContactFormBlock config | Contact form field labels/copy are admin-editable, actual submission handling (Resend send) is Phase 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-09 | Tampering | Section block's nested `blocks` array silently drifting back to old DROP-listed block references over time | accept | No runtime security impact in Phase 1 (schema/admin-UI only, no rendering yet) — mitigated procedurally via explicit acceptance-criteria grep checks in this plan and Pitfall-5 documentation established in 01-05 |
</threat_model>

<verification>
All 7 block config files exist under `src/blocks/*/config.ts`, each exports a valid `Block` typed object, ContactFormBlock has zero form-builder plugin coupling, Section references only the new consolidated block set.
</verification>

<success_criteria>
TestimonialsCarousel, ContactFormBlock, Code, RelatedPosts, TableOfContentsBlock, ResultsSection, and Section block configs exist, completing the ~12-14 block consolidated library (combined with the 6 blocks from 01-05, total is 13), ready for registration on `Pages.layout` in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/01-schema-foundation/01-06-SUMMARY.md` when done
</output>
