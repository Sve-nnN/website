---
phase: 42-meta-tags-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/canonical.ts
  - "src/app/(frontend)/[locale]/page.tsx"
  - "src/app/(frontend)/[locale]/contact/page.tsx"
  - "src/app/(frontend)/[locale]/privacy/page.tsx"
  - "src/app/(frontend)/[locale]/terms/page.tsx"
  - "src/app/(frontend)/[locale]/blog/page.tsx"
  - "src/app/(frontend)/[locale]/case-studies/page.tsx"
  - "src/app/(frontend)/[locale]/authors/page.tsx"
  - "src/app/(frontend)/[locale]/websites/page.tsx"
  - "src/app/(frontend)/[locale]/search/page.tsx"
autonomous: true
requirements: [META-01]

must_haves:
  truths:
    - "Home, Contact, Privacy, Terms, Blog listing, Case Studies listing, Authors listing, Websites listing, and Search each emit a locale-aware, self-referencing alternates.canonical tag"
    - "The 4 Servicios/Services routes' existing buildServiceAlternates-based canonical logic is left completely untouched — no duplicate or conflicting canonical mechanism introduced for those routes"
  artifacts:
    - "src/lib/canonical.ts (new exported buildAlternates function, buildServiceAlternates unchanged)"
  key_links:
    - "buildAlternates(locale, esPath, enPath) -> SITE_URL from src/lib/sitemap-data.ts, the same source of truth already used by buildServiceAlternates and buildOpenGraph"
    - "Each route's canonical esPath/enPath must match the es/en URL pattern the route's own already-live buildOpenGraph({ url }) call uses, so canonical and og:url never diverge"
---

<objective>
Audit every public `generateMetadata` for existing `alternates` coverage, add a generic sitewide `buildAlternates(locale, esPath, enPath)` helper to `src/lib/canonical.ts` (without touching the existing `buildServiceAlternates`), and wire it into the 9 Pages-collection/no-doc routes that currently have zero canonical tag: Home, Contact, Privacy, Terms, Blog listing, Case Studies listing, Authors listing, Websites listing, Search.

Purpose: META-01 requires `alternates.canonical` on every public route. Phase 41 already proved Servicios has its own working `buildServiceAlternates` mechanism (in `src/lib/services-data.ts`) — this plan must not duplicate or fight that pattern, only fill the routes it doesn't cover.
Output: `src/lib/canonical.ts` gains `buildAlternates`; 9 route files gain a correct, locale-aware `alternates.canonical`/`alternates.languages`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-meta-tags-completion/42-CONTEXT.md

@src/lib/canonical.ts
@src/lib/services-data.ts
@src/lib/sitemap-data.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit + buildAlternates helper + wire Home, Contact, Privacy, Terms</name>
  <files>src/lib/canonical.ts, src/app/(frontend)/[locale]/page.tsx, src/app/(frontend)/[locale]/contact/page.tsx, src/app/(frontend)/[locale]/privacy/page.tsx, src/app/(frontend)/[locale]/terms/page.tsx</files>
  <read_first>
    src/lib/canonical.ts (existing buildServiceAlternates — the exact recipe to generalize)
    src/lib/services-data.ts (confirms Servicios/Services already call buildServiceAlternates — do not touch)
    src/app/(frontend)/[locale]/page.tsx
    src/app/(frontend)/[locale]/contact/page.tsx
    src/app/(frontend)/[locale]/privacy/page.tsx
    src/app/(frontend)/[locale]/terms/page.tsx
  </read_first>
  <action>
    Step 1 — audit (run before editing anything): `grep -rL "alternates" "src/app/(frontend)/[locale]" --include=page.tsx` will list every `page.tsx` with zero `alternates` reference. Cross-reference against `grep -rln "buildServiceAlternates" src/lib` — confirms `services/page.tsx`, `servicios/page.tsx`, `services/[slug]/page.tsx`, `servicios/[slug]/page.tsx` are already covered indirectly (their `generateMetadata` delegates to `getServicesIndexMetadata`/`getServiceMetadata` in `src/lib/services-data.ts`, which already calls `buildServiceAlternates`) even though the route files themselves don't literally contain the string "alternates". Do not add anything to those 4 route files or to `services-data.ts` in this plan.

    Step 2 — in `src/lib/canonical.ts`, add a new exported function below the existing `buildServiceAlternates`: `buildAlternates(locale: Locale, esPath: string, enPath: string): Metadata['alternates']`. It computes `targetPath = locale === 'es' ? esPath : enPath` and returns the same shape `buildServiceAlternates` returns — `canonical: \`${SITE_URL}${targetPath}\`` and `languages: { es: \`${SITE_URL}${esPath}\`, en: \`${SITE_URL}${enPath}\`, 'x-default': \`${SITE_URL}${esPath}\` }` (x-default points at the `es` URL because `routing.ts` sets `defaultLocale: 'es'`, same rule `buildServiceAlternates` already follows). Do not modify `buildServiceAlternates` itself — this is a new, separate, generic sibling function for the 1:1 es/en path pairs the remaining routes use (unlike Servicios' 4-to-2 collapsing).

    Step 3 — wire 4 routes. Each already imports `buildOpenGraph` from `@/lib/og-image` and casts `locale as 'es' | 'en'` — add a matching `import { buildAlternates } from '@/lib/canonical'` and add `alternates: buildAlternates(locale as 'es' | 'en', esPath, enPath)` as a sibling key to `openGraph` in the object `generateMetadata` returns:
    - `page.tsx` (Home): `esPath` is `'/'`, `enPath` is `'/en'` — matches the existing `url: locale === 'en' ? '/en' : '/'` line already used for `buildOpenGraph`.
    - `contact/page.tsx`: `esPath` `'/contact'`, `enPath` `'/en/contact'` — the file already computes `const url = locale === 'en' ? '/en/contact' : '/contact'`; reuse that exact same es/en pair for canonical instead of re-deriving it separately.
    - `privacy/page.tsx`: `esPath` `'/privacy'`, `enPath` `'/en/privacy'` — mirrors its existing `url` const.
    - `terms/page.tsx`: `esPath` `'/terms'`, `enPath` `'/en/terms'` — mirrors its existing `url` const.
  </action>
  <verify>
    <automated>
npx tsc --noEmit -p tsconfig.json
grep -c "export function buildAlternates" src/lib/canonical.ts
grep -c "buildServiceAlternates" src/lib/canonical.ts
for f in "src/app/(frontend)/[locale]/page.tsx" "src/app/(frontend)/[locale]/contact/page.tsx" "src/app/(frontend)/[locale]/privacy/page.tsx" "src/app/(frontend)/[locale]/terms/page.tsx"; do
  echo "-- $f --"
  grep -c "buildAlternates(" "$f"
done
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/canonical.ts` exports both `buildServiceAlternates` (unchanged) and the new `buildAlternates`
    - `page.tsx`, `contact/page.tsx`, `privacy/page.tsx`, `terms/page.tsx` each contain exactly one `buildAlternates(` call, with the correct es/en path literals listed above
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `services/page.tsx`, `servicios/page.tsx`, `services/[slug]/page.tsx`, `servicios/[slug]/page.tsx`, and `src/lib/services-data.ts` are byte-identical to their pre-task state (git diff empty for those 5 files)
  </acceptance_criteria>
  <done>Home, Contact, Privacy, and Terms each return a correct `alternates.canonical`/`alternates.languages` object from `generateMetadata`, and the pre-existing Servicios canonical mechanism is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Blog listing, Case Studies listing, Authors listing, Websites listing, Search</name>
  <files>src/app/(frontend)/[locale]/blog/page.tsx, src/app/(frontend)/[locale]/case-studies/page.tsx, src/app/(frontend)/[locale]/authors/page.tsx, src/app/(frontend)/[locale]/websites/page.tsx, src/app/(frontend)/[locale]/search/page.tsx</files>
  <read_first>
    src/app/(frontend)/[locale]/blog/page.tsx
    src/app/(frontend)/[locale]/case-studies/page.tsx
    src/app/(frontend)/[locale]/authors/page.tsx
    src/app/(frontend)/[locale]/websites/page.tsx
    src/app/(frontend)/[locale]/search/page.tsx
  </read_first>
  <action>
    Same mechanical pattern as Task 1: each of these 5 files already imports `buildOpenGraph` from `@/lib/og-image` and computes a locale-correct `url` (either as a `const url = ...` or inline inside the `buildOpenGraph({ url: ... })` call). Add `import { buildAlternates } from '@/lib/canonical'` and add `alternates: buildAlternates(locale as 'es' | 'en', esPath, enPath)` as a sibling key to `openGraph`, reusing the exact same es/en path pair each file's own `url` computation already encodes:
    - `blog/page.tsx`: `esPath` `'/blog'`, `enPath` `'/en/blog'` (mirrors its existing `const url = locale === 'en' ? '/en/blog' : '/blog'`).
    - `case-studies/page.tsx`: `esPath` `'/case-studies'`, `enPath` `'/en/case-studies'` (mirrors its inline `url: locale === 'en' ? '/en/case-studies' : '/case-studies'`).
    - `authors/page.tsx`: `esPath` `'/authors'`, `enPath` `'/en/authors'` (mirrors its inline `url: locale === 'en' ? '/en/authors' : '/authors'`).
    - `websites/page.tsx`: `esPath` `'/websites'`, `enPath` `'/en/websites'` (mirrors its inline `url: locale === 'en' ? '/en/websites' : '/websites'`).
    - `search/page.tsx`: `esPath` `'/search'`, `enPath` `'/en/search'` (mirrors its inline `url: locale === 'en' ? '/en/search' : '/search'`).
  </action>
  <verify>
    <automated>
npx tsc --noEmit -p tsconfig.json
for f in "src/app/(frontend)/[locale]/blog/page.tsx" "src/app/(frontend)/[locale]/case-studies/page.tsx" "src/app/(frontend)/[locale]/authors/page.tsx" "src/app/(frontend)/[locale]/websites/page.tsx" "src/app/(frontend)/[locale]/search/page.tsx"; do
  echo "-- $f --"
  grep -c "buildAlternates(" "$f"
done
npm run dev &
DEV_PID=$!
sleep 6
curl -s http://localhost:3000/blog | grep -o '<link rel="canonical" href="[^"]*"'
curl -s http://localhost:3000/en/blog | grep -o '<link rel="canonical" href="[^"]*"'
curl -s http://localhost:3000/case-studies | grep -o '<link rel="canonical" href="[^"]*"'
kill $DEV_PID 2>/dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - each of the 5 files contains exactly one `buildAlternates(` call with the correct es/en path literals listed above
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `curl http://localhost:3000/blog` HTML contains `<link rel="canonical" href="{SITE_URL}/blog">`; `curl http://localhost:3000/en/blog` contains `<link rel="canonical" href="{SITE_URL}/en/blog">`
  </acceptance_criteria>
  <done>Blog listing, Case Studies listing, Authors listing, Websites listing, and Search all return a correct `alternates.canonical` from `generateMetadata`, confirmed live via curl for at least one route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Route `locale`/`slug` params (from the URL path, already validated against known segments/collection lookups) → `buildAlternates` canonical/hreflang output | Inputs are constrained to a fixed set of literal path strings this plan writes — never derived from request headers, query strings, or unvalidated user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-03 | Spoofing | `buildAlternates(locale, esPath, enPath)` in `src/lib/canonical.ts` | low | mitigate | `esPath`/`enPath` are always literal string constants hardcoded per route in this plan (never built from `request.headers.get('host')` or a query string), preventing canonical-tag injection via a crafted `Host`/`X-Forwarded-Host` header or query param — the same safety property `buildServiceAlternates` already relies on, now shared by the generic helper. |
</threat_model>

<verification>
`npx tsc --noEmit -p tsconfig.json` passes after both tasks. Live curl of Home/Contact/Blog/Case-Studies (both locales) confirms `<link rel="canonical">` renders with the correct absolute, locale-correct URL. `git diff` on the 5 Servicios-related files (4 routes + `services-data.ts`) is empty.
</verification>

<success_criteria>
9 of the 15 gap routes identified by this phase's canonical audit now emit `alternates.canonical`; `src/lib/canonical.ts` exposes a reusable `buildAlternates` helper Plan 42-03 will use for the remaining 6 routes.
</success_criteria>

<output>
Create `.planning/phases/42-meta-tags-completion/42-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/lib/canonical.ts` — new exported `buildAlternates(locale, esPath, enPath)` function (generic sitewide canonical/hreflang builder), `buildServiceAlternates` unchanged
- `alternates.canonical`/`alternates.languages` wired into: Home, Contact, Privacy, Terms, Blog listing, Case Studies listing, Authors listing, Websites listing, Search (9 routes)
