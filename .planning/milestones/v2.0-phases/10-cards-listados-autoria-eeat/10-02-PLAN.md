---
phase: 10-cards-listados-autoria-eeat
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/seed-phase10-eeat-fixtures.ts
  - scripts/verify-phase10-cards-eeat.mjs
  - scripts/cleanup-phase10-eeat-fixtures.ts
autonomous: true
requirements: [UI-09, UI-10]

must_haves:
  truths:
    - "Card grids render correctly at repeater min (1 item) and at real/representative max (6 items — the Payload-configured max for FeaturedPostsBlock/FeaturedCaseStudiesBlock/RelatedPosts limit fields) without broken grid layout, using real Postgres data for posts-based grids and seeded fixtures for case-study grids (0 real CaseStudies exist in production)"
    - "AuthorCard/AuthorByline render bio, credentials, yearsExperience, and socialLinks prominently when tested against a full seeded E-E-A-T fixture author"
    - "The two longest real ES post titles (72-post corpus) render in /es without a broken/overflowing layout across PostCard and the blog-post-detail author byline"
    - "All seeded verification fixtures are deleted afterward and every temporarily-toggled real page limit field is restored to its original value"
    - "The phase closing report explicitly states Juan's real author (id=1) still needs credentials/yearsExperience/socialLinks populated via /admin — a content task, not a code blocker for this phase"
  artifacts:
    - path: "scripts/seed-phase10-eeat-fixtures.ts"
      provides: "Guarded Payload Local API fixture seeding (1 test Author with full E-E-A-T fields, 6 test CaseStudies for repeater-max boundary), state snapshot for cleanup"
    - path: "scripts/verify-phase10-cards-eeat.mjs"
      provides: "Automated dev-server render assertions for repeater boundaries, ES longest-title rendering, and AuthorCard/Byline E-E-A-T prominence"
    - path: "scripts/cleanup-phase10-eeat-fixtures.ts"
      provides: "Guarded fixture deletion + FeaturedContent/limit-field restoration, following scripts/cleanup-phase1-fixtures.ts's exact-match-guard precedent"
    - path: ".planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md"
      provides: "Verification report with the explicit Juan content-gap flag"
  key_links:
    - from: "scripts/verify-phase10-cards-eeat.mjs"
      to: "running next dev server"
      via: "fetch(`${BASE_URL}...`)"
      pattern: "fetch\\(`\\$\\{BASE_URL\\}"
    - from: "scripts/cleanup-phase10-eeat-fixtures.ts"
      to: "scripts/seed-phase10-eeat-fixtures.ts"
      via: "shared state snapshot file listing created fixture ids + original field values"
      pattern: "phase10-fixture-state"
---

<objective>
Prove, with concrete real-and-representative data, that the card-grid consistency and E-E-A-T prominence styling built in `10-01` actually holds at the boundary conditions 10-CONTEXT.md calls out by name: repeater min/max counts, the real longest Spanish titles, and a fully-populated author profile (since the one real author's E-E-A-T fields are still empty in production).

Purpose: 10-CONTEXT.md is explicit that "verification of success criterion #3 should confirm the visual treatment is correct by testing with seeded/representative sample data" — this is not optional polish, it is this phase's own definition of done. Direct Postgres inspection during planning confirmed: 72 real posts (categories range from 0 to 24 posts each), 0 real CaseStudies, 1 real Author with `years_experience: null` and 0 rows in `authors_credentials`/`authors_social_links`. Posts-based grids can be verified against real content; case-study grids and full E-E-A-T rendering cannot, and require throwaway fixtures — following the exact guarded-fixture-and-cleanup pattern already established in `scripts/cleanup-phase1-fixtures.ts`.
Output: Three new scripts (seed, verify, cleanup) and a `10-VERIFICATION.md` report. No production content is left behind; no real page's field values are left altered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cards-listados-autoria-eeat/10-CONTEXT.md
@.planning/phases/10-cards-listados-autoria-eeat/10-01-SUMMARY.md

@scripts/cleanup-phase1-fixtures.ts
@scripts/smoke-check-phase8.mjs
@src/collections/CaseStudies/index.ts
@src/collections/Authors/index.ts
@src/globals/FeaturedContent
</context>

<interfaces>
<!-- Real production data facts gathered directly from Postgres during planning (2026-07-10). Use these literal values — do not re-derive them differently. -->

**Real content facts (direct Neon Postgres query, `DATABASE_URI` in `.env`):**
- `posts`: 72 rows. Category post-counts (via `posts_rels` path=`categories`): one category has 0 posts (empty-state boundary, already handled by `ArchiveBlock`'s existing `emptyStateHeading`/`emptyStateBody`), the other four have 17, 18, 18, and 24.
- `case_studies`: **0 rows** — this collection is empty in production (confirmed independently by Phase 8's SUMMARY, which SKIPped `ResultsSection`'s smoke check for the same reason). Any verification of `FeaturedCaseStudiesBlock`, `ArchiveBlock` in case-study mode, or `CaseStudyCard` at a real max-item count requires seeded fixtures.
- `authors`: 1 row (id=1, "Juan Carlos Angulo", `years_experience: null`). `authors_credentials`: 0 rows. `authors_social_links`: 0 rows. This is the exact content gap 10-CONTEXT.md flags — do not populate these fields on the real author id=1 as part of this plan; use a separate throwaway fixture author instead.
- Longest real ES post titles (75 chars each): id=53 `"Tech Seo Guide: Guía de SEO Técnico para Desarrolladores: Rendimiento y ..."`, id=66 `"Normalización de Bases de Datos: Guía técnica de formas normales e integ..."`. Re-fetch each post's exact `slug` via the Payload Local API at execution time (slugs were not captured during planning) rather than hardcoding a guessed slug.
- Real Payload-configured block instances (direct Postgres read of `pages_blocks_*` tables): home page (`pages.id=1`) `ArchiveBlock` limit=12 (posts, category-filter on); home page `FeaturedPostsBlock` limit=3; home page `FeaturedCaseStudiesBlock` limit=3; blog page (`pages.id=2`) `FeaturedPostsBlock` limit=3. `RelatedPosts` is invoked directly by `src/app/(frontend)/[locale]/blog/[slug]/page.tsx` with a template-hardcoded `limit={3}` (not a page-builder block instance — do not look for it in `pages_blocks_related_posts`, that table is empty by design).

**Payload schema boundary facts (direct read of `config.ts` files):**
- `FeaturedPostsBlock`, `FeaturedCaseStudiesBlock`, `RelatedPosts` all declare `limit: { type: 'number', min: 1, max: 6 }` — 1 and 6 are the true schema-enforced boundary values to test.
- `ArchiveBlock`'s `limit` field has no `min`/`max` (defaultValue 3) — its real ceiling is whatever an editor configures; the home page's real value (12) is the representative "max" to test, no synthetic value needed.
- `CaseStudies` collection: only `title` (localized text) is `required: true`. Every other field is optional. `versions.drafts.schedulePublish: true` is enabled and `access.read: authenticatedOrPublished` — **a created CaseStudy will not appear on the public frontend unless `_status: 'published'` is set explicitly on `payload.create()`**.
- `Authors` collection: only `name` is `required: true`. `credentials[].label` and `socialLinks[].platform`/`.url` are required *within* their array items, but the arrays themselves are optional — matches `AuthorCard`'s conditional-render design.

**Precedent pattern to follow exactly** (`scripts/cleanup-phase1-fixtures.ts`, read in full before writing this plan's scripts):
```ts
const FIXTURES: FixtureTarget[] = [
  { collection: 'authors', id: 3, matchField: 'name', matchValue: 'Test Author X' },
  // ...
]
// findByID -> compare matchField/matchValue -> delete only on exact match, log + skip otherwise
```
This plan's fixtures must use an equally unambiguous, exact-match-guarded naming scheme (e.g. `"Test Author E-E-A-T Fixture (Phase 10)"`, slugs `test-case-study-phase10-boundary-{1..6}`) so cleanup can never delete the wrong document.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seed E-E-A-T and case-study repeater-boundary fixtures</name>
  <files>scripts/seed-phase10-eeat-fixtures.ts</files>
  <action>
Write a `tsx`-runnable script (`import { getPayload } from 'payload'; import config from '../src/payload.config'`) that, via Payload Local API `payload.create()` calls (Local API bypasses access control by default — no auth setup needed):

1. Creates exactly 1 test Author: `name: "Test Author E-E-A-T Fixture (Phase 10)"`, `slug: "test-author-phase10-eeat"`, `jobTitle: "Test Role"`, `bio: "Seeded bio for Phase 10 E-E-A-T verification."`, `yearsExperience: 12`, `credentials: [{label: "Certificación Google Analytics"}, {label: "10+ años en SEO técnico"}, {label: "Speaker en conferencias tech"}]` (3 items — realistic upper bound, not the schema max since `credentials` has no declared max), `socialLinks: [{platform: "linkedin", url: "https://linkedin.com/in/test-phase10"}, {platform: "github", url: "https://github.com/test-phase10"}, {platform: "x", url: "https://x.com/test_phase10"}]`.

2. Creates exactly 6 test CaseStudies (`title: "Test Case Study Phase 10 Boundary N"` for N in 1..6, `slug: "test-case-study-phase10-boundary-N"`, `heroMetric: "+42% test metric"`, `_status: "published"` — **required**, since `CaseStudies` has drafts enabled and public reads only see published docs). Only `title` is required by schema; leave every other field at its default/omitted so this also exercises `CaseStudyCard`'s conditional guards (`client &&`, `sector &&`, `heroMetric &&` — set `heroMetric` on all 6 so the primary metric line is exercised, leave `client`/`sector` unset on at least 2 of the 6 so the omitted-field render path is also covered).

3. Reads the current `FeaturedContent` global (`payload.findGlobal({ slug: 'featured-content' })`), saves its existing `featuredCaseStudies` array value, then updates the global so `featuredCaseStudies` contains the 6 newly-created test CaseStudy IDs (in order) — this is what makes `FeaturedCaseStudiesBlock` (which reads exclusively from this global, confirmed by direct read of its `Component.tsx`) have anything to render for the boundary check in Task 2.

4. Reads the home page's (`pages.id=1`) current `FeaturedCaseStudiesBlock` limit value (3, confirmed during planning) directly via `payload.findByID({ collection: 'pages', id: 1 })` and locates the block in its `layout` array — save this value too.

5. Writes a single JSON state file to `scripts/.phase10-fixture-state.json` (repo-local scratch file, not committed — confirm it does not match any existing `.gitignore` entry and add one if needed, or rely on the cleanup task deleting it before the plan closes) containing: `{ authorId, caseStudyIds: number[], originalFeaturedCaseStudies: unknown, homePageId: 1, originalFeaturedCaseStudiesBlockLimit: number }`. Task 2 and Task 3 read this file — no other cross-task ID-passing mechanism.

Do not modify the real Author (id=1) or any real Post/CaseStudy. Additive only, plus the one `FeaturedContent` global field update (which is itself reversible and restored in Task 3).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && npx tsx scripts/seed-phase10-eeat-fixtures.ts && test -f scripts/.phase10-fixture-state.json && node -e "const s=require('./scripts/.phase10-fixture-state.json'); if(!s.authorId||!Array.isArray(s.caseStudyIds)||s.caseStudyIds.length!==6) process.exit(1); console.log('PASS: fixture state has 1 author id + 6 case study ids')"</automated>
  </verify>
  <done>1 test Author with full credentials/yearsExperience/socialLinks and 6 test CaseStudies exist in the real DB, all "Test..." named/slugged; FeaturedContent.featuredCaseStudies temporarily points at the 6 test docs; scripts/.phase10-fixture-state.json records everything Task 2/3 need to verify and restore.</done>
</task>

<task type="auto">
  <name>Task 2: Automated boundary/ES/E-E-A-T verification against the running dev server</name>
  <files>scripts/verify-phase10-cards-eeat.mjs</files>
  <action>
Write a Node script (pattern-matched after `scripts/smoke-check-phase8.mjs`: zero-dependency, `fetch()`-based, reads `SMOKE_BASE_URL`/`BASE_URL` env var defaulting to `http://localhost:3000`, requires `npm run dev` already running) that performs the following checks in order, using Payload's Local API (`getPayload({config})`, same pattern as the seed script) both to read `scripts/.phase10-fixture-state.json` and to temporarily toggle/restore real page field values:

1. **Posts-grid min boundary (real data):** Temporarily set the home page's `FeaturedPostsBlock` `limit` to 1 via `payload.update({collection:'pages', id:1, ...})`, `fetch('/en')`, assert HTTP 200 and exactly 1 `PostCard` rendered (grep the response body for a stable marker — e.g. count occurrences of the `rounded-xl` class from `Card`, or better, count `<Card`-equivalent DOM anchor via a distinctive combination like `group block` + `aspect-[16/10]` wrapper divs), then restore the original limit (3) immediately after the assertion, before moving to the next check.
2. **Posts-grid real max boundary:** `fetch('/en')` again with the home page's real `ArchiveBlock` limit (12, untouched — no toggle needed) and assert 12 cards render without a broken/duplicated grid-class pattern (grid container class string appears exactly once per grid, not nested/duplicated).
3. **Category-filter min boundary (real data):** Identify the one real category with 0 posts (query `categories`/`posts_rels` directly, matching the planning-time finding) and its `slug`, `fetch('/en/blog?category={slug}')`, assert the existing empty-state copy renders (`emptyStateHeading`/`emptyStateBody` text) — this is already-implemented behavior, confirm it still works post-restyle, do not treat a failure here as this plan's job to fix (flag and stop if it fails, since that would be a Task 1 regression).
4. **Case-study grid min+max boundary (seeded fixtures):** Read `authorId`/`caseStudyIds` from the state file. Temporarily set the home page's `FeaturedCaseStudiesBlock` `limit` to 1, `fetch('/en')`, assert exactly 1 test `CaseStudyCard` renders (match on one of the seeded titles, e.g. `"Test Case Study Phase 10 Boundary 1"`); then set `limit` to 6 (schema max), `fetch('/en')` again, assert all 6 render; then restore the original limit (3) captured in Task 1's state file.
5. **AuthorCard/AuthorByline E-E-A-T prominence (seeded fixture):** `fetch('/en/authors/test-author-phase10-eeat')` (Authors profile route per 05-UI-SPEC's page inventory), assert HTTP 200 and that the response body contains: the bio text, all 3 seeded credential labels, the `"12+ years of experience"` yearsLabel string (exact locale string built by `AuthorCard.tsx`), and all 3 seeded social URLs as `href` values. Repeat against `/es/authors/test-author-phase10-eeat` asserting the `"12+ años de experiencia"` ES variant instead.
6. **ES longest-title rendering:** Look up the real `slug` for post id=53 and id=66 via Local API (`payload.findByID`), `fetch('/es/blog/{slug}')` for each, assert HTTP 200, no error marker (reuse `ERROR_MARKERS` list pattern from `smoke-check-phase8.mjs`), and that the full untruncated title string appears verbatim in the response body (proves no server-side truncation; genuine visual overflow/wrap at the exact pixel level is a CSS concern this automated check cannot fully verify without a headless browser — no new dependency is authorized to add one, so log this residual risk explicitly in the script's final output as a recommended human eyeball-check, not a blocking failure).
7. Print a final PASS/FAIL summary table (same style as `smoke-check-phase8.mjs`) and exit 1 if any check failed, exit 0 otherwise. **Before exiting, whatever the outcome, restore any field this script itself toggled that is not already back to its original value** (defensive double-restore, even though each check above already restores inline) — read the state file's original values as the source of truth.
  </action>
  <verify>
    <automated>(npm run dev > /tmp/phase10-dev.log 2>&1 &) && sleep 8 && node scripts/verify-phase10-cards-eeat.mjs; EXIT=$?; pkill -f "next dev" || true; exit $EXIT</automated>
  </verify>
  <done>All 6 checks (posts-min, posts-max, category-empty-state, case-study-min/max, AuthorCard/Byline E-E-A-T prominence in both locales, ES longest-title rendering) pass; script exits 0; every field it toggled is restored to its original value by the time it exits.</done>
</task>

<task type="auto">
  <name>Task 3: Guarded fixture cleanup and phase verification report</name>
  <files>scripts/cleanup-phase10-eeat-fixtures.ts, .planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md</files>
  <action>
Write `scripts/cleanup-phase10-eeat-fixtures.ts` following `scripts/cleanup-phase1-fixtures.ts`'s exact guard pattern: read `scripts/.phase10-fixture-state.json`, for each of the 6 `caseStudyIds` and the 1 `authorId`, `payload.findByID` and confirm the document's `title`/`name` still matches the expected `"Test Case Study Phase 10 Boundary N"` / `"Test Author E-E-A-T Fixture (Phase 10)"` value before deleting — skip and log (never force-delete) on any mismatch. Restore `FeaturedContent.featuredCaseStudies` to `originalFeaturedCaseStudies` from the state file. Confirm (re-read) the home page's `FeaturedCaseStudiesBlock` limit equals `originalFeaturedCaseStudiesBlockLimit`; if Task 2 already restored it correctly this is a no-op assertion, if not, restore it here as the final safety net. Delete `scripts/.phase10-fixture-state.json` itself once cleanup completes successfully.

Write `.planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md` summarizing: which of the 4 ROADMAP success criteria passed and how (cite the specific real/seeded data points used — 72 posts, category counts 0/17/18/18/24, 0 real case studies hence seeded fixtures, longest ES titles ids 53/66), confirm zero diffs in `src/blocks/*/config.ts`/`payload-types.ts`, and include this **explicit, non-blocking flag**: *"AuthorCard/AuthorByline correctly render credentials, years of experience, and social links prominently when present — verified against a seeded fixture author, since the real production author (id=1, Juan Carlos Angulo) still has `years_experience: null` and 0 rows in `authors_credentials`/`authors_social_links`. This is a pending content-population task for Juan to complete via `/admin` (add credentials, years of experience, and social links to his real author profile) — it does not block this phase's code/styling work, which is complete and verified."*
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && npx tsx scripts/cleanup-phase10-eeat-fixtures.ts && test ! -f scripts/.phase10-fixture-state.json && test -f .planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md && grep -qi "pending content-population" .planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md && echo "PASS: fixtures cleaned, report flags Juan's pending content task"</automated>
  </verify>
  <done>All 7 seeded fixtures (1 author + 6 case studies) are deleted from the real DB (or explicitly logged as skipped if a match-guard failed); FeaturedContent.featuredCaseStudies and the home page's FeaturedCaseStudiesBlock limit are back to their pre-plan values; the state file is removed; 10-VERIFICATION.md exists and explicitly flags Juan's real-author content gap as non-blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Verification scripts -> production Neon Postgres | Seed/cleanup scripts run `payload.create()`/`payload.delete()` against the same real database used by the live site (no separate test DB exists for this project) |
| verify script -> local dev server | `fetch()` calls a locally-running `next dev` instance only (`localhost:3000` by default), never a deployed/public URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-------------------|
| T-10-04 | Tampering | seed/cleanup scripts writing to production DB | mitigate | Every fixture is named/slugged with an unambiguous `"Test ... Phase 10"` marker; cleanup deletes only after an exact `findByID` + field-value match, identical guard to `scripts/cleanup-phase1-fixtures.ts` — never a blanket `DELETE WHERE` or ID-range delete |
| T-10-05 | Tampering | verify script temporarily toggling real page `limit` fields | mitigate | Every toggle is scoped to a single named field on a single known page ID, immediately restored within the same check (inline), plus a defensive double-restore pass at script exit reading the original values from the state file |
| T-10-06 | Denial of Service | Task 2 spinning up `next dev` as a background process for verification | accept | Local-only, single dev-server process, killed at the end of the verify command (`pkill -f "next dev"`); no production traffic or deployed infrastructure involved |
| T-10-07 | Repudiation | No audit trail if a fixture delete silently no-ops on a guard mismatch | mitigate | Cleanup script logs every skip (mismatch) to stdout by design (precedent behavior), captured in the task's own verification output, not silently swallowed |

No new npm dependencies are installed by this plan — the package-legitimacy gate does not apply.
</threat_model>

<verification>
1. `npx tsx scripts/seed-phase10-eeat-fixtures.ts` creates exactly 1 test Author + 6 test CaseStudies and writes a valid state file.
2. `npx tsx scripts/verify-phase10-cards-eeat.mjs` (against a running `npm run dev`) exits 0 with all 6 documented checks passing.
3. `npx tsx scripts/cleanup-phase10-eeat-fixtures.ts` deletes all 7 fixtures, restores `FeaturedContent` and the toggled `limit` field, and removes the state file.
4. `git diff --stat -- 'src/blocks/*/config.ts' src/payload-types.ts` remains empty after this plan (no schema drift introduced by the verification tooling itself).
5. `.planning/phases/10-cards-listados-autoria-eeat/10-VERIFICATION.md` exists and explicitly states the real author's E-E-A-T fields are still pending population by Juan, framed as non-blocking.
</verification>

<success_criteria>
- Repeater boundary conditions (1 item, real/schema max) are verified for all four card-grid surfaces, using real Postgres data where it exists (posts) and guarded seeded fixtures where it doesn't (case studies).
- AuthorCard/AuthorByline are verified to render bio, credentials, years of experience, and social links prominently, in both `/en` and `/es`, against a fully-populated fixture author.
- The two longest real Spanish post titles render correctly in `/es` with no broken layout.
- Zero residual fixtures or toggled field values remain in the production database after this plan completes.
- The phase's closing verification artifact explicitly and clearly flags Juan's pending real-author content-population task as separate from, and non-blocking to, this phase's code completion.
</success_criteria>

<output>
Create `.planning/phases/10-cards-listados-autoria-eeat/10-02-SUMMARY.md` when done
</output>
