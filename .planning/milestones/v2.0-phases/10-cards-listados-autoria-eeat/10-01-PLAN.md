---
phase: 10-cards-listados-autoria-eeat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PostCard.tsx
  - src/components/CaseStudyCard.tsx
  - src/components/AuthorByline.tsx
  - src/components/AuthorCard.tsx
autonomous: true
requirements: [UI-09, UI-10]

must_haves:
  truths:
    - "ArchiveBlock, FeaturedPostsBlock, FeaturedCaseStudiesBlock, and RelatedPosts cards all show the same resting elevation, hover elevation, and corner radius, because all four already delegate rendering to the two shared card components this plan restyles"
    - "AuthorCard visibly displays credentials, years of experience, and social links prominently when those fields are present, without any field being unconditionally assumed present"
    - "AuthorByline keeps rendering compact avatar+name+one-credential-badge context, restyled only, same data contract"
    - "Zero diffs in any src/blocks/*/config.ts file or payload-types.ts"
  artifacts:
    - path: "src/components/PostCard.tsx"
      provides: "Shared post card composed from ui/card.tsx Card/CardContent primitives"
    - path: "src/components/CaseStudyCard.tsx"
      provides: "Shared case-study card composed from ui/card.tsx Card/CardContent primitives"
    - path: "src/components/AuthorCard.tsx"
      provides: "E-E-A-T-prominent author card, composed from ui/card.tsx primitives"
    - path: "src/components/AuthorByline.tsx"
      provides: "Compact author byline, restyled to match the same visual family"
  key_links:
    - from: "src/components/PostCard.tsx"
      to: "src/components/ui/card.tsx"
      via: "import { Card, CardContent } from '@/components/ui/card'"
      pattern: "from '@/components/ui/card'"
    - from: "src/components/CaseStudyCard.tsx"
      to: "src/components/ui/card.tsx"
      via: "import { Card, CardContent } from '@/components/ui/card'"
      pattern: "from '@/components/ui/card'"
    - from: "src/components/AuthorCard.tsx"
      to: "src/components/ui/card.tsx"
      via: "import { Card, CardContent } from '@/components/ui/card'"
      pattern: "from '@/components/ui/card'"
---

<objective>
Make all four card-grid surfaces (`ArchiveBlock`, `FeaturedPostsBlock`, `FeaturedCaseStudiesBlock`, `RelatedPosts` — UI-09) share one consistent elevation/spacing visual language by fixing the two shared card components they all delegate to, and restyle `AuthorByline`/`AuthorCard` to make E-E-A-T credentials/years/social-links visually prominent when present (UI-10).

Purpose: Phase 7 (shadow/motion tokens) and Phase 8 (refined `card.tsx` primitive, `duration-*`/`ease-*` utilities) exist specifically so later phases consume them instead of hand-rolling styling. `PostCard.tsx` and `CaseStudyCard.tsx` currently hand-roll `rounded-lg border ... hover:shadow-md transition-shadow` instead of composing the already-refined `Card`/`CardContent` primitive — this is the exact inconsistency this phase must close. `AuthorCard.tsx` has the same hand-rolled-div problem plus understated E-E-A-T field treatment.
Output: Four restyled component files. Zero changes to any block's Payload field schema (`config.ts`) or `payload-types.ts` — this is a pure `Component.tsx`-adjacent styling pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cards-listados-autoria-eeat/10-CONTEXT.md
@.planning/phases/05-frontend-pages/05-UI-SPEC.md

@src/components/ui/card.tsx
@src/components/PostCard.tsx
@src/components/CaseStudyCard.tsx
@src/components/AuthorByline.tsx
@src/components/AuthorCard.tsx
</context>

<interfaces>
<!-- Card primitive this plan must consume, not reinvent. Read directly from src/components/ui/card.tsx (Phase 8-refined). -->

```tsx
// src/components/ui/card.tsx (already token-driven, do not edit)
const Card = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div
      ref={ref}
      className={cn(
        "rounded-xl border bg-card text-card-foreground shadow-sm transition-shadow duration-base ease-standard hover:shadow-md",
        className
      )}
      {...props}
    />
  )
)
// Also exported: CardHeader, CardTitle, CardDescription, CardContent (p-6 pt-0), CardFooter
```

Confirmed by direct read: `ArchiveBlock`, `FeaturedPostsBlock`, `FeaturedCaseStudiesBlock`, `RelatedPosts` all use the identical grid container `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` and delegate every card render to `PostCard`/`CaseStudyCard`. No block `Component.tsx` file needs to change for UI-09 — restyling the two shared card components is sufficient and is the intended fix (do not touch block files).

Current `PostCard.tsx` (hand-rolled, to be replaced with `Card`/`CardContent` composition):
```tsx
<Link href={...} className="group block rounded-lg border border-border overflow-hidden bg-card hover:shadow-md transition-shadow">
  <div className="relative aspect-[16/10] bg-muted">
    <Image ... className="object-cover group-hover:scale-105 transition-transform" />
  </div>
  <div className="p-6">
    <h3 className="font-display text-heading">{post.title}</h3>
    {post.excerpt && <p className="mt-2 text-body text-muted-foreground line-clamp-2">{post.excerpt}</p>}
  </div>
</Link>
```

Current `CaseStudyCard.tsx` (hand-rolled, to be replaced with `Card`/`CardContent` composition):
```tsx
<Link href={...} className="group block rounded-lg border border-border overflow-hidden bg-card hover:shadow-md transition-shadow p-6">
  {client && <p className="text-label text-muted-foreground">{client.name}</p>}
  <h3 className="font-display text-heading mt-1">{caseStudy.title}</h3>
  {caseStudy.sector && <p className="mt-1 text-body text-muted-foreground">{caseStudy.sector}</p>}
  {caseStudy.heroMetric && <p className="mt-4 font-display text-heading font-semibold text-primary">{caseStudy.heroMetric}</p>}
</Link>
```

Current `AuthorCard.tsx` outer wrapper (hand-rolled, to be replaced with `Card`/`CardContent`):
```tsx
<div className="rounded-lg border border-border bg-card p-8">
  ...avatar/name/bio/credentials/yearsLabel/socialLinks, each behind a conditional guard...
</div>
```

Phase 7 token vocabulary available (from `tailwind.config.ts`, do not hardcode raw values): `shadow-sm/md/lg/focus`, `duration-fast/base/slow`, `ease-out/ease-standard`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Card-grid visual consistency — PostCard and CaseStudyCard compose the Card primitive</name>
  <files>src/components/PostCard.tsx, src/components/CaseStudyCard.tsx</files>
  <action>
Refactor both components to compose `Card`/`CardContent` from `@/components/ui/card` instead of hand-rolled `div` classNames, per 10-CONTEXT.md's locked decision ("use Phase 7's shadow tokens and Phase 8's refined card.tsx primitive consistently across all four [blocks]").

`PostCard.tsx`: Keep the outer `<Link>` as the clickable element (`className="group block"`, no border/shadow classes on the Link itself — those now live on `Card`). Nest a `<Card className="overflow-hidden p-0">` inside the Link (Card's own `rounded-xl border bg-card shadow-sm hover:shadow-md duration-base ease-standard` supplies elevation; `overflow-hidden` is needed so the image's rounded corners clip correctly, `p-0` overrides nothing since Card has no default padding). Inside: keep the `relative aspect-[16/10] bg-muted` image wrapper exactly as-is, but change the image's transition from bare `transition-transform` to `transition-transform duration-base ease-standard` (token-driven, matching Card's own timing). Replace the trailing `<div className="p-6">` with `<CardContent className="p-6">` (CardContent already ships `p-6 pt-0`; explicit `p-6` override keeps top padding since there's no CardHeader above it). Preserve the `post.excerpt &&` conditional exactly as written — do not make it unconditional.

`CaseStudyCard.tsx`: Same pattern — outer `<Link className="group block">`, nested `<Card>` (no `overflow-hidden` needed, no image), `<CardContent className="p-6">` wrapping the existing client/title/sector/heroMetric block. Preserve all three existing conditionals (`client &&`, `caseStudy.sector &&`, `caseStudy.heroMetric &&`) exactly — do not remove any guard or assume a field is always present (RESEARCH.md Pitfall 1).

Do not touch `src/blocks/ArchiveBlock/Component.tsx`, `src/blocks/FeaturedPostsBlock/Component.tsx`, `src/blocks/FeaturedCaseStudiesBlock/Component.tsx`, or `src/blocks/RelatedPosts/Component.tsx` — their grid container markup is already identical across all four and needs no change; this task's fix to the two shared card components is what satisfies UI-09 for all four blocks simultaneously. Do not touch any `config.ts` file.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && npx tsc --noEmit 2>&1 | tail -30 && test "$(grep -l "from '@/components/ui/card'" src/components/PostCard.tsx src/components/CaseStudyCard.tsx | wc -l | tr -d ' ')" = "2" && echo "PASS: both cards import ui/card.tsx" && git diff --stat -- 'src/blocks/*/config.ts' src/payload-types.ts | grep -c . | grep -qx 0 && echo "PASS: zero config/type diffs"</automated>
  </verify>
  <done>PostCard.tsx and CaseStudyCard.tsx both compose Card/CardContent from ui/card.tsx; `npx tsc --noEmit` passes clean; every pre-existing conditional field guard (excerpt, client, sector, heroMetric) is unchanged; no block Component.tsx or config.ts file is modified.</done>
</task>

<task type="auto">
  <name>Task 2: AuthorByline/AuthorCard — E-E-A-T prominence styling</name>
  <files>src/components/AuthorByline.tsx, src/components/AuthorCard.tsx</files>
  <action>
Restyle `AuthorCard.tsx` to compose `Card`/`CardContent` from `@/components/ui/card` (replacing its hand-rolled `<div className="rounded-lg border border-border bg-card p-8">` outer wrapper) for elevation consistency with the card-grid work in Task 1 — this is the same "trust/credibility surface" category per RESEARCH.md's SUMMARY.md. Use `<Card className="p-8">` (Card has no default padding, so the existing `p-8` visual density carries over directly) wrapping the exact same inner JSX tree that exists today. Do not remove any of the four existing conditional guards (`author.bio &&`, `author.credentials && author.credentials.length > 0 &&`, `yearsLabel &&`, `author.socialLinks && author.socialLinks.length > 0 &&`) — per 10-CONTEXT.md, this component's job is to render these fields prominently WHEN PRESENT, not to assume they exist (RESEARCH.md Pitfall 1).

Increase visual prominence of the three E-E-A-T fields per UI-10 and 10-CONTEXT.md's discretion clause ("informed by 05-UI-SPEC.md's E-E-A-T section and research's author-credibility guidance"):
- `yearsLabel` (years of experience): currently `<p className="mt-4 text-label text-primary">`. Change to a standout stat treatment using the existing 4-size type budget — `font-display text-heading font-semibold text-primary` — so it reads as a headline credibility stat rather than a caption, matching the KPI-number treatment pattern already established in `ResultsSection` (Phase 9: "tracking-tight tabular-nums value" for metric dominance — reuse `tracking-tight` here too since a year count is itself a metric).
  Given `yearsLabel` is only known at render time inside the async component (it interpolates the raw number into a locale string), keep the existing string-building logic unchanged; only the wrapping `<p>`'s className changes.
- `credentials` badges: keep `<Badge variant="secondary">` (no new component), but confirm the wrapping `<div className="mt-4 flex flex-wrap gap-2">` stays — this already provides good touch-target spacing (`gap-2` = 8px, matches 05-UI-SPEC's `sm` token). No change needed here beyond what Card composition already gives (no action item, just confirm during review — do not add unnecessary classes).
- `socialLinks` icon links: currently plain `<a className="text-muted-foreground hover:text-primary">`. Read `src/components/ui/button.tsx`'s `focus-visible` ring utility classes (from the `cva()` base string) and apply the same `focus-visible:` ring/outline utility classes to these `<a>` tags (do not import `Button` — these stay plain anchors per the existing markup pattern, just match its accessible-focus visual language) plus a token-driven hover transition: add `transition-colors duration-fast` to the existing className string.

`AuthorByline.tsx`: No structural/data-contract change — restyle only, to visually match the same family as the restyled `AuthorCard`. Confirm the existing `text-label` classes on name/jobTitle and the single credential `<Badge variant="secondary">` stay consistent with `AuthorCard`'s badge styling (both already use `Badge variant="secondary"` — no divergent styling to fix here beyond a visual read-through; if `AuthorCard`'s Card composition changes its ambient background/border treatment in a way that visually clashes with `AuthorByline`'s bare `flex items-center gap-3` (no card wrapper — it's an inline byline, not a card), do NOT wrap `AuthorByline` in a `Card` — it is intentionally chromeless per its own docstring ("Compact byline for post/case-study cards and headers"). Leave its structure untouched; only touch it if a token-vocabulary mismatch is found (e.g., non-token spacing) during this task's read-through.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && npx tsc --noEmit 2>&1 | tail -30 && grep -q "from '@/components/ui/card'" src/components/AuthorCard.tsx && echo "PASS: AuthorCard imports ui/card.tsx" && grep -c '&&' src/components/AuthorCard.tsx | awk '{if ($1 >= 4) print "PASS: at least 4 conditional guards preserved"; else print "FAIL: guard count dropped below 4"}'</automated>
  </verify>
  <done>AuthorCard.tsx composes Card from ui/card.tsx; yearsExperience renders with a headline-stat treatment (font-display text-heading font-semibold text-primary tracking-tight); socialLinks show a token-driven hover/focus affordance; all four existing conditional field guards remain intact; AuthorByline.tsx keeps its chromeless structure and data contract, restyled only where a token mismatch exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Payload admin -> rendered frontend | `credentials[].label`, `socialLinks[].url`, `bio`, `heroMetric`, `sector`, `excerpt` are admin-authored text rendered into public HTML — trusted-author input, not public user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-------------------|
| T-10-01 | Information Disclosure | AuthorCard socialLinks `<a href>` | accept | URLs come from an authenticated-admin-only field (`authors.socialLinks[].url`), rendered with existing `target="_blank" rel="noopener noreferrer"` — no change to this attribute pair in this plan, no public write path exists to this field |
| T-10-02 | Tampering | PostCard/CaseStudyCard/AuthorCard className composition | accept | Pure Tailwind class-string changes on trusted, admin-only-editable component code; no user input flows into `className` construction |
| T-10-03 | Elevation of privilege | N/A | accept | No access-control, auth, or route-handler code is touched by this plan — visual-only component refactor |

No new trust boundaries, no new dependencies, no new data flows are introduced by this plan.
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors after both tasks.
2. `git diff --stat -- 'src/blocks/*/config.ts' src/payload-types.ts` is empty (zero-diff gate, per 10-CONTEXT.md's hard constraint).
3. `git diff --stat -- src/blocks/ArchiveBlock/Component.tsx src/blocks/FeaturedPostsBlock/Component.tsx src/blocks/FeaturedCaseStudiesBlock/Component.tsx src/blocks/RelatedPosts/Component.tsx` is empty — confirms UI-09 was satisfied purely by fixing the two shared card components, not by touching each block.
4. Manual read-through: every `{field && ...}` conditional guard present before this plan (post.excerpt, caseStudy.client/sector/heroMetric, author.bio/credentials/yearsLabel/socialLinks) is still present after.
</verification>

<success_criteria>
- PostCard.tsx and CaseStudyCard.tsx both compose `Card`/`CardContent` from `@/components/ui/card`, giving `ArchiveBlock`, `FeaturedPostsBlock`, `FeaturedCaseStudiesBlock`, and `RelatedPosts` an identical resting/hover elevation and corner-radius language (UI-09).
- AuthorCard.tsx composes the same `Card` primitive and visually foregrounds `yearsExperience` as a headline stat, `credentials` as badges, and `socialLinks` as accessible icon links, all fully conditional on field presence (UI-10).
- AuthorByline.tsx is visually consistent with the restyled AuthorCard without becoming a card itself.
- Zero diffs in any `src/blocks/*/config.ts` file, `payload-types.ts`, or any block `Component.tsx` file.
</success_criteria>

<output>
Create `.planning/phases/10-cards-listados-autoria-eeat/10-01-SUMMARY.md` when done
</output>
