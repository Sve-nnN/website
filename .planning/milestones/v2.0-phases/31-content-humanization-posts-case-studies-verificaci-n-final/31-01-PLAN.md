---
phase: 31-content-humanization-posts-case-studies-verificaci-n-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json
  - .planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase31-*.json (generated, gitignored, not hand-written)
autonomous: true
requirements: [VOICE-07]

must_haves:
  truths:
    - "A full-text pre-sweep snapshot JSON (tag pre-sweep-phase31) exists, captured before any Posts/CaseStudies content write in Wave 2 of this phase runs"
    - "A fresh 'before' Lighthouse mobile capture exists for the 4 real representative blog/case-studies routes (both locales) — Phase 32's regression baseline does not cover these 2 route types, so this is the only 'before' reference the final gate (31-17) can compare against"
    - ".gitignore covers Phase 31's posts-rewrite checkpoint files with one wildcard entry, so the 13 posts-batch plans in Wave 2 never need to touch .gitignore themselves (avoids a same-wave file conflict across parallel plans)"
  artifacts:
    - path: ".gitignore"
      provides: "New entry ignoring this phase's posts-progress-batch-*.json resumable checkpoint logs"
      contains: "posts-progress-batch"
    - path: ".planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json"
      provides: "Fresh pre-rewrite Lighthouse mobile scores (performance/accessibility/best-practices/seo/LCP/CLS/TBT) for /blog/tech-seo-guide and /case-studies/migracion-ecommerce-nextjs-seo-tecnico, both locales"
      contains: "performance"
  key_links:
    - from: "scripts/content-humanization-snapshot.ts"
      to: ".planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase31-*.json"
      via: "--tag pre-sweep-phase31"
      pattern: "pre-sweep-phase31"
    - from: "scripts/lighthouse-mobile.mjs"
      to: ".planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json"
      via: "--routes-only <4 routes> --out lh-phase31-pre.json"
      pattern: "lh-phase31-pre.json"
---

<objective>
Capture the two "before" artifacts this phase's closing verification (31-16) and final gate (31-17) both depend on, before a single word of Posts/CaseStudies content is rewritten: a full-text snapshot of the whole database (tag `pre-sweep-phase31`) and a fresh Lighthouse mobile baseline for the 2 route types (blog, case-studies) that Phase 32's regression baseline never covered. Also adds the one shared `.gitignore` entry that lets all 13 posts-rewrite batch plans in Wave 2 write their own resumable checkpoint file without any of them needing to edit `.gitignore` themselves.

Purpose: Phase 31's success criterion 2 requires a post-sweep snapshot "diffado contra el snapshot pre-humanize de VOICE-04" — that original snapshot already exists (`pre-sweep-phase30-2026-07-14T20:38:02.242Z.json`, captured before Phase 30 touched anything), but this phase's OWN before/after pair (pre-sweep-phase31 vs post-sweep-phase31) is what proves Posts/CaseStudies specifically changed cleanly. Success criterion 5 requires a Lighthouse gate "sin regresión" on routes representative of both tracks — Phase 32's baseline (home + geo-pages, captured 2026-07-14, chronologically before this phase despite the lower phase number) covers half of that; blog/case-studies routes have no prior baseline anywhere in the repo, so one must be captured fresh, right now, before content changes.
Output: `.gitignore` updated; `pre-sweep-phase31-<timestamp>.json` in the (already-gitignored) Phase 29 snapshots directory; `lh-phase31-pre.json` committed in this phase's directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-CONTEXT.md
@.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Existing reusable scripts — run as-is, do not modify their internals. -->

From `scripts/content-humanization-snapshot.ts` (full file, unmodified) — usage:
```
node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag pre-sweep-phase31
```
Writes to `.planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase31-<ISO timestamp>.json` (hardcoded output directory inside the script — do not redirect). `COLLECTIONS` already includes `posts` and `case-studies`.

From `scripts/lighthouse-mobile.mjs` (full file, unmodified) — usage:
```
node scripts/lighthouse-mobile.mjs --base-url http://localhost:3046 --out .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json --routes-only "/blog/tech-seo-guide,/en/blog/tech-seo-guide,/case-studies/migracion-ecommerce-nextjs-seo-tecnico,/en/case-studies/migracion-ecommerce-nextjs-seo-tecnico"
```
Must run against a production build (`npx next build` + `npx next start`), never `next dev` — the script's own header comment states dev-mode scores are not representative. These two slugs are the same real, already-confirmed-live posts/case-study the script's own hardcoded `ROUTES` constant uses for its default `/en/...` variants (`tech-seo-guide`, `migracion-ecommerce-nextjs-seo-tecnico`) — reuse them verbatim rather than picking new slugs, and add the `/es`-equivalent (no-prefix) path for each, since next-intl's `localePrefix: 'as-needed'` with `defaultLocale: 'es'` means the ES route has no `/es` prefix (confirmed pattern already used by Phase 32's baseline for home/geo routes).

From `.planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md` (build/start pattern to reuse exactly):
```
npx next build
PORT=3046 npx next start &
# wait for the server to report ready, then run lighthouse-mobile.mjs
# kill the PORT=3046 process afterward, confirm via lsof -i :3046
```

From `.gitignore` (existing precedent to follow, lines 32-34):
```
# Phase 10 fixture-cleanup cross-script state scratch file — regenerable,
# always deleted by scripts/cleanup-phase10-eeat-fixtures.ts, not source code
scripts/.phase10-fixture-state.json
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore entry for posts-rewrite checkpoints + pre-sweep content snapshot</name>
  <files>.gitignore, .planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase31-*.json</files>
  <read_first>
    .gitignore (full file, 56 lines — follow the existing comment + pattern style at lines 32-34 and 28-30)
    scripts/content-humanization-snapshot.ts (full file — run as-is, do not modify)
  </read_first>
  <action>
    Add one new entry to `.gitignore`, in the same style as the existing Phase 10/29 entries (a 2-line comment explaining what it is and why it's regenerable, then the glob):
    ignore `.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/posts-progress-batch-*.json` — these are resumable checkpoint logs (post id -> done) that the 13 posts-rewrite plans in Wave 2 will each create; they are execution-state scratch files, not source code, same class as the existing `scripts/.phase10-fixture-state.json` entry. Adding this single wildcard entry now means none of the 13 Wave 2 plans need to touch `.gitignore` themselves, which would otherwise create a same-wave file conflict across parallel plans.

    Then run the pre-sweep content snapshot, tagged specifically for this phase (distinct from Phase 30's own `pre-sweep-phase30` tag — do not reuse that tag): `node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag pre-sweep-phase31`. This must run before any Wave 2 plan writes to `posts` or `case-studies`. Do not modify the snapshot script itself.
  </action>
  <verify>
    <automated>grep -c "posts-progress-batch" .gitignore | grep -q '^1$' && node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag pre-sweep-phase31 && ls .planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase31-*.json</automated>
  </verify>
  <done>.gitignore has exactly one new entry matching `posts-progress-batch`; a `pre-sweep-phase31-<timestamp>.json` file exists under the Phase 29 content-snapshots directory, containing full es/en field values for all 9 collections (including posts and case-studies) and 3 globals, captured before any Wave 2 write.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh "before" Lighthouse mobile capture for blog/case-studies routes</name>
  <files>.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json</files>
  <read_first>
    scripts/lighthouse-mobile.mjs (full file — run as-is, do not modify)
    .planning/milestones/v1.7-phases/32-regression-baseline/32-REGRESSION-BASELINE.md (full file — exact build/start/capture/teardown pattern to reuse)
  </read_first>
  <action>
    Build production and start a throwaway server on a port that cannot collide with the dev server (3000) or any other phase's convention: `npx next build`, then `PORT=3046 npx next start` in the background. Optionally run `caffeinate -u &` alongside it (macOS, prevents display sleep during the capture) per Phase 32's precedent — skip silently if `caffeinate` is unavailable on the runtime OS.

    Wait for the server to report ready on port 3046, then run: `node scripts/lighthouse-mobile.mjs --base-url http://localhost:3046 --out .planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json --routes-only "/blog/tech-seo-guide,/en/blog/tech-seo-guide,/case-studies/migracion-ecommerce-nextjs-seo-tecnico,/en/case-studies/migracion-ecommerce-nextjs-seo-tecnico"`.

    These 4 routes are the ONLY "before" reference this milestone will ever have for blog/case-studies performance — Phase 32's baseline never captured them, and no earlier committed Lighthouse artifact in `.planning/` covers these two route types either (confirmed during pattern-mapping). If any route reports an `error` key instead of scores, do not proceed to close this task — investigate (server not fully warm, wrong port, 404 on the slug) and re-run before declaring this task done.

    After the capture completes, kill the `PORT=3046` process (and `caffeinate` if started) and confirm via `lsof -i :3046` that nothing is still listening.
  </action>
  <verify>
    <automated>node -e "const d=require('./.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/lh-phase31-pre.json'); const routes=Object.keys(d); if (routes.length!==4) throw new Error('expected 4 routes, got '+routes.length); for (const r of routes) { if (d[r].error) throw new Error(r+' has error: '+d[r].error); } console.log('OK: 4 routes captured, no errors')"</automated>
  </verify>
  <done>`lh-phase31-pre.json` exists with exactly 4 route entries (2 real slugs x 2 locales), each with numeric performance/accessibility/best-practices/seo scores and lcpMs/cls/tbtMs — no `error` key on any route; the port-3046 server process has been killed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| script -> Local API -> Neon Postgres | The snapshot script reads (never writes) the full production database via `payload.find`/`payload.findGlobal` |
| throwaway prod server (port 3046) -> Lighthouse | A temporary, locally-bound production build serves real content to a local Lighthouse run — never exposed beyond localhost, torn down at the end of the task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-31-01 | Information Disclosure | content-humanization-snapshot.ts reading full production content | low | accept | Read-only, same script already used safely in Phase 29/30; output stays gitignored/local, never pushed |
| T-31-02 | Denial of Service | Leftover `next start` process on port 3046 after the task ends | low | mitigate | Task explicitly requires killing the process and confirming via `lsof -i :3046` before marking done |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installed this plan — reuses `next`, `payload`, and the existing `lighthouse-mobile.mjs` script already in the project |
</threat_model>

<verification>
1. `.gitignore` contains exactly one new line matching `posts-progress-batch`
2. A `pre-sweep-phase31-<timestamp>.json` file exists under `.planning/phases/29-content-humanization-safety-net/content-snapshots/`, with `collections.posts.count === 72` and `collections['case-studies'].count === 7`
3. `lh-phase31-pre.json` exists with 4 route entries, all with numeric scores, no `error` key
4. No content in `posts` or `case-studies` has been modified by this plan (read-only except for the two output artifacts)
</verification>

<success_criteria>
- `.gitignore` updated with the shared posts-progress-batch wildcard entry
- Pre-sweep snapshot (tag `pre-sweep-phase31`) captured before any Wave 2 content write
- Fresh "before" Lighthouse mobile capture exists for the 4 blog/case-studies representative routes, no errors
- Zero content mutations — this plan is measurement/setup only
</success_criteria>

<output>
Create `.planning/phases/31-content-humanization-posts-case-studies-verificaci-n-final/31-01-SUMMARY.md` when done
</output>