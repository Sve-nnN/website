---
phase: 03-cloudinary-media-spike
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [scripts/spike-cloudinary-upload.ts, scripts/fixtures/test-image.jpg, "src/app/(frontend)/[locale]/cloudinary-spike-test/page.tsx"]
autonomous: false
requirements: [MEDIA-01, MEDIA-02, MEDIA-03]

must_haves:
  truths:
    - "Un upload real vía Payload Local API contra la cuenta Cloudinary real termina con un doc.filename con forma de public_id de Cloudinary (no el filename local original con extension)"
    - "La URL publica generada (doc.url) responde 200 al hacer fetch directo"
    - "Esa URL contiene las transformaciones f_auto,q_auto"
    - "Borrar el doc via Local API borra tambien el asset en Cloudinary (fetch subsecuente ya no 200)"
    - "La misma imagen se renderiza correctamente en el navegador vía next/image sin error de host no configurado"
    - "Sin credenciales de Cloudinary, el mismo flujo de upload sigue funcionando via disco local sin romper el admin"
  artifacts:
    - path: "scripts/spike-cloudinary-upload.ts"
      provides: "Script standalone que sube, verifica URL, y borra un documento Media real contra Cloudinary"
      exports: []
    - path: "scripts/fixtures/test-image.jpg"
      provides: "Imagen de prueba real y pequeña usada por el spike script"
  key_links:
    - from: "scripts/spike-cloudinary-upload.ts"
      to: "src/payload.config.ts"
      via: "import config from '../src/payload.config' (relative import, same convention que scripts/seed-phase2.ts)"
      pattern: "import config from '\\.\\./src/payload\\.config'"
    - from: "cloudinary-spike-test/page.tsx"
      to: "next/image"
      via: "<Image src={doc.url} /> usando el remotePattern de res.cloudinary.com agregado en 03-02"
      pattern: "next/image"
---

<objective>
Prove the Cloudinary adapter works end-to-end against the real Cloudinary account (credentials already in `.env`) — not just that the code compiles. Run a real upload through Payload's Local API, confirm the resulting document has a Cloudinary-shaped filename with a working public URL carrying `f_auto,q_auto`, confirm delete actually removes the asset, and confirm that same URL renders correctly through `next/image` in a real browser (satisfying MEDIA-03's "verifiable in the generated URL" requirement). Also confirm the local-disk fallback path (MEDIA-02) still works when Cloudinary credentials are absent.

Purpose: this is the actual validation step the spike exists to produce — closing out MEDIA-01/02/03 with real evidence, not just type-checked code. The phase goal (03-CONTEXT.md) literally requires "un adapter validado contra una cuenta real de Cloudinary" — this plan is that validation, which no prior plan in this phase actually performed.
Output: a real Cloudinary upload/delete cycle proven end-to-end, a human-confirmed browser render, both clearly marked as removable test data (no permanent content added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cloudinary-media-spike/03-RESEARCH.md
@.planning/phases/03-cloudinary-media-spike/03-PATTERNS.md
@scripts/seed-phase2.ts
</context>

<interfaces>
<!-- scripts/seed-phase2.ts establishes the exact standalone-script bootstrap convention this
     repo already uses — read it in full before writing the spike script so the import style
     (relative `../src/payload.config`, not `@payload-config`) and run command (`npx tsx`) match. -->

RESEARCH.md "Fastest end-to-end spike validation" (adapted to this project's collection/field names):
```typescript
import { getPayload } from 'payload'
import config from '../src/payload.config'

const payload = await getPayload({ config })

const doc = await payload.create({
  collection: 'media',
  data: { alt: 'spike test image' },
  filePath: './scripts/fixtures/test-image.jpg',
})

console.log('Created doc:', doc)
// Expect doc.filename to look like a Cloudinary public_id (e.g. "media/test-image"),
// NOT the original local filename with extension.

const res = await fetch(doc.url!)
console.log('Public URL status:', res.status) // expect 200
console.log('Public URL:', doc.url)

await payload.delete({ collection: 'media', id: doc.id })
const res2 = await fetch(doc.url!)
console.log('After delete, status (expect 404 or Cloudinary "not found"):', res2.status)

process.exit(0)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the spike validation script and test fixture</name>
  <files>scripts/spike-cloudinary-upload.ts, scripts/fixtures/test-image.jpg</files>
  <read_first>
    scripts/seed-phase2.ts (full file, for the exact getPayload/config-import bootstrap
    convention already established in this repo); RESEARCH.md "Fastest end-to-end spike
    validation" and "Code Examples" section.
  </read_first>
  <action>
    Read `scripts/seed-phase2.ts` in full first and match its exact bootstrap idiom (relative
    import `../src/payload.config`, `getPayload({ config })`, run via `npx tsx`) rather than
    RESEARCH.md's generic `@payload-config` alias assumption. Create
    `scripts/spike-cloudinary-upload.ts` implementing: (1) `payload.create({ collection: 'media',
    data: { alt: 'spike test image — Phase 3 validation, safe to delete' }, filePath:
    './scripts/fixtures/test-image.jpg' })`, (2) log the created doc and assert/log whether
    `doc.filename` looks like a Cloudinary public_id (contains `media/` prefix, no local file
    extension survives per the corrected handleUpload) rather than the original local filename,
    (3) `fetch(doc.url)` and log the status code (expect 200) and confirm the URL string contains
    `f_auto` and `q_auto` (or `fetch_format`/`quality` equivalents baked into the URL), (4)
    `payload.delete({ collection: 'media', id: doc.id })` then `fetch(doc.url)` again and log the
    status (expect non-200, confirming the Cloudinary asset was actually removed), (5)
    `process.exit(0)`. Create `scripts/fixtures/test-image.jpg` as a small real JPEG (a few KB is
    fine) — generate it programmatically via a short one-off Node/sharp snippet (sharp is already
    a project dependency) rather than downloading from an external source, to keep the fixture
    fully reproducible offline.
  </action>
  <verify>
    <automated>test -f scripts/spike-cloudinary-upload.ts && test -f scripts/fixtures/test-image.jpg && node -e "require('fs').statSync('scripts/fixtures/test-image.jpg').size > 0 || process.exit(1)"</automated>
  </verify>
  <done>Both files exist; the fixture is a valid non-empty JPEG; the script follows this repo's established Local API bootstrap convention (verified against scripts/seed-phase2.ts).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Run the spike against the real Cloudinary account</name>
  <files>scripts/spike-cloudinary-upload.ts</files>
  <read_first>
    RESEARCH.md "Sampling Rate > Phase gate" — spike script must show 200 status on the generated
    URL and mimeType/filename reflecting a Cloudinary public_id (not the original local filename)
    before MEDIA-01 is considered done. STATE.md Blockers/Concerns — same escalation pattern used
    in Phase 1/2 and 03-02's Task 3 for commands that write to real external services/databases.
  </read_first>
  <action>
    Run `npx tsx scripts/spike-cloudinary-upload.ts` against the real Cloudinary account
    (credentials already live in `.env`, confirmed present by RESEARCH.md's Environment
    Availability check). This is a genuine production-service write — it uploads and then deletes
    one real test asset in Juan's real Cloudinary account, and creates/deletes one real row in the
    real Neon Postgres `media` table. If the permission classifier blocks this command from
    running against a real external service, STOP — do not attempt to bypass — and return a
    CHECKPOINT REACHED report; the orchestrator will obtain Juan's direct authorization in its own
    transcript, exactly as in the Phase 1/2 and 03-02 Task 3 escalations referenced in STATE.md.
    If the command runs, inspect the output: confirm `doc.filename` has the Cloudinary public_id
    shape (RESEARCH.md Pitfall 1's fix working — proves handleUpload's return value, not a
    mutated `file` param, was persisted), confirm the first fetch returns `200`, confirm the URL
    string contains `f_auto`/`q_auto` (or their `fetch_format=auto`/`quality=auto` query-string
    equivalents), and confirm the post-delete fetch returns a non-200 status (proving
    `handleDelete` actually removed the Cloudinary asset, not just the Postgres row). If any
    assertion fails, do not silently patch around it and do not mark this task done — report
    exactly what failed against which RESEARCH.md pitfall it maps to (a failure here means the
    adapter port from 03-02 has a real remaining bug, out of this plan's scope to fix — flag it
    for a follow-up plan rather than modifying 03-02's files here).
  </action>
  <verify>
    <automated>npx tsx scripts/spike-cloudinary-upload.ts 2>&1 | tee /tmp/spike-output.log | grep -q "Public URL status: 200" && grep -qE "f_auto|fetch_format" /tmp/spike-output.log</automated>
  </verify>
  <done>Script runs to completion against the real Cloudinary account; output confirms Cloudinary-shaped filename, 200 status with f_auto/q_auto present in the URL, and successful delete (non-200 on re-fetch). No leftover Cloudinary asset remains (script's own delete step cleans up after itself). Console output captured in the plan's SUMMARY as evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-confirm next/image rendering and local-disk fallback</name>
  <files>src/app/(frontend)/[locale]/cloudinary-spike-test/page.tsx</files>
  <action>
    Create a minimal temporary Server Component page at
    `src/app/(frontend)/[locale]/cloudinary-spike-test/page.tsx` that, on render, calls
    `getPayload({ config })` and `payload.create({ collection: 'media', data: { alt: 'spike
    browser test — safe to delete' }, filePath: './scripts/fixtures/test-image.jpg' })` (reusing
    the same fixture from Task 1), then renders the result with `next/image`
    (`<Image src={doc.url} alt={doc.alt} width={768} height={512} />`). This page exists solely
    to prove MEDIA-03's browser-rendering requirement and MEDIA-02's fallback path — mark it
    clearly with a code comment noting it must be deleted after this checkpoint is approved.
  </action>
  <what-built>
    Automated end-to-end proof (Task 2) that the Cloudinary adapter uploads, generates a
    transformed public URL, and deletes correctly against the real account. This checkpoint adds
    the one thing the Local API script cannot prove by itself: that `next/image` actually renders
    the Cloudinary URL correctly in a browser (MEDIA-03's "next/image renders without
    'unconfigured host' error" requirement), plus a manual confirmation that the local-disk
    fallback (MEDIA-02) still works when Cloudinary credentials are absent. A minimal temporary
    page was created at `src/app/(frontend)/[locale]/cloudinary-spike-test/page.tsx` that uploads
    one test Media doc via the Local API on render and displays it with `next/image`.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit `http://localhost:3000/es/cloudinary-spike-test` (or `/en/...`).
    2. Confirm the test image renders — no broken image icon, no Next.js "Invalid src prop ...
       hostname is not configured" console error.
    3. Open the rendered `<img>`'s `src` attribute (browser devtools) and confirm it points at
       `res.cloudinary.com` and contains `f_auto`/`q_auto` (or `fetch_format`/`quality`
       equivalents) in the URL path.
    4. Separately, to confirm MEDIA-02 (local-disk fallback): in a second terminal, temporarily
       run the dev server with the three Cloudinary env vars unset for that process only (e.g.
       `env -u CLOUDINARY_CLOUD_NAME -u CLOUDINARY_API_KEY -u CLOUDINARY_API_SECRET npm run dev
       -- -p 3001`, leaving the real `.env` file untouched) and confirm the admin at
       `http://localhost:3001/admin/collections/media/create` still accepts an upload without
       crashing (it will land on local disk instead of Cloudinary — that is the expected/correct
       fallback behavior).
    5. Once both checks pass, this task's cleanup will remove the temporary test page and its
       Media test document — nothing from this validation should remain in the codebase or the
       database afterward.
  </how-to-verify>
  <resume-signal>Type "approved" once both the Cloudinary render and the local-disk fallback are confirmed working, or describe what failed.</resume-signal>
  <verify>
    <automated>test -f "src/app/(frontend)/[locale]/cloudinary-spike-test/page.tsx" && echo exists</automated>
  </verify>
  <done>Juan has typed "approved" confirming both the next/image Cloudinary render and the local-disk fallback work; the temporary test page and its Media test document have been deleted, leaving no permanent trace of this validation in the codebase or database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Node script -> Cloudinary API (real account) / Neon Postgres (real database) | Server-to-server write (upload + delete) against a production Cloudinary account and the real `media` table, using real credentials from .env |
| Browser -> temporary spike test page -> Payload Local API (server) -> Cloudinary | Same trust boundary as production media upload flow, exercised here only with disposable test data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | scripts/spike-cloudinary-upload.ts writing to real Cloudinary account + temporary `cloudinary-spike-test` route left in the codebase | mitigate | Blocking checkpoint:human-action (Task 2) before the script runs against production credentials, with explicit stop-and-escalate instruction if the permission classifier blocks it; checkpoint:human-verify (Task 3) explicitly instructs deletion of the temporary page and its test Media document immediately after human confirmation — this route must not reach a committed, shipped state beyond this phase's validation window |
| T-03-06 | Information Disclosure | Spike script/page output logging Cloudinary URLs/filenames to console or rendering them in a browser | accept | Public `res.cloudinary.com` URLs are not secrets (media is intentionally public); no API keys or secrets are logged, only public_id-derived filenames and public URLs |
</threat_model>

<verification>
`npx tsx scripts/spike-cloudinary-upload.ts` completes with a real 200 status and f_auto/q_auto present in the generated URL, delete confirmed via non-200 re-fetch; human confirms browser rendering via `next/image` and the local-disk fallback path both work; temporary test page and its Media doc are removed after confirmation.
</verification>

<success_criteria>
MEDIA-01, MEDIA-02, and MEDIA-03 are proven true against real infrastructure (not just type-checked code): real upload lands in Cloudinary with a working public URL, real fallback to local disk works without Cloudinary creds, and the transformed URL renders correctly via next/image in an actual browser. No permanent test artifacts remain in the codebase or database after this plan completes.
</success_criteria>

<output>
Create `.planning/phases/03-cloudinary-media-spike/03-03-SUMMARY.md` when done
</output>
</content>
