---
phase: 03-cloudinary-media-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json]
autonomous: false
requirements: [MEDIA-01]

must_haves:
  truths:
    - "cloudinary and @payloadcms/plugin-cloud-storage are installed and present in package.json dependencies"
  artifacts:
    - path: "package.json"
      provides: "cloudinary + @payloadcms/plugin-cloud-storage as installed dependencies"
      contains: "\"cloudinary\""
  key_links: []
---

<objective>
Install the `cloudinary` SDK and `@payloadcms/plugin-cloud-storage` framework package, with a lightweight human legitimacy checkpoint on `cloudinary` per RESEARCH.md's Package Legitimacy Audit (tagged `[ASSUMED]` — slopcheck could not run in the research sandbox).

Purpose: unblock Wave 2 (adapter code + config wiring), which imports both packages and cannot type-check or install without them first.
Output: `cloudinary@2.10.0` and `@payloadcms/plugin-cloud-storage@3.85.2` installed and locked in package-lock.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cloudinary-media-spike/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cloudinary and @payloadcms/plugin-cloud-storage</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    RESEARCH.md "Package Legitimacy Audit" section — cloudinary is 13+ years old, 874,251
    weekly downloads, official repo github.com/cloudinary/cloudinary_npm, no postinstall
    script. Tagged [ASSUMED] only because slopcheck itself could not run in the research
    sandbox — manual verification signals are already strong.
  </read_first>
  <action>
    Run `npm install cloudinary@2.10.0 @payloadcms/plugin-cloud-storage@3.85.2` (pin the
    plugin-cloud-storage version to match this project's existing `@payloadcms/*` lockstep
    versioning at 3.85.2, per RESEARCH.md Standard Stack). Do not install any of the fallback
    community packages (`@jhb.software/payload-cloudinary-plugin`, `payload-storage-cloudinary`)
    — those are fallback-only per CONTEXT.md and REQUIREMENTS.md MEDIA-01, not installed unless
    the custom adapter hits a real blocker in Wave 2/3.
  </action>
  <verify>
    <automated>grep -c '"cloudinary"' package.json && grep -c '"@payloadcms/plugin-cloud-storage"' package.json</automated>
  </verify>
  <done>Both packages appear in package.json dependencies at the pinned versions, package-lock.json updated, `npm install` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirm cloudinary package legitimacy before relying on it</name>
  <files></files>
  <what-built>
    Installed `cloudinary@2.10.0` from the public npm registry. This package was tagged
    `[ASSUMED]` (not `[VERIFIED]`) in RESEARCH.md's Package Legitimacy Audit because slopcheck
    could not execute in the research sandbox — manual signals (age, downloads, official repo)
    are strong, but per the Package Legitimacy Gate protocol, any `[ASSUMED]`/`[SUS]` package
    install requires a blocking human-verify checkpoint before proceeding, not an automated
    approval.
  </what-built>
  <action>
    Present the npm registry stats gathered during Task 1 (age, weekly downloads, official repo
    URL) to Juan and wait for explicit confirmation before Wave 2 relies on this package.
  </action>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/cloudinary and confirm:
       - Repository link points to `github.com/cloudinary/cloudinary_npm`
       - Weekly downloads are in the hundreds of thousands
       - Latest version matches `2.10.0` (or a newer patch/minor published after 2026-07-08)
    2. Run `npm ls cloudinary @payloadcms/plugin-cloud-storage` locally and confirm both
       resolve with no `UNMET DEPENDENCY` errors.
  </how-to-verify>
  <verify>
    <automated>npm ls cloudinary @payloadcms/plugin-cloud-storage 2>&1 | grep -c UNMET || true</automated>
  </verify>
  <done>Juan has typed "approved" confirming cloudinary's legitimacy per the checkpoint verification steps above.</done>
  <resume-signal>Type "approved" to continue, or describe concerns to halt and reassess.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Untrusted third-party package code enters the build/runtime supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of `cloudinary` (flagged [ASSUMED], slopcheck unavailable) | mitigate | Blocking `checkpoint:human-verify` (Task 2) before this package is relied upon by any Wave 2 code — human confirms npmjs.com listing, official repo, download stats before proceeding |
</threat_model>

<verification>
`npm ls cloudinary @payloadcms/plugin-cloud-storage` shows both resolved with no errors; `package.json` and `package-lock.json` reflect the pinned versions.
</verification>

<success_criteria>
Both packages installed, human has confirmed `cloudinary`'s legitimacy per the checkpoint, Wave 2 can safely `import { v2 as cloudinary } from 'cloudinary'` and `import { cloudStoragePlugin } from '@payloadcms/plugin-cloud-storage'`.
</success_criteria>

<output>
Create `.planning/phases/03-cloudinary-media-spike/03-01-SUMMARY.md` when done
</output>
