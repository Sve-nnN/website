---
phase: 03-cloudinary-media-spike
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/lib/cloudinary-adapter.ts, src/payload.config.ts, src/collections/Media/index.ts, next.config.mjs, src/migrations/*]
autonomous: false
requirements: [MEDIA-01, MEDIA-02, MEDIA-03]

must_haves:
  truths:
    - "Un archivo subido con credenciales de Cloudinary configuradas es enviado a Cloudinary vía handleUpload, que retorna metadata (no muta el parametro file)"
    - "Sin credenciales de Cloudinary, el registro condicional del plugin no ocurre y Payload usa su comportamiento local-disk por defecto"
    - "La URL generada para un media de Cloudinary incluye las transformaciones f_auto,q_auto"
    - "imageSizes en la coleccion Media esta gateado por el mismo boolean hasCloudinaryCreds usado para el registro del plugin, evitando el bug de shallow-merge clobbering documentado en RESEARCH.md Pitfall 2"
    - "next/image tiene permiso explicito para optimizar imagenes servidas desde res.cloudinary.com"
    - "El cambio de forma de imageSizes en Media esta reflejado en una migracion committeada, aplicada contra la Neon Postgres real"
  artifacts:
    - path: "src/lib/cloudinary-adapter.ts"
      provides: "GeneratedAdapter custom: handleUpload (retorna filename/mimeType/filesize), handleDelete, generateFileURL (f_auto,q_auto), staticHandler (501)"
      exports: ["cloudinaryAdapter"]
    - path: "src/payload.config.ts"
      provides: "hasCloudinaryCreds gate + cloudStoragePlugin registrado condicionalmente en plugins[]"
      contains: "hasCloudinaryCreds"
    - path: "src/collections/Media/index.ts"
      provides: "imageSizes condicional: undefined cuando hasCloudinaryCreds, array de 3 sizes en fallback local"
      contains: "hasCloudinaryCreds"
    - path: "next.config.mjs"
      provides: "images.remotePatterns permite res.cloudinary.com"
      contains: "res.cloudinary.com"
  key_links:
    - from: "src/payload.config.ts"
      to: "src/lib/cloudinary-adapter.ts"
      via: "import { cloudinaryAdapter } from './lib/cloudinary-adapter'"
      pattern: "import.*cloudinaryAdapter.*cloudinary-adapter"
    - from: "src/payload.config.ts plugins[]"
      to: "cloudStoragePlugin collections.media.adapter"
      via: "cloudStoragePlugin({ collections: { media: { adapter: cloudinaryAdapter, ... } } })"
      pattern: "cloudStoragePlugin"
    - from: "src/collections/Media/index.ts"
      to: "src/payload.config.ts"
      via: "both independently compute hasCloudinaryCreds from CLOUDINARY_CLOUD_NAME/CLOUDINARY_API_KEY/CLOUDINARY_API_SECRET — must stay identical"
      pattern: "CLOUDINARY_CLOUD_NAME.*CLOUDINARY_API_KEY.*CLOUDINARY_API_SECRET"
---

<objective>
Port the Cloudinary custom storage adapter from the reference repo (`github.com/Sahitya1707/payload-cloudinary`), fixed for `@payloadcms/plugin-cloud-storage@3.85.2`'s `afterChange`-hook architecture (RESEARCH.md Pitfalls 1-3), and wire it into `payload.config.ts` gated by env-var presence. Gate `Media`'s `imageSizes` off the same flag to avoid the shallow-merge metadata-clobbering bug (Pitfall 2), relying on Cloudinary's own URL-transformation sizing instead. Allow `next/image` to optimize from `res.cloudinary.com`. Since this changes `Media`'s upload/imageSizes shape, generate and apply the required schema migration against the real Neon Postgres database.

Purpose: deliver the actual storage-adapter code satisfying MEDIA-01/MEDIA-02/MEDIA-03, with the two real bugs found in RESEARCH.md already fixed rather than naively ported.
Output: working, env-gated Cloudinary adapter wired into the collection and config, migration committed and applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cloudinary-media-spike/03-RESEARCH.md
@.planning/phases/03-cloudinary-media-spike/03-PATTERNS.md
</context>

<interfaces>
<!-- Exact target code from RESEARCH.md Pattern 3 / PATTERNS.md, corrected per Pitfalls 1-3.
     Executor should use these directly — no further design decisions needed. -->

From `@payloadcms/plugin-cloud-storage/types` (existing package, installed in 03-01):
```typescript
export type HandleUpload = (args: {
  file: { filename: string; buffer: Buffer; mimeType: string; filesize: number }
  collection: CollectionConfig
  data: Record<string, unknown>
  req: PayloadRequest
  clientUploadContext?: unknown
}) => Promise<Partial<FileData & TypeWithID>> | Partial<FileData & TypeWithID> | void

export type HandleDelete = (args: {
  collection: CollectionConfig
  doc: Record<string, unknown>
  filename: string
  req: PayloadRequest
}) => Promise<void> | void
```

Current full `src/collections/Media/index.ts` (24 lines, to be modified):
```typescript
import type { CollectionConfig } from 'payload'

export const Media: CollectionConfig = {
  slug: 'media',
  access: {
    read: () => true,
  },
  upload: {
    mimeTypes: ['image/*'],
    imageSizes: [
      { name: 'thumbnail', width: 300, height: undefined },
      { name: 'card', width: 768, height: undefined },
      { name: 'hero', width: 1600, height: undefined },
    ],
  },
  fields: [
    {
      name: 'alt',
      type: 'text',
      required: true,
      localized: true,
    },
  ],
}
```

Current `src/payload.config.ts` plugins array (lines 59-72, to be extended):
```typescript
plugins: [
  seoPlugin({ /* ... */ }),
  redirectsPlugin({ /* ... */ }),
],
```

Current `next.config.mjs` (full file, to be modified):
```javascript
import { withPayload } from '@payloadcms/next/withPayload'
import createNextIntlPlugin from 'next-intl/plugin'

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')

const nextConfig = {
  output: 'standalone',
  images: {
    remotePatterns: [],
  },
}

export default withPayload(withNextIntl(nextConfig))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the corrected Cloudinary storage adapter</name>
  <files>src/lib/cloudinary-adapter.ts</files>
  <read_first>
    RESEARCH.md "Code Examples > Full Ported Adapter" and "Pitfalls 1-3"; PATTERNS.md
    "src/lib/cloudinary-adapter.ts" section for the exact corrected code template.
  </read_first>
  <action>
    Create `src/lib/` (does not exist yet) and `src/lib/cloudinary-adapter.ts`. Configure
    `cloudinary.config({ cloud_name: process.env.CLOUDINARY_CLOUD_NAME, api_key:
    process.env.CLOUDINARY_API_KEY, api_secret: process.env.CLOUDINARY_API_SECRET, secure:
    true })` — use `CLOUDINARY_CLOUD_NAME` exactly (per RESEARCH.md Pitfall 3, NOT
    `CLOUDINARY_NAME` from the reference repo). Export `cloudinaryAdapter` as a factory function
    returning an object with `name: 'cloudinary-adapter'`. Implement `handleUpload({ file })` to
    Promise-wrap `cloudinary.uploader.upload_stream({ resource_type: 'auto', public_id:
    \`media/${file.filename.replace(/\.[^/.]+$/, '')}\`, overwrite: false, use_filename: true },
    callback)`, call `.end(file.buffer)`, and — critically — RETURN `{ filename:
    uploadResult.public_id, mimeType: uploadResult.format, filesize: uploadResult.bytes }` from
    the async function (do NOT mutate the `file` parameter — RESEARCH.md Pitfall 1: mutation is
    a no-op under the 3.85.2 afterChange-hook architecture). Implement `handleDelete({ filename
    })` calling `cloudinary.uploader.destroy(\`media/${filename.replace(/\.[^/.]+$/, '')}\`)`
    inside try/catch with `console.error` on failure (matches reference repo's error-swallow
    convention — acceptable for a spike). Implement `generateFileURL(filename: string)` returning
    `cloudinary.url(\`media/${filename}\`, { secure: true, fetch_format: 'auto', quality: 'auto'
    })` — this satisfies MEDIA-03's `f_auto,q_auto` requirement. Implement `staticHandler()`
    returning `new Response('Not implemented', { status: 501 })` (Cloudinary serves public URLs
    directly, no server-side static serving needed — RESEARCH.md Pitfall 4 confirms this is a
    type-safe minimal implementation despite the fuller `StaticHandler` signature). Use the
    `media/` folder prefix consistently across all three methods (handleUpload, handleDelete,
    generateFileURL) per CONTEXT.md's "keep unless reason against" instruction — a mismatch
    would silently target the wrong Cloudinary asset.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^$' | grep -c 'cloudinary-adapter.ts' || true</automated>
  </verify>
  <done>src/lib/cloudinary-adapter.ts exists, exports cloudinaryAdapter, handleUpload returns (not mutates) metadata, type-checks with zero errors attributed to this file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire conditional plugin registration, gate Media imageSizes, allow Cloudinary in next/image</name>
  <files>src/payload.config.ts, src/collections/Media/index.ts, next.config.mjs</files>
  <read_first>
    RESEARCH.md "Pattern 1: Conditional Plugin Registration" and "Open Question 1"; PATTERNS.md
    sections for `src/payload.config.ts`, `src/collections/Media/index.ts`, and
    `next.config.mjs` — all three show the existing file content plus the exact diff to apply.
  </read_first>
  <action>
    In `src/payload.config.ts`: import `cloudStoragePlugin` from
    `@payloadcms/plugin-cloud-storage` and `cloudinaryAdapter` from `./lib/cloudinary-adapter`.
    Compute `const hasCloudinaryCreds = Boolean(process.env.CLOUDINARY_CLOUD_NAME &&
    process.env.CLOUDINARY_API_KEY && process.env.CLOUDINARY_API_SECRET)` near the top of the
    file (alongside the existing `filename`/`dirname` computation at lines 23-24), matching this
    file's existing env-gate convention (`process.env.DATABASE_URI`, `process.env.RESEND_API_KEY
    || ''`). Extend the `plugins` array with
    `...(hasCloudinaryCreds ? [cloudStoragePlugin({ collections: { media: { adapter:
    cloudinaryAdapter, disableLocalStorage: true, generateFileURL: ({ filename }) =>
    cloudinaryAdapter().generateFileURL(filename) } } })] : [])` — spread pattern is type-safe in
    strict TS, no cast needed. In `src/collections/Media/index.ts`: add the identical
    `hasCloudinaryCreds` boolean (module-level, same three env var names — RESEARCH.md flags this
    intentional duplication as a spike-scoped risk, not worth extracting to a shared constant for
    a single-collection spike) and change `imageSizes` to `hasCloudinaryCreds ? undefined : [
    {name:'thumbnail',width:300,height:undefined}, {name:'card',width:768,height:undefined},
    {name:'hero',width:1600,height:undefined} ]` — this is the RESEARCH.md Pitfall 2 fix: disable
    Payload's local sharp-based imageSizes when Cloudinary is active to avoid the shallow-merge
    metadata-clobbering bug across parallel per-size handleUpload calls, and add a one-line code
    comment noting this boolean must stay identical to the one in `payload.config.ts` (same class
    of dual-definition risk as the existing `defaultLocale` comment in that file). In
    `next.config.mjs`: replace the empty `remotePatterns: []` with `remotePatterns: [{ protocol:
    'https', hostname: 'res.cloudinary.com', pathname: '/**' }]` and remove the stale "add
    Cloudinary hostname pattern in Phase 3" comment (this change fulfills it).
  </action>
  <verify>
    <automated>grep -c 'hasCloudinaryCreds' src/payload.config.ts src/collections/Media/index.ts && grep -c 'res.cloudinary.com' next.config.mjs</automated>
  </verify>
  <done>payload.config.ts registers cloudStoragePlugin only when hasCloudinaryCreds is true; Media's imageSizes is undefined when hasCloudinaryCreds, the 3-size array otherwise; next.config.mjs allows res.cloudinary.com in images.remotePatterns.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Generate and apply schema migration for Media imageSizes change</name>
  <files>src/migrations/*</files>
  <read_first>
    STATE.md Blockers/Concerns — same escalation pattern used in Phase 1 (01-10-PLAN.md) and
    Phase 2 (02-02-PLAN.md): `payload migrate:create` + `payload migrate` against the real Neon
    UNPOOLED `DATABASE_URI` already present in `.env`.
  </read_first>
  <action>
    Run `npx payload migrate:create` to generate a migration reflecting the `imageSizes` shape
    change on the `media` collection (conditional imageSizes changes the columns Payload expects
    for size variants). Review the generated migration file under `src/migrations/` for
    correctness (it should only affect `media`-related schema, not any other collection). Then
    run `npx payload migrate` against the real Neon Postgres (the connection string in `.env` is
    already the direct/unpooled one, matching the pattern established in 01-10 and 02-02). If the
    permission classifier blocks either command from running against production, STOP — do not
    attempt to bypass — and return a CHECKPOINT REACHED report; the orchestrator will obtain
    Juan's direct authorization in its own transcript, exactly as in the Phase 1/2 escalations
    referenced in STATE.md.
  </action>
  <verify>
    <automated>ls src/migrations/*.ts | tail -1</automated>
  </verify>
  <done>New migration file exists under src/migrations/, committed, and successfully applied against the real Neon Postgres database (no pending migrations reported by `npx payload migrate:status`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload admin/Local API (authenticated editor) -> Cloudinary API | Server-to-server call carrying CLOUDINARY_API_SECRET; only reachable from Node process, never client bundle |
| Browser -> res.cloudinary.com CDN | Public, unauthenticated read of already-uploaded media assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | src/lib/cloudinary-adapter.ts (CLOUDINARY_API_SECRET) | mitigate | Adapter module only imported from payload.config.ts (server-only context, never a client component); no NEXT_PUBLIC_ prefix on any of the three Cloudinary env vars — verified present in .env without that prefix |
| T-03-02 | Tampering | Media.upload (file type bypass via crafted Content-Type) | mitigate | Existing `mimeTypes: ['image/*']` restriction on Media collection left unchanged; Cloudinary's `resource_type: 'auto'` in handleUpload does not widen or bypass Payload's own upload validation, which runs before handleUpload fires |
| T-03-03 | Denial of Service | Cloudinary account (upload volume / storage cost runaway) | accept | Media creation requires authenticated Payload admin access (Users collection auth); public unauthenticated upload is not exposed. Account-level Cloudinary upload presets/limits are out of this spike's scope — flagged to Juan for future hardening |
| T-03-04 | Information Disclosure | Media.access.read: () => true + Cloudinary's inherently-public URLs | accept | Pre-existing, intentional public-read policy for a public portfolio site; Media collection has no draft/unpublished-only concept, so there is no accidental exposure surface beyond what was already true on local disk |
</threat_model>

<verification>
`npx tsc --noEmit` passes with no errors in the four modified/created files; `grep -c 'hasCloudinaryCreds'` returns >=1 in both payload.config.ts and Media/index.ts; migration applied cleanly against real Neon Postgres with `npx payload migrate:status` showing no pending migrations.
</verification>

<success_criteria>
Adapter code, config wiring, and Media collection gating are all in place and type-check cleanly; the schema change is committed and live against the real database, ready for Wave 3's end-to-end validation.
</success_criteria>

<output>
Create `.planning/phases/03-cloudinary-media-spike/03-02-SUMMARY.md` when done
</output>
