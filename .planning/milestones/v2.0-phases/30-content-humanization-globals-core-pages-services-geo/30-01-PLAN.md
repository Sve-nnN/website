---
phase: 30-content-humanization-globals-core-pages-services-geo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/humanize-globals-header-footer.ts
  - scripts/humanize-lean-collections.ts
  - .planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase30-*.json (generated, not hand-written)
autonomous: true
requirements: [VOICE-06, VOICE-07]

must_haves:
  truths:
    - "A pre-sweep snapshot JSON exists, tagged pre-sweep-phase30, taken before any rewrite in this plan runs"
    - "Header.ctaButton.label reads a real, distinct, humanized value in es (not the leftover en default 'Get in Touch') and in en, both consistent with the VOICE-PROFILE's collaborative-CTA rule"
    - "Footer.legalLinks has exactly 3 entries (Privacidad/Privacy, Términos/Terms, Sitemap) with both es and en labels present and non-empty on all 3 — the previously-missing en value for the sitemap entry is filled in"
    - "Header.navItems and Footer.columns/dynamicColumns/copyrightText read in Juan's calibrated voice in both locales, with no em dash and no AI-writing tells"
    - "Authors.bio, Testimonials.role/testimonial, SpeakingEvents.title/description/role, Categories.title/description are rewritten in Juan's voice in both locales, preserving all factual credentials/numbers verbatim"
    - "Clientes is confirmed untouched (documented no-op, every field is a non-localized proper noun/URL)"
    - "No meta.title/meta.description/targetKeyword field is touched anywhere in this plan"
  artifacts:
    - path: "scripts/humanize-globals-header-footer.ts"
      provides: "Rewrites Header.navItems/ctaButton and Footer.columns/dynamicColumns/legalLinks/copyrightText in both locales, id-reuse safe"
      contains: "updateGlobal"
    - path: "scripts/humanize-lean-collections.ts"
      provides: "Rewrites Authors/Testimonials/SpeakingEvents/Categories editorial fields in both locales"
      contains: "payload.update"
    - path: ".planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase30-*.json"
      provides: "Pre-rewrite baseline snapshot for Phase 31 diffing"
  key_links:
    - from: "scripts/humanize-globals-header-footer.ts"
      to: "src/globals/Header/index.ts, src/globals/Footer/index.ts"
      via: "payload.updateGlobal({ slug: 'header'|'footer', locale, data })"
      pattern: "updateGlobal"
    - from: "scripts/humanize-lean-collections.ts"
      to: "src/collections/Authors|Testimonials|SpeakingEvents|Categories/index.ts"
      via: "payload.find + payload.update({ collection, id, locale, data })"
      pattern: "payload.update"
---

<objective>
Take the mandatory pre-sweep content snapshot, then rewrite the real editorial copy of Header, Footer, Authors, Testimonials, SpeakingEvents, and Categories in Juan's calibrated voice, in both locales (es/en), via Payload Local API against the real Neon Postgres. Fix the two real locale-parity bugs PATTERNS.md found along the way: Header's `ctaButton.label` stuck on the English default in both locales, and Footer's 3rd `legalLinks` entry missing its `en` value entirely.

Purpose: This is the lowest-risk, smallest-surface content group in the v1.6 Track B humanization sweep — validates the id-reuse/locale-write discipline before touching pages with nested block layouts (Plan 30-02/30-03).
Output: Two new one-off scripts under `scripts/`, applied once against production; a pre-sweep snapshot JSON for later diffing; Clientes and Llms explicitly confirmed as no-ops (no code needed for either).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-content-humanization-globals-core-pages-services-geo/30-CONTEXT.md
@.planning/phases/30-content-humanization-globals-core-pages-services-geo/30-PATTERNS.md
@.planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md
@.planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md
@research/voice-sample-juan.md
@CLAUDE.md
</context>

<interfaces>
<!-- Existing global configs — exact field shapes the rewrite script must respect. -->

From src/globals/Header/index.ts (full file, relevant fields):
```typescript
fields: [
  { name: 'logo', type: 'upload', relationTo: 'media' },
  { name: 'navItems', type: 'array', fields: [link({ appearances: false })] }, // link().label is localized text, link().url is NOT
  {
    name: 'ctaButton',
    type: 'group',
    fields: [
      { name: 'label', type: 'text', localized: true, defaultValue: 'Get in Touch' },
      { name: 'href', type: 'text', defaultValue: '/contact' }, // NOT localized, do not touch
    ],
  },
]
```

From src/globals/Footer/index.ts (full file, relevant fields):
```typescript
fields: [
  { name: 'columns', type: 'array', fields: [{ name: 'title', type: 'text', localized: true }, linkGroup(...)] },
  { name: 'dynamicColumns', type: 'array', fields: [{ name: 'title', type: 'text', localized: true }, { name: 'source', type: 'select' }, { name: 'limit', type: 'number' }] },
  { name: 'socialLinks', type: 'array', fields: [{ name: 'platform', type: 'select' }, { name: 'url', type: 'text' }] }, // NOT localized, do not touch
  { name: 'legalLinks', type: 'array', fields: [{ name: 'label', type: 'text', localized: true, required: true }, { name: 'href', type: 'text', required: true }] }, // href NOT localized
  // copyrightText: text, localized: true (below legalLinks in the file)
]
```

From scripts/seed-header-footer-content.ts (full file, 117 lines) — the exact `updateGlobal` per-locale call shape to reuse:
```typescript
await payload.updateGlobal({ slug: 'header', locale: 'es', data: { navItems: navItemsEs } })
await payload.updateGlobal({ slug: 'header', locale: 'en', data: { navItems: navItemsEn } })
// Same shape for footer: data: { columns, dynamicColumns, socialLinks, legalLinks, copyrightText }
```
This script is stale (4 nav items, missing the live 5th `/services` entry; footer legalLinks only has 2 entries, missing the live 3rd `/sitemap.html` entry) — do NOT copy its literal content values, only its call shape and import pattern (`import { getPayload } from 'payload'; import config from '../src/payload.config'`).

From scripts/seed-legal-pages.ts (id-reuse pattern, lines 191-236, applies conceptually to any array with items you're editing in place):
```typescript
// Fetch existing ids first via locale:'all' or a per-locale read, then echo
// them back on every locale write — Payload full-replaces array fields on
// update, so writing a new array without the original per-item `id`s
// creates duplicate rows and orphans the other locale's already-saved data.
```

From src/collections/Authors/index.ts, Testimonials/index.ts, SpeakingEvents/index.ts, Categories/index.ts (full files, read during 29-FIELD-AUDIT.md and 30-PATTERNS.md) — fields to touch:
```
Authors: jobTitle (text, localized), bio (textarea, localized), credentials[].label (localized), expertise[].topic (localized), education[].degree/institution/description (localized), experience[].company/role/description (localized)
Testimonials: role (text, localized, required), testimonial (textarea, localized, required) — NOT name/company (proper nouns, non-localized)
SpeakingEvents: title (localized, required), description (textarea, localized), role (text, localized) — NOT location (likely city/proper noun, confirm live value before deciding) or coSpeakers[].name (proper noun) or link/date/attendeeCount
Categories: title (text, localized, required), description (textarea, localized)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the mandatory pre-sweep snapshot</name>
  <files>.planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase30-*.json (generated by existing script, not hand-written)</files>
  <read_first>
    scripts/content-humanization-snapshot.ts (full file — do NOT modify, run as-is)
  </read_first>
  <action>
    Run the existing snapshot script exactly as documented in its own header comment, BEFORE any rewrite task in this plan (or any Wave 2 plan) touches the database:

    `node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag pre-sweep-phase30`

    Do not modify this script. Confirm it exits 0 and writes a JSON file under `.planning/phases/29-content-humanization-safety-net/content-snapshots/` with the `pre-sweep-phase30` tag prefix (the script hardcodes this directory intentionally per PATTERNS.md — do not redirect output to Phase 30's own directory).
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag pre-sweep-phase30 && ls .planning/phases/29-content-humanization-safety-net/content-snapshots/pre-sweep-phase30-*.json</automated>
  </verify>
  <done>Script exits 0; a pre-sweep-phase30-<timestamp>.json file exists under the Phase 29 content-snapshots directory containing full es/en field values for all 9 collections and 3 globals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Humanize Header and Footer globals, fix the 2 real bugs</name>
  <files>scripts/humanize-globals-header-footer.ts</files>
  <read_first>
    src/globals/Header/index.ts, src/globals/Footer/index.ts (full files)
    scripts/seed-header-footer-content.ts (call-shape analog only, values are stale)
    ~/.claude/skills/humanizer/SKILL.md
    research/voice-sample-juan.md
    .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md ("Globals y navegación" section — breve, directo, CTA colaborativo)
  </read_first>
  <action>
    Create `scripts/humanize-globals-header-footer.ts` (same import/run-command convention as `seed-header-footer-content.ts`: `import { getPayload } from 'payload'; import config from '../src/payload.config'`, run with `node --env-file=.env node_modules/.bin/tsx scripts/humanize-globals-header-footer.ts`).

    First, read the LIVE current state of both globals via `payload.findGlobal({ slug: 'header', locale: 'all' })` and `payload.findGlobal({ slug: 'footer', locale: 'all' })` — do not trust the stale seed script's hardcoded arrays. Confirmed live shape: Header has 5 `navItems` (Blog, Casos de éxito/Case Studies, Autores/Authors, Contacto/Contact, Servicios/Services); Footer has a 3rd `legalLinks` entry (`/sitemap.html`) beyond Privacidad/Privacy and Términos/Terms.

    Capture each array item's existing `id` from the `locale: 'all'` read (ids are shared across locales, not per-locale) and echo them back on every per-locale write so no duplicate rows are created.

    Humanize, calibrated against `research/voice-sample-juan.md` per the humanizer skill (short-copy context: brief, direct, per VOICE-PROFILE's "sin espacio para desarrollar voz" guidance — this is navigation/CTA copy, not prose):
    - `navItems[].link.label` in both locales (keep meaning: Blog, Case studies, Authors, Contact, Services — polish only, no anglicisms, no em dash)
    - `ctaButton.label`: fix the real bug here — write a genuinely distinct, humanized es value ("Hablemos" or an equally collaborative alternative per VOICE-PROFILE's CTA rule, not "Contáctame ahora"/imperative) and a distinct en value (e.g. "Let's talk" — collaborative, not "Contact me now"). Confirm after writing that es and en values differ and neither equals the leftover default "Get in Touch" in the es locale.
    - `columns[].title`, `dynamicColumns[].title`, `copyrightText` in both locales
    - `legalLinks[].label` in both locales for ALL 3 entries — write both an `es` and `en` label for the 3rd (`/sitemap.html`) entry, since `en` is currently completely missing for it. Do not invent a 4th entry or remove any.

    Do NOT touch: `navItems[].link.url`, `ctaButton.href`, `socialLinks[].url`, `legalLinks[].href` (all non-localized URLs, correct as-is per 29-FIELD-AUDIT.md).

    Call `payload.updateGlobal({ slug: 'header'|'footer', locale, data })` once per locale (es then en), passing the full array with reused ids, same shape as `seed-header-footer-content.ts`.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/humanize-globals-header-footer.ts</automated>
    <manual>Confirm via a follow-up read (payload.findGlobal({ slug: 'header', locale: 'all' }) and payload.findGlobal({ slug: 'footer', locale: 'all' })) that ctaButton.label.es differs from ctaButton.label.en and neither equals the stale "Get in Touch" default in es, and that legalLinks[2].label has both es and en populated.</manual>
  </verify>
  <done>Script exists and, when run, humanizes Header navItems/ctaButton and Footer columns/dynamicColumns/legalLinks/copyrightText in both locales; ctaButton.label.es is a genuinely distinct, collaborative-tone value (not "Get in Touch"); legalLinks[2] (sitemap) has both es and en labels populated; no non-localized URL field was modified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Humanize lean collections (Authors, Testimonials, SpeakingEvents, Categories); confirm Clientes no-op</name>
  <files>scripts/humanize-lean-collections.ts</files>
  <read_first>
    src/collections/Authors/index.ts, src/collections/Testimonials/index.ts, src/collections/SpeakingEvents/index.ts, src/collections/Categories/index.ts, src/collections/Clientes/index.ts (full files)
    ~/.claude/skills/humanizer/SKILL.md
    research/voice-sample-juan.md
    .planning/phases/29-content-humanization-safety-net/29-VOICE-PROFILE.md ("referencia positiva" bio sample — already-correct voice reference)
    .planning/phases/29-content-humanization-safety-net/29-FIELD-AUDIT.md Action Needed #4 (SpeakingEvents.location)
  </read_first>
  <action>
    Create `scripts/humanize-lean-collections.ts` (same import/run convention as other seed scripts). For each collection, `payload.find({ collection, limit: 0 })` to get all doc ids, then loop `for (const locale of ['es','en'])` calling `payload.update({ collection, id, locale, data })`.

    Fields to rewrite per collection (per 29-FIELD-AUDIT.md, confirmed against source configs):
    - `Authors`: `jobTitle`, `bio` — the live ES bio is already close to the calibrated voice (per PATTERNS.md, it's the "referencia positiva" sample itself); polish rhythm only, do not lose any credential/number/fact. The live EN bio reads more compressed/listy than the ES ("I build... I conduct... and help...") — rewrite it to carry the same mixed long/short rhythm as the ES version per VOICE-PROFILE's explicit rule against a more "polished/corporate" EN register. For `credentials[].label`, `expertise[].topic`, `education[].degree/institution/description`, `experience[].company/role/description`: if editing in place with the same array length/order, fetch existing ids first via a `locale: 'all'` read and echo them back per item on each locale write (same discipline as Task 2).
    - `Testimonials`: `role`, `testimonial` only — do NOT touch `name`/`company` (proper nouns, non-localized).
    - `SpeakingEvents`: `title`, `description`, `role`. Before deciding on `location`, read its live value with a quick `payload.find` — if it's a city/proper noun (as 29-FIELD-AUDIT.md's Action Needed #4 expects), leave it untouched and note this confirmation in the script's own header comment. Do NOT touch `coSpeakers[].name`, `link`, `date`, `attendeeCount`, `flyer`.
    - `Categories`: `title`, `description` for every category doc.
    - `Clientes`: do NOT create any update logic for this collection — every field (`name`, `websiteUrl`) is a non-localized proper noun/URL, confirmed correct as-is in 30-PATTERNS.md. Add a one-line comment in the script noting Clientes is an intentional no-op for this phase, so the omission reads as a decision, not a gap.

    Humanize every rewritten string calibrated against `research/voice-sample-juan.md` via the humanizer skill: preserve all quantified facts (years of experience, percentages, client counts) verbatim, keep first person where the field is prose (`bio`, `description`), keep short fields (`jobTitle`, `role`, `topic`) brief and unpolished-sounding rather than corporate.
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/humanize-lean-collections.ts</automated>
  </verify>
  <done>Script exists and, when run, rewrites Authors.jobTitle/bio(+arrays), Testimonials.role/testimonial, SpeakingEvents.title/description/role, and Categories.title/description in both locales in Juan's calibrated voice with no facts lost; Clientes is untouched by design; no meta/SEO field is touched anywhere in this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| script -> Local API -> Neon Postgres | These scripts write directly to the real production database via `payload.updateGlobal`/`payload.update`, bypassing any HTTP-layer access control (Local API runs server-side with full trust) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-30-01 | Tampering | scripts/humanize-*.ts writing to production Neon | mitigate | Task 1's pre-sweep snapshot runs first so any bad write is diffable/recoverable; all writes are content-only field updates (no schema change, no DROP/DELETE), consistent with CLAUDE.md's Database Safety non-destructive-write rule |
| T-30-02 | Tampering | Array full-replace on updateGlobal/update orphaning the other locale | mitigate | Every array write in Task 2/3 fetches existing item ids first (locale: 'all' read) and echoes them back per-locale, same discipline as the 2026-07-12 incident fix |
| T-30-03 | Repudiation | No audit trail of what text changed from what | accept | Pre-sweep snapshot (Task 1) plus Phase 31's formal post-sweep diff (VOICE-07 close) together provide the audit trail; not duplicated here |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new package installed this plan — reuses `payload` Local API already in the project |
</threat_model>

<verification>
1. `node --env-file=.env node_modules/.bin/tsx scripts/content-humanization-snapshot.ts --tag pre-sweep-phase30` exits 0 and produces a snapshot file, run before Task 2/3
2. After Task 2: `payload.findGlobal({ slug: 'header', locale: 'all' })` shows `ctaButton.label.es` distinct from `ctaButton.label.en` and neither equal to the stale "Get in Touch" default in es; `payload.findGlobal({ slug: 'footer', locale: 'all' })` shows `legalLinks[2].label` populated for both es and en
3. After Task 3: spot-check `payload.find({ collection: 'authors' })` and `payload.find({ collection: 'testimonials' })` with `locale: 'all'` show non-empty, distinct es/en values for every rewritten field, with no quantified fact altered
4. No task in this plan touches `meta.title`, `meta.description`, or `targetKeyword` on any doc/global
</verification>

<success_criteria>
- Pre-sweep snapshot tagged `pre-sweep-phase30` exists before any content write in this plan or Wave 2 plans
- Header/Footer/Authors/Testimonials/SpeakingEvents/Categories are rewritten in Juan's calibrated voice, both locales
- Header `ctaButton.label` and Footer `legalLinks[2]` locale-parity bugs are fixed with real, distinct es/en values
- Clientes is confirmed untouched (documented no-op)
- No SEO/meta field touched
</success_criteria>

<output>
Create `.planning/phases/30-content-humanization-globals-core-pages-services-geo/30-01-SUMMARY.md` when done
</output>
