---
phase: 04-migraci-n-mongo-postgres
plan: 08
type: execute
wave: 8
depends_on: ["04-07"]
files_modified: [scripts/migrate/steps/07-redirects-and-verify.ts, .planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md]
autonomous: true
requirements: [MIGR-01, MIGR-03, MIGR-06]

must_haves:
  truths:
    - "Toda URL congelada en URL-INVENTORY.json que ya no resuelve verbatim en el backend nuevo (slug distinto o contenido inexistente) tiene una entrada 301 correspondiente en la coleccion redirects"
    - "El diff final entre el inventario congelado y las URLs reales generables desde el backend nuevo (slug por slug, por coleccion) queda documentado en 04-VERIFICATION.md, no solo en el log de consola"
    - "La remap-table final cubre el 100% (o documenta explicitamente cada excepcion) de los IDs de origen para media/authors/categories/posts/case-studies/testimonials/clientes"
  artifacts:
    - path: "scripts/migrate/steps/07-redirects-and-verify.ts"
      provides: "Diff URL-INVENTORY.json vs slugs reales del backend nuevo + creacion de redirects para deltas + reporte de cobertura de la remap-table"
    - path: ".planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md"
      provides: "Reporte final de la migracion: cobertura por coleccion, deltas de URL, redirects creados, casos needsReview/needsStructuredContent consolidados"
  key_links:
    - from: "scripts/migrate/steps/07-redirects-and-verify.ts"
      to: "@payloadcms/plugin-redirects"
      via: "payload.create({collection:'redirects', data:{from, to:{type:'custom', url: to}}})"
      pattern: "collection: 'redirects'"
---

<objective>
Cerrar la fase: comparar el inventario de URLs congelado en wave 1 contra las URLs reales que el backend migrado puede generar, crear un redirect 301 para cada URL que cambio intencionalmente, y producir el reporte de verificacion final que consolida todos los needsReview/needsStructuredContent acumulados por las waves anteriores.

Purpose: Satisfacer MIGR-06 (mapa de redirects para URLs que cambiaron) y cerrar el contrato de verificacion abierto en MIGR-01/MIGR-03 con un diff real, no solo una promesa de que "deberia coincidir".
Output: scripts/migrate/steps/07-redirects-and-verify.ts, redirects creados en el backend real, 04-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-migraci-n-mongo-postgres/URL-INVENTORY.json
@scripts/migrate/lib/remap-table.ts
@scripts/seed-phase2.ts

<redirect_shape>
El plugin @payloadcms/plugin-redirects (ya registrado en src/payload.config.ts para pages/posts/case-studies/categories/authors) expone la coleccion redirects con forma {from: string, to: {type:'custom', url: string} | {type:'reference', reference:{relationTo, value}}} -- ver el uso real en scripts/seed-phase2.ts (seccion "6. Redirect").
</redirect_shape>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff de URLs congeladas vs backend migrado + generacion de redirects</name>
  <files>scripts/migrate/steps/07-redirects-and-verify.ts</files>
  <action>
Crear scripts/migrate/steps/07-redirects-and-verify.ts: importa config desde ../../../src/payload.config, getPayload({config}).

Leer URL-INVENTORY.json. Para cada entrada con collection definido (posts, categories, authors -- pages queda fuera de alcance de esta fase), construir la URL esperada en el backend nuevo usando el mismo esquema de locale-prefix ("es" sin prefijo, "en" con /en) y el slug real del documento migrado (query payload.find por el remap-table -> ID -> leer el doc -> su slug actual). Si el slug resultante difiere del original (no deberia pasar dado que todo el pipeline preserva slugs verbatim, pero verificar de todas formas como red de seguridad), o si la entrada de inventario no tiene documento migrado correspondiente (ID sin mapear), clasificar como delta. Para cada delta, crear una entrada en la coleccion redirects: from = path original congelado, to = {type:'custom', url: <path nuevo real, o '/' si no hay destino equivalente>}. Idempotente: verificar con payload.find({collection:'redirects', where:{from:{equals: path}}}) antes de crear.

Generar tambien un resumen de cobertura de remap-table: por cada coleccion (media, authors, categories, posts, case-studies, testimonials, clientes), contar entradas del dump vs entradas de la remap-table y calcular el porcentaje.
  </action>
  <verify>
    <automated>npx tsx scripts/migrate/steps/07-redirects-and-verify.ts</automated>
  </verify>
  <done>El script corre de punta a punta; cualquier URL congelada que no resuelve 1:1 en el backend nuevo tiene un redirect 301 real creado en la coleccion redirects; la cobertura de remap-table por coleccion queda calculada.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir el reporte final de verificacion de la fase</name>
  <files>.planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md</files>
  <action>
Escribir .planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md consolidando: (1) tabla de cobertura de remap-table por coleccion (conteo dump vs migrado, % ), (2) lista de deltas de URL detectados y sus redirects creados (o "ninguno" si el diff salio limpio), (3) consolidado de needsReview de wave 4 (testimonials/clientes con datos faltantes), (4) consolidado de needsStructuredContent de wave 6 y 7 (case studies -- incluyendo los fold-in de Works -- pendientes de autoria editorial de kpis/challenge/solution/results), (5) confirmacion explicita de que la coleccion works NO existe en el esquema nuevo y que el sitio viejo no tenia rutas /works que preservar (por lo tanto no aplica ningun redirect para Works). Este archivo es el entregable que Juan revisa para decidir que trabajo editorial manual queda pendiente antes de Phase 5.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md && grep -q "needsStructuredContent" .planning/phases/04-migraci-n-mongo-postgres/04-VERIFICATION.md</automated>
  </verify>
  <done>04-VERIFICATION.md existe con las 5 secciones descritas, listo para que Juan revise el estado real de la migracion y el trabajo editorial pendiente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Script -> Postgres nuevo (Local API) | Crea redirects reales en produccion basados en un diff automatizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | Re-ejecucion duplica redirects | mitigate | Verificacion `find` por `from` antes de cada `create` |
| T-04-17 | Repudiation | Deltas de URL no detectados quedan sin redirect, causando 404s silenciosos en produccion futura | mitigate | 04-VERIFICATION.md deja constancia explicita de cada delta encontrado y su redirect; ausencia de deltas tambien se documenta explicitamente (no se omite la seccion) |
</threat_model>

<verification>
Correr los 2 comandos automated de las tasks. Revisar manualmente 04-VERIFICATION.md para confirmar que las 5 secciones tienen contenido real (no placeholders vacios).
</verification>

<success_criteria>
MIGR-06 satisfecho: todo delta de URL real tiene un redirect. El reporte final consolida el estado completo de la migracion (cobertura, deltas, pendientes editoriales) como entregable verificable por Juan antes de Phase 5.
</success_criteria>

<output>
Create .planning/phases/04-migraci-n-mongo-postgres/04-08-SUMMARY.md when done
</output>
