---
phase: 10.5-typography-header-footer-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/globals/Footer/index.ts
  - src/migrations/index.ts
  - src/payload-types.ts
autonomous: true
requirements: [10.5-SC3, 10.5-SC5]

must_haves:
  truths:
    - "El global Footer tiene una nueva capacidad de columnas dinamicas (ultimos posts / ultimos case studies) editable/configurable desde el admin de Payload, ademas de las columnas manuales existentes"
    - "El cambio de schema esta commiteado como migracion generada por payload migrate:create, con push:false respetado (sin push directo a Postgres)"
  artifacts:
    - path: "src/globals/Footer/index.ts"
      provides: "Campo dynamicColumns array con sub-campos title/source/limit, conviviendo con columns/socialLinks/legalLinks existentes"
      contains: "dynamicColumns"
    - path: "src/migrations/index.ts"
      provides: "Nueva entrada de migracion que agrega las columnas de Postgres para dynamicColumns"
    - path: "src/payload-types.ts"
      provides: "Tipo Footer actualizado con dynamicColumns tras payload generate:types"
      contains: "dynamicColumns"
  key_links:
    - from: "src/globals/Footer/index.ts"
      to: "src/migrations"
      via: "payload migrate:create genera el diff de schema para el nuevo campo dynamicColumns"
      pattern: "dynamicColumns"
---

<objective>
Agregar al global Footer de Payload una nueva capacidad de contenido dinamico: columnas cuyo contenido se computa en render time desde las colecciones posts/case-studies (ultimos N por fecha), no curadas manualmente. Se modela como un array dynamicColumns con un campo source tipo select (latestPosts | latestCaseStudies). Genera y commitea la migracion correspondiente.

Purpose: Juan pidio explicitamente "ultimos 5 blog posts" y "ultimos 5 casos de estudio" como secciones dinamicas del footer, computadas desde Payload real, no hardcodeadas. Esto requiere schema nuevo, explicitamente en scope para esta fase (a diferencia de la restriccion "zero config.ts diff" de Phases 7-10).
Output: src/globals/Footer/index.ts con el nuevo campo, migracion commiteada en src/migrations/, payload-types.ts regenerado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10.5-typography-header-footer-overhaul/10.5-CONTEXT.md
@src/globals/Footer/index.ts
@src/collections/Posts/index.ts
@src/collections/CaseStudies/index.ts
</context>

<interfaces>
Patron existente de array field con sub-campos en este proyecto, a seguir para dynamicColumns.

From src/globals/Footer/index.ts (patron de array field ya usado para columns):

    {
      name: 'columns',
      type: 'array',
      admin: { initCollapsed: true },
      fields: [
        { name: 'title', type: 'text', localized: true },
        linkGroup({ appearances: false }),
      ],
    },

Notas sobre las colecciones fuente (confirmado por lectura directa del schema, no asumir otros nombres de campo):
- posts tiene campo publishedAt (type: date). Usar sort: '-publishedAt'.
- case-studies NO tiene campo publishedAt, solo timestamps automaticos de Payload (createdAt/updatedAt). Usar sort: '-createdAt'.
- Ambas colecciones tienen versions.drafts habilitado (campo _status implicito: draft | published) y controlan lectura publica via authenticatedOrPublished access, pero el Local API (payload.find) por defecto usa overrideAccess true y NO respeta ese access control. El filtro _status published debe aplicarse explicitamente en la query del componente de render (eso se hace en el plan 10.5-04, no en este plan; este plan solo define el schema).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar campo dynamicColumns al global Footer</name>
  <files>src/globals/Footer/index.ts</files>
  <action>
    Agrega un nuevo campo dynamicColumns (type array) al global Footer, ubicado despues de columns y antes de socialLinks, con admin.initCollapsed true y una description explicando que son columnas computadas en render time desde Posts/CaseStudies, no curadas manualmente. Sub-campos:
    - title (type text, localized true): encabezado editable de la columna, por ejemplo "Ultimas entradas del blog".
    - source (type select, required true, options latestPosts y latestCaseStudies): determina que coleccion alimenta la columna.
    - limit (type number, defaultValue 5, min 1, max 10): cuantos items mostrar.

    No toques columns, socialLinks, legalLinks ni copyrightText: deben permanecer exactamente como estan hoy (Juan pidio mantenerlos, per 10.5-CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && grep -q "dynamicColumns" src/globals/Footer/index.ts && grep -q "latestPosts" src/globals/Footer/index.ts && grep -q "latestCaseStudies" src/globals/Footer/index.ts && echo OK</automated>
  </verify>
  <done>El campo dynamicColumns existe en src/globals/Footer/index.ts con sub-campos title/source/limit, y los campos existentes del global no cambiaron.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generar migracion, aplicarla y regenerar payload-types.ts</name>
  <files>src/migrations/index.ts, src/payload-types.ts</files>
  <action>
    Corre payload migrate:create con un nombre descriptivo (por ejemplo phase10_5_footer_dynamic_columns) para que Payload introspeccione el diff de schema entre el estado actual de Postgres y el nuevo campo dynamicColumns del Task 1, y genere el archivo de migracion up/down bajo src/migrations/. Confirma que el archivo se registro automaticamente en src/migrations/index.ts (siguiendo el patron ya usado por las migraciones previas de este proyecto). Aplica la migracion contra la base de datos de desarrollo con payload migrate. No uses push true en ningun entorno.

    Despues de aplicar la migracion, corre payload generate:types para regenerar src/payload-types.ts y confirmar que el tipo Footer incluye ahora dynamicColumns con su forma correcta (array de objetos con title, source como union literal, limit).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && ls src/migrations | grep -i "footer" && grep -q "dynamicColumns" src/payload-types.ts && echo OK</automated>
  </verify>
  <done>Existe un archivo de migracion nuevo bajo src/migrations/ referenciando el cambio del Footer, registrado en src/migrations/index.ts, la migracion se aplico exitosamente contra Postgres, y src/payload-types.ts refleja dynamicColumns en el tipo Footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin de Payload -> Postgres | Un editor autenticado configura dynamicColumns (title/source/limit) desde /admin; el valor se persiste sin validacion adicional mas alla del select constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10.5-04 | Tampering | Migracion generada por payload migrate:create | mitigate | Migracion se revisa (diff up/down) antes de commitear y se aplica con push:false, nunca con push directo |
| T-10.5-05 | Information Disclosure | dynamicColumns.source solo permite 2 valores fijos (select), no texto libre ni relationship arbitraria | accept | El schema mismo no puede filtrar datos sensibles porque source esta constreñido a un enum; el riesgo real de fuga de borradores se mitiga en el componente de render (plan 10.5-04), no en este schema |

</threat_model>

<verification>
- payload migrate corre sin errores contra la base de datos de desarrollo.
- payload-types.ts incluye dynamicColumns en el tipo Footer.
- El admin de Payload muestra el nuevo campo dynamicColumns dentro del global Footer sin romper los campos existentes.
</verification>

<success_criteria>
SiteFooter tiene una nueva capacidad de schema para columnas de contenido dinamico (ultimos posts, ultimos case studies), respaldada por una migracion commiteada con push:false respetado, y payload-types.ts refleja el nuevo campo. ROADMAP Phase 10.5 Success Criteria 3 y 5 (parte de schema).
</success_criteria>

<output>
Create `.planning/phases/10.5-typography-header-footer-overhaul/10.5-02-SUMMARY.md` when done
</output>
