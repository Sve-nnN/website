---
phase: 10.5-typography-header-footer-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/fonts.ts
  - src/fonts/Array-Regular.woff2
  - src/app/(frontend)/[locale]/layout.tsx
  - tailwind.config.ts
  - src/components/SiteHeader.tsx
  - src/components/PostCard.tsx
  - src/components/AuthorCard.tsx
  - src/components/CaseStudyCard.tsx
  - src/components/Prose.tsx
  - src/blocks/ArchiveBlock/Component.tsx
  - src/blocks/ContactFormBlock/Component.tsx
  - src/blocks/FAQ/Component.tsx
  - src/blocks/FeaturedPostsBlock/Component.tsx
  - src/blocks/ClientLogosBlock/Component.tsx
  - src/blocks/FeaturedCaseStudiesBlock/Component.tsx
  - src/blocks/RelatedPosts/Component.tsx
  - src/blocks/TestimonialsCarousel/Component.tsx
  - src/blocks/ResultsSection/Component.tsx
  - src/blocks/Hero/Component.tsx
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
  - src/app/(frontend)/[locale]/authors/page.tsx
  - src/app/(frontend)/[locale]/terms/page.tsx
  - src/app/(frontend)/[locale]/privacy/page.tsx
  - src/app/(frontend)/[locale]/case-studies/page.tsx
  - src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
  - src/app/(frontend)/[locale]/blog/[slug]/page.tsx
  - src/app/(frontend)/[locale]/search/page.tsx
autonomous: false
requirements: [10.5-SC1]

must_haves:
  truths:
    - "El sitio ya no usa Inter ni Fraunces en ningún elemento — las cuatro familias Array/Khand/Geist Sans/Geist Mono son las únicas fuentes wireadas"
    - "Los headings secundarios (h2/h3, títulos de tarjetas, secciones) usan Khand; el display más prominente (h1/hero) usa Array; el cuerpo de texto usa Geist Sans"
  artifacts:
    - path: "src/fonts.ts"
      provides: "Módulo central de las 4 familias tipográficas (array, khand, geistSans, geistMono) con .variable CSS vars, display:swap"
      exports: ["array", "khand", "geistSans", "geistMono"]
    - path: "src/fonts/Array-Regular.woff2"
      provides: "Archivo woff2 local de Array, copiado desde auditor"
    - path: "tailwind.config.ts"
      provides: "fontFamily.sans -> Geist Sans, fontFamily.display -> Array, fontFamily.heading -> Khand, fontFamily.mono -> Geist Mono"
      contains: "font-heading"
  key_links:
    - from: "src/app/(frontend)/[locale]/layout.tsx"
      to: "src/fonts.ts"
      via: "import { array, khand, geistSans, geistMono } from '@/fonts'; className on <html>"
      pattern: "from '@/fonts'"
    - from: "tailwind.config.ts"
      to: "src/fonts.ts"
      via: "fontFamily tokens reference --font-array/--font-khand/--font-geist-sans/--font-geist-mono CSS vars emitted by .variable"
      pattern: "var\\(--font-"
---

<objective>
Reemplazar la dirección tipográfica actual (Inter + Fraunces, decisión de `05-UI-SPEC.md` ahora revertida por feedback directo de Juan) por el stack de 4 fuentes usado en el proyecto hermano `auditor`: Array (display, local woff2), Khand (headings/UI, `next/font/google`), Geist Sans (body, paquete `geist`), Geist Mono (código, paquete `geist`). Sigue el patrón de módulo de `auditor/apps/web/app/fonts.ts` casi literal.

Purpose: Juan rechazó explícitamente Inter+Fraunces ("las fuentes no me gustan") tras revisar el sitio acumulado de Phases 7-10. Esto reabre `05-UI-SPEC.md` de forma deliberada, per D-XX de `10.5-CONTEXT.md`.
Output: `src/fonts.ts`, `src/fonts/Array-Regular.woff2`, layout raíz del frontend actualizado, `tailwind.config.ts` con 4 tokens de fuente, y todo el codebase migrado del token `font-display` (usado hoy indistintamente para h1 Y h2/h3) a una distinción real: `font-display` (Array, solo el heading más prominente: h1/hero) vs `font-heading` (Khand, todo lo demás que hoy usa `text-heading`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10.5-typography-header-footer-overhaul/10.5-CONTEXT.md

<!-- Reference project — patrón de módulo de fuentes a replicar casi literal -->
@/Users/juan/Documents/Codigo/Personal/juantech/auditor/apps/web/app/fonts.ts
@/Users/juan/Documents/Codigo/Personal/juantech/auditor/apps/web/app/layout.tsx

<!-- Estado actual de este proyecto -->
@src/app/(frontend)/[locale]/layout.tsx
@tailwind.config.ts
@src/app/globals.css
</context>

<interfaces>
<!-- Contrato del módulo de fuentes a crear, calcado del de auditor. NO cambiar la forma —
     solo el path del woff2 y el hecho de que el `<html>` está en un layout con next-intl. -->

From auditor's app/fonts.ts (patrón a replicar en src/fonts.ts de este proyecto):

    import localFont from "next/font/local";
    import { Khand } from "next/font/google";
    import { GeistSans } from "geist/font/sans";
    import { GeistMono } from "geist/font/mono";

    export const array = localFont({
      src: [{ path: "./fonts/Array-Regular.woff2", weight: "400", style: "normal" }],
      variable: "--font-array",
      display: "swap",
      fallback: ["Arial Narrow", "system-ui", "sans-serif"],
    });

    export const khand = Khand({
      subsets: ["latin"],
      weight: ["400", "500", "600", "700"],
      variable: "--font-khand",
      display: "swap",
      fallback: ["Arial Narrow", "system-ui", "sans-serif"],
    });

    export const geistSans = GeistSans;
    export const geistMono = GeistMono;

Diferencia clave para este proyecto: `src/fonts.ts` vive en `src/`, no en `app/`, así que el
`path` del `localFont` debe apuntar a `./fonts/Array-Regular.woff2` relativo a `src/fonts.ts`
(es decir, el woff2 va en `src/fonts/Array-Regular.woff2`, hermano del módulo).

Current root layout to modify — `src/app/(frontend)/[locale]/layout.tsx` currently does:

    import { Fraunces, Inter } from 'next/font/google'
    const inter = Inter({ subsets: ['latin'], variable: '--font-sans', display: 'swap' })
    const fraunces = Fraunces({ subsets: ['latin'], variable: '--font-display', display: 'swap' })
    // ...
    <html lang={locale} className={`${inter.variable} ${fraunces.variable}`}>
      <body className="font-sans">

Este bloque de Inter/Fraunces debe eliminarse por completo y reemplazarse por imports desde `@/fonts`.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirmar licencia de Array y legitimidad del paquete npm `geist` antes de instalar</name>
  <action>No hay acción autónoma en este task — es un gate bloqueante de confirmación humana antes de copiar el font file o instalar el paquete geist.</action>
  <what-built>Nada todavía — este checkpoint bloquea el resto del plan a propósito, per instrucción explícita de `10.5-CONTEXT.md`: "Verify its license permits this reuse (Array is commonly distributed under SIL OFL — confirm before shipping, don't assume)".</what-built>
  <how-to-verify>
    No se pudo confirmar la licencia de Array de forma automatizada en esta sesión de planificación (búsquedas web a fuentes candidatas devolvieron 404). Dos cosas necesitan confirmación manual de Juan antes de ejecutar este plan:

    1. Licencia de Array: confirmar que el archivo `/Users/juan/Documents/Codigo/Personal/juantech/auditor/apps/web/app/fonts/Array-Regular.woff2` (21KB) tiene una licencia que permite su reuso en este proyecto (ej. SIL OFL, o cualquier licencia que Juan ya haya verificado al usarlo en `auditor`). Si Juan ya lo verificó para `auditor`, basta confirmarlo aquí — no hace falta repetir la investigación desde cero.
    2. Legitimidad del paquete `geist` (npm): visitar https://www.npmjs.com/package/geist y confirmar que es el paquete oficial de Vercel (autor `vercel`, no un typosquat), antes de que el executor lo instale como dependencia de producción.
  </how-to-verify>
  <resume-signal>Escribe "confirmado" (o indica qué corregir) para desbloquear el resto del plan.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear módulo de fuentes, copiar Array, instalar geist, wirear en el layout raíz</name>
  <files>src/fonts.ts, src/fonts/Array-Regular.woff2, package.json, src/app/(frontend)/[locale]/layout.tsx</files>
  <action>
    Copia el archivo `/Users/juan/Documents/Codigo/Personal/juantech/auditor/apps/web/app/fonts/Array-Regular.woff2` a `src/fonts/Array-Regular.woff2` en este proyecto (usa `cp`, preserva el binario tal cual, no lo regeneres).

    Instala el paquete `geist` (última versión estable, actualmente `^1.7.2`) como dependencia de producción — no dev dependency, ya que se importa en el layout que renderiza en runtime.

    Crea `src/fonts.ts` siguiendo el patrón del bloque `<interfaces>` de arriba: cuatro exports (`array`, `khand`, `geistSans`, `geistMono`), usando `localFont` para Array (peso único 400, `variable: '--font-array'`), `Khand` de `next/font/google` (pesos 400/500/600/700, `variable: '--font-khand'`), y re-exportando `GeistSans`/`GeistMono` de `geist/font/sans` y `geist/font/mono`. Usa `display: 'swap'` y los mismos fallbacks del patrón (`["Arial Narrow", "system-ui", "sans-serif"]` para Array/Khand).

    Modifica `src/app/(frontend)/[locale]/layout.tsx`: elimina el import de `Fraunces, Inter` de `next/font/google` y las constantes `inter`/`fraunces`. Importa `{ array, khand, geistSans, geistMono }` desde `@/fonts`. Aplica las cuatro `.variable` al `className` del `<html>` (join con espacio, igual que el `fontVariables` de auditor). El `<body>` mantiene `className="font-sans"` (ahora resuelve a Geist Sans vía el tailwind.config actualizado en Task 3 — este task NO toca tailwind.config, así que el build puede no aplicar bien los tokens hasta Task 3; eso es esperado y se verifica junto en Task 3).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && test -f src/fonts/Array-Regular.woff2 && test -f src/fonts.ts && grep -q '"geist"' package.json && ! grep -q "Inter\|Fraunces" "src/app/(frontend)/[locale]/layout.tsx" && echo OK</automated>
  </verify>
  <done>`src/fonts.ts` exporta las 4 fuentes, `src/fonts/Array-Regular.woff2` existe, `geist` está en `package.json` dependencies, `layout.tsx` no importa `next/font/google` para Inter/Fraunces y sí importa de `@/fonts`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mapear los 4 tokens en Tailwind y migrar el codebase a la distinción font-display / font-heading</name>
  <files>tailwind.config.ts, src/components/SiteHeader.tsx, src/components/PostCard.tsx, src/components/AuthorCard.tsx, src/components/CaseStudyCard.tsx, src/components/Prose.tsx, src/blocks/ArchiveBlock/Component.tsx, src/blocks/ContactFormBlock/Component.tsx, src/blocks/FAQ/Component.tsx, src/blocks/FeaturedPostsBlock/Component.tsx, src/blocks/ClientLogosBlock/Component.tsx, src/blocks/FeaturedCaseStudiesBlock/Component.tsx, src/blocks/RelatedPosts/Component.tsx, src/blocks/TestimonialsCarousel/Component.tsx, src/blocks/ResultsSection/Component.tsx, src/blocks/Hero/Component.tsx, src/app/(frontend)/[locale]/authors/[slug]/page.tsx, src/app/(frontend)/[locale]/authors/page.tsx, src/app/(frontend)/[locale]/terms/page.tsx, src/app/(frontend)/[locale]/privacy/page.tsx, src/app/(frontend)/[locale]/case-studies/page.tsx, src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx, src/app/(frontend)/[locale]/blog/[slug]/page.tsx, src/app/(frontend)/[locale]/search/page.tsx</files>
  <action>
    En `tailwind.config.ts`, dentro de `theme.extend.fontFamily`, cambia el mapeo actual (`sans: ['var(--font-sans)']`, `display: ['var(--font-display)']`) por: `sans: ['var(--font-geist-sans)']`, `display: ['var(--font-array)']`, `heading: ['var(--font-khand)']`, `mono: ['var(--font-geist-mono)']`. El paquete `geist` fija internamente sus propias variables `--font-geist-sans`/`--font-geist-mono` — no reutilices el nombre genérico `--font-sans` para evitar colisión con cualquier resto de la variable antigua de Inter.

    El codebase actual usa la clase compuesta `font-display text-heading` en ~20 archivos para TODOS los headings (h1 grande Y h2/h3/títulos de tarjeta pequeños) porque hasta ahora `font-display` era la única fuente de heading disponible. Ahora que existen dos roles distintos (Array = display muy prominente, Khand = headings/UI general), sepáralos así:

    - Donde la clase combinada es `font-display text-display` (el h1/hero más grande de cada página — blog/[slug], case-studies/[slug], authors/page, privacy, terms, search, la cifra grande de ResultsSection/CaseStudyCard/case-studies/[slug]) → DEJAR `font-display` tal cual (sigue siendo Array).
    - Donde la clase combinada es `font-display text-heading` (h2/h3, títulos de card, wordmark del header, sidebar titles, empty states — la gran mayoría de las ocurrencias) → CAMBIAR `font-display` por `font-heading` (pasa a ser Khand). El tamaño (`text-heading`) no cambia.

    Usa `sed` dirigido por patrón (`font-display text-heading` -> `font-heading text-heading`) sobre los archivos listados en `<files>` para hacerlo mecánico y consistente, y NO toques ninguna ocurrencia de `font-display text-display`. Antes del sed masivo, corre un grep de variantes con el orden de clases invertido (`text-heading font-display`) para no dejar casos sin migrar.

    Finalmente, corre un grep global para confirmar que no queda ninguna referencia a Inter/Fraunces en `src/`, y corre `npx tsc --noEmit` para confirmar que no hay errores de tipos ni imports rotos.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && ! grep -rq "font-display text-heading\|text-heading font-display" src --include="*.tsx" && ! grep -rlq "Inter\b\|Fraunces" src --include="*.ts" --include="*.tsx" && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>`tailwind.config.ts` mapea los 4 tokens a las 4 CSS vars correctas, ninguna ocurrencia de `font-display text-heading` queda en `src/` (todas migraron a `font-heading text-heading`), `font-display text-display` sigue intacto donde correspondía, cero referencias a Inter/Fraunces, `tsc --noEmit` pasa limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> build | `geist` se instala como dependencia nueva de producción, descargada de npm en cada install/build |
| auditor repo -> este repo | El archivo woff2 de Array se copia de otro repositorio local del propio Juan, no de una fuente externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10.5-01 | Tampering | `geist` npm package install | mitigate | Task 1 checkpoint bloqueante: confirmar en npmjs.com/package/geist que el autor es `vercel` antes de instalar |
| T-10.5-02 | Information Disclosure / IP | `Array-Regular.woff2` reuso entre proyectos | mitigate | Task 1 checkpoint bloqueante: confirmar licencia de Array antes de copiar/shippear, per instrucción explícita de 10.5-CONTEXT.md |
| T-10.5-03 | Denial of Service (build) | Fuente local mal referenciada rompe el build de Next | accept | Bajo riesgo — verificado con `tsc --noEmit` y el propio dev server tras Task 2/3 |

</threat_model>

<verification>
- `npx tsc --noEmit` pasa sin errores.
- `npm run dev` levanta sin errores de resolución de fuentes (Array/Khand/Geist).
- Grep de `Inter\b|Fraunces` en `src/` devuelve cero resultados.
- Grep de `font-display text-heading` en `src/**/*.tsx` devuelve cero resultados.
</verification>

<success_criteria>
Las cuatro familias tipográficas (Array display, Khand headings/UI, Geist Sans body, Geist Mono código) están self-hosted y wireadas, reemplazando toda referencia a Inter/Fraunces en el codebase — ROADMAP Phase 10.5 Success Criterion 1.
</success_criteria>

<output>
Create `.planning/phases/10.5-typography-header-footer-overhaul/10.5-01-SUMMARY.md` when done
</output>
