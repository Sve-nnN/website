---
phase: 10.6-header-footer-completion-mobile-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SiteHeader.tsx
autonomous: true
requirements: [UI-17]

must_haves:
  truths:
    - "SiteHeader muestra navegacion completa y visible en mobile (~375px) via menu hamburguesa/Sheet, con nav horizontal completa en desktop — ambos estados verificados por separado, no solo el desktop"
    - "header.navItems se renderiza con jerarquia visual clara (tap targets grandes en mobile, separacion de secciones, estados hover/focus visibles) en ambos contextos"
  artifacts:
    - path: "src/components/SiteHeader.tsx"
      provides: "Header con Sheet movil de jerarquia clara (tap targets >=44px, separador entre nav y CTA/locale) y nav horizontal desktop con mayor presencia visual"
      contains: "SheetContent"
  key_links:
    - from: "src/components/SiteHeader.tsx"
      to: "header.navItems"
      via: "header.navItems?.map(...) renderizado en NavigationMenu (desktop) y dentro del Sheet (mobile)"
      pattern: "navItems"
---

<objective>
Cerrar UI-17: `SiteHeader` debe sentirse completo y con navegacion visible tanto en mobile (Sheet/hamburguesa, diseñado primero) como en desktop (nav horizontal), no "simple/vacio" (queja verbatim de Juan: "el header tampoco me gusta, esta muy simple").

Hay una edicion sin commitear en `src/components/SiteHeader.tsx` (de una corrida de agente interrumpida anterior) que ya implementa la mayor parte de este trabajo: tap targets de 44px (`py-3 min-h-11`) en el Sheet movil, `font-heading` en los labels de nav, un `Separator` entre el bloque de nav links y el bloque de LocaleSwitcher+CTA, hover con fondo (`hover:bg-muted`) en mobile, `gap-8` y `hover:text-primary` en el nav de desktop, y un `border-b border-border/20` en el header sticky para que se perciba solido. Se confirmo por lectura directa que `npx tsc --noEmit` pasa limpio con este estado. Este plan AUDITA esa edicion contra el objetivo completo de UI-17, cierra cualquier gap real que quede, y la deja commiteada.

Purpose: resolver la parte de header del feedback directo de Juan, verificada mobile-first (Sheet antes que desktop).
Output: src/components/SiteHeader.tsx finalizado y commiteado, con navegacion completa visible en ambos breakpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/SiteHeader.tsx
@src/globals/Header/index.ts
</context>

<interfaces>
Estado actual confirmado de src/components/SiteHeader.tsx (uncommitted, leido directamente — NO asumido):

- `header.navItems` ya se mapea en un `NavigationMenu` horizontal (desktop, `hidden md:flex`) y tambien dentro del `Sheet` (mobile, trigger visible solo en `md:hidden`) — no hay wiring faltante, el campo ya se lee y renderiza en ambos contextos.
- El `SheetContent` movil ya tiene: un `div` de nav links con `gap-1` y cada `CMSLink` con clases `font-heading rounded-md px-3 py-3 min-h-11 flex items-center border-b-2 border-transparent hover:border-primary hover:bg-muted focus-visible:border-primary focus-visible:bg-muted`; luego un `<Separator className="opacity-30 my-6" />`; luego un `div` con `LocaleSwitcher` + `ctaButton`.
- El nav horizontal desktop ya tiene `gap-8`, cada `CMSLink` con `hover:text-primary focus-visible:text-primary` ademas del borde inferior existente.
- El `<header>` ya tiene `border-b border-border/20` ademas de `shadow-md`.
- `SheetTrigger` ya usa `Button variant="outline" size="icon" className="size-10"` (40x40px, cumple tap target minimo).
- `npx tsc --noEmit` pasa limpio con este estado (confirmado antes de planear).

Gaps NO cubiertos por la edicion existente que este plan debe revisar y cerrar si aplican:
- El wordmark de fallback (sin logo subido) usa `font-heading text-heading` — confirmar que sigue siendo legible/con peso visual adecuado junto al nuevo border-b del header.
- No hay indicador de "pagina activa" en el nav (ni desktop ni mobile) — evaluar si agregarlo es necesario para "navegacion completa" o si excede el alcance de UI-17 (Claude's discretion, documentar la decision en el summary).
- El `Sheet` no cierra automaticamente al hacer click en un nav item (comportamiento por defecto de shadcn Sheet no incluye auto-close) — verificar si esto rompe la UX movil; si cerrarlo requiere convertir el componente a client component con estado, evalua el costo/beneficio y documenta la decision (aceptar sin auto-close es razonable, UI-17 pide visibilidad de nav, no gestion de estado del Sheet).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Auditar y cerrar gaps de la edicion existente contra UI-17</name>
  <files>src/components/SiteHeader.tsx</files>
  <action>
    Lee el estado actual (sin commitear) de src/components/SiteHeader.tsx completo. Verifica cada uno de los truths de must_haves contra el JSX real: (a) el Sheet movil tiene tap targets >=44px y jerarquia de secciones (nav vs locale/CTA) via el Separator, (b) el nav horizontal desktop tiene espaciado y feedback de hover claros, (c) ningun breakpoint md:/lg: oculta contenido en el estado incorrecto (Sheet trigger solo visible en mobile via md:hidden, NavigationMenu solo visible en desktop via hidden md:flex).

    Cierra los gaps reales que encuentres: si el wordmark de fallback pierde peso visual junto al nuevo border-b, ajusta su tamaño/tracking (ej. text-lg o tracking-tight). Decide si un indicador de pagina activa es necesario para que la nav se sienta "completa" — si lo agregas, usa el pathname actual (requiere convertir a lectura de headers()/usePathname segun corresponda a un Server Component async como este); si decides que no es necesario para cumplir UI-17, no lo agregues y documenta el motivo en el summary. Decide y documenta la postura sobre el auto-close del Sheet al navegar (aceptar sin auto-close es razonable si agregarlo requiere reescribir el componente a client-side).

    No cambies la estructura de datos de header.navItems/header.ctaButton ni el query a Payload — es una pasada de cierre de gaps visuales/UX sobre el JSX existente.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && grep -q "SheetContent" src/components/SiteHeader.tsx && grep -q "NavigationMenu" src/components/SiteHeader.tsx && grep -q "min-h-11\|py-3" src/components/SiteHeader.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>SiteHeader.tsx tiene navegacion completa y visible en mobile (Sheet con jerarquia clara) y desktop (nav horizontal con presencia visual), sin breakpoints invertidos, y tsc pasa limpio.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commitear el header finalizado</name>
  <files>src/components/SiteHeader.tsx</files>
  <action>
    Corre `npx tsc --noEmit` y `npm run dev` brevemente para confirmar que el header renderiza sin errores de servidor (Payload Local API resuelve header.navItems). Crea un commit dedicado para src/components/SiteHeader.tsx con mensaje `feat(10.6-01): complete SiteHeader mobile-first nav (UI-17)`. No incluyas otros archivos sin relacion en este commit.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Personal/juantech/juan-payload && git diff --stat src/components/SiteHeader.tsx | grep -q "SiteHeader" && git log -1 --oneline -- src/components/SiteHeader.tsx | grep -q "10.6-01" && echo OK</automated>
  </verify>
  <done>src/components/SiteHeader.tsx esta commiteado con el header finalizado, tsc pasa limpio, y npm run dev renderiza el header sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS (Header global) -> render publico | navItems/ctaButton vienen de contenido editable por un editor autenticado en /admin; se renderizan a usuarios anonimos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10.6-01 | Tampering | Ninguna dependencia nueva instalada en este plan | accept | Solo cambios de clases Tailwind/JSX sobre componentes shadcn ya auditados en fases previas |

</threat_model>

<verification>
- npx tsc --noEmit pasa sin errores.
- npm run dev levanta sin errores de render en SiteHeader.
- Revision manual del JSX confirma que ningun breakpoint md:/lg: oculta el trigger del Sheet en mobile ni el nav horizontal en desktop.
</verification>

<success_criteria>
SiteHeader muestra navegacion completa y visible en mobile (Sheet) y desktop (nav horizontal), ambos estados diseñados/verificados por separado. ROADMAP Phase 10.6 Success Criterion 1 (UI-17).
</success_criteria>

<output>
Create `.planning/phases/10.6-header-footer-completion-mobile-verification/10.6-01-SUMMARY.md` when done
</output>
</content>
