---
phase: 05-frontend-pages
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/blocks/FeaturedPostsBlock/config.ts
  - src/blocks/FeaturedCaseStudiesBlock/config.ts
  - src/blocks/ClientLogosBlock/config.ts
  - src/blocks/ArchiveBlock/config.ts
  - src/collections/Pages/index.ts
  - migrations/ (new migration file)
autonomous: true
requirements: [CONT-01, CONT-03]

must_haves:
  truths:
    - "An editor can add a 'Featured Posts' section to any Pages doc that pulls from the FeaturedContent global's curated list, not a hardcoded recent-N query"
    - "An editor can add a 'Featured Case Studies' section and a 'Client Logos' section to Pages the same way"
    - "The existing Archive/Featured Grid block can render posts with an editor-togglable category filter, satisfying the blog listing's chronological+filterable requirement without spawning a redundant near-duplicate block"
  artifacts:
    - path: "src/blocks/FeaturedPostsBlock/config.ts"
      provides: "Block: title (localized text), limit (number, default 3) — reads FeaturedContent.featuredPosts at render time"
    - path: "src/blocks/FeaturedCaseStudiesBlock/config.ts"
      provides: "Block: title (localized text), limit (number, default 3) — reads FeaturedContent.featuredCaseStudies at render time"
    - path: "src/blocks/ClientLogosBlock/config.ts"
      provides: "Block: title (localized text), clients (relationship→clientes, hasMany, optional — empty means show all)"
    - path: "src/collections/Pages/index.ts"
      provides: "Pages blocks array includes the 3 new blocks alongside the 13 existing ones"
  key_links:
    - from: "src/blocks/ArchiveBlock/config.ts"
      to: "categories collection"
      via: "new enableCategoryFilter checkbox field, condition: relationTo === 'posts'"
      pattern: "enableCategoryFilter"
---

<objective>
Define the remaining Payload block contracts 05-UI-SPEC.md's page inventory requires but don't exist yet: Featured Posts, Featured Case Studies, and Client Logos sections (needed by Home and Blog listing), plus a category-filter extension to the existing Archive/Featured Grid block (needed by the Blog listing's "chronological grid filterable by category" requirement).

Purpose: this is the second half of the interface-first data-model wave — by the end of this plan, every Payload artifact the Page Inventory table names exists as a real block config. Depends on 05-02 (FeaturedContent global + Clientes relationship target) because these blocks read from it.

Deliberately extends the existing `ArchiveBlock` (per its own code comment: "Any future 'grid of N items from a collection' need MUST extend this select's options — never spawn a new block slug") rather than creating a redundant `PostsArchiveBlock`, honoring both the codebase's established pattern and CONTEXT.md's "Claude's Discretion" on exact block structure.
Output: 3 new block configs, 1 extended block config, Pages registration, 1 migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@.planning/phases/05-frontend-pages/05-CONTEXT.md
@src/blocks/ArchiveBlock/config.ts
@src/collections/Pages/index.ts
@src/collections/Clientes/index.ts
@src/globals/FeaturedContent/index.ts
</context>

<interfaces>
`FeaturedContent` global (from 05-02): `featuredPosts` (relationship→posts, hasMany), `featuredCaseStudies` (relationship→case-studies, hasMany). The two new Featured*Block configs below are intentionally thin (title + limit only) — the actual curated document list is NOT duplicated onto the block; it's read from this global at render time by the Phase 5 rendering plan (05-04/05-05). Do not add a `selectedDocs` relationship field to these two blocks — that would create two competing curation mechanisms (global vs. per-block), which CONTEXT.md's single-source curation intent forbids.

Existing `ArchiveBlock` (read in full above): `relationTo` (select: posts|case-studies), `mode` (radio: latest|manual), `limit`, `selectedDocs`. Add the new field as a sibling, conditioned on `relationTo === 'posts'` only (case studies listing does not need a category filter per the Page Inventory table).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FeaturedPostsBlock, FeaturedCaseStudiesBlock, ClientLogosBlock configs + ArchiveBlock category-filter extension</name>
  <files>src/blocks/FeaturedPostsBlock/config.ts, src/blocks/FeaturedCaseStudiesBlock/config.ts, src/blocks/ClientLogosBlock/config.ts, src/blocks/ArchiveBlock/config.ts</files>
  <action>
Create `src/blocks/FeaturedPostsBlock/config.ts`: `Block` with slug `featuredPostsBlock`, `interfaceName: 'FeaturedPostsBlock'`, fields `title` (text, localized, admin.description explaining copy is editable per-instance), `limit` (number, defaultValue 3, min 1, max 6). No relationship field — document selection comes from the `FeaturedContent` global, read at render time.

Create `src/blocks/FeaturedCaseStudiesBlock/config.ts`: identical shape, slug `featuredCaseStudiesBlock`, `interfaceName: 'FeaturedCaseStudiesBlock'`.

Create `src/blocks/ClientLogosBlock/config.ts`: slug `clientLogosBlock`, `interfaceName: 'ClientLogosBlock'`, fields `title` (text, localized), `clients` (relationship→`clientes`, hasMany, required: false, admin.description: "Leave empty to show all clients"). This block IS self-contained (unlike the Featured* blocks) because `Clientes` has no site-wide "featured" curation concept elsewhere — this is its only curation surface.

Extend `src/blocks/ArchiveBlock/config.ts`: add `enableCategoryFilter` (checkbox, defaultValue: true, admin.condition: `(_, siblingData) => siblingData?.relationTo === 'posts'`, admin.description: "Show category filter tabs above the grid (posts only)"). Keep the existing code comment about never spawning a new block slug intact, and update it to reference this extension as the precedent example.
  </action>
  <verify>
    <automated>grep -l "featuredPostsBlock\|featuredCaseStudiesBlock\|clientLogosBlock" src/blocks/*/config.ts | wc -l | grep -q 3 && grep -q "enableCategoryFilter" src/blocks/ArchiveBlock/config.ts && echo OK</automated>
  </verify>
  <done>3 new block config files exist with correct slugs/fields, and ArchiveBlock has the new conditional `enableCategoryFilter` field.</done>
</task>

<task type="auto">
  <name>Task 2: Register new blocks on Pages + migration</name>
  <files>src/collections/Pages/index.ts, migrations/ (new file)</files>
  <action>
Import and add `FeaturedPostsBlock`, `FeaturedCaseStudiesBlock`, `ClientLogosBlock` to the `blocks` array in `src/collections/Pages/index.ts` (currently 13 blocks — becomes 16), alongside the existing imports for `Hero`/`Content`/etc. Do not reorder or remove any existing block.

Run `payload migrate:create` then `payload migrate` against the real Neon Postgres to apply the schema change (3 new block-table sets + the `enableCategoryFilter` column on the existing archive block table). Commit the migration file.
  </action>
  <verify>
    <automated>grep -c "FeaturedPostsBlock\|FeaturedCaseStudiesBlock\|ClientLogosBlock" src/collections/Pages/index.ts</automated>
  </verify>
  <done>Pages registers 16 blocks total; migration generated, committed, and applied cleanly against the real database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Admin editor → block config | Same authenticated-write trust level as all existing Pages blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | `ClientLogosBlock.clients` relationship | accept | Relationship read access inherits `Clientes` collection's existing public `read: () => true` — no new exposure, same as every other block referencing public collections |
</threat_model>

<verification>
Admin panel's Pages block picker shows "Featured Posts", "Featured Case Studies", and "Client Logos" as addable blocks; the existing Archive/Featured Grid block shows the new category-filter toggle when `relationTo` is set to Posts.
</verification>

<success_criteria>
- [ ] 3 new block configs exist and are registered on Pages
- [ ] ArchiveBlock has a working `enableCategoryFilter` conditional field
- [ ] Migration applied against real Neon Postgres
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-03-SUMMARY.md` when done
</output>
