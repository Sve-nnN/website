---
phase: 05-frontend-pages
plan: 10
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - src/app/(frontend)/[locale]/authors/page.tsx
  - src/app/(frontend)/[locale]/authors/[slug]/page.tsx
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "A visitor can see a grid of all authors (avatar, name, jobTitle, one credential highlight) in both locales"
    - "A visitor can open an author's profile and see the full E-E-A-T card plus a list of that author's posts and case studies"
  artifacts:
    - path: "src/app/(frontend)/[locale]/authors/page.tsx"
      provides: "New listing route querying authors collection"
    - path: "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"
      provides: "New profile route: AuthorCard + posts/case-studies by that author"
  key_links:
    - from: "src/app/(frontend)/[locale]/authors/[slug]/page.tsx"
      to: "src/components/AuthorCard.tsx"
      via: "renders the full expanded E-E-A-T card"
      pattern: "AuthorCard"
---

<objective>
Build the two Authors-facing routes that don't exist yet: a listing grid and an individual profile page, per 05-UI-SPEC.md's Page Inventory. These are new routes (unlike blog/case-studies, which had Phase 2 stubs) built directly against the E-E-A-T components from 05-05.

Purpose: this is where the CONT-02 differentiator gets its own dedicated surface — a reader can go "who wrote this" from any byline and land on a real credibility page, not a dead link.
Output: `/authors` and `/authors/[slug]` routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-frontend-pages/05-UI-SPEC.md
@src/collections/Authors/index.ts
@src/components/AuthorByline.tsx
@src/components/AuthorCard.tsx
</context>

<interfaces>
`Authors` collection after 05-02: `name`, `jobTitle`, `bio`, `avatar`, `credentials`, `yearsExperience`, `socialLinks`, `slug`. `AuthorCard` (05-05) renders the full set given an `Author` doc. Phase 4 confirms exactly 1 real Author doc exists today — both pages must work correctly with that single real author (not assume a large roster), while remaining correct if more authors are added later.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Authors listing grid</name>
  <files>src/app/(frontend)/[locale]/authors/page.tsx</files>
  <action>
Create `src/app/(frontend)/[locale]/authors/page.tsx`: Local API query of `authors` collection (current locale), render a card grid using `AuthorByline`-equivalent presentation (avatar, name, jobTitle, first credential) per the Page Inventory's "Grid of author cards" spec, each card linking to `/authors/{slug}`. Include `generateMetadata` following the same pattern as other listing pages.
  </action>
  <verify>
    <automated>ls src/app/"(frontend)"/"[locale]"/authors/page.tsx && echo OK</automated>
  </verify>
  <done>`/authors` and `/en/authors` render the real migrated author(s) as cards linking to their profile.</done>
</task>

<task type="auto">
  <name>Task 2: Author profile page</name>
  <files>src/app/(frontend)/[locale]/authors/[slug]/page.tsx</files>
  <action>
Create `src/app/(frontend)/[locale]/authors/[slug]/page.tsx`: fetch the `Author` doc by slug (Local API, current locale), `notFound()` if missing, render `<AuthorCard author={doc} />` (full E-E-A-T), then query `posts` where `author` equals this author's id and `case-studies` where `author` equals this author's id (added in 05-09), rendering both as simple lists/grids under "Posts" and "Case Studies" headings (localized). Add `Person` JSON-LD for the author following the same pattern as the home page's existing Person schema, plus a `BreadcrumbList` consistent with the case-study detail page's pattern.
  </action>
  <verify>
    <automated>ls src/app/"(frontend)"/"[locale]"/authors/"[slug]"/page.tsx && echo OK</automated>
  </verify>
  <done>Author profile page renders the full E-E-A-T card and correctly lists that author's real posts/case studies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| N/A | Read-only public content, same trust posture as blog/case-study listings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Information Disclosure | Author profile queries | accept | Scoped to already-public `authors`/`posts`/`case-studies` collections, no new exposure |
</threat_model>

<verification>
`/authors` and `/en/authors` list the real author(s); the profile page for the real migrated author shows full E-E-A-T data plus a correct list of their real posts.
</verification>

<success_criteria>
- [ ] Authors listing renders real data
- [ ] Author profile renders full AuthorCard + real posts/case-studies list
</success_criteria>

<output>
Create `.planning/phases/05-frontend-pages/05-10-SUMMARY.md` when done
</output>
