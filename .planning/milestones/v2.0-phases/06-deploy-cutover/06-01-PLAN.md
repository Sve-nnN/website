---
phase: 06-deploy-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ecosystem.config.cjs, scripts/deploy.sh, .planning/phases/06-deploy-cutover/DEPLOY-ENV-CHECKLIST.md]
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02]

must_haves:
  truths:
    - "Un `npm run build` local produce .next/standalone/server.js con public/ y .next/static/ copiados dentro por el postbuild, listo para ejecutarse standalone"
    - "Existe un archivo de configuración PM2 (ecosystem.config.cjs) válido que arranca ese server.js con las env vars de producción correctas (PORT, HOSTNAME, NODE_ENV)"
    - "Juan tiene un runbook numerado y ejecutado para: confirmar el tier real de Hostinger, subir el código, instalar PM2, arrancar el proceso, y confirmar que sobrevive un restart"
  artifacts:
    - path: "ecosystem.config.cjs"
      provides: "Config PM2 para el proceso Node standalone"
      contains: "server.js"
    - path: "scripts/deploy.sh"
      provides: "Script de deploy repetible (pull + install + build + reload PM2)"
    - path: ".planning/phases/06-deploy-cutover/DEPLOY-ENV-CHECKLIST.md"
      provides: "Checklist de env vars requeridas en el servidor (solo keys, no valores)"
  key_links:
    - from: "ecosystem.config.cjs"
      to: ".next/standalone/server.js"
      via: "script: '.next/standalone/server.js'"
      pattern: "standalone/server\\.js"
    - from: "scripts/deploy.sh"
      to: "ecosystem.config.cjs"
      via: "pm2 startOrReload / pm2 reload"
      pattern: "pm2 (startOrReload|reload)"
---

<objective>
Producir el pipeline de build+deploy y la configuración de proceso persistente (PM2) para correr el server standalone de Next/Payload en el Hostinger real de Juan, más el runbook exacto para que Juan lo ejecute contra su servidor real (Claude no tiene SSH ni panel de Hostinger).

Purpose: Satisfacer DEPLOY-01 (build standalone sirviendo tráfico real) y DEPLOY-02 (proceso Node persistente entre reinicios/deploys, confirmado contra el tier real contratado).
Output: `ecosystem.config.cjs`, `scripts/deploy.sh`, `DEPLOY-ENV-CHECKLIST.md`, y confirmación por parte de Juan (con outputs pegados) de que el proceso corre en el servidor real y sobrevive un restart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-cutover/06-CONTEXT.md
@.planning/ROADMAP.md
@package.json

<interfaces>
Scripts existentes en package.json que este plan NO debe reinventar, solo orquestar:
- `build`: `payload migrate && payload generate:importmap && payload generate:types && next build`
- `postbuild`: `cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/`
- `start:standalone`: `node .next/standalone/server.js`

`next.config.mjs` ya tiene `output: 'standalone'`. El server standalone de Next lee `PORT` y `HOSTNAME` de `process.env` (default 3000 / localhost si no se setean) — en un VPS hay que fijar `HOSTNAME=0.0.0.0` para aceptar conexiones externas, no solo loopback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar build standalone local + escribir ecosystem.config.cjs y scripts/deploy.sh</name>
  <files>ecosystem.config.cjs, scripts/deploy.sh</files>
  <action>
Correr `npm run build` localmente (usa el `.env` real ya presente) y confirmar que produce `.next/standalone/server.js`, `.next/standalone/public/` y `.next/standalone/.next/static/` — esto reproduce exactamente lo que correrá en el servidor real, así que sirve como prueba de que el pipeline de build no está roto antes de escribir nada para el runbook.

Escribir `ecosystem.config.cjs` en la raíz del repo (CommonJS porque PM2 lee `.cjs` de forma más confiable en proyectos `"type": "module"` como este): una sola app llamada `juan-payload`, `script: '.next/standalone/server.js'`, `cwd: __dirname`, `exec_mode: 'fork'` (NO `cluster` — un solo proceso, porque múltiples workers en cluster multiplicarían el pool de conexiones Postgres de forma descontrolada, ver Plan 06-02), `instances: 1`, `autorestart: true`, `env: { NODE_ENV: 'production', PORT: 3000, HOSTNAME: '0.0.0.0' }`, `max_memory_restart` dejarlo comentado con una nota de que Juan debe fijarlo según la RAM real del tier confirmado en Task 2, `out_file`/`error_file` apuntando a `./logs/out.log` y `./logs/error.log`.

Escribir `scripts/deploy.sh`: script bash idempotente que Juan corre por SSH desde el directorio del repo en el servidor — `set -euo pipefail`, detectar la rama actual con `git rev-parse --abbrev-ref HEAD` y hacer `git pull origin <esa rama>` (no hardcodear `main`/`master`), `npm ci` (instalación completa, no `--omit=dev`, porque `next build` requiere `typescript` que está en devDependencies), `npm run build`, luego `pm2 startOrReload ecosystem.config.cjs --env production` (arranca si no existe, recarga sin downtime si ya corre), terminar con `pm2 save` para persistir la lista de procesos.
  </action>
  <verify>
    <automated>test -f .next/standalone/server.js && test -d .next/standalone/public && test -d .next/standalone/.next/static && node -e "const c=require('./ecosystem.config.cjs'); if(!c.apps || !c.apps[0].script.includes('standalone/server.js')) process.exit(1)" && bash -n scripts/deploy.sh</automated>
  </verify>
  <done>El build standalone local produce los tres artefactos esperados, ecosystem.config.cjs es un módulo CJS válido apuntando al server.js correcto, y scripts/deploy.sh pasa el chequeo de sintaxis bash.</done>
</task>

<task type="auto">
  <name>Task 2: Checklist de env vars de producción</name>
  <files>.planning/phases/06-deploy-cutover/DEPLOY-ENV-CHECKLIST.md</files>
  <action>
Escribir `DEPLOY-ENV-CHECKLIST.md` listando SOLO las keys (nunca valores) que deben existir como variables de entorno reales en el servidor Hostinger, extraídas del `.env` local real: `DATABASE_URI`, `PAYLOAD_SECRET`, `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`, `CONTACT_TO_EMAIL`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `NEXT_PUBLIC_SERVER_URL` (debe apuntar al dominio de producción real, no localhost), más `PORT`/`HOSTNAME`/`NODE_ENV` que ya vienen fijadas por `ecosystem.config.cjs`. Documentar dos formas válidas de setearlas en Hostinger: (a) archivo `.env` en la raíz del repo en el servidor con permisos `chmod 600`, gitignorado, leído automáticamente por Payload/Next en runtime; o (b) variables de entorno del panel de Hostinger (hPanel → Node.js app → Environment Variables) si el tier de Juan expone esa UI — a confirmar en el runbook de la Task 3. Advertir explícitamente: nunca commitear valores reales, nunca pegarlos en este archivo ni en el chat.
  </action>
  <verify>
    <automated>test -s .planning/phases/06-deploy-cutover/DEPLOY-ENV-CHECKLIST.md && grep -c "DATABASE_URI\|PAYLOAD_SECRET\|CLOUDINARY_CLOUD_NAME" .planning/phases/06-deploy-cutover/DEPLOY-ENV-CHECKLIST.md | grep -qv "^0$"</automated>
  </verify>
  <done>El checklist lista las 9 keys reales requeridas, sin ningún valor, con instrucciones de dónde setearlas en Hostinger.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Runbook manual — deploy real y arranque de PM2 en el servidor Hostinger</name>
  <action>Juan ejecuta el runbook de 11 pasos numerados en how-to-verify por SSH contra el servidor Hostinger real y pega los outputs pedidos; Claude no tiene acceso a este servidor.</action>
  <what-built>ecosystem.config.cjs (PM2), scripts/deploy.sh, y DEPLOY-ENV-CHECKLIST.md — todo lo que Claude puede producir sin acceso al servidor real.</what-built>
  <how-to-verify>
Runbook numerado — ejecutar en orden por SSH contra el servidor Hostinger real:

1. Confirmar el tier exacto contratado: en hPanel → tu plan actual (debe decir algo como "Cloud Startup" / "Cloud Professional" / "Cloud Business"). Pegar el nombre exacto.
2. Por SSH, correr `node -v` — debe ser >= 20. Pegar el output.
3. Verificar si hPanel ofrece "Setup Node.js App" (supervisor nativo tipo Passenger) para tu tier — si existe, indicar que lo viste, pero seguimos con PM2 porque el entrypoint standalone de Next (`server.js` custom) no encaja limpio con las convenciones de Passenger. Confirmar por SSH que tienes acceso de shell normal (no solo el wizard de la app Node).
4. Clonar o subir el repo al servidor (`git clone <tu-repo>` o `rsync`), `cd` al directorio.
5. Crear el archivo `.env` real en el servidor con los valores reales (según DEPLOY-ENV-CHECKLIST.md), `chmod 600 .env`.
6. Instalar PM2 globalmente: `npm install -g pm2` (paquete estándar de la industria, ~16M descargas semanales, sin riesgo de legitimidad). Pegar el output de `pm2 -v`.
7. Correr `bash scripts/deploy.sh`. Pegar el output completo (o al menos las últimas 20 líneas).
8. Pegar el output de `pm2 list` — debe mostrar `juan-payload` con status `online`.
9. Pegar el output de `curl -I http://localhost:3000` — debe responder `200` o `308` (redirect a locale default).
10. Correr `pm2 startup` y ejecutar el comando sudo que PM2 imprime (persiste el arranque tras reboot del servidor), luego `pm2 save`. Pegar el output de `pm2 startup`.
11. Simular persistencia: correr `pm2 restart juan-payload`, esperar 5s, pegar `pm2 list` de nuevo — debe seguir `online` con el `restart` counter incrementado en 1.
  </how-to-verify>
  <resume-signal>Pega los 11 outputs pedidos (o indica en cuál paso falló y qué error viste)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH → servidor Hostinger | Canal de administración con acceso completo al filesystem/proceso; solo Juan lo controla |
| Internet público → puerto 3000/proceso Node | Superficie que recibirá tráfico real una vez DNS apunte aquí (Plan 06-03) |
| Filesystem del servidor → .env | Secretos en texto plano en disco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `.env` en el servidor | mitigate | `chmod 600 .env`, archivo gitignorado, nunca commiteado (paso 5 del runbook) |
| T-06-02 | Denial of Service | Proceso Node único (fork, no cluster) | accept | Sitio de bajo tráfico (portfolio); PM2 `autorestart` cubre crashes; escalar a cluster solo si el tráfico real lo justifica más adelante |
| T-06-03 | Tampering | `scripts/deploy.sh` corriendo `git pull` sin verificar firma/rama | accept | Juan controla el repo remoto y el servidor; no hay CI de terceros en el path de deploy |
| T-06-SC | Tampering | `npm install -g pm2` en el servidor | accept | PM2 es el process manager de facto para Node en producción (~16M descargas/semana), instalación manual de Juan por SSH, no un install automatizado por Claude — no aplica el gate de legitimidad de paquetes (ese gate cubre installs que Claude ejecuta) |
</threat_model>

<verification>
Local: build standalone reproduce los artefactos esperados, ecosystem.config.cjs carga sin error, deploy.sh pasa `bash -n`.
Remoto (Juan): los 11 outputs del runbook confirman que el proceso corre, responde en localhost, y sobrevive un restart.
</verification>

<success_criteria>
`npm run build` local funciona de punta a punta. `pm2 list` en el servidor real muestra `juan-payload` `online`. `curl -I http://localhost:3000` en el servidor responde 200/308. El proceso sobrevive `pm2 restart` y quedó configurado para sobrevivir un reboot real (`pm2 startup` + `pm2 save`).
</success_criteria>

<output>
Create `.planning/phases/06-deploy-cutover/06-01-SUMMARY.md` when done
</output>
