---
phase: 06-deploy-cutover
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [scripts/content-freeze-snapshot.ts, scripts/verify-content-freeze.ts, .planning/phases/06-deploy-cutover/CONTENT-FREEZE-RUNBOOK.md, .gitignore]
autonomous: false
requirements: [DEPLOY-05]

must_haves:
  truths:
    - "Existe un mecanismo repetible para tomar una fotografía verificable del contenido real en Postgres (conteos + ids + updatedAt por colección)"
    - "Existe un mecanismo para diffear dos fotografías y detectar cualquier contenido publicado entre el freeze y el go-live"
    - "Juan tiene un runbook para declarar el freeze, tomar la fotografía inicial, y re-verificar cero drift inmediatamente antes del corte de DNS (Plan 06-03)"
  artifacts:
    - path: "scripts/content-freeze-snapshot.ts"
      provides: "Snapshot JSON con conteo+ids+updatedAt de cada colección de contenido publicable"
    - path: "scripts/verify-content-freeze.ts"
      provides: "Diff entre dos snapshots, exit code no-cero si hay drift"
    - path: ".planning/phases/06-deploy-cutover/CONTENT-FREEZE-RUNBOOK.md"
      provides: "Procedimiento numerado para declarar y verificar el freeze"
  key_links:
    - from: "scripts/content-freeze-snapshot.ts"
      to: "scripts/verify-content-freeze.ts"
      via: "snapshots JSON con mismo shape, comparados por --before/--after"
      pattern: "verify-content-freeze"
---

<objective>
Este backend ya migró todo el contenido real en Phase 4 (completada) — DEPLOY-05 aplica ahora al backend Postgres/Payload nuevo como fuente de verdad actual (per 06-CONTEXT.md), no al sitio Mongo viejo ya muerto. El objetivo es poder demostrar, con evidencia verificable y no solo la palabra de Juan, que nada se publicó en el backend nuevo entre el momento del freeze y el corte de DNS a producción.

Purpose: Satisfacer DEPLOY-05 con un mecanismo verificable de cero-drift, ejecutable por Claude localmente contra la DB real ahora mismo.
Output: `scripts/content-freeze-snapshot.ts`, `scripts/verify-content-freeze.ts`, `CONTENT-FREEZE-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-cutover/06-CONTEXT.md

<interfaces>
Patrón real de scripts standalone del repo (ej. `scripts/backfill-case-study-author.ts`):

```
import { getPayload } from 'payload'
import config from '../src/payload.config'
const payload = await getPayload({ config })
```

Corrido con `node --env-file=.env node_modules/.bin/tsx scripts/<archivo>.ts`.

Colecciones de contenido publicable a incluir en el snapshot: `pages`, `posts`, `authors`, `case-studies`, `categories`, `testimonials`, `clientes`, `media`, `redirects` (las mismas 9 colecciones esenciales de SCHEMA-02, todas relevantes a lo que un visitante ve o a lo que gobierna el routing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de snapshot + gitignore de los dumps</name>
  <files>scripts/content-freeze-snapshot.ts, .gitignore</files>
  <action>
Crear `scripts/content-freeze-snapshot.ts`: usa `getPayload({config})`, recibe un argumento CLI `--tag <nombre>` (ej. `freeze`, `pre-golive`), y para cada una de las 9 colecciones hace `payload.find({collection, limit: 0, locale: 'all', depth: 0})`, extrae `{count: docs.length, docs: docs.map(d => ({id: d.id, slug: d.slug ?? d.from ?? null, updatedAt: d.updatedAt}))}` ordenado por `id`. Escribe el resultado como JSON en `.planning/phases/06-deploy-cutover/freeze-snapshots/<tag>-<timestamp ISO>.json` con un campo top-level `{tag, takenAt, collections: {...}}`. Crear el directorio si no existe.

Añadir `.planning/phases/06-deploy-cutover/freeze-snapshots/` al `.gitignore` (son dumps de datos reales regenerables, no código fuente — mismo criterio ya aplicado a `scripts/migrate/data/` en el gitignore existente).
  </action>
  <verify>
    <automated>node --env-file=.env node_modules/.bin/tsx scripts/content-freeze-snapshot.ts --tag freeze-test && ls .planning/phases/06-deploy-cutover/freeze-snapshots/freeze-test-*.json | head -1 | xargs -I{} node -e "const s=require('{}'); if(!s.collections || !s.collections.posts) process.exit(1)"</automated>
  </verify>
  <done>El script corre contra la DB real, produce un JSON con las 9 colecciones y sus conteos/ids/updatedAt reales, y el path queda gitignorado.</done>
</task>

<task type="auto">
  <name>Task 2: Script de diff entre dos snapshots</name>
  <files>scripts/verify-content-freeze.ts</files>
  <action>
Crear `scripts/verify-content-freeze.ts`: recibe `--before <path>` y `--after <path>` (paths a los JSON generados por Task 1), carga ambos, y para cada colección compara el set de ids y sus `updatedAt`. Reporta: ids nuevos en `after` (contenido publicado durante el freeze — DRIFT), ids con `updatedAt` distinto entre ambos snapshots (contenido editado durante el freeze — DRIFT), ids eliminados. Si hay cualquier drift, imprime el detalle por colección y `process.exit(1)`. Si no hay drift, imprime `NO DRIFT — freeze held` y `process.exit(0)`.
  </action>
  <verify>
    <automated>cp $(ls .planning/phases/06-deploy-cutover/freeze-snapshots/freeze-test-*.json | head -1) /tmp/freeze-a.json && node --env-file=.env node_modules/.bin/tsx scripts/content-freeze-snapshot.ts --tag freeze-test-b && node --env-file=.env node_modules/.bin/tsx scripts/verify-content-freeze.ts --before /tmp/freeze-a.json --after $(ls -t .planning/phases/06-deploy-cutover/freeze-snapshots/freeze-test-b-*.json | head -1)</automated>
  </verify>
  <done>Corriendo el diff entre dos snapshots tomados segundos aparte contra la misma DB real (sin cambios de contenido de por medio) el script reporta NO DRIFT y exit 0, confirmando que el mecanismo detecta correctamente el caso sin drift.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Runbook manual — declarar el freeze y tomar el snapshot inicial</name>
  <action>Juan declara el freeze del admin de Payload y corre el snapshot inicial siguiendo CONTENT-FREEZE-RUNBOOK.md; esto requiere una decisión operativa de Juan (dejar de publicar), no algo que Claude pueda forzar.</action>
  <what-built>scripts/content-freeze-snapshot.ts y scripts/verify-content-freeze.ts, verificados contra la DB real.</what-built>
  <how-to-verify>
CONTENT-FREEZE-RUNBOOK.md — procedimiento a ejecutar cronológicamente antes del corte de DNS (Plan 06-03):

1. Declarar el freeze: a partir de este momento, no publiques ni edites contenido en el admin de Payload (`/admin`) hasta que el go-live (Plan 06-04) esté confirmado.
2. Correr `node --env-file=.env node_modules/.bin/tsx scripts/content-freeze-snapshot.ts --tag freeze` y confirmar que el JSON generado tiene los conteos que esperas para cada colección (posts, case-studies, authors, etc.).
3. Proceder con el resto del phase (Plan 06-03 DNS cutover, Plan 06-04 go-live).
4. Inmediatamente antes de confirmar el go-live final, correr `node --env-file=.env node_modules/.bin/tsx scripts/content-freeze-snapshot.ts --tag pre-golive`.
5. Correr `node --env-file=.env node_modules/.bin/tsx scripts/verify-content-freeze.ts --before <path del freeze> --after <path del pre-golive>`. Pegar el output completo.
6. Si el output dice `NO DRIFT`, el freeze se sostuvo — proceder con confianza. Si reporta drift, detener el go-live e investigar qué se publicó/editó antes de continuar.
  </how-to-verify>
  <resume-signal>Confirma que ejecutaste el paso 1 (freeze declarado) y pega el output del paso 2 (snapshot inicial tomado)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin de Payload (`/admin`) → editores | Superficie donde Juan (u otro editor) podría publicar contenido durante la ventana de freeze |
| Scripts locales → DB real de Neon | Los snapshots leen datos de producción reales, sin escribir nada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation | Afirmar "no publiqué nada" sin evidencia | mitigate | verify-content-freeze.ts produce evidencia objetiva (diff de ids/updatedAt), no depende de la palabra de Juan |
| T-06-08 | Tampering | Snapshots editados manualmente para forzar NO DRIFT | accept | Riesgo solo mitigable con firma/hash externo, fuera de alcance para un proyecto solo-developer; el diff sigue siendo mejor evidencia que ninguna |
</threat_model>

<verification>
Los dos scripts corridos localmente contra la DB real de Neon producen snapshots reales y detectan correctamente el caso sin drift.
</verification>

<success_criteria>
Juan declaró el freeze y tomó el snapshot inicial real contra la DB de producción. El mecanismo de diff está probado y listo para re-ejecutarse justo antes del go-live (Plan 06-04).
</success_criteria>

<output>
Create `.planning/phases/06-deploy-cutover/06-05-SUMMARY.md` when done
</output>
