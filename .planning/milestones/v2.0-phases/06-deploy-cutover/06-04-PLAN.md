---
phase: 06-deploy-cutover
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified: [scripts/go-live-checklist.mjs, .planning/phases/06-deploy-cutover/GO-LIVE-CHECKLIST.md]
autonomous: false
requirements: [DEPLOY-04]

must_haves:
  truths:
    - "robots.txt se fetchea desde la URL real de producción (no se lee del código) y contiene las reglas esperadas"
    - "El sitemap de producción, fetcheado en vivo, no diverge del inventario de 152 URLs congelado en Phase 4"
    - "Un redirect 301 real, creado en la colección redirects, se verifica en vivo contra la URL de producción"
    - "Ambos locales (es/en) fueron muestreados manualmente por Juan además de los chequeos automatizados de Claude"
  artifacts:
    - path: "scripts/go-live-checklist.mjs"
      provides: "Chequeos automatizados contra la URL de producción real: robots.txt, sitemap diff, redirect 301 en vivo, ambos locales"
    - path: ".planning/phases/06-deploy-cutover/GO-LIVE-CHECKLIST.md"
      provides: "Resultado real de la corrida del checklist + confirmación humana de Juan"
  key_links:
    - from: "scripts/go-live-checklist.mjs"
      to: ".planning/phases/04-migraci-n-mongo-postgres/URL-INVENTORY.json"
      via: "diff de URLs del sitemap.xml en vivo contra el inventario congelado"
      pattern: "URL-INVENTORY"
    - from: "scripts/go-live-checklist.mjs"
      to: "https://juan-tech.com"
      via: "fetch nativo de Node contra la URL de producción real"
      pattern: "fetch\\(.*PROD_URL"
---

<objective>
Cerrar la fase con el checklist de go-live que ROADMAP exige verificado *en producción*, no en código: robots.txt fetcheado en vivo, sitemap sin divergencia contra el inventario congelado de Phase 4, redirects 301 funcionando en vivo, y ambos locales muestreados. Como el dominio ya apunta al servidor real (Plan 06-03), Claude puede ejecutar la mayoría de estos chequeos directamente contra la URL pública, sin depender del testimonio de Juan.

Purpose: Satisfacer DEPLOY-04 con evidencia real fetcheada de producción.
Output: `scripts/go-live-checklist.mjs`, `GO-LIVE-CHECKLIST.md` con el resultado real de la corrida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-cutover/06-CONTEXT.md
@.planning/phases/04-migraci-n-mongo-postgres/URL-INVENTORY.json
@src/app/robots.ts
@src/app/sitemap.ts

<interfaces>
`src/app/robots.ts` expone reglas `disallow: ['/admin', '/api']` y referencia `sitemap: ${SITE_URL}/sitemap.xml`.
`src/app/sitemap.ts` genera URLs para `pages` (sin prefijo salvo `home` → raíz), `posts` (prefijo `blog`), `case-studies`, `authors`, `categories`, cada una con alternates `es`/`en` (en usa prefijo `/en`).

Phase 4 no creó ningún redirect real (`04-VERIFICATION.md`: "0 deltas de URL detectados → 0 redirects necesarios"). Para probar el mecanismo de redirects en vivo, este plan crea un redirect de prueba temporal vía Local API antes del chequeo, y lo borra después — no depende de que existan redirects reales de la migración.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script de checklist de go-live contra producción</name>
  <files>scripts/go-live-checklist.mjs</files>
  <action>
Crear `scripts/go-live-checklist.mjs` (Node puro, usa `fetch` nativo, sin dependencias nuevas — evita el gate de legitimidad de paquetes). Toma `PROD_URL` de `process.env` (default `https://juan-tech.com`). Chequeos, cada uno imprimiendo PASS/FAIL con detalle:

1. `GET {PROD_URL}/robots.txt` — confirmar 200 y que el body contiene `Disallow: /admin` y `Disallow: /api` y una línea `Sitemap:`.
2. `GET {PROD_URL}/sitemap.xml` — parsear las URLs (regex simple sobre `<loc>...</loc>`), compararlas contra las 152 URLs de `.planning/phases/04-migraci-n-mongo-postgres/URL-INVENTORY.json` (normalizar dominio/protocolo antes de comparar) — reportar cualquier URL congelada ausente en el sitemap real y cualquier URL nueva no prevista, sin fallar automáticamente por URLs nuevas (contenido legítimo puede haberse agregado) pero SÍ fallar si falta alguna URL congelada.
3. `GET {PROD_URL}/es` y `GET {PROD_URL}/en` — confirmar 200 en ambos y que el HTML devuelto difiere entre sí (evidencia de que el locale realmente cambia el contenido, no solo la URL).
4. Redirect 301 en vivo: antes de este chequeo, crear un redirect temporal real vía Local API (`payload.create({collection:'redirects', data:{from:'/go-live-redirect-test', to:{type:'custom', url:'/'}}})`, usando el patrón `getPayload({config})` ya usado en el repo) contra la DB real, esperar 2s, hacer `fetch(PROD_URL + '/go-live-redirect-test', {redirect:'manual'})` y confirmar status 301/308 con `Location` apuntando a `/`. Al final (pase o falle), borrar el redirect temporal (`payload.delete`) para no dejar basura en la colección real.

Salir con `process.exit(1)` si cualquier chequeo obligatorio (1, 2 con URLs congeladas faltantes, 3, 4) falla; imprimir un resumen final `X/4 checks passed`.
  </action>
  <verify>
    <automated>node -c scripts/go-live-checklist.mjs</automated>
  </verify>
  <done>El script tiene sintaxis válida y los 4 chequeos implementados contra PROD_URL configurable, incluyendo creación/limpieza del redirect de prueba real.</done>
</task>

<task type="auto">
  <name>Task 2: Correr el checklist real contra producción</name>
  <files>.planning/phases/06-deploy-cutover/GO-LIVE-CHECKLIST.md</files>
  <action>
Correr `PROD_URL=https://juan-tech.com node --env-file=.env scripts/go-live-checklist.mjs` contra el sitio real ya en producción (Plan 06-03 completado). Si algún chequeo falla por un bug del script (no del sitio), corregir el script y re-correr. Si falla porque el sitio real tiene un problema real (ej. robots.txt mal servido, sitemap divergente), reportarlo — no lo silencies ni lo marques como skip.

Escribir `GO-LIVE-CHECKLIST.md` con el output real completo de la corrida, timestamp, y un resumen pass/fail por chequeo.
  </action>
  <verify>
    <automated>PROD_URL="${PROD_URL:-https://juan-tech.com}" node --env-file=.env scripts/go-live-checklist.mjs</automated>
  </verify>
  <done>El checklist corrió contra la URL real de producción y GO-LIVE-CHECKLIST.md documenta el resultado real (4/4 o el detalle de qué falló y por qué).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Muestreo visual manual final en producción</name>
  <action>Juan navega el sitio real en un navegador (ambos locales, un post, un case study, el formulario de contacto) para la verificación visual/funcional que un fetch automatizado no cubre.</action>
  <what-built>Checklist automatizado corrido contra producción real (robots.txt, sitemap vs inventario congelado, redirect 301 en vivo, ambos locales responden).</what-built>
  <how-to-verify>
    Muestreo manual final, en un navegador real:
    1. Visita `https://juan-tech.com/es` — confirma que el home se ve correcto (hero, case studies destacados, logos de clientes, posts destacados, testimonios).
    2. Visita `https://juan-tech.com/en` — confirma lo mismo en inglés, y que efectivamente es una traducción real, no el mismo texto en español.
    3. Abre un post de blog y un case study en cada locale — confirma que el byline de autor E-E-A-T se ve, y que las imágenes cargan (vienen de Cloudinary).
    4. Prueba el formulario de contacto una vez en producción real y confirma que te llega el email.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe cualquier problema visual/funcional encontrado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script go-live-checklist.mjs → colección redirects real | Crea y borra un documento real en producción durante la verificación |
| Internet público → producción | Todos los fetches de este plan son contra la URL pública real, sin credenciales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering | Redirect de prueba dejado huérfano en producción si el script crashea a mitad de camino | mitigate | Limpieza en bloque try/finally alrededor de la creación/borrado del redirect temporal |
| T-06-13 | Repudiation | "El sitio funciona" sin evidencia fetcheada real | mitigate | Todo el checklist corre contra URLs reales de producción, no contra código local ni memoria |
</threat_model>

<verification>
`scripts/go-live-checklist.mjs` corrido contra `https://juan-tech.com` real reporta los 4 chequeos, con evidencia fetcheada en vivo (no leída del código fuente).
</verification>

<success_criteria>
robots.txt, sitemap (vs inventario de 152 URLs), redirect 301 y ambos locales verificados en vivo contra producción real por Claude. Juan aprobó el muestreo visual manual final.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-cutover/06-04-SUMMARY.md` when done
</output>
