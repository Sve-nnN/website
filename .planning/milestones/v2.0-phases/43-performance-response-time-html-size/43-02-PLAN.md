---
phase: 43-performance-response-time-html-size
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/lib/services-data.ts
  - "src/app/(frontend)/[locale]/blog/page.tsx"
  - src/blocks/ArchiveBlock/Component.tsx
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Servicios índice sirve su query de 'pages' desde el mismo cache lib que Home (getCachedPageBySlug), con overrideAccess:false preservado (ya lo tenía, WR-02 de Phase 24) — segundo request al mismo locale es más rápido que el primero."
    - "Blog listing sirve su propia query de 'pages' (slug 'blog') desde cache."
    - "El grid de ArchiveBlock en modo posts (usado por Blog listing) ya no arrastra el `content` richText completo de cada post al HTML — solo título/slug/excerpt/heroImage, igual que el fix de Home en 43-01."
    - "Filtros distintos de categoría en Blog listing (?category=X vs sin filtro) NUNCA comparten una entrada de cache — cada combinación de relationTo+limit+locale+categoryId tiene su propia cache key."
  artifacts: []
  key_links:
    - "getServicesIndexPage (services-data.ts) -> getCachedPageBySlug('services'/'servicios' real slug, locale, depth:1) — reusa el fetcher de 43-01, no crea uno nuevo"
    - "getBlogPage (blog/page.tsx) -> getCachedPageBySlug('blog', locale)"
    - "ArchiveBlockComponent (relationTo=posts|case-studies) -> getCachedArchive({...}) con select scoping, ya definido en 43-01's cache.ts"
---

<objective>
Replicar el patrón probado en 43-01 (cache + select scoping) sobre las 2 rutas de expansión de mayor tráfico restantes: Servicios índice y Blog listing (incluye el grid real de ArchiveBlock, no solo el shell de la página). No se toca `src/lib/cache.ts`/`cache-tags.ts` — ambos fetchers usados acá (`getCachedPageBySlug`, `getCachedArchive`) ya quedaron construidos en 43-01.

Purpose: Extender la mejora de PERF-01/PERF-02 más allá de Home, sin repetir el trabajo de infraestructura.
Output: Servicios índice y Blog listing (shell + grid de posts) sirviendo desde cache con el mismo TTL/invalidación que Home.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-performance-response-time-html-size/43-CONTEXT.md
@.planning/phases/43-performance-response-time-html-size/43-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Servicios índice — cachear getServicesIndexPage</name>
  <read_first>
    src/lib/services-data.ts:18-33 (getServicesIndexPage — ya usa overrideAccess:false, depth:1, slug=SERVICES_INDEX_SLUG)
    "src/app/(frontend)/[locale]/services/page.tsx" (llama getPage -> getServicesIndexPage, y getServicesIndexMetadata -> getServicesIndexPage de nuevo — misma duplicación por-request que Home tenía antes de 43-01)
    "src/app/(frontend)/[locale]/servicios/page.tsx" (segundo route twin, mismo patrón — confirmar si existe y si también llama getServicesIndexPage; si es un archivo separado que reimplementa la misma lógica, aplica el mismo fix ahí también)
    src/lib/cache.ts (getCachedPageBySlug ya construido en 43-01 — firma: slug, locale, depth opcional)
  </read_first>
  <files>src/lib/services-data.ts</files>
  <action>
    En `src/lib/services-data.ts`, reemplazar el cuerpo de `getServicesIndexPage(locale)` por `return getCachedPageBySlug(SERVICES_INDEX_SLUG, locale, 1)` (import `getCachedPageBySlug` desde `@/lib/cache`) — preserva `depth:1` y `overrideAccess:false` exactos que ya tenía. `getServicesIndexMetadata`/`getServicePage`/`getServiceMetadata` no cambian de firma, siguen llamando a `getServicesIndexPage`/`getServicePage` normalmente; si `getServicePage` (landing individual por slug) también debería cachearse queda FUERA de esta tarea — el tracer de 43-CONTEXT.md solo nombra "Servicios índice", no las 4 landings individuales.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio.
    - `grep -n "getCachedPageBySlug" src/lib/services-data.ts` confirma que `getServicesIndexPage` ya no arma su propia `payload.find`.
    - MEJOR ESFUERZO: `curl -o /dev/null -s -w '%{time_total}\n' http://localhost:3000/servicios` y `http://localhost:3000/services` (o su locale correcto) dos veces seguidas — el 2do debe ser más rápido. Diferir a producción si la DB local no conecta.
  </acceptance_criteria>
  <done>Servicios índice lee desde el mismo cache lib que Home, sin cambiar overrideAccess/depth existentes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Blog listing — cachear el shell + el grid de ArchiveBlock</name>
  <read_first>
    "src/app/(frontend)/[locale]/blog/page.tsx":15-24,57-58 (getBlogPage: payload.find sobre 'pages' slug='blog', llamado 2 veces por request — generateMetadata + body, sin overrideAccess)
    src/blocks/ArchiveBlock/Component.tsx:35-81 (la query real que arma el grid: payload.find({collection: relationTo, limit, sort:'-publishedAt', locale, where: categoryFilter...}) — sin cache, sin select, sin overrideAccess; relationTo es dinámico ('posts'|'case-studies'|'websites'))
    src/lib/cache.ts (getCachedArchive ya construido en 43-01 — firma: {relationTo, limit, locale, categoryId})
    src/components/PostCard.tsx, src/components/CaseStudyCard.tsx (ya tipados con PostCardData/CaseStudyCardData desde 43-01 — el grid de ArchiveBlock les pasa docs, deben seguir siendo compatibles)
  </read_first>
  <files>"src/app/(frontend)/[locale]/blog/page.tsx", src/blocks/ArchiveBlock/Component.tsx</files>
  <action>
    En `"src/app/(frontend)/[locale]/blog/page.tsx"`, reemplazar el cuerpo de `getBlogPage(locale)` por `return getCachedPageBySlug('blog', locale)` (import desde `@/lib/cache`, sin depth explícito — preserva el comportamiento actual sin depth).

    En `src/blocks/ArchiveBlock/Component.tsx`, en la rama `mode !== 'manual'` (la que arma `docs` vía `payload.find`), reemplazar la llamada directa por `await getCachedArchive({ relationTo, limit: limit ?? 3, locale, categoryId: categoryFilter })` (import desde `@/lib/cache`). El `categoriesResult` (fetch de la lista de categorías para los tabs de filtro) se queda SIN cachear — `categories` no está en la lista de colecciones que 43-CONTEXT.md autoriza tocar con hooks (Pages, Posts, CaseStudies, FeaturedContent, Redirects), y es una colección chica que cambia poco; no introduce el bug de HTML-size que sí tienen posts/case-studies. Si `tsc` rechaza pasar `collection: relationTo` (tipo unión) junto con un `select` correlacionado dentro de `getCachedArchive` (ya armado en 43-01 con 3 branches literales por esta razón), no reabras esa función — el problema, si aparece, está del lado del caller pasando un tipo demasiado ancho; ajusta el tipo de `relationTo` en la llamada, no la implementación de `getCachedArchive`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` limpio.
    - `grep -n "getCachedArchive" src/blocks/ArchiveBlock/Component.tsx` confirma 1 match en la rama no-manual.
    - `grep -n "getCachedPageBySlug" "src/app/(frontend)/[locale]/blog/page.tsx"` confirma que `getBlogPage` ya no arma su propia query.
    - MEJOR ESFUERZO: `curl -s http://localhost:3000/blog | wc -c` antes/después — el HTML de Blog listing debe bajar de tamaño (mismo mecanismo de select scoping que bajó el de Home en 43-01). `curl -s "http://localhost:3000/blog?category=<slug-real>"` vs sin `?category=` — ambas respuestas deben tener contenido DISTINTO y correcto (nunca cache cruzado entre filtros). Diferir a producción si la DB local no conecta.
  </acceptance_criteria>
  <done>Blog listing (shell + grid real de posts) sirve desde cache, con select scoping en el grid; el filtro por categoría sigue funcionando sin cruzarse entre variantes cacheadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Mismas boundaries que 43-01 (visitante anónimo → RSC/Route Handlers → Data Cache compartido). Esta plan solo agrega superficie de cache nueva sobre call sites ya cubiertos por las mitigaciones definidas en 43-01's `src/lib/cache.ts`/`cache-tags.ts` — no introduce mecanismos nuevos.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-05 | Information Disclosure | `getCachedArchive` aplicado a `relationTo='posts'|'case-studies'` en ArchiveBlock (Blog listing) | medium | mitigate | Reusa `getCachedPageBySlug`/`getCachedArchive` de 43-01, que ya fuerzan `overrideAccess:false` — un post/case-study en borrador nunca entra al grid cacheado. Verificado por grep en 43-01; esta plan no agrega ninguna query nueva sin ese guard. |
| T-43-06 | Information Disclosure (cache-key collision) | `getCachedArchive` keyParts (relationTo, limit, locale, categoryId) | medium | mitigate | El filtro de categoría (`categoryId`) y el `locale` son parte explícita de `keyParts` en `getCachedArchive` (armado en 43-01) — un visitante filtrando por categoría A nunca puede recibir la cache generada para categoría B o para "todas". Verificado por acceptance_criteria de Task 2 (curl con y sin `?category=`). |

</threat_model>

<verification>
- `npx tsc --noEmit` limpio tras ambas tareas.
- Servicios índice y Blog listing (shell + grid) usan los fetchers de `src/lib/cache.ts` sin duplicar lógica de query.
- Filtro por categoría verificado sin cruce de cache.
</verification>

<success_criteria>
Servicios índice y Blog listing sirven desde el mismo cache lib probado en Home, con select scoping aplicado al grid real de posts.
</success_criteria>

## Artifacts this phase produces

- `src/lib/services-data.ts` (modificado) — `getServicesIndexPage` delega a `getCachedPageBySlug`.
- `"src/app/(frontend)/[locale]/blog/page.tsx"` (modificado) — `getBlogPage` delega a `getCachedPageBySlug`.
- `src/blocks/ArchiveBlock/Component.tsx` (modificado) — grid en modo automático delega a `getCachedArchive`.

<output>
Create `.planning/phases/43-performance-response-time-html-size/43-02-SUMMARY.md` when done
</output>
