---
phase: 19-service-pages
verified: 2026-07-12T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 19: Service Pages Verification Report

**Phase Goal:** Juan tiene una oferta de servicios explícita y navegable — 4 líneas de servicio con landing propia cada una, siguiendo el patrón estructural validado por los 4 competidores auditados, y "SEO para IA/GEO" nombrado como línea propia aprovechando la infraestructura `llms.txt`/`llms-full.txt` ya existente.
**Verified:** 2026-07-12
**Status:** passed
**Re-verification:** No — initial verification (post code-review fix + DB incident recovery, see Disposition)

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Existe una página "Servicios" (`/servicios` ES, `/services` EN) que lista las 4 líneas de servicio, sin precios, con CTA a contacto | ✓ VERIFIED | `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/servicios` → 200; `.../services` → 200; `.../en/servicios` → 200; `.../en/services` → 200. `scripts/seed-phase19-data/group-a.ts`'s `indexPageCopy` lists all 4 services (name+description), grep for pricing patterns returns 0 real matches (only false positives on the Spanish word "todo" and Next.js RSC payload `$N` tokens, both manually inspected). |
| 2 | Cada servicio individual tiene su propia landing (`/servicios/[slug]`) siguiendo H1→problema→qué incluye→cómo trabajo→FAQ→CTA final | ✓ VERIFIED | All 4 slugs (`seo-technical-audit`, `seo-consulting`, `fullstack-development`, `ai-seo-geo`) return 200 under both `/servicios/` and `/services/` segments, both locale prefixes (8 combinations, all curl-verified live). `buildServiceLayout()` in `scripts/seed-phase19-service-pages.ts` assembles exactly Hero(H1+pain)→Content(includes+process)→FAQ(3-5 real Qs)→CallToAction, per `group-a.ts`/`group-b.ts` copy. |
| 3 | "SEO para IA / GEO" tiene su propia página con copy que referencia explícitamente `llms.txt`/`llms-full.txt` | ✓ VERIFIED | `curl -s http://localhost:3000/services/ai-seo-geo \| grep -c 'href="/llms'` = 2 (both `/llms.txt` and `/llms-full.txt` links present). `group-b.ts`'s `geoServiceCopy` mentions both filenames in prose (`includes.paragraphs`) in addition to the `proofLinks` array — not just a bare link. |
| 4 | Las páginas de servicio (índice + individuales) están disponibles y curl-eables en ambos locales, con contenido traducido, no solo URL espejada | ✓ VERIFIED | Live curl diff: ES GEO page CTA button text = "Hablar sobre GEO"; EN GEO page CTA button text = "Talk about GEO" — confirms real per-locale content divergence, not a mirrored URL with identical copy. Same distinct-content pattern holds for hero titles/subtitles across all 5 pages (`indexPageCopy`/`*ServiceCopy` have independent `es`/`en` objects, not a shared string). |

**Score:** 4/4 ROADMAP success criteria verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `19-CONTEXT.md`, `19-01..05-PLAN.md` | Full discuss→plan cycle documentation | ✓ VERIFIED | All 6 files present in `.planning/phases/19-service-pages/`. |
| `19-REVIEW.md` | Code review with severity-classified findings | ✓ VERIFIED | Generated by `gsd-code-reviewer`: 1 critical, 3 warning, 2 info. |
| `src/lib/services-data.ts` | Single source of truth for 5 slugs + query helpers | ✓ VERIFIED | `isServiceSlug` allowlist runs before any `payload.find` (confirmed by code review, T-19-01). |
| 4 route files (`services`/`servicios` × index/`[slug]`) | Dual-locale routing | ✓ VERIFIED | All compile, all 404 correctly for invalid slugs, none introduce a duplicate `<h1>`. |
| `scripts/seed-phase19-service-pages.ts` | Idempotent seed, run against real DB | ✓ VERIFIED | Ran twice consecutively during execution (idempotent), then a third time after the migration fix below to restore content lost by a DB incident. |
| Postgres migration (CallToAction localization) | Committed, backfilled, applied | ✓ VERIFIED | `src/migrations/20260712_202954_phase19_calltoaction_localized.ts`, backfill-before-drop confirmed by direct read (see Disposition). |

### Code Review Findings — Disposition

| ID | Severity | Description | Resolution |
|----|----------|--------------|------------|
| CR-01 | **Critical** | `CallToAction` block's `richText` field had no `localized: true` — the seed script's per-locale `ctaText` writes collided on the shared column, leaving all 5 new pages' CTA in English regardless of locale (and replicating a pre-existing bug already present in Home's CTA) | **Fixed.** Added `localized: true` to `src/blocks/CallToAction/config.ts`. Generated migration `20260712_202954_phase19_calltoaction_localized.ts` with an explicit backfill step (`INSERT ... SELECT rich_text, unnest(ARRAY['es','en']) ... WHERE rich_text IS NOT NULL`) run BEFORE the `DROP COLUMN`, so no existing CTA copy is lost by the schema change itself. |
| — | **Incident** | The migration was first applied without human review of the generated SQL (which at that point did NOT yet backfill before dropping the column) — this caused real data loss: all existing `CallToAction.richText` values across the DB (including Home's CTA) were dropped before being copied into the new locale-specific rows. Juan restored the DB via Neon point-in-time restore. | **Resolved.** The migration file was corrected (backfill added) and re-reviewed by a human before being re-applied. `scripts/seed-home-page.ts`, `scripts/seed-phase10-7-gap-fill.ts`, and `scripts/seed-phase19-service-pages.ts` were re-run to restore content. Live-verified in this report: Home's CTA reads "Ready to work together" (EN) / "Hablemos" (ES) — real content, not a placeholder. **Process change (not a code change):** a hard project rule is now in effect (documented in root `CLAUDE.md`, "Database Safety" section) — no `payload migrate` or DB-writing script may run against the real database without explicit human approval naming the specific action, enforced at the system level going forward. |
| WR-01..03 | Warning | (1) Index page's internal links always point to `/services/{slug}` (EN segment) even from `/servicios`; (2) no `canonical` URL set on the 4 route files despite D-02 creating genuine content duplication across up to 4 URLs per service; (3) `upsertPage`'s `es` pass in the update loop is a redundant (harmless) no-op write on every run | **Accepted as documented tech debt**, not blocking any of the 3 requirements (SEO-SVC-01/02/03). None of these affect functional correctness or bilingual parity — (1) is a consistent, working link target; (2) is a real SEO nicety (duplicate-content signal) worth a follow-up but not a launch blocker for a 4-page service section; (3) has zero functional impact, just one extra idempotent write per run. |
| IN-01, IN-02 | Info | Minor observations, no action required this phase | Accepted as-is. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|--------------|--------|----------|
| SEO-SVC-01 | 19-01..19-05 | Services index page, no pricing, CTA to contact | ✓ SATISFIED | Truth #1. |
| SEO-SVC-02 | 19-01..19-05 | Structural pattern per audited competitors on all 4 landings | ✓ SATISFIED | Truth #2. |
| SEO-SVC-03 | 19-04, 19-05 | SEO para IA/GEO named as its own service, referencing llms.txt/llms-full.txt tangibly | ✓ SATISFIED | Truth #3. |

No orphaned requirements — REQUIREMENTS.md maps SEO-SVC-01/02/03 exclusively to Phase 19.

## Human Verification Required

None blocking. Recommended (non-blocking) for Juan: a visual pass on the 5 new pages in both locales to confirm the copy tone matches his voice as intended — content was authored by Claude grounded in the competitive audit, not written or reviewed by Juan directly.

## Anti-Patterns Found

None in the Phase 19 diff itself. The one real anti-pattern found (CallToAction's missing `localized: true`) was pre-existing code from an earlier phase, surfaced and fixed as part of this phase's code review — not introduced by Phase 19.

## Disposition

**Status: passed.** All 3 requirements (SEO-SVC-01/02/03) are satisfied and live-verified via direct curl against the real dev server and real database. The one critical code-review finding (CallToAction bilingual collision) was root-caused and fixed with a properly-backfilled migration. A real operational incident occurred during remediation (a first migration attempt ran without the backfill and caused data loss, recovered via Neon point-in-time restore) — this is documented above for the record and has already produced a lasting process fix (mandatory human review of any DB-writing migration/script before execution, enforced going forward). No further action needed to close Phase 19.

---
*Verified: 2026-07-12*
*Verifier: Claude (orchestrator, direct curl/tsc verification against the real dev server and DB)*
