---
phase: 19-service-pages
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/app/(frontend)/[locale]/services/page.tsx
  - src/app/(frontend)/[locale]/servicios/page.tsx
  - src/app/(frontend)/[locale]/services/[slug]/page.tsx
  - src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
autonomous: true
requirements: [SEO-SVC-01, SEO-SVC-02, SEO-SVC-03]

must_haves:
  truths:
    - "Visiting /servicios or /services (either locale prefix) renders the services index page, listing all 4 service lines with no prices anywhere"
    - "Visiting /servicios/{slug} or /services/{slug} for any of the 4 real service slugs renders that service's landing page"
    - "Visiting /services/not-a-real-service or /servicios/not-a-real-service returns a real 404 (next/navigation notFound()), never a raw Payload error or a blank 200"
    - "Both /servicios and /services (and their /en/ prefixed counterparts) render successfully even though the [locale] param does not literally match the route segment's language — content is driven by the locale route param, not the segment name (D-02)"
  artifacts:
    - path: "src/app/(frontend)/[locale]/services/page.tsx"
      provides: "Services index page (EN URL segment), fetches via getServicesIndexPage, renders via RenderBlocks"
      contains: "getServicesIndexPage"
    - path: "src/app/(frontend)/[locale]/servicios/page.tsx"
      provides: "Services index page (ES URL segment) — same doc, same rendering logic as services/page.tsx"
      contains: "getServicesIndexPage"
    - path: "src/app/(frontend)/[locale]/services/[slug]/page.tsx"
      provides: "Individual service landing (EN URL segment), fetches via getServicePage, notFound() for invalid slugs"
      contains: "getServicePage"
    - path: "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"
      provides: "Individual service landing (ES URL segment) — same logic as services/[slug]/page.tsx"
      contains: "getServicePage"
  key_links:
    - from: "src/app/(frontend)/[locale]/services/page.tsx"
      to: "src/lib/services-data.ts"
      via: "import { getServicesIndexPage } from '@/lib/services-data'"
      pattern: "from '@/lib/services-data'"
    - from: "src/app/(frontend)/[locale]/services/[slug]/page.tsx"
      to: "src/lib/services-data.ts"
      via: "import { getServicePage } from '@/lib/services-data'"
      pattern: "getServicePage"
    - from: "src/app/(frontend)/[locale]/services/page.tsx"
      to: "src/blocks/RenderBlocks.tsx"
      via: "<RenderBlocks blocks={doc.content?.layout ?? []} />"
      pattern: "RenderBlocks"
---

<objective>
Wire the 4 thin route files that serve the 5 new `Pages` docs at their required dual-locale URLs
(SEO-SVC-01, SEO-SVC-02, SEO-SVC-03) — `/servicios` + `/servicios/[slug]` (ES segment) and
`/services` + `/services/[slug]` (EN segment), both functional under either `[locale]` value per
D-02, since next-intl has no translated-pathnames config in this project.

Purpose: make the service pages actually reachable and curl-able, in both locales, via both URL
segment spellings — the literal requirement text of SEO-SVC-01.

Output: 4 new route files, all thin wrappers around `src/lib/services-data.ts`'s query helpers and
the existing `RenderBlocks` renderer (same pattern already proven by `/contact` and `/privacy`).
No content exists yet at this point in the phase (19-03/19-04/19-05 create it) — these routes will
404 until 19-05 runs the seed script, which is expected and does not block this plan's own
verification (see `<verify>`, which checks compilation and the guard-clause behavior only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-service-pages/19-CONTEXT.md
@.planning/REQUIREMENTS.md

@src/lib/services-data.ts
@src/app/(frontend)/[locale]/contact/page.tsx
@src/app/(frontend)/[locale]/privacy/page.tsx
@src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
@src/blocks/RenderBlocks.tsx

<interfaces>
<!-- src/lib/services-data.ts (created by 19-01, already merged into the working tree before this
plan runs — depends_on: ["19-01"]):

export const SERVICES_INDEX_SLUG = 'services' as const
export const SERVICE_SLUGS = ['seo-technical-audit', 'seo-consulting', 'fullstack-development', 'ai-seo-geo'] as const
export type ServiceSlug = (typeof SERVICE_SLUGS)[number]
export function isServiceSlug(slug: string): slug is ServiceSlug
export async function getServicesIndexPage(locale: 'es' | 'en')
export async function getServicePage(locale: 'es' | 'en', slug: string)

Both query functions return `docs[0]` from a `payload.find` (i.e. `Page | undefined`), exactly the
same return shape as `case-studies/[slug]/page.tsx`'s `getCaseStudy`.

privacy/page.tsx is the closest existing analog for a fixed-slug static page (no [slug] param):

async function getPage(locale: string) {
  const payload = await getPayload({ config })
  const { docs } = await payload.find({ collection: 'pages', where: { slug: { equals: 'privacy' } }, locale: locale as 'es' | 'en', limit: 1 })
  return docs[0]
}
export async function generateMetadata({ params }) {
  const { locale } = await params
  const doc = await getPage(locale)
  const meta = doc?.meta
  return { title: meta?.title ?? doc?.title ?? fallback, description: meta?.description ?? '' }
}
export default async function PrivacyPage({ params }) {
  const { locale } = await params
  const doc = await getPage(locale)
  if (!doc) notFound()
  return (<main><Container className="py-16 max-w-3xl"><h1 ...>{doc.title}</h1><RenderBlocks blocks={doc.content?.layout ?? []} /></Container></main>)
}

IMPORTANT DEVIATION for services pages: do NOT add a manual `<h1>{doc.title}</h1>` like
privacy/page.tsx does — per 19-CONTEXT.md's <code_context>/"Established Patterns" note, every
service page's `content.layout` will include a `hero` block (Hero variant='listing') whose
`HeroComponent` already renders the real `<h1>` from `title` (src/blocks/Hero/Component.tsx:68).
Adding a second manual `<h1>` here would violate the one-real-H1-per-page discipline established in
Phase 18. Just wrap `<RenderBlocks>` directly in `<main>`, no `<Container>` wrapper needed either
(RenderBlocks' own blocks — Hero, Content, FAQ, CallToAction — already apply their own `Container`
internally, confirmed in src/blocks/Hero/Component.tsx, Content/Component.tsx, FAQ/Component.tsx).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index routes — /services and /servicios</name>
  <files>
    src/app/(frontend)/[locale]/services/page.tsx,
    src/app/(frontend)/[locale]/servicios/page.tsx
  </files>
  <read_first>
    src/lib/services-data.ts,
    src/app/(frontend)/[locale]/privacy/page.tsx,
    src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
  </read_first>
  <action>
    Create `src/app/(frontend)/[locale]/services/page.tsx` and
    `src/app/(frontend)/[locale]/servicios/page.tsx` as two files with byte-identical logic (thin
    enough that duplicating is simpler and less risky than a shared abstraction for 2 files — do
    not create a shared component for these two, follow the existing project convention of
    thin per-route files calling a shared lib, established by every other route pair in this
    codebase). Each file:
    - `async function getPage(locale: string) { return getServicesIndexPage(locale as 'es' | 'en') }`
      (or call `getServicesIndexPage` directly inline — either is fine, match whichever reads more
      naturally alongside `generateMetadata`'s duplicate call, mirroring `privacy/page.tsx`'s
      `getPage` pattern)
    - `export async function generateMetadata({ params }: { params: Promise<{ locale: string }> })`
      — resolve `{ locale }`, fetch the doc, return
      `{ title: doc?.meta?.title ?? doc?.title ?? (locale === 'es' ? 'Servicios' : 'Services'),
      description: doc?.meta?.description ?? '' }`
    - `export default async function ServicesIndexPage({ params }: { params: Promise<{ locale: string }> })`
      — resolve `{ locale }`, fetch the doc, `if (!doc) notFound()`, then
      `return (<main><RenderBlocks blocks={doc.content?.layout ?? []} /></main>)` — no manual
      `<h1>`, no `<Container>` wrapper (see interfaces note above: the Hero block inside
      `content.layout` provides both)
    Import `notFound` from `'next/navigation'`, `getServicesIndexPage` from `'@/lib/services-data'`,
    `RenderBlocks` from `'@/blocks/RenderBlocks'`. No `getPayload`/`@payload-config` imports needed
    in these route files — all Payload access goes through the lib helper.
  </action>
  <acceptance_criteria>
    - `grep -c "getServicesIndexPage" "src/app/(frontend)/[locale]/services/page.tsx"` returns >= 1
    - `grep -c "getServicesIndexPage" "src/app/(frontend)/[locale]/servicios/page.tsx"` returns >= 1
    - `grep -c "notFound" "src/app/(frontend)/[locale]/services/page.tsx" "src/app/(frontend)/[locale]/servicios/page.tsx"` returns >= 1 for each file
    - `grep -c "<h1" "src/app/(frontend)/[locale]/services/page.tsx" "src/app/(frontend)/[locale]/servicios/page.tsx"` returns 0 for each file (no manual H1 — Hero block owns it)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Both index route files compile, fetch the services index `Pages` doc via the shared lib helper,
    404 cleanly when the doc does not yet exist (expected until 19-05 seeds it), and render via
    `RenderBlocks` with no manually-added H1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Individual service routes — /services/[slug] and /servicios/[slug]</name>
  <files>
    src/app/(frontend)/[locale]/services/[slug]/page.tsx,
    src/app/(frontend)/[locale]/servicios/[slug]/page.tsx
  </files>
  <read_first>
    src/lib/services-data.ts,
    src/app/(frontend)/[locale]/case-studies/[slug]/page.tsx
  </read_first>
  <action>
    Create `src/app/(frontend)/[locale]/services/[slug]/page.tsx` and
    `src/app/(frontend)/[locale]/servicios/[slug]/page.tsx`, same byte-identical-logic convention as
    Task 1. Each file:
    - `async function getPage(locale: string, slug: string) { return getServicePage(locale as 'es' | 'en', slug) }`
    - `export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> })`
      — resolve `{ locale, slug }`, fetch via `getPage`, `if (!doc) return {}` (matches
      `case-studies/[slug]/page.tsx`'s exact early-return-empty-object pattern for missing docs),
      otherwise return `{ title: doc.meta?.title ?? doc.title, description: doc.meta?.description
      ?? '' }`
    - `export default async function ServicePage({ params }: { params: Promise<{ locale: string; slug: string }> })`
      — resolve `{ locale, slug }`, fetch via `getPage`, `if (!doc) notFound()`, then
      `return (<main><RenderBlocks blocks={doc.content?.layout ?? []} /></main>)` — same no-manual-H1
      rule as Task 1 (every individual service page's seeded content includes a Hero block, per
      D-03)
    `getServicePage` itself (from `src/lib/services-data.ts`, created in 19-01) already guards
    against non-`SERVICE_SLUGS` values via `isServiceSlug` and returns `undefined` for anything
    else — so these route files do not need to re-implement that allowlist check, they only need to
    call `notFound()` when `getPage` returns `undefined`, which correctly covers both "slug not in
    the allowlist" and "slug is valid but the doc row doesn't exist yet" cases with one guard clause.
  </action>
  <acceptance_criteria>
    - `grep -c "getServicePage" "src/app/(frontend)/[locale]/services/[slug]/page.tsx"` returns >= 1
    - `grep -c "getServicePage" "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"` returns >= 1
    - `grep -c "notFound()" "src/app/(frontend)/[locale]/services/[slug]/page.tsx" "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"` returns >= 1 for each file
    - `grep -c "<h1" "src/app/(frontend)/[locale]/services/[slug]/page.tsx" "src/app/(frontend)/[locale]/servicios/[slug]/page.tsx"` returns 0 for each file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Both `[slug]` route files compile, delegate slug validation entirely to `getServicePage`'s
    internal `isServiceSlug` allowlist guard, call `notFound()` for any invalid or not-yet-seeded
    slug, and render seeded content via `RenderBlocks` with no manually-added H1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `params.slug` (URL) → `getServicePage` → `payload.find` | Same boundary already mitigated in 19-01 (T-19-01) at the lib layer — this plan's routes are the actual callers exercising that guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Denial of Service | `/services/[slug]`, `/servicios/[slug]` route handlers | accept | No rate limiting added — matches every other public dynamic route in this codebase (`/case-studies/[slug]`, `/blog/[slug]`, `/authors/[slug]`), all of which rely on the hosting layer (Hostinger/Cloudflare, per root CLAUDE.md) for DoS protection, not per-route application logic; introducing route-specific rate limiting here would be inconsistent with the rest of the site and out of this phase's scope |
| T-19-04 | Tampering | `<RenderBlocks blocks={doc.content?.layout}>` rendering admin-authored blocks | accept | Identical rendering path already used by every other `pages`-collection route (`/contact`, `/privacy`, `/terms`) — content is admin-authenticated-write / public-read, React's default JSX escaping applies, no new XSS surface versus already-shipped pages |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan |

No new trust boundary is introduced beyond what 19-01 already mitigated (T-19-01) — this plan is
the consumer of that guard, not a new input surface.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 after both tasks
2. `npm run build` succeeds (routes compile into the Next.js route manifest even with no seeded content yet — dynamic rendering, not static generation, since no `generateStaticParams` is defined, matching `case-studies/[slug]/page.tsx`'s convention)
3. Manual dev-server check (informational only, not blocking since content doesn't exist until 19-05): `curl -s http://localhost:3000/services/not-a-real-slug -o /dev/null -w '%{http_code}'` returns `404`
</verification>

<success_criteria>
- 4 new route files exist, all compiling cleanly
- Individual service routes 404 for any slug outside `SERVICE_SLUGS`, delegating that check to `src/lib/services-data.ts`
- No route file introduces a second `<h1>` alongside the Hero block's own heading
- `npx tsc --noEmit` and `npm run build` both exit 0
</success_criteria>

<output>
Create `.planning/phases/19-service-pages/19-02-SUMMARY.md` when done
</output>
